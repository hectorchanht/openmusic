---
phase: 39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she
plan: 10
type: execute
wave: 4
depends_on: ["39-05", "39-06", "39-07"]
files_modified:
  - src/routes/(app)/settings/home/+page.svelte
autonomous: true
requirements: [P39-10, P39-05, P39-11]
must_haves:
  truths:
    - "/settings/home shows, top to bottom: PageHeader (unchanged), Sections & order (ONE global drag list of the 13 non-classic rows with .rtext label + .rsub source line and a role=switch visibility toggle), Charts (Chart region single-select accordion, More regions multi-select accordion with chipReorder, Genres chips), a collapsed Classic (Last.fm / Deezer) accordion holding the four classic rows (no grip) plus the old tag and country chip pickers, then the five existing global controls unchanged"
    - "Dragging a listed row writes homeSectionOrder through reorderListed so classic ids keep their array slot; toggling a switch / chip persists immediately via settings.save()"
    - "Chart region shows 'Auto (<resolved region>)' when saved is 'auto', lists the 27 regions in the fixed research order via regionLabel; More regions .cur is an Intl.ListFormat join or 'None'; the Classic .cur is 'Off' or '{n} on'; New releases' .rsub reads 'Not available in {region}' outside hk/tw/sg"
    - "Every chip carries aria-pressed; the region chip group has role=group + aria-label; .sw carries role=switch + aria-checked"
  artifacts:
    - path: "src/routes/(app)/settings/home/+page.svelte"
      provides: "the redesigned Home layout settings page per UI-SPEC §2"
      contains: "reorderListed("
  key_links:
    - from: "src/routes/(app)/settings/home/+page.svelte onReorder"
      to: "src/lib/services/home-layout.ts reorderListed"
      via: "import — classic ids keep their exact index"
      pattern: "reorderListed\\("
    - from: "src/routes/(app)/settings/home/+page.svelte"
      to: "src/lib/services/home-charts.ts regionLabel / regionListLabel / CHART_GENRE_LABEL"
      via: "Intl-based labels + typed genre key map"
      pattern: "regionListLabel\\("
    - from: "src/routes/(app)/settings/home/+page.svelte"
      to: "settings.homeChartRegion / homeExtraRegions / homeChartGenres"
      via: "tap → assign → settings.save()"
      pattern: "settings\\.homeChartRegion = "
---

<objective>
Redesign /settings/home per the approved UI-SPEC §2: one global sections list with source lines, a Charts group (Chart region, More regions, Genres), and a de-emphasised Classic accordion that keeps the four old shelves and their tag/country chips fully operable.

Purpose: the settings page is where the region, the extra regions and the genre selection are chosen, where the classic shelves live on, and where the migration's result is explained by structure (UI-17: no toast). Everything reuses in-repo idioms — `.rrow`, `.chips` + `chipReorder`, the translation page's `.advanced` accordion, `SettingHint`, `dragReorder`.
Output: rewritten `settings/home/+page.svelte`.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-PATTERNS.md
@src/routes/(app)/settings/home/+page.svelte
@src/routes/(app)/settings/translation/+page.svelte
@src/routes/(app)/settings/playback/+page.svelte
@src/lib/services/home-layout.ts
@src/lib/services/home-charts.ts
@src/lib/stores/settings.svelte.ts

<interfaces>
```typescript
// $lib/services/home-layout (39-02): HOME_SECTIONS, CLASSIC_SECTIONS, CHART_REGIONS, KKBOX_REGIONS, CHART_GENRE_IDS, type ChartGenre, type ChartRegion, resolveSectionOrder, resolveChartRegion(saved, appLang, navLang?), resolveExtraRegions(saved, main), resolveChartGenres(saved), reorderListed(order, from, to)
// $lib/services/home-charts (39-05): regionLabel(cc, lang), regionListLabel(ccs, lang), CHART_GENRE_LABEL: Record<ChartGenre, TranslationKey>
// $lib/stores/settings.svelte (39-06): settings.homeChartRegion ('auto' | ChartRegion), settings.homeExtraRegions: string[], settings.homeChartGenres: string[], settings.appLang, settings.save()
// existing page-local: sectionLabel (17 entries after 39-02), toggleHidden(id), sectionDensity/setSectionDensity, toggleTag/toggleCountry, selectedTags/unselectedTags, reorderList, chipReorder, dragReorder, SettingHint, SettingPicker
// icons to add from @lucide/svelte: TrendingUp, MapPin, Archive, ChevronDown (Globe already imported)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script — listed rows, reorderListed, region / extra-region / genre handlers, source lines and labels</name>
  <read_first>
    - src/routes/(app)/settings/home/+page.svelte lines 1-178 (whole script: `sectionLabel`, `order`, `onReorder`, `toggleHidden`, density handlers, tag/country handlers, `reorderList`, pickers, `tagsShowingAll`)
    - src/lib/services/home-layout.ts (39-02 exports) and src/lib/services/home-charts.ts (39-05 exports)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §2.2 (rows listed, `.rsub` table, drag semantics), §2.3 (a/b/c: `.cur` rules, chip order, defaults), §2.4 (Classic `.cur`), §2.6 (label localization)
  </read_first>
  <files>src/routes/(app)/settings/home/+page.svelte</files>
  <action>
    In the `<script>` of `src/routes/(app)/settings/home/+page.svelte` (39-D-42):
    1. Imports: add `CLASSIC_SECTIONS, CHART_REGIONS, KKBOX_REGIONS, CHART_GENRE_IDS, type ChartGenre, type ChartRegion, resolveChartRegion, resolveExtraRegions, resolveChartGenres, reorderListed` from `$lib/services/home-layout`; `regionLabel, regionListLabel, CHART_GENRE_LABEL` from `$lib/services/home-charts`; icons `TrendingUp, MapPin, Archive, ChevronDown`; drop the `Tags` import if it becomes unused.
    2. Derived: `listed = order.filter(id => !CLASSIC_SECTIONS.includes(id))` (the 13 rows), `classicOn = CLASSIC_SECTIONS.filter(id => !settings.homeHidden.includes(id)).length`, `chartRegion = resolveChartRegion(settings.homeChartRegion, settings.appLang, navigator.language)` (page is client-only; guard with `typeof navigator !== 'undefined'`), `isKkbox = KKBOX_REGIONS.includes(chartRegion)`, `selectedRegions = resolveExtraRegions(settings.homeExtraRegions, chartRegion)`, `unselectedRegions = CHART_REGIONS.filter(cc => cc !== chartRegion && !selectedRegions.includes(cc))`, `selectedGenres = resolveChartGenres(settings.homeChartGenres)`, `unselectedGenres = CHART_GENRE_IDS.filter(g => !selectedGenres.includes(g))`.
    3. `onReorder(from, to)` becomes `settings.homeSectionOrder = reorderListed(order, from, to); settings.save();` (indices are into `listed` — UI-SPEC §2.2: classic ids keep their exact array index).
    4. Handlers: `setRegion(v: 'auto' | ChartRegion)` → `settings.homeChartRegion = v; settings.save()`; `toggleRegion(cc)` / `onReorderRegion(from, to)` mirroring `toggleCountry` / `onReorderCountry` over `settings.homeExtraRegions` (reorder uses `reorderList(selectedRegions, from, to)`); `toggleGenre(g)` / `onReorderGenre(from, to)` likewise over `settings.homeChartGenres` (an empty result is a real choice — no fallback).
    5. Labels: `regionName = (cc) => regionLabel(cc, settings.appLang)`; `regionCur = settings.homeChartRegion === 'auto' ? t('settings.chartRegionAuto', { region: regionName(chartRegion) }) : regionName(settings.homeChartRegion)`; `moreRegionsCur = selectedRegions.length ? regionListLabel(selectedRegions, settings.appLang) : t('settings.moreRegionsNone')`; `classicCur = classicOn === 0 ? t('settings.optOff') : t('settings.homeClassicOnCount', { n: classicOn })`; `genreName = (g: ChartGenre) => t(CHART_GENRE_LABEL[g])` (typed map, no template-string key cast).
    6. `sourceLine(id: HomeSectionId): string` — brand names are LITERAL strings (proper nouns, never translated): `chart-songs` → `isKkbox ? 'KKBOX · Apple Music' : 'Apple Music'`; `new-releases` → `isKkbox ? 'KKBOX' : t('settings.homeSrcUnavailable', { region: regionName(chartRegion) })`; `chart-artists` / `yt-trending` → `'YouTube'`; `chart-albums` → `'Apple Music'`; `genres` → `'Apple Music · Deezer'`; `regions` → `'Apple Music'`; `top-hits` / `top-artists` → `'Deezer'`; `tags` / `countries` → `'Last.fm'`; every library id → `t('settings.homeSrcLibrary')`.
    Keep `toggleHidden`, `sectionDensity`, `setSectionDensity`, tag/country handlers, the range/landing/density/chrome handlers and the preview snippets exactly as they are.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reorderListed(order, from, to)" "src/routes/(app)/settings/home/+page.svelte"` matches and the old 3-line splice in `onReorder` is gone
    - `grep -n "function sourceLine\|const sourceLine" "src/routes/(app)/settings/home/+page.svelte"` matches and `grep -c "'KKBOX · Apple Music'\|'Apple Music · Deezer'\|'Last.fm'\|'Deezer'\|'YouTube'" "src/routes/(app)/settings/home/+page.svelte"` ≥ 5
    - `grep -n "CHART_GENRE_LABEL\[" "src/routes/(app)/settings/home/+page.svelte"` matches and `grep -c "as TranslationKey" "src/routes/(app)/settings/home/+page.svelte"` prints 0
    - `grep -n "settings.homeChartRegion = \|settings.homeExtraRegions = \|settings.homeChartGenres = " "src/routes/(app)/settings/home/+page.svelte"` prints ≥ 3 lines, each followed within 2 lines by `settings.save()`
    - `pnpm check` exits 0 (the template may temporarily not use every new symbol; unused-variable warnings are acceptable until Task 2)
  </acceptance_criteria>
  <done>All state, handlers and label/source helpers for the redesigned page exist and type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Template + CSS — sections list, Charts group, Classic accordion, accessibility</name>
  <read_first>
    - src/routes/(app)/settings/home/+page.svelte lines 263-330 (the three sections being restructured) and 412-460 (`<style>`: `.rrow`, `.rlabel`, `.chips`, `.chip`, `.sw`, `.sub`)
    - src/routes/(app)/settings/translation/+page.svelte lines 281-294 (accordion markup: `<details class="advanced"><summary>` icon · label · `SettingHint` · `.cur` · `.chev`) and lines 350-373 (`.advanced`, `.advanced summary`, `.advanced .chips`, `.cur`, `.advanced .chev`, `[open] .chev`, reduced-motion, `.advanced .chip`, `.advanced .chip.on` CSS — copy verbatim)
    - src/routes/(app)/settings/playback/+page.svelte line 322 (the uppercase `.advanced summary` variant for the Classic summary)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §2.1 (page outline), §2.2 (row anatomy `.rtext`/`.rlabel`/`.rsub`), §2.3 (a/b/c markup), §2.4 (Classic body order), §2.5 (unchanged controls), §4 (aria), §Typography (`.rsub` = 0.75rem/400/1.3 muted, one line ellipsis), §Spacing, §Color (`.advanced .rrow { background: var(--color-bg) }`)
  </read_first>
  <files>src/routes/(app)/settings/home/+page.svelte</files>
  <action>
    Template (39-D-43), in this order after `PageHeader` and the preview snippets:
    1. SECTIONS & ORDER: keep `<section>` + `<h2><LayoutGrid …/> {t('settings.homeSections')}</h2>` + `<ul class="reorder" use:dragReorder={{ onReorder }}>` but iterate `listed` (13 rows). Row: grip (unchanged) → `<span class="rtext"><span class="rlabel">{t(sectionLabel[id])}</span><span class="rsub">{sourceLine(id)}</span></span>` → `.density-seg` (unchanged) → `<button class="sw" class:on={…} role="switch" aria-checked={!settings.homeHidden.includes(id)} aria-label={t(sectionLabel[id])} onclick={() => toggleHidden(id)}></button>`. Help line `settings.dragToReorder` unchanged. An unavailable New releases row stays fully toggleable, no dimming.
    2. CHARTS: `<section><h2><TrendingUp size={15} /> {t('settings.homeGroupCharts')}</h2>` containing: (a) `<details class="advanced">` — summary `<MapPin size={15} />` · `{t('settings.chartRegion')}` · `<SettingHint label={t('settings.chartRegion')} text={t('settings.chartRegionDesc')} />` · `<span class="cur">{regionCur}</span>` · `<span class="chev" aria-hidden="true"><ChevronDown size={15} /></span>`; body `<div class="chips" role="group" aria-label={t('settings.chartRegion')}>` with the Auto chip first (`class:on={settings.homeChartRegion === 'auto'}`, `aria-pressed`, label `t('settings.chartRegionAuto', { region: regionName(chartRegion) })`, `onclick={() => setRegion('auto')}`, `use:tapBounce`) then one chip per `CHART_REGIONS` in the fixed order (`class:on={settings.homeChartRegion === cc}`, `aria-pressed`, `regionName(cc)`, `setRegion(cc)`); collapsed by default, open state not persisted. (b) `<details class="advanced">` — summary `<Globe size={15} />` · `{t('settings.moreRegions')}` · `SettingHint` (`settings.moreRegionsDesc`) · `.cur` = `moreRegionsCur` · `.chev`; body = the countries-chip idiom: `<div class="chips" use:chipReorder={{ onReorder: onReorderRegion }}>` with `selectedRegions` first (`class="chip on" data-chip-index={i} aria-pressed="true"`), then `unselectedRegions` (`aria-pressed="false"`), labels via `regionName`; below `<p class="muted">{t('settings.homeDragReorderChips')}</p>`. (c) `<h3 class="sub">{t('settings.homeGenres')}<SettingHint label={t('settings.homeGenres')} text={t('settings.chartGenresDesc')} /></h3>` + `<div class="chips" use:chipReorder={{ onReorder: onReorderGenre }}>` selected genres first (draggable, `aria-pressed`), then unselected in `CHART_GENRE_IDS` order, labels `genreName(g)`; muted line `selectedGenres.length ? t('settings.homeDragReorderChips') : t('settings.chartGenresNone')`.
    3. CLASSIC: `<details class="advanced classic">` (no `open`) — summary in the Playback uppercase type: `<Archive size={15} />` · `{t('settings.homeGroupClassic')}` · `<SettingHint label={t('settings.homeGroupClassic')} text={t('settings.homeClassicDesc')} />` · `<span class="cur">{classicCur}</span>` · `.chev`. Body: `<ul class="classic-rows">` with the four classic ids in the fixed order top-hits, top-artists, tags, countries — the `.rrow` WITHOUT the grip (`.rtext` block with `sourceLine(id)` → `.density-seg` → `.sw` with role/aria as above; NOT draggable); then `<h3 class="sub">{t('settings.homeSectionTags')}</h3>` + the existing tag chips block (with `aria-pressed` added to every chip) + its existing `tagsShowingAll` hint; then `<h3 class="sub">{t('settings.homeSectionCountries')}</h3>` + the existing country chips block + hint. Delete the old top-level Genres (`Tags` icon) and Countries (`Globe`) sections.
    4. Sections 4-7 (Items per shelf, Home grid columns, Default landing tab, Tile density, Home controls): byte-for-byte unchanged.
    CSS: add `.rtext { flex: 1; min-width: 0; display: flex; flex-direction: column; }`, `.rsub { font-size: 0.75rem; line-height: 1.3; color: var(--color-text-muted); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }` (`.rlabel` loses `flex: 1` since `.rtext` owns it; it may wrap to two lines); copy the translation page's `.advanced`, `.advanced summary` (row type, 0.875rem), `.advanced .chips`, `.cur`, `.advanced .chev`, `.advanced[open] .chev`, the reduced-motion rule, `.advanced .chip`, `.advanced .chip.on` verbatim; add `.advanced.classic summary { font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.5px; color: var(--color-text-muted); }` (the Playback variant) and `.advanced .rrow { background: var(--color-bg); }`; `.classic-rows { list-style: none; margin: 12px 0 0; padding: 0; display: flex; flex-direction: column; gap: 8px; }`. No new font sizes beyond the four in UI-SPEC §Typography; no new colours; accent only on `.chip.on` / `.dseg-btn.on` / `.sw.on`.
  </action>
  <verify>
    <automated>pnpm check && pnpm vitest --run src/lib/i18n/i18n.test.ts src/lib/services/home-layout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<details class="advanced' "src/routes/(app)/settings/home/+page.svelte"` prints 3 and `grep -c 'class="advanced classic"' "src/routes/(app)/settings/home/+page.svelte"` prints 1
    - `grep -c 'role="switch"' "src/routes/(app)/settings/home/+page.svelte"` ≥ 2 (listed rows + classic rows) and `grep -c "aria-checked" "src/routes/(app)/settings/home/+page.svelte"` ≥ 2
    - `grep -c "aria-pressed" "src/routes/(app)/settings/home/+page.svelte"` ≥ 8 (region auto/list, more-regions selected/unselected, genres selected/unselected, tags, countries) and `grep -c 'role="group" aria-label={t(.settings.chartRegion.)}' "src/routes/(app)/settings/home/+page.svelte"` prints 1
    - `grep -c "{#each listed as" "src/routes/(app)/settings/home/+page.svelte"` prints 1 and `grep -c "{#each order as" "src/routes/(app)/settings/home/+page.svelte"` prints 0
    - `grep -c 'class="rsub"' "src/routes/(app)/settings/home/+page.svelte"` ≥ 2 and `grep -n "\.rsub {" "src/routes/(app)/settings/home/+page.svelte"` matches
    - `grep -c "<Tags " "src/routes/(app)/settings/home/+page.svelte"` prints 0 (old top-level Genres heading removed) and `grep -c "settings.homeCountriesLabel" "src/routes/(app)/settings/home/+page.svelte"` prints 0
    - `grep -c "SettingPicker" "src/routes/(app)/settings/home/+page.svelte"` is unchanged from before this plan (global controls untouched)
    - `pnpm check` exits 0 with no unused-selector or unused-variable warnings from this file
    - Dev smoke (browser pane): the page shows 13 listed rows with source lines; Chart region summary reads "Auto (Hong Kong)" for a zh-Hant app language; picking `tw` updates the Home titles to "· Taiwan" after navigating back; the Classic summary reads "Off"; turning on Top hits makes the Classic summary "1 on" and the Home shows the Deezer Top hits shelf with its See-all chevron again
  </acceptance_criteria>
  <done>The page matches UI-SPEC §2.1-2.6 and §4; every control persists immediately; classic shelves remain reachable and restorable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user taps → settings store → localStorage | user-controlled but typed values from closed lists |
| settings values → Home fetch plan | resolved through 39-02 resolvers on both pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-39 | Tampering | region / genre chip writes | mitigate | chips are rendered ONLY from `CHART_REGIONS` / `CHART_GENRE_IDS`, so a tap can only write an allowlisted id; the resolvers re-validate on read |
| T-39-40 | Tampering (order) | drag reorder | mitigate | `reorderListed` bounds-checks indices and preserves classic slots; `resolveSectionOrder` remains the render robustness layer |
| T-39-41 | Information disclosure | brand/source lines | accept | static literals, no user data |
| T-39-42 | Elevation (a11y regression) | switches/chips without state | mitigate | `role=switch` + `aria-checked`, `aria-pressed` on every chip, `role=group` on the region set (UI-SPEC §4) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase |
</threat_model>

<verification>
- `pnpm check` green; `pnpm test` green after the wave.
- VALIDATION manual row "settings/home shows Charts / Library / Classic groups; un-hiding a classic section makes it fetch + render" executed via the dev smoke in Task 2.
</verification>

<success_criteria>
- Page outline, row anatomy, accordions, chips, defaults and accessibility match UI-SPEC §2 and §4 exactly; global controls unchanged.
</success_criteria>

<output>
Create `.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-10-SUMMARY.md` when done.
</output>
