---
phase: 39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she
plan: 09
type: execute
wave: 4
depends_on: ["39-02", "39-05", "39-07"]
files_modified:
  - src/lib/stores/settings.svelte.ts
  - src/lib/config/defaults.ts
  - src/lib/stores/settings-persist.svelte.test.ts
  - src/routes/(app)/settings/data/+page.svelte
autonomous: true
requirements: [P39-06, P39-07, P39-12]
must_haves:
  truths:
    - "An existing user's first load after the update runs migrateHomeLayout ONCE: the seven chart ids are inserted where the old chart block sat, the four classic ids are added to homeHidden, per-section density carries old→new, library sections / relative order / shelf size / landing tab / chrome toggles are untouched, and homeLayoutVersion=2 is persisted in the same load"
    - "Un-hiding a classic section after the migration survives a reload (the version gate prevents a re-run); a blob already at version 2 is untouched"
    - "A fresh install gets HOME_DEFAULTS with the classic ids pre-hidden and never runs the migration; resetHome() yields the new layout"
    - "The /settings/data 'Clear picks' button removes the CURRENT home cache key (v3) plus the orphaned v1/v2 keys and the library shelf cache"
  artifacts:
    - path: "src/lib/config/defaults.ts"
      provides: "homeHidden = [...CLASSIC_SECTIONS], homeLayoutVersion = HOME_LAYOUT_VERSION"
      contains: "homeLayoutVersion"
    - path: "src/lib/stores/settings.svelte.ts"
      provides: "homeLayoutVersion field, version-gated migration inside load(), post-load save, save payload, resetHome"
      contains: "migrateHomeLayout("
    - path: "src/lib/stores/settings-persist.svelte.test.ts"
      provides: "migration round-trip cases (VALIDATION P39-06)"
      min_lines: 320
    - path: "src/routes/(app)/settings/data/+page.svelte"
      provides: "clearPicks over HOME_CACHE_KEY + LEGACY_HOME_CACHE_KEYS"
      contains: "LEGACY_HOME_CACHE_KEYS"
  key_links:
    - from: "src/lib/stores/settings.svelte.ts load()"
      to: "src/lib/services/home-layout.ts migrateHomeLayout / HOME_LAYOUT_VERSION"
      via: "version gate + `if (migrated) this.save()` after the try block"
      pattern: "if \\(migrated\\) this\\.save\\(\\)"
    - from: "src/routes/(app)/settings/data/+page.svelte clearPicks"
      to: "src/lib/services/home-charts.ts HOME_CACHE_KEY"
      via: "import — one constant shared with the home page"
      pattern: "import \\{ HOME_CACHE_KEY, LEGACY_HOME_CACHE_KEYS \\} from '\\$lib/services/home-charts'"
---

<objective>
Land the one-time layout switch for existing users and the new-layout defaults for fresh installs — now that the new shelves render (39-07) — and fix the Clear-picks button that has been a no-op since the home cache moved to v2.

Purpose: CONTEXT locks a silent, idempotent, version-gated migration inside `settings.load()` that must persist its own version marker (RESEARCH Pitfall 5: without the save, a re-enabled classic shelf is re-hidden on every reload). This plan is sequenced AFTER the home renders so no deploy ever hides the classic shelves before their replacements exist.
Output: `defaults.ts`, `settings.svelte.ts`, migration tests, `settings/data/+page.svelte`.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-PATTERNS.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-06-SUMMARY.md
@src/lib/config/defaults.ts
@src/lib/stores/settings.svelte.ts
@src/lib/stores/settings-persist.svelte.test.ts
@src/lib/services/home-layout.ts
@src/routes/(app)/settings/data/+page.svelte

<interfaces>
```typescript
// $lib/services/home-layout (39-02)
export const HOME_LAYOUT_VERSION = 2; export const CLASSIC_SECTIONS;
export function migrateHomeLayout(order: string[], hidden: string[], density?: Partial<Record<HomeSectionId, HomeDensity>>): { order: string[]; hidden: string[]; density: Partial<Record<HomeSectionId, HomeDensity>> };
// $lib/services/home-charts (39-05)
export const HOME_CACHE_KEY = 'openmusic:top-picks:v3'; export const LEGACY_HOME_CACHE_KEYS: readonly string[];
// Added here:
//   HOME_DEFAULTS.homeHidden: [...CLASSIC_SECTIONS] as string[]; HOME_DEFAULTS.homeLayoutVersion: HOME_LAYOUT_VERSION
//   class Settings { homeLayoutVersion: number = HOME_DEFAULTS.homeLayoutVersion } // PLAIN field, not $state (UI never reads it)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the version-gated migration into settings.load(), flip the defaults, persist the version</name>
  <read_first>
    - src/lib/stores/settings.svelte.ts: lines 291-297 (the existing "a one-shot migration needs its own persisted version marker" record — cite it), the home coercion block through `homeSectionDensity` (39-06 result), the `} catch {` / `this.applyTheme()` tail of `load()`, the `save()` payload, `resetHome()`
    - src/lib/config/defaults.ts `HOME_DEFAULTS` (39-06 result) and its home-layout import block
    - src/lib/services/home-layout.ts `migrateHomeLayout`, `HOME_LAYOUT_VERSION`, `CLASSIC_SECTIONS` (39-02)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Pattern 5 and §Common Pitfalls 5
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §3 (what each user sees; UI-13 density carry-over; UI-17 silent — no toast/banner)
    - CLAUDE.md §Runes Usage ("internal non-reactive counters/guards use PLAIN class fields, NOT $state")
  </read_first>
  <files>src/lib/config/defaults.ts, src/lib/stores/settings.svelte.ts</files>
  <behavior>
    - Old blob { homeSectionOrder: [the old 10 ids], homeHidden: [], homeSectionDensity: {'top-hits':'pile'} } and no homeLayoutVersion → after load(): homeSectionOrder === ['liked','downloads','radio', ...CHART_SECTIONS, 'top-hits','top-artists','fav-artists','tags','countries','playlists','history']; homeHidden deep-equals [...CLASSIC_SECTIONS]; homeSectionDensity deep-equals {'top-hits':'pile','chart-songs':'pile'}; the persisted blob now has homeLayoutVersion 2 (load() saved)
    - Old blob with homeHidden ['liked'] and a custom order → 'liked' stays hidden; library ids keep their relative order; homeShelfSize / homeLandingTab / homeShowSearchPill unchanged
    - Un-hide after migration survives reload: load → set homeHidden to exclude 'top-hits' → save → freshSettings().load() → 'top-hits' is NOT in homeHidden and the order is unchanged (no second migration)
    - Blob with homeLayoutVersion 2 and homeHidden [] → load() leaves homeHidden [] and does NOT write to localStorage (no save on load)
    - Fresh install (no blob): homeHidden deep-equals [...CLASSIC_SECTIONS]; homeSectionOrder deep-equals HOME_DEFAULTS.homeSectionOrder; localStorage still has NO settings blob after load() (first-visit path does not save)
    - resetHome() after any state → homeHidden deep-equals [...CLASSIC_SECTIONS], order = defaults, homeLayoutVersion 2 persisted
    - save() always writes homeLayoutVersion
  </behavior>
  <action>
    `src/lib/config/defaults.ts` (39-D-39): import `CLASSIC_SECTIONS` and `HOME_LAYOUT_VERSION` from `$lib/services/home-layout`; change `homeHidden: [] as string[]` to `homeHidden: [...CLASSIC_SECTIONS] as string[]` with a comment (CONTEXT: the four Deezer/Last.fm shelves stay available but hidden by default; a fresh install and Reset-to-default get the new layout from here; existing blobs get it from the migration because their `homeHidden: []` is a real persisted value the type guard keeps); add `homeLayoutVersion: HOME_LAYOUT_VERSION`. Remove the 39-06 "do not add yet" comment.

    `src/lib/stores/settings.svelte.ts` (39-D-40): import `migrateHomeLayout`, `HOME_LAYOUT_VERSION` from `$lib/services/home-layout`. Declare a PLAIN class field `homeLayoutVersion: number = HOME_DEFAULTS.homeLayoutVersion` next to the home `$state` fields (comment: not `$state` — the UI never reads it; it only gates the migration and is persisted). In `load()`: declare `let migrated = false` before the `try`; inside the `if (raw)` branch, immediately after the `homeSectionDensity` coercion, add the version gate: `const ver = typeof v.homeLayoutVersion === 'number' ? v.homeLayoutVersion : 1;` (absent = 1 — every pre-phase blob); `if (ver < HOME_LAYOUT_VERSION) { const m = migrateHomeLayout(this.homeSectionOrder, this.homeHidden, this.homeSectionDensity); this.homeSectionOrder = m.order; this.homeHidden = m.hidden; this.homeSectionDensity = m.density; migrated = true; }` then `this.homeLayoutVersion = HOME_LAYOUT_VERSION;`. Comment block: cite lines 291-297's record verbatim in spirit ("a one-shot migration would also need its own persisted version marker — without one it re-applies on every load"), state the UI-SPEC §3 guarantees (silent, once, preserves library sections/order/density/size/landing/chrome), and RESEARCH Pitfall 5. After the `try/catch` and BEFORE `this.applyTheme()`: `if (migrated) this.save();` (comment: the save is REQUIRED — without a persisted version a user who re-enables top-hits would have it re-hidden on every load). Append `homeLayoutVersion: this.homeLayoutVersion` to the `save()` payload. In `resetHome()`: `this.homeLayoutVersion = d.homeLayoutVersion;` (and `homeHidden = [...d.homeHidden]` now yields the classic-hidden default automatically — leave that line as is). No toast, no banner (UI-17).
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/stores/settings-persist.svelte.test.ts src/lib/stores/settings.svelte.test.ts src/lib/backup/backup-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "homeHidden: \[...CLASSIC_SECTIONS\] as string\[\]" src/lib/config/defaults.ts` matches and `grep -n "homeLayoutVersion: HOME_LAYOUT_VERSION" src/lib/config/defaults.ts` matches
    - `grep -n "migrateHomeLayout(" src/lib/stores/settings.svelte.ts` matches exactly one call site and `grep -n "if (migrated) this.save()" src/lib/stores/settings.svelte.ts` matches
    - `grep -n "homeLayoutVersion" src/lib/stores/settings.svelte.ts` matches ≥ 5 lines and `grep -n "homeLayoutVersion.*\$state" src/lib/stores/settings.svelte.ts` prints nothing (plain field)
    - the 39-06 guard test "does not write homeLayoutVersion yet" is REPLACED (Task 2) — `grep -c "does not write homeLayoutVersion yet" src/lib/stores/settings-persist.svelte.test.ts` prints 0 after Task 2
    - `pnpm check` exits 0; `pnpm vitest --run src/lib/backup/backup-logic.test.ts` still passes (backup allowlist unaffected)
  </acceptance_criteria>
  <done>Existing users migrate once and persist the version; fresh installs and Reset get the new layout from defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Migration round-trip tests + the Clear-picks fix</name>
  <read_first>
    - src/lib/stores/settings-persist.svelte.test.ts (harness + the 39-06 "home chart settings" block; the "does not write homeLayoutVersion yet" test to replace)
    - src/lib/stores/settings.svelte.ts (Task 1 result)
    - src/routes/(app)/settings/data/+page.svelte lines 1-30 (imports, `TOP_PICKS_KEY`, `HOME_LIBRARY_KEY`) and 155-159 (`clearPicks`)
    - src/lib/services/home-charts.ts (`HOME_CACHE_KEY`, `LEGACY_HOME_CACHE_KEYS`)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md §"Folded in" (the v1 no-op bug)
    - src/lib/backup/backup-logic.ts `BACKUP_EXACT_KEYS` (confirm top-picks keys are not in the backup allowlist — no change needed)
  </read_first>
  <files>src/lib/stores/settings-persist.svelte.test.ts, src/routes/(app)/settings/data/+page.svelte</files>
  <action>
    Tests: in `src/lib/stores/settings-persist.svelte.test.ts` replace the 39-06 "does not write homeLayoutVersion yet" test with "save() writes homeLayoutVersion 2" and add `describe('one-time home layout switch (39-D-40 / P39-06)')` encoding every line of Task 1's `<behavior>` with the file's harness: seed `memStore` with an OLD blob (the ten 39-02-era ids in `homeSectionOrder`, `homeHidden: []`, a `homeSectionDensity`, `homeShelfSize: 16`, `homeLandingTab: 'search'`, `homeShowSearchPill: false`), `freshSettings().load()`, assert order/hidden/density/version and that the persisted blob was rewritten (`JSON.parse(localStorage.getItem(KEY)).homeLayoutVersion === 2`); the "un-hide survives reload" case does load → mutate `homeHidden` → `save()` → `freshSettings().load()` → assert; the "already at v2 → no write" case spies `localStorageMock.setItem` (or compares the raw string before/after) to prove load() did not save; the fresh-install case asserts `localStorage.getItem(KEY) === null` after `load()` and `homeHidden` equals the classic ids; the `resetHome()` case asserts the persisted blob.

    Clear-picks (39-D-41, CONTEXT "Folded in"): in `src/routes/(app)/settings/data/+page.svelte` delete the `TOP_PICKS_KEY = 'openmusic:top-picks:v1'` literal, add `import { HOME_CACHE_KEY, LEGACY_HOME_CACHE_KEYS } from '$lib/services/home-charts';`, and make `clearPicks()` remove `HOME_CACHE_KEY`, every `LEGACY_HOME_CACHE_KEYS` entry, and `HOME_LIBRARY_KEY` (each in its own try/catch as today). Comment: the button removed `v1` while the home used `v2` since the last bump, so "Clear picks" had been a no-op — the key now lives in ONE `$lib` module shared with the home page, so a future bump cannot desync them. Backups are unaffected (`backup-logic.ts` uses an exact-key allowlist that never included top-picks).
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/stores/settings-persist.svelte.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest --run src/lib/stores/settings-persist.svelte.test.ts` passes with ≥ 7 tests in a describe whose name contains "one-time home layout switch", including names matching /survives.*reload/i, /fresh install/i, /already.*(v2|version 2)/i, /resetHome/
    - `grep -c "openmusic:top-picks:v1" "src/routes/(app)/settings/data/+page.svelte"` prints 0 and `grep -n "import { HOME_CACHE_KEY, LEGACY_HOME_CACHE_KEYS } from '\$lib/services/home-charts'" "src/routes/(app)/settings/data/+page.svelte"` matches
    - `awk '/function clearPicks/,/^\t}/' "src/routes/(app)/settings/data/+page.svelte" | grep -c "removeItem"` ≥ 3 (HOME_CACHE_KEY, legacy loop, HOME_LIBRARY_KEY)
    - `pnpm check` exits 0
    - Dev smoke: seed `openmusic:settings:v1` in devtools with an old blob (no `homeLayoutVersion`, `homeHidden: []`), reload → Home shows the new shelves and none of Top hits / Top artists (Deezer) / genre-tag / country shelves; /settings/home lists the classic rows as off; reload again → no change (no second migration); Settings → Data → Clear picks then reload → the home refetches (cold)
  </acceptance_criteria>
  <done>Migration behaviour is pinned by store round-trip tests; Clear picks clears the real key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage `openmusic:settings:v1` (incl. imported backups) → load() migration | attacker/corruption-controllable order, hidden, density, version |
| localStorage home cache keys → Clear picks | user action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-35 | Tampering | homeLayoutVersion | mitigate | `typeof === 'number'` else 1; a forged high version merely skips the migration (the user keeps today's layout — no security impact); `resolveSectionOrder` still appends missing ids at render |
| T-39-36 | DoS (loop) | migration on every load | mitigate | version persisted in the same `load()` via `if (migrated) this.save()`; migration is idempotent by construction (39-02 tests) |
| T-39-37 | Tampering | poisoned order/hidden arrays into migrateHomeLayout | mitigate | pure function de-dupes and only inserts known chart ids; never throws (39-02 tests); type guards precede it |
| T-39-38 | Information disclosure | Clear picks | accept | removes only the app's own non-sensitive cache keys; backups unaffected |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/stores/settings-persist.svelte.test.ts src/lib/services/home-layout.test.ts` green (VALIDATION P39-06 unit + store round-trip).
- `pnpm check` green.
- VALIDATION manual row "Existing-user one-time switch on a real old settings blob" executed as in Task 2's dev smoke.
</verification>

<success_criteria>
- Existing users see the new layout once, silently, with everything else preserved; un-hiding a classic shelf sticks.
- Fresh installs and Reset-to-default get the new layout from `HOME_DEFAULTS`.
- Clear picks clears the v3 key and the orphaned v1/v2 keys.
</success_criteria>

<output>
Create `.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-09-SUMMARY.md` when done.
</output>
