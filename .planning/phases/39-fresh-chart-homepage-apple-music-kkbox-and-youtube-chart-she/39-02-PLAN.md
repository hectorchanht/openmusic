---
phase: 39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/home-layout.ts
  - src/lib/services/home-layout.test.ts
  - src/lib/i18n/en.ts
  - src/lib/i18n/zh-Hant.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/es.ts
  - src/lib/i18n/fr.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/pt.ts
  - src/lib/i18n/it.ts
  - src/lib/i18n/ru.ts
  - src/lib/i18n/tr.ts
  - src/lib/i18n/ar.ts
  - src/lib/i18n/hi.ts
  - src/lib/i18n/id.ts
  - src/lib/i18n/vi.ts
  - src/lib/i18n/th.ts
  - src/routes/(app)/settings/home/+page.svelte
autonomous: true
requirements: [P39-05, P39-06, P39-11]
must_haves:
  truths:
    - "HOME_SECTIONS carries the seven new chart section ids in the canonical order liked, downloads, radio, chart-songs, new-releases, chart-artists, chart-albums, yt-trending, genres, regions, top-hits, top-artists, tags, countries, fav-artists, playlists, history"
    - "resolveChartRegion maps every one of the 15 AppLangs to a verified storefront, prefers a navigator.language region subtag that is in the offered list, never yields 'cn', and falls back to 'us'"
    - "resolveExtraRegions drops the main region, unknown codes and duplicates; resolveChartGenres keeps only pool ids and an empty selection stays empty"
    - "migrateHomeLayout inserts only the MISSING chart ids at the first classic id's slot (after 'radio' when no classic id is present), unions the four classic ids into hidden, carries per-section density old→new, and is idempotent"
    - "reorderListed moves one listed (non-classic) row while every classic id keeps its exact array index"
    - "All 15 locales carry the 36 new keys, the changed settings.groupHomeDesc, and no settings.homeCountriesLabel — parity test green"
  artifacts:
    - path: "src/lib/services/home-layout.ts"
      provides: "CHART_REGIONS, ChartRegion, KKBOX_REGIONS, YT_REGIONS, resolveChartRegion, resolveExtraRegions, CHART_GENRE_IDS, ChartGenre, CHART_GENRES, DEEZER_GENRE_IDS, DEFAULT_CHART_GENRES, resolveChartGenres, CLASSIC_SECTIONS, CHART_SECTIONS, ChartSectionId, HOME_LAYOUT_VERSION, migrateHomeLayout, reorderListed"
      exports: ["CHART_REGIONS", "KKBOX_REGIONS", "YT_REGIONS", "resolveChartRegion", "resolveExtraRegions", "CHART_GENRES", "DEEZER_GENRE_IDS", "DEFAULT_CHART_GENRES", "resolveChartGenres", "CLASSIC_SECTIONS", "CHART_SECTIONS", "HOME_LAYOUT_VERSION", "migrateHomeLayout", "reorderListed"]
    - path: "src/lib/services/home-layout.test.ts"
      provides: "extended tests for the region/genre resolvers, migration and reorderListed (VALIDATION P39-05 / P39-06)"
      min_lines: 400
    - path: "src/lib/i18n/en.ts"
      provides: "36 new keys (home.chartSongs … settings.homeClassicOnCount)"
      contains: "settings.homeClassicOnCount"
  key_links:
    - from: "src/routes/(app)/settings/home/+page.svelte sectionLabel"
      to: "HomeSectionId (17 ids)"
      via: "Record<HomeSectionId, TranslationKey> — TypeScript forces the 7 new entries"
      pattern: "'chart-songs': 'settings.homeSectionChartSongs'"
    - from: "src/lib/services/home-layout.ts migrateHomeLayout"
      to: "CLASSIC_SECTIONS / CHART_SECTIONS"
      via: "insert-at-first-classic + hidden union"
      pattern: "CLASSIC_SECTIONS"
---

<objective>
Extend the pure home-layout model with everything the phase's persisted config needs — the 17 section ids, the 27-region allowlist and language default, the 11-genre pool, the version-gated one-time migration, and the settings-drag helper — and land the i18n keys in all 15 locales so the type system (`Record<HomeSectionId, TranslationKey>`) stays green in the same commit.

Purpose: `home-layout.ts` is the robustness layer between attacker-controllable localStorage and the render; every new setting gets a pure resolver here with node tests before any UI reads it. Landing the section ids and their labels together keeps `pnpm check` green and the settings page correct at every intermediate deploy (main auto-deploys).
Output: extended `home-layout.ts` + tests, 15 locale files, a minimal `sectionLabel` map update in `settings/home/+page.svelte`.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md
@src/lib/services/home-layout.ts
@src/lib/services/home-layout.test.ts
@src/lib/i18n/en.ts
@src/lib/i18n/i18n.test.ts

<interfaces>
<!-- Contracts THIS plan creates in home-layout.ts. Plans 03-10 import these exactly. -->
```typescript
export const HOME_SECTIONS = ['liked','downloads','radio','chart-songs','new-releases','chart-artists','chart-albums','yt-trending','genres','regions','top-hits','top-artists','tags','countries','fav-artists','playlists','history'] as const;
export type HomeSectionId = (typeof HOME_SECTIONS)[number];
export const CLASSIC_SECTIONS = ['top-hits', 'top-artists', 'tags', 'countries'] as const;
export const CHART_SECTIONS = ['chart-songs', 'new-releases', 'chart-artists', 'chart-albums', 'yt-trending', 'genres', 'regions'] as const;
export type ChartSectionId = (typeof CHART_SECTIONS)[number];

export const CHART_REGIONS = ['hk','tw','sg','jp','kr','us','gb','ca','au','de','fr','es','it','pt','br','mx','ru','tr','th','vn','id','in','ph','my','sa','ae','eg'] as const;
export type ChartRegion = (typeof CHART_REGIONS)[number];
export const KKBOX_REGIONS: readonly ChartRegion[]; // ['hk','tw','sg']
export const YT_REGIONS: readonly ChartRegion[];    // = CHART_REGIONS (kept separate for drift)
export function resolveChartRegion(saved: unknown, appLang: string, navLang?: string): ChartRegion;
export function resolveExtraRegions(saved: unknown, main: ChartRegion): ChartRegion[];

export const CHART_GENRE_IDS = ['cantopop','mandopop','kpop','jpop','hiphop','rock','dance','rnb','electronic','alternative','asian'] as const;
export type ChartGenre = (typeof CHART_GENRE_IDS)[number];
export type ChartGenreSource = { src: 'itunes'; cc: 'hk' | 'tw' | 'jp'; id: number } | { src: 'deezer'; id: number };
export const CHART_GENRES: Record<ChartGenre, ChartGenreSource>;
export const DEEZER_GENRE_IDS: readonly number[];   // [116,152,113,165,106,85,16]
export const DEFAULT_CHART_GENRES: readonly ChartGenre[]; // ['cantopop','mandopop','kpop','jpop','hiphop','rock','dance','rnb']
export function resolveChartGenres(saved: unknown): ChartGenre[];

export const HOME_LAYOUT_VERSION = 2;
export function migrateHomeLayout(order: string[], hidden: string[], density?: Partial<Record<HomeSectionId, HomeDensity>>): { order: string[]; hidden: string[]; density: Partial<Record<HomeSectionId, HomeDensity>> };
export function reorderListed(order: HomeSectionId[], from: number, to: number): HomeSectionId[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend home-layout.ts with sections, regions, genres, migration and reorderListed (with tests)</name>
  <read_first>
    - src/lib/services/home-layout.ts (whole file — the module being extended; keep every existing decision-ref comment)
    - src/lib/services/home-layout.test.ts (whole file — the 10-id test at lines 24-38 MUST be updated to the 17-id list, not weakened; RESEARCH Pitfall 6)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Region table, §Code Examples 6-7, §Pattern 5
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md §Region, §Genres, §Existing users, §Claude's Discretion (density carry-over)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §2.2 "Drag semantics" (reorderListed contract) and §2.3 (region list order)
    - src/lib/i18n/index.ts lines 29-45 (the 15 AppLang values — the LANG_REGION map must cover all of them)
  </read_first>
  <files>src/lib/services/home-layout.ts, src/lib/services/home-layout.test.ts</files>
  <behavior>
    - HOME_SECTIONS deep-equals the 17-id list in `<interfaces>`; DEFAULT_SECTION_ORDER still deep-equals HOME_SECTIONS and is a distinct array
    - resolveSectionOrder(['top-hits','liked']) appends the 15 missing ids in canonical order (existing behaviour, now covering the new ids)
    - resolveChartRegion('tw', 'en') === 'tw' (saved wins); ('auto','zh-Hant') → 'hk'; ('auto','zh-Hans') → 'tw'; ('auto','en') → 'us'; de→de, fr→fr, es→es, it→it, pt→'br', ru→ru, tr→tr, th→th, vi→'vn', id→id, hi→'in', ar→'sa'; ('auto','xx') → 'us'; ('cn','zh-Hans') → 'tw'; (42,'en') → 'us'; (null,'en') → 'us'
    - resolveChartRegion('auto','zh-Hant','zh-TW') === 'tw'; ('auto','zh-Hans','zh-CN') === 'tw' (cn is not offered → language default, never 'cn'); ('auto','en','en-GB') === 'gb'; ('auto','en','en') === 'us'; ('hk','en','en-GB') === 'hk' (explicit saved beats navLang)
    - resolveExtraRegions(['tw','hk','tw','xx','jp'], 'hk') deep-equals ['tw','jp']; (undefined,'hk') → []; ('tw','hk') → []
    - CHART_GENRES.cantopop deep-equals {src:'itunes',cc:'hk',id:1251}; mandopop {itunes,tw,1253}; kpop {itunes,hk,51}; jpop {itunes,jp,27}; hiphop {deezer,116}; rock {deezer,152}; dance {deezer,113}; rnb {deezer,165}; electronic {deezer,106}; alternative {deezer,85}; asian {deezer,16}
    - DEEZER_GENRE_IDS deep-equals [116,152,113,165,106,85,16]; DEFAULT_CHART_GENRES deep-equals ['cantopop','mandopop','kpop','jpop','hiphop','rock','dance','rnb']
    - resolveChartGenres(['rock','bogus','rock','cantopop']) → ['rock','cantopop']; ([]) → []; (undefined) → [...DEFAULT_CHART_GENRES]; ('rock') → [...DEFAULT_CHART_GENRES]
    - migrateHomeLayout(oldDefault10, []) → order === ['liked','downloads','radio', ...CHART_SECTIONS, 'top-hits','top-artists','fav-artists','tags','countries','playlists','history'] and hidden deep-equals [...CLASSIC_SECTIONS]
    - migrateHomeLayout(['history','tags','liked','top-hits'], ['liked']) → chart ids inserted at index 1 (before 'tags'), hidden === ['liked', ...CLASSIC_SECTIONS]
    - migrateHomeLayout(['liked','radio','history'], []) → chart ids inserted right after 'radio'; migrateHomeLayout(['history'], []) → chart ids at index 0
    - migrateHomeLayout(['liked','liked','chart-songs','top-hits'], []) → 'liked' once, 'chart-songs' stays at its slot, the other 6 chart ids inserted before 'top-hits'
    - migrateHomeLayout applied twice deep-equals applied once (idempotent)
    - density carry-over: migrateHomeLayout(order, [], {'top-hits':'pile','tags':'grid','chart-songs':'list'}).density deep-equals {'top-hits':'pile','tags':'grid','chart-songs':'list','genres':'grid'} (chart-songs already overridden → untouched; top-artists/countries absent → nothing added)
    - reorderListed(['liked','top-hits','downloads','tags','radio'], 0, 2) deep-equals ['downloads','top-hits','radio','tags','liked']; reorderListed(order, 1, 1) deep-equals order; out-of-range from/to returns a copy unchanged
  </behavior>
  <action>
    In `src/lib/services/home-layout.ts` (39-D-08 for the section ids, 39-D-09 for regions, 39-D-10 for genres, 39-D-11 for the migration, 39-D-12 for reorderListed — tag each block):
    1. Replace the `HOME_SECTIONS` tuple with the 17-id list from `<interfaces>` and extend the persisted-string warning comment: the seven chart ids are Claude's naming and are PERSISTED, so they must never change once shipped. Add `CLASSIC_SECTIONS`, `CHART_SECTIONS`, `ChartSectionId` right below.
    2. Regions: `CHART_REGIONS` in the UI-SPEC §2.3 order (geographic, Asia-first — NOT alphabetical), `ChartRegion`, `KKBOX_REGIONS = ['hk','tw','sg']`, `YT_REGIONS = CHART_REGIONS` (comment: all 27 echo-verified 2026-09-25; kept a separate const so a YouTube drop can shrink it without touching Apple). A private `LANG_REGION: Record<string, ChartRegion>` = { en:'us', 'zh-Hant':'hk', 'zh-Hans':'tw', de:'de', fr:'fr', es:'es', it:'it', pt:'br', ru:'ru', tr:'tr', th:'th', vi:'vn', id:'id', hi:'in', ar:'sa' } — a fixed-map lookup in the `LANDING_PATHS` posture (never taken from the raw persisted string). Comment the two non-obvious rows: zh-Hans → tw NEVER cn (CN storefront top-20 median age ~22 y), ar → sa not ae (UAE chart is expat/Western-skewed). `resolveChartRegion(saved, appLang, navLang?)`: (a) if `saved` is a string in CHART_REGIONS → it; (b) else if `navLang` is a string whose BCP-47 region subtag (the `-XX` after the language, lowercased, e.g. 'zh-TW' → 'tw', 'en-GB' → 'gb') is in CHART_REGIONS → it (CONTEXT Claude's-discretion refinement, 39-D-09b); (c) else `LANG_REGION[appLang] ?? 'us'`. 'cn' can never come out because it is not in CHART_REGIONS. `resolveExtraRegions(saved, main)`: the `resolveSubset` loop (known-set filter, de-dupe, saved order preserved) over CHART_REGIONS, additionally dropping `main`; non-array → [] (NOT the full pool — UI-SPEC UI-11 default is none).
    3. Genres: `CHART_GENRE_IDS`, `ChartGenre`, `ChartGenreSource`, `CHART_GENRES` with the exact 11 entries in `<behavior>` (comment: regional genres use a FIXED storefront independent of the Chart region — the HK storefront's J-Pop/Mandopop are stale purchase charts; Western genres are Deezer because the legacy feed ranks purchases), `DEEZER_GENRE_IDS` derived by filtering CHART_GENRES for `src === 'deezer'` in CHART_GENRE_IDS order, `DEFAULT_CHART_GENRES` (CONTEXT locked default: Asian pop + Western core). `resolveChartGenres(saved)`: non-array → `[...DEFAULT_CHART_GENRES]`; array → known-filter + de-dupe in saved order; empty stays EMPTY (UI-SPEC UI-12: no fall-back-to-all — contrast resolveSubset, and say so in the comment).
    4. Migration: `HOME_LAYOUT_VERSION = 2` (comment: absent in a persisted blob means 1; cite settings.svelte.ts's existing "a one-shot migration needs its own persisted version marker" record at the upnextPerContext block). `migrateHomeLayout(order, hidden, density = {})`: `out = [...new Set(order)]`; `missing = CHART_SECTIONS.filter(id => !out.includes(id))`; `at = out.findIndex(id => CLASSIC_SECTIONS.includes(id))`; if `at < 0` then `at = out.includes('radio') ? out.indexOf('radio') + 1 : 0`; `out.splice(at, 0, ...missing)`; `hidden = [...new Set([...hidden, ...CLASSIC_SECTIONS])]`; density carry-over map `{'top-hits':'chart-songs','top-artists':'chart-artists','tags':'genres','countries':'regions'}` — for each pair, if `density[old]` is set and `density[new]` is not, copy it (UI-SPEC UI-13); return `{ order: out, hidden, density: {...density, ...carried} }`. Pure, never throws on any array input.
    5. `reorderListed(order, from, to)`: `listed = order.filter(id => !CLASSIC_SECTIONS.includes(id))`; if from/to out of `[0, listed.length)` return `[...order]`; splice-move within `listed` (the `reorderList` idiom from settings/home/+page.svelte lines 113-118); then rebuild: walk `order`, keep a classic id at its index, otherwise take the next id from the moved list. Comment: classic ids keep their exact array slot so a hidden classic shelf never moves when the user drags a listed row (UI-SPEC §2.2).

    In `src/lib/services/home-layout.test.ts`: update the HOME_SECTIONS test title and expected list to the 17 ids (name it "is the seventeen home section ids in canonical order (39-D-08: chart block after radio, classic block after charts)"); add describe blocks `resolveChartRegion (39-D-09)`, `resolveExtraRegions`, `CHART_GENRES / resolveChartGenres (39-D-10)`, `migrateHomeLayout (39-D-11)`, `reorderListed (39-D-12)` encoding every `<behavior>` line; the AppLang coverage test iterates the literal 15-value list and asserts the result is in CHART_REGIONS and never 'cn'. Keep the file's determinism header comment.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/services/home-layout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'chart-songs', 'new-releases', 'chart-artists', 'chart-albums', 'yt-trending', 'genres', 'regions'" src/lib/services/home-layout.ts` ≥ 1
    - `grep -n "export function resolveChartRegion\|export function resolveExtraRegions\|export function resolveChartGenres\|export function migrateHomeLayout\|export function reorderListed" src/lib/services/home-layout.ts` prints 5 lines
    - `grep -n "export const HOME_LAYOUT_VERSION = 2" src/lib/services/home-layout.ts` matches
    - `grep -c "'cn'" src/lib/services/home-layout.ts` prints 0 outside comments: `grep -v "^\s*//" src/lib/services/home-layout.ts | grep -c "'cn'"` prints 0
    - `pnpm vitest --run src/lib/services/home-layout.test.ts` passes with ≥ 40 tests total and ≥ 1 test whose name matches /never.*cn|cn.*never/i
    - home-layout.ts still imports nothing (`grep -c "^import" src/lib/services/home-layout.ts` prints 0)
  </acceptance_criteria>
  <done>The persisted-config model for regions, genres, sections and the migration is pure, exported, and node-tested; the existing 10-id test is deliberately updated, not weakened.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 36 new i18n keys, change one, remove one — in all 15 locales</name>
  <read_first>
    - src/lib/i18n/en.ts lines 24-37 (`home.*` block) and lines 238-275 (`settings.home*` block) — the two insertion neighbourhoods
    - src/lib/i18n/i18n.test.ts (parity + no-blank guard across all 15 locales; the quote-style convention)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §Copywriting Contract "New keys" table (the exact 36 keys + en values), "Changed value", "Removed"
    - CLAUDE.md §i18n — DOUBLE QUOTES for every key AND value in src/lib/i18n/*.ts (manual convention, no formatter)
    - src/lib/i18n/zh-Hant.ts and src/lib/i18n/zh-Hans.ts around the same keys (see how the existing home/settings strings are phrased in each script)
  </read_first>
  <files>src/lib/i18n/en.ts, src/lib/i18n/zh-Hant.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/es.ts, src/lib/i18n/fr.ts, src/lib/i18n/de.ts, src/lib/i18n/pt.ts, src/lib/i18n/it.ts, src/lib/i18n/ru.ts, src/lib/i18n/tr.ts, src/lib/i18n/ar.ts, src/lib/i18n/hi.ts, src/lib/i18n/id.ts, src/lib/i18n/vi.ts, src/lib/i18n/th.ts</files>
  <action>
    In EVERY one of the 15 locale files (en is the key source; `i18n.test.ts` enforces identical key sets and no blank values), using DOUBLE QUOTES for keys and values and a `// --- 39: chart shelves ---` marker comment:
    1. Add the 36 keys from UI-SPEC §Copywriting Contract, placed next to their siblings: the five shelf titles (`home.chartSongs` "Top songs · {region}", `home.newReleases` "New releases · {region}", `home.chartArtists` "Top artists · {region}", `home.chartAlbums` "Top albums · {region}", `home.ytTrending` "Trending on YouTube · {region}") and the eleven `home.genre.<id>` labels (cantopop "Cantopop", mandopop "Mandopop", kpop "K-Pop", jpop "J-Pop", hiphop "Hip-Hop", rock "Rock", dance "Dance", rnb "R&B", electronic "Electronic", alternative "Alternative", asian "Asian music") go after `home.seeAll`; the twenty `settings.*` keys (`settings.homeSectionChartSongs` "Top songs", `settings.homeSectionNewReleases` "New releases", `settings.homeSectionChartArtists` "Top artists", `settings.homeSectionChartAlbums` "Top albums", `settings.homeSectionYtTrending` "Trending on YouTube", `settings.homeSectionGenres` "Genre charts", `settings.homeSrcLibrary` "Your library", `settings.homeSrcUnavailable` "Not available in {region}", `settings.homeGroupCharts` "Charts", `settings.chartRegion` "Chart region", `settings.chartRegionDesc` "Picks the country whose charts fill Top songs, New releases, Top artists, Top albums and Trending on YouTube. Auto follows your app language.", `settings.chartRegionAuto` "Auto ({region})", `settings.moreRegions` "More regions", `settings.moreRegionsDesc` "Adds a Top songs shelf for each extra region you pick, in the order shown.", `settings.moreRegionsNone` "None", `settings.chartGenresDesc` "Each genre you pick becomes its own chart shelf on Home.", `settings.chartGenresNone` "None selected — no genre shelves on Home", `settings.homeGroupClassic` "Classic (Last.fm / Deezer)", `settings.homeClassicDesc` "The Home shelves from earlier versions, built from Deezer and Last.fm. They are off by default; turn one on to bring it back exactly as it was.", `settings.homeClassicOnCount` "{n} on") go after `settings.homeDragReorderChips`.
    2. Change `settings.groupHomeDesc` to the localized equivalent of "Charts, region, sections, layout".
    3. Delete `settings.homeCountriesLabel` from all 15 files (its only template use is replaced in Task 3).
    Translate properly for each locale (a translator call, not a copy of English): keep `{region}` and `{n}` placeholders verbatim; brand names (YouTube, Last.fm, Deezer, Apple Music, KKBOX) stay verbatim; the ` · ` separator may be re-ordered per locale (e.g. zh-Hant `"{region}熱門歌曲"` is fine). Genre names: use the native genre word where one exists (zh-Hant 廣東歌 / 華語流行 / 韓國流行 / 日本流行 …; keep "Hip-Hop", "R&B" where locales use the loanword). Do not add keys only to en.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/i18n/i18n.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `for f in src/lib/i18n/{en,zh-Hant,zh-Hans,es,fr,de,pt,it,ru,tr,ar,hi,id,vi,th}.ts; do grep -c '"home.genre.asian"\|"settings.homeClassicOnCount"\|"settings.chartRegionAuto"' $f; done` prints 3 fifteen times
    - `grep -rc "homeCountriesLabel" src/lib/i18n/ | grep -v ":0"` prints nothing
    - `grep -c "'" src/lib/i18n/en.ts` is unchanged from before this task except inside string VALUES (apostrophes in prose are fine); every key line in the new block starts with a tab and a double quote: `grep -c '^\t"home.genre\.' src/lib/i18n/en.ts` prints 11
    - `pnpm vitest --run src/lib/i18n/i18n.test.ts` passes (key parity across 15 locales, no blanks)
  </acceptance_criteria>
  <done>36 keys added, 1 changed, 1 removed, in all 15 locales, double-quoted, parity test green.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the settings sectionLabel map for the seven new ids and drop the removed key's one use</name>
  <read_first>
    - src/routes/(app)/settings/home/+page.svelte lines 40-53 (`sectionLabel: Record<HomeSectionId, TranslationKey>` — TypeScript now requires 7 more entries) and line 320 (`t('settings.homeCountriesLabel')`, the removed key's only use)
    - src/lib/services/home-layout.ts (Task 1 result: the 17 `HomeSectionId`s)
  </read_first>
  <files>src/routes/(app)/settings/home/+page.svelte</files>
  <action>
    Minimal, type-driven edit only (the full redesign is plan 39-10, wave 4 — do NOT restructure this page here):
    1. Add the seven entries to `sectionLabel`: `'chart-songs': 'settings.homeSectionChartSongs'`, `'new-releases': 'settings.homeSectionNewReleases'`, `'chart-artists': 'settings.homeSectionChartArtists'`, `'chart-albums': 'settings.homeSectionChartAlbums'`, `'yt-trending': 'settings.homeSectionYtTrending'`, `genres: 'settings.homeSectionGenres'`, `regions: 'settings.moreRegions'` (UI-SPEC: one key, one concept for the regions row).
    2. Replace `t('settings.homeCountriesLabel')` in the Countries `<h2>` with `t('settings.homeSectionCountries')` ("Country shelves") so the page compiles against the trimmed dictionary.
    Result at this intermediate deploy: the existing list shows 17 toggle-able rows (the 7 new ones do nothing on Home until 39-07 lands) — acceptable and shippable.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "settings.homeSectionChartSongs\|settings.homeSectionNewReleases\|settings.homeSectionChartArtists\|settings.homeSectionChartAlbums\|settings.homeSectionYtTrending\|settings.homeSectionGenres\|settings.moreRegions" "src/routes/(app)/settings/home/+page.svelte"` prints 7
    - `grep -c "homeCountriesLabel" "src/routes/(app)/settings/home/+page.svelte"` prints 0
    - `pnpm check` exits 0 (svelte-check reports 0 errors)
    - `pnpm vitest --run src/lib/services/home-layout.test.ts src/lib/i18n/i18n.test.ts` passes
  </acceptance_criteria>
  <done>The settings page type-checks against the 17-id union and the trimmed dictionary; nothing else on the page changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage `openmusic:settings:v1` → resolvers | region / extra regions / genres / order / hidden / version are attacker- or corruption-controllable |
| resolved region / genre → upstream URL (via 39-03 / 39-04) | only allowlisted values may ever reach a URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering | resolveChartRegion | mitigate | closed `CHART_REGIONS` allowlist; garbage/'cn'/unknown → language default; `LANG_REGION` is a fixed map, never the raw string |
| T-39-07 | Tampering | resolveExtraRegions / resolveChartGenres | mitigate | known-set filter + de-dupe; a poisoned id is dropped before any fetch is planned |
| T-39-08 | DoS | migrateHomeLayout on a giant/looping order | mitigate | `new Set` de-dupe, single splice, no recursion; idempotent + version-gated (39-09) so it never re-runs per load |
| T-39-09 | Tampering | reorderListed with out-of-range indices | mitigate | bounds check returns a copy unchanged; `resolveSectionOrder` remains the render-time robustness layer |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/services/home-layout.test.ts src/lib/i18n/i18n.test.ts` green.
- `pnpm check` green after Task 3 (the `Record<HomeSectionId, TranslationKey>` map is complete).
- `grep -rn "homeCountriesLabel" src` prints nothing.
</verification>

<success_criteria>
- All exports in `<interfaces>` exist with the specified semantics and tests.
- All 15 locales carry the new keys; parity test green; double quotes preserved.
- Intermediate deploy is shippable: settings page compiles and lists 17 rows; home unaffected.
</success_criteria>

<output>
Create `.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-02-SUMMARY.md` when done.
</output>
