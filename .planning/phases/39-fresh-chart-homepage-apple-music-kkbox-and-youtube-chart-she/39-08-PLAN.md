---
phase: 39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she
plan: 08
type: execute
wave: 4
depends_on: ["39-07"]
files_modified:
  - src/routes/(app)/+page.svelte
autonomous: true
requirements: [P39-07]
must_haves:
  truths:
    - "A shelf that is planned (visible, capable in the current region, fetch in flight) and has no cached pool for its exact key shows a per-shelf placeholder in its slot: a 44px heading skeleton plus a density-matched body (list = two compactSkeletonColumn, pile = 4 cover-sized .sk tiles, grid = 9 .sk squares); it is replaced in place when the task settles non-empty and removed when it settles empty"
    - "A placeholder counts as one shelf in shelfCount so the first 3 reveal slots fill the viewport on a cold load; a section with no planned task or a settled-empty one counts 0"
    - "The global 'Top picks' skeleton shows only while loading, nothing has content AND no placeholder is rendered; warm loads show neither; a Chart region change shows placeholders under the new region's titles (region-qualified keys) instead of the previous region's items"
  artifacts:
    - path: "src/routes/(app)/+page.svelte"
      provides: "inflight key set, shelfPlaceholder snippet, planned-aware shelfCount, extended skeleton condition, placeholder CSS"
      contains: "shelfPlaceholder"
  key_links:
    - from: "src/routes/(app)/+page.svelte runChartTasks"
      to: "inflight (Set<string>)"
      via: "add key before fetch, delete on land"
      pattern: "inflight"
    - from: "src/routes/(app)/+page.svelte shelfCount"
      to: "isPlanned(key)"
      via: "placeholder counts as a shelf"
      pattern: "isPlanned\\("
---

<objective>
Add the per-shelf loading placeholders from UI-SPEC §1.7 so a slow Top songs (Apple, up to 5 s) never pops in above already-rendered shelves and pushes content under the user's thumb, and make the reveal budget and the global skeleton aware of planned-but-empty shelves.

Purpose: 39-07 assigns pools as they land, which is right for latency but wrong for layout stability without a slot reservation. Placeholders reserve the slot; the budget rule keeps the progressive mount honest on a cold load.
Output: `+page.svelte` (script + template + CSS).
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-07-SUMMARY.md
@src/routes/(app)/+page.svelte
@src/app.css

<interfaces>
```typescript
// page-local from 39-07: pools, picks, chartRegion, chartExtraRegions, chartGenres, chartTasks(), runChartTasks(tasks, gen, apply), sampledSongs/Artists/Albums(key), genreShelves, regionShelves, shelfCount(id), shelfBudget, hasAnyContent(), compactSkeletonColumn(), densityOf(id), REVEAL_INITIAL
// $lib/services/home-charts: poolKey(section, cc), genrePoolKey(genre)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track in-flight pool keys and make shelfCount / the global skeleton planned-aware</name>
  <read_first>
    - src/routes/(app)/+page.svelte — the 39-07 result: `runChartTasks`, `revalidatePools`, `shelfCount`, `shelfBudget`, `hasAnyContent`, the skeleton condition, `genreShelves` / `regionShelves`
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §1.7 "Placeholder rule", "Global skeleton", "Progressive mount"
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Pattern 6 last bullet (shelfCount rules) and §Common Pitfalls 10 (browser pane rAF frozen — verify budget logic by reading, not by screenshot)
  </read_first>
  <files>src/routes/(app)/+page.svelte</files>
  <action>
    In the `<script>` (39-D-37):
    1. `let inflight = $state<Set<string>>(new Set())` (a `$state` Set is reactive in Svelte 5 via `SvelteSet` semantics — if `$state(new Set())` does not trigger updates, use `import { SvelteSet } from 'svelte/reactivity'` and `new SvelteSet<string>()`; pick whichever `pnpm check` accepts and note it). In `runChartTasks(tasks, gen, apply)` when `apply` is true: before the fan-out, `inflight.add(key)` for every group key; when a group lands (after the gen check), `inflight.delete(key)`; on a superseded gen, still `delete` the key (a stale runner must not leave a permanent placeholder). `revalidatePools` (apply=false) never touches `inflight` (silent refresh shows no placeholder — UI-SPEC "Background refresh: nothing changes on screen").
    2. `isPlanned(key)` = `inflight.has(key) && sampled items for key are empty` — the placeholder condition ("planned, in flight, no cached pool for its exact key").
    3. `shelfCount(id)`: single chart sections return `1` when their sampled items are non-empty OR `isPlanned(key)`, else `0`; `genres` returns `genreShelves.length + (planned genre keys not already counted)`; `regions` likewise for `poolKey('region', cc)`; unchanged for tags/countries/playlists/library.
    4. Derived `anyPlaceholder` = any of the current plan's keys satisfies `isPlanned`. Skeleton condition becomes `loading && !useFallback && !hasAnyContent() && !fallbackSongs.length && !anyPlaceholder` (UI-SPEC: the global skeleton is effectively only a classic-only config's first fetch).
    5. Make `genreShelves` / `regionShelves` carry planned entries too: each element gets `items` (possibly empty) and `planned: boolean`; the `.filter` keeps entries with items OR `planned`. Task 2's templates branch on it.
  </action>
  <verify>
    <automated>pnpm check && pnpm vitest --run src/lib/services/home-charts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "inflight" "src/routes/(app)/+page.svelte"` matches ≥ 5 lines (declaration, add, delete ×2, isPlanned)
    - `grep -n "function isPlanned" "src/routes/(app)/+page.svelte"` matches and `grep -n "anyPlaceholder" "src/routes/(app)/+page.svelte"` matches ≥ 2 lines (derived + skeleton condition)
    - `awk '/function revalidatePools/,/^\t}/' "src/routes/(app)/+page.svelte" | grep -c "inflight"` prints 0
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Planned keys are tracked; budget and skeleton respect them; silent revalidate never shows a placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Render the placeholders per density and wire them into every chart section</name>
  <read_first>
    - src/routes/(app)/+page.svelte: `compactSkeletonColumn` snippet, `.compact-skel-pager` / `.cs-bar*` CSS, `.albumrow` / `.al-cover` / `.section.compact .al-cover` sizes, `.grid` rule, the seven chart block snippets from 39-07
    - src/app.css (`.sk` shimmer class + its reduce-motion exemption; `--radius-md`)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §1.7 items 1-4 (exact placeholder anatomy), §Spacing Scale (sm 8px gap, md 12px tile gap, 2xl 44px heading), §4 (placeholders aria-hidden)
  </read_first>
  <files>src/routes/(app)/+page.svelte</files>
  <action>
    Template + CSS (39-D-38):
    1. `{#snippet shelfPlaceholder(density: HomeDensity, round = false)}` → wrapper `<div class="ph" aria-hidden="true">` containing (a) heading skeleton `<div class="ph-head"><span class="sk ph-bar"></span></div>` (44px row with the heading margins; bar 12px tall, 40% wide); (b) body by density: `list` → `<div class="compact-skel-pager">` with two `{@render compactSkeletonColumn()}` (existing snippet, reused verbatim); `pile` → `<div class="albumrow">` of 4 `<span class="ph-tile"><span class="sk ph-cover" class:round></span><span class="cs-bar cs-bar-title sk"></span><span class="cs-bar cs-bar-sub sk"></span></span>` (cover = `.al-cover` size: `calc(130px * var(--cover-scale, 1))`, 96px under `.section.compact`; radius 10px or 50% when `round`; 4px gap between cover and bars); `grid` → `<div class="grid">` of 9 `<span class="sk ph-cell"></span>` (`aspect-ratio: 1/1; border-radius: var(--radius-md)`). No new font sizes, no new colours; `.sk` keeps shimmering under reduce-motion (existing exemption).
    2. In each single-section snippet (`chartSongsBlock`, `newReleasesBlock`, `chartArtistsBlock`, `chartAlbumsBlock`, `ytTrendingBlock`): `{#if items.length} …real shelf… {:else if isPlanned(key)} {@render shelfPlaceholder(densityOf(id), id === 'chart-artists')} {/if}` — settled empty ⇒ neither branch ⇒ nothing (no header, no gap). In `genresBlock` / `regionsBlock`, inside the budgeted `{#each}`: items non-empty → title + shelf; else (planned) → `shelfPlaceholder(densityOf('genres' | 'regions'))`.
    3. CSS: `.ph-head { min-height: 44px; display: flex; align-items: center; margin: 14px 16px 14px 0; }`, `.ph-bar { height: 12px; width: 40%; border-radius: 6px; }`, `.ph-tile { flex: 0 0 calc(130px * var(--cover-scale, 1)); display: flex; flex-direction: column; gap: 4px; }`, `.ph-cover { width: calc(130px * var(--cover-scale, 1)); height: calc(130px * var(--cover-scale, 1)); border-radius: 10px; }`, `.ph-cover.round { border-radius: 50%; }`, `.section.compact .ph-tile { flex-basis: calc(96px * var(--cover-scale, 1)); }`, `.section.compact .ph-cover { width: calc(96px * var(--cover-scale, 1)); height: calc(96px * var(--cover-scale, 1)); }`, `.ph-cell { aspect-ratio: 1 / 1; border-radius: var(--radius-md); }`. Reuse `.albumrow`, `.grid`, `.cs-bar*`, `.compact-skel-pager` as they are.
    4. Region change path: nothing extra — pool keys are region-qualified (39-05), so switching the Chart region makes every main-shelf key new, `configSig` changes, `refresh(false, true)` plans the new keys, and `isPlanned` shows placeholders under the new region's titles until they land (UI-SPEC §1.7 "Chart region change").
  </action>
  <verify>
    <automated>pnpm check && pnpm vitest --run src/lib/services/home-charts.test.ts src/lib/i18n/i18n.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "{#snippet shelfPlaceholder" "src/routes/(app)/+page.svelte"` prints 1 and `grep -c "shelfPlaceholder(" "src/routes/(app)/+page.svelte"` ≥ 8 (definition + 7 call sites)
    - `awk '/{#snippet shelfPlaceholder/,/{\/snippet}/' "src/routes/(app)/+page.svelte" | grep -c 'aria-hidden="true"'` ≥ 1
    - `grep -c "\.ph-head\|\.ph-cover\|\.ph-cell\|\.ph-tile\|\.ph-bar" "src/routes/(app)/+page.svelte"` ≥ 5
    - `grep -c "font-size" "src/routes/(app)/+page.svelte"` is unchanged from before this plan (no new type sizes)
    - `pnpm check` exits 0
    - Dev smoke (browser pane, fresh localStorage): during the first second the Top songs slot shows a heading bar + compact skeleton columns; after settle, the real shelf occupies the same slot; set Chart region to a non-KKBOX region in settings and return: New releases shows no placeholder and no header
  </acceptance_criteria>
  <done>Cold loads, region changes, new genre/region selections and un-hides all reserve their slot with a density-matched placeholder that resolves in place or disappears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-flight key set → template | page-internal state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-33 | DoS (stuck UI) | inflight keys | mitigate | keys are deleted on land AND on a superseded generation, so a placeholder can never outlive its fetch; silent revalidate never adds keys |
| T-39-34 | DoS (layout) | placeholder count | mitigate | bounded by the plan size (≤ 5 + 11 + 26 keys); each placeholder is static markup with no listeners or observers |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase |
</threat_model>

<verification>
- `pnpm check` green; `pnpm test` green after the wave.
- Browser pane: cold load shows placeholders in section order, then real shelves in place; warm load shows neither skeleton nor placeholders; Randomize shows no placeholder.
</verification>

<success_criteria>
- UI-SPEC §1.7 placeholder rule, global-skeleton rule and progressive-mount rule are implemented as written.
</success_criteria>

<output>
Create `.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-08-SUMMARY.md` when done.
</output>
