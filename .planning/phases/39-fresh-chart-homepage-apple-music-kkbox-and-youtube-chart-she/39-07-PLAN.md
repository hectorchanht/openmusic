---
phase: 39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she
plan: 07
type: execute
wave: 3
depends_on: ["39-03", "39-04", "39-05", "39-06"]
files_modified:
  - src/routes/(app)/+page.svelte
  - src/lib/components/CompactRow.svelte
autonomous: true
requirements: [P39-07, P39-08, P39-12, P39-13]
must_haves:
  truths:
    - "On a cold home the page runs exactly the tasks planChartShelves emits (visible + capable shelves only), under FANOUT_CAP, assigns each pool as it lands (gen-guarded), fuses the two Top Songs lists for hk/tw/sg with Apple names first, and renders Top songs / New releases / Top artists / Top albums / Trending / genre shelves / extra-region shelves in the user's section order with region-qualified titles"
    - "A hidden classic section issues ZERO requests: deezerChart only when top-hits or top-artists is visible, the tag fan-out only when tags is visible, the country fan-out only when countries is visible (today only radio was gated)"
    - "The D-06 fallback grid appears only when EVERY visible shelf (new + classic + library) is empty after settle, and the cold skeleton only while nothing at all has content"
    - "Pools (≤50/key) and sampled picks persist in openmusic:top-picks:v3; a reload repaints the persisted arrangement; Randomize re-samples locally with zero requests; the v1/v2 keys are removed on the first v3 save"
    - "A cached pool older than 6 h revalidates silently in the background: the fresh pool is written to the cache (and adopted by the next Randomize press) but tiles on screen do not swap until the next app open"
    - "Song tiles play via player.playStub(artist, title, cover, 'home-discovery') (Trending passes null cover — UI-15); artist tiles go to /artist/{name}; album tiles go to chartAlbumHref with zero calls at tap"
  artifacts:
    - path: "src/routes/(app)/+page.svelte"
      provides: "v3 cache, chart task runner, gated classic refresh, titleStatic, seven section arms, artistShelf + albumShelf snippets"
      contains: "planChartShelves"
    - path: "src/lib/components/CompactRow.svelte"
      provides: "variant='album' (one button, square art, title + subtitle, onopen, no ⋮, no long-press)"
      contains: "variant === 'album'"
  key_links:
    - from: "src/routes/(app)/+page.svelte refresh()"
      to: "src/lib/services/home-charts.ts planChartShelves"
      via: "task list → mapWithConcurrency(groups, FANOUT_CAP, …)"
      pattern: "planChartShelves\\("
    - from: "src/routes/(app)/+page.svelte"
      to: "src/lib/services/chart-parse.ts fuseCharts"
      via: "two-task pool keys (chart-songs in KKBOX regions), Apple list first"
      pattern: "fuseCharts\\("
    - from: "src/routes/(app)/+page.svelte refresh()"
      to: "deezerChart / getTagTopTracks / getGeoTopTracks"
      via: "visibility gates (39-D-28)"
      pattern: "classicVisible"
    - from: "src/routes/(app)/+page.svelte saveCache"
      to: "HOME_CACHE_KEY / LEGACY_HOME_CACHE_KEYS"
      via: "localStorage write + legacy removal"
      pattern: "LEGACY_HOME_CACHE_KEYS"
---

<objective>
Wire the new chart shelves into the home page: a v3 pools+picks cache, a `planChartShelves`-driven task runner with per-pool assignment, KKBOX+Apple fusion for Top Songs, classic fetches gated on visibility, a fallback gate that knows every shelf, silent next-visit revalidation, local Randomize, and the seven render arms (song / artist / album shelves) with static region-qualified titles. Add the `album` CompactRow variant.

Purpose: this is the phase goal made visible. Everything below the page is already tested; this plan is the wiring, so it copies the page's existing idioms (gen guard, capped fan-out, snippet-per-section, `tileCover`, `playStub`/`tileMenu`) rather than inventing new ones. Classic shelves stay visible at this deploy (the one-time switch is 39-09) — busy but correct.
Output: rewritten orchestration + new snippets in `+page.svelte`, `CompactRow` album variant.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-PATTERNS.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-05-SUMMARY.md
@src/routes/(app)/+page.svelte
@src/lib/components/CompactRow.svelte
@src/lib/services/home-charts.ts
@src/lib/services/charts.ts
@src/lib/services/chart-parse.ts
@src/lib/services/home-layout.ts

<interfaces>
```typescript
// $lib/services/home-charts (39-05)
export interface ChartPlanConfig { region: ChartRegion; extraRegions: readonly ChartRegion[]; genres: readonly ChartGenre[]; hidden: readonly string[] }
export type ChartTask = { key: string; section: ChartSectionId } & ({ src: 'apple'; kind: 'songs' | 'albums'; cc: ChartRegion } | { src: 'kkbox'; kind: 'song' | 'newrelease'; cc: ChartRegion } | { src: 'yt'; kind: 'tracks' | 'artists'; cc: ChartRegion } | { src: 'genre'; genre: ChartGenre });
export function planChartShelves(cfg: ChartPlanConfig): ChartTask[];
export function poolKey(section: 'chart-songs' | 'new-releases' | 'chart-artists' | 'chart-albums' | 'yt-trending' | 'region', cc: ChartRegion): string;
export function genrePoolKey(genre: ChartGenre): string;
export function samplePicks(len: number, n: number): number[];
export function regionLabel(cc: string, lang: string): string;
export function chartAlbumHref(album: ChartAlbum): string;
export const CHART_GENRE_LABEL: Record<ChartGenre, TranslationKey>;
export const HOME_CACHE_KEY: string; export const LEGACY_HOME_CACHE_KEYS: readonly string[]; export const POOL_STALE_MS: number; export const POOL_CAP: number;
// $lib/services/charts (39-04)
export function appleSongs(cc, signal?): Promise<DiscoveryTrack[]>; appleAlbums(cc): Promise<ChartAlbum[]>; kkboxSongs(cc); kkboxNewReleases(cc); ytTracks(cc); ytArtists(cc): Promise<DiscoveryArtist[]>; genreChart(genre: ChartGenre): Promise<DiscoveryTrack[]>;
// $lib/services/chart-parse (39-01): fuseCharts(lists: DiscoveryTrack[][]): DiscoveryTrack[]; interface ChartAlbum { name; artist; image }
// $lib/services/home-layout (39-02): resolveChartRegion(saved, appLang, navLang?), resolveExtraRegions(saved, main), resolveChartGenres(saved), CHART_SECTIONS, CLASSIC_SECTIONS, type ChartGenre, type ChartRegion
// $lib/stores/settings.svelte (39-06): settings.homeChartRegion, settings.homeExtraRegions, settings.homeChartGenres, settings.appLang
// page-local (existing): FANOUT_CAP=4, refreshGen, densityOf(id), compactSlice, tileCover(item), playStub(item), tileMenu(item), discoveryShelf(items, density), topArtistsBlock, titleNav(label, dest), compactSkeletonColumn(), shelfCount(id), shelfBudget, hideOnError, fallbackCover(seed)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the home orchestration — v3 cache, chart task runner, gated classic refresh, fallback gate, silent revalidate, local Randomize</name>
  <read_first>
    - src/routes/(app)/+page.svelte lines 1-120 (imports, CACHE_KEY, ShelfCache), 149-159 (pickN — leave as is), 319-357 (scheduleBackfill, hasAnyDiscovery), 359-398 (configSig, saveCache, loadCache, applyCache), 400-544 (RANDOM_PAGE_BOUND + refresh() — every decision-ref comment in here stays), 546-556 (playStub), 618-645 (reveal budget, shelfCount), 691-758 (onMount), 776-784 (Randomize button + skeleton condition)
    - src/lib/services/home-charts.ts, src/lib/services/charts.ts, src/lib/services/chart-parse.ts (fuseCharts), src/lib/services/home-layout.ts (39-02 exports)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Pattern 6, §Common Pitfalls 1, 2, 4
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md §Randomize + first render (the "set B not A" contract + next-visit refresh) and §Old shelves (hidden = zero requests; fallback only when every visible shelf is empty)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §1.5, §1.7 "Background refresh" / "Randomize" / "Global skeleton", §1.8
  </read_first>
  <files>src/routes/(app)/+page.svelte</files>
  <action>
    All edits in the `<script>` of `src/routes/(app)/+page.svelte`. Keep every existing decision-ref comment (WR-02/WR-04/VX2/w87/D-06/hhd/pgu/…); add `39-D-26…39-D-32` comments as noted. Imports to add: `planChartShelves, poolKey, genrePoolKey, samplePicks, regionLabel, chartAlbumHref, CHART_GENRE_LABEL, HOME_CACHE_KEY, LEGACY_HOME_CACHE_KEYS, POOL_STALE_MS, POOL_CAP, type ChartTask` from `$lib/services/home-charts`; `appleSongs, appleAlbums, kkboxSongs, kkboxNewReleases, ytTracks, ytArtists, genreChart` from `$lib/services/charts`; `fuseCharts, type ChartAlbum` from `$lib/services/chart-parse`; `resolveChartRegion, resolveExtraRegions, resolveChartGenres, CHART_SECTIONS, CLASSIC_SECTIONS, type ChartGenre, type ChartRegion` from `$lib/services/home-layout`.

    1. Cache (39-D-26): delete the page-local `CACHE_KEY` constant and use `HOME_CACHE_KEY`. Define `type ChartPools = { songs: Record<string, DiscoveryTrack[]>; artists: Record<string, DiscoveryArtist[]>; albums: Record<string, ChartAlbum[]> }` and change `ShelfCache` to `v: 3` plus `pools: ChartPools`, `picks: Record<string, number[]>`, `fetchedAt: number` (keep the classic fields + `useFallback`/`fallback`/`cfg`). State: `let pools = $state<ChartPools>({ songs: {}, artists: {}, albums: {} })`, `let picks = $state<Record<string, number[]>>({})`; plain (non-`$state`) fields `let poolsFetchedAt = 0` and `let pendingPools: { pools: ChartPools; fetchedAt: number } | null = null`. `loadCache()` accepts only `v === 3` with a shape guard (`pools` is an object with `songs`/`artists`/`albums` objects, `picks` is an object, `fetchedAt` is a number) — anything else → null (an old v2 blob simply forces a cold refresh). `saveCache()` becomes argument-less: builds `{ v: 3, cfg: configSig(), fetchedAt: poolsFetchedAt, pools, picks, topHits, topArtists, tagShelves, countryShelves, useFallback, fallback: fallbackSongs }`, writes it under `HOME_CACHE_KEY`, then `localStorage.removeItem` each of `LEGACY_HOME_CACHE_KEYS` (try/catch each). `applyCache(c)` additionally assigns `pools`, `picks`, `poolsFetchedAt = c.fetchedAt`.
    2. Resolved config as `$derived`: `chartRegion = resolveChartRegion(settings.homeChartRegion, settings.appLang, typeof navigator !== 'undefined' ? navigator.language : undefined)`, `chartExtraRegions = resolveExtraRegions(settings.homeExtraRegions, chartRegion)`, `chartGenres = resolveChartGenres(settings.homeChartGenres)`, `classicVisible = CLASSIC_SECTIONS.some(id => !settings.homeHidden.includes(id))`. A function `chartTasks(): ChartTask[]` returns `planChartShelves({ region: chartRegion, extraRegions: chartExtraRegions, genres: chartGenres, hidden: settings.homeHidden })`.
    3. `configSig()` (39-D-27, RESEARCH Pitfall 4): JSON of `{ s: clampShelfSize(...), t: tagsVisible ? resolveSubset(homeTags, DISCOVERY_TAGS) : [], c: countriesVisible ? resolveSubset(homeCountries, DISCOVERY_COUNTRIES) : [], r: chartRegion, x: chartExtraRegions, g: chartGenres, v: [...CHART_SECTIONS, ...CLASSIC_SECTIONS].filter(id => !settings.homeHidden.includes(id)) }` — hidden now gates fetching, so un-hiding a section MUST change the signature.
    4. Sampled accessors (pure functions of state): `sampledSongs(key)`, `sampledArtists(key)`, `sampledAlbums(key)` = `(picks[key] ?? []).flatMap(i => pool[i] ? [pool[i]] : [])` over the matching `pools.*[key] ?? []` (an out-of-range persisted index is simply skipped — T-39-29). `$derived` lists `genreShelves = chartGenres.map(g => ({ id: g, key: genrePoolKey(g), items: sampledSongs(genrePoolKey(g)) })).filter(s => s.items.length)` and `regionShelves = chartExtraRegions.map(cc => ({ cc, key: poolKey('region', cc), items: sampledSongs(poolKey('region', cc)) })).filter(s => s.items.length)`.
    5. Task runner (39-D-28): `fetchTask(task)` switches on `task.src`/`task.kind` to the seven charts.ts functions (`genre` → `genreChart(task.genre)`), returning `{ kind: 'songs' | 'artists' | 'albums', items }`. `runChartTasks(tasks, gen, apply)`: group tasks by `key` (a `Map<string, ChartTask[]>` in first-seen order); `await mapWithConcurrency([...groups.values()], FANOUT_CAP, async (group) => { results = await Promise.all(group.map(fetchTask)); if (gen !== refreshGen) return; …assign })` where a songs group with 2+ results is `fuseCharts(orderedLists)` with the APPLE task's list FIRST (display precedence per CONTEXT — Apple's name wins), a 1-result group is its list as-is; cap with `.slice(0, POOL_CAP)`; `newPicks = samplePicks(items.length, clampShelfSize(settings.homeShelfSize))`; write into a local `out: ChartPools` + `outPicks`; and when `apply` also into the reactive `pools.<kind>[key]` / `picks[key]` (assign AS EACH GROUP LANDS so one hanging Apple call never holds back KKBOX/YT shelves — RESEARCH Pitfall 9). Return `{ pools: out, picks: outPicks }`. Every charts.ts call is never-throw, so no try/catch inside.
    6. `refresh(seedQueue, background, randomize)` (keep the signature and the WR-02/WR-04/VX2 semantics): after `buildLibraryShelves` + `gen = ++refreshGen` + `loading`/`error` handling — (a) NEW SHELVES: if `randomize` → `redrawPicks()` (39-D-29: if `pendingPools` exists adopt it into `pools` + `poolsFetchedAt` first — a Randomize press samples from the fresh pool; then `picks[key] = samplePicks(len, N)` for every key in all three pool maps; ZERO requests); else `tasks = chartTasks()`; if non-empty `await runChartTasks(tasks, gen, true)`, gen-check, `poolsFetchedAt = Date.now()`. (b) CLASSIC, gated (39-D-30, RESEARCH Pitfall 1): wrap the existing dzChart + tag/country + Last.fm-fallback block in `if (classicVisible)`; inside, `tagPool = tagsVisible ? resolveSubset(...) : []`, `countryPool = countriesVisible ? … : []`, and `dzChart = (topHitsVisible || topArtistsVisible) ? await deezerChart(perShelf) : EMPTY` (the `getChartTopTracks`/`getChartTopArtists` fallbacks likewise only when their section is visible). When `classicVisible` is false, set the four classic states to `[]`. The random-page trick (`pg()`) and the randomize shuffles stay exactly as they are for classic. (c) FALLBACK: replace `hasAnyDiscovery(...)` with `hasAnyContent()` = any planned pool key (from `chartTasks()`) with sampled items, OR the four classic shelves, OR any VISIBLE library shelf (`liked`/`downloads`/`history`/`radio`/`playlists`/`fav-artists` non-empty and not hidden) — UI-SPEC §1.8 + RESEARCH Pitfall 2. Primary path: assign classic state, `useFallback = false`, `fallbackSongs = []`, `saveCache()`, `scheduleBackfill()`. Fallback path unchanged apart from `saveCache()` (pools are kept in the payload even when empty).
    7. `scheduleBackfill()` (39-D-31): extend `rows` with imageless sampled items from every planned song pool key and `artistNames` with imageless sampled artists from `pools.artists` — normally none (art is embedded), so the backfill stays a rare backup.
    8. Mount (39-D-32, CONTEXT "stale refresh lands on the NEXT visit"): keep `if (cached.cfg !== configSig()) void refresh(false, true)` and add `else if (Date.now() - cached.fetchedAt > POOL_STALE_MS) void revalidatePools()`. `revalidatePools()`: `gen = refreshGen` (NO bump — it must never supersede a user action; it bails if anything else starts), `tasks = chartTasks()`, `fresh = await runChartTasks(tasks, gen, false)`, gen-check, then `pendingPools = { pools: merge(fresh.pools over current pools), fetchedAt: Date.now() }` and write the cache with the merged pools, `picks` = fresh picks over current picks, `fetchedAt` = now, `cfg` = configSig() — WITHOUT assigning `pools`/`picks` state (nothing swaps on screen; the next mount or a Randomize press uses it).
    9. `shelfCount(id)`: `genres` → `genreShelves.length`; `regions` → `regionShelves.length`; `chart-songs`/`new-releases`/`yt-trending` → 1 if the sampled songs for `poolKey(id, chartRegion)` are non-empty else 0; `chart-artists` → same over artists; `chart-albums` → over albums (39-08 upgrades these to count planned placeholders).
    10. Template-adjacent script: `playStub(item, cover: string | null = item.image)` passes `cover` to `player.playStub` (UI-15 hook for Trending). Randomize button: `disabled={loading && classicVisible}` and label `loading && classicVisible ? t('home.loadingPicks') : t('home.randomize')` (UI-SPEC §1.7: the disabled/"Loading…" state is confined to classic). Skeleton condition (line 784): `loading && !useFallback && !hasAnyContent() && !fallbackSongs.length`.
    Do NOT touch `pickN`, `buildLibraryShelves`, the library cache, or the share-arrival block.
  </action>
  <verify>
    <automated>pnpm check && pnpm vitest --run src/lib/services/home-charts.test.ts src/lib/services/charts.test.ts src/lib/services/chart-parse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openmusic:top-picks:v2" "src/routes/(app)/+page.svelte"` prints 0 and `grep -n "HOME_CACHE_KEY\|LEGACY_HOME_CACHE_KEYS" "src/routes/(app)/+page.svelte"` matches ≥ 3 lines
    - `grep -n "planChartShelves(\|fuseCharts(\|samplePicks(\|POOL_STALE_MS\|mapWithConcurrency(" "src/routes/(app)/+page.svelte"` shows all five identifiers
    - `grep -n "classicVisible" "src/routes/(app)/+page.svelte"` matches ≥ 3 lines (derived, refresh gate, Randomize button)
    - `grep -c "hasAnyDiscovery" "src/routes/(app)/+page.svelte"` prints 0 and `grep -c "hasAnyContent()" "src/routes/(app)/+page.svelte"` ≥ 2
    - `grep -n "pendingPools" "src/routes/(app)/+page.svelte"` matches ≥ 3 lines and `grep -n "let pendingPools" "src/routes/(app)/+page.svelte"` does not contain `$state`
    - `grep -n "WR-04\|WR-02\|VX2\|D-06\|quick-260607-hhd\|quick-260924-pgu" "src/routes/(app)/+page.svelte"` still matches (no decision-ref comment removed)
    - `pnpm check` exits 0; the listed vitest files pass
  </acceptance_criteria>
  <done>The page plans, fetches, fuses, persists, samples and revalidates the chart pools with the gates and guards above; classic is fetched only when visible; nothing renders yet for the new ids (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Render the seven new sections — titleStatic, song shelves, artist shelf, genres and regions</name>
  <read_first>
    - src/routes/(app)/+page.svelte lines 811-832 (render loop), 835-843 (titleNav), 873-923 (topHitsBlock / topArtistsBlock — the artist body to extract), 925-987 (discoveryShelf, tagsBlock, countriesBlock — the per-group `{#each … slice(0, shelfBudget.per.X)}` idiom), 1137-1158 (`.subhead-nav` CSS)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §1.1 (titles + keys + render-when), §1.2 (titleStatic markup/CSS), §1.3 (density bodies), §1.4, §1.6 (taps; Trending null cover = UI-15, accepted at plan review), §1.9
    - src/lib/services/home-charts.ts (`regionLabel`, `CHART_GENRE_LABEL`, `poolKey`, `genrePoolKey`)
  </read_first>
  <files>src/routes/(app)/+page.svelte</files>
  <action>
    In the template of `src/routes/(app)/+page.svelte`:
    1. Add `{#snippet titleStatic(label: string)}` → `<h3 class="subhead-static"><span class="subhead-label">{label}</span></h3>` (UI-SPEC §1.2: the new shelves have NO See-all page — 39-D-33; a chevron leading nowhere is a false affordance; `.subhead-label` gives the ellipsis). CSS: turn the `.subhead-nav { … }` rule into a shared `.subhead-nav, .subhead-static { … }` block for the box + type values (width, flex, align, gap, min-height 44px, margin `14px 16px 14px 0`, padding, padding-right 6px, background none, border none, text-align left, color, font-size calc, weight 700) and keep `cursor: pointer` plus the `@media (hover: hover)` hover rule on `.subhead-nav` ONLY; do not copy values into a second block.
    2. Extract the body of `topArtistsBlock` (everything after its `titleNav`) into `{#snippet artistShelf(artists: DiscoveryArtist[], density: HomeDensity)}` and make `topArtistsBlock` call `{@render artistShelf(topArtists, densityOf('top-artists'))}` — a pure extraction, zero behaviour change (the list/grid/pile branches, `artistGridTile`, `ShelfChevrons` placement and the quick-260919-et3 comment move verbatim).
    3. Give `discoveryShelf` a third parameter `coverOnPlay = true` and change its three `playStub(item)` calls to `playStub(item, coverOnPlay ? item.image : null)` (long-press `tileMenu(item)` unchanged).
    4. Add the seven arms to the render loop in the same style as `{:else if id === 'radio'}{@render radioBlock()}`: `chart-songs` → `chartSongsBlock`, `new-releases` → `newReleasesBlock`, `chart-artists` → `chartArtistsBlock`, `chart-albums` → `chartAlbumsBlock` (Task 3), `yt-trending` → `ytTrendingBlock`, `genres` → `genresBlock`, `regions` → `regionsBlock`.
    5. Snippets (each renders NOTHING — no header — when its items are empty, like the library blocks): `chartSongsBlock` → `{@const items = sampledSongs(poolKey('chart-songs', chartRegion))}` then `{#if items.length}` `titleStatic(t('home.chartSongs', { region: regionLabel(chartRegion, settings.appLang) }))` + `discoveryShelf(items, densityOf('chart-songs'))`; `newReleasesBlock` same with `home.newReleases` and `poolKey('new-releases', chartRegion)`; `ytTrendingBlock` same with `home.ytTrending`, `poolKey('yt-trending', chartRegion)` and `discoveryShelf(items, densityOf('yt-trending'), false)` — add the 39-D-34 comment: UI-15 / research A12 — Google-hosted thumbnails (some 16:9 i.ytimg frames, often unloadable for CN-facing users) blank the NowPlaying hero because it paints the cover as a CSS background with no error event; passing `null` lets `play()` run the normal iTunes-first cover chain while the tile itself still shows the thumbnail; a deliberate deviation from CONTEXT's tap line, accepted at plan review; `chartArtistsBlock` → `sampledArtists(poolKey('chart-artists', chartRegion))`, `titleStatic(t('home.chartArtists', {region}))`, `artistShelf(items, densityOf('chart-artists'))`; `genresBlock` → `{#each genreShelves.slice(0, shelfBudget.per.genres ?? 0) as shelf (shelf.key)}` `titleStatic(t(CHART_GENRE_LABEL[shelf.id]))` + `discoveryShelf(shelf.items, densityOf('genres'))`; `regionsBlock` → `{#each regionShelves.slice(0, shelfBudget.per.regions ?? 0) as shelf (shelf.key)}` `titleStatic(t('home.chartSongs', { region: regionLabel(shelf.cc, settings.appLang) }))` + `discoveryShelf(shelf.items, densityOf('regions'))`.
    Text goes through `names.dnTitle` / `names.dnArtist` inside the reused snippets already; covers through `tileCover(item)` (embedded art wins, backfill is the backup). No source attribution on Home (UI-SPEC §1.9).
  </action>
  <verify>
    <automated>pnpm check && pnpm vitest --run src/lib/i18n/i18n.test.ts src/lib/services/home-charts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "{#snippet titleStatic\|{#snippet artistShelf\|{#snippet chartSongsBlock\|{#snippet newReleasesBlock\|{#snippet chartArtistsBlock\|{#snippet ytTrendingBlock\|{#snippet genresBlock\|{#snippet regionsBlock" "src/routes/(app)/+page.svelte"` prints 8
    - `grep -c "id === 'chart-songs'\|id === 'new-releases'\|id === 'chart-artists'\|id === 'chart-albums'\|id === 'yt-trending'\|id === 'genres'\|id === 'regions'" "src/routes/(app)/+page.svelte"` ≥ 7
    - `grep -n "\.subhead-nav, \.subhead-static\|\.subhead-static, \.subhead-nav" "src/routes/(app)/+page.svelte"` matches, and `grep -n "cursor: pointer" "src/routes/(app)/+page.svelte"` is not inside the shared block (the shared block must not contain `cursor`)
    - `grep -n "39-D-34" "src/routes/(app)/+page.svelte"` matches and `grep -n "densityOf('yt-trending'), false)" "src/routes/(app)/+page.svelte"` matches
    - `grep -c "titleNav(" "src/routes/(app)/+page.svelte"` is unchanged from before this task (classic + library shelves keep See-all)
    - `pnpm check` exits 0
    - Dev smoke (browser pane, fresh localStorage, `pnpm dev`): the home shows "Top songs · Hong Kong" (or the resolved region) with covers; the network log shows `/api/charts?src=…` calls, `itunes.apple.com/…/genre=1251` from the browser, and `/api/deezer/chart?genre=116`; use back/forward to mount all shelves (pane rAF is frozen)
  </acceptance_criteria>
  <done>Six of the seven new sections render with static region-qualified titles in the user's order; artist body is shared with the classic Top artists; Trending resolves with a null cover.</done>
</task>

<task type="auto">
  <name>Task 3: CompactRow album variant + the Top albums shelf with zero-call album taps</name>
  <read_first>
    - src/lib/components/CompactRow.svelte lines 1-60 (header, Props.variant) and 99-135 (artist branch — the template to mirror), plus the `.crow.is-artist` CSS rule (quick-260919-et3) further down
    - src/routes/(app)/+page.svelte `discoveryShelf` (Task 2 result — the list/grid/pile skeleton to mirror for albums) and `artistShelf`
    - src/lib/services/home-charts.ts (`chartAlbumHref`), src/lib/services/chart-parse.ts (`ChartAlbum`)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-UI-SPEC.md §1.3 (Album row: `CompactRow variant="album"` minimal contract; pile/grid tile anatomy), §1.6 (album tap), UI-14
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Pattern 7 (album path verified against Last.fm tracklists)
  </read_first>
  <files>src/lib/components/CompactRow.svelte, src/routes/(app)/+page.svelte</files>
  <action>
    `src/lib/components/CompactRow.svelte` (39-D-35): extend `Props.variant` to `'track' | 'artist' | 'album'` and add a header line: `'album': 40×40 SQUARE art + title/subtitle (both marquee), the whole row is ONE button firing onopen; NO ⋮, NO long-press, NO RowBadges (album tiles have no song to resolve — parity with the artist variant)`. Add an `{:else if variant === 'album'}` branch before the track `{:else}`: `<button class="crow is-artist" use:tapBounce onclick={() => onopen?.()}>` with `<span class="art" style:background-image={effectiveCover ? `url(${effectiveCover})` : fallbackGradient(seed)}></span>` (square — no `round`) and a `.meta` holding `.r-title` (marquee, `{title}`) and `.r-sub` (marquee, `{subtitle}`). Reuse the `is-artist` class and extend its quick-260919-et3 comment: the album row is the same direct-child-of-pager-column situation, so the one CSS rule applies to both. Nothing else in the component changes.

    `src/routes/(app)/+page.svelte`: add `{#snippet albumShelf(items: ChartAlbum[], density: HomeDensity)}` structurally mirroring `discoveryShelf`: `list` → `<CompactPager items={compactSlice(items)} key={(a) => a.artist + ' ' + a.name}>` with `<CompactRow variant="album" title={names.dnTitle(a.name)} subtitle={names.dnArtist(a.artist)} cover={a.image} seed={a.artist + a.name} onopen={() => goto(chartAlbumHref(a))} />`; `grid` → `HomeGridPager items={items.slice(0, 27)}` with a `<button class="tile" use:tapBounce onclick={() => goto(chartAlbumHref(a))}>` holding `.art` gradient + `<img class="al-cover-img">` when `a.image` + `.scrim` + `.label` (`.t-title` = name, `.t-artist` = artist); `pile` → `.albumrow use:dragScroll` of `<button class="album" use:tapBounce onclick={goto(chartAlbumHref(a))}>` with square `.al-cover` (gradient seed `a.artist + a.name`, `al-cover-img` when image, `onerror={hideOnError}`), `.al-name` = name, `.al-count` = artist, followed by `<ShelfChevrons />` as the IMMEDIATE sibling (et3). NO `use:longpress` anywhere in this snippet (UI-14). Then `{#snippet chartAlbumsBlock()}`: `{@const items = sampledAlbums(poolKey('chart-albums', chartRegion))}` → if non-empty `titleStatic(t('home.chartAlbums', { region: regionLabel(chartRegion, settings.appLang) }))` + `albumShelf(items, densityOf('chart-albums'))`. Comment 39-D-36 on the tap: `chartAlbumHref` = the existing name-only `/album/{name}?artist=` page (Last.fm `album.getinfo` tracklist + enrichAlbum + deezerAlbum), zero calls at tap; Apple's ` - EP` / ` - Single` is stripped at parse AND in the href builder.
  </action>
  <verify>
    <automated>pnpm check && pnpm vitest --run src/lib/services/home-charts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "variant === 'album'" src/lib/components/CompactRow.svelte` matches and `grep -n "'track' | 'artist' | 'album'" src/lib/components/CompactRow.svelte` matches
    - the album branch of CompactRow contains no `use:longpress`, no `MoreVertical`, no `RowBadges` (`awk '/variant === .album./,/{:else}/' src/lib/components/CompactRow.svelte | grep -c "longpress\|MoreVertical\|RowBadges"` prints 0)
    - `grep -c "{#snippet albumShelf\|{#snippet chartAlbumsBlock" "src/routes/(app)/+page.svelte"` prints 2 and `grep -c "chartAlbumHref(" "src/routes/(app)/+page.svelte"` ≥ 3 (list/grid/pile taps)
    - `awk '/{#snippet albumShelf/,/{\/snippet}/' "src/routes/(app)/+page.svelte" | grep -c "use:longpress"` prints 0
    - `pnpm check` exits 0
    - Dev smoke: tapping a Top albums tile navigates to `/album/<name>?artist=<artist>` and the album page lists tracks; tapping a Top songs tile seats the song in the now-bar (CN upstream permitting)
  </acceptance_criteria>
  <done>All seven sections render; album rows/tiles open the existing album page with no fetch at tap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage `openmusic:top-picks:v3` → page state | pools / picks / cfg are user-writable |
| settings (region / genres / hidden) → fetch plan | resolved through 39-02 resolvers before planning |
| chart items → `goto()` / `playStub` | names and image URLs from upstreams (edge-validated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-28 | Tampering | loadCache v3 | mitigate | `v === 3` + shape guard (objects/arrays/number); anything else → null → cold refresh; never throws (try/catch) |
| T-39-29 | Tampering | persisted `picks` indices | mitigate | sampled accessors skip out-of-range/undefined entries; pools capped at POOL_CAP on write |
| T-39-30 | DoS (request flood) | refresh() | mitigate | tasks only from `planChartShelves` (hidden ⇒ none); `mapWithConcurrency(FANOUT_CAP)` over key-groups; every call via `apiFetch` + 6 h memo; classic fetches gated on visibility; silent revalidate does not bump `refreshGen` and runs at most once per mount |
| T-39-31 | Tampering (navigation) | album / artist taps | mitigate | fixed in-app paths (`/artist/` + encodeURIComponent, `chartAlbumHref` → `albumHref`); no user-controlled destination |
| T-39-32 | Tampering | chart image URLs on tiles | mitigate | edge `safeImageUrl` already validated them; rendered as `<img src>` with `onerror` hide, never a CSS url() from an untrusted string on Home tiles (the hero risk is what UI-15's null cover avoids) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase |
</threat_model>

<verification>
- `pnpm check` green; `pnpm test` green after the wave.
- Browser pane (fresh profile, dev server): cold home request count ≈ 13 for hk (5 chart + 4 iTunes + 4 Deezer genre) with classic still visible at THIS deploy adding the classic calls only because they are visible; hide the four classic sections in /settings/home and reload → `/api/lastfm/discovery` and bare `/api/deezer/chart` (no `genre=`) do NOT appear (VALIDATION manual row P39-07).
- Reload keeps the same tiles (persisted picks); Randomize changes tiles with zero new `/api/charts` requests.
- Set `openmusic:top-picks:v3`.fetchedAt to now − 7 h in devtools, reload: tiles unchanged, one background batch of `/api/charts` requests, cache `fetchedAt` updated; reload again → fresh sample.
</verification>

<success_criteria>
- All seven sections render from sampled pools with static region-qualified titles; song/artist/album taps behave per UI-SPEC §1.6.
- Hidden = zero requests holds for new AND classic shelves; fallback grid only when everything visible is empty.
- Persisted arrangement survives reload; Randomize is local; stale pools land next visit.
</success_criteria>

<output>
Create `.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-07-SUMMARY.md` when done. Record the observed cold request count and the exact `/api/charts` URLs seen.
</output>
