---
phase: 39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/chart-parse.ts
  - src/lib/services/chart-parse.test.ts
  - src/lib/services/__fixtures__/charts/apple-hk-songs.json
  - src/lib/services/__fixtures__/charts/apple-hk-albums.json
  - src/lib/services/__fixtures__/charts/kkbox-hk-song.json
  - src/lib/services/__fixtures__/charts/kkbox-hk-newrelease.json
  - src/lib/services/__fixtures__/charts/yt-hk-tracks.json
  - src/lib/services/__fixtures__/charts/yt-hk-artists.json
  - src/lib/services/__fixtures__/charts/yt-cn-global.json
  - src/lib/services/__fixtures__/charts/itunes-hk-1251.json
  - src/lib/services/__fixtures__/charts/itunes-hk-1251-single.json
  - src/lib/services/__fixtures__/charts/itunes-bogus.json
  - src/lib/services/__fixtures__/charts/deezer-116.json
  - src/lib/proxy/safe-image-url.ts
  - src/lib/proxy/safe-image-url.test.ts
autonomous: true
requirements: [P39-01, P39-09, P39-13]
must_haves:
  truths:
    - "Every Apple / KKBOX / YouTube / legacy-iTunes chart response is parsed into DiscoveryTrack / DiscoveryArtist / ChartAlbum items by a pure function that never throws on null, {}, [] or a string"
    - "A YouTube Charts body whose echoed countryCode differs from the requested cc parses to [] (the global-fallback 200 is never trusted)"
    - "A legacy iTunes feed row survives only when category.attributes['im:id'] equals the requested genre id; a single-entry feed (object, not array) parses to one row"
    - "KKBOX / YouTube artist names lose a trailing Latin alias (田馥甄 (Hebe) → 田馥甄); Apple album names lose ' - EP' / ' - Single'"
    - "fuseCharts merges two ranked lists with reciprocal-rank fusion: a song on both charts outranks single-chart peers, Apple's display name wins on overlap, one empty list yields the other alone"
    - "Image URLs from mzstatic / i.kfs.io / googleusercontent / i.ytimg / yt3.ggpht pass the allowlist; look-alikes and apexes are rejected"
  artifacts:
    - path: "src/lib/services/chart-parse.ts"
      provides: "parseAppleRss, parseKkbox, parseYtCharts, parseItunesGenreFeed, fuseCharts, stripLatinAlias, stripReleaseSuffix, resizeMzstatic, resizeYtThumb, ChartAlbum, ImageValidator"
      exports: ["parseAppleRss", "parseKkbox", "parseYtCharts", "parseItunesGenreFeed", "fuseCharts", "stripLatinAlias", "stripReleaseSuffix", "resizeMzstatic", "resizeYtThumb"]
    - path: "src/lib/services/chart-parse.test.ts"
      provides: "fixture-driven parser + fusion tests (Wave 0 for P39-01 / P39-13)"
      min_lines: 120
    - path: "src/lib/proxy/safe-image-url.ts"
      provides: "APPLE_IMAGE_HOSTS, KKBOX_IMAGE_HOSTS, YOUTUBE_IMAGE_HOSTS"
      contains: "YOUTUBE_IMAGE_HOSTS"
    - path: "src/lib/services/__fixtures__/charts/yt-cn-global.json"
      provides: "the global-fallback body with countryCode 'global' kept at the perspectiveMetadata path"
      contains: "countryCode"
  key_links:
    - from: "src/lib/services/chart-parse.ts fuseCharts"
      to: "src/lib/services/match-key.ts matchKey"
      via: "import — the fusion identity key (its norm() already drops bracketed aliases)"
      pattern: "import \\{ matchKey \\} from '\\$lib/services/match-key'"
    - from: "src/lib/services/chart-parse.ts parseYtCharts"
      to: "echoed countryCode"
      via: "equality gate before any row is read"
      pattern: "countryCode"
---

<objective>
Build the pure, node-tested data layer every later plan consumes: the four chart parsers, the name-hygiene helpers, the reciprocal-rank fusion for Top Songs (KKBOX + Apple), the three new image-host allowlists, and the trimmed real-response fixtures that pin all of it.

Purpose: every new upstream here is undocumented or lies on bad input (YouTube returns a global chart with a 200, iTunes returns the overall chart for a bogus genre id). The validation therefore lives in pure parsers with real fixtures, so the edge route (39-03) and the client services (39-04) are thin callers with nothing to get wrong.
Output: `src/lib/services/chart-parse.ts` + test, 11 fixture files, extended `safe-image-url.ts` + test.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md
@.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-PATTERNS.md
@.claude/skills/spike-findings-openmusic/references/home-charts.md
@src/lib/proxy/safe-image-url.ts
@src/lib/proxy/safe-image-url.test.ts
@src/lib/services/match-key.ts
@src/routes/api/deezer/chart/+server.ts
@src/lib/sources/kuwo.test.ts

<interfaces>
<!-- Contracts THIS plan creates. Later plans import these exactly; do not rename. -->

From src/lib/services/chart-parse.ts (NEW — the shapes below are the contract):
```typescript
import type { DiscoveryTrack, DiscoveryArtist } from '$lib/services/lastfm'; // type-only, erased
export interface ChartAlbum { name: string; artist: string; image: string | null }
/** The image validator is a PARAMETER so the same parser serves the edge (safeImageUrl + APPLE_IMAGE_HOSTS) and the client iTunes feed. */
export type ImageValidator = (u?: string | null) => string | null;
export function resizeMzstatic(url: string | null | undefined, px?: number): string | null; // /\/\d+x\d+bb\./ → /{px}x{px}bb.  default px=600
export function resizeYtThumb(url: string | null | undefined): string | null;              // '=w180-h180-l90-rj' → '=w544-h544-l90-rj'; other hosts untouched
export function stripLatinAlias(name: string): string;      // '田馥甄 (Hebe)' → '田馥甄'; ASCII-only names untouched
export function stripReleaseSuffix(name: string): string;   // 'X - EP' | 'X - Single' → 'X'
export function parseAppleRss(data: unknown, kind: 'songs', img: ImageValidator): DiscoveryTrack[];
export function parseAppleRss(data: unknown, kind: 'albums', img: ImageValidator): ChartAlbum[];
export function parseKkbox(data: unknown, type: 'song' | 'newrelease', img: ImageValidator): DiscoveryTrack[];
export function parseYtCharts(data: unknown, cc: string, kind: 'tracks', img: ImageValidator): DiscoveryTrack[];
export function parseYtCharts(data: unknown, cc: string, kind: 'artists', img: ImageValidator): DiscoveryArtist[];
export function parseItunesGenreFeed(data: unknown, genreId: number, img: ImageValidator): DiscoveryTrack[];
export function fuseCharts(lists: DiscoveryTrack[][], k?: number, cap?: number): DiscoveryTrack[]; // k=60, cap=50
```

From src/lib/proxy/safe-image-url.ts (EXTENDED):
```typescript
export const APPLE_IMAGE_HOSTS: ImageHostAllowlist;   // { suffix: ['.mzstatic.com'] }
export const KKBOX_IMAGE_HOSTS: ImageHostAllowlist;   // { exact: ['i.kfs.io'], suffix: [] }
export const YOUTUBE_IMAGE_HOSTS: ImageHostAllowlist; // { exact: ['i.ytimg.com', 'yt3.ggpht.com'], suffix: ['.googleusercontent.com'] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture trimmed real-response fixtures and add the three image-host allowlists</name>
  <read_first>
    - src/lib/proxy/safe-image-url.ts (the ImageHostAllowlist shape and the DEEZER/LASTFM list style; do NOT touch safeImageUrl())
    - src/lib/proxy/safe-image-url.test.ts (case style to mirror for the three new lists)
    - src/lib/services/__fixtures__/README.md (fixture conventions: co-located, inert, never imported by app source)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Standard Stack "Upstreams" table + §Code Examples 1-3, 5 (exact request URLs, the YT POST body, the response shapes)
    - .claude/skills/spike-findings-openmusic/references/home-charts.md §"How to Build It" 2-3 (parser field names)
  </read_first>
  <files>
    src/lib/proxy/safe-image-url.ts,
    src/lib/proxy/safe-image-url.test.ts,
    src/lib/services/__fixtures__/charts/apple-hk-songs.json,
    src/lib/services/__fixtures__/charts/apple-hk-albums.json,
    src/lib/services/__fixtures__/charts/kkbox-hk-song.json,
    src/lib/services/__fixtures__/charts/kkbox-hk-newrelease.json,
    src/lib/services/__fixtures__/charts/yt-hk-tracks.json,
    src/lib/services/__fixtures__/charts/yt-hk-artists.json,
    src/lib/services/__fixtures__/charts/yt-cn-global.json,
    src/lib/services/__fixtures__/charts/itunes-hk-1251.json,
    src/lib/services/__fixtures__/charts/itunes-hk-1251-single.json,
    src/lib/services/__fixtures__/charts/itunes-bogus.json,
    src/lib/services/__fixtures__/charts/deezer-116.json
  </files>
  <action>
    Part A — allowlists (P39-09). In `src/lib/proxy/safe-image-url.ts`, append three exported constants after `LASTFM_IMAGE_HOSTS`, each with a one-line doc comment that states the apex rule explicitly, in the exact style of the two existing lists:
    - `APPLE_IMAGE_HOSTS: ImageHostAllowlist = { suffix: ['.mzstatic.com'] }` (Apple RSS `artworkUrl100` and the legacy feed's `im:image` both live on `is1-ssl.mzstatic.com` and siblings; apex not permitted).
    - `KKBOX_IMAGE_HOSTS: ImageHostAllowlist = { exact: ['i.kfs.io'], suffix: [] }` (every KKBOX cover is `i.kfs.io/…/fit/500x500.jpg`; `suffix` is a required field, so it is an explicit empty list — `kfs.io` and `notkfs.io` are rejected).
    - `YOUTUBE_IMAGE_HOSTS: ImageHostAllowlist = { exact: ['i.ytimg.com', 'yt3.ggpht.com'], suffix: ['.googleusercontent.com'] }` (RESEARCH finding 3: thumbnails come from yt3/lh3.googleusercontent.com AND i.ytimg.com (16:9 frames) AND yt3.ggpht.com; apex `googleusercontent.com` not permitted).
    Tag the block with a `39-D-01` decision-ref comment naming the three sources. Do not modify `safeImageUrl()` itself.

    In `src/lib/proxy/safe-image-url.test.ts`, add a `describe('safeImageUrl — chart image hosts (39-D-01)')` block mirroring the existing "accepts exact + subdomain" / "rejects lookalike" / "apex not permitted" cases for all three lists. Accept: `https://is1-ssl.mzstatic.com/image/thumb/a/600x600bb.jpg`, `https://i.kfs.io/album/global/1/fit/500x500.jpg`, `https://yt3.googleusercontent.com/a=w544-h544-l90-rj`, `https://lh3.googleusercontent.com/a`, `https://i.ytimg.com/vi/a/hqdefault.jpg`, `https://yt3.ggpht.com/a`. Reject: `https://evil-mzstatic.com/a.jpg`, `https://mzstatic.com/a.jpg`, `https://notkfs.io/a.jpg`, `https://kfs.io/a.jpg`, `https://googleusercontent.com/a`, `https://evil-ggpht.com/a`, and `http://i.kfs.io/a.jpg` (scheme).

    Part B — fixtures (Wave 0 for P39-01 / P39-04 / P39-13). Create `src/lib/services/__fixtures__/charts/` and capture each upstream LIVE (all reachable from this sandbox per project memory; RESEARCH probed them 2026-09-25), then TRIM to at most 5 rows while keeping the envelope structure intact. Write a throwaway node script in the session scratchpad directory (never in the repo) that fetches, trims and writes each file. Files and sources:
    - `apple-hk-songs.json` ← GET `https://rss.marketingtools.apple.com/api/v2/hk/music/most-played/50/songs.json`; keep `feed` with `results` sliced to 5 (fields per row: `id`, `name`, `artistName`, `artworkUrl100`, `releaseDate`, `url`; drop other keys if large).
    - `apple-hk-albums.json` ← same URL with `albums.json`; choose 5 rows such that at least one `name` ends with ` - EP` or ` - Single` (scan the full 50; if none today, take 4 real rows and append one real row with its `name` suffixed ` - Single`, and say so in a top-level `"_note"` field is NOT allowed — instead say so in the SUMMARY).
    - `kkbox-hk-song.json` ← GET `https://kma.kkbox.com/charts/api/v1/daily?type=song&terr=hk&lang=tc&category=297&limit=50`; keep `data.charts.song` sliced to 5 such that at least one `artist_name` carries a trailing Latin alias like `田馥甄 (Hebe)` (60-70% do). Row fields: `song_name`, `artist_name`, `album_name`, `cover_image.normal`, `release_date`.
    - `kkbox-hk-newrelease.json` ← same with `type=newrelease`; `data.charts.newrelease` sliced to 5.
    - `yt-hk-tracks.json` ← POST `https://charts.youtube.com/youtubei/v1/browse?alt=json` with header `content-type: application/json` and body `{"context":{"client":{"clientName":"WEB_MUSIC_ANALYTICS","clientVersion":"2.0","hl":"zh-TW","gl":"HK"}},"browseId":"FEmusic_analytics_charts_home","query":"perspective=CHART_DETAILS&chart_params_country_code=hk&chart_params_chart_type=TRACKS&chart_params_period_type=WEEKLY"}`. Trim: locate the array under the key `trackViews` (deep search) and slice it to 5; KEEP the path `contents.sectionListRenderer.contents[0].musicAnalyticsSectionRenderer.content.perspectiveMetadata.requestParams.chartParams.countryCode` (must equal `"hk"`); drop unrelated top-level keys (`responseContext` etc.) to keep the file small. Row fields used: `name`, `artists[].name`, `encryptedVideoId`, `thumbnail.thumbnails[].url`.
    - `yt-hk-artists.json` ← same with `chart_params_chart_type=ARTISTS`; the array key is `artistViews` (row fields: `name`, `thumbnail.thumbnails[].url`, `id`).
    - `yt-cn-global.json` ← same body with `chart_params_country_code=cn` and `gl:"CN"`; trim as for tracks; the echoed `countryCode` MUST read `"global"` (that is the whole point of this fixture — RESEARCH Pitfall 3). If today's response echoes `cn` instead, still save it and note the drift in the SUMMARY; the parser test then uses a hand-edited copy with `"global"`.
    - `itunes-hk-1251.json` ← GET `https://itunes.apple.com/hk/rss/topsongs/limit=100/genre=1251/json`; keep `feed.entry` sliced to 5 where every row has `category.attributes['im:id'] === "1251"` (row fields: `im:name.label`, `im:artist.label`, `im:image[].label`, `category.attributes.im:id`).
    - `itunes-hk-1251-single.json` ← derived from the file above: `feed.entry` is the FIRST entry as a bare OBJECT, not an array (the upstream does this when there is exactly one row).
    - `itunes-bogus.json` ← GET `https://itunes.apple.com/hk/rss/topsongs/limit=100/genre=99999/json` (a 200 with the OVERALL chart); slice `feed.entry` to 5. No row may carry `im:id` 99999.
    - `deezer-116.json` ← GET `https://api.deezer.com/chart/116/tracks?limit=50`; keep `{ data: [...5 rows], total }` (row fields: `title`, `artist.name`, `album.cover_xl`).
    Keep every file under ~15 KB and valid JSON (no comments, no `_note` keys). Fixtures are inert: Vitest only collects `*.test.ts`, and no app source imports them. If any single upstream is unreachable at execution time, hand-write that fixture from the exact shapes in RESEARCH §Code Examples and record which ones were synthetic in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/proxy/safe-image-url.test.ts && node -e "const d='src/lib/services/__fixtures__/charts/';for(const f of require('fs').readdirSync(d)){JSON.parse(require('fs').readFileSync(d+f,'utf8'))};console.log('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const APPLE_IMAGE_HOSTS\|export const KKBOX_IMAGE_HOSTS\|export const YOUTUBE_IMAGE_HOSTS" src/lib/proxy/safe-image-url.ts` prints 3
    - `grep -n "39-D-01" src/lib/proxy/safe-image-url.ts` matches at least one line
    - `pnpm vitest --run src/lib/proxy/safe-image-url.test.ts` passes with the new describe block containing ≥6 accept assertions and ≥7 reject assertions
    - `ls src/lib/services/__fixtures__/charts/ | wc -l` prints 11 and every file parses as JSON
    - `node -e "const j=require('./src/lib/services/__fixtures__/charts/yt-cn-global.json');console.log(j.contents.sectionListRenderer.contents[0].musicAnalyticsSectionRenderer.content.perspectiveMetadata.requestParams.chartParams.countryCode)"` prints `global`
    - `node -e "const j=require('./src/lib/services/__fixtures__/charts/itunes-hk-1251-single.json');console.log(Array.isArray(j.feed.entry))"` prints `false`
    - `node -e "const j=require('./src/lib/services/__fixtures__/charts/kkbox-hk-song.json');console.log(j.data.charts.song.some(r=>/\([\x20-\x7E]+\)\s*$/.test(r.artist_name)))"` prints `true`
  </acceptance_criteria>
  <done>Three allowlists exported and tested; 11 trimmed fixtures on disk, all valid JSON, with the YT global echo and the iTunes single-entry object preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the four pure parsers and the name-hygiene helpers against the fixtures</name>
  <read_first>
    - src/routes/api/deezer/chart/+server.ts lines 47-87 (`reshapeChart`: optional-field interfaces for untrusted JSON, slice → map → trim → safe image → filter blanks — the style to copy, with ZERO `as any`)
    - src/lib/services/match-key.ts (the "Pure module" header wording + the regex-chain helper style)
    - src/lib/sources/kuwo.test.ts lines 1-10 (JSON fixture import idiom)
    - src/lib/services/__fixtures__/charts/*.json (all 11 — the parsers are written against these exact shapes)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-RESEARCH.md §Code Examples 1, 2, 5 and §Common Pitfalls 3, 8
    - src/lib/services/lastfm.ts lines 168-181 (`DiscoveryTrack` / `DiscoveryArtist` — the output shapes)
  </read_first>
  <files>src/lib/services/chart-parse.ts, src/lib/services/chart-parse.test.ts</files>
  <behavior>
    - resizeMzstatic('https://is1-ssl.mzstatic.com/image/thumb/a/100x100bb.jpg') === 'https://is1-ssl.mzstatic.com/image/thumb/a/600x600bb.jpg'; '…/170x170bb.png' → '…/600x600bb.png'; null/undefined/'' → null
    - resizeYtThumb('https://yt3.googleusercontent.com/abc=w180-h180-l90-rj') === 'https://yt3.googleusercontent.com/abc=w544-h544-l90-rj'; 'https://i.ytimg.com/vi/x/hqdefault.jpg' unchanged; 'https://lh3.googleusercontent.com/x' unchanged; null → null
    - stripLatinAlias('田馥甄 (Hebe)') === '田馥甄'; '五月天 (Mayday)' → '五月天'; 'Taylor Swift (Deluxe)' unchanged; '(Hebe)' unchanged; '  周杰倫 ' → '周杰倫' (trimmed); '' → ''
    - stripReleaseSuffix('Short n Sweet - EP') === 'Short n Sweet'; 'Song - Single' → 'Song'; 'Show - Live' unchanged; 'X - ep' → 'X' (case-insensitive)
    - parseAppleRss(appleHkSongs, 'songs', img): length ≥ 1 and ≤ 50; every item has non-empty artist + title, mbid === null, and image either null or matching /^https:\/\/.*\/600x600bb\./
    - parseAppleRss(appleHkAlbums, 'albums', img): items are { name, artist, image }; no name ends with ' - EP' or ' - Single'
    - parseKkbox(kkboxHkSong, 'song', img): every artist passes stripLatinAlias unchanged (already stripped); images start with 'https://i.kfs.io/'
    - parseKkbox(kkboxHkNewrelease, 'newrelease', img) length ≥ 1; parseKkbox(kkboxHkSong, 'newrelease', img) → [] (wrong bucket is empty, not a throw)
    - parseYtCharts(ytHkTracks, 'hk', 'tracks', img): length ≥ 1; artist is artists[].name joined by ', ' (each alias-stripped); image passes YOUTUBE_IMAGE_HOSTS and a googleusercontent URL carries '=w544-h544'
    - parseYtCharts(ytHkArtists, 'hk', 'artists', img): items { name, image, mbid: null }
    - parseYtCharts(ytCnGlobal, 'cn', 'tracks', img) → [] AND parseYtCharts(ytHkTracks, 'tw', 'tracks', img) → [] (echo mismatch both ways)
    - parseItunesGenreFeed(itunesHk1251, 1251, img): length === number of fixture entries whose category.attributes['im:id'] === '1251'; parseItunesGenreFeed(itunesBogus, 99999, img) → []; parseItunesGenreFeed(itunesHk1251Single, 1251, img).length === 1; images end in /600x600bb\.(png|jpg)/
    - Every parser returns [] (never throws) for data = null, undefined, {}, [], 'garbage', 42
  </behavior>
  <action>
    Create `src/lib/services/chart-parse.ts` as a PURE module (header comment in the match-key.ts style: no runes, no `$app/*`, no store imports; imported by BOTH the edge proxy and the client — the resolve-cache.ts precedent of `$lib/proxy` importing `$lib/services/match-key`). Export exactly the signatures in the `<interfaces>` block. Rules:
    - Type every untrusted payload with optional-field interfaces (`AppleFeed { feed?: { results?: AppleRow[] } }`, `KkboxBody { data?: { charts?: Partial<Record<string, KkboxRow[]>> } }`, `ItunesFeed { feed?: { entry?: ItunesEntry | ItunesEntry[] } }`, a `YtBody` type for the echo path `contents.sectionListRenderer.contents[0].musicAnalyticsSectionRenderer.content.perspectiveMetadata.requestParams.chartParams.countryCode`) and narrow with `typeof`/`Array.isArray`; ZERO `as any` (CLAUDE.md). RESEARCH's sketches use `as any` as shorthand — do not copy that.
    - `stripLatinAlias`: match `/^(.*\S)\s*\(([\x20-\x7E]+)\)\s*$/`; strip only when the remainder contains a non-ASCII char, else return the trimmed input (so 'Taylor Swift (Deluxe)' is untouched).
    - `stripReleaseSuffix`: `/\s+-\s+(Single|EP)$/i` → '' then trim.
    - `resizeMzstatic(url, px = 600)`: replace `/\/\d+x\d+bb\./` with `/${px}x${px}bb.`; the legacy feed's 170x170bb.png and Apple's 100x100bb.jpg both match (39-D-02 comment: `upgradeArtwork()` in itunes-cover.ts only swaps the literal '100x100bb', which is why this is a new helper and not a reuse).
    - `resizeYtThumb`: replace a trailing `/=w\d+-h\d+(-[a-z0-9-]*)?$/` with `=w544-h544$1`; anything else returned as-is.
    - `parseAppleRss`: `feed.results.slice(0, 50)`; songs → `{ artist: artistName.trim(), title: name.trim(), image: img(resizeMzstatic(artworkUrl100)), mbid: null }`; albums → `{ name: stripReleaseSuffix(name), artist: artistName.trim(), image }`; filter rows with blank artist/title (songs) or blank name/artist (albums). Use TypeScript overloads so the return type follows `kind`.
    - `parseKkbox`: `data.charts[type]` (missing → []), slice 50, `{ artist: stripLatinAlias(artist_name), title: song_name.trim(), image: img(cover_image?.normal), mbid: null }`, filter blanks. Do NOT strip KKBOX ` - 電影《…》主題曲` subtitles (CONTEXT discretion + UI-SPEC 1.4: displayed title keeps them; resolve impact unmeasured — note this as 39-D-03 in a comment).
    - `parseYtCharts`: FIRST read the echoed countryCode through the typed narrowing chain; if it is not a string equal to `cc`, return [] (39-D-04, RESEARCH Pitfall 3: "an unsupported country returns 200 + the GLOBAL chart; the only tell is the echo"). Then a private typed `deepFind(o: unknown, key: string): unknown[] | null` (recursive, returns the first array found under `key`) locates `trackViews` / `artistViews`; slice 50; tracks → `{ artist: (artists ?? []).map(a => stripLatinAlias(a.name)).filter(Boolean).join(', '), title: name.trim(), image: img(resizeYtThumb(lastThumbnailUrl)), mbid: null }`; artists → `{ name: stripLatinAlias(name), image, mbid: null }` where `lastThumbnailUrl` = `thumbnail.thumbnails.at(-1)?.url`. Filter blanks.
    - `parseItunesGenreFeed`: `entries = ([] as ItunesEntry[]).concat(feed.entry ?? [])` (39-D-05: one row is an OBJECT); keep rows where `category?.attributes?.['im:id'] === String(genreId)` (39-D-06: a bogus id returns the overall chart with a 200); slice 50; `{ artist: e['im:artist']?.label, title: e['im:name']?.label, image: img(resizeMzstatic(e['im:image']?.at(-1)?.label)), mbid: null }` with `.trim()` + blank filter.
    Wrap each parser body so a non-object/garbage input short-circuits to [] — no try/catch around the whole thing is needed if every access is optional-chained on a narrowed type, but a top-level `if (!data || typeof data !== 'object') return []` is required.

    Create `src/lib/services/chart-parse.test.ts` importing the 11 fixtures (kuwo.test.ts idiom) and `safeImageUrl` + the three host lists from `$lib/proxy/safe-image-url` to build `img` validators (`const apple = (u) => safeImageUrl(u, APPLE_IMAGE_HOSTS)` etc.). Encode every line of `<behavior>` as an `it(...)`. For the two YT echo-mismatch cases, if `yt-cn-global.json` echoed `cn` at capture time, build the 'global' variant in-test by deep-cloning the hk fixture and setting `countryCode = 'global'`.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/services/chart-parse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" src/lib/services/chart-parse.ts` prints 0
    - `grep -n "39-D-04" src/lib/services/chart-parse.ts` matches inside parseYtCharts, and `grep -n "countryCode" src/lib/services/chart-parse.ts` matches at least one line
    - `grep -c "^export function" src/lib/services/chart-parse.ts` is ≥ 8 (resizeMzstatic, resizeYtThumb, stripLatinAlias, stripReleaseSuffix, parseAppleRss, parseKkbox, parseYtCharts, parseItunesGenreFeed; overload declarations may add more lines)
    - `pnpm vitest --run src/lib/services/chart-parse.test.ts` passes with ≥ 20 tests including one named to match /echo|global/ and one named to match /single/
    - chart-parse.ts imports nothing from `$lib/stores`, `$app`, or `svelte` (`grep -c "\$lib/stores\|\$app/\|from 'svelte" src/lib/services/chart-parse.ts` prints 0)
  </acceptance_criteria>
  <done>All four parsers + four helpers exported, fixture-tested, never-throw on garbage, echo-gated for YT, genre-gated for iTunes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add fuseCharts (reciprocal-rank fusion for Top Songs)</name>
  <read_first>
    - src/lib/services/chart-parse.ts (the file being extended — from Task 2)
    - src/lib/services/match-key.ts (`matchKey(artist, title)`; `norm()` drops bracketed groups, so '田馥甄 (Hebe)' and '田馥甄' already share a key)
    - src/lib/services/dedupe.ts `groupVariants` (Map grouping + the blank-key guard idiom)
    - src/lib/services/catalog.ts `interleave` (multi-list merge with a `seen` Map — the shape RRF replaces)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-CONTEXT.md §"Top Songs = KKBOX + Apple together" (the locked contract)
    - .planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-PATTERNS.md §home-charts.ts "fuseCharts" (suggested contract + the ponytail ceiling note)
  </read_first>
  <files>src/lib/services/chart-parse.ts, src/lib/services/chart-parse.test.ts</files>
  <behavior>
    - fuseCharts([[a1,a2,a3],[b1,b2,b3]]) where a2 and b1 are the same song (same matchKey): that song is result[0] (Σ 1/(60+2) + 1/(60+1) beats every singleton), and the result contains each distinct song exactly once
    - Overlap keeps the FIRST list's artist/title strings (Apple precedence: caller passes [apple, kkbox]) — e.g. apple row '田馥甄 / 要去什麼地方' and kkbox row '田馥甄 (Hebe) / 要去什麼地方' fuse to artist '田馥甄'
    - image = the first non-null image across the group (apple image null + kkbox image set → kkbox image)
    - fuseCharts([[], list]) deep-equals list (same order); fuseCharts([list, []]) deep-equals list; fuseCharts([[], []]) → []; fuseCharts([]) → []
    - A row whose matchKey is '|' (blank artist AND title) is skipped
    - Result length ≤ cap (default 50): fusing two 50-row disjoint lists yields exactly 50
    - Ties (equal score) keep first-appearance order (stable)
    - k is a parameter: fuseCharts(lists, 1) still ranks the overlap first
  </behavior>
  <action>
    Append `fuseCharts(lists: DiscoveryTrack[][], k = 60, cap = 50): DiscoveryTrack[]` to `src/lib/services/chart-parse.ts` (39-D-07, CONTEXT plan-review decision 2026-09-25: Top Songs for hk/tw/sg = KKBOX + Apple blended into ONE ranked list by reciprocal-rank fusion). Algorithm: iterate lists in order, rows with 1-based rank; `key = matchKey(artist, title)`; skip `key === '|'`; a `Map<string, { item: DiscoveryTrack; score: number; first: number }>` where the first-seen row supplies `artist`/`title` (so the caller's list order IS display precedence), `image` is filled from the first non-null image, `score += 1 / (k + rank)`, `first` = a running insertion counter. Sort entries by score descending, tie-break by `first` ascending, `slice(0, cap)`, return the items. Import `matchKey` from `$lib/services/match-key`. Add the ponytail comment from PATTERNS: `matchKey` does not fold Traditional/Simplified script or artist separators ('A & B' vs 'A、B'), so such pairs remain separate entries; upgrade path = zh-convert + a separator fold if duplicates show up on real charts.

    Extend `src/lib/services/chart-parse.test.ts` with a `describe('fuseCharts (39-D-07 / P39-13)')` block covering every `<behavior>` line, using small hand-built DiscoveryTrack arrays (helper `tr(artist, title, image = null)`), plus one case that fuses `parseAppleRss(appleHkSongs,'songs',img)` with `parseKkbox(kkboxHkSong,'song',img)` and asserts the result is non-empty, unique by matchKey, and ≤ 50.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/services/chart-parse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function fuseCharts" src/lib/services/chart-parse.ts` matches one line
    - `grep -n "import { matchKey } from '\$lib/services/match-key'" src/lib/services/chart-parse.ts` matches
    - `grep -n "ponytail:" src/lib/services/chart-parse.ts` matches at least one line
    - `pnpm vitest --run src/lib/services/chart-parse.test.ts` passes and reports ≥ 8 tests under a describe whose name contains "fuseCharts"
    - `pnpm vitest --run src/lib/services/chart-parse.test.ts src/lib/proxy/safe-image-url.test.ts` completes green in < 10 s
  </acceptance_criteria>
  <done>fuseCharts exported with RRF semantics per CONTEXT (overlap rises, first-list name wins, one-empty passthrough, cap 50), pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream JSON → parser | Apple / KKBOX / YouTube / iTunes / Deezer bodies are untrusted; YouTube and iTunes return a WRONG chart with a 200 |
| parsed image URL → client `<img src>` / CSS `url()` | image strings are echoed to every client of the edge cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Spoofing (of data) | parseYtCharts | mitigate | echo `countryCode === cc` gate before any row is read; mismatch → [] (39-D-04) |
| T-39-02 | Spoofing (of data) | parseItunesGenreFeed | mitigate | per-row `category.attributes['im:id'] === String(genreId)` (39-D-06); bogus id → [] |
| T-39-03 | Tampering | image URLs from all four parsers | mitigate | parsers take an `ImageValidator` and never emit a raw URL; edge binds `safeImageUrl` + APPLE_/KKBOX_/YOUTUBE_IMAGE_HOSTS (char screen, https-only, dot-anchored suffix, no apex) |
| T-39-04 | DoS | parser input size | mitigate | every parser `slice(0, 50)` before mapping; garbage/non-object short-circuits to [] |
| T-39-05 | Tampering | upstream schema drift | mitigate | optional-field interfaces + `typeof`/`Array.isArray` narrowing; never throws → [] (empty is never cached by 39-03) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (RESEARCH §Package Legitimacy Audit: none); slopcheck not applicable |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/services/chart-parse.test.ts src/lib/proxy/safe-image-url.test.ts` green (VALIDATION rows P39-01 unit, P39-09, P39-13).
- `pnpm check` green (no new type errors; chart-parse.ts has zero `as any`).
- `git status` shows 11 new fixture files, all valid JSON, none imported by app source (`grep -rn "__fixtures__/charts" src --include=*.ts --include=*.svelte | grep -v test` prints nothing).
</verification>

<success_criteria>
- Parsers, helpers and fuseCharts exported from one pure module with the exact signatures in `<interfaces>`.
- YT global fallback and iTunes bogus-genre both parse to [] on real fixtures.
- Three new allowlists accept the five verified hosts and reject look-alikes and apexes.
</success_criteria>

<output>
Create `.planning/phases/39-fresh-chart-homepage-apple-music-kkbox-and-youtube-chart-she/39-01-SUMMARY.md` when done. Record which fixtures (if any) were synthetic and whether `yt-cn-global.json` echoed `global` at capture time.
</output>
