---
phase: 25-zh-hant-offline-conversion-fallback-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/lib/services/zh-convert.ts
  - src/lib/services/zh-convert.test.ts
autonomous: false
requirements: [D-01, D-03, D-04]
user_setup: []

must_haves:
  truths:
    - "Simplified Chinese converts to Traditional at phrase level (头发→頭髮; 里 disambiguated to 裡/裏 by context)"
    - "A line that contains Japanese kana is NOT treated as offline-convertible (isChineseLine returns false)"
    - "A line with no Han ideographs (pure Latin/EN) is NOT offline-convertible"
    - "The s2t dictionary is pulled via dynamic import(), so it stays out of the initial bundle and loads once (memoized)"
    - "Offline conversion performs zero network calls"
    - "Realizes decisions D-01 (tongwen-core + tongwen-dict s2t lib), D-03 (client-side lazy dynamic import), D-04 (CJK/kana offline-eligibility predicate)"
  artifacts:
    - path: "src/lib/services/zh-convert.ts"
      provides: "Lazy client-side Simplified→Traditional converter + offline-eligibility predicate"
      exports: ["s2tConvertLines", "isChineseLine"]
      min_lines: 30
    - path: "src/lib/services/zh-convert.test.ts"
      provides: "s2t conversion + CJK/kana detection unit tests (node-runnable, no jsdom)"
      contains: "isChineseLine"
    - path: "package.json"
      provides: "Pinned tongwen deps"
      contains: "tongwen-core"
  key_links:
    - from: "src/lib/services/zh-convert.ts"
      to: "tongwen-core"
      via: "dynamic import of createConverterMap + LangType"
      pattern: "import\\(.*tongwen"
    - from: "src/lib/services/zh-convert.ts"
      to: "src/lib/i18n/detect.ts"
      via: "reuse detectLang for isChineseLine (kana/hangul-first classification)"
      pattern: "detectLang"
---

<objective>
Add a lazy, client-side Simplified→Traditional Chinese converter (TongWenTang s2t, D-01/D-03) plus a testable CJK-source predicate (D-04) that decides which lines are safe to convert offline. This is the foundation the translate choke point (Plan 03) wires in.

Purpose: zh-Hans→zh-Hant is a deterministic char/phrase conversion — it needs no API, works offline / on lockscreen, and never rate-limits. Isolating it here (module + tests) keeps Plan 03 a thin wiring change.
Output: `src/lib/services/zh-convert.ts` (pure/lazy, node-testable) + co-located unit tests; `tongwen-core` + `tongwen-dict` pinned in package.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-zh-hant-offline-conversion-fallback-cascade/25-CONTEXT.md
@.planning/notes/zh-hant-offline-conversion.md
@CLAUDE.md

<interfaces>
<!-- Existing detection contract to reuse — DO NOT re-implement the Unicode ranges. -->
From src/lib/i18n/detect.ts:
```typescript
export type LangTag = 'en' | 'zh-Hant' | 'zh-Hans' | 'ja' | 'ko';
// Priority: kana → 'ja', hangul → 'ko', Han (no kana/hangul) → 'zh-Hant'|'zh-Hans', else 'en'.
// Kanji-only Japanese acceptably misclassifies as Chinese (accepted edge, see detect.ts header).
export function detectLang(text: string): LangTag;
```

<!-- tongwen-core@4.1.1 public API (confirmed from package README). -->
From tongwen-core:
```typescript
type SrcPack = { s2t: Record<string, string>[]; t2s: Record<string, string>[] };
enum LangType { s2t, t2s }
function createConverterMap(dics: SrcPack): { phrase(dir: LangType, text: string): string; char(dir: LangType, text: string): string };
```
<!-- tongwen-dict@1.0.2 ships dict JSON under `tongwen-dict/dist/*.json` (package exports map `./dist/*.json`). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify tongwen package legitimacy before install</name>
  <read_first>
    - .planning/notes/zh-hant-offline-conversion.md (research: TongWenTang chosen, MIT, ~72 KB s2t-only)
    - package.json (current dependency set — web app has NO third-party runtime deps today)
  </read_first>
  <action>
    Do NOT run `pnpm add` until this gate is approved. Present the two proposed packages (`tongwen-core@4.1.1`, `tongwen-dict@1.0.2`) to the human with their npm + repo links and the legitimacy evidence below, then WAIT for explicit approval. On approval, Task 2 performs the pinned install. If the human names substitute versions, use those exact pins in Task 2 instead.
  </action>
  <what-built>
    No code yet — this is the supply-chain gate that precedes the first-ever third-party runtime npm dependency in this repo. Packages proposed: `tongwen-core@4.1.1` and `tongwen-dict@1.0.2` (both MIT; repo github.com/tongwentang). No RESEARCH.md Package Legitimacy Audit exists, so both are treated as [ASSUMED] and require human confirmation (T-25a-SC).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/tongwen-core and https://www.npmjs.com/package/tongwen-dict
    2. Confirm each: reputable publisher (org `tongwentang`), a real GitHub repo, non-trivial download history, MIT license, and NO suspicious/obfuscated postinstall in the package (tongwen-core's only lifecycle script is `prepare` = husky, which does not run for consumers installing the published registry tarball).
    3. Confirm the versions match (4.1.1 / 1.0.2) and are not typosquats.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-only legitimacy gate; no automated check. Task 2's `pnpm add` is the downstream automated proof the packages resolve.</automated>
  </verify>
  <acceptance_criteria>
    - Human types "approved" (or names the exact substitute versions) before any `pnpm add` runs.
    - This gate is NOT auto-approvable regardless of workflow.auto_advance (legitimacy checkpoints are always blocking-human).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install tongwen (pinned) and build the lazy s2t converter module</name>
  <files>package.json, pnpm-lock.yaml, src/lib/services/zh-convert.ts</files>
  <read_first>
    - src/lib/i18n/detect.ts (reuse detectLang; do NOT duplicate the KANA/HANGUL/HAN ranges)
    - CLAUDE.md (tabs, single-quote TS, named exports in $lib, never-throw service boundary, `$app/environment` browser guard convention)
    - .planning/phases/25-zh-hant-offline-conversion-fallback-cascade/25-CONTEXT.md (D-01, D-03, D-04)
  </read_first>
  <action>
    Run `pnpm add tongwen-core@4.1.1 tongwen-dict@1.0.2` (exact pins, no caret) so the versions verified in Task 1 are what ship.

    Create `src/lib/services/zh-convert.ts` (pure `.ts`, NOT `.svelte.ts` — no runes). Export two named symbols:

    (a) `isChineseLine(text: string): boolean` — the D-04 CJK-source predicate. Implement it by delegating to `detectLang` from `$lib/i18n/detect`: return true only when `detectLang(text)` is `'zh-Hant'` or `'zh-Hans'`. Because detectLang classifies kana→'ja' and hangul→'ko' BEFORE Han, any line containing kana/hangul returns false and is left for the API cascade (this is the D-04 Japanese-kanji guard). Empty/whitespace lines return false.

    (b) `s2tConvertLines(lines: string[]): Promise<string[]>` — positionally-aligned batch convert (out.length === lines.length). On first call, lazily `import()` ONLY the s2t char + phrase dictionaries from `tongwen-dict/dist` and `createConverterMap` + `LangType` from `tongwen-core`; build a `SrcPack` whose `s2t` array holds the loaded char + phrase dicts and whose `t2s` is empty (t2s is out of scope — Deferred). Memoize the built converter in a module-scoped promise (a single dynamic-import + build, reused across calls) so the ~72 KB dict is fetched at most once. Convert each line with the converter's `phrase(LangType.s2t, line)` (phrase-level, per D-01 — this is what disambiguates 头发→頭髮 and 里→裡/裏). Never-throw boundary: wrap the import/build/convert in try/catch and, on any failure, return the input lines unchanged (identity) so a converter fault degrades to "not converted" instead of throwing into a caller.

    Confirm the exact s2t dict JSON entry points from the tongwen-dict `dist` folder + its README "Usage" section; import ONLY the s2t (Simplified→Traditional) char and phrase JSON — never the t2s dicts (keeps the lazy chunk near the measured ~72 KB and avoids pulling the reverse table). Do NOT add a top-level (static) import of tongwen — it MUST be dynamic so it stays out of the initial bundle (D-03).
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `tongwen-core` at `4.1.1` and `tongwen-dict` at `1.0.2` (exact, no `^`).
    - `src/lib/services/zh-convert.ts` exports `isChineseLine` and `s2tConvertLines`; it contains NO static/top-level `import ... from 'tongwen*'` — the tongwen import appears only inside an `import(...)` call.
    - `zh-convert.ts` imports `detectLang` from `$lib/i18n/detect` and does not redefine kana/hangul/Han regexes.
    - `pnpm check` passes (0 errors).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-test s2t conversion + CJK/kana detection</name>
  <files>src/lib/services/zh-convert.test.ts</files>
  <read_first>
    - src/lib/services/zh-convert.ts (the module under test — exports confirmed in Task 2)
    - src/lib/services/translate.test.ts (test-style reference: describe/it, node project, no jsdom)
    - src/lib/i18n/detect.ts (expected classification behavior the predicate rides on)
  </read_first>
  <behavior>
    - s2tConvertLines(['简体中文']) → ['簡體中文'] (basic char conversion).
    - s2tConvertLines(['头发']) → ['頭髮'] (phrase-level disambiguation, D-01 quality claim).
    - s2tConvertLines preserves length and blank slots: input ['', '中国'] → ['', '中國'] (out.length === in.length).
    - s2tConvertLines(['台灣']) → ['台灣'] (already-Traditional passes through unchanged; s2t is a no-op on trad).
    - isChineseLine('简体') === true; isChineseLine('中國風') === true.
    - isChineseLine('さくら') === false (kana → not offline-convertible; D-04 Japanese guard).
    - isChineseLine('안녕') === false (hangul → not offline-convertible).
    - isChineseLine('hello world') === false; isChineseLine('') === false.
  </behavior>
  <action>
    Create `src/lib/services/zh-convert.test.ts` co-located with the module. Cover every case in <behavior>. These are pure/node tests — no `$app/environment` mock, no localStorage, no jsdom (the converter is pure and loads the dict via dynamic import, which Vitest resolves natively). Assert conversion output exactly (specific Traditional strings) and assert the boolean predicate for the Chinese / kana / hangul / Latin / empty cases so the JA-kanji-vs-Chinese edge is pinned.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/services/zh-convert.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- src/lib/services/zh-convert.test.ts` passes with every <behavior> case asserted.
    - Test file contains an explicit assertion that `isChineseLine('さくら')` is `false`.
    - `pnpm check` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | First third-party runtime dependency enters the bundle (supply chain) |
| dict chunk → client | ~72 KB s2t dict shipped to the browser (bundle-size / main-thread) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25a-SC | Tampering | `pnpm add tongwen-core / tongwen-dict` | mitigate | Blocking-human legitimacy checkpoint (Task 1) + exact version pins + verify no consumer-run postinstall |
| T-25a-01 | Denial of Service | initial bundle / cold load | mitigate | Dict imported via `import()` only (dynamic, lazy, memoized) — never a static import; stays out of the initial chunk (D-03) |
| T-25a-02 | Tampering | JA-kanji lines wrongly s2t-converted | mitigate | `isChineseLine` delegates to kana/hangul-first `detectLang`; unit test pins `isChineseLine('さくら') === false` |
</threat_model>

<verification>
- `pnpm check` → 0 errors.
- `pnpm test -- src/lib/services/zh-convert.test.ts` → all green.
- `grep -n "import(" src/lib/services/zh-convert.ts` shows the tongwen import is dynamic; `grep -c "from 'tongwen" src/lib/services/zh-convert.ts` (top-level) is 0.
</verification>

<success_criteria>
- Lazy s2t converter + `isChineseLine` predicate exist, are exported, and are covered by passing unit tests.
- tongwen-core@4.1.1 + tongwen-dict@1.0.2 pinned; legitimacy human-approved.
- No behavior wired into the app yet (Plan 03 does the wiring) — this plan ships the tested foundation only.
</success_criteria>

<output>
Create `.planning/phases/25-zh-hant-offline-conversion-fallback-cascade/25-01-SUMMARY.md` when done.
</output>
