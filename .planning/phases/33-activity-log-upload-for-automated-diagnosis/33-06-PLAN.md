---
phase: 33-activity-log-upload-for-automated-diagnosis
plan: 06
type: execute
wave: 3
depends_on: [33-04, 33-05]
files_modified: []
autonomous: false
requirements: [D-03, D-07]
tags: [cloudflare, r2, provisioning, secrets]

must_haves:
  truths:
    - "The R2 bucket `openmusic-diag` exists on the Cloudflare account that hosts the `openmusic` Pages project (`f1868a071996e836eae6da2b65f37929`) — the D-07 storage backend is provisioned, not just declared (D-07)"
    - "`DIAG_UPLOAD_TOKEN` and `DIAG_READ_TOKEN` are set as Pages SECRETS via `wrangler pages secret put` on that project — they exist only in Cloudflare `platform.env` (and the gitignored `.dev.vars`), never in the repo, never in the bundle (D-03)"
    - "`wrangler whoami` from this machine can reach the hosting account, or the human has completed the equivalent steps in the dashboard — verified by `wrangler r2 bucket list` and `wrangler pages secret list`"
  artifacts:
    - path: ".dev.vars"
      provides: "local DIAG_UPLOAD_TOKEN / DIAG_READ_TOKEN for `pnpm preview` (gitignored — NOT a repo artifact)"
      contains: "DIAG_UPLOAD_TOKEN="
  key_links:
    - from: "wrangler.jsonc r2_buckets[0].bucket_name (openmusic-diag)"
      to: "the created R2 bucket"
      via: "exact name match"
      pattern: "openmusic-diag"
---

<objective>
Provision what no executor can create from this machine: enable R2 on the `openmusic` account,
create the `openmusic-diag` bucket, and set the two Pages secrets. Automation is attempted first;
the human step is the fallback the research proved necessary (`wrangler whoami` is logged into two
accounts, neither hosting `openmusic`; API calls return `Authentication error [code: 10000]`).

Purpose: everything in Plan 07 (deploy + Tier 2/3 verification) is a 503 until the binding and the
secrets exist. This is a scheduling dependency on Plans 04/05, not a code one — it is placed here so
the human does provisioning and the deploy approval in one sitting.
Output: bucket + two secrets exist; `.dev.vars` carries both values locally; SUMMARY records the
`wrangler r2 bucket list` and `wrangler pages secret list` output.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-VALIDATION.md
@wrangler.jsonc
@.dev.vars
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt provisioning from the CLI (automation first)</name>
  <files>.dev.vars</files>
  <read_first>
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "Environment Availability" (the exact auth failure, account id, and the 5-step human list) and "Secret provisioning"
    - .dev.vars — existing format and comment style for JOOX/LASTFM entries (gitignored; confirm with `git check-ignore .dev.vars`)
    - wrangler.jsonc — the bucket name the code expects: `openmusic-diag`
  </read_first>
  <action>
    Never print a token value into the transcript or a SUMMARY. Steps:
    1. `git check-ignore .dev.vars` must print `.dev.vars` (else STOP — do not write secrets to a tracked file).
    2. `npx wrangler whoami` — record which accounts the current OAuth token can reach.
    3. Try `CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler r2 bucket list`.
       - `Authentication error [code: 10000]` => the account is unreachable; skip to the checkpoint (Task 2) with this evidence.
       - `[code: 10042] Please enable R2` => account reachable, R2 not enabled; checkpoint for the one-time dashboard enablement, then continue.
       - A list (with or without `openmusic-diag`) => proceed.
    4. If reachable and R2 enabled: `CLOUDFLARE_ACCOUNT_ID=… npx wrangler r2 bucket create openmusic-diag` (idempotent-ish: "already exists" is success).
    5. Generate tokens WITHOUT echoing them: `UP=$(openssl rand -hex 32); RD=$(openssl rand -hex 32)`.
       `printf '%s' "$UP" | CLOUDFLARE_ACCOUNT_ID=… npx wrangler pages secret put DIAG_UPLOAD_TOKEN --project-name openmusic` and the same for `DIAG_READ_TOKEN` with `$RD`.
    6. Append both to `.dev.vars` under a Phase 33 comment block in the existing style: purpose of each token, that the READ token must NEVER be entered on any device (T-33-07), that prod values are set via `wrangler pages secret put`, and that rotation = re-run `secret put`. Write the lines with `printf >> .dev.vars`, never echo the values to stdout.
    7. Verify: `npx wrangler pages secret list --project-name openmusic` names both `DIAG_UPLOAD_TOKEN` and `DIAG_READ_TOKEN`; `npx wrangler r2 bucket list` includes `openmusic-diag`.
    If ANY step fails on auth or R2 enablement, record the exact error text and stop — Task 2 hands the remaining steps to the human. Do not attempt to log in to a different Cloudflare user yourself.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && git check-ignore -q .dev.vars && grep -q '^DIAG_UPLOAD_TOKEN=' .dev.vars && grep -q '^DIAG_READ_TOKEN=' .dev.vars && ! git grep -q "DIAG_UPLOAD_TOKEN=" -- ':!.dev.vars' 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -q .dev.vars` exits 0 (the only local file holding token VALUES is gitignored)
    - `git grep -c "DIAG_UPLOAD_TOKEN=\|DIAG_READ_TOKEN=" -- ':!.dev.vars'` returns nothing (no tracked file carries a value; D-03)
    - Either (a) `npx wrangler pages secret list --project-name openmusic` output contains both `DIAG_UPLOAD_TOKEN` and `DIAG_READ_TOKEN` AND `npx wrangler r2 bucket list` output contains `openmusic-diag`, or (b) the SUMMARY records the verbatim `[code: 10000]` / `[code: 10042]` error and defers to Task 2
    - No token value appears in any command output captured in the SUMMARY (`grep -cE '[0-9a-f]{64}' 33-06-SUMMARY.md` returns 0)
  </acceptance_criteria>
  <done>Provisioning is either complete and verified from the CLI, or the exact blocker is recorded and the human checkpoint is reached with the precise remaining steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human provisioning — reach the openmusic account, enable R2, create the bucket, set both secrets</name>
  <files>.dev.vars</files>
  <read_first>
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-06-SUMMARY.md — Task 1's recorded blocker (which step failed and the verbatim error code)
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "Environment Availability" (the 5-step human list) and "Fallback if the account owner declines R2"
  </read_first>
  <action>
    PAUSE here — this is a blocking human step, not something to work around. Present `what-built` and
    the numbered `how-to-verify` steps to the user verbatim, then WAIT for the resume signal. On
    "approved": re-run `npx wrangler pages secret list --project-name openmusic` and
    `npx wrangler r2 bucket list` (with `CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929`) and
    record their output (names only, never values) in the SUMMARY. On "kv": stop and return to the
    orchestrator — Plans 02/04 need the documented ~6-line R2->KV swap before Plan 07. Do not log in
    as a different Cloudflare user yourself; do not create resources on the two reachable non-openmusic accounts.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler pages secret list --project-name openmusic 2>/dev/null | grep -q DIAG_UPLOAD_TOKEN && CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler pages secret list --project-name openmusic 2>/dev/null | grep -q DIAG_READ_TOKEN && CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler r2 bucket list 2>/dev/null | grep -q openmusic-diag</automated>
  </verify>
  <acceptance_criteria>
    - User resumed with "approved" (or "kv" — recorded as a scope change for the orchestrator)
    - `npx wrangler pages secret list --project-name openmusic` output contains both `DIAG_UPLOAD_TOKEN` and `DIAG_READ_TOKEN`
    - `npx wrangler r2 bucket list` output contains `openmusic-diag`
    - `grep -cE '[0-9a-f]{64}' 33-06-SUMMARY.md` returns 0 (no token value recorded)
  </acceptance_criteria>
  <done>Bucket and both secrets verified present on the hosting account by the executor after the human step.</done>
  <what-built>Plans 01-05 delivered the route, helpers, CORS fix, i18n, and the Upload button; `wrangler.jsonc` binds `DIAG` -> `openmusic-diag`. Task 1 attempted provisioning from the CLI and (if you are reading this) hit the authentication wall research predicted: the wrangler login on this machine reaches accounts `280bedba…` and `0b9e5c70…`, but `openmusic` lives on account `f1868a071996e836eae6da2b65f37929`.</what-built>
  <how-to-verify>
    Do these in a terminal in `/Users/laichan/code/tung/openmusic`. Skip any step Task 1 already completed (see 33-06-SUMMARY.md).
    1. `npx wrangler login` — sign in as the Cloudflare user that owns account `f1868a071996e836eae6da2b65f37929` (the one that deploys openmusic.lol). Confirm with `npx wrangler whoami` listing that account id.
    2. Enable R2 on that account: Cloudflare dashboard -> R2 -> "Purchase R2" / enable (one-time checkout; free tier: 10 GB-month, 1M Class A ops, 10M Class B ops, free egress — RESEARCH.md "Verified free-tier numbers"). You have already done this once on the Flow account, so the flow is familiar.
       If you decline R2: say so in the resume message — the fallback is KV (~6-line route swap: `kv_namespaces` + `KVNamespace` + `.keys` instead of `.objects`; RESEARCH.md "Fallback if the account owner declines R2"), accepting up to 60 s propagation and cached 404s.
    3. `CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler r2 bucket create openmusic-diag` — the name must match `wrangler.jsonc` exactly.
    4. Generate and set the secrets without echoing them:
       `openssl rand -hex 32 | CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler pages secret put DIAG_UPLOAD_TOKEN --project-name openmusic`
       `openssl rand -hex 32 | CLOUDFLARE_ACCOUNT_ID=f1868a071996e836eae6da2b65f37929 npx wrangler pages secret put DIAG_READ_TOKEN --project-name openmusic`
       (If you want the values locally, generate to variables first — `UP=$(openssl rand -hex 32)` — pipe with `printf '%s' "$UP"`, and append `DIAG_UPLOAD_TOKEN=$UP` / `DIAG_READ_TOKEN=$RD` to the gitignored `.dev.vars`. The UPLOAD token is what you will paste into the phone in Plan 07; the READ token stays on this laptop only.)
    5. Confirm: `npx wrangler pages secret list --project-name openmusic` shows both names; `npx wrangler r2 bucket list` shows `openmusic-diag`.
    Expected outcome: both commands in step 5 succeed with no `[code: 10000]` error. The executor will re-run step 5 to record the evidence.
  </how-to-verify>
  <resume-signal>Type "approved" when step 5 shows both secrets and the bucket, or "kv" if you declined R2, or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Laptop -> Cloudflare API | wrangler OAuth; account membership decides what can be provisioned |
| Terminal -> `.dev.vars` | Token VALUES written to a gitignored local file only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Information Disclosure | token committed to the repo | mitigate | `git check-ignore .dev.vars` gate before any write; `git grep` gate that no tracked file carries a value; secrets set via `wrangler pages secret put`, never `vars` (D-03) |
| T-33-02 | Information Disclosure | token echoed into transcript / SUMMARY | mitigate | Pipe `openssl rand` straight into `secret put`; hex-64 grep gate on the SUMMARY |
| T-33-07 | Information Disclosure | read token reaches a device | mitigate | Checkpoint text states the READ token stays on the laptop; only the UPLOAD token is pasted into the phone (Plan 07) |
| T-33-SC | Tampering | npm installs | accept | No packages installed; `wrangler` 4.98.0 is an existing devDependency run via `npx` |
</threat_model>

<verification>
- `npx wrangler pages secret list --project-name openmusic` names `DIAG_UPLOAD_TOKEN` and `DIAG_READ_TOKEN`
- `npx wrangler r2 bucket list` (against account `f1868a07…`) names `openmusic-diag`
- No tracked file contains a token value
</verification>

<success_criteria>
- Bucket and both secrets exist on the hosting account (D-07 provisioned, D-03 honoured)
- Plan 07's Tier-2 curls have a live binding + secrets to hit
</success_criteria>

<output>
Create `.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-06-SUMMARY.md` when done
</output>
