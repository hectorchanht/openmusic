---
phase: 33-activity-log-upload-for-automated-diagnosis
plan: 07
type: execute
wave: 4
depends_on: [33-04, 33-05, 33-06]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-04, D-05, D-06]
tags: [deploy, verification, cloudflare, android]

must_haves:
  truths:
    - "Nothing is pushed to `main` (which auto-deploys openmusic.lol) until the user explicitly approves in a blocking checkpoint (T-33-14)"
    - "Against the LIVE deploy: POST without a token -> 401; wrong token -> 401; a 600 KB body with the right token -> 413 (not a 5xx); a real log with the right token -> 200 + `log/…` key; GET list with the read token includes that key; GET ?key= returns the byte-identical body (D-01, D-02, D-05)"
    - "An OPTIONS preflight from `Origin: https://localhost` requesting `authorization` returns 204 with `Access-Control-Allow-Headers` containing `Authorization` and `Access-Control-Allow-Origin: https://localhost` (T-33-06 proven on the edge)"
    - "On the phone: Settings -> Activity log -> Upload log (one tap, D-04/D-06) -> prompt -> 'Log uploaded.'; within ~10 s the laptop lists and fetches that same log with curl — the phase's success criterion: no copy-paste"
  artifacts:
    - path: ".planning/phases/33-activity-log-upload-for-automated-diagnosis/33-07-SUMMARY.md"
      provides: "recorded status codes for every Tier-2 curl + device result"
  key_links:
    - from: "git push origin main"
      to: "Cloudflare Pages Git deploy of openmusic.lol"
      via: "native Git integration (pnpm run deploy is the fallback — `pnpm deploy` is shadowed by a pnpm builtin)"
      pattern: "git push origin main"
---

<objective>
Ship and prove it: get explicit approval, push `main`, wait for the Pages deploy, run the Tier-2 curl
matrix against `https://openmusic.lol/api/diag`, then hand the phone to the human for the Tier-3
round trip (upload from the APK, fetch from the laptop).

Purpose: the R2 binding, the Pages secrets, the CPU budget, the CORS preflight and the
verb-exports-only rule are ALL unobservable locally (33-VALIDATION.md "Manual-Only Verifications").
Pushing to main deploys production and has shipped half a decision before — so the push is a
user-approved step, never incidental.
Output: SUMMARY with every observed status code; device round trip confirmed or the exact failure
classified (CORS => T-33-06; no prompt => A1 text-input fallback; 503 => binding not applied).
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-VALIDATION.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-04-SUMMARY.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-06-SUMMARY.md
@src/routes/api/diag/+server.ts

<interfaces>
Response contract (Plan 04): POST 200 `{ ok:true, key, entries }` · 401 `unauthorized` · 413 `too-large` · 400 `invalid` · 503 `unconfigured`;
GET 200 `{ ok:true, logs:[{key,size,uploaded}] }` · GET ?key= 200 raw JSON · 400 `invalid-key` · 404 `not-found`.
Tokens: read from the gitignored `.dev.vars` (`DIAG_UPLOAD_TOKEN=`, `DIAG_READ_TOKEN=`) — never echo them.
APK build gotcha (STATE.md:45): `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home pnpm apk`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve the push to main (auto-deploys production)</name>
  <files>none — approval gate only</files>
  <read_first>
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-06-SUMMARY.md — provisioning evidence (bucket + secrets present)
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — Pitfall 1 (push to main deploys production; prior incident)
  </read_first>
  <action>
    PAUSE before any `git push`. Run `pnpm check && pnpm test` yourself and paste the pass/fail
    summary line (test count) into the checkpoint message, together with `git log --oneline
    origin/main..HEAD`. Then present `how-to-verify` and WAIT. Under NO circumstances push before the
    user types "approved" — the push is a production deploy (T-33-14). If the user asks for changes,
    return to the orchestrator with their notes instead of editing here.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check && pnpm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0 and `pnpm test` reports 0 failures with >= 2023 + new tests
    - `git log --oneline origin/main..HEAD` lists the Phase 33 commits and `git status --porcelain` shows no stray tracked changes
    - The user has typed "approved" before Task 2 runs; no `git push` has occurred yet (`git status -sb` still shows `ahead`)
  </acceptance_criteria>
  <done>Explicit user approval recorded; local gates green; nothing pushed yet.</done>
  <what-built>Phase 33 is complete locally: `$lib/proxy/diag-auth.ts` + `diag-payload.ts` (tested), `Authorization` added to the shared CORS Allow-Headers (tested), `Env`/`wrangler.jsonc` R2 binding `DIAG` -> `openmusic-diag`, 5 i18n keys x 15 locales, `src/routes/api/diag/+server.ts` (POST upload / GET list / GET ?key= fetch, tested + dev-server-smoked at 401), and the Upload-log button on Settings -> Activity log. Plan 06 provisioned the bucket and both secrets. All of it is committed on `main` but NOT pushed.</what-built>
  <how-to-verify>
    In `/Users/laichan/code/tung/openmusic`:
    1. `pnpm check && pnpm test` — expect exit 0, >= 2023 baseline tests plus the new diag-auth / diag-payload / diag-endpoint / http cases, 0 failures.
    2. `git log --oneline origin/main..HEAD` — review the Phase 33 commits that will go live; `git status --porcelain` should show nothing unexpected.
    3. Confirm Plan 06 finished: `npx wrangler pages secret list --project-name openmusic` shows `DIAG_UPLOAD_TOKEN` and `DIAG_READ_TOKEN`; `npx wrangler r2 bucket list` shows `openmusic-diag`. (If not, the deploy will 503 on every authenticated call — finish Plan 06 first.)
    4. Be aware: `git push origin main` deploys to openmusic.lol via Cloudflare Pages' Git integration within a few minutes. Cellular users get the new bundle. The change is additive (one Settings button + one new `/api/diag` route + one CORS allow-header) and nothing on the playback path was touched.
    Expected: gates green, commits reviewed, provisioning confirmed.
  </how-to-verify>
  <resume-signal>Type "approved" to push to main now, or describe what to change first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push, wait for the deploy, run the Tier-2 curl matrix against openmusic.lol</name>
  <files>.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-VALIDATION.md — "Tier 2 commands" (the curl set to run) and the Manual-Only table (what each result proves)
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — Pitfall 1 (`pnpm run deploy`, not `pnpm deploy`), Pitfall 4 (413 not 5xx), Pitfall 6 (a 500 on the first curl = illegal export)
    - src/routes/api/diag/+server.ts — the response contract to assert against
  </read_first>
  <action>
    Only after Task 1 resumed with "approved".
    1. `git push origin main`. Poll `curl -s -o /dev/null -w '%{http_code}' -X POST https://openmusic.lol/api/diag -d '[]'` every 20 s for up to 10 min until it returns `401` (pre-deploy the route 404s). If it never flips, run `pnpm run deploy` (NOT `pnpm deploy` — shadowed by a pnpm builtin) as the fallback deploy path and poll again.
    2. Load tokens into shell variables WITHOUT printing them: `UP=$(grep '^DIAG_UPLOAD_TOKEN=' .dev.vars | cut -d= -f2-)`, `RD=$(grep '^DIAG_READ_TOKEN=' .dev.vars | cut -d= -f2-)`.
    3. Build a sample payload: `node -e 'const e=[];for(let i=0;i<1500;i++)e.push({t:1789252228376+i*1000,ev:"media.progress",d:{ms:i,rs:4,ns:2,be:12.5}});process.stdout.write(JSON.stringify(e))' > /private/tmp/claude-501/-Users-laichan-code-tung-openmusic/890f7b66-5219-4dfb-8a61-9ea36430159f/scratchpad/sample-log.json` (~130 KB; a realistic full-session log).
    4. Run and record `%{http_code}` for each (T-33-01/04/05/13 on the real edge):
       a. POST, no token, `-d '[]'` -> expect 401
       b. POST, `Authorization: Bearer wrong`, `-d '[]'` -> expect 401
       c. POST, correct `$UP`, `--data-binary @sample-log.json`, `-H 'content-type: application/json'` -> expect 200 and a body `{"ok":true,"key":"log/…","entries":1500}`; capture `KEY` from the body with `node -e` or `sed`
       d. POST, correct `$UP`, body = `head -c 600000 /dev/urandom | base64` -> expect 413 (a 5xx here is the Workers CPU limit — T-33-04 failed; lower `MAX_UPLOAD_BYTES` and re-plan)
       e. POST, correct `$UP`, `-d '{"a":1}'` -> expect 400
       f. GET, `Authorization: Bearer $RD` -> expect 200 and the body's `logs[]` contains `KEY` (list-after-write, R2 strong consistency)
       g. GET `?key=$KEY`, `Bearer $RD` -> expect 200 and `cmp` of the response body against sample-log.json shows byte-identical (read-after-write)
       h. GET, `Authorization: Bearer $UP` (upload token on a read) -> expect 401 (T-33-07)
       i. GET `?key=../x`, `Bearer $RD` -> expect 400
       j. OPTIONS `-H 'Origin: https://localhost' -H 'Access-Control-Request-Method: POST' -H 'Access-Control-Request-Headers: authorization,content-type'` -> expect 204 and response headers `access-control-allow-headers` containing `Authorization` and `access-control-allow-origin: https://localhost` (T-33-06 proven at the edge; use `curl -sI -X OPTIONS`)
    5. If (c) returns 503 `unconfigured`: the R2 binding from `wrangler.jsonc` was not applied to the Git-integrated deploy. Record it and add to the Task 3 checkpoint text: in the Cloudflare dashboard -> Pages -> openmusic -> Settings -> Bindings, add R2 bucket binding `DIAG` -> `openmusic-diag`, then retry (c) after the next deploy. If (c) returns 500: illegal export (T-33-13) — inspect `+server.ts` exports.
    6. Write every status code (and the `KEY`, which is not secret) into 33-07-SUMMARY.md. Never write `$UP` / `$RD`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && [ "$(curl -s -o /dev/null -w '%{http_code}' -X POST https://openmusic.lol/api/diag -d '[]')" = "401" ] && curl -sI -X OPTIONS https://openmusic.lol/api/diag -H 'Origin: https://localhost' -H 'Access-Control-Request-Method: POST' -H 'Access-Control-Request-Headers: authorization' | grep -i '^access-control-allow-headers:' | grep -qi authorization</automated>
  </verify>
  <acceptance_criteria>
    - Curls (a)(b)(h) return 401; (c) returns 200 with `"ok":true` and a key matching `^log/.+\.json$`; (d) returns 413 (NOT any 5xx); (e) and (i) return 400; (f) returns 200 and lists the key from (c); (g) returns 200 and `cmp` against sample-log.json reports no differences
    - (j) returns 204 with `access-control-allow-headers` containing `Authorization` and `access-control-allow-origin: https://localhost`; no `access-control-allow-origin: *` anywhere (T-33-10)
    - No response body from (a)-(j) contains the upload or read token (`grep -c` of each captured body against `$UP`/`$RD` is 0 — check in-shell, do not print)
    - 33-07-SUMMARY.md records all ten status codes and the deploy method used (Git push vs `pnpm run deploy`); `grep -cE '[0-9a-f]{64}' 33-07-SUMMARY.md` returns 0
  </acceptance_criteria>
  <done>The live edge behaves per the response contract on every path; R2 read/list-after-write observed; CORS Allow-Headers fix verified on the deployed seam; oversize returns a clean 413 within the CPU budget.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tier 3 — phone upload, laptop fetch (the phase's success criterion)</name>
  <files>.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-07-SUMMARY.md — Task 2's Tier-2 status codes (and whether a 503 required a dashboard binding)
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-VALIDATION.md — "Tier 3 — the phase's actual success criterion" and the Manual-Only rows for `prompt()` (A1) and APK CORS (T-33-06)
  </read_first>
  <action>
    Before pausing, do the automatable part: build the APK with
    `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home pnpm apk` and confirm
    `android/app/build/outputs/apk/debug/app-debug.apk` exists (record its size). Then PAUSE, present
    `how-to-verify`, and WAIT. On resume, if the user reports success, record the fetched key and
    entry count in the SUMMARY (not the tokens). If they report a failure, classify it in the SUMMARY
    against: no prompt => A1 (text-input fallback needed); CORS error => T-33-06 (re-check curl (j));
    503 => binding not applied to the deploy; 401 => token mismatch (cleared on device, re-prompt).
    Do not attempt to fix production from this task — return the classification to the orchestrator.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test -s android/app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `android/app/build/outputs/apk/debug/app-debug.apk` exists and is non-empty after `pnpm apk`
    - User resumed with "approved" confirming: one tap uploaded from the APK (D-04/D-06), and `curl … /api/diag` then `curl … /api/diag?key=…` from the laptop returned that log within ~10 s (D-01/D-02)
    - Or: the SUMMARY records the failure classified as A1 / T-33-06 / binding / token, with the on-device `diag.upload {status}` row value
    - `grep -cE '[0-9a-f]{64}' 33-07-SUMMARY.md` returns 0
  </acceptance_criteria>
  <done>The phone-to-laptop round trip is confirmed on a real device, or the exact failure class is recorded for follow-up.</done>
  <what-built>`/api/diag` is live and passed the Tier-2 matrix (see 33-07-SUMMARY.md for status codes). The Settings -> Activity log screen has the Upload-log button. The cross-origin preflight from `https://localhost` is confirmed to allow `Authorization`.</what-built>
  <how-to-verify>
    1. Build and install the APK: `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home pnpm apk`, then install `android/app/build/outputs/apk/debug/app-debug.apk` on the phone. (The PWA at https://openmusic.lol also works for a same-origin check, but only the APK exercises the cross-origin preflight — T-33-06.)
    2. Play a couple of tracks so the Activity log has entries.
    3. Settings -> Activity log -> tap "Upload log" (the middle button). A prompt asks for the diagnostics upload token — paste the UPLOAD token (from `.dev.vars` `DIAG_UPLOAD_TOKEN`; NEVER the read token). Expected flash: "Log uploaded." (in your app language). Tap again — no prompt this time (token persisted), same flash.
       - No prompt appears at all => assumption A1 failed (`prompt()` in the Capacitor WebView); report it — the pre-planned fallback is a text input on the screen.
       - "Upload failed." => check the newest Activity-log row `diag.upload {status}`: 401 = wrong token (it was cleared; tap again to re-prompt), 413/400 = payload, 503 = binding, and a network/CORS error will show no status row — report which.
    4. Within ~10 s, on the laptop: `RD=$(grep '^DIAG_READ_TOKEN=' .dev.vars | cut -d= -f2-); curl -sS https://openmusic.lol/api/diag -H "Authorization: Bearer $RD"` — the newest `log/…` key should be the one just uploaded (list order is chronological). Then `curl -sS "https://openmusic.lol/api/diag?key=<that key>" -H "Authorization: Bearer $RD" | head -c 300` — you should see your own log entries. That round trip — phone tap, laptop read, zero copy-paste — is what this phase exists to deliver.
    5. Optional: run the same tap in the phone's browser at https://openmusic.lol/settings/activity (same-origin path).
  </how-to-verify>
  <resume-signal>Type "approved" if the log uploaded from the APK and was fetched from the laptop, or describe exactly what happened (no prompt / CORS error / status code).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Laptop -> `origin/main` | A push is a production deploy |
| Laptop shell -> transcript/SUMMARY | Token values must not cross into recorded output |
| Phone -> openmusic.lol | Cross-origin bearer POST from the APK WebView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Repudiation / Denial of Service | unapproved push auto-deploys a half-shipped phase | mitigate | Task 1 blocking `checkpoint:human-verify` BEFORE `git push`; prior incident `openmusic-pushes-autodeploy-live` |
| T-33-02 | Information Disclosure | tokens in transcript / SUMMARY | mitigate | Tokens loaded into shell vars via `grep|cut`, never echoed; hex-64 grep gate on the SUMMARY |
| T-33-04 | Denial of Service | CPU-limit 5xx on large uploads | mitigate | Curl (d) asserts 413 on the real edge; a 5xx triggers lowering `MAX_UPLOAD_BYTES` |
| T-33-06 | Denial of Service | APK preflight | mitigate | Curl (j) asserts Allow-Headers on the edge; Task 3 exercises the real WebView |
| T-33-07 | Information Disclosure | read token on the phone | mitigate | Checkpoint instructs pasting ONLY the upload token; the read token is used from the laptop curl |
| T-33-13 | Denial of Service | illegal export -> 500 | mitigate | First curl after deploy must be 401 not 500 |
| T-33-SC | Tampering | npm installs | accept | No packages installed in this phase |
</threat_model>

<verification>
- All Tier-2 status codes recorded and matching the contract; OPTIONS preflight includes `Authorization`
- Tier-3 device round trip approved by the user, or the failure classified against A1 / T-33-06 / binding
</verification>

<success_criteria>
- Production `/api/diag` enforces D-01/D-02/D-05 on the real edge and stores/serves via R2 (D-07)
- A log uploaded from the phone with one tap (D-04/D-06) is fetched from the laptop within seconds — the manual reproduce -> export -> paste loop is gone
</success_criteria>

<output>
Create `.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-07-SUMMARY.md` when done
</output>
