---
phase: 33-activity-log-upload-for-automated-diagnosis
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/en.ts
  - src/lib/i18n/zh-Hant.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/es.ts
  - src/lib/i18n/fr.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/pt.ts
  - src/lib/i18n/it.ts
  - src/lib/i18n/ru.ts
  - src/lib/i18n/tr.ts
  - src/lib/i18n/ar.ts
  - src/lib/i18n/hi.ts
  - src/lib/i18n/id.ts
  - src/lib/i18n/vi.ts
  - src/lib/i18n/th.ts
autonomous: true
requirements: [D-06]
tags: [i18n]

must_haves:
  truths:
    - "Every new user-facing string for the Upload-log control exists in ALL 15 locale dictionaries, key AND value in DOUBLE quotes, with key sets identical to `en` — so `t('settings.activityUpload')` etc. compile as `TranslationKey`s and `i18n.test.ts` parity passes (D-06)"
  artifacts:
    - path: "src/lib/i18n/en.ts"
      provides: "5 reference keys: settings.activityUpload, settings.activityUploadPrompt, settings.activityUploaded, settings.activityUploadFailed, settings.activityUploadEmpty"
      contains: "\"settings.activityUpload\""
    - path: "src/lib/i18n/zh-Hant.ts"
      provides: "same 5 keys, translated"
      contains: "\"settings.activityUploadEmpty\""
  key_links:
    - from: "src/lib/i18n/en.ts"
      to: "TranslationKey union (src/lib/i18n/index.ts)"
      via: "en defines the key type; a missing key in any use site is a compile error"
      pattern: "settings\\.activityUpload"
---

<objective>
Add the five user-facing strings the Settings -> Activity log "Upload log" control needs to all 15
locale dictionaries, in double quotes, placed with the existing `settings.activity*` group.

Purpose: D-06 — every new string in every dictionary; `en` defines `TranslationKey`, and
`i18n.test.ts:51-56` asserts every locale's key set is identical to `en`, so a key added to fewer
than 15 files fails the suite in every locale that lacks it. This is the phase's one mechanical
chunk (5 x 15 = 75 lines) and is isolated here so Plan 05 (the button) only has to consume the keys.
Output: 15 edited dictionaries; `pnpm test -- i18n` and `pnpm check` green.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-CONTEXT.md
@CLAUDE.md

@src/lib/i18n/index.ts
@src/lib/i18n/en.ts
@src/lib/i18n/zh-Hant.ts
@src/lib/i18n/i18n.test.ts

<interfaces>
Dictionaries (exactly these 15, from src/lib/i18n/index.ts:12-26; `detect.ts` is NOT a dictionary):
en, zh-Hant, zh-Hans, es, fr, de, pt, it, ru, tr, ar, hi, id, vi, th

Existing sibling group in en.ts (lines 143-148) — insert the new keys immediately after `settings.activityCopied` in EVERY file:
```ts
	// --- activity log (quick-260630-sgw) ---
	"settings.activityHeading": "Activity log",
	"settings.activityEmpty": "No activity recorded yet.",
	"settings.activityClear": "Clear log",
	"settings.activityCopy": "Copy log",
	"settings.activityCopied": "Activity log copied.",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 5 Upload-log keys to all 15 dictionaries (double quotes, identical key sets)</name>
  <files>src/lib/i18n/en.ts, src/lib/i18n/zh-Hant.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/es.ts, src/lib/i18n/fr.ts, src/lib/i18n/de.ts, src/lib/i18n/pt.ts, src/lib/i18n/it.ts, src/lib/i18n/ru.ts, src/lib/i18n/tr.ts, src/lib/i18n/ar.ts, src/lib/i18n/hi.ts, src/lib/i18n/id.ts, src/lib/i18n/vi.ts, src/lib/i18n/th.ts</files>
  <read_first>
    - src/lib/i18n/en.ts — the `settings.activity*` group (lines 143-148) and the file's quoting style
    - src/lib/i18n/zh-Hant.ts — the same group (lines 137-142) to see how the existing translations were phrased (terse, app-chrome register)
    - src/lib/i18n/i18n.test.ts — lines 45-56, the key-set parity assertion that will fail if any locale is missed
    - CLAUDE.md — "i18n — DOUBLE QUOTES Convention"
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "i18n cost (D-06)" incl. the two rules the executor gets wrong (double quotes; keep flash text token-free, no interpolation)
  </read_first>
  <action>
    In each of the 15 dictionary files, insert these five entries directly after the
    `"settings.activityCopied"` line, matching the surrounding indentation (tabs) and DOUBLE quotes
    for both key and value (D-06; no formatter enforces this — it is manual). `en` values:
    `"settings.activityUpload": "Upload log"`,
    `"settings.activityUploadPrompt": "Paste the diagnostics upload token"`,
    `"settings.activityUploaded": "Log uploaded."`,
    `"settings.activityUploadFailed": "Upload failed."`,
    `"settings.activityUploadEmpty": "Nothing to upload."`.
    Precede the block in `en.ts` only with a one-line comment `// --- activity log upload (Phase 33 / D-06) ---`.

    Translate the values for the other 14 locales in the same terse app-chrome register as the
    existing `settings.activityCopy` / `settings.activityCopied` entries in each file. No
    interpolation placeholders — the flash text is deliberately token-free so nothing has to survive
    translation intact (RESEARCH "i18n cost" rule 2). Keys are byte-identical across all 15 files.

    Because project memory records grep returning false-empty for real matches, audit with BOTH
    `grep -c '"settings.activityUpload' <file>` (expect 5 per file) AND a `sed -n` / Read of the
    inserted block in at least `en.ts`, `zh-Hant.ts` and `ar.ts` before declaring done.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- i18n && for f in en zh-Hant zh-Hans es fr de pt it ru tr ar hi id vi th; do n=$(grep -c '"settings.activityUpload' src/lib/i18n/$f.ts); [ "$n" = "5" ] || { echo "FAIL $f=$n"; exit 1; }; done && [ "$(grep -rcE "^\s+'settings\.activityUpload" src/lib/i18n/ | grep -v ':0' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- i18n` exits 0 (the "every locale exposes a key set IDENTICAL to en" test passes with the 5 new keys)
    - For EACH of the 15 files, `grep -c '"settings.activityUpload' src/lib/i18n/<locale>.ts` returns exactly 5 (the prefix matches all five keys)
    - `grep -rcE "^\s+'settings\.activityUpload" src/lib/i18n/` shows 0 matches in every file (no single-quoted key)
    - `grep -cE '"settings\.activityUpload[A-Za-z]*": "[^"]*"' src/lib/i18n/th.ts` returns 5 (values double-quoted too; th is the spot-check for a non-Latin script)
    - `grep -c "{" src/lib/i18n/en.ts` count is unchanged relative to before the edit for the five new lines (no `{placeholder}` interpolation introduced in the new values — check `grep -E '"settings\.activityUpload[A-Za-z]*": ".*\{' src/lib/i18n/*.ts` returns nothing)
    - `pnpm check` exits 0 (`TranslationKey` union now includes the five keys; nothing else broken)
  </acceptance_criteria>
  <done>All 15 dictionaries carry the 5 keys with double-quoted keys and values, parity test green, typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static UI strings only; no untrusted input, no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02 | Information Disclosure | flash strings | mitigate | Flash text is token-free by design — no key/token is ever interpolated into a user-visible string, so a screenshot of the Settings screen leaks nothing |
| T-33-SC | Tampering | npm installs | accept | No packages installed in this phase |
</threat_model>

<verification>
- `pnpm test -- i18n` exits 0; `pnpm check` exits 0
- Per-file count of `"settings.activityUpload` == 5 for all 15 locales; zero single-quoted new keys
</verification>

<success_criteria>
- The five `TranslationKey`s exist so Plan 05 compiles against them
- D-06 quoting convention honoured in every file; parity test enforces the identical key set
</success_criteria>

<output>
Create `.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-03-SUMMARY.md` when done
</output>
