---
phase: 33-activity-log-upload-for-automated-diagnosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/proxy/diag-auth.ts
  - src/lib/proxy/diag-auth.test.ts
  - src/lib/proxy/diag-payload.ts
  - src/lib/proxy/diag-payload.test.ts
autonomous: true
requirements: [D-01, D-03, D-05]
tags: [cloudflare, security, validation, proxy]

must_haves:
  truths:
    - "An absent or blank DIAG_UPLOAD_TOKEN / DIAG_READ_TOKEN makes bearerMatches return false for EVERY header — the gate fails CLOSED, never open (D-01, D-03)"
    - "bearerMatches accepts only an exact `Bearer <token>` match; a prefix, a suffix-extended token, a different scheme, or an empty credential is rejected without early-exit on length (T-33-08)"
    - "screenLogPayload rejects an empty, oversize (> 512 KiB), non-JSON, non-array, or all-malformed body with `null` and accepts a real serializeActionLog() payload with its entry count — screen -> parse -> validate, reusing parseActionLog, no second validator (D-05)"
    - "diagKey(now, rand) yields `log/<iso-with-[:.]->-><rand8>.json` so R2 lexicographic list order equals chronological order, and isDiagKey accepts exactly that shape and rejects traversal / foreign prefixes (T-33-09)"
  artifacts:
    - path: "src/lib/proxy/diag-auth.ts"
      provides: "bearerMatches(header, expected): Promise<boolean> — SHA-256 digest-then-XOR compare, fail closed"
      exports: ["bearerMatches"]
      contains: "crypto.subtle.digest"
    - path: "src/lib/proxy/diag-payload.ts"
      provides: "MAX_UPLOAD_BYTES, screenLogPayload, diagKey, isDiagKey"
      exports: ["MAX_UPLOAD_BYTES", "screenLogPayload", "diagKey", "isDiagKey"]
      contains: "parseActionLog"
    - path: "src/lib/proxy/diag-auth.test.ts"
      provides: "fail-closed + scheme + prefix/suffix + exact-match cases"
      min_lines: 30
    - path: "src/lib/proxy/diag-payload.test.ts"
      provides: "size / shape / entry-count / key-format / key-screen cases"
      min_lines: 40
  key_links:
    - from: "src/lib/proxy/diag-payload.ts"
      to: "src/lib/diagnostics/action-log-logic.ts"
      via: "import { parseActionLog, ACTION_LOG_CAP }"
      pattern: "import \\{[^}]*parseActionLog[^}]*\\} from '\\$lib/diagnostics/action-log-logic'"
    - from: "src/lib/proxy/diag-auth.ts"
      to: "Web Crypto (workerd + Node 22 identical)"
      via: "crypto.subtle.digest('SHA-256', …)"
      pattern: "subtle\\.digest\\('SHA-256'"
---

<objective>
Create the two pure, node-testable helpers every other plan in this phase builds on: the bearer-token
gate (`$lib/proxy/diag-auth.ts`) and the D-05 payload screen + key helpers (`$lib/proxy/diag-payload.ts`),
each with a co-located Vitest file. These are contracts first: Plan 04 (the route) imports exactly
these names and nothing else.

Purpose: D-01/D-03 need an auth check whose PRODUCTION path is the TESTED path (so no
`crypto.subtle.timingSafeEqual`, which is Cloudflare-only and absent from the node Vitest runtime),
and D-05 needs a server-side validator that reuses the already-tested `parseActionLog` instead of a
second one that will drift. Both live in `$lib/proxy/*.ts` — NEVER in `+server.ts`, because a
non-verb export there 500s at request time (commit `29c1c7d`, Pitfall 6).
Output: `diag-auth.ts`, `diag-auth.test.ts`, `diag-payload.ts`, `diag-payload.test.ts`; `pnpm test -- diag-` green.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-CONTEXT.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md
@CLAUDE.md

@src/lib/proxy/safe-image-url.ts
@src/lib/proxy/safe-image-url.test.ts
@src/lib/diagnostics/action-log-logic.ts
@src/lib/diagnostics/action-log-logic.test.ts

<interfaces>
<!-- Existing exports the executor consumes. Use directly — no exploration. -->

From src/lib/diagnostics/action-log-logic.ts:
```typescript
export const ACTION_LOG_CAP = 2000;
export interface ActionLogEntry { t: number; ev: string; d?: Record<string, unknown>; n?: number; tl?: number; }
/** [] on null / parse error / non-array; filters out entries lacking {t:number, ev:string}. Never throws. */
export function parseActionLog(raw: string | null): ActionLogEntry[];
export function serializeActionLog(entries: ActionLogEntry[]): string;   // JSON.stringify(entries)
```

Contracts THIS plan creates (Plan 04 imports these exact names):
```typescript
// src/lib/proxy/diag-auth.ts
export async function bearerMatches(header: string | null, expected: string | undefined): Promise<boolean>;

// src/lib/proxy/diag-payload.ts
export const MAX_UPLOAD_BYTES: number;                         // 512 * 1024
export function screenLogPayload(text: string): number | null; // entry count, or null on reject
export function diagKey(now: number, rand: string): string;    // 'log/<iso>-<rand8>.json'
export function isDiagKey(key: string | null): key is string;  // /^log\/[A-Za-z0-9._-]+\.json$/
```

Vitest: single node project, `expect: { requireAssertions: true }` — a test with no assertion FAILS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bearerMatches — fail-closed, digest-then-XOR bearer check</name>
  <files>src/lib/proxy/diag-auth.ts, src/lib/proxy/diag-auth.test.ts</files>
  <read_first>
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "Auth Mechanism" section (the two-token rationale and the constant-time comparison block; replicate its rules, not its prose)
    - src/lib/proxy/safe-image-url.ts — the posture-comment header style every new proxy helper copies (lines 1-19: "every check is load-bearing")
    - src/lib/proxy/safe-image-url.test.ts — the test style: one `describe` per guard dimension, each `it` comment naming WHY the assertion exists
    - CLAUDE.md — Conventions (tabs, single quotes, named exports, `import type`, comment density with decision refs)
  </read_first>
  <behavior>
    - returns false when `expected` is undefined even for header 'Bearer anything' — an UNCONFIGURED secret fails CLOSED (D-01/D-03; the load-bearing case)
    - returns false when `expected` is '' (blank secret is also unconfigured)
    - returns false when header is null
    - returns false for a non-Bearer scheme ('Token abc', 'Basic abc') and for 'Bearer ' with an empty credential
    - returns false when the presented token is a strict PREFIX of the expected one ('secret-12' vs 'secret-1234') and when it is the expected one plus a suffix ('secret-1234x')
    - returns true for exactly 'Bearer secret-1234' against expected 'secret-1234'
    - is a Promise<boolean> (all cases `await`ed)
  </behavior>
  <action>
    Write the failing tests first (RED), run `pnpm test -- diag-auth`, confirm they fail on a missing
    module, then create `src/lib/proxy/diag-auth.ts` exporting ONE function,
    `bearerMatches(header: string | null, expected: string | undefined): Promise<boolean>`, plus a
    module-private `sha256(s: string): Promise<Uint8Array>` built on
    `crypto.subtle.digest('SHA-256', new TextEncoder().encode(s))`.

    Rules, in order: `!expected` -> false; header not starting with the literal `'Bearer '` -> false;
    empty credential after the scheme -> false; otherwise digest BOTH strings (in parallel with
    `Promise.all`), XOR-accumulate all 32 bytes into one `diff` integer with NO early exit, return
    `diff === 0`. Comparing digests rather than raw strings means both operands are always 32 bytes,
    so no length-mismatch branch exists to leak the token length (T-33-08).

    Header comment must record, with refs: (a) D-01/D-03 fail-closed posture — an unconfigured
    secret rejects everything, contrast with the fail-OPEN optional-key routes like `/api/similar`
    which is correct there and wrong here; (b) why NOT `crypto.subtle.timingSafeEqual` — it is a
    Cloudflare-only Web Crypto extension absent from Node 22, so using it would make this security
    control's production path untestable under the project's single node Vitest project; (c) that
    `crypto.subtle.digest` + `TextEncoder` are globals on BOTH workerd and Node 22 so the tested path
    IS the production path. Tabs, single quotes, named export only, no `as any` in the source file.
    Then GREEN: `pnpm test -- diag-auth` passes.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- diag-auth</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- diag-auth` exits 0 with >= 7 passing tests in `src/lib/proxy/diag-auth.test.ts`
    - `grep -c "timingSafeEqual" src/lib/proxy/diag-auth.ts` returns a value >= 1 ONLY inside a comment (the identifier appears in the "why not" comment) and `grep -c "subtle.timingSafeEqual(" src/lib/proxy/diag-auth.ts` returns 0 (never called)
    - `grep -c "subtle.digest('SHA-256'" src/lib/proxy/diag-auth.ts` returns 1
    - `grep -cE "^export " src/lib/proxy/diag-auth.ts` returns 1 (single named export: `bearerMatches`)
    - `grep -c "as any" src/lib/proxy/diag-auth.ts` returns 0
    - The test file contains an `it(` whose title includes the word "closed" or "undefined" and asserts `await bearerMatches('Bearer x', undefined)` is `false` (T-33-01 fail-closed evidence)
  </acceptance_criteria>
  <done>bearerMatches exists as the ONLY export of diag-auth.ts; all behaviour cases pass; the fail-closed-on-undefined case is an explicit named test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: screenLogPayload + diagKey + isDiagKey — the D-05 screen and the R2 key helpers</name>
  <files>src/lib/proxy/diag-payload.ts, src/lib/proxy/diag-payload.test.ts</files>
  <read_first>
    - src/lib/proxy/safe-image-url.ts — the D-05 precedent: screen (cheapest) -> parse (in try/catch) -> validate -> return a never-throw sentinel; copy its header-comment style
    - src/lib/proxy/safe-image-url.test.ts — including the "rejects a lookalike that merely ENDS WITH …" style of adversarial case
    - src/lib/diagnostics/action-log-logic.ts — `parseActionLog` (lines 33-47) IS the parse+validate step; `ACTION_LOG_CAP` (line 15) IS the entry-count ceiling; `serializeActionLog` (line 84) produces the real payload the tests should feed in
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "Reading the body with a size cap", "Key naming", Pitfall 4 (10 ms CPU), and the `?key=` path-traversal row in Security Domain
  </read_first>
  <behavior>
    - screenLogPayload('') -> null; screenLogPayload('[]') -> null (nothing to store); 'not json' -> null; '{"a":1}' (non-array) -> null; '[{"x":1}]' (every entry malformed) -> null
    - screenLogPayload('x'.repeat(MAX_UPLOAD_BYTES + 1)) -> null WITHOUT parsing (oversize screened before JSON.parse)
    - screenLogPayload(serializeActionLog([{t:1,ev:'a'},{t:2,ev:'b',d:{ms:1},n:3,tl:5}])) -> 2 (a real payload, coalesced fields preserved)
    - screenLogPayload('[{"t":1,"ev":"a"},{"bad":true}]') -> 1 (parseActionLog's per-entry filter drops junk; the count is of VALID entries)
    - screenLogPayload(serializeActionLog(<ACTION_LOG_CAP + 1 valid entries>)) -> null (more than the client can even hold)
    - diagKey(Date.UTC(2026,8,12,14,3,22,123), 'a1b2c3d4-ffff-…') === 'log/2026-09-12T14-03-22-123Z-a1b2c3d4.json'
    - diagKey with a later `now` sorts lexicographically AFTER an earlier one; same `now` with different `rand` yields different keys
    - isDiagKey accepts every diagKey() output; rejects null, '', 'log/../x.json', 'other/a.json', 'log/a b.json', 'log/a.json?x', 'log/sub/a.json', and 'log/a.txt'
    - MAX_UPLOAD_BYTES === 512 * 1024
  </behavior>
  <action>
    RED first: write `src/lib/proxy/diag-payload.test.ts` covering the behaviour list, run
    `pnpm test -- diag-payload`, confirm failure on the missing module. Then create
    `src/lib/proxy/diag-payload.ts` with exactly four named exports:

    `MAX_UPLOAD_BYTES = 512 * 1024` — header comment: sized from the MEASURED full-cap payload
    (2000 entries x ~82 B ~= 160 KB) with 3x headroom for name-heavy logs, and kept low enough that
    `JSON.parse` stays inside the Workers FREE 10 ms CPU budget; raising it to be "generous" trades
    a clean 413 for an opaque CPU-limit 5xx (Pitfall 4, T-33-04). Do not hard-code a second entry
    ceiling — import `ACTION_LOG_CAP` and `parseActionLog` from `$lib/diagnostics/action-log-logic`.

    `screenLogPayload(text: string): number | null` — order is load-bearing (D-05, mirrors
    safeImageUrl): (1) `!text || text.length > MAX_UPLOAD_BYTES` -> null, before any parse;
    (2) `parseActionLog(text)` (already try/catch + per-entry filter); (3) `entries.length === 0`
    -> null; `entries.length > ACTION_LOG_CAP` -> null; else return `entries.length`. Comment that
    `text.length` counts UTF-16 code units and under-counts CJK bytes up to 3x — accepted
    deliberately because this is a safety bound, not an accounting figure, and `TextEncoder` on a
    several-hundred-KB string spends the CPU we do not have.

    `diagKey(now: number, rand: string): string` — returns
    `log/${new Date(now).toISOString().replace(/[:.]/g, '-')}-${rand.slice(0, 8)}.json`. `now` and
    `rand` are PARAMETERS so the function is deterministic and testable (the route passes
    `Date.now()` and `crypto.randomUUID()`); comment that R2 lists keys in lexicographic UTF-8 order
    so this prefix makes list order == chronological order.

    `isDiagKey(key: string | null): key is string` — `/^log\/[A-Za-z0-9._-]+\.json$/`. Comment: R2
    keys are a flat namespace so this is defence-in-depth against a `?key=` that is not one of ours
    (T-33-09), and it also means the fetch-one endpoint can only ever return objects the upload
    endpoint could have written.

    GREEN: `pnpm test -- diag-payload` passes. Tabs, single quotes, `import type` where type-only.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- diag-payload</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- diag-payload` exits 0 with >= 12 passing tests
    - `grep -c "from '\$lib/diagnostics/action-log-logic'" src/lib/proxy/diag-payload.ts` returns 1, and that import line names both `parseActionLog` and `ACTION_LOG_CAP`
    - `grep -cE "^export (const|function) (MAX_UPLOAD_BYTES|screenLogPayload|diagKey|isDiagKey)" src/lib/proxy/diag-payload.ts` returns 4 and `grep -cE "^export " src/lib/proxy/diag-payload.ts` returns 4 (no other exports)
    - `grep -c "512 \* 1024" src/lib/proxy/diag-payload.ts` returns 1
    - `grep -c "JSON.parse" src/lib/proxy/diag-payload.ts` returns 0 (parsing is delegated to parseActionLog — no second validator, D-05)
    - `grep -c "2000" src/lib/proxy/diag-payload.ts` returns 0 outside comments (`grep -v '^\s*//' src/lib/proxy/diag-payload.ts | grep -v '^\s*\*' | grep -c 2000` returns 0)
    - The test file contains an assertion that `screenLogPayload('x'.repeat(MAX_UPLOAD_BYTES + 1))` is `null` and one that `isDiagKey('log/../x.json')` is `false`
  </acceptance_criteria>
  <done>Four exports exist with the stated signatures; oversize / empty / malformed / over-cap all return null; a real serializeActionLog payload returns its entry count; key format and key screen are round-trip tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> `bearerMatches` | Untrusted `Authorization` header value crosses here (consumed by Plan 04's route) |
| Internet -> `screenLogPayload` / `isDiagKey` | Untrusted request body / `?key=` query crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Spoofing | `bearerMatches` | mitigate | Fail CLOSED on undefined/blank secret; exact `Bearer ` scheme; digest compare. Tested in Task 1 |
| T-33-04 | Denial of Service | `screenLogPayload` | mitigate | Length screen BEFORE parse at 512 KiB; `parseActionLog` reuse; `ACTION_LOG_CAP` ceiling. Tested in Task 2 |
| T-33-08 | Information Disclosure | `bearerMatches` timing oracle | mitigate | SHA-256 digest-then-XOR over fixed 32 bytes, no early exit, no length branch. Remote timing over the internet is infeasible anyway — cheap insurance |
| T-33-09 | Tampering | `isDiagKey` (`?key=` screen) | mitigate | `/^log\/[A-Za-z0-9._-]+\.json$/` before any `bucket.get`; R2 keys are flat so this is defence-in-depth |
| T-33-SC | Tampering | npm installs | accept | This phase installs NO packages (no `pnpm add`); `@cloudflare/workers-types` and Vitest already present. Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `pnpm test -- diag-` exits 0 (both new files); `pnpm test` stays >= 2023 baseline + new tests
- `pnpm check` exits 0 (both helpers compile under strict TS; no runes, plain `.ts`)
- Neither file imports from `$lib/stores/*` or `$app/*` (pure, store-free, node-testable)
</verification>

<success_criteria>
- `bearerMatches` and the four payload helpers exist with the exact signatures in `<interfaces>` so Plan 04 can import them without exploration
- Fail-closed on unconfigured secret, oversize-before-parse, and key-shape screen are each an explicit named test
- No `crypto.subtle.timingSafeEqual` call, no `JSON.parse`, no second `2000` literal
</success_criteria>

<output>
Create `.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-01-SUMMARY.md` when done
</output>
