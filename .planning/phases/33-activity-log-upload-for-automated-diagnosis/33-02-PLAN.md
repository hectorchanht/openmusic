---
phase: 33-activity-log-upload-for-automated-diagnosis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/proxy/http.ts
  - src/lib/proxy/http.test.ts
  - src/lib/proxy/proxy-types.ts
  - src/app.d.ts
  - wrangler.jsonc
autonomous: true
requirements: [D-03, D-07]
tags: [cloudflare, cors, config, proxy]

must_haves:
  truths:
    - "The shared CORS seam advertises `Authorization` in Access-Control-Allow-Headers so the cross-origin APK preflight for a bearer-token POST succeeds — with Access-Control-Allow-Origin still never `*` (T-33-06, T-33-10)"
    - "`Env` types the R2 binding `DIAG` and the two secrets `DIAG_UPLOAD_TOKEN` / `DIAG_READ_TOKEN` as OPTIONAL edge-side members — the only place tokens are ever typed, never a `VITE_` var (D-03)"
    - "`wrangler.jsonc` declares the R2 binding `DIAG` -> bucket `openmusic-diag`, the storage backend chosen against Cloudflare free-tier limits (D-07 — R2 per RESEARCH.md: strong read-after-write + list-after-write, unlike KV)"
  artifacts:
    - path: "src/lib/proxy/http.ts"
      provides: "corsHeaders with Authorization allowed"
      contains: "Content-Type, Range, Authorization"
    - path: "src/lib/proxy/http.test.ts"
      provides: "Allow-Headers assertion on the shared security seam"
      contains: "Authorization"
    - path: "src/lib/proxy/proxy-types.ts"
      provides: "Env.DIAG?: R2Bucket; Env.DIAG_UPLOAD_TOKEN?: string; Env.DIAG_READ_TOKEN?: string"
      contains: "DIAG?: R2Bucket"
    - path: "src/app.d.ts"
      provides: "App.Platform.env mirror of the three new members"
      contains: "DIAG_READ_TOKEN"
    - path: "wrangler.jsonc"
      provides: "r2_buckets binding DIAG -> openmusic-diag"
      contains: "\"binding\": \"DIAG\""
  key_links:
    - from: "src/hooks.server.ts"
      to: "src/lib/proxy/http.ts corsHeaders"
      via: "OPTIONS preflight answered with corsHeaders(origin) — no route-level CORS"
      pattern: "corsHeaders\\(origin\\)"
    - from: "wrangler.jsonc r2_buckets[0].binding"
      to: "src/lib/proxy/proxy-types.ts Env.DIAG"
      via: "identical binding name DIAG"
      pattern: "DIAG"
---

<objective>
Wire the three shared seams the new route depends on: the CORS allow-headers one-word fix in
`proxy/http.ts` (with its own assertion), the `Env` typing of the R2 binding + two secrets, and the
`r2_buckets` binding in `wrangler.jsonc`.

Purpose: `corsHeaders` currently emits `Access-Control-Allow-Headers: 'Content-Type, Range'`. Web is
same-origin so it works; the APK (`https://localhost` -> `https://openmusic.lol`) preflights and
FAILS on an `Authorization` header (Pitfall 2, T-33-06). This is a change to a shared security seam,
so it gets a test. The typing makes `env.DIAG` compile; the binding makes it exist at the edge.
Output: four edited files + one new `it()` in `http.test.ts`; `pnpm test -- http` and `pnpm check` green.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-CONTEXT.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md
@CLAUDE.md

@src/lib/proxy/http.ts
@src/lib/proxy/http.test.ts
@src/hooks.server.ts
@src/lib/proxy/proxy-types.ts
@src/app.d.ts
@wrangler.jsonc

<interfaces>
From src/lib/proxy/http.ts (line 32-42, current):
```typescript
export function corsHeaders(origin: string | null): Record<string, string>
// emits Vary: Origin, Access-Control-Allow-Methods: 'GET, POST, OPTIONS',
// Access-Control-Allow-Headers: 'Content-Type, Range'   <- Authorization MISSING
// Access-Control-Allow-Origin ONLY when origin matches ALLOWED_ORIGIN_PATTERNS (never '*')
```

From src/lib/proxy/proxy-types.ts:
```typescript
export interface Env { JOOX_TOKEN: string; LASTFM_KEY?: string; LASTFM_SECRET?: string; JAMENDO_CLIENT_ID?: string; AZURE_TRANSLATOR_KEY?: string; AZURE_TRANSLATOR_REGION?: string; DEEPL_KEY?: string; }
```
`R2Bucket` is a GLOBAL type — `@cloudflare/workers-types` is already in `tsconfig.json` `types`; no import needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `Authorization` to the shared Access-Control-Allow-Headers, with a seam assertion</name>
  <files>src/lib/proxy/http.ts, src/lib/proxy/http.test.ts</files>
  <read_first>
    - src/lib/proxy/http.ts — `corsHeaders` (lines 32-42) and the file header explaining T-01-02 (never an open relay)
    - src/lib/proxy/http.test.ts — the existing `describe('corsHeaders (T-01-02 …)')` block; the new `it` goes inside it
    - src/hooks.server.ts — confirms the preflight is answered with `corsHeaders(origin)` and NEVER falls into route logic; the new route must add NO CORS code of its own
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — Pitfall 2 (why the APK fails preflight and why widening Allow-Origin is the forbidden "fix")
  </read_first>
  <action>
    In `src/lib/proxy/http.ts` change the `'Access-Control-Allow-Headers'` value from
    `'Content-Type, Range'` to `'Content-Type, Range, Authorization'`. Add a trailing line comment
    carrying the ref: Phase 33 / T-33-06 — `Authorization` is not a CORS-safelisted request header,
    so the Capacitor WebView (`https://localhost` -> `https://openmusic.lol`) preflights the bearer
    POST; Allow-Headers only tells the browser which headers an ALREADY allowlisted origin may send
    and grants nothing to a non-allowlisted one. Do NOT touch `ALLOWED_ORIGIN_PATTERNS` or the
    Allow-Origin logic (T-33-10: never `*`).

    In `src/lib/proxy/http.test.ts`, inside the existing `corsHeaders` describe, add one `it` titled
    to name T-33-06: split `corsHeaders('https://localhost')['Access-Control-Allow-Headers']` on
    `,`, trim, and assert the list contains `'Authorization'`, `'Content-Type'` AND `'Range'` (the
    fix must add, not replace). In the same `it`, assert `corsHeaders('https://evil.example.com')`
    still has NO `Access-Control-Allow-Origin` — proving the header addition did not widen origin
    trust.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- http</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'Content-Type, Range, Authorization'" src/lib/proxy/http.ts` returns 1
    - `grep -c "'Content-Type, Range'" src/lib/proxy/http.ts` returns 0
    - `grep -c "T-33-06" src/lib/proxy/http.ts` returns >= 1 and `grep -c "T-33-06" src/lib/proxy/http.test.ts` returns >= 1
    - `grep -c "Access-Control-Allow-Origin.*\*\|'\*'" src/lib/proxy/http.ts` returns 0 (no wildcard origin introduced)
    - `pnpm test -- http` exits 0 and reports one more test than before the change (baseline: 6 `it` blocks in http.test.ts -> 7)
  </acceptance_criteria>
  <done>Allow-Headers includes Authorization alongside the existing two; a named test guards it; origin allowlist behaviour is unchanged and re-asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Type the R2 binding + two secrets in Env / App.Platform.env and declare the binding in wrangler.jsonc</name>
  <files>src/lib/proxy/proxy-types.ts, src/app.d.ts, wrangler.jsonc</files>
  <read_first>
    - src/lib/proxy/proxy-types.ts — the `Env` interface and the comment style on `LASTFM_KEY` / `DEEPL_KEY` (each optional member documents: server-only, never echoed, what "absent" means)
    - src/app.d.ts — `App.Platform.env` (the drifted mirror declaration — it lacks JAMENDO/AZURE/DEEPL already; do not "fix" the drift, just add the three new members)
    - wrangler.jsonc — current file (only `vars`; NO `[env.*]` blocks)
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "Binding config — exact", "Typing the binding — TWO files", and the non-inheritable-keys caveat
  </read_first>
  <action>
    `src/lib/proxy/proxy-types.ts` — append three OPTIONAL members to `Env`, each with a doc comment
    in the house style: `DIAG?: R2Bucket` (Phase 33 / D-07: R2 bucket holding maintainer-uploaded
    activity logs; ABSENT under `vite dev` and unit tests is a SUPPORTED state — the route 503s
    rather than throwing, same posture as an absent LASTFM_KEY); `DIAG_UPLOAD_TOKEN?: string`
    (D-01/D-03: bearer token required to WRITE a log; held on the maintainer's device; absent =>
    uploads disabled, fail closed, never fail open); `DIAG_READ_TOKEN?: string` (D-02/D-03: bearer
    token required to LIST/FETCH; NEVER shipped to any device — used only from curl on the
    maintainer's laptop; absent => reads disabled). `R2Bucket` is a global from
    `@cloudflare/workers-types` (already in `tsconfig.json` `types`) — add NO import.

    `src/app.d.ts` — add the same three members to `App.Platform.env` with one-line comments
    pointing at `proxy-types.ts` as the functional contract (every route casts `platform?.env as Env`).

    `wrangler.jsonc` — add, after the `vars` block, an `r2_buckets` array with one entry
    `{ "binding": "DIAG", "bucket_name": "openmusic-diag" }`. Precede it with a `//` comment: Phase
    33 / D-07 diagnostics log store; bucket holds ONLY maintainer-uploaded activity logs; write and
    read paths are token-gated (D-01/D-02/D-03); secrets `DIAG_UPLOAD_TOKEN` / `DIAG_READ_TOKEN`
    are set via `wrangler pages secret put`, NEVER as `vars`. Add a second comment line: `vars`,
    `kv_namespaces` and `r2_buckets` are NON-INHERITABLE — if anyone later adds an `env.preview`
    block they must re-declare BOTH `vars` and `r2_buckets` inside it or `JAMENDO_CLIENT_ID`
    silently disappears from previews. The bucket name `openmusic-diag` is the name Plan 06's
    human checkpoint creates — they must match exactly.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check && node -e "const s=require('fs').readFileSync('wrangler.jsonc','utf8').replace(/\/\/.*$/gm,'');const j=JSON.parse(s);if(j.r2_buckets?.[0]?.binding!=='DIAG'||j.r2_buckets[0].bucket_name!=='openmusic-diag'||j.vars?.JAMENDO_CLIENT_ID!=='1df0a42f')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0 (R2Bucket resolves globally; no new import added: `grep -c "workers-types" src/lib/proxy/proxy-types.ts` returns 0)
    - `grep -c "DIAG?: R2Bucket" src/lib/proxy/proxy-types.ts` returns 1; `grep -c "DIAG_UPLOAD_TOKEN?: string" src/lib/proxy/proxy-types.ts` returns 1; `grep -c "DIAG_READ_TOKEN?: string" src/lib/proxy/proxy-types.ts` returns 1
    - `grep -c "DIAG" src/app.d.ts` returns >= 3
    - `wrangler.jsonc` parses (after stripping `//` comments) with `r2_buckets[0] == { binding: 'DIAG', bucket_name: 'openmusic-diag' }` and `vars.JAMENDO_CLIENT_ID` still `'1df0a42f'` (the node one-liner in `<verify>` exits 0)
    - `grep -c "DIAG_UPLOAD_TOKEN\|DIAG_READ_TOKEN" wrangler.jsonc` returns >= 1 ONLY in comment lines: `grep -v '^\s*//' wrangler.jsonc | grep -c "DIAG_.*TOKEN"` returns 0 (secrets never as `vars`, D-03)
    - `grep -rc "VITE_DIAG" src/ wrangler.jsonc | grep -v ':0' | wc -l` returns 0 (no build-time token, T-33-03)
  </acceptance_criteria>
  <done>Env and App.Platform.env carry DIAG / DIAG_UPLOAD_TOKEN / DIAG_READ_TOKEN as optional; wrangler.jsonc binds DIAG -> openmusic-diag with the non-inheritable-keys warning; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/WebView -> `/api/*` preflight | Cross-origin OPTIONS from the APK origin `https://localhost` crosses the CORS seam |
| Build -> client bundle | Anything `VITE_`-prefixed is inlined into public JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Information Disclosure | token in client bundle | mitigate | Tokens typed ONLY in `Env` (`platform.env`); grep gate `VITE_DIAG` == 0; wrangler `vars` never carries them (D-03) |
| T-33-06 | Denial of Service (availability) | `corsHeaders` Allow-Headers | mitigate | Add `Authorization` to Allow-Headers; assert in `http.test.ts`; verified live by an OPTIONS curl in Plan 07 |
| T-33-10 | Elevation of Privilege | `corsHeaders` Allow-Origin | mitigate | `ALLOWED_ORIGIN_PATTERNS` untouched; existing "never `*`" tests still pass; new test re-asserts no Allow-Origin for a foreign origin |
| T-33-SC | Tampering | npm installs | accept | No packages installed in this phase |
</threat_model>

<verification>
- `pnpm test -- http` exits 0 with the new Allow-Headers assertion
- `pnpm check` exits 0
- `pnpm test` full suite >= 2023 + new tests, no failures
</verification>

<success_criteria>
- One-word CORS fix landed with a named test; origin allowlist provably unchanged
- `env.DIAG`, `env.DIAG_UPLOAD_TOKEN`, `env.DIAG_READ_TOKEN` compile in any `+server.ts` via the established `platform?.env as Env | undefined` cast
- `wrangler.jsonc` declares the `DIAG` -> `openmusic-diag` R2 binding (D-07) and documents the non-inheritable-keys trap
</success_criteria>

<output>
Create `.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-02-SUMMARY.md` when done
</output>
