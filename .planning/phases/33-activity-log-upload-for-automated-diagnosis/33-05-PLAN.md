---
phase: 33-activity-log-upload-for-automated-diagnosis
plan: 05
type: execute
wave: 2
depends_on: [33-03]
files_modified:
  - src/routes/(app)/settings/activity/+page.svelte
autonomous: true
requirements: [D-03, D-04, D-06]
tags: [svelte, settings, ui]

must_haves:
  truths:
    - "Upload happens ONLY when the user taps the 'Upload log' button — the handler is bound to `onclick`, there is no `$effect`, `onMount`, `setInterval`, `setTimeout`-retry, or catch-and-retry path; a failure flashes once and STOPS (D-04)"
    - "The Settings -> Activity log screen gains exactly ONE new control — a third `<button class=\"item\" … use:tapBounce>` in the existing `.actions` row — with no new component, page, or style block, and every label read via `t()` from the Plan 03 keys (D-06)"
    - "The upload token is entered by the user (prompt on first use), persisted under `openmusic:diag:v1`, cleared on a 401, and is NEVER a build-time value — no `import.meta.env.VITE_*` credential exists in the client (D-03, T-33-03)"
    - "The upload payload is `serializeActionLog(actionLog.entries)` — byte-identical to what 'Copy log' produces — sent through `apiFetch` (concurrency cap, 25 s timeout, circuit breaker), never raw `fetch`"
  artifacts:
    - path: "src/routes/(app)/settings/activity/+page.svelte"
      provides: "uploadLog() handler + Upload button"
      contains: "apiFetch('/api/diag'"
  key_links:
    - from: "src/routes/(app)/settings/activity/+page.svelte"
      to: "src/lib/services/api-base.ts apiFetch"
      via: "POST /api/diag with Authorization: Bearer <token>"
      pattern: "apiFetch\\('/api/diag'"
    - from: "src/routes/(app)/settings/activity/+page.svelte"
      to: "src/lib/diagnostics/action-log-logic.ts serializeActionLog"
      via: "same serializer as copyLog()"
      pattern: "serializeActionLog\\(actionLog\\.entries\\)"
---

<objective>
Add the single "Upload log" control to the existing Settings -> Activity log screen: one more button
in the `.actions` row and one `uploadLog()` handler that serializes the log exactly like `copyLog()`,
prompts once for the upload token, POSTs via `apiFetch`, and flashes the result.

Purpose: D-04 (explicit action, never automatic, never retried), D-06 (one control, existing
patterns, no UI-SPEC), D-03 (the token is user-entered and device-local, never baked into the
bundle). This is the phone-side half of the phase's success criterion.
Output: `activity/+page.svelte` edited; `pnpm check` green; static gates for the D-04 prohibitions.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-CONTEXT.md
@.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md
@CLAUDE.md

@src/routes/(app)/settings/activity/+page.svelte
@src/routes/(app)/settings/data/+page.svelte
@src/lib/services/api-base.ts
@src/lib/stores/actionLog.svelte.ts
@src/lib/diagnostics/action-log-logic.ts

<interfaces>
From src/lib/services/api-base.ts:
```typescript
export function apiFetch(path: string, init?: RequestInit): Promise<Response>;
// a bodied POST is NOT deduped but IS governed: MAX_CONCURRENT_REQUESTS=8, 25 s timeout, circuit breaker.
// On the native build VITE_API_BASE='https://openmusic.lol' makes '/api/diag' hit the deployed proxy.
```
From src/lib/stores/actionLog.svelte.ts (runes singleton):
```typescript
actionLog.entries: ActionLogEntry[]   // $state, oldest-first
actionLog.load(); actionLog.log(ev: string, d?: Record<string, unknown>); actionLog.clear();
```
From src/lib/diagnostics/action-log-logic.ts:
```typescript
export function serializeActionLog(entries: ActionLogEntry[]): string;
```
Plan 03 TranslationKeys: settings.activityUpload · settings.activityUploadPrompt · settings.activityUploaded · settings.activityUploadFailed · settings.activityUploadEmpty

Existing page (lines 38-46, 60-63):
```svelte
async function copyLog() { const text = serializeActionLog(actionLog.entries); … flash(t('settings.activityCopied')); }
<div class="actions">
	<button class="item" onclick={copyLog} use:tapBounce><Copy size={18} /> {t('settings.activityCopy')}</button>
	<button class="item danger" onclick={clearLog} use:tapBounce><Trash2 size={18} /> {t('settings.activityClear')}</button>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: uploadLog() handler + Upload button on the Activity-log screen</name>
  <files>src/routes/(app)/settings/activity/+page.svelte</files>
  <read_first>
    - src/routes/(app)/settings/activity/+page.svelte — the whole file (98 lines): imports, `msg`/`flash`, `copyLog`, the `.actions` row, `.item` styles
    - src/routes/(app)/settings/data/+page.svelte — line 36: the bare `confirm(t(...))` precedent that makes a bare `prompt(t(...))` idiomatic here
    - src/lib/services/api-base.ts — `apiFetch` (line 249) and the governor comment block (lines 60-75)
    - src/lib/stores/actionLog.svelte.ts — its "cheap, never throws, no network" contract: do NOT add an upload() method to the store
    - .planning/phases/33-activity-log-upload-for-automated-diagnosis/33-RESEARCH.md — "Uploading from the Settings page (D-04, D-06)" and Pitfall 3 (no `VITE_` token, ever)
  </read_first>
  <action>
    Edit `src/routes/(app)/settings/activity/+page.svelte` only.

    Script: add `Upload` to the existing `@lucide/svelte` import line; add
    `import { apiFetch } from '$lib/services/api-base'`. Add a module const
    `DIAG_TOKEN_KEY = 'openmusic:diag:v1'` (namespaced like every other `openmusic:<domain>:v<N>`
    key) and `let uploading = $state(false)` (runes; the page already uses `$state`).

    Add `async function uploadLog()` with a D-04 comment block ("EXPLICIT user action only. No
    timer, no app-start hook, no background retry — a failure flashes and STOPS;
    api-fetch-flood-freeze: three recorded incidents"):
    1. `if (uploading) return;` — re-entrancy guard (double-tap), NOT a retry.
    2. `const text = serializeActionLog(actionLog.entries);` — the SAME line as `copyLog()`, so
       Copy and Upload are byte-identical; `if (!text || text === '[]') return flash(t('settings.activityUploadEmpty'));`
    3. Token: `let token = ''; try { token = localStorage.getItem(DIAG_TOKEN_KEY) ?? ''; } catch {}`;
       if empty, `token = (prompt(t('settings.activityUploadPrompt')) ?? '').trim(); if (!token) return;`
       then persist with a try/catch `localStorage.setItem`. This runs inside a click handler so
       there is no SSR exposure, but keep the try/catch (localStorage-always-try/catch convention).
       Comment: the token is WRITE-ONLY (the read token never comes near a device — two-token split,
       T-33-07/T-33-11) and it is NEVER a `VITE_` var: Vite inlines those into the public bundle
       (D-03, T-33-03; `VITE_API_BASE` is a public URL, which is why that one is allowed).
    4. `uploading = true; try { const res = await apiFetch('/api/diag', { method: 'POST', headers: { Authorization: \`Bearer ${token}\`, 'content-type': 'application/json' }, body: text });`
       `actionLog.log('diag.upload', { status: res.status });` (one line of on-device audit — lets
       the viewer show 401 vs 413 vs 200 without a laptop);
       `if (res.status === 401) { try { localStorage.removeItem(DIAG_TOKEN_KEY); } catch {} }` —
       a mistyped token must not wedge the button forever; the NEXT tap re-prompts (still no
       automatic retry);
       `flash(res.ok ? t('settings.activityUploaded') : t('settings.activityUploadFailed'));`
       `} catch { flash(t('settings.activityUploadFailed')); } finally { uploading = false; }`.
       Comment on `apiFetch`: a bodied POST skips dedupe but keeps the 8-way concurrency cap, the
       25 s timeout and the circuit breaker — use it, never raw `fetch`.

    Markup: insert ONE button between the Copy and Clear buttons in the existing `.actions` row:
    `<button class="item" onclick={uploadLog} disabled={uploading} use:tapBounce><Upload size={18} /> {t('settings.activityUpload')}</button>`.
    No new component, no new `<style>` rules (`.item` already covers it; three flex:1 items share the row).
    Do NOT add anything to `actionLog.svelte.ts`. Do NOT show the returned key in the flash (token-free strings, D-06).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check && F="src/routes/(app)/settings/activity/+page.svelte" && [ "$(grep -c "apiFetch('/api/diag'" "$F")" = "1" ] && [ "$(grep -c 'onclick={uploadLog}' "$F")" = "1" ] && [ "$(grep -cE 'setInterval|\$effect|onMount\(uploadLog|import\.meta\.env\.VITE_DIAG|fetch\(' "$F")" = "0" ] && [ "$(grep -c 'serializeActionLog(actionLog.entries)' "$F")" = "2" ] && [ "$(grep -c 'use:tapBounce' "$F")" = "4" ] && [ "$(git diff --stat -- src/lib/stores/actionLog.svelte.ts | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0 (all five `t('settings.activityUpload…')` keys resolve as `TranslationKey`)
    - `grep -c "apiFetch('/api/diag'" "src/routes/(app)/settings/activity/+page.svelte"` returns 1 and `grep -cE '[^a-zA-Z]fetch\(' "$F"` returns 0 (governed apiFetch only)
    - `grep -c 'onclick={uploadLog}' "$F"` returns 1 and `grep -cE 'setInterval|setTimeout\([^)]*upload|\$effect|onMount\(\(\) => upload' "$F"` returns 0 (D-04: explicit tap only; the existing `setTimeout` in `flash()` is the sole timer and does not call upload)
    - `grep -c 'serializeActionLog(actionLog.entries)' "$F"` returns 2 (copyLog + uploadLog share the serializer)
    - `grep -c "openmusic:diag:v1" "$F"` returns 1 and `grep -c "VITE_" "$F"` returns 0 (T-33-03)
    - `grep -c 'use:tapBounce' "$F"` returns 4 (back + copy + upload + clear) and `grep -c '<style>' "$F"` returns 1 with no new selectors added (`git diff "$F" | grep -c '^+.*{ .*}'` returns 0 for style lines)
    - `git status --porcelain src/lib/stores/actionLog.svelte.ts` is empty (store untouched)
    - The 401 branch removes `openmusic:diag:v1` from localStorage (`grep -c "removeItem(DIAG_TOKEN_KEY)" "$F"` returns 1)
  </acceptance_criteria>
  <done>One new button, one handler; upload is tap-only through apiFetch with the shared serializer; token is prompted, persisted device-side, cleared on 401; no store change, no new styles, no VITE_ credential.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> prompt() | The maintainer types the upload token into their own device |
| Device -> `/api/diag` | Bearer POST leaves the device; on the APK it is cross-origin (Plan 02 fixed Allow-Headers) |
| Build -> bundle | Any `VITE_*` var becomes public JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Information Disclosure | token in client bundle | mitigate | Token is user-entered at runtime and stored in localStorage; grep gate `VITE_` == 0 in the page (D-03) |
| T-33-11 | Information Disclosure | upload token in localStorage (XSS) | accept | D-01: maintainer's own device; WRITE-ONLY token, blast radius = junk uploads; rotate via `wrangler pages secret put`; the READ token never exists on any device (T-33-07) |
| T-33-12 | Denial of Service | fetch flood / background retry | mitigate | D-04: onclick only, re-entrancy guard, no retry; `apiFetch` governor (cap 8, 25 s, breaker) |
| T-33-06 | Denial of Service | APK CORS preflight | transfer | Fixed in Plan 02's shared seam; verified on device in Plan 07 |
| T-33-SC | Tampering | npm installs | accept | No packages installed in this phase |
</threat_model>

<verification>
- `pnpm check` exits 0; `pnpm test` full suite green
- Static gates in the task's `<verify>` command all hold
</verification>

<success_criteria>
- The Activity-log screen has exactly one new control and it uploads only on tap (D-04, D-06)
- The payload equals Copy-log's payload; the request is governed by apiFetch; the token is device-entered, never built in (D-03)
</success_criteria>

<output>
Create `.planning/phases/33-activity-log-upload-for-automated-diagnosis/33-05-SUMMARY.md` when done
</output>
