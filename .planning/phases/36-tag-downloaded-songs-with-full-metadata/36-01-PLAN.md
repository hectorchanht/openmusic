---
phase: 36-tag-downloaded-songs-with-full-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nvmrc
  - package.json
  - pnpm-lock.yaml
  - .github/workflows/android-main.yml
  - .github/workflows/android-release.yml
  - .github/workflows/upstream-health.yml
  - src/routes/(app)/settings/about/+page.svelte
autonomous: true
requirements: [D-01, D-04]

must_haves:
  truths:
    - "`pnpm install` succeeds under `engine-strict=true` with taglib-wasm pinned at exactly 2.2.2"
    - "`pnpm check && pnpm test && pnpm build` are all green on Node 24 before any tagging code exists"
    - "The About page names TagLib and its LGPL/MPL licence (attribution obligation from RESEARCH §Licensing)"
  artifacts:
    - path: ".nvmrc"
      provides: "Node 24 toolchain pin (Cloudflare Pages reads it)"
      contains: "24"
    - path: "package.json"
      provides: "engines.node >=24 + exact taglib-wasm 2.2.2 runtime dependency"
      contains: "\"taglib-wasm\": \"2.2.2\""
    - path: "src/routes/(app)/settings/about/+page.svelte"
      provides: "TagLib attribution line + licence link"
      contains: "TagLib"
  key_links:
    - from: ".github/workflows/*.yml"
      to: "package.json engines"
      via: "node-version: 24 on all three workflows"
      pattern: "node-version: 24"
---

<objective>
Clear the one hard, verified blocker for Phase 36: the chosen tag codec `taglib-wasm@2.2.2` declares `engines.node >= 24` and this repo runs `engine-strict=true` on Node 22, so `pnpm add` fails today. Bump the project toolchain to Node 24, gate it on a full install/check/test/build, then add the dependency at an exact pin and its licence attribution.

Purpose: nothing else in the phase can start until the dependency installs. RESEARCH reproduced both the failure (`ERR_PNPM_UNSUPPORTED_ENGINE`) and the fact that `packageExtensions` cannot override `engines` — so the bump is the only non-vendoring path. D-01 is the user's informed, explicit exception to the zero-runtime-deps house rule; this plan implements it exactly once, for tagging only.

Output: Node 24 toolchain across `.nvmrc` / `package.json` / 3 workflows; `taglib-wasm` 2.2.2 in `dependencies` and `pnpm-lock.yaml`; TagLib attribution on the About page.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-CONTEXT.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-RESEARCH.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-PATTERNS.md

<interfaces>
<!-- Current toolchain state, verified 2026-09-13. All literal value edits. -->

.nvmrc                                   → `22`
package.json `engines.node`              → `">=22"`   (file is TAB-indented)
.npmrc                                   → `engine-strict=true`   (UNCHANGED — the guard is correct)
.github/workflows/upstream-health.yml:51 → `node-version: 22`
.github/workflows/android-release.yml:42 → `node-version: 22`
.github/workflows/android-main.yml:42    → `node-version: 22`
package.json `dependencies`              → Capacitor plugins + @lucide/svelte + tongwen-core/dict only (no wasm, no tag lib)
Local shell node                         → v25.9.0 (so a local green install does NOT prove CI is green — that is why the workflows must change too)

RESEARCH Package Legitimacy Audit: taglib-wasm → slopcheck `[OK]`, npm, v2.2.2 published 2026-08-23, MIT wrapper, single dep `@msgpack/msgpack@^3.1.3`, repo github.com/CharlesWiltgen/TagLib-Wasm. Disposition: Approved. No `[ASSUMED]`/`[SUS]` packages → no blocking human legitimacy checkpoint is required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump the project toolchain to Node 24 and gate it on a full install/check/test/build</name>
  <files>.nvmrc, package.json, .github/workflows/android-main.yml, .github/workflows/android-release.yml, .github/workflows/upstream-health.yml</files>
  <read_first>
    - .nvmrc
    - package.json (the `engines` block at ~:6-8; keep tab indentation)
    - .npmrc (confirm `engine-strict=true` and leave it — RESEARCH rejects disabling it)
    - .github/workflows/android-main.yml (~:42), .github/workflows/android-release.yml (~:42), .github/workflows/upstream-health.yml (~:51)
    - .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-RESEARCH.md §"The Node Engine Blocker" and Assumptions A7/A8
  </read_first>
  <action>
    Make the literal edits: `.nvmrc` content `24`; `package.json` `engines.node` → `">=24"`; each of the three workflows' `node-version: 22` → `node-version: 24`. Do NOT touch `.npmrc` (`engine-strict=true` stays — it correctly caught the mismatch). Do NOT add the dependency in this task.

    Then run the gate in this order and stop on the first failure: `pnpm install` → `pnpm check` → `pnpm test` → `pnpm build`. The gate exists because a Node major bump can independently break SvelteKit / adapter-cloudflare / wrangler (RESEARCH A7). If `pnpm build` fails on Node 24 for a reason unrelated to this repo's code, record the exact error in the SUMMARY and stop — the fallback is RESEARCH's vendor-the-wasm option, which is a plan change, not something to improvise here.

    Add a one-line comment above `engines` is NOT possible in JSON — instead record the reason in the commit message body: "36-D-01/D-04: taglib-wasm@2.2.2 declares engines.node>=24; engine-strict=true makes the install fail on 22 (verified). The package runs on 22 — the field gates an unused WASI backend — but packageExtensions cannot override engines."

    Note for the SUMMARY (RESEARCH A8): Cloudflare Pages reads `.nvmrc` for the build image; also setting a `NODE_VERSION=24` environment variable in the Pages project is a dashboard action the executor cannot perform from the repo — list it under user_setup-style notes in the SUMMARY, do not block on it.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test "$(cat .nvmrc)" = "24" && grep -q '"node": ">=24"' package.json && test "$(grep -c 'node-version: 24' .github/workflows/android-main.yml .github/workflows/android-release.yml .github/workflows/upstream-health.yml | grep -c ':1')" = "3" && grep -q 'engine-strict=true' .npmrc && pnpm install && pnpm check && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `cat .nvmrc` prints exactly `24`
    - `grep -n '"node": ">=24"' package.json` matches one line
    - `grep -c 'node-version: 22' .github/workflows/*.yml` totals 0 across all three files; `node-version: 24` appears once in each
    - `.npmrc` still contains `engine-strict=true`
    - `pnpm install` exits 0; `pnpm check` exits 0; `pnpm test` exits 0 (all ~67 files green); `pnpm build` exits 0 and `.svelte-kit/cloudflare` exists
  </acceptance_criteria>
  <done>Toolchain files all say 24, `.npmrc` untouched, and the four-command gate is green on Node 24 with zero tagging code present.</done>
</task>

<task type="auto">
  <name>Task 2: Add taglib-wasm at an exact pin and fulfil the TagLib licence attribution</name>
  <files>package.json, pnpm-lock.yaml, src/routes/(app)/settings/about/+page.svelte</files>
  <read_first>
    - package.json (`dependencies` block — currently Capacitor plugins, @lucide/svelte, tongwen-*)
    - src/routes/(app)/settings/about/+page.svelte (the literal-English `features` array at ~:15-23 and the `.item.link` anchor rows at ~:44-47 — this page is explicitly NOT part of the translated chrome, so no i18n keys are needed)
    - .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-RESEARCH.md §"Package Legitimacy Audit" (the honest supply-chain caveat + 3 mitigations) and §"Licensing"
  </read_first>
  <action>
    Install with an exact pin: `pnpm add taglib-wasm@2.2.2` then edit `package.json` so the entry reads `"taglib-wasm": "2.2.2"` with NO caret/tilde (RESEARCH Pitfall 8: a routine `pnpm update` must never swap a 686 kB opaque wasm binary unreviewed). Confirm `pnpm-lock.yaml` gained `taglib-wasm@2.2.2` and its single transitive dep `@msgpack/msgpack`. Smoke the install without writing app code: `node -e "import('taglib-wasm/simple').then(m => { if (typeof m.readFormat !== 'function') process.exit(1); })"` must exit 0.

    Licence attribution (RESEARCH §Licensing: the npm tarball ships only the wrapper's MIT LICENSE and NOT TagLib's `COPYING.LGPL`/`COPYING.MPL`, so the app currently inherits an unfulfilled notice obligation). In `settings/about/+page.svelte`: add one entry to the literal `features` array reading "Downloaded songs are tagged (title, artist, album, cover) via TagLib — compiled to WebAssembly, used unmodified under its LGPL-2.1 / MPL-1.1 dual licence", and add one `<a class="item link" … target="_blank" rel="noopener noreferrer">` row next to the GitHub row pointing at `https://github.com/taglib/taglib` with the visible text "TagLib licence (LGPL / MPL)". Use an existing lucide icon already imported on that page or add one per-icon named import (e.g. `Tag`) — never a namespace import. Add a `// 36-D-01` decision-ref comment above the new array entry explaining WHY the attribution is required (weak-copyleft notice obligation; wasm is consumed unmodified, so no contribute-back obligation). No i18n edits — the About page is literal English by design.

    Do NOT import taglib-wasm from any app source file in this plan; the module lands in 36-02 behind a dynamic import.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -q '"taglib-wasm": "2.2.2"' package.json && ! grep -q '"taglib-wasm": "[\^~]' package.json && grep -q 'taglib-wasm@2.2.2' pnpm-lock.yaml && node -e "import('taglib-wasm/simple').then(m => process.exit(typeof m.readFormat === 'function' ? 0 : 1))" && grep -q 'TagLib' "src/routes/(app)/settings/about/+page.svelte" && grep -q 'github.com/taglib/taglib' "src/routes/(app)/settings/about/+page.svelte" && ! grep -rl "from 'taglib-wasm" src/ && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the exact string `"taglib-wasm": "2.2.2"` and no `^`/`~` on that entry
    - `pnpm-lock.yaml` contains `taglib-wasm@2.2.2` and `@msgpack/msgpack`
    - `node -e "import('taglib-wasm/simple')…"` exits 0 (module resolves and exposes `readFormat`)
    - `src/routes/(app)/settings/about/+page.svelte` contains `TagLib`, `LGPL`, `MPL`, `https://github.com/taglib/taglib`, and a `// 36-D-01` comment
    - `grep -rl "from 'taglib-wasm" src/` returns nothing (no static import anywhere in app source)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Dependency installed at an exact pin, lockfile updated, smoke import works, About page carries the TagLib dual-licence notice, and no app source statically imports the package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | A third-party runtime package (the FIRST ever in this app) and a 686 kB opaque wasm binary enter the deployed supply chain |
| CI (GitHub Actions) → APK / Pages | Toolchain version change alters what builds the shipped artefacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | `taglib-wasm` wasm binary (supply chain — the real residual risk of D-01's exception) | mitigate | Exact pin `2.2.2` with no range (Task 2); CI already runs `--frozen-lockfile`; SUMMARY records "never auto-bump — a wasm update is a review event with a fresh round-trip run"; vendoring documented as the escape hatch |
| T-36-02 | Tampering | Node 24 toolchain bump | mitigate | Full `install && check && test && build` gate BEFORE any new code (Task 1); `.npmrc engine-strict=true` deliberately kept so future mismatches still fail loudly |
| T-36-03 | Repudiation | Licence notice | mitigate | TagLib LGPL/MPL attribution on the About page (Task 2) fulfils the notice obligation the npm tarball omits |
| T-36-SC | Tampering | npm installs | mitigate | RESEARCH slopcheck audit: taglib-wasm `[OK]`, no `[ASSUMED]`/`[SUS]`; single dependency `@msgpack/msgpack`; no blocking legitimacy checkpoint required |
</threat_model>

<verification>
- `pnpm install && pnpm check && pnpm test && pnpm build` green on Node 24 (Task 1, before the dependency; Task 2 re-runs `pnpm check`)
- `grep -rl "from 'taglib-wasm" src/` empty — the dependency is present but not yet wired
</verification>

<success_criteria>
- Node 24 across `.nvmrc`, `package.json`, and all three workflows; `.npmrc` unchanged
- `taglib-wasm` at exact `2.2.2` in `package.json` + lockfile; smoke import passes
- About page attributes TagLib with the dual licence + link
- Full toolchain gate green; wave 2 can start
</success_criteria>

<output>
Create `.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-01-SUMMARY.md` when done. Include: the exact `pnpm build` result on Node 24, the Cloudflare Pages `NODE_VERSION=24` dashboard note (human action, not blocking), and the "never auto-bump taglib-wasm" rule.
</output>
