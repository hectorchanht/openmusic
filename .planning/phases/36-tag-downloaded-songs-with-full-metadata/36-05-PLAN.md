---
phase: 36-tag-downloaded-songs-with-full-metadata
plan: 05
type: execute
wave: 4
depends_on: [36-03, 36-04]
files_modified:
  - .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md
autonomous: false
requirements: [D-15, D-02]

must_haves:
  truths:
    - "A debug APK built from the wired seam contains the taglib wasm asset and loads it in the Capacitor WebView — a real download on-device completes with `download.tag` → `tagged` in the Activity log"
    - "The device's OWN music player shows title, artist, album, album grouping (one album, not N one-song albums), track order, and cover art for an m4a and a FLAC downloaded from an album page — proving the Android media scanner read the in-file tags with the Kotlin plugin UNCHANGED (D-15)"
    - "An mp3 (ID3v2.4, the only version taglib-wasm can emit) shows correct title/artist in the device player (RESEARCH Pitfall 4 / A1)"
    - "The Settings → Downloads retag rewrites an existing on-device download and the device player reflects the new tags after a rescan, with no duplicate file in Music/OpenMusic/"
    - "The refutation signal, if it appears, is recorded as a re-research trigger — NOT patched by adding MediaStore ContentValues columns"
  artifacts:
    - path: "android/app/build/outputs/apk/debug/app-debug.apk"
      provides: "Installable debug build with the wasm bundled under assets/public"
    - path: ".planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md"
      provides: "D-15 checklist recorded as executed, nyquist_compliant flipped, wave_0_complete flipped"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "android/app/src/main/assets/public"
      to: "taglib-web-*.wasm"
      via: "cap sync copies build/ (adapter-static) into the APK assets"
      pattern: "taglib-web-.*\\.wasm"
    - from: "Music/OpenMusic/<artist> - <title>.m4a"
      to: "MediaStore TITLE/ARTIST/ALBUM/ALBUM_ARTIST/TRACK columns"
      via: "ModernMediaScanner.scanItemAudio → MediaMetadataRetriever on IS_PENDING → 0"
      pattern: "IS_PENDING"
---

<objective>
Close the phase on the only evidence that matters: what a device music player shows. CI proves the bytes are valid (36-02) and the seam is wired (36-03/36-04); it cannot prove that Android's media scanner reads those tags into its library, that the Capacitor WebView loads a 686 kB wasm, or that OEM players accept ID3v2.4. Those are the three `[ASSUMED]` markers left in RESEARCH (A1, A2, A5), and this plan is where they get settled by observation.

Purpose: D-15 is the load-bearing bet of the native half — in-file tags ONLY, Kotlin `MediaStoreSaverPlugin.kt` untouched. Its premise is CONFIRMED at AOSP source level (`scanItemAudio` upserts TITLE/ARTIST/ALBUM/ALBUM_ARTIST/TRACK from `MediaMetadataRetriever`), but OEM scanner behaviour is unknown, and D-15's own named fallback is REFUTED (see the constraint below). So this is a real verification gate, not a formality.

Output: an installed debug APK, the executed 6-step checklist from `36-VALIDATION.md` with observed results, `36-VALIDATION.md` sign-off flipped, and the phase-level SUMMARY notes (the "OpenMusic" pseudo-album surprise; the `persist:false` album-bulk backlog item).
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-CONTEXT.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-RESEARCH.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-03-SUMMARY.md
@.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-04-SUMMARY.md
@android/app/src/main/java/com/openmusic/app/MediaStoreSaverPlugin.kt

<interfaces>
<!-- Facts the executor needs; none require exploration. -->

Build (from package.json + the `apk-build-needs-jdk21` project note):
  pnpm apk  ==  pnpm build:native (BUILD_TARGET=native VITE_API_BASE=https://openmusic.lol vite build → build/)
             && npx cap sync android (copies build/ → android/app/src/main/assets/public/)
             && cd android && ./gradlew assembleDebug
  Gradle toolchain detection misses Homebrew's JDK 21 → prefix with
  JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
  Output: android/app/build/outputs/apk/debug/app-debug.apk

Emulator fallback (project note `apk-debug-via-emulator-cdp`): existing AVD `Pixel_3a_API_34` + `chrome://inspect` CDP into the WebView — usable for the WASM-load and memory checks, NOT trustworthy for OEM scanner behaviour (RESEARCH A2 is about OEM skins; the emulator is stock AOSP).

Diagnostics (36-03 Task 1): `logAction('download.tag', { uid, result, format?, art, bytes })` — visible in-app at Settings → Activity log. `result` ∈ 'tagged' | 'skipped-size' | 'unknown-container' | 'no-fields' | 'error'.

Kotlin side (D-15 — must stay UNCHANGED through this plan):
  MediaStoreSaverPlugin.kt ~:150 ContentValues: DISPLAY_NAME / MIME_TYPE / RELATIVE_PATH / IS_PENDING only.
  performSave flips IS_PENDING → 0, which is the documented MediaProvider scan trigger (RESEARCH A3, [ASSUMED]).

AOSP ModernMediaScanner.scanItemAudio (RESEARCH, VERIFIED at source, lines 1456-1498):
  op.withValue(ARTIST, "<unknown>");  op.withValue(ALBUM, file.getParentFile().getName()) → "OpenMusic";
  then withOptionalValue(...) from MediaMetadataRetriever ONLY when the retriever produced a value.
  ⇒ (a) an album-less file shows album "OpenMusic", not "Unknown album" (Pitfall 5 — expected, NOT a bug);
  ⇒ (b) app-supplied ContentValues for TITLE/ARTIST/ALBUM are OVERWRITTEN by any later scan — the
        ContentValues fallback D-15 names as its remedy does NOT durably work. REFUTED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the debug APK from the wired seam and prove the wasm is in the assets</name>
  <files>android/app/build/outputs/apk/debug/app-debug.apk</files>
  <read_first>
    - package.json (`apk`, `build:native`, `cap:sync` scripts)
    - src/lib/services/audio-tags.ts (confirm the dynamic import specifiers the build must chunk)
    - .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-03-SUMMARY.md (the web build's emitted wasm/wrapper asset names + sizes — the native build must emit the same set)
    - .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-RESEARCH.md §Runtime State Inventory "Build artifacts" row + Assumption A5
  </read_first>
  <action>
    Run `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home pnpm apk` from the repo root. This is the FIRST time a `.wasm` asset passes through `adapter-static` → `cap sync` in this repo (PATTERNS "No Analog Found"), so verify each hop rather than trusting the exit code:
    1. After `build:native`: `find build -name 'taglib-web-*.wasm'` lists exactly one file; `grep -l taglib build/_app/immutable/entry/*.js` is empty (still lazy under the native adapter); the built `build/service-worker.js` (if emitted for the static target) contains no `.wasm` in its precache list.
    2. After `cap sync`: `find android/app/src/main/assets/public -name 'taglib-web-*.wasm'` lists exactly one file with a byte size equal to the one under `build/`.
    3. After gradle: `android/app/build/outputs/apk/debug/app-debug.apk` exists; `unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -c 'taglib-web-.*\.wasm'` prints `1`.
    Record the APK size delta versus the previous build noted in STATE.md (5.2 MB) in the SUMMARY — the expected growth is ≈ 0.7 MB (the wasm is stored, not gzip-served, inside the APK).
    Do NOT modify `MediaStoreSaverPlugin.kt`, `capacitor.config.ts`, or any `src/` file in this plan. If the build fails, record the exact error and stop — a native build failure here is a plan-level finding, not something to patch in a verification plan.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test "$(find build -name 'taglib-web-*.wasm' | wc -l | tr -d ' ')" = "1" && test "$(find android/app/src/main/assets/public -name 'taglib-web-*.wasm' | wc -l | tr -d ' ')" = "1" && test -z "$(grep -l taglib build/_app/immutable/entry/*.js 2>/dev/null)" && test -f android/app/build/outputs/apk/debug/app-debug.apk && test "$(unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -c 'taglib-web-.*\.wasm')" = "1" && git diff --quiet android/app/src/main/java/com/openmusic/app/MediaStoreSaverPlugin.kt</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm apk` (with the JDK 21 `JAVA_HOME`) exits 0
    - Exactly one `taglib-web-*.wasm` under `build/`, exactly one (same size) under `android/app/src/main/assets/public/`, exactly one inside `app-debug.apk`
    - No `taglib` reference in any `build/_app/immutable/entry/*.js`
    - `git diff --quiet` on `MediaStoreSaverPlugin.kt` succeeds (Kotlin untouched — D-15)
    - SUMMARY records the APK size and the wasm byte size
  </acceptance_criteria>
  <done>An installable APK that carries the wasm as a lazily-loaded asset, with the Kotlin bridge byte-identical to before the phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device D-15 checklist — does the device's own music player read what we wrote?</name>
  <files>none — observation only; results are recorded by Task 3</files>
  <action>Install the Task 1 APK on a PHYSICAL Android device, execute checklist steps 1-9 in <how-to-verify> exactly as written, and report outcome tokens (approved / SCANNER-BLIND / ART-MISS / ID3V24-UNREAD / OOM / DUPLICATE-FILE) with device model + Android version. No source, Kotlin, or config change is permitted in this task under any outcome — a failure is a re-research trigger, and the ContentValues fallback named in CONTEXT D-15 is REFUTED (AOSP scanItemAudio overwrites app-supplied columns) and must not be attempted.</action>
  <what-built>
    Phase 36 tags every downloaded file in memory (title, artist, album when known, album artist, track number from album pages, embedded cover) before it lands on disk, on web and native alike, through the one `downloadTrack` seam. The Kotlin MediaStore plugin was deliberately NOT changed (D-15): the bet is that Android's media scanner reads our in-file tags when `IS_PENDING` flips to 0. A Settings → Downloads page (36-04) retags existing downloads on demand. Task 1 produced `android/app/build/outputs/apk/debug/app-debug.apk` with the taglib wasm bundled.
  </what-built>
  <how-to-verify>
    Install `android/app/build/outputs/apk/debug/app-debug.apk` on a physical Android phone (`adb install -r …`). A physical device is required for steps 4-6 — the emulator is stock AOSP and cannot stand in for OEM scanner/player behaviour (RESEARCH A2). Then execute the checklist from `36-VALIDATION.md` §Manual-Only Verifications, extended with two items (mp3 and retag):

    1. Open the app → Settings → Activity log; clear it so the run is readable.
    2. Open an ALBUM page (artist page → an album, so `?artist=` is in the URL) and tap the album Download button, OR download two individual tracks from that album via their row/menu Download. NOTE the asymmetry from 36-03: the album BULK button uses `persist:false` and writes NO public file on native — so for THIS check, download at least one **m4a** and one **FLAC** track individually from the album page rows (Settings → Playback → download quality `lossless` for the FLAC, `320`/`auto` for the m4a). Confirm in Activity log: for each, `download.tag` with `result: tagged`, the right `format`, and `art: true`.
    3. Stock **Files** app → Internal storage → `Music/OpenMusic/` → both files exist with the human `<artist> - <title>.<ext>` name, and there is exactly ONE file per song (no ` (1)` duplicate).
    4. Open the phone's OWN music player (Samsung Music / YT Music local / Files' audio view — NOT OpenMusic). Per file confirm: title, artist, album name, **album grouping** (both tracks appear under ONE album entry, not two one-song albums), **cover-art thumbnail**. Where the player shows track numbers, confirm ordering matches the album page.
    5. **Read the refutation signal precisely** — three distinct outcomes, do not conflate them:
       - Album shows **"OpenMusic"** AND artist shows **`<unknown>`** → the scanner read NOTHING from our tags. D-15 is refuted on this device. Report "SCANNER-BLIND".
       - Album shows **"OpenMusic"** but title/artist are CORRECT → the track genuinely had no album (`Track.album` was empty → omitted per D-10) and Android fell back to the parent folder. This is EXPECTED behaviour (RESEARCH Pitfall 5), not a failure — but it is the surprise the user must hear from us. Report "PARENT-FOLDER-ALBUM" and note which track.
       - Album/title/artist correct but **no cover** → cover RESOLUTION failed (art tier), not scanning. Check Activity log `art:` for that uid. Report "ART-MISS".
    6. **Reboot the phone**, reopen the device music player, re-check step 4 — rules out a stale MediaProvider cache in either direction.
    7. **mp3 / ID3v2.4 (RESEARCH Pitfall 4, A1):** force an mp3 by downloading a track that only resolves on the kuwo/netease/joox ladder (or temporarily set only `kuwo` enabled in Settings → Playback), confirm Activity log shows `format: mp3`, and confirm the device player shows its title/artist. If the mp3 shows NO metadata while m4a/FLAC do, report "ID3V24-UNREAD" — the scoped escape hatch is a hand-written ID3v2.3 post-pass on the MP3 branch only, which is a follow-up decision, NOT something to build now.
    8. **Retag (36-04):** Settings → Downloads → the button shows a count ≥ 2 → tap → confirm dialog → progress line → flash "Tagged N of N. Skipped 0." Then re-check Files (`Music/OpenMusic/` still has ONE file per song — the 36-D-19 `nativePut` guard) and the device player after a rescan/reboot.
    9. **Memory (RESEARCH Pitfall 2, A6):** download the largest lossless track you can find (a 40-60 MB FLAC). Either it tags (`result: tagged`) or the ceiling trips (`result: skipped-size`) — both are correct. A WebView reload / app restart mid-download is the failure to report ("OOM").

    **Constraint on remedies — read before reporting:** if step 5 yields SCANNER-BLIND, do NOT ask for, and the executor must NOT attempt, the `ContentValues` MediaStore-column fallback that CONTEXT D-15 names. RESEARCH refuted it at AOSP source level: `scanItemAudio` pre-sets `ARTIST="<unknown>"` / `ALBUM=<parent folder>` and its upsert overwrites app-supplied columns on any later scan. The honest remedy path is re-research (most likely: make the tags readable by `MediaMetadataRetriever`, e.g. the ID3v2.3 post-pass if only mp3 fails, or an explicit `MediaStore.scanFile` after the `IS_PENDING` flip if A3 is what failed).
  </how-to-verify>
  <verify>
    <automated>MISSING — device-only; the automated counterpart is Task 3's grep of the recorded `## D-15 Device Run` section in 36-VALIDATION.md</automated>
  </verify>
  <done>Human reply received with either "approved" or explicit outcome tokens + device model + Android version; Kotlin and src unchanged (`git diff --quiet -- src android`).</done>
  <resume-signal>Reply "approved" if steps 3, 4, 6 and 8 pass for both the m4a and the FLAC (a PARENT-FOLDER-ALBUM note on an album-less track is fine and should be included). Otherwise reply with the outcome tokens observed — SCANNER-BLIND / ART-MISS / ID3V24-UNREAD / OOM / DUPLICATE-FILE — plus device model + Android version, so the follow-up is a re-research decision rather than a blind patch.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the device results and flip the validation sign-off</name>
  <files>.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md</files>
  <read_first>
    - .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md (frontmatter `nyquist_compliant` / `wave_0_complete`, the §Validation Sign-Off checklist, the D-15 row in §Per-Task Verification Map)
    - The human's reply to Task 2 (outcome tokens, device model, Android version)
  </read_first>
  <action>
    Update `36-VALIDATION.md` ONLY (no source changes): set frontmatter `wave_0_complete: true` (36-01/36-02 landed) and, if Task 2 was approved, `nyquist_compliant: true` and `status: verified`; tick every line of §Validation Sign-Off that is now true; append a `## D-15 Device Run — <date>` section recording device model, Android version, per-step outcome (PASS / token), the mp3 result, the retag result, and the large-FLAC result (tagged vs skipped-size vs OOM). If Task 2 reported SCANNER-BLIND / ID3V24-UNREAD / OOM, leave `nyquist_compliant: false`, record the tokens verbatim, and add a `## Re-research trigger` paragraph stating that the `ContentValues` fallback is refuted and MUST NOT be attempted, listing the candidate remedies from RESEARCH (ID3v2.3 post-pass on mp3 only; explicit `MediaStore.scanFile` if A3 failed; `TAG_MAX_BYTES` tuning if OOM) as decisions for the user, not tasks for an executor.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -q 'wave_0_complete: true' .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md && grep -q '## D-15 Device Run' .planning/phases/36-tag-downloaded-songs-with-full-metadata/36-VALIDATION.md && git diff --quiet -- src android</automated>
  </verify>
  <acceptance_criteria>
    - `36-VALIDATION.md` frontmatter has `wave_0_complete: true`; `nyquist_compliant` reflects the ACTUAL Task 2 outcome (true only on "approved")
    - A `## D-15 Device Run` section exists with device model, Android version, and one line per checklist step 3-9
    - If any refutation token was reported, a `## Re-research trigger` section exists and contains the phrase "ContentValues" together with "refuted"
    - `git diff --quiet -- src android` succeeds (this plan changed no source)
  </acceptance_criteria>
  <done>The phase's validation contract carries the observed device evidence and an honest sign-off state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → APK → user's phone | A debug build carrying a third-party wasm is installed on a real device |
| App-written file → Android MediaProvider | The OS parses our tag bytes with `MediaMetadataRetriever` (a native parser) during scan |
| Tester → planning artefacts | Human-reported outcomes become the phase's sign-off evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-17 | Tampering | Wasm inside the APK differs from the audited npm artefact | mitigate | Task 1 asserts byte-size equality `build/` ↔ `assets/public/` ↔ APK entry; the source is the exact-pinned 2.2.2 lockfile entry from 36-01 |
| T-36-18 | Denial of Service | OS-side parser fed our tagged bytes (a malformed tag could stall MediaProvider) | accept | Bytes are produced by TagLib (the reference implementation) and proven parse-back-valid by the 36-02 suite; the scanner runs in MediaProvider's own process, isolated from the app |
| T-36-19 | Repudiation | A failed device test "fixed" by the refuted `ContentValues` fallback, hiding the real cause | mitigate | Task 2 `<how-to-verify>` and Task 3 both forbid it in writing; Task 3 acceptance requires the word "refuted" next to "ContentValues" whenever a refutation token is reported |
| T-36-20 | Information Disclosure | Debug APK on a personal device | accept | `assembleDebug` is the existing project flow (`pnpm apk`); no new permissions, no new secrets in the bundle (tagging reads none) |
| T-36-21 | Denial of Service | WebView OOM on a large FLAC (RESEARCH A6) | mitigate | Checklist step 9 observes the `TAG_MAX_BYTES` ceiling on a real device; outcome tunes the constant in a follow-up, never disables the ceiling |
| T-36-SC | Tampering | npm installs | mitigate | No installs in this plan; `pnpm apk` runs against the frozen lockfile |
</threat_model>

<verification>
- Task 1 automated: one wasm at each hop, APK exists, Kotlin unchanged
- Task 2 human: checklist steps 3-9 on a physical Android device with outcome tokens
- Task 3 automated: VALIDATION.md carries the device run; `git diff --quiet -- src android`
- `pnpm test && pnpm check` still green (no source changed in this plan)
</verification>

<success_criteria>
- Debug APK built with the wasm bundled and lazily loaded; Kotlin bridge untouched (D-15)
- Device player shows title / artist / album grouping / track order / cover for an m4a and a FLAC from an album page, surviving a reboot
- mp3 (ID3v2.4) result, retag result, and large-FLAC ceiling result are all observed and recorded
- `36-VALIDATION.md` sign-off reflects the real outcome; any refutation is routed to re-research, never to the `ContentValues` patch
</success_criteria>

<output>
Create `.planning/phases/36-tag-downloaded-songs-with-full-metadata/36-05-SUMMARY.md` when done. This SUMMARY is also where the PHASE-LEVEL notes land — write them even when everything passed:

1. **User-facing surprise (RESEARCH Pitfall 5):** omitting `album` when `Track.album` is empty (D-10, correct — no placeholder) shows on Android as album **"OpenMusic"** (the parent folder name), NOT "Unknown album" as CONTEXT D-10 anticipated. This is the OS's `scanItemAudio` default, not a string the app wrote. State it plainly so the user learns it here, not from their phone.
2. **Backlog item (pre-existing, NOT fixed in Phase 36):** the album BULK download (`album/[name]/+page.svelte` `downloadAlbum`, `persist: false` since Phase 29) writes no offline blob and therefore NO public `Music/OpenMusic/` file on native. Album downloads ARE tagged (track number + album artist, 36-03) but the device music player never sees them for exactly the album case that motivated those two fields — and they are outside 36-04's retag scope because the app holds no copy. Flipping `persist:false` is a scope decision for the roadmap, not a research finding; record it under a "Roadmap backlog" heading.
3. **Assumptions settled:** A1 (ID3v2.4 on this device), A2 (OEM scanner on this device model), A3 (IS_PENDING flip triggers the scan), A5 (WebView loads the wasm), A6 (`TAG_MAX_BYTES` on-device) — one line each with the observed answer.
4. **Refuted fallback:** restate that D-15's `ContentValues` remedy is refuted at AOSP source level and must not be attempted; any future scanner failure is a re-research trigger.
</output>
