---
phase: 26-minimal-api-click-to-play-redesign
plan: 09
type: execute
wave: 2
depends_on: [26-06, 26-07]
gap_closure: true
files_modified:
  - src/lib/stores/player.svelte.ts
  - src/lib/stores/player.svelte.test.ts
autonomous: true
requirements: [UPNEXT-01]

must_haves:
  truths:
    - "A fresh click-to-play never leaves Up-Next empty: when buildSimilarQueue returns no tail, regenerate() applies the same buildDiversePicks safety net ensureAhead() already has"
    - "Every fresh play logs the Up-Next formation SOURCE (track.getSimilar 'similar' / artist fallback / last-resort / diverse safety net) so behavior is verifiable in the Activity log"
    - "The WR-06 queueGen guard is preserved around the added safety-net await so a stale regenerate never clobbers an explicit queue"
  artifacts:
    - path: "src/lib/stores/player.svelte.ts"
      provides: "regenerate() empty-result safety net + upnext.source activity-log event via the buildSimilarQueue report callback (26-07)"
      contains: "buildDiversePicks"
  key_links:
    - from: "src/lib/stores/player.svelte.ts regenerate()"
      to: "src/lib/services/similar.ts buildSimilarQueue(report)"
      via: "passes a report callback that logs upnext.source; installs buildDiversePicks fallback when the tail is empty"
      pattern: "upnext\\.source|buildDiversePicks"
---

<objective>
Close the player half of Gap 2 (Up-Next formation hardening). `track.getSimilar` is already the
primary path, but `regenerate()` — invoked on EVERY fresh click-to-play in the default 'generated'
Up-Next mode — installs whatever `buildSimilarQueue` returns, including `[]`, with no safety net
(unlike `ensureAhead()`, which falls back to `buildDiversePicks`). Combined with 26-07's CR-01 fix,
this guarantees a fresh play never yields an empty Up-Next.

Purpose: mirror ensureAhead's never-stop safety net in regenerate, and add an `upnext.source` Activity
-log event (fed by 26-07's `report(via)` callback) so the UAT claim "up-next is one track.getSimilar
call, never empty" is verifiable on device.
Output: the regenerate() safety net + the upnext.source log, with player-store tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-minimal-api-click-to-play-redesign/26-UAT.md
@.planning/phases/26-minimal-api-click-to-play-redesign/26-REVIEW.md

Depends on 26-07 (buildSimilarQueue's new report callback contract):
@.planning/phases/26-minimal-api-click-to-play-redesign/26-07-PLAN.md

<interfaces>
<!-- Contracts the executor works against. Extracted from the codebase + 26-07. Do NOT re-explore. -->

src/lib/services/similar.ts (AFTER 26-07):
  export async function buildSimilarQueue(
    track: Track, excludeUids?: Set<string>,
    report?: (via: 'similar' | 'artist' | 'lastresort' | 'empty') => void
  ): Promise<Track[]>   // report fires on the terminal path; signature backward-compatible.

src/lib/services/picks.ts:
  export async function buildDiversePicks(count: number, excludeUids?: Set<string>): Promise<Track[]>

src/lib/stores/player.svelte.ts (TODAY):
  private async regenerate(seed: Track) {
    const myQueueGen = this.queueGen;
    // head = queue up to+including seed; manualEntries; exclude = headUids ∪ manualUids ∪ removedUids
    const auto = await buildSimilarQueue(seed, exclude);
    if (myQueueGen !== this.queueGen) return;                 // WR-06 guard
    this.queue = this.queueWithAnchor([...head, ...manualEntries, ...auto], seed);
  }
  private async ensureAhead(...) {                             // the pattern to MIRROR:
    let more = await buildSimilarQueue(current, have);
    if (!more.length) more = await buildDiversePicks(8, have); // ← the safety net regenerate lacks
    if (myQueueGen !== this.queueGen) return;
    if (more.length) this.queue = this.queueWithAnchor([...this.queue, ...more], current);
    logAction('grow.added', { count: more.length });
  }
  logAction(event, payload)  // src/lib/stores/actionLog.svelte.ts — tiny never-throw; already imported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: regenerate() empty-result safety net + upnext.source Activity-log event</name>
  <files>src/lib/stores/player.svelte.ts, src/lib/stores/player.svelte.test.ts</files>
  <read_first>
    - src/lib/stores/player.svelte.ts regenerate() (~2965-2992) + ensureAhead() safety net (~1978-1984) + logAction usage
    - src/lib/services/similar.ts buildSimilarQueue report signature (from 26-07)
    - src/lib/stores/player.svelte.test.ts (mockSimilar = buildSimilarQueue, mockPicks = buildDiversePicks; the ensureAhead-fallback test ~664 shows the dry→buildDiversePicks assertion pattern)
  </read_first>
  <behavior>
    - A fresh play whose buildSimilarQueue returns a non-empty tail installs it and logs
      upnext.source with the via it reported ('similar' | 'artist' | 'lastresort') + the tail count.
      buildDiversePicks is NOT called.
    - A fresh play whose buildSimilarQueue returns an EMPTY tail triggers the safety net:
      regenerate() calls buildDiversePicks(8, exclude), installs those, and logs upnext.source
      via 'diverse' + count. The Up-Next is not left empty.
    - The WR-06 queueGen guard still discards a stale regenerate: a superseding setQueue() during
      EITHER await (buildSimilarQueue or the buildDiversePicks safety net) makes regenerate return
      without clobbering the explicit queue.
  </behavior>
  <action>
    In regenerate(): pass a `report` callback to buildSimilarQueue that captures the reported `via` (a
    local `let via`). After the await + the existing `if (myQueueGen !== this.queueGen) return;` guard,
    if `auto.length === 0`, apply the ensureAhead-style safety net — `const diverse = await
    buildDiversePicks(8, exclude);` then RE-CHECK `if (myQueueGen !== this.queueGen) return;` (a second
    guard for the second await), set the tail to `diverse` and `via = 'diverse'`. Install
    `[...head, ...manualEntries, ...tail]` via queueWithAnchor exactly as today. Emit
    `logAction('upnext.source', { via, count: tail.length })` on the successful install path. Keep the
    outer try/catch (leave-queue-as-is on throw) and every existing guard. Optionally thread the same
    report callback into ensureAhead's buildSimilarQueue call and log upnext.source there too (grow path
    verifiability) — but ensureAhead already has its own buildDiversePicks net, so do not change its
    control flow. Add player.svelte.test.ts cases mirroring the existing ensureAhead-fallback test:
    (1) buildSimilarQueue mock returns [tracks] and invokes report('similar') → assert buildDiversePicks
    NOT called + a queue tail installed + logAction upnext.source {via:'similar'}; (2) buildSimilarQueue
    mock returns [] → assert buildDiversePicks IS called + queue non-empty + logAction upnext.source
    {via:'diverse'}. Use the file's existing logAction spy pattern (or spy on logAction) to assert the event.
  </action>
  <acceptance_criteria>
    - Test: non-empty similar → no buildDiversePicks, upnext.source logged with the reported via + count.
    - Test: empty similar → buildDiversePicks invoked, queue non-empty, upnext.source {via:'diverse'} logged.
    - grep confirms regenerate() now has a `buildDiversePicks` safety net AND a `logAction('upnext.source', …)` call, both inside the WR-06 queueGen guards.
    - `pnpm test -- src/lib/stores/player.svelte.test.ts` passes; `pnpm check` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/lib/stores/player.svelte.test.ts && pnpm check</automated>
  </verify>
  <done>regenerate() never installs an empty Up-Next and logs the formation source; the WR-06 guard is preserved across both awaits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) buildSimilarQueue → regenerate() queue install | no external boundary; a stale async result must not clobber an explicit queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09-01 | Denial of Service | empty Up-Next dead end on a fresh play | mitigate | regenerate() gains the ensureAhead buildDiversePicks safety net — Up-Next is never left empty when any source has data |
| T-26-09-02 | Tampering | a stale regenerate clobbering an explicit setQueue() | mitigate | WR-06 queueGen guard re-checked after BOTH awaits (buildSimilarQueue and the new buildDiversePicks) |
| T-26-09-SC | (n/a) package installs | none | accept | No new dependency added this plan |
</threat_model>

<verification>
- `pnpm check` + `pnpm test` green (only the pre-existing deferred searchHistory SSR-guard failure may remain).
- Non-empty similar → no diverse fallback + upnext.source logged; empty similar → diverse fallback + upnext.source {via:'diverse'}.
- WR-06 guard preserved across both awaits (asserted / grep).
</verification>

<success_criteria>
- regenerate() mirrors ensureAhead's buildDiversePicks safety net — a fresh play never yields an empty Up-Next.
- Every fresh play logs upnext.source with the formation source + count (verifiable in the Activity log).
- All existing generation guards (queueGen/WR-06) are preserved.
</success_criteria>

<output>
Create `.planning/phases/26-minimal-api-click-to-play-redesign/26-09-SUMMARY.md` when done.
</output>
