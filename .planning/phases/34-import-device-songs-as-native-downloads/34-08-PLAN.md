---
phase: 34-import-device-songs-as-native-downloads
plan: 08
type: execute
wave: 4
depends_on: [34-07, 34-05]
files_modified:
  - src/routes/(app)/settings/downloads/+page.svelte
  - src/routes/(app)/settings/+page.svelte
  - src/routes/(app)/+layout.svelte
autonomous: true
requirements: [34-D-12, 34-D-13, 34-D-14, 34-D-11]

must_haves:
  truths:
    - "Settings shows a Downloads row (native only) leading to a page whose primary CTA imports with one tap"
    - "While scanning, the CTA slot shows a determinate progress rail that only moves forward, plus Cancel; afterwards a persistent summary block lists only non-zero categories"
    - "The rules panel is a collapsed accordion below the CTA with presets, two toggles, a nested custom-pattern disclosure with live preview and non-destructive rejection, a minimum-length slider, extension chips and a skip-rules textarea"
    - "Permission denial renders an honest inline message; permanent denial names the OS path; the web build shows only 'Only available in the Android app.'"
    - "Import completion / failure / pattern-fallback toasts appear even when the user has navigated away"
  artifacts:
    - path: "src/routes/(app)/settings/downloads/+page.svelte"
      provides: "the import page — CTA, progress, summary, permission notices, rules accordion"
      min_lines: 200
    - path: "src/routes/(app)/settings/+page.svelte"
      provides: "native-gated Downloads group row"
      contains: "/settings/downloads"
    - path: "src/routes/(app)/+layout.svelte"
      provides: "deviceImport.notice → toast host"
      contains: "deviceImport.notice"
  key_links:
    - from: "src/routes/(app)/settings/downloads/+page.svelte"
      to: "deviceImport.runImport / cancel / setRules / setCustomPattern"
      via: "store method calls; page renders $state one-way"
      pattern: "deviceImport\\.runImport\\(\\)"
    - from: "src/routes/(app)/+layout.svelte"
      to: "toast.show(t(n.key, n.params))"
      via: "$effect on deviceImport.notice"
      pattern: "deviceImport\\.notice"
---

<objective>
Build the one new surface this phase ships — `/settings/downloads` — exactly to UI-SPEC Contracts 1-7, register it in the settings index behind the native flag, and wire the store's notice channel into the app's toast host.

Purpose: D-14 says the button must work with zero configuration and the rules must never be a prerequisite; UI-SPEC settled every visual question (disclosure order, progress idiom, summary block, rejection state, permission copy, web fallback). The page is a renderer over `deviceImport` — no logic of its own beyond formatting.

Output: new route page, one `$derived` in the index, ~10 lines in the layout.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-UI-SPEC.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-PATTERNS.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-07-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 34-07: -->
```typescript
import { deviceImport, type ImportNotice } from '$lib/stores/device-import.svelte';
deviceImport.rules / phase / done / total / summary / permission / patternError / notice
deviceImport.load(); setRules(patch); setCustomPattern(src): boolean; runImport(); cancel()
// From Plan 34-03:
import { PRESET_ORDER, PRESET_LABELS, IMPORT_EXTENSIONS, parseFilename, validateCustomPattern, type PresetId } from '$lib/services/device-filename';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /settings/downloads page — CTA, progress, summary, permission states, web fallback (Contracts 1-4, 7)</name>
  <files>src/routes/(app)/settings/downloads/+page.svelte</files>
  <read_first>
    - src/routes/(app)/settings/playback/+page.svelte (whole file, 188 lines — copy the script head 1-12, header 71-75, section shape 97-105, `<details class="advanced">` 147-157, and the ENTIRE `<style>` block 160-188 verbatim; tabs + single quotes)
    - src/routes/(app)/settings/data/+page.svelte lines 1-30 (transient `msg`/`counts` $state + `onMount` load pattern; the `.item.danger { color: #ff7a90 }` literal)
    - src/lib/components/Nowbar.svelte lines 76-81 and 242-262 (the `.np-prog` / `.np-prog.indet > i.sliver.motion-always` progress idiom to copy for the scan rail)
    - src/routes/(app)/library/+page.svelte — grep `class="empty"` for the `.empty` empty-state markup to mirror for "No songs found"
    - src/lib/stores/device-import.svelte.ts (Plan 34-07 — the fields you render)
    - .planning/phases/34-import-device-songs-as-native-downloads/34-UI-SPEC.md Contracts 1, 2, 3, 4, 7 and the Accessibility Contract (this task) — every string is a `t()` key from Plan 34-05
  </read_first>
  <action>
    Create `src/routes/(app)/settings/downloads/+page.svelte` (tabs, single quotes — NOT the appearance page's style). Script: `onMount`, `goto`, lucide `ChevronLeft, HardDriveDownload, Smartphone, CircleAlert, Check, FolderSearch, SlidersHorizontal, FileMusic, Timer, ListFilter, ListX, Regex`, `deviceImport`, `settings` (for `settings.load()`), `library` (`library.load()` so `downloads` is hydrated before an import), `tapBounce`, `t`. `let native = $state(false); onMount(() => { settings.load(); library.load(); deviceImport.load(); native = Capacitor.isNativePlatform(); })` — import `Capacitor` from `@capacitor/core`; comment: UI-SPEC Contract 7 — first UI-layer native gate in the app; kept to this page body and the index row only. Derived: `const scanning = $derived(deviceImport.phase === 'requesting' || deviceImport.phase === 'scanning'); const pct = $derived(deviceImport.total > 0 ? Math.min(100, Math.round((deviceImport.done / deviceImport.total) * 100)) : 0); const s = $derived(deviceImport.summary);`.

    Markup order (Contract 2): `<svelte:head><title>{t('settings.title')}</title></svelte:head>`; `<header class="head">` with back button (`aria-label={t('settings.backToSettings')}`, `goto('/settings')`, `use:tapBounce`) and `<h1>{t('settings.groupDownloads')}</h1>` (no reset button). Then `{#if !native}<p class="muted">{t('import.webOnly')}</p>{:else} … {/if}`. Inside native: `<section><h2><HardDriveDownload size={15} /> {t('import.heading')}</h2>` then the CTA SLOT: `{#if scanning}` a `<div class="cta busy" aria-busy="true" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow={deviceImport.total > 0 ? pct : undefined} aria-valuetext={deviceImport.total > 0 ? undefined : t('import.scanning')}>` containing the 6px rail `<div class="rail" class:indet={deviceImport.total === 0}><i class="fill" class:sliver={deviceImport.total === 0} class:motion-always={deviceImport.total === 0} style:width={deviceImport.total > 0 ? \`${pct}%\` : undefined}></i></div>`, the label `{deviceImport.total > 0 ? t('import.scanProgress', { done: deviceImport.done.toLocaleString(), total: deviceImport.total.toLocaleString() }) : t('import.scanning')}`, and `<button class="chip cancel" onclick={() => deviceImport.cancel()} use:tapBounce aria-label={t('import.cancel')}>{t('import.cancel')}</button>`; `{:else}<button class="cta" onclick={() => deviceImport.runImport()} use:tapBounce><Smartphone size={18} aria-hidden="true" /> {t('import.cta')}</button>{/if}`; `<p class="muted">{t('import.ctaNote')}</p>`.

    RESULT SLOT (one of three, Contracts 3/4/7): `{#if deviceImport.permission === 'denied-permanently'}<p class="hint perm"><CircleAlert size={14} aria-hidden="true" /> {t('import.permBlocked')}</p>{:else if deviceImport.permission === 'denied'}<p class="hint perm"><CircleAlert size={14} aria-hidden="true" /> {t('import.permDenied')}</p>{:else if s && !scanning}` → if `s.added === 0 && s.relinked === 0 && s.removed === 0 && s.complete` render the EMPTY block `<div class="empty-block"><FolderSearch size={20} aria-hidden="true" /><strong>{t('import.none')}</strong><span class="hint">{t('import.noneBody')}</span></div>`; else the SUMMARY block `<div class="summary">` with headline `<strong><Check size={16} aria-hidden="true" /> {s.complete ? t('import.summaryAdded', { count: s.added }) : t('import.summaryCancelled', { count: s.added })}</strong>` and detail lines in EXACTLY this order, each wrapped in `{#if s.X > 0}`: `import.summaryRelinked {count: s.relinked}`, `import.skipAlready {count: s.already}`, `import.skipTooShort {count: s.skippedShort, seconds: s.minSeconds}`, `import.skipExtension {count: s.skippedExt}`, `import.skipRule {count: s.skippedRule}`, and last `import.summaryRemoved {count: s.removed}` with `class="hint removed"` (`#ff7a90`). Comment: a zero-count category renders NOTHING (Contract 4). Then the rules accordion — a placeholder `<details class="advanced">` with just the summary line in THIS task; Task 2 fills it.

    Styles: paste the playback page's `<style>` block VERBATIM, then append (Contract 3/4 + UI-SPEC Color): `.cta { width: 100%; min-height: 48px; display: flex; align-items: center; justify-content: center; gap: 8px; background: var(--color-primary); color: #fff; border: none; border-radius: 12px; font-size: 15px; font-weight: 600; cursor: pointer; }` `.cta:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }` `.cta.busy { background: var(--color-surface-2); color: var(--color-text); border: 1px solid var(--color-border); justify-content: space-between; padding: 0 8px 0 14px; gap: 12px; }` `.rail { flex: 1; height: 6px; background: var(--color-surface-2); border: 1px solid var(--color-border); border-radius: 999px; overflow: hidden; position: relative; }` `.rail .fill { display: block; height: 100%; background: var(--color-primary); border-radius: 999px; transition: width 0.25s linear; }` `.rail.indet .fill.sliver { width: 35%; animation: np-indet 1.1s ease-in-out infinite; }` plus the `@keyframes np-indet` copied from Nowbar.svelte; `.chip.cancel { min-height: 44px; min-width: 44px; }`; `.hint.perm { display: flex; gap: 6px; align-items: flex-start; color: var(--color-text-muted); font-size: 12px; margin: 8px 0; }` `.summary, .empty-block { background: var(--color-surface-2); border: 1px solid var(--color-border); border-radius: 12px; padding: 14px; margin: 12px 0; display: flex; flex-direction: column; gap: 6px; }` `.summary strong { font-size: 15px; font-weight: 600; display: flex; gap: 6px; align-items: center; }` `.summary .hint { font-size: 12px; color: var(--color-text-muted); }` `.summary .hint.removed { color: #ff7a90; }` `.empty-block { align-items: center; text-align: center; color: var(--color-text-muted); }`. `.motion-always` is the global escape hatch from app.css — the class is applied in markup, not redefined here (comment: quick-260809-mvz — a frozen progress indicator reads as a hung app).
  </action>
  <verify>
    <automated>pnpm check && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - file exists; `grep -c "t('import\." "src/routes/(app)/settings/downloads/+page.svelte"` ≥ 16 (all Contract 1-4/7 keys used); `grep -n "Only available\|Import songs from device\|Scanning" "src/routes/(app)/settings/downloads/+page.svelte"` returns nothing (no raw English)
    - `grep -n "isNativePlatform()" "src/routes/(app)/settings/downloads/+page.svelte"` returns 1 line inside `onMount`; `grep -n "t('import.webOnly')"` returns 1 line
    - `grep -n "motion-always" "src/routes/(app)/settings/downloads/+page.svelte"` returns ≥ 1 line; `grep -n 'role="progressbar"'` returns 1 line; `grep -n "toLocaleString()"` returns ≥ 2 lines
    - `grep -n "import.summaryRemoved" "src/routes/(app)/settings/downloads/+page.svelte"` appears AFTER `import.skipRule` in file order (`awk` line numbers) and inside an element with class `removed`
    - `grep -n "AlertCircle" "src/routes/(app)/settings/downloads/+page.svelte"` returns nothing; `grep -c "CircleAlert"` ≥ 2
    - `head -c 2000 "src/routes/(app)/settings/downloads/+page.svelte" | grep -c $'^\t'` ≥ 5 (tab-indented) and `grep -c '"' "src/routes/(app)/settings/downloads/+page.svelte"` counts only attribute quotes — `grep -n "import .* from \"" …` returns nothing (single-quoted imports)
    - `pnpm check` 0 errors; `pnpm build` succeeds
  </acceptance_criteria>
  <done>The page renders CTA / progress / summary / permission / web-only states exactly per Contracts 1-4 and 7, with the rules accordion shell in place.</done>
</task>

<task type="auto">
  <name>Task 2: Rules accordion — presets, toggles, custom pattern with preview + rejection, min-length, extensions, skip rules (Contracts 5-6)</name>
  <files>src/routes/(app)/settings/downloads/+page.svelte</files>
  <read_first>
    - src/routes/(app)/settings/downloads/+page.svelte (Task 1 output)
    - src/routes/(app)/settings/playback/+page.svelte lines 14-28 (literal-vs-translated label list idiom), 97-118 (chips + row-toggle), 147-157 (details.advanced)
    - src/routes/(app)/settings/appearance/+page.svelte — grep `type="range"` ONLY to copy the slider's `accent-color` + label-row shape (do NOT copy that file's indentation or quotes)
    - src/lib/services/device-filename.ts (`PRESET_ORDER`, `PRESET_LABELS`, `IMPORT_EXTENSIONS`, `parseFilename`, `validateCustomPattern`)
    - .planning/phases/34-import-device-songs-as-native-downloads/34-UI-SPEC.md Contracts 5 and 6, "Free-text input styling", Accessibility Contract
  </read_first>
  <action>
    Fill the `<details class="advanced">` from Task 1 (collapsed by default, state NOT persisted): `<summary use:tapBounce><SlidersHorizontal size={15} aria-hidden="true" /> {t('import.rules')}</summary>` then `<p class="muted">{t('import.rulesNote')}</p>` FIRST, then four sections in this fixed order:

    1. `<section><h2><FileMusic size={15} /> {t('import.parsing')}</h2>` — preset chips `{#each PRESET_ORDER as p (p)}<button class="chip" class:on={deviceImport.rules.presets.includes(p)} aria-pressed={deviceImport.rules.presets.includes(p)} onclick={() => togglePreset(p)} use:tapBounce>{PRESET_LABELS[p]}</button>{/each}` (LITERAL labels — Contract 5 / Copywriting "LITERAL strings"); `togglePreset` builds the new array keeping DECLARED order = order of toggling on (append when enabling, filter when disabling) and calls `deviceImport.setRules({ presets })`. Two `.row-toggle` rows for `stripTrackNo` / `stripBrackets` (`<span><span class="sw" class:on={…}></span>` idiom, `aria-pressed`). `<p class="muted">{t('import.parsingNote')}</p>` — REQUIRED (D-15 stated plainly). Nested `<details class="advanced nested">` with `<summary use:tapBounce><Regex size={14} aria-hidden="true" /> {t('import.customPattern')}</summary>`, `<p class="muted">{t('import.customPatternNote')}</p>`, then `<input class="txt mono" type="text" bind:value={patternDraft} placeholder="^(?<artist>.+?) - (?<title>.+)$" autocapitalize="off" autocorrect="off" spellcheck="false" aria-invalid={deviceImport.patternError ? 'true' : undefined} aria-describedby={deviceImport.patternError ? 'pattern-msg' : undefined} onblur={commitPattern} />` where `let patternDraft = $state('')` is seeded from `deviceImport.rules.customPattern` in `onMount` and `commitPattern()` calls `deviceImport.setCustomPattern(patternDraft)` (Contract 6: validate on blur — NEVER per keystroke; the typed text stays in the field on rejection). Also call `commitPattern` from the nested details' `ontoggle` when it closes. Rejection message `{#if deviceImport.patternError}<p id="pattern-msg" class="hint reject">{t(deviceImport.patternError === 'invalid' ? 'import.patternInvalid' : deviceImport.patternError === 'no-groups' ? 'import.patternNoGroups' : 'import.patternTooSlow')}</p>{/if}`. Live PREVIEW (required): `const PREVIEW_SAMPLE = '01. Adele - Hello (Live).mp3';` `const preview = $derived.by(() => { const v = deviceImport.rules.customPattern ? validateCustomPattern(deviceImport.rules.customPattern) : null; const r = parseFilename(PREVIEW_SAMPLE, deviceImport.rules, v && v.ok ? v.re : null); return r.artist ? \`${r.artist} · ${r.title}\` : r.title || '—'; })` rendered as `<p class="hint mono"><span class="muted">{t('import.patternPreview')}</span> {PREVIEW_SAMPLE} → {preview}</p>` (comment: runs against ONE fixed sample using the SAVED pattern, so it can never become a second unbounded execution path — Contract 6).

    2. `<section><h2><Timer size={15} /> {t('import.minLength')}</h2>` — label row `<div class="lbl"><span>{t('import.minLength')}</span><span class="val">{deviceImport.rules.minSeconds}s</span></div>` then `<input type="range" min="0" max="120" step="5" value={deviceImport.rules.minSeconds} oninput={(e) => deviceImport.setRules({ minSeconds: Number((e.currentTarget as HTMLInputElement).value) })} aria-label={t('import.minLength')} />`; `<p class="muted">{t('import.minLengthNote')}</p>`.

    3. `<section><h2><ListFilter size={15} /> {t('import.fileTypes')}</h2>` — `{#each IMPORT_EXTENSIONS as ext (ext)}` chips, LITERAL labels, `aria-pressed`, toggling `deviceImport.setRules({ extensions })`; `<p class="muted">{t('import.fileTypesNote')}</p>`.

    4. `<section><h2><ListX size={15} /> {t('import.skipRules')}</h2>` — `<textarea class="txt" rows="3" placeholder={'interview\nringtone\nvoice memo'} autocapitalize="off" autocorrect="off" spellcheck="false" value={deviceImport.rules.skipRules.join('\n')} onblur={(e) => deviceImport.setRules({ skipRules: (e.currentTarget as HTMLTextAreaElement).value.split('\n').map(s => s.trim()).filter(Boolean) })} aria-label={t('import.skipRules')}></textarea>`; `<p class="muted">{t('import.skipRulesNote')}</p>`.

    Styles to append (UI-SPEC "Free-text input styling" verbatim): `.txt { width: 100%; min-height: 44px; padding: 12px 14px; background: var(--color-surface-2); border: 1px solid var(--color-border); border-radius: 12px; color: var(--color-text); font-family: inherit; font-size: 14px; }` `.txt::placeholder { color: var(--color-text-muted); }` `.txt:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }` `.txt[aria-invalid='true'] { border-color: #ff7a90; }` `.mono { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 13px; }` `.hint.reject { color: #ff7a90; font-size: 12px; margin: 6px 0 0; }` `.advanced.nested { margin: 12px 0 0; }` `.advanced summary { min-height: 44px; display: inline-flex; align-items: center; }` `.lbl { display: flex; justify-content: space-between; font-size: 14px; margin-bottom: 6px; }` `input[type='range'] { width: 100%; accent-color: var(--color-primary); }` `.chip:focus-visible, .row-toggle:focus-visible, .advanced summary:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }`. Record the `.chip` 33px touch-height debt as a one-line comment (UI-SPEC Spacing "KNOWN FLAG") — reuse verbatim, do not fix here.
  </action>
  <verify>
    <automated>pnpm check && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "t('import\." "src/routes/(app)/settings/downloads/+page.svelte"` ≥ 34 (all 37 import.* keys minus the ≤3 that are state-exclusive are referenced)
    - `grep -n 'autocapitalize="off" autocorrect="off" spellcheck="false"' "src/routes/(app)/settings/downloads/+page.svelte"` returns 2 lines (input + textarea)
    - `grep -n "oninput={commitPattern}\|on:input" "src/routes/(app)/settings/downloads/+page.svelte"` returns nothing on the pattern input (no per-keystroke validation) and `grep -n "onblur={commitPattern}"` returns 1 line
    - `grep -n "01. Adele - Hello (Live).mp3" "src/routes/(app)/settings/downloads/+page.svelte"` returns 1 line; `grep -n 'min="0" max="120" step="5"'` returns 1 line
    - `grep -n "PRESET_LABELS\[p\]\|{ext}" "src/routes/(app)/settings/downloads/+page.svelte"` returns ≥ 2 lines (literal labels, not t())
    - `grep -c "aria-pressed" "src/routes/(app)/settings/downloads/+page.svelte"` ≥ 4
    - `grep -n "import.rulesNote" "src/routes/(app)/settings/downloads/+page.svelte"` line number is smaller than the `import.parsing` line number (note is first inside the accordion) and `import.parsingNote` is present
    - `pnpm check` 0 errors; `pnpm build` succeeds
  </acceptance_criteria>
  <done>All four rule sections and the nested custom-pattern hatch render and persist through the store; rejection is non-destructive and announced; the preview is live against one fixed sample.</done>
</task>

<task type="auto">
  <name>Task 3: Settings index row (native-gated) + layout toast host for import notices</name>
  <files>src/routes/(app)/settings/+page.svelte, src/routes/(app)/+layout.svelte</files>
  <read_first>
    - src/routes/(app)/settings/+page.svelte (whole file, 60 lines — the `groups` const at 14-24)
    - src/routes/(app)/+layout.svelte lines 20-80 (the `player.notice` `$effect` host with `untrack()` — the WR-04 pattern to mirror for a second, smaller host; the `toast` and `t` imports already present)
    - src/lib/stores/device-import.svelte.ts (`notice`, `ImportNotice`)
  </read_first>
  <action>
    settings/+page.svelte: import `HardDriveDownload` in the lucide list and `Capacitor` from `@capacitor/core`; add `let native = $state(false);` set in the existing `onMount` (`native = Capacitor.isNativePlatform();`). Rename the const array to `ALL_GROUPS`, insert `{ href: '/settings/downloads', icon: HardDriveDownload, title: 'settings.groupDownloads', desc: 'settings.groupDownloadsDesc' }` immediately AFTER the playback entry and BEFORE data (Contract 1), and add `const groups = $derived(ALL_GROUPS.filter((g) => native || g.href !== '/settings/downloads'));` with a comment: `34 (UI-SPEC Contract 7): the Downloads row is ABSENT on web — a disabled row that can never be enabled is a permanent lie in the navigation; this and the page body are the only two UI-layer native gates.` Update the "order =" comment.

    (app)/+layout.svelte: import `deviceImport` from `$lib/stores/device-import.svelte`. Add a second `$effect` next to the player-notice host: `$effect(() => { const n = deviceImport.notice; if (!n) return; untrack(() => { toast.show(t(n.key, n.params)); deviceImport.notice = null; }); });` with a comment: `34: store→UI channel for the import (mirrors the player.notice host above — the store emits a TranslationKey + params, the host localises; UI-SPEC Contract 3: fires on completion even when the user navigated away from /settings/downloads). untrack() keeps settings.appLang out of this effect's dependencies (WR-04) and clearing the channel here prevents a re-toast on remount.`
  </action>
  <verify>
    <automated>pnpm check && pnpm build 2>&1 | tail -5 && pnpm test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "href: '/settings/downloads'" "src/routes/(app)/settings/+page.svelte"` returns 1 line and its line number is between the `/settings/playback` and `/settings/data` entries
    - `grep -n "const groups = \$derived(" "src/routes/(app)/settings/+page.svelte"` returns 1 line; `grep -n "HardDriveDownload" …` returns 2 lines (import + entry)
    - `grep -n "deviceImport.notice" "src/routes/(app)/+layout.svelte"` returns ≥ 2 lines (read + clear) and `grep -n "toast.show(t(n.key, n.params))"` returns 1 line
    - `pnpm check` 0 errors; `pnpm build` succeeds; `pnpm test` fully green (end-of-wave gate)
  </acceptance_criteria>
  <done>Downloads row appears in Settings on native only; import notices reach the toast host via the key channel; whole suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → store | regex text and skip-rule text entered on this page |
| scan-derived strings → DOM | summary counts only; no filenames are rendered on this page (counts are numbers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-21 | Denial of service (ReDoS via preview) | live preview | mitigate | preview uses the SAVED (already probed) pattern against ONE fixed sample — never the draft, never a file list |
| T-34-22 | Tampering (XSS) | any rendered string | mitigate | Svelte text interpolation only; `{@html}` forbidden (`grep -c "{@html}"` must be 0 on the page) |
| T-34-23 | Information disclosure | web build exposing a native-only route | accept | route renders only `import.webOnly`; no bridge call is made on web |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- `pnpm check` clean, `pnpm build` succeeds, `pnpm test` green
- `grep -c "{@html}" "src/routes/(app)/settings/downloads/+page.svelte"` returns 0
</verification>

<success_criteria>
- `/settings/downloads` exists and matches UI-SPEC Contracts 1-7; web shows one line
- Index row native-gated; import toasts routed through the layout host
</success_criteria>

<output>
Create `.planning/phases/34-import-device-songs-as-native-downloads/34-08-SUMMARY.md` when done
</output>
