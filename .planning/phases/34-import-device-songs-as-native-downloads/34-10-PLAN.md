---
phase: 34-import-device-songs-as-native-downloads
plan: 10
type: execute
wave: 6
depends_on: [34-09]
files_modified: []
autonomous: false
requirements: [34-D-04, 34-D-05, 34-D-06, 34-D-07, 34-D-08, 34-D-09]

must_haves:
  truths:
    - "UAT 8: the first import after an in-app download does not duplicate it; the app-private copy can be removed and the song still plays from its public copy"
    - "UAT 9: deleting a file outside the app leaves the entry listed with a red 'File missing' badge; tapping it explains why and does not remove it; the next import drops it"
    - "UAT 3: an imported mp3, flac and m4a each play end to end through the content:// path (audio.src is a blob: URL)"
    - "UAT 4: seeking mid-track on an imported file holds position and audio continues"
  artifacts: []
  key_links:
    - from: "device UAT"
      to: "34-VALIDATION.md items 8, 9, 3, 4"
      via: "checkpoint:human-verify tasks"
      pattern: "UAT"
---

<objective>
Verify the D-09 merge lane, the D-06/D-07/D-08 missing-file lifecycle, and the D-05 playback + seek path on the emulator. Ordered after Plan 34-09 so the data-loss check (item 7) is already signed off before the first playback.

Purpose: items 3 and 4 are the phase's functional core and RESEARCH Pitfall 2 (Capacitor's broken Range handling) can only be observed on a real WebViewLocalServer. Items 8 and 9 exercise real `Music/OpenMusic/` contents and a real filesystem delete.

Output: four signed-off checkpoints in the SUMMARY.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-import-device-songs-as-native-downloads/34-VALIDATION.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-RESEARCH.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-09-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT 8 — D-09: first import does not duplicate existing downloads; relink restores playability</name>
  <what-built>Rows under Music/OpenMusic/ matching an existing real-source download by matchKey are relinked (one URI write via blobStore.linkPublicUri) instead of imported as device: entries; nativeGet falls back to that URI when the app-private copy is gone (Plans 34-01, 34-06, 34-07).</what-built>
  <how-to-verify>
    1. In the app, search and download TWO streamed songs (long-press → Download). Confirm `adb shell ls /sdcard/Music/OpenMusic/` shows two files named `{artist} - {title}.{ext}`. Note the Downloads tab count in Library (N).
    2. Settings → Downloads → Import. Expected summary: "0 songs added" is NOT shown as the headline — instead the headline shows the count of device files added (0 if none new) and a line **"2 songs relinked to files already on this device"**. Library → Downloads count is still N (no doubles; the two songs appear once each, with their original covers).
    3. Playability restore: find the sanitized uid path of one downloaded song: `adb shell run-as com.openmusic.app ls files/downloads/` and delete its app-private copy: `adb shell run-as com.openmusic.app rm "files/downloads/<that-file>"`. Turn on airplane mode on the emulator (swipe down → Airplane). Tap that song in Library → Downloads. Expected: it plays (from the public Music/OpenMusic/ copy via the relinked URI) — no "couldn't play", no skip. Turn airplane mode off afterwards.
    4. Verify via CDP (chrome://inspect → the app WebView): `localStorage.getItem('openmusic-blob-uri:<real uid>')` returns a `content://media/external/audio/media/<id>` string; `Object.keys(localStorage).filter(k => k.startsWith('openmusic-blob-uri:device:'))` returns `[]` (no device URI ever enters the index).
  </how-to-verify>
  <resume-signal>Type "uat8 pass" or describe the mismatch (duplicate rows / failed offline play / a device: key in the index).</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT 9 — D-06/D-07/D-08: a deleted file shows unavailable, is not removed, and is dropped only by the next import</name>
  <what-built>Player offline-miss branch marks the entry unavailable and sets the inline "File missing" error, then skips (Plan 34-02); RowBadges/DownloadControl show a red CircleAlert (Plan 34-07); syncDevice drops unseen device entries only on a complete scan (Plan 34-06).</what-built>
  <how-to-verify>
    1. Delete a seeded file behind the app's back: `adb shell rm "/sdcard/Music/Sample Track.m4a"` then rescan (`adb shell content call --uri content://media/external/file --method scan_volume --arg external_primary`). Do NOT re-import yet.
    2. Library → Downloads. Expected: "Sample Track" is STILL listed (it may still show the grey check — nothing has tried to read it yet).
    3. Tap "Sample Track". Expected: no audio; the Nowbar shows an inline error "File missing · re-import to restore it"; playback advances to the next track (never-stop skip) or a skip toast appears; the row now shows a **red alert-circle badge** ("File missing" on long-press title/aria) in place of the grey check, and the row's download control shows the same red glyph. The entry is NOT removed. Confirm the same red badge appears for that song on any other surface it is listed (e.g. Home shelf if present).
    4. Open the ⋮ menu on any imported (device) song. Expected: NO Download row/button and NO Share row; Like, Add to playlist, Go to artist, Detail are still present. Open the menu on a streamed song: Download and Share are present (gate is narrow).
    5. Kill and relaunch the app. Library → Downloads: the red badge on "Sample Track" persists (unavailable state is persisted).
    6. Settings → Downloads → Import → let it complete. Expected summary includes **"1 removed — the files are no longer on this device"** in red as the LAST line; Library → Downloads no longer lists "Sample Track".
    7. Cancel semantics: push 20+ more seeded files into /sdcard/Music/ (any 45 s files), rescan, delete one MORE seeded file, start Import and tap **Cancel** as soon as the progress row appears. Expected: the summary headline reads "Import cancelled — N songs added before it stopped." and there is NO "removed" line; the deleted file's entry is still listed (D-08: a cancelled scan never drops). A subsequent full import drops it.
  </how-to-verify>
  <resume-signal>Type "uat9 pass" or describe which step differed.</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT 3 — an imported mp3, flac and m4a play end to end via the content:// path</name>
  <what-built>blobStore.get('device:…') fetches Capacitor.convertFileSrc(content://…) → Blob → the player's existing URL.createObjectURL path (Plan 34-01). No MIME re-type is applied — Chromium sniffs blob: sources (RESEARCH A3, assumed).</what-built>
  <how-to-verify>
    1. Turn on airplane mode (proves no network is involved). Library → Downloads → tap "Hello" (Adele, mp3). Expected: audible tone within ~1 s, the Nowbar shows title/artist, the progress rail advances.
    2. Via CDP console: `document.querySelector('audio').src` → starts with `blob:` (NOT `content://`, NOT `https://localhost/_capacitor_content_`). Record the value prefix.
    3. Tap "One More Time" (Daft Punk, **flac**). Expected: plays. Then "Download Test - Song" (mp3 from Download/, if UAT 2 passed). Then re-push and re-import a 45 s **m4a** if UAT 9 consumed the original, and play it. Expected: all play. If FLAC or m4a does NOT play while mp3 does, record it — that is RESEARCH Pitfall 4 (MIME sniffing) and the documented upgrade path is persisting mimeType and re-typing the Blob (a follow-up, not a blocker for mp3).
    4. Let a track play to its end. Expected: it advances to the next downloaded track (ended → next). Turn airplane mode off.
    5. Lock the emulator screen (power button) mid-playback for 15 s. Expected: audio continues (existing background-audio behaviour unchanged for device files).
  </how-to-verify>
  <resume-signal>Type "uat3 pass" with the audio.src prefix, or list which formats failed.</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT 4 — seek works mid-track on an imported file (RESEARCH Pitfall 2)</name>
  <what-built>Playback goes through a blob: URL (Chromium's blob storage implements Range correctly). If anyone bypassed the Blob, seeking would return byte-0 data labelled as the offset.</what-built>
  <how-to-verify>
    1. Play the 45 s "Hello" (Adele) imported mp3. At ~5 s, expand Now Playing and drag the scrubber to ~30 s.
    2. Expected: the time readout jumps to ~0:30 and KEEPS COUNTING from there; the tone continues without a restart, glitch, or a jump back to 0:00. Repeat on the flac.
    3. Seek to ~40 s and let it finish. Expected: `ended` fires after ~5 s and the player advances.
    4. Via CDP: `const a = document.querySelector('audio'); a.currentTime = 20; setTimeout(() => console.log(a.currentTime), 500)` → logs ≈ 20.5, not ≈ 0.5.
  </how-to-verify>
  <resume-signal>Type "uat4 pass" or describe the scrub behaviour (jumped to 0 / stuttered / stalled) — a failure here means a Blob bypass was introduced somewhere and blocks the phase.</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebViewLocalServer ← content:// | real Capacitor Range/MIME behaviour only observable here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-25 | Tampering (silent seek corruption) | any direct `_capacitor_content_` audio.src | mitigate | UAT 4 + CDP `audio.src` prefix check in UAT 3 |
| T-34-26 | Repudiation (silent removal) | missing-file lifecycle | mitigate | UAT 9 steps 3, 5, 7 |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- UAT 8, 9, 3, 4 recorded with verbatim observations in the SUMMARY
- Any format-specific playback failure recorded against RESEARCH Pitfall 4 with the upgrade path
</verification>

<success_criteria>
- No duplicates on first import; relinked entries play offline without their app-private copy
- Missing files: badged, explained, never silently removed, dropped only by a complete import
- mp3/flac/m4a play through blob: URLs; seeking holds position
</success_criteria>

<output>
Create `.planning/phases/34-import-device-songs-as-native-downloads/34-10-SUMMARY.md` when done
</output>
