---
phase: 34-import-device-songs-as-native-downloads
plan: 11
type: execute
wave: 7
depends_on: [34-10]
files_modified: []
autonomous: false
requirements: [34-D-12, 34-D-14, 34-D-04]

must_haves:
  truths:
    - "UAT 6: a 1000+ file scan completes with visibly advancing progress and no UI jank; rules changes (min length 0) take effect on the next import"
    - "UAT 5: a >100 MB lossless file plays without an OOM crash (or the failure is recorded with the documented fallback options)"
    - "UAT 10: lock-screen / media-session metadata shows the device track's title and artist"
  artifacts: []
  key_links:
    - from: "device UAT"
      to: "34-VALIDATION.md items 6, 5, 10"
      via: "checkpoint:human-verify tasks"
      pattern: "UAT"
---

<objective>
Run the scale, memory and media-session checks — the three UAT items that need volume or a real phone. Items 5 and 10 want a real device (RESEARCH Environment Availability); the emulator result is recorded as provisional if a phone is unavailable.

Purpose: RESEARCH Pitfall 9 (bridge payload) and Pitfall 3 (whole-file Blob) have no unit-test surface. A scan that "appears hung" or an OOM on a large FLAC would ship silently otherwise.

Output: three signed-off checkpoints; `nyquist_compliant` can be set in 34-VALIDATION.md once all ten items are recorded.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-import-device-songs-as-native-downloads/34-VALIDATION.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-RESEARCH.md
@.planning/phases/34-import-device-songs-as-native-downloads/34-10-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT 6 — 1000+ file scan completes, progress advances, no jank; rules panel takes effect</name>
  <what-built>Paged scanAudio (500 rows/page) driven by the store's generation-guarded loop; determinate progress rail; rules persisted under openmusic:import-rules:v1 (Plans 34-04, 34-07, 34-08).</what-built>
  <how-to-verify>
    1. Seed volume (Claude prepares this before handing over — run in the scratchpad): with ffmpeg, generate 1000 tiny 2-second mp3s in a loop (`for i in $(seq -w 1 1000); do ffmpeg -loglevel error -f lavfi -i sine=frequency=440:duration=2 -c:a libmp3lame -b:a 32k "bulk/Bulk Artist - Track $i.mp3"; done`), `adb push bulk /sdcard/Music/Bulk`, rescan. Confirm `adb shell content query --uri content://media/external/audio/media --projection _id | wc -l` ≥ 1000.
    2. Settings → Downloads → open **Import rules**. Confirm the first line reads "The defaults already work…". Drag **Minimum length** to **0s** (otherwise the 2 s files are skipped — that is also a valid first check: import once at 30 s and confirm the summary says "1000 skipped — shorter than 30s").
    3. Tap Import. Expected: within ~1 s the row switches from "Scanning…" to "N of 1,0xx files checked" with the thousands separator; the fill only moves forward; the screen stays responsive (scroll the page while it runs); it completes in well under a minute with a summary headline "1000 songs added" (± the earlier seeds). Record the elapsed time.
    4. Navigate to Home mid-scan on a second run (re-import after deleting the bulk folder and re-pushing, or just re-run — "already in your library" will count) and come back: the progress row is still live or the summary is waiting; a toast "Import finished · N songs added" appeared even while on Home.
    5. Toggle a preset chip off and on, flip **Strip bracketed tags**, type a custom pattern `^(?<title>.+?)__(?<artist>.+)$` and tap away: the preview line shows "01. Adele - Hello (Live).mp3 → …" updating; type `(?<title>[` and tap away: red border + "Not a valid pattern — your last working one is still in use."; the Import button is still enabled. Type `^(?<title>(a+)+)$` and tap away: "This pattern is too slow…". Kill and relaunch: the rules (min length 0, your last VALID pattern) persisted.
    6. Optional cleanup: `adb shell rm -r /sdcard/Music/Bulk`, rescan, re-import → "1000 removed" in red.
  </how-to-verify>
  <resume-signal>Type "uat6 pass" with the elapsed time and any jank observed, or describe what stalled.</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT 5 — a >100 MB lossless file plays without OOM (real phone preferred)</name>
  <what-built>Device playback materialises the whole file as a Blob before play (the same shape as every native app-download; RESEARCH Pitfall 3). This is the realistic OOM case on 2-3 GB RAM phones.</what-built>
  <how-to-verify>
    1. Generate a large FLAC in the scratchpad: `ffmpeg -f lavfi -i "anoisesrc=d=1200:c=pink:r=48000" -ac 2 -c:a flac "Big Noise - Twenty Minutes.flac"` (stereo noise barely compresses; expect 150-250 MB — check with `ls -lh`, aim for > 100 MB). Push to `/sdcard/Music/` (emulator) and/or to a REAL phone via `adb push` or a file transfer. Rescan; import.
    2. Tap the track. Expected on the emulator: plays within a few seconds; `adb logcat -d | grep -i "OutOfMemory\|lowmemorykiller\|FATAL" | tail` shows nothing new. Seek to 10:00; audio continues.
    3. On a real phone (preferred — record the model and RAM): same steps. Expected: plays without the app being killed. If it IS killed: record device, RAM, file size, and the logcat OOM line. RESEARCH names the two fallbacks (size ceiling with "too large to play offline", or direct `_capacitor_content_` src with broken seek for oversize only) — this is a follow-up decision for the user, not an in-phase fix.
    4. Play a normal-size track afterwards to confirm the app is healthy.
  </how-to-verify>
  <resume-signal>Type "uat5 pass" with device + file size, "uat5 emulator-only pass" if no phone was available, or "uat5 FAIL" with the logcat line.</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT 10 — lock-screen / media-session metadata is correct for a device track</name>
  <what-built>Media session metadata is written from the Track on every start/resume/restore path (existing services/media-session.ts + native-media-session.ts); device tracks carry title/artist from tags-or-filename and a cover resolved lazily by the existing name-layer chain (RESEARCH bite #13).</what-built>
  <how-to-verify>
    1. Play the imported "Hello" (Adele). Lock the screen (power button). Expected on the lock screen / notification shade: title **Hello**, artist **Adele** (NOT "OpenMusic", not the raw filename, not "kuwo"). Play/pause and next controls work.
    2. Play "Sample Track" (untagged, no artist). Expected: title "Sample Track", artist empty or the app's neutral placeholder — never a crash (memory note: the native plugin must never be handed a URL it must fetch; a device track with no cover must not crash the process). `adb logcat -d | grep -i "FATAL" | tail` → nothing new.
    3. Tap next from the lock screen into another device track: metadata updates.
    4. Real phone preferred for the OS notification rendering; note which device was used.
  </how-to-verify>
  <resume-signal>Type "uat10 pass" with the device used, or describe the metadata shown.</resume-signal>
  <action>Human verification step (device-only — see 34-VALIDATION.md "Manual-Only Verifications"). Present <how-to-verify> to the user exactly as written, wait for the resume signal, and record the observed outcome VERBATIM (counts, shell outputs, audio.src prefix, device model) in this plan's SUMMARY. No code is changed by this task; a FAIL / correction outcome is escalated to the user, never silently accepted.</action>
  <verify>
    <human-check>Resume signal received; outcome text recorded in the SUMMARY under this UAT item number</human-check>
  </verify>
  <done>The user has typed the pass/fail resume signal and the verbatim observation is in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge payload / renderer memory | 1000-row pages and >100 MB Blobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-27 | Denial of service | large library scan | mitigate | paging + progress; UAT 6 measures |
| T-34-28 | Denial of service (OOM) | large lossless playback | accept (measured) | UAT 5 records the outcome; documented fallbacks are a user decision |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- UAT 6, 5, 10 recorded; with Plans 34-09/34-10 all ten VALIDATION items are covered
- Set `nyquist_compliant: true` and `wave_0_complete: true` in 34-VALIDATION.md frontmatter once all ten are recorded (edit only those two lines and tick the Sign-Off checklist)
</verification>

<success_criteria>
- Scale scan completes with live progress; rules changes persist and take effect
- Large-file playback outcome recorded (pass or documented decision)
- Lock-screen metadata correct for tagged and untagged device tracks
</success_criteria>

<output>
Create `.planning/phases/34-import-device-songs-as-native-downloads/34-11-SUMMARY.md` when done
</output>
