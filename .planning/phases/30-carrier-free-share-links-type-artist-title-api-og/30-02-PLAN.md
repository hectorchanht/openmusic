---
phase: 30-carrier-free-share-links-type-artist-title-api-og
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/proxy/deezer-cover.ts
  - src/lib/proxy/deezer-cover.test.ts
  - src/routes/api/deezer/search/+server.ts
autonomous: true
requirements: [OG-EP-03]
must_haves:
  truths:
    - "/api/deezer/search behaves byte-identically to before the extraction (the existing endpoint test suite passes with ZERO edits)"
    - "fetchDeezerCover / reshapeDeezerSearch / safeDeezerImageUrl are importable from $lib/proxy/deezer-cover by any module (the /api/og route in plan 30-03)"
    - "fetchDeezerCover distinguishes a clean no-match (returns { cover: null, artistPicture: null }) from a fault (returns null) — the miss-vs-error signal OG-EP-01 caches on"
  artifacts:
    - path: "src/lib/proxy/deezer-cover.ts"
      provides: "Extracted Deezer cover upstream call (OG-EP-03)"
      exports: ["DeezerCover", "DeezerHit", "DEEZER_COVER_TTL", "safeDeezerImageUrl", "safeDeezerPreviewUrl", "reshapeDeezerSearch", "deezerSearchUrl", "fetchDeezerCover"]
    - path: "src/lib/proxy/deezer-cover.test.ts"
      provides: "Direct unit coverage of the extracted helpers (Wave 0 gap)"
    - path: "src/routes/api/deezer/search/+server.ts"
      provides: "Thin route: GET/OPTIONS + cache orchestration only, importing $lib/proxy/deezer-cover"
      contains: "from '$lib/proxy/deezer-cover'"
  key_links:
    - from: "src/routes/api/deezer/search/+server.ts"
      to: "src/lib/proxy/deezer-cover.ts"
      via: "fetchDeezerCover import"
      pattern: "fetchDeezerCover"
    - from: "src/routes/api/deezer/search/deezer-endpoint.test.ts"
      to: "src/routes/api/deezer/search/+server.ts"
      via: "UNMODIFIED regression harness — the behavior-preservation proof"
      pattern: "import \\{ GET, OPTIONS \\} from './\\+server'"
---

<objective>
Extract the Deezer cover upstream call out of `api/deezer/search/+server.ts` into
`$lib/proxy/deezer-cover.ts` (OG-EP-03) so `/api/og` (plan 30-03) and `/api/deezer/search` share one
implementation, and add direct unit coverage for the now-reachable helpers.

Purpose: a `+server.ts` cannot export non-verb helpers — it 500s at request time and unit tests miss
it (project finding `svelte-server-endpoint-only-verb-exports`). This extraction is REQUIRED, not
cosmetic.
Output: `deezer-cover.ts` + `deezer-cover.test.ts`; rewired route; the existing endpoint test suite
passing with zero edits as the behavior-preservation proof.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-RESEARCH.md  (§C.12 — the exact extraction table, §C.13 tier-1 facts)
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-PATTERNS.md  (§2 — module shape + never-throw posture, §9 — test shape)
@src/routes/api/deezer/search/+server.ts
@src/lib/proxy/edge-cache.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract $lib/proxy/deezer-cover.ts and rewire the route — harness must pass untouched</name>
  <files>src/lib/proxy/deezer-cover.ts, src/routes/api/deezer/search/+server.ts</files>
  <read_first>
    - src/routes/api/deezer/search/+server.ts (WHOLE file — every item being moved: DEEZER_SEARCH :28, TTL :31, DeezerCover :34-42, DeezerHit :45-53, safeImageUrl :74-86 with its 6-line JSDoc, safePreviewUrl :115-127, DzAlbum/DzArtist/DzResult/DeezerSearchResponse :89-112, reshapeHit :130-141, reshapeSearch :149-162, the inline upstream URL :179 and fetch+parse :207-209)
    - src/routes/api/deezer/search/deezer-endpoint.test.ts:1-40 (what the harness imports and stubs — it must keep passing UNMODIFIED)
    - .planning/phases/30-*/30-RESEARCH.md §C.12 (the per-symbol rename/visibility table — follow it exactly, including widening reshapeDeezerSearch's param to unknown)
    - .planning/phases/30-*/30-PATTERNS.md §2 (module-shape analog: edge-cache.ts header style; never-throw JSDoc analog: itunes-cover.ts:83-100 with the enumerated null-return list and the mandatory `// RAW fetch (not apiFetch — fetch→apiFetch audit)` tag)
    - src/lib/proxy/edge-cache.ts (whole file, 39 lines — the canonical $lib/proxy pure-extraction header to copy)
  </read_first>
  <action>
    Create src/lib/proxy/deezer-cover.ts per the RESEARCH §C.12 table: move DEEZER_SEARCH (private),
    export DEEZER_COVER_TTL = 86400, export interfaces DeezerCover/DeezerHit, keep
    DzAlbum/DzArtist/DzResult/DeezerSearchResponse module-private, move safeImageUrl RENAMED to
    `safeDeezerImageUrl` and safePreviewUrl to `safeDeezerPreviewUrl` — move their JSDoc (the
    CSS/attribute-breaker rationale at :67-73) WITH them, both guards intact (the `/[)\s"'\\(]/`
    reject and the https-only check). Export `reshapeDeezerSearch(data: unknown, limit: number,
    prefer: 'xl' | 'big' = 'xl'): DeezerCover` — widen to unknown and narrow inside; `prefer`
    controls the album-cover pick order (`'xl'` → cover_xl ?? cover_big ?? cover_medium, the
    byte-identical legacy order; `'big'` → cover_big ?? cover_xl ?? cover_medium for /api/og per
    §C.13 — 73 KB vs 208 KB, Pitfall 6). Export `deezerSearchUrl(q: string, limit = 1): string`
    (the passthrough-only template with the T-wv8-01 comment moved along) and
    `export async function fetchDeezerCover(q, signal: AbortSignal, retries = 2, prefer: 'xl' | 'big' = 'xl'): Promise<DeezerCover | null>`
    — never-throw: returns null on non-ok / malformed JSON / abort / any throw (enumerate the list in
    the JSDoc like itunes-cover.ts), returns `{ cover: null, artistPicture: null }` on a clean
    no-match (the two-valued miss-vs-error contract /api/og depends on, §C.9). File header: line 1 =
    purpose + OG-EP-03; body names the forcing constraint (`svelte-server-endpoint-only-verb-exports`)
    like edge-cache.ts names its duplication. Then thin the route: `+server.ts` keeps ONLY jsonResult,
    GET, OPTIONS and all cache orchestration; its body calls
    `fetchDeezerCover(q, AbortSignal.timeout(8000), 2)` and maps a null return to the existing empty
    jsonResult (the old catch branch) — same timeout, same retries, same no-cache-write-on-error,
    same TTL, same WR-01 CORS re-application, byte-identical payload. Do NOT touch the cache-key
    line (`new Request(url.toString())` at :184 stays as-is — changing it is out of scope and risks
    the harness). Do NOT edit deezer-endpoint.test.ts under ANY circumstances — an executor editing
    it to make it pass has invalidated OG-EP-03.
  </action>
  <verify>
    <automated>pnpm vitest --run src/routes/api/deezer/search/deezer-endpoint.test.ts && git diff --exit-code src/routes/api/deezer/search/deezer-endpoint.test.ts && pnpm check</automated>
  </verify>
  <done>Route is a thin shell over $lib/proxy/deezer-cover; the untouched harness passes; check clean.</done>
  <acceptance_criteria>
    - `pnpm vitest --run src/routes/api/deezer/search/deezer-endpoint.test.ts` exits 0 **with the file unmodified** (`git diff --exit-code` on it succeeds)
    - `pnpm check` exits 0
    - `grep -c 'export' src/routes/api/deezer/search/+server.ts` shows only verb exports remain (GET, OPTIONS — no helper export in the route)
    - src/lib/proxy/deezer-cover.ts exports exactly: DeezerCover, DeezerHit, DEEZER_COVER_TTL, safeDeezerImageUrl, safeDeezerPreviewUrl, reshapeDeezerSearch, deezerSearchUrl, fetchDeezerCover (grep each name preceded by `export`)
    - `grep -c 'RAW fetch' src/lib/proxy/deezer-cover.ts` ≥ 1 (the fetch→apiFetch audit tag)
    - reshapeDeezerSearch default prefer='xl' keeps the cover_xl-first order (assert in Task 2's tests; behavioral proof is the harness)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deezer-cover.test.ts — direct coverage of the extracted helpers</name>
  <files>src/lib/proxy/deezer-cover.test.ts</files>
  <read_first>
    - src/lib/proxy/deezer-cover.ts (just created)
    - src/lib/proxy/http.test.ts (same-directory test analog)
    - src/lib/services/share.test.ts:38-67 (pure-function describe/it block shape, incl. the `'… without throwing'` null/empty edge case idiom)
    - .planning/phases/30-*/30-PATTERNS.md §9
  </read_first>
  <behavior>
    - safeDeezerImageUrl: accepts https cdn-images.dzcdn.net and *.dzcdn.net; rejects http, non-dzcdn hosts, and any URL containing `)`/quote/space/backslash/`(` (CSS-breaker reject); null/undefined/'' → null without throwing
    - reshapeDeezerSearch: prefer 'xl' picks cover_xl ?? cover_big ?? cover_medium; prefer 'big' picks cover_big first; a `{ data: [], total: 0 }` body → { cover: null, artistPicture: null }; a malformed/unknown body → the empty sentinel, never a throw
    - deezerSearchUrl: encodeURIComponent's q into the fixed template; limit default 1
    - fetchDeezerCover (vi.stubGlobal fetch): clean no-match 200 → { cover: null, artistPicture: null } (MISS); non-ok / rejected fetch / malformed JSON → null (ERROR); a hit reshapes per prefer
  </behavior>
  <action>
    Create src/lib/proxy/deezer-cover.test.ts: one flat import from './deezer-cover', one describe per
    exported function, one it per behavior above. Stub global fetch with vi.stubGlobal +
    beforeEach/afterEach vi.unstubAllGlobals (copy the lifecycle from deezer-endpoint.test.ts:17-24).
    Every it() must contain at least one expect (vite.config.ts sets requireAssertions: true). The
    miss-vs-error describe block must have a comment naming why the two-valued return exists (OG-EP-01
    negative-caching, RESEARCH §C.9).
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/proxy/deezer-cover.test.ts src/routes/api/deezer/search/deezer-endpoint.test.ts && pnpm test</automated>
  </verify>
  <done>Extracted helpers directly covered; harness still untouched and green; full suite green.</done>
  <acceptance_criteria>
    - `pnpm vitest --run src/lib/proxy/deezer-cover.test.ts` exits 0
    - `git diff --exit-code src/routes/api/deezer/search/deezer-endpoint.test.ts` still succeeds
    - the test file asserts BOTH sentinel shapes: `null` (fault) and `{ cover: null, artistPicture: null }` (clean miss)
    - `pnpm test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deezer upstream JSON → reshape | untrusted third-party body parsed edge-side |
| reshaped cover URL → client/crawler | must never smuggle a non-Deezer host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wv8-05 | Tampering (SSRF/relay) | safeDeezerImageUrl | mitigate | guard moves VERBATIM with both checks (https-only + dzcdn host suffix + CSS-breaker reject); renamed per-tier so a Deezer body can never pass an mzstatic/kuwo URL |
| T-wv8-01 | Tampering | deezerSearchUrl | mitigate | passthrough-only: q is encodeURIComponent'd into a fixed template; comment moved with the code |
| T-09-05 / T-2os-02 / T-wv8-06 | Info disclosure | route cache key | mitigate (unchanged) | own-origin cache key untouched in this plan; upstream URL never becomes a key |
| T-30-03 | Tampering (regression) | extraction itself | mitigate | deezer-endpoint.test.ts passes with ZERO edits — editing it invalidates OG-EP-03 |
| T-{30}-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- Harness green + unmodified (`git diff --exit-code`); new helper tests green; `pnpm check` + `pnpm test` green.
</verification>

<success_criteria>
- `/api/deezer/search` behavior provably unchanged; helpers reachable by `/api/og`; miss-vs-error contract tested.
</success_criteria>

<output>
Create `.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-02-SUMMARY.md` when done.
</output>
