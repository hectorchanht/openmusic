---
phase: 30-carrier-free-share-links-type-artist-title-api-og
plan: 06
type: execute
wave: 4
depends_on: [30-01, 30-02, 30-03, 30-04, 30-05]
files_modified:
  - .planning/todos/pending/og-png-raster-fallback.md
autonomous: false
requirements: [OG-VERIFY-01, OG-PAGE-01]
must_haves:
  truths:
    - "pnpm test green, pnpm check clean, pnpm build and pnpm build:native both exit 0 with the whole phase present"
    - "The full curl matrix passes: route coexistence, the % fix, /api/og tiers, per-surface og heads"
    - "A real card with real album art renders in at least 3 messengers against the deployed URL"
    - "The APK shows the song-page cover image, not a broken image"
  artifacts:
    - path: ".planning/todos/pending/og-png-raster-fallback.md"
      provides: "The deferred static/og.png raster follow-up (SVG og:image is unsupported by Slack/iMessage/Twitter — pre-existing, not a Phase 30 regression)"
  key_links:
    - from: "deployed https://openmusic.lol/song/{artist}/{title}"
      to: "/api/og"
      via: "crawler fetches og:image (the only end-to-end proof of OG-VERIFY-01)"
      pattern: "og:image"
---

<objective>
Phase gate: run every automated gate against the finished phase, corroborate the full curl matrix,
then verify the two things no sandbox can prove — real messenger cards against the deployed URL
(OG-VERIFY-01) and the APK `<img>` (OG-PAGE-01's native trap).

Purpose: crawler cards and Capacitor-WebView behavior are only observable post-deploy / on-device.
Output: gates green, two human sign-offs, the og.png follow-up logged.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-VALIDATION.md  (per-requirement map + manual-only table)
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-RESEARCH.md  (§A.2 coexistence matrix, §D.15–D.16 crawler facts + verification split, §C.11 SVG-fallback status quo)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated phase gate + curl matrix + og.png follow-up todo</name>
  <files>.planning/todos/pending/og-png-raster-fallback.md</files>
  <read_first>
    - .planning/phases/30-*/30-VALIDATION.md (the sampling contract this task discharges)
    - .planning/phases/30-*/30-RESEARCH.md §A.2 (the exact live matrix) and §C.11 (the raster recommendation being logged)
  </read_first>
  <action>
    Run the four gates: `pnpm test`, `pnpm check`, `pnpm build`, `pnpm build:native`. Then with
    `pnpm dev` running (:5173), execute the full curl matrix and record every status in the SUMMARY:
    coexistence (§A.2: legacy song/album 200, new song/album 200, /song/A/B/C 404), the compat fix
    (/album/50%25%20Off → 200, /artist/50%25%20Cent → 200), /api/og (known song → 200 image/*,
    immutable Cache-Control; no params → 200 image/svg+xml; bad type → 200), and per-surface heads
    (`grep -c 'og:type'` → 1 on each of the five routes; values music.song / music.album / profile;
    og:url shows the requested origin). Optionally corroborate cache mechanics under real workerd
    with `pnpm preview` (edgeCache is null under vite dev — Pitfall 8; TTL/eviction still needs the
    deploy). Create .planning/todos/pending/og-png-raster-fallback.md recording RESEARCH §C.11's
    recommendation: SVG og:image is unsupported on Slack/iMessage/Twitter (pre-existing status quo,
    NOT a Phase 30 regression); the fix is a one-time committed static/og.png (1200×630, `qlmanage -t`
    locally) + swapping the OG_FALLBACK constant — deferred to keep phase scope.
  </action>
  <verify>
    <automated>pnpm test && pnpm check && pnpm build && pnpm build:native</automated>
  </verify>
  <done>All gates green; curl matrix recorded; follow-up todo exists.</done>
  <acceptance_criteria>
    - `pnpm test && pnpm check && pnpm build && pnpm build:native` exits 0
    - Every curl in the matrix returns its expected status/header (recorded verbatim in the SUMMARY)
    - `.planning/todos/pending/og-png-raster-fallback.md` exists and names §C.11
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real messenger cards against the deployed URL (OG-VERIFY-01)</name>
  <action>Automation BEFORE pausing: run `pnpm deploy` (build + wrangler pages deploy → https://openmusic.lol), then re-run the head checks against production — `curl -A 'facebookexternalhit/1.1' https://openmusic.lol/song/Nirvana/Come-As-You-Are` (expect 200 + og tags) and `curl -sI 'https://openmusic.lol/api/og?type=song&artist=Nirvana&title=Come+As+You+Are'` (expect 200 image/*; a SECOND request should show an edge cache hit via cf-cache-status / Workers logs — the only place TTL behavior is real). Record results, then pause for the human card checks below.</action>
  <verify>Production curls above return the expected statuses/headers; human confirms real album art in ≥3 messengers.</verify>
  <done>Deployed; production heads verified; human approval received naming the messengers that rendered the art.</done>
  <what-built>Carrier-free share links whose OG card art streams from the new own-origin /api/og endpoint, deployed to https://openmusic.lol.</what-built>
  <how-to-verify>
    1. Facebook Sharing Debugger (developers.facebook.com/tools/debug): enter https://openmusic.lol/song/Nirvana/Come-As-You-Are — the card must show the real Nirvana album art, not the branded og.svg. If a bad first crawl was cached, hit "Scrape Again".
    2. Twitter/X Card Validator (or paste the link in a tweet draft): card renders with the art (a square cover center-cropped to 1.91:1 is EXPECTED — same as today).
    3. Paste the link into at least 3 real messengers — WhatsApp, iMessage, Slack (Discord/Telegram bonus): each unfurls with the album art.
    4. Paste one CJK link — https://openmusic.lol/song/周杰倫/稻香 — the preview must show the decoded CJK path/title and a real cover (kuwo tier reachable in prod).
    5. Paste one legacy link from before this phase (any old ?n=&a=&c= URL) — its card must still render (OG-COMPAT-01 in the wild).
  </how-to-verify>
  <resume-signal>Type "approved" naming which messengers rendered the art, or describe which platform showed og.svg/blank so a gap plan can target it</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: APK song-page cover image (OG-PAGE-01 native trap)</name>
  <action>Automation BEFORE pausing: run `pnpm apk` (build:native + cap sync + gradle assembleDebug) and confirm the gradle build exits 0. Then pause for the on-device check below.</action>
  <verify>`pnpm apk` exits 0; human confirms the cover image renders on-device (and the gradient appears offline, never a broken image).</verify>
  <done>APK built; human approval received for the on-device cover render.</done>
  <what-built>The song share pages render the cover via apiUrl('/api/og?…'), which resolves to VITE_API_BASE (https://openmusic.lol) inside the Capacitor WebView — a bare /api/og would have resolved to https://localhost and shown a broken image (Pitfall 7).</what-built>
  <how-to-verify>
    1. Install the debug APK on an Android device.
    2. Open a shared song link in the app (or navigate to a /song/{artist}/{title} URL in the WebView).
    3. Confirm the album cover image renders on the song landing page — not a broken-image icon.
    4. Kill network and reopen — the gradient fallback should appear (onerror path), never a broken image.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the broken state (screenshot of the landing page helps)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet crawlers → deployed /api/og | first real exposure of the endpoint to arbitrary callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wv8-04 | DoS | deployed /api/og | mitigate (verify) | after deploy, confirm a repeat request is an edge cache hit (cf-cache-status) — the 86400 immutable layer is the rate cap in practice |
| T-og-01 | DoS | crawler storms on junk queries | mitigate (verify) | spot-check that a no-term request costs zero subrequests in Workers logs |
| T-30-06 | Repudiation | phase gate | mitigate | every curl result recorded verbatim in the SUMMARY (auditable gate) |
| T-{30}-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- All automated gates green (Task 1), messenger cards approved (Task 2), APK approved (Task 3).
</verification>

<success_criteria>
- OG-VERIFY-01: full suite + both builds green AND a real card with real art in ≥3 messengers.
- OG-PAGE-01: cover image confirmed on-device in the APK.
</success_criteria>

<output>
Create `.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-06-SUMMARY.md` when done.
</output>
