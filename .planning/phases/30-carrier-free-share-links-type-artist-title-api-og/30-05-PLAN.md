---
phase: 30-carrier-free-share-links-type-artist-title-api-og
plan: 05
type: execute
wave: 3
depends_on: [30-01, 30-04]
files_modified:
  - src/routes/(app)/song/[slug]/+page.ts
  - src/routes/(app)/song/[slug]/+page.svelte
  - src/routes/(app)/song/[slug]/loader.test.ts
  - src/routes/(app)/album/[name]/+page.ts
  - src/routes/(app)/album/[name]/loader.test.ts
  - src/routes/(app)/album/[name]/+page.svelte
  - src/routes/(app)/artist/[name]/+page.ts
  - src/routes/(app)/artist/[name]/loader.test.ts
autonomous: true
requirements: [OG-COMPAT-01, OG-PAGE-01]
must_haves:
  truths:
    - "Legacy /song/{slug}?n=&a=&c= and /album/{name}?artist=&c=&dn=&da= still return 200 with the same card as today"
    - "GET /album/50%25%20Off returns 200 (today: 500 — the double-decode URIError); same for /artist/50%25%20Cent"
    - "/artist/{name} accepts BOTH a carrier-free path (new shape, /api/og-backed card) and the legacy ?c=&dn= form"
    - "The legacy song share page renders the carried cover as an <img> when og.image exists, gradient otherwise; the stale 'cover is never carried' comment is corrected"
    - "Each legacy loader emits the correct og:type (music.song / music.album / profile)"
  artifacts:
    - path: "src/routes/(app)/song/[slug]/loader.test.ts"
      provides: "First-ever legacy loader regression gate (Wave 0 gap, OG-COMPAT-01)"
    - path: "src/routes/(app)/album/[name]/loader.test.ts"
      provides: "%-name no-throw + carrier regression gate"
    - path: "src/routes/(app)/artist/[name]/loader.test.ts"
      provides: "dual-shape (carrier-free + legacy) handler gate"
  key_links:
    - from: "src/routes/(app)/artist/[name]/+page.ts"
      to: "$lib/services/share"
      via: "decodePathSegment(params.name) + ogImageUrl fallback when no legacy c"
      pattern: "ogImageUrl"
    - from: "src/routes/(app)/song/[slug]/+page.svelte"
      to: "data.og.image"
      via: "<img class=\"cover\"> with gradient error fallback"
      pattern: "data.og.image"
---

<objective>
Lock backward compatibility: keep every legacy query-carrier URL working, fix the live double-decode
500 (`/album/50%25%20Off`), make `/artist/[name]` the dual new+legacy handler, add per-surface
`og:type` to the three legacy loaders, add the three legacy loader tests (fail-first on the % bug),
and finish OG-PAGE-01's song-page `<img>` swap on the legacy route.

Purpose: every `?n=&a=&c=` / `?artist=&c=&dn=&da=` URL in someone's chat history must keep working
forever — that IS the runtime state of this phase.
Output: three fixed loaders + three loader tests + the legacy song page `<img>`; full suite green.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-RESEARCH.md  (§A.3 the 500 bug + plan actions, §F.19 query-carrier reader inventory, §F.18 the <img> rules)
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-PATTERNS.md  (§10 loader-test constraints — fail-first is mandated for the % case)
@src/routes/(app)/album/[name]/+page.ts
@src/routes/(app)/artist/[name]/+page.ts
@src/routes/(app)/song/[slug]/+page.ts

<interfaces>
From src/lib/services/share.ts (plan 30-01): `decodePathSegment`, `ogImageUrl`, `OgType`, `buildOg` (with `type`), `isHttpsUrl`.
From src/lib/components/PageOg.svelte (plan 30-04): og prop accepts optional `type?: OgType`, default 'music.song'.
The three double-decode sites to DELETE (live 500 on a literal %):
- src/routes/(app)/album/[name]/+page.ts:22 — `decodeURIComponent(params.name ?? '')`
- src/routes/(app)/artist/[name]/+page.ts:22 — same
- src/routes/(app)/album/[name]/+page.svelte:50 — `decodeURIComponent(page.params.name ?? '')`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Legacy loader tests (fail-first on %) + double-decode fixes + og.type + dual-shape artist handler</name>
  <files>src/routes/(app)/song/[slug]/+page.ts, src/routes/(app)/song/[slug]/loader.test.ts, src/routes/(app)/album/[name]/+page.ts, src/routes/(app)/album/[name]/loader.test.ts, src/routes/(app)/album/[name]/+page.svelte, src/routes/(app)/artist/[name]/+page.ts, src/routes/(app)/artist/[name]/loader.test.ts</files>
  <read_first>
    - src/routes/(app)/album/[name]/+page.ts and src/routes/(app)/artist/[name]/+page.ts (whole files — the decode lines, the c/dn/da carrier reads that MUST stay, the DQ/T-24-08 comments that MUST keep their tags)
    - src/routes/(app)/song/[slug]/+page.ts (whole file — only gains og.type; carriers stay VERBATIM)
    - src/routes/(app)/album/[name]/+page.svelte:40-60 (the :50 decode + ?artist= read — keep the ?artist= read, delete only the decode)
    - .planning/phases/30-*/30-RESEARCH.md §A.3 (plan actions + the live-verified round-trip table), §F.19 rows for these files
    - .planning/phases/30-*/30-PATTERNS.md §10 (test shape; constraint 3 — tests pass ALREADY-DECODED params)
  </read_first>
  <behavior>
    - FAIL-FIRST: write the three loader tests first; the case `load({ params: { name: '50% Off' }, url })` must THROW against today's album/artist loaders (URIError) — run, watch it fail, then delete the decode and watch it pass (that sequence is the regression proof)
    - song/[slug]: `?n=&a=&c=` carriers populate og exactly as today; og.type === 'music.song'
    - album/[name]: ?artist=/c/dn/da still read; og.type === 'music.album'; '50% Off' no longer throws
    - artist/[name]: legacy `?c=&dn=` still wins for the card; a bare carrier-free path yields name via decodePathSegment (so /artist/Post-Malone → 'Post Malone') and og.image = ogImageUrl(url.origin, 'artist', name); og.type === 'profile'
  </behavior>
  <action>
    First create the three loader tests per PATTERNS §10 (co-located `loader.test.ts`, import { load }
    from './+page', already-decoded params, `as any` test-cast idiom, ≥1 expect per it) covering the
    behaviors above, and run them — the %-name cases MUST fail against HEAD (URIError). Then: delete
    the `decodeURIComponent` at album/[name]/+page.ts:22, artist/[name]/+page.ts:22 and
    album/[name]/+page.svelte:50 (read `params.name` / `page.params.name` directly — SvelteKit's
    decode_params already ran; annotate with OG-COMPAT-01 + Pitfall 1 and KEEP the surrounding DQ/D-xx
    tags, correcting prose where now wrong). Add og.type to each loader's buildOg call: song/[slug] →
    'music.song' (NO other change — the n/a/c reads stay verbatim, legacy c stays isHttpsUrl-gated
    exactly as today); album/[name] → 'music.album'; artist/[name] → 'profile'. Make artist/[name] the
    dual handler (PATTERNS correction 4): `const name = decodePathSegment(params.name ?? '')` (comment
    the accepted hyphen→space loss — LOCKED edge, matchKey-invariant, RESEARCH §B.8; internal nav's
    %20 form decodes identically), keep the c/dn reads with today's precedence, and when no https `c`
    is carried set og.image = ogImageUrl(url.origin, 'artist', name) instead of null (the carrier-free
    artist link gets a real card; own-origin URL — bypass the isHttpsUrl carrier gate the same way the
    new loaders do). Do NOT touch the `?play=` reader at (app)/+page.ts:15 (out of scope) and do NOT
    update the album goto at artist/[name]/+page.svelte:464 (stays legacy — see 30-04 Task 3's
    documented redirect design).
  </action>
  <verify>
    <automated>pnpm vitest --run 'src/routes/(app)/song/[slug]/loader.test.ts' 'src/routes/(app)/album/[name]/loader.test.ts' 'src/routes/(app)/artist/[name]/loader.test.ts' && pnpm check && pnpm test</automated>
  </verify>
  <done>Live 500 fixed with a failing-first regression test; all legacy carrier shapes green; artist route serves both link generations.</done>
  <acceptance_criteria>
    - All three loader tests exit 0, including a '50% Off' / '50% Cent' no-throw case per loader that demonstrably failed before the fix (note the fail-first run in the summary)
    - `pnpm check` and `pnpm test` exit 0
    - `grep -rn 'decodeURIComponent(params' 'src/routes/(app)/album/[name]' 'src/routes/(app)/artist/[name]'` returns no matches; `grep -c 'decodeURIComponent' 'src/routes/(app)/album/[name]/+page.svelte'` outputs 0
    - With `pnpm dev`: `curl -s -o /dev/null -w '%{http_code}' 'http://localhost:5173/album/50%25%20Off'` outputs 200 (today: 500); `curl -s -o /dev/null -w '%{http_code}' 'http://localhost:5173/artist/50%25%20Cent'` outputs 200
    - `curl -s 'http://localhost:5173/song/come-as-you-are-nirvana?n=Come%20As%20You%20Are&a=Nirvana' | grep -c 'og:title'` ≥ 1 (legacy shape intact); `curl -s http://localhost:5173/artist/Nirvana | grep -o 'og:image[^>]*'` contains '/api/og?type=artist'
    - `grep -c 'DQ-1' 'src/routes/(app)/song/[slug]/+page.ts'` ≥ 1 (legacy carrier decision refs preserved)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Legacy song page <img> swap + stale-comment fix</name>
  <files>src/routes/(app)/song/[slug]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/song/[slug]/+page.svelte (whole file — the :19 stale comment 'The cover is never carried', the :66 placeholder div, the .cover/.cover--placeholder CSS)
    - src/routes/(app)/song/[artist]/[title]/+page.svelte (from 30-04 — the <img>+onerror-fallback shape to mirror; NOTE the difference: here the src is data.og.image, the https-gated legacy c carrier — an external CDN URL, so apiUrl does NOT apply)
    - .planning/todos/pending/song-share-stale-cover-comment.md (the todo this folds in)
  </read_first>
  <action>
    Replace the `<div class="cover cover--placeholder">` at :66 with: when `data.og.image` is non-null
    and no error has fired, `<img class="cover" src={data.og.image} alt="" referrerpolicy="no-referrer">`
    with an onerror handler flipping a `$state` boolean back to the gradient div; when og.image is
    null (no c carrier on the legacy link), keep the gradient exactly as today. Correct the :19 header
    comment — the cover IS carried since quick-260723-r4p and is now also rendered on the page
    (OG-PAGE-01); keep the quick-260723-r4p tag, fix the prose. Preserve the SSR-safety-by-construction
    import list (an <img> and a $state flag add no store imports). Delete the todo file
    .planning/todos/pending/song-share-stale-cover-comment.md is NOT this task's job — note it as
    resolved in the SUMMARY instead.
  </action>
  <verify>
    <automated>pnpm check && pnpm test && pnpm build && pnpm build:native</automated>
  </verify>
  <done>Legacy song landing shows the carried album art (crawler card and landing page show the same art); gradient preserved as the null/error fallback; both builds green.</done>
  <acceptance_criteria>
    - `pnpm check`, `pnpm test`, `pnpm build`, `pnpm build:native` all exit 0
    - `grep -c 'data.og.image' 'src/routes/(app)/song/[slug]/+page.svelte'` ≥ 1 (the <img> src) and `grep -c 'cover--placeholder' 'src/routes/(app)/song/[slug]/+page.svelte'` ≥ 1 (fallback retained)
    - `grep -c 'quick-260723-r4p' 'src/routes/(app)/song/[slug]/+page.svelte'` ≥ 1 with the stale 'never carried' wording gone (`grep -c 'never carried' …` outputs 0)
    - With `pnpm dev`: `curl -s 'http://localhost:5173/song/x?n=A&a=B&c=https%3A%2F%2Fcdn-images.dzcdn.net%2Fx.jpg' | grep -c 'og:image'` ≥ 1 (legacy c still drives the card)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy share URL (wild) → loaders | attacker-craftable query carriers (c, dn, da) and path text |
| legacy c carrier → <img src> | sharer-supplied URL now rendered on-page, not just in a meta tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | S/SSRF | legacy loaders | mitigate (unchanged) | loaders still perform NO fetch; legacy c stays isHttpsUrl-gated exactly as today; artist fallback image is own-origin constructed |
| T-30-05 | Tampering | <img src={data.og.image}> | mitigate | og.image only exists after the isHttpsUrl gate (absolute https, CSS-breaker-free shapes only); referrerpolicy no-referrer matches the app's audio posture; onerror degrades to the gradient (healCover precedent) |
| Pitfall 1 | DoS (500) | double-decode | mitigate | delete all three decodeURIComponent(params) sites; fail-first regression tests pin it |
| T-gln-02 | Tampering (XSS) | og.type additions | mitigate | closed OgType union, content={} binding via PageOg |
| T-{30}-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- Three loader tests green (with the documented fail-first run); full suite + check + both builds green; curl compat matrix per task criteria.
</verification>

<success_criteria>
- Every legacy URL shape in the wild keeps resolving with its card; the % 500 is dead; artist route serves both generations; legacy song page shows the carried art.
</success_criteria>

<output>
Create `.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-05-SUMMARY.md` when done.
</output>
