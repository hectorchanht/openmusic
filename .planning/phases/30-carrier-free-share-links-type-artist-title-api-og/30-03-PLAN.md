---
phase: 30-carrier-free-share-links-type-artist-title-api-og
plan: 03
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - src/lib/proxy/og-cover.ts
  - src/routes/api/og/+server.ts
  - src/routes/api/og/og-endpoint.test.ts
  - src/lib/services/itunes-cover.ts
autonomous: true
requirements: [OG-EP-01, OG-EP-02, OG-EP-03]
must_haves:
  truths:
    - "GET /api/og?type=song&artist=X&title=Y returns 200 with Content-Type image/* and Cache-Control: public, max-age=86400, immutable — never a 30x, never a 500"
    - "Tier order is Deezer → iTunes → kuwo; a miss falls through; any fault falls through to the branded /og.svg body; total resolve stays under one ~2.5s deadline"
    - "A second identical request is served from caches.default with no second upstream fetch; nothing is cached on a tier error"
    - "No query terms → the branded fallback with ZERO subrequests"
  artifacts:
    - path: "src/lib/proxy/og-cover.ts"
      provides: "resolveCoverTiered + safeItunesImageUrl + safeKuwoImageUrl + inlined OG fallback SVG (OG-EP-01/03)"
      exports: ["resolveCoverTiered", "safeItunesImageUrl", "safeKuwoImageUrl", "OG_FALLBACK_SVG"]
      min_lines: 80
    - path: "src/routes/api/og/+server.ts"
      provides: "GET/OPTIONS streaming image endpoint, two caches.default layers via ownOriginCacheKey (OG-EP-02)"
      exports: ["GET", "OPTIONS"]
    - path: "src/routes/api/og/og-endpoint.test.ts"
      provides: "Wave 0 gate for OG-EP-01/OG-EP-02"
  key_links:
    - from: "src/routes/api/og/+server.ts"
      to: "src/lib/proxy/og-cover.ts"
      via: "resolveCoverTiered import (verb-only route constraint)"
      pattern: "resolveCoverTiered"
    - from: "src/lib/proxy/og-cover.ts"
      to: "src/lib/proxy/deezer-cover.ts"
      via: "fetchDeezerCover import (the OG-EP-03 shared implementation)"
      pattern: "fetchDeezerCover"
    - from: "src/routes/api/og/+server.ts"
      to: "src/lib/proxy/edge-cache.ts"
      via: "edgeCache + ownOriginCacheKey (BOTH layers — never new Request(url.toString()) hand-roll)"
      pattern: "ownOriginCacheKey"
---

<objective>
Build the `/api/og` cover endpoint: `$lib/proxy/og-cover.ts` (the tiered, bounded Deezer → iTunes →
kuwo-only resolve chain plus the per-tier host allowlists and the inlined `/og.svg` fallback) and
`src/routes/api/og/+server.ts` (streaming pass-through, two `caches.default` layers, never-500),
gated by `og-endpoint.test.ts`.

Purpose: the cover half of the phase — `og:image` becomes an own-origin URL so the shared link
carries no cover carrier at all.
Output: the endpoint live-verifiable on :5173 (all three tiers reachable in this sandbox), unit-gated
with stubbed fetch + in-memory caches.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-RESEARCH.md  (§C.9–C.14 — the full endpoint design; §D.15 crawler constraints)
@.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-PATTERNS.md  (§1 hybrid analog, §3 tier-chain analogs, §8 test shape)
@.claude/skills/spike-findings-openmusic/SKILL.md  (kuwo-first resolution rationale — why the CN tier is kuwo ONLY, never searchAll fan-out)
@src/routes/api/deezer/search/+server.ts
@src/routes/api/ytmusic/stream/[videoId]/+server.ts

<interfaces>
From src/lib/proxy/edge-cache.ts (use the helper — do NOT copy deezer/search's hand-rolled `new Request(url.toString())` at :184; five newer routes use the helper and CONTEXT locks it):
```typescript
export function edgeCache(): EdgeCache | null;
export function ownOriginCacheKey(url: URL | string): Request;
```
From src/lib/proxy/deezer-cover.ts (plan 30-02):
```typescript
export async function fetchDeezerCover(q: string, signal: AbortSignal, retries?: number, prefer?: 'xl' | 'big'): Promise<DeezerCover | null>;
```
From src/lib/services/itunes-cover.ts: `buildItunesSearchUrl(term, entity, attribute?)` (reuse as-is; artist type → entity 'album' + attribute 'artistTerm' — musicArtist carries no artwork, itunes-cover.ts:28-31). `upgradeArtwork` swaps to 1200x1200bb — parameterize it (default '1200x1200bb') so /api/og can request '600x600bb' (101 KB vs 332 KB, Pitfall 6) without changing client behavior.
From src/lib/proxy/kuwo.ts: `kuwoProxy.buildUrl('search', new URLSearchParams({ name, limit: '1' }), undefined)` — THROWS on unsupported path, wrap the tier body in try. Search response already carries `pic` (sources/kuwo.ts:82) — kuwo is 1 subrequest, not 2.
From src/lib/services/match-key.ts: `matchKey(artist, title)` — the layer-1 resolve cache key normalizer.
From src/lib/proxy/http.ts: `fetchWithRetry(url, init, retries)` — pass retries=0 (backoff doesn't fit the 2.5s deadline); it THROWS on budget exhaustion, every tier needs its own try.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: $lib/proxy/og-cover.ts — tier chain, per-tier allowlists, inlined fallback + direct tier tests</name>
  <files>src/lib/proxy/og-cover.ts, src/lib/services/itunes-cover.ts, src/routes/api/og/og-endpoint.test.ts</files>
  <read_first>
    - .planning/phases/30-*/30-RESEARCH.md §C.9 (deadline composition, TierOutcome hit/miss/error, resolveCoverTiered sketch in Code Examples), §C.13 (per-tier request sequences + measured variants — transcribe these facts into the module header as a LIVE PROBE block, the house style)
    - .planning/phases/30-*/30-PATTERNS.md §3 (tierSignal analog itunes-cover.ts:69-81 combinedSignal — copy the AbortSignal.any feature-detect verbatim; kuwo drift guard sources/kuwo.ts:62-68)
    - src/lib/services/itunes-cover.ts (whole file — buildItunesSearchUrl :44-56, upgradeArtwork :63-67, combinedSignal :69-81, fetchTopArtwork :83-100)
    - src/lib/proxy/kuwo.ts:15-43 (buildUrl contract)
    - src/lib/proxy/deezer-cover.ts (from 30-02 — fetchDeezerCover contract)
    - static/og.svg (the 1892-byte constant being inlined)
    - src/routes/api/deezer/search/deezer-endpoint.test.ts:17-63 (lifecycle + capturedUpstream fetch-stub idiom for the tier-order test)
  </read_first>
  <behavior>
    - Tier order: with all three tiers stubbed to hit, only ONE upstream fetch fires (Deezer); with Deezer stubbed to miss, iTunes fires next; kuwo last (capture upstream URLs in call order, assert sequence)
    - Miss vs error: a clean Deezer `{ data: [], total: 0 }` → falls through as MISS; a rejected fetch → falls through as ERROR; resolveCoverTiered returns null when all tiers cleanly miss and 'ERROR' when any tier errored and no tier hit
    - A pre-aborted deadline signal → resolveCoverTiered returns without a single fetch call
    - safeItunesImageUrl accepts only https `*.mzstatic.com`; safeKuwoImageUrl only https `*.kuwo.cn`; both reject CSS/attribute breakers and never throw on null/'' (same guard shape as safeDeezerImageUrl)
    - A tier returning a URL on the WRONG host (e.g. Deezer JSON smuggling an mzstatic URL) is rejected by that tier's own allowlist → falls through
    - upgradeArtwork(url) still returns the 1200x1200bb form (client unchanged); upgradeArtwork(url, '600x600bb') returns the 600 form
  </behavior>
  <action>
    Create src/lib/proxy/og-cover.ts (pure .ts — a +server.ts may only export verbs; this module is
    what makes OG-EP-01 unit-testable). Contents: a `TierOutcome` union ({kind:'hit';url} | {kind:'miss'}
    | {kind:'error'}); `resolveCoverTiered(type: 'song' | 'album' | 'artist', artist: string, title:
    string, deadline: AbortSignal): Promise<string | null | 'ERROR'>` running the tiers SEQUENTIALLY
    (Deezer hit must cost exactly one subrequest — kuwo-first spike rationale caps the CN tier at kuwo
    ONLY, never searchAll), checking `deadline.aborted` before each tier, per RESEARCH's sketch; a
    module-private `tierSignal(ms)` copying itunes-cover.ts's combinedSignal AbortSignal.any
    feature-detect (per-tier budgets TIER_MS deezer 1200 / itunes 900 / kuwo 1200 under OVERALL_MS
    2500 — budgets deliberately over-sum, the deadline is the ceiling); the Deezer tier calling
    `fetchDeezerCover(`${artist} ${title}`.trim(), tierSignal(...), 0, 'big')` and mapping type
    'artist' → artistPicture else cover, each through safeDeezerImageUrl; the iTunes tier reusing
    buildItunesSearchUrl (song→'song', album→'album', artist→'album'+attribute 'artistTerm'), raw
    fetch with the `// RAW fetch (not apiFetch — …)` tag, artworkUrl100 through the parameterized
    upgradeArtwork(url, '600x600bb'), then safeItunesImageUrl; the kuwo tier building the URL via
    kuwoProxy.buildUrl('search', new URLSearchParams({ name: `${artist} ${title}`.trim(), limit: '1' }),
    undefined) inside its try (buildUrl throws), treating code!==200 or non-array data as ERROR
    (drift, matching sources/kuwo.ts posture), empty data as MISS, and data[0].pic through
    safeKuwoImageUrl; `safeItunesImageUrl`/`safeKuwoImageUrl` copying the safeDeezerImageUrl guard
    shape exactly (https-only + host suffix + CSS-breaker reject — three separate per-tier functions,
    never one widened allowlist, OG-EP-03); and `OG_FALLBACK_SVG` — the static/og.svg file contents
    inlined as a template-literal constant with `OG_FALLBACK_TYPE = 'image/svg+xml'` (RESEARCH §C.11:
    zero network, zero loop risk; do NOT create a third og-fallback.ts module; note in the comment
    that an SVG card is the pre-existing status quo and a PNG raster is a logged follow-up).
    Modify itunes-cover.ts ONLY to add the optional size parameter to upgradeArtwork (default
    '1200x1200bb' — one token-swap implementation, client byte-identical). Every export gets JSDoc
    with decision refs (OG-EP-01, T-og-01/T-og-02 where relevant); tabs; single quotes; zero as any.
    Create src/routes/api/og/og-endpoint.test.ts with the lifecycle from deezer-endpoint.test.ts and
    the DIRECT tier-chain describe blocks per <behavior> (import { resolveCoverTiered, … } from
    '$lib/proxy/og-cover'; route-level blocks land in Task 2). Every it() needs ≥1 expect
    (requireAssertions).
  </action>
  <verify>
    <automated>pnpm vitest --run src/routes/api/og/og-endpoint.test.ts && pnpm check && pnpm test</automated>
  </verify>
  <done>Tier chain unit-gated: order, miss-vs-error, deadline short-circuit, per-tier allowlist rejection all asserted; client itunes behavior unchanged; suite green.</done>
  <acceptance_criteria>
    - `pnpm vitest --run src/routes/api/og/og-endpoint.test.ts` exits 0 with describe blocks covering: tier order, miss fallthrough, error vs miss return values, pre-aborted deadline → zero fetches, per-tier host allowlist rejection
    - `pnpm check` exits 0; `pnpm test` exits 0 (proves the upgradeArtwork param change broke no client caller)
    - `grep -c 'searchAll' src/lib/proxy/og-cover.ts` outputs 0 (kuwo-only lock — no fan-out)
    - `grep -c 'export' src/lib/proxy/og-cover.ts | cat` ≥ 4 and OG_FALLBACK_SVG contains '1200' (the real og.svg dimensions — proves the constant is the real asset, not a stub)
    - three distinct safe*ImageUrl functions exist (`grep -c 'ImageUrl' src/lib/proxy/og-cover.ts` shows safeItunes + safeKuwo; safeDeezer stays in deezer-cover.ts)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/routes/api/og/+server.ts — streaming GET with two cache layers + route-level tests + live curl</name>
  <files>src/routes/api/og/+server.ts, src/routes/api/og/og-endpoint.test.ts</files>
  <read_first>
    - src/routes/api/deezer/search/+server.ts (the SHELL analog: header LIVE-PROBE style :1-23, imports :24-26, zero-work short-circuit :169-171, cache read + WR-01 CORS re-application :181-203, cache-write-on-success-only :210-222, OPTIONS :225-228)
    - src/routes/api/ytmusic/stream/[videoId]/+server.ts:107-133 (the BODY analog: fresh header object with an explicit allowlist — never spread upstream.headers; Content-Length passthrough rules)
    - .planning/phases/30-*/30-RESEARCH.md §C.9 (the GET skeleton incl. the normalized layer-1 resolve key), §C.10 (clone() buffering + CACHE_BYTES_CAP + Content-Type validation rules), §C.14 (cache.put restrictions; Vary: Origin is applied by hooks AFTER the route — the cached copy is header-clean)
    - .planning/phases/30-*/30-PATTERNS.md §1 (incl. the DEVIATION warning: use ownOriginCacheKey, NOT new Request(url.toString())), §8 (in-memory caches.default harness :279-347, cache-key assertion, no-write-on-error)
    - src/routes/api/[source]/[...path]/+server.ts:28-39 (closed-set predicate style for the type param — but /api/og COERCES to 'song' instead of 404ing; the route never fails)
  </read_first>
  <behavior>
    - GET with no artist and no title → 200 OG_FALLBACK_SVG, image/svg+xml, ZERO fetch calls (T-og-01)
    - type outside ['song','album','artist'] → coerced to 'song' (still 200)
    - A tier hit → 200, Content-Type from the validated upstream (must match ^image/, normalized to jpeg/png/webp allowlist), Cache-Control 'public, max-age=86400, immutable', body streamed
    - Upstream image responding text/html → branded fallback, not an html body relayed as an image
    - Second identical request → served from caches.default, upstream fetch count unchanged, CORS re-applied for THIS origin (WR-01)
    - Cache keys are own-origin (contain openmusic.lol/api/og, never api.deezer.com / itunes.apple.com / kw-api); param-order variants (?title=…&artist=… vs ?artist=…&title=…) share ONE layer-1 resolve entry (matchKey-normalized synthetic key)
    - resolveCoverTiered returning 'ERROR' → NO cache.put on either layer; returning null (clean all-miss) → the negative IS cached on layer 1
    - The route never returns 500 or 30x under any stubbed fault; OPTIONS → 204 with own-origin CORS
  </behavior>
  <action>
    Create src/routes/api/og/+server.ts implementing RESEARCH §C.9's skeleton with these hard rules:
    imports only from './$types', '$lib/proxy/http' (corsHeaders), '$lib/proxy/edge-cache'
    (edgeCache + ownOriginCacheKey — BOTH layers; the deezer route's :184 hand-roll is a documented
    deviation, do not copy it), '$lib/proxy/og-cover', '$lib/services/match-key'. Read + trim
    artist/title (cap each at 200 chars — pathological-input guard), coerce type via
    `const OG_TYPES = ['song','album','artist'] as const` + a type predicate. Empty artist+title →
    fallback response, zero subrequests (T-og-01 comment). Layer 2 (bytes): `ownOriginCacheKey(url)`,
    match → WR-01 CORS re-application and return. Layer 1 (resolve): synthetic key
    `ownOriginCacheKey(`${url.origin}/api/og/_resolve?k=${encodeURIComponent(matchKey(artist, title))}&t=${type}`)`
    storing a small JSON body ({ cover: string | null }) with max-age=86400 — undefined (cache miss)
    triggers resolveCoverTiered with a fresh AbortSignal.timeout(2500) deadline; a returned 'ERROR' is
    NEVER written to cache; null (known-none) IS written (negative cache — comment why, §C.9). On a
    resolved cover: fetch the image bytes (RAW fetch tag; the URL already passed its tier allowlist),
    validate Content-Type against ^image/ + the jpeg/png/webp allowlist (anything else → branded
    fallback, never-throw shape), build a FRESH header object (ytmusic-stream style): own-origin CORS
    + validated content-type + 'Cache-Control: public, max-age=86400, immutable' + Content-Length
    passthrough only when upstream provided it; stream `new Response(upstream.body, { headers })`;
    cache-write the bytes only when Content-Length is finite and ≤ CACHE_BYTES_CAP = 3_000_000
    (T-og-02 — clone() buffers the whole body per CF docs; the CLONE goes to cache, the original
    streams to the client; `await cache.put` — the existing pattern in every route; do NOT touch
    src/app.d.ts / ctx.waitUntil, PATTERNS correction 3). The branded-fallback builder returns 200
    OG_FALLBACK_SVG with the same Cache-Control and CORS. Whole GET wrapped so no path can 500 —
    outermost catch returns the fallback. OPTIONS: the standard 204 + corsHeaders export. Header
    comment: OG-EP-01/02 refs, the three-tier LIVE PROBE facts from §C.13, stream-don't-302 rationale
    (§D.15 — WhatsApp/iMessage don't reliably follow og:image redirects). Extend og-endpoint.test.ts
    with route-level describe blocks per <behavior>: fakeEvent helper (platform: undefined — all
    tiers keyless), in-memory caches.default stub, capturedUpstream ordering, no-write-on-error,
    own-origin key assertions, never-500, OPTIONS 204.
  </action>
  <verify>
    <automated>pnpm vitest --run src/routes/api/og/og-endpoint.test.ts && pnpm check && pnpm test && pnpm build</automated>
  </verify>
  <done>Endpoint unit-gated on all OG-EP-01/02 contracts; builds green; live tier chain corroborated on :5173.</done>
  <acceptance_criteria>
    - `pnpm vitest --run src/routes/api/og/og-endpoint.test.ts` exits 0 covering ALL behaviors listed (incl. cache-key own-origin assertion `not.toContain('api.deezer.com')` and the no-cache-write-on-error spy)
    - `pnpm check`, `pnpm test`, `pnpm build` all exit 0
    - Live corroboration with `pnpm dev` running (dev server is :5173 — NOT 4321): `curl -sI 'http://localhost:5173/api/og?type=song&artist=Nirvana&title=Come+As+You+Are'` returns `200` with `content-type: image/` prefix and `cache-control: public, max-age=86400, immutable` (all three upstreams reachable in this sandbox; note edgeCache() is null under vite dev so caching is unit-proof only — Pitfall 8)
    - `curl -sI 'http://localhost:5173/api/og'` (no params) returns `200` with `content-type: image/svg+xml`
    - `curl -s -o /dev/null -w '%{http_code}' 'http://localhost:5173/api/og?type=banana&artist=Nirvana&title=x'` outputs `200` (coercion, never-fail)
    - `grep -c 'ownOriginCacheKey' src/routes/api/og/+server.ts` ≥ 2 (both layers via the helper); `grep -c 'new Request(url.toString())' src/routes/api/og/+server.ts` outputs 0
    - `git diff --exit-code src/app.d.ts` succeeds (file untouched)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler/client → /api/og query | untrusted type/artist/title text |
| upstream JSON (Deezer/iTunes/kuwo) → tier resolver | untrusted third-party bodies naming image URLs |
| upstream image bytes → streamed response + edge cache | untrusted body relayed under our origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | S/SSRF | /api/og input | mitigate (tightened) | NO URL parameter accepted — input is text only, encodeURIComponent'd into fixed upstream templates (T-wv8-01 passthrough-only); this phase strictly tightens the old `?c=` posture |
| T-wv8-05 | Tampering (open relay) | per-tier allowlists | mitigate | three fixed https host suffixes (.dzcdn.net / .mzstatic.com / .kuwo.cn), per-tier so one upstream's JSON cannot smuggle another's host; anything else → branded fallback |
| T-09-05 / T-2os-02 / T-wv8-06 | Info disclosure | cache keys | mitigate | BOTH layers keyed via ownOriginCacheKey; upstream URL never a key; cached copy CORS-free with WR-01 re-application (all upstreams keyless — no secret exists to leak) |
| T-wv8-04 | DoS (self/upstream) | tier chain | mitigate | retries=0, per-tier timeouts under one 2.5s deadline, ≤4 subrequests worst case, negative caching of clean misses, max-age=86400 |
| T-og-01 | DoS | empty-query path | mitigate | no query terms → fallback with zero subrequests |
| T-og-02 | DoS (memory) | clone() for cache.put | mitigate | clone only when Content-Length finite and ≤ 3 MB cap; otherwise stream without caching |
| T-30-04 | Tampering (content confusion) | Content-Type | mitigate | validate ^image/ + jpeg/png/webp allowlist; a CDN html error page falls through to the branded fallback, never relayed as the card image |
| T-{30}-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm vitest --run src/routes/api/og/og-endpoint.test.ts` green; `pnpm test` + `pnpm check` + `pnpm build` green.
- Live curl matrix on :5173 per Task 2 criteria. (`pnpm preview` proves workerd clone()/cache mechanics if needed; real TTL/eviction needs the deploy — 30-06.)
</verification>

<success_criteria>
- Endpoint never 500s/redirects, streams a real cover for a known song, falls back branded, caches own-origin, ≤4 subrequests worst case.
</success_criteria>

<output>
Create `.planning/phases/30-carrier-free-share-links-type-artist-title-api-og/30-03-SUMMARY.md` when done.
</output>
