---
phase: 37-enrich-imported-device-songs-lyrics-cover-and-up-next-for-lo
plan: 04
type: execute
wave: 4
depends_on: ["37-03"]
files_modified:
  - android/app/build/outputs/apk/debug/app-debug.apk
autonomous: false
requirements: [ENRICH-02, ENRICH-04]

must_haves:
  truths:
    - "A debug APK carrying the phase-37 code exists and is installed on the emulator or a device"
    - "A human has reported, per item, whether a real MediaStore-imported file's embedded FrontCover shows in the hero and on the lock screen, and whether the lock screen repaints from the data: artwork"
    - "Neither device-only item is recorded as verified without that report"
  artifacts:
    - path: "android/app/build/outputs/apk/debug/app-debug.apk"
      provides: "installable debug build of main at the phase-37 head"
  key_links:
    - from: "37-04-SUMMARY.md"
      to: "the two [UNVERIFIED-SANDBOX] items in 37-RESEARCH.md"
      via: "per-item PASS/FAIL with device model + Android version"
      pattern: "UNVERIFIED-SANDBOX|PASS|FAIL"
---

<objective>
Close the two items the sandbox cannot: (1) a real Android MediaStore file's embedded FrontCover surviving `getPictures()` and painting the hero + nowbar, and (2) a `data:` artwork actually repainting the Android lock screen through `@jofr/capacitor-media-session`. Build and install the APK automatically; only the observation is human.

Purpose: RESEARCH verified the picture read against synthetic node fixtures and read the plugin's `;base64,` branch from a source quote — neither is device evidence. This phase must not claim them.
Output: installed debug APK, a filled-in checklist in the SUMMARY, and STATE.md's pending-human table updated.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-enrich-imported-device-songs-lyrics-cover-and-up-next-for-lo/37-RESEARCH.md
@.planning/phases/37-enrich-imported-device-songs-lyrics-cover-and-up-next-for-lo/37-03-SUMMARY.md
@.planning/phases/36-*/36-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the debug APK and install it on the emulator</name>
  <files>android/app/build/outputs/apk/debug/app-debug.apk</files>
  <action>
    Build with the JDK the toolchain needs (MEMORY `apk-build-needs-jdk21`): `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home pnpm apk`. Record the APK byte size and confirm `unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -i taglib` lists the `taglib-web.*.wasm` asset (the embedded-tag read depends on it shipping).

    Install if a target is up: `adb devices`; if `emulator-5554` (Pixel_3a_API_34, MEMORY `apk-debug-via-emulator-cdp`) or a device is listed, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`. Push one fixture with KNOWN embedded art + LRC so item 1 has a deterministic input: produce it in node with the repo's own codec — a short script that reads `src/lib/services/__fixtures__/tiny.mp3`, runs `writeAudioTags(bytes, { title: 'Phase37 Probe', artist: 'Probe Artist', lyrics: '[00:00.50]Line one\n[00:03.00]Line two' }, { data: <a real small JPEG or PNG, e.g. static/ favicon rendered to PNG or any 200x200 PNG generated via canvas-less bytes>, mimeType: 'image/png' })` and writes `/tmp/…/phase37-probe.mp3`; then `adb push … /sdcard/Music/phase37-probe.mp3` and trigger a MediaStore scan (`adb shell am broadcast -a android.intent.action.MEDIA_SCANNER_SCAN_FILE -d file:///sdcard/Music/phase37-probe.mp3` or a reboot of the media scanner). A visibly distinctive picture (solid magenta square) makes the lock-screen check unambiguous. Do NOT push to `origin/main` — Pages auto-deploys production on push (MEMORY `openmusic-pushes-autodeploy-live`).

    If no adb target is available, stop after the build, say so plainly, and leave the checkpoint for the human to install manually.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test -s android/app/build/outputs/apk/debug/app-debug.apk && unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -qi "taglib.*\.wasm"</automated>
  </verify>
  <done>APK exists, contains the taglib wasm asset, size recorded; installed + probe file pushed and indexed when a target exists (or explicitly noted as not available).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device checklist — embedded cover in the hero and on the lock screen, name-based fallbacks, online Up-Next</name>
  <files>none — observation only; results are recorded by Task 3</files>
  <action>Install the Task 1 APK on the emulator or a physical Android device, run checklist steps 1-7 in <how-to-verify> exactly as written (airplane mode ON for steps 1-4 so any cover seen is provably the embedded one), and report `approved` or the outcome tokens (CARD-ICON / ART-MISS / UPNEXT-LIST / OOM) with device model + Android version. No source or config change is permitted in this task under any outcome — a failure is recorded by Task 3 and routed to a follow-up, never patched blind here.</action>
  <what-built>Phase 37 on device: an imported (`device:`) file now reads its own embedded FrontCover + LRC on first play (one wasm decode, memoised), paints the cover on the hero/nowbar and hands it to the OS media card as a `data:` URL, falls back to name-based lyrics/cover only when the file carries neither, and generates online similar-song Up-Next like a streamed track.</what-built>
  <how-to-verify>
    Install `android/app/build/outputs/apk/debug/app-debug.apk` (`adb install -r …`) on a device or the API 34 emulator. Settings → Library → Import from device (Phase 34 flow) so `phase37-probe.mp3` (or any of your own tagged files) appears under Library → Downloads. Then, with AIRPLANE MODE ON (so any cover you see is provably the embedded one):

    1. Tap the probe song. EXPECT within ~2 s: the nowbar and the expanded hero show the magenta probe picture (not a gradient, not the app icon). Lyrics pane shows "Line one" / "Line two" scrolling in time.
    2. Lock the screen. EXPECT the lock-screen media card shows the SAME magenta picture. This is `[UNVERIFIED-SANDBOX]` item 2 — a `data:` artwork through `@jofr/capacitor-media-session`. If the card shows the OpenMusic icon instead, reply `CARD-ICON`.
    3. Settings → Activity log. EXPECT an `enrich.local` line for the probe uid with `lrc: true, art: true`.
    4. Tap a DIFFERENT imported file that you know has embedded art (a real ripped MP3/M4A/FLAC, not made by this app). EXPECT its own cover in the hero + on the lock screen. This is `[UNVERIFIED-SANDBOX]` item 1 — a real MediaStore file through `getPictures()`. If the hero stays a gradient while Activity log says `art: false`, reply `ART-MISS` with the file's container (mp3/m4a/flac) and the ripper if known.
    5. Turn airplane mode OFF. Tap an imported file with NO embedded art/lyrics (or an untagged file). EXPECT: after a few seconds a cover appears (Deezer/iTunes/CN by name) and lyrics appear if any CN source has them; the Up-Next list below the player fills with online songs of a similar style (not the rest of your downloads list). If Up-Next shows only your downloads, reply `UPNEXT-LIST`.
    6. Play the probe song again. EXPECT no visible re-decode delay; Activity log shows a second `enrich.local` line for that uid with `cached: true` (the memo answered — no second wasm decode).
    7. Background stress: with a large imported library (100+ files), scroll Library → Downloads quickly and start/stop 10 songs in a row. EXPECT no freeze, no app kill. If the app dies, reply `OOM` with the device model + RAM.
  </how-to-verify>
  <verify>
    <automated>MISSING — device-only; the automated counterpart is Task 3's grep of the per-item table in 37-04-SUMMARY.md and the `37-04 checkpoint status` block in STATE.md</automated>
  </verify>
  <done>Human reply received with `approved` or explicit outcome tokens + device model + Android version; `git diff --quiet -- src android` (nothing changed by this task).</done>
  <resume-signal>Reply `approved`, or one or more of `CARD-ICON` / `ART-MISS <container>` / `UPNEXT-LIST` / `OOM`, ALWAYS with device model + Android version.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record results; never claim what was not observed</name>
  <files>.planning/STATE.md</files>
  <action>
    Write the per-item outcome into `37-04-SUMMARY.md` as a table: item, expected, observed, device + Android version, PASS/FAIL/NOT-RUN. Map the two `[UNVERIFIED-SANDBOX]` items from RESEARCH explicitly (item 1 = step 4, item 2 = step 2). Add a `### 37-04 checkpoint status` block to `.planning/STATE.md` in the same shape as the `36-05` block (pending-human table with the reply tokens) if the human has NOT yet replied; replace it with the outcomes once they have.

    On `CARD-ICON`: do NOT "fix" by writing the `data:` URL into the cover cache or by fetching anything natively — record it, note that `/api/og` remains the card's native fallback path (RESEARCH Q3 "Also worth knowing"), and open a follow-up quick task to inspect `native-media-session.ts`'s `artwork[0]` handling with the plugin's `;base64,` branch. On `ART-MISS`: record the container; the likely cause is an ID3v2.4/odd-encoding APIC (RESEARCH A3) — a re-research trigger, not a code change here. On `UPNEXT-LIST`: check `settings.upnextPerContext` on that device first (a user override to `same-list` is expected behaviour, not a bug).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -q "UNVERIFIED-SANDBOX" .planning/phases/37-enrich-imported-device-songs-lyrics-cover-and-up-next-for-lo/37-04-SUMMARY.md && grep -q "37-04 checkpoint status" .planning/STATE.md</automated>
  </verify>
  <done>SUMMARY carries the per-item table with device info; STATE.md carries the checkpoint block; no item is marked PASS without a human observation behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS `data:` artwork → Capacitor bridge → `@jofr/capacitor-media-session` `urlToBitmap` | The one native seam this phase changes the input shape of |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | DoS | native plugin crash on artwork (MEMORY `media-session-artwork-native-crash`) | mitigate | only `data:image/*;base64` or `/favicon.svg` ever reach the plugin from this path (both are network-free branches); step 2 + step 7 observe it on device |
| T-37-03 | DoS | WebView OOM from repeated decodes on a large library | mitigate | memo + `TAG_MAX_BYTES`; step 7 observes it |
| T-37-SC | Tampering | npm installs | accept | none this phase; APK build uses the pinned toolchain |
</threat_model>

<verification>
- APK built from the phase-37 head, wasm asset present
- Human checklist steps 1-7 run; outcomes recorded per item
- STATE.md updated
</verification>

<success_criteria>
- Both `[UNVERIFIED-SANDBOX]` items have a recorded human observation (or are explicitly still pending — never silently assumed)
- No production push occurred during the phase
</success_criteria>

<output>
Create `.planning/phases/37-enrich-imported-device-songs-lyrics-cover-and-up-next-for-lo/37-04-SUMMARY.md` when done
</output>
