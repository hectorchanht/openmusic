---
phase: 29-download-ux-folder-control
plan: 29-03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - src/lib/services/download-track.ts
  - src/lib/services/download-track.test.ts
  - src/lib/services/blob-store.ts
  - src/lib/services/blob-store.test.ts
autonomous: true
requirements: [DL-FILE-01, DL-BUG-01, DL-STATE-01]

must_haves:
  truths:
    - "One shared downloadTrack(track) orchestrates a single-song download: resolve→addDownload→fetch→(persist)→save"
    - "downloadTrack drives library.beginDownload/endDownload and NEVER mutates player state"
    - "A failed download returns a sentinel — never window.open — and keeps the song in the Library list"
    - "blobStore.put accepts an optional human filename threaded to the native public write"
  artifacts:
    - path: "src/lib/services/download-track.ts"
      provides: "downloadTrack orchestration (isolation-safe, never-throws, persist flag)"
      exports: ["downloadTrack"]
      min_lines: 45
    - path: "src/lib/services/download-track.test.ts"
      provides: "isolation + no-window.open + begin/end + persist coverage"
      min_lines: 70
    - path: "src/lib/services/blob-store.ts"
      provides: "put(uid, blob, filename?) threading filename to the native bridge"
      contains: "filename"
  key_links:
    - from: "src/lib/services/download-track.ts"
      to: "blobStore.put"
      via: "put(uid, blob, filename)"
      pattern: "blobStore\\.put\\("
    - from: "src/lib/services/download-track.ts"
      to: "library.beginDownload / endDownload"
      via: "bracketed begin/finally-end"
      pattern: "beginDownload"
---

<objective>
Extract the single-song download flow (today inlined in TrackMenu.doDownload) into one shared,
node-testable service `downloadTrack`, and give `blobStore.put` a filename argument so the human
filename reaches the native public write. This is the interface layer that 29-04 (UI) and 29-05
(native) consume.

Purpose: DL-FILE-01/DL-BUG-01 need ONE tested initiation path so the format never drifts and the
`window.open` bug can never come back; DL-STATE-01 needs begin/endDownload bracketed around it; the
DOWNLOAD ISOLATION CONTRACT (D-18/pzs-04) must be preserved verbatim. Only ONE caller of
`blobStore.put` exists today (TrackMenu L202), so adding the filename param is safe (Pitfall 3).
Output: `download-track.ts` (+ test) and the `put(uid, blob, filename?)` signature.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-download-ux-folder-control/29-CONTEXT.md
@.planning/phases/29-download-ux-folder-control/29-RESEARCH.md
@.planning/phases/29-download-ux-folder-control/29-VALIDATION.md

<interfaces>
<!-- CONSUMES 29-01 (download-filename, download-save) + 29-02 (library.downloading). -->
From src/lib/services/download-filename.ts:
  extFromAudioUrl(audioUrl: string | null): string;
  buildDownloadFilename(artist: string, title: string, ext: string): string;
From src/lib/services/download-save.ts:
  saveBlobToDisk(blob: Blob, filename: string, doc?: Document): boolean;
From src/lib/stores/library.svelte.ts:
  library.beginDownload(uid); library.endDownload(uid); library.addDownload(t); library.isDownloaded(uid);
From src/lib/stores/names.svelte.ts:
  names.dnArtist(text): string;  names.dnTitle(text): string;   // SYNCHRONOUS cached-or-raw (D-07)

<!-- CREATES: -->
From src/lib/services/download-track.ts (NEW):
  export type DownloadResult = 'saved' | 'no-audio' | 'failed';
  export function downloadTrack(track: Track, opts?: { persist?: boolean }): Promise<DownloadResult>;
    // persist defaults TRUE. persist:false (album path) skips blobStore.put — matches album's
    // current behavior (no offline blob / no native public copy). NEVER throws, NEVER calls
    // window.open, NEVER touches player.current/playGen/the shared <audio>/lyrics (D-18).

From src/lib/services/blob-store.ts (CHANGED):
  export function put(uid: string, blob: Blob, filename?: string): Promise<boolean>;
    // when filename is present, nativePut passes it to the bridge instead of nativeFileName(uid);
    // when absent, falls back to nativeFileName(uid) = `<uid>.mp3` (album/legacy path).
</interfaces>

@src/lib/components/TrackMenu.svelte
@src/lib/services/blob-store.ts
@src/lib/services/blob-store.test.ts
@src/lib/stores/names.svelte.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: blobStore.put gains an optional filename threaded to the native bridge</name>
  <files>src/lib/services/blob-store.ts, src/lib/services/blob-store.test.ts</files>
  <read_first>
    - src/lib/services/blob-store.ts lines 63-66 (`nativeFileName`), 93-116 (`nativePut` — the `saveToMusic({ fileName: nativeFileName(uid), sourcePath })` call at L110), 187-201 (the exported `put`).
    - src/lib/services/blob-store.test.ts lines 130-187 — the native-put describe block whose saveToMusic/getUri assertions this task extends.
  </read_first>
  <behavior>
    - put('netease-1', blob, 'Artist - Song.mp3') on native → the bridge is called with fileName === 'Artist - Song.mp3' (NOT '<uid>.mp3').
    - put('netease-1', blob) with no filename on native → the bridge is called with nativeFileName('netease-1') (the `<uid>.mp3` fallback), preserving the album/legacy path.
    - The app-private write (capacitor-blob-writer) and the WR-01 best-effort/never-reject posture are unchanged; put still returns true when the public copy fails.
  </behavior>
  <action>
    Change the exported signature to `put(uid, blob, filename?: string)` and thread `filename` into
    `nativePut(uid, blob, filename?)`. In nativePut, pass `filename ?? nativeFileName(uid)` as the
    bridge `fileName`. Keep `nativeFileName(uid)` as the fallback for callers that omit it (D-06:
    the public native filename becomes the human name ONLY when supplied). The web (IDB) branch
    ignores `filename` (the browser anchor names the file). Preserve every never-throws sentinel and
    the WR-01/WR-02 comments; add a DL-FILE-01 comment on the new param. Extend blob-store.test.ts
    with the two behavior cases (filename passed through; fallback when absent).
  </action>
  <verify>
    <automated>pnpm test -- blob-store</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- blob-store` passes including the new "filename threaded to saveToMusic" + "falls back to nativeFileName when absent" cases (29-VALIDATION DL-FILE-01 native row).
    - `put` signature is `(uid, blob, filename?)`; the web-branch and every existing native never-throw test still passes unchanged.
  </acceptance_criteria>
  <done>put threads a human filename to the native public write; all blob-store tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: download-track.ts — shared, isolation-safe single-song download</name>
  <files>src/lib/services/download-track.ts, src/lib/services/download-track.test.ts</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte lines 147-235 — the CURRENT doDownload (the reuse-current-quality logic L166-189, the RAW-fetch note L193-196, blobStore.put L202, filename build L203-204, and the window.open catch L231-234 that this replaces). Port the reuse + isolation logic EXACTLY.
    - src/lib/services/blob-store.test.ts lines 13-57 — the vi.mock idiom (Capacitor, filesystem, media-store) to reuse when mocking dependencies in download-track.test.ts.
  </read_first>
  <behavior>
    - downloadTrack(currentTrack) reuses player.current's already-resolved URL when uid matches AND currentQualityMeets (no second resolve — pzs-04); for a non-current track it re-resolves at settings.downloadQuality on a COPY.
    - After a successful fetch+save → returns 'saved', library.isDownloaded(uid) is true, blobStore.put was called with the buildDownloadFilename(names.dnArtist, names.dnTitle, extFromAudioUrl) name.
    - When the resolved track has no audioUrl → returns 'no-audio', no fetch, endDownload ran.
    - When fetch rejects OR saveBlobToDisk returns false → returns 'failed', window.open (spied global) was NEVER called, the track stays in library.downloads (addDownload already ran).
    - opts.persist === false → blobStore.put is NOT called (album parity), but saveBlobToDisk + addDownload + begin/end still run.
    - beginDownload(uid) ran before the await and endDownload(uid) ran in finally on every path (saved/no-audio/failed); player.current / playGen were never assigned (isolation — assert the mocked player is untouched).
  </behavior>
  <action>
    Write `downloadTrack(track, opts)` porting the doDownload body: begin with
    `library.beginDownload(track.uid)` in a try whose `finally` calls `library.endDownload`. Apply
    the reuse-current-quality branch reading `player.current` (READ ONLY — the DOWNLOAD ISOLATION
    CONTRACT: never assign player.current, never bump playGen, never touch the shared &lt;audio&gt;
    or lyrics; operate on COPIES + own fetch, per D-18/quick-260625-pzs-04) else re-resolve via
    `ensureTrackDetails({ ...track, detailsLoaded:false, audioUrl:null, lrc:null }, undefined,
    settings.downloadQuality).catch(() => track)`. `library.addDownload(r)`; if `!r.audioUrl` return
    'no-audio'. RAW `fetch(r.audioUrl)` (not apiFetch — absolute CDN URL; keep the audit comment) →
    blob. Build `ext = extFromAudioUrl(r.audioUrl)` + `filename = buildDownloadFilename(names.dnArtist(
    r.artist), names.dnTitle(r.title), ext)`. If `opts?.persist !== false` await
    `blobStore.put(r.uid, blob, filename)`. Call `saveBlobToDisk(blob, filename)`; return 'saved'
    when it returns true else 'failed'. Wrap in try/catch → return 'failed' (NEVER window.open — the
    DL-BUG-01 guarantee; the caller shows toast.downloadFailedKeptInLibrary). Do NOT import `t` or
    `toast` (i18n reads $state and would break the node Vitest project — the caller localizes). Do
    NOT import the player for anything but reading `.current`. Reproduce the `currentQualityMeets`
    helper here (or import it if exported). download-track.test.ts mocks player/library/settings/
    names/blob-store/download-save/fetch and asserts every behavior case above.
  </action>
  <verify>
    <automated>pnpm test -- download-track</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- download-track` passes all behavior cases (saved / no-audio / failed / persist:false / begin-end bracket / player-untouched / window.open-never-called) — 29-VALIDATION DL-BUG-01 spy seam + DL-STATE-01 isolation.
    - download-track.ts imports neither `$lib/i18n` nor `$lib/stores/toast` (grep -c = 0 after comment filtering) so the node test compiles.
    - A test asserts the mocked player object was not mutated (no assignment to `.current` / no `playGen` bump) during a download.
  </acceptance_criteria>
  <done>A single tested initiation path exists; isolation + never-throw + no-navigation proven in node.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN audio stream → app | untrusted response bytes fetched to a Blob |
| audioUrl (provider) → failure path | a failed save must not navigate to the raw stream URL |
| download flow → player state | isolation seam: download must not cross into playback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Elevation | downloadTrack catch path | mitigate | return 'failed'; NO window.open (raw CDN URL never opened) — DL-BUG-01 |
| T-29-03-02 | Tampering | player state during download | mitigate | read-only `player.current`; never assign current/playGen/audio/lyrics (D-18); asserted in test |
| T-29-03-03 | Denial of Service | full-file blob fetch | accept | RAW fetch of one resolved stream (bypasses the apiFetch governor by design — absolute CDN URL); bounded per user action |
| T-29-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (29-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm test -- download-track blob-store` green.
- `pnpm check` clean.
</verification>

<success_criteria>
- `downloadTrack` is the single tested initiation path (isolation-safe, never-throws, never-navigates, persist flag).
- `blobStore.put(uid, blob, filename?)` threads the human filename to the native bridge with a `<uid>.mp3` fallback.
</success_criteria>

<output>
Create `.planning/phases/29-download-ux-folder-control/29-03-SUMMARY.md` when done.
</output>
