---
phase: 29-download-ux-folder-control
plan: 29-04
type: execute
wave: 3
depends_on: [29-03, 29-02]
files_modified:
  - src/lib/components/DownloadControl.svelte
  - src/lib/components/TrackMenu.svelte
  - src/routes/(app)/album/[name]/+page.svelte
  - src/routes/(app)/library/+page.svelte
  - src/lib/components/CompactRow.svelte
autonomous: true
requirements: [DL-FILE-01, DL-BUG-01, DL-STATE-01]

must_haves:
  truths:
    - "Tapping Download in the ⋮ menu, on a library row, or on an album row runs the shared downloadTrack path"
    - "A failed download shows a toast and NEVER opens a media page (window.open + showSaveFilePicker are gone)"
    - "Each row's download control shows idle → spinner (downloading.has(uid)) → greyed disabled Downloaded (isDownloaded)"
    - "One song downloading never spins another song's control"
  artifacts:
    - path: "src/lib/components/DownloadControl.svelte"
      provides: "Shared tri-state download control reading library.downloading + isDownloaded"
      min_lines: 40
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "doDownload delegates to downloadTrack; Download row is the tri-state control; no window.open / showSaveFilePicker"
      contains: "downloadTrack"
    - path: "src/routes/(app)/album/[name]/+page.svelte"
      provides: "downloadAlbum + per-row download via downloadTrack(persist:false); no window.open"
      contains: "downloadTrack"
  key_links:
    - from: "src/lib/components/DownloadControl.svelte"
      to: "library.downloading / library.isDownloaded"
      via: "reactive reads"
      pattern: "library\\.(downloading|isDownloaded)"
    - from: "src/lib/components/TrackMenu.svelte"
      to: "download-track"
      via: "import downloadTrack"
      pattern: "downloadTrack"
---

<objective>
Wire the shared `downloadTrack` service into every download-initiation surface and roll out the
tri-state download control. Delete the `window.open` + `showSaveFilePicker` bug paths in TrackMenu
and the album page. Render idle→spinner→"Downloaded" on the library rows, album rows and the ⋮
menu Download row, and a passive "Downloaded" badge on the home/search CompactRow.

Purpose: DL-BUG-01 (remove the two `window.open` fallbacks + `showSaveFilePicker`), DL-FILE-01
(both save sites now flow through the translated-filename path), DL-STATE-01 (the visible per-song
tri-state on every row, D-11). The menu Download row must reflect `library.downloading` regardless
of whether the menu stays open (D-12).
Output: `DownloadControl.svelte` + rewired TrackMenu / album / library / CompactRow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-download-ux-folder-control/29-CONTEXT.md
@.planning/phases/29-download-ux-folder-control/29-RESEARCH.md

<interfaces>
<!-- CONSUMES 29-03 (downloadTrack), 29-02 (library.downloading, menu.downloaded, toast key). -->
From src/lib/services/download-track.ts:
  type DownloadResult = 'saved' | 'no-audio' | 'failed';
  downloadTrack(track: Track, opts?: { persist?: boolean }): Promise<DownloadResult>;
From src/lib/stores/library.svelte.ts:
  library.downloading.has(uid): boolean;  library.isDownloaded(uid): boolean;
From src/lib/i18n:  t('menu.downloaded'), t('toast.downloadFailedKeptInLibrary'),
  t('toast.preparingDownload'), t('toast.downloaded'), t('toast.noAudio')

<!-- CREATES: -->
DownloadControl.svelte props:
  { track: Track; persist?: boolean }   // persist forwarded to downloadTrack (album rows pass false)
  // renders three states from library.downloading.has(track.uid) + library.isDownloaded(track.uid);
  // idle onclick → toast(preparing) → downloadTrack → map DownloadResult to a toast.
</interfaces>

@src/lib/components/TrackMenu.svelte
@src/routes/(app)/album/[name]/+page.svelte
@src/routes/(app)/library/+page.svelte
@src/lib/components/CompactRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: DownloadControl.svelte + TrackMenu rewire (delete the bug paths)</name>
  <files>src/lib/components/DownloadControl.svelte, src/lib/components/TrackMenu.svelte</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte lines 56-76 (the gated()/inFlight idiom), 147-235 (doDownload — the whole body being replaced), 384-387 (the Download menu row currently driven by inFlight.has('download')), 466-472 (the .row-spinner CSS to reuse).
    - src/lib/services/download-track.ts (from 29-03) — the DownloadResult contract to map to toasts.
    - src/lib/stores/library.svelte.ts (from 29-02) — downloading/isDownloaded reads.
  </read_first>
  <action>
    Create `DownloadControl.svelte`: a &lt;button&gt; (or span when downloaded) that reads
    `library.isDownloaded(track.uid)` and `library.downloading.has(track.uid)`. Downloaded → greyed,
    `disabled`, Check/Download glyph + `t('menu.downloaded')` label. Downloading → the existing
    `.row-spinner`, `disabled`, `aria-busy`. Idle → Download icon, enabled; onclick shows
    `t('toast.preparingDownload')`, awaits `downloadTrack(track, { persist })`, and maps the result:
    'no-audio' → `t('toast.noAudio')`, 'failed' → `t('toast.downloadFailedKeptInLibrary')`, 'saved'
    → `t('toast.downloaded')`. Accept a `persist` prop (default true) forwarded to downloadTrack.
    Reuse tapBounce + the neutral spinner CSS (copy the reduced-motion rules). Then in TrackMenu:
    replace the entire `doDownload` body with a thin delegate to `downloadTrack(resolved)` (keep the
    gated() stub-resolve wrapper for the menu's stub case) OR swap the Download menu row to render
    `<DownloadControl track={track} />` directly (D-12 — reflects library.downloading whether or not
    the menu is open); DELETE the showSaveFilePicker block (old L209-224) and BOTH the
    `window.open(r.audioUrl,'_blank')` + `toast.openedAudio` catch (old L231-234). Remove now-unused
    imports (blobStore direct use, the inline mime/ext map). Keep the DOWNLOAD ISOLATION CONTRACT
    comment pointing at downloadTrack as the isolation-preserving path.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "window.open\|showSaveFilePicker" src/lib/components/TrackMenu.svelte` returns NOTHING (DL-BUG-01; 29-VALIDATION DL-BUG-01 row).
    - TrackMenu imports `downloadTrack` from `$lib/services/download-track` and no longer inlines the fetch/blob/filename/save logic.
    - DownloadControl.svelte renders all three states off `library.downloading.has` + `library.isDownloaded`; `pnpm check` clean.
  </acceptance_criteria>
  <done>The ⋮ Download row uses the shared control/path; both web bug fallbacks are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Album page — downloadAlbum + per-row control via downloadTrack(persist:false)</name>
  <files>src/routes/(app)/album/[name]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/album/[name]/+page.svelte lines 355-410 (downloadAlbum — the per-track loop with the inline fetch/anchor + the `window.open(full.audioUrl)` fallback L397-401 being removed), 604-616 (the track row template where the per-row control mounts), 597 (the album-level bulk Download button).
    - src/lib/components/DownloadControl.svelte (Task 1) — the component to mount on rows.
    - src/lib/services/download-track.ts — the persist:false album contract (RESEARCH Open Q2: album stays out of the native public folder; only the filename helper + window.open removal apply).
  </read_first>
  <action>
    Rewrite `downloadAlbum` to loop `await downloadTrack(tr, { persist: false })` per track (keep the
    ~250ms stagger + the busyAction guard + the aggregate final toast based on how many returned
    'saved'). DELETE the inline fetch/blob/anchor build AND the `window.open(full.audioUrl,'_blank')`
    fallback (DL-BUG-01) — downloadTrack owns save now. Mount `<DownloadControl track={track}
    persist={false} />` on each album track row (D-11 album-row rollout). Add a comment noting the
    persist:false limitation (album downloads intentionally do NOT land in the native public folder
    this phase — RESEARCH Open Q2 / planning-context; the filename + bug-fix DO apply). Remove
    now-unused imports.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "window.open" "src/routes/(app)/album/[name]/+page.svelte"` returns NOTHING (DL-BUG-01).
    - downloadAlbum calls `downloadTrack(..., { persist: false })`; each album row renders a `DownloadControl`.
    - `pnpm check` clean.
  </acceptance_criteria>
  <done>Album save path is deduplicated onto downloadTrack; its window.open fallback is gone; rows show the tri-state.</done>
</task>

<task type="auto">
  <name>Task 3: Library rows tri-state + CompactRow passive badge</name>
  <files>src/routes/(app)/library/+page.svelte, src/lib/components/CompactRow.svelte</files>
  <read_first>
    - src/routes/(app)/library/+page.svelte lines 218-278 (the row template shared across liked/playlists/downloads/history tabs) — where the DownloadControl mounts on track rows.
    - src/lib/components/CompactRow.svelte lines 89-124 (the track-variant row markup) — where the passive badge sits; note the ⋮ button stays the initiation surface for home/search (RESEARCH Open Q1).
    - src/lib/components/DownloadControl.svelte (Task 1) + src/lib/stores/library.svelte.ts (isDownloaded).
  </read_first>
  <action>
    Add `<DownloadControl track={track} />` to the library track rows (liked / playlists / history /
    downloads tabs — a per-row initiation + state affordance; on the downloads tab it renders the
    greyed "Downloaded" state, which is correct). Keep the existing swipe/longpress/play wiring
    intact (the control is its own tap target beside them). In CompactRow, add a PASSIVE indicator
    (a small greyed Download/Check glyph, `aria-label` = `t('menu.downloaded')`, NO onclick — NOT a
    tap target) shown only when `library.isDownloaded(track.uid)`; initiation on home/search stays in
    the ⋮ menu (D-11 / RESEARCH Open Q1 recommendation). Import `library` into CompactRow for the
    read. Do not alter the CompactRow play/⋮/longpress behavior.
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - Library track rows render `DownloadControl`; CompactRow shows a passive downloaded indicator gated on `library.isDownloaded(track.uid)` with no new tap target.
    - CompactRow's existing play / ⋮ / longpress handlers are unchanged (grep confirms onplay/onrequestmenu still wired).
    - `pnpm check` clean.
  </acceptance_criteria>
  <done>Tri-state control is on library + album + menu rows; CompactRow shows a passive downloaded badge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → downloadTrack | initiation surfaces call the isolation-safe shared path only |
| failure path → navigation | a failed save must render a toast, never a media page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Elevation | TrackMenu / album catch | mitigate | window.open + showSaveFilePicker deleted; failure → toast.downloadFailedKeptInLibrary (DL-BUG-01) |
| T-29-04-02 | Spoofing | per-uid control state | mitigate | control keys on track.uid via library.downloading/isDownloaded — one song's state cannot spin another (DL-STATE-01) |
| T-29-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (29-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm check` clean; `pnpm test` full suite green (no regression in TrackMenu/album/library consumers).
- `grep -rn "window.open\|showSaveFilePicker" src/lib/components/TrackMenu.svelte "src/routes/(app)/album/[name]/+page.svelte"` → empty.
- Manual UAT (device / browser, per 29-VALIDATION DL-BUG-01 iOS row): download a song → file saves, no media page opens; a forced save failure shows a toast and keeps the song in Library.
</verification>

<success_criteria>
- Every download-initiation surface flows through downloadTrack; no `window.open` / `showSaveFilePicker` remain.
- The tri-state control renders idle/spinner/Downloaded per uid on library + album + menu rows; CompactRow shows a passive badge.
</success_criteria>

<output>
Create `.planning/phases/29-download-ux-folder-control/29-04-SUMMARY.md` when done.
</output>
