---
phase: 29-download-ux-folder-control
plan: 29-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/services/download-filename.ts
  - src/lib/services/download-filename.test.ts
  - src/lib/services/download-save.ts
  - src/lib/services/download-save.test.ts
autonomous: true
requirements: [DL-FILE-01, DL-BUG-01]

must_haves:
  truths:
    - "A download filename is built as `{artist} - {song}.{ext}` with unsafe path chars stripped"
    - "The audio extension is derived from the resolved audio URL, defaulting to mp3"
    - "A blob is saved to disk via an anchor download — no window.open, no showSaveFilePicker"
  artifacts:
    - path: "src/lib/services/download-filename.ts"
      provides: "Pure buildDownloadFilename + extFromAudioUrl helpers"
      exports: ["buildDownloadFilename", "extFromAudioUrl"]
      min_lines: 12
    - path: "src/lib/services/download-filename.test.ts"
      provides: "Translated/raw/sanitize/extension/default coverage (DL-FILE-01)"
      min_lines: 30
    - path: "src/lib/services/download-save.ts"
      provides: "saveBlobToDisk anchor helper with no window.open / showSaveFilePicker"
      exports: ["saveBlobToDisk"]
      min_lines: 15
    - path: "src/lib/services/download-save.test.ts"
      provides: "Anchor-save happy path + failure never touches window.open (DL-BUG-01 seam)"
      min_lines: 25
  key_links:
    - from: "src/lib/services/download-filename.ts"
      to: "sanitize regex"
      via: "String.replace"
      pattern: "replace\\(/\\[/"
---

<objective>
Create the two pure download-service helpers that every save site (TrackMenu, album, native
blob-store, migration) will share, and their node tests. These are the Wave 0 test seams named in
29-VALIDATION.md: the translated-filename builder (DL-FILE-01) and the anchor-save helper that
replaces the `window.open` / `showSaveFilePicker` bug paths (DL-BUG-01 testable seam).

Purpose: Consolidate the `{artist} - {title}.{ext}` shape that has drifted across TrackMenu (L204)
and the album page (L393) into ONE pure helper (D-08), and give DL-BUG-01 a node-testable seam
(the Vitest project has no jsdom, so the fix is proven at a service seam, not a component test).
Output: `download-filename.ts` + `download-save.ts` + co-located tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-download-ux-folder-control/29-CONTEXT.md
@.planning/phases/29-download-ux-folder-control/29-RESEARCH.md
@.planning/phases/29-download-ux-folder-control/29-VALIDATION.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (29-03 download-track, 29-05 native, 29-06
     migration) import these directly — no exploration needed. -->

From src/lib/services/download-filename.ts (NEW):
  export function extFromAudioUrl(audioUrl: string | null): string;
    // query-stripped, case-insensitive match of /\.(mp3|flac|m4a|aac|ogg|wav)$/i; returns the
    // lowercased extension without the dot; unknown/null → 'mp3'.
  export function buildDownloadFilename(artist: string, title: string, ext: string): string;
    // returns `${artist} - ${title}.${ext}`.replace(/[/\\?%*:|"<>]/g, '_');
    // artist/title MUST already be run through names.dn* by the caller (D-05/D-07 raw fallback).

From src/lib/services/download-save.ts (NEW):
  export function saveBlobToDisk(blob: Blob, filename: string, doc?: Document): boolean;
    // creates an <a download={filename} href=URL.createObjectURL(blob)>, clicks it, revokes the
    // URL. Returns true on success, false if the DOM is unavailable or the anchor throws.
    // MUST contain NO window.open and NO showSaveFilePicker (D-02 / DL-BUG-01). `doc` is
    // injectable (default globalThis.document) so the node test drives it with a fake document.
</interfaces>

<!-- Source of the drifted shapes being consolidated (read to match the exact sanitize regex): -->
@src/lib/components/TrackMenu.svelte
@src/routes/(app)/album/[name]/+page.svelte
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: download-filename.ts — translated filename + extension helpers</name>
  <files>src/lib/services/download-filename.ts, src/lib/services/download-filename.test.ts</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte lines 203-204 — the current inline ext regex + `${artist} - ${title}.${ext}` sanitize (source of truth for the exact shape).
    - src/routes/(app)/album/[name]/+page.svelte lines 390-393 — the second (drifted) copy this consolidates.
    - src/lib/services/blob-store.test.ts — the co-located Vitest style (describe/it, node-only, no jsdom) to mirror.
  </read_first>
  <behavior>
    - extFromAudioUrl('https://cdn/x.FLAC?token=1') → 'flac' (query-stripped, lowercased).
    - extFromAudioUrl('https://cdn/x.m4a') → 'm4a'; each of mp3|flac|m4a|aac|ogg|wav resolves.
    - extFromAudioUrl(null) → 'mp3'; extFromAudioUrl('https://cdn/nomatch') → 'mp3'.
    - buildDownloadFilename('G.E.M. 鄧紫棋', '光年之外', 'mp3') → 'G.E.M. 鄧紫棋 - 光年之外.mp3' (CJK preserved — translated names pass through unchanged).
    - buildDownloadFilename('AC/DC', 'T:N:T', 'mp3') → every one of /\\?%*:|"<> replaced with '_' (assert no '/' , ':' , '"' remain).
  </behavior>
  <action>
    Write the two pure functions per the &lt;interfaces&gt; contract. `extFromAudioUrl` splits on
    '?' first, then matches /\.(mp3|flac|m4a|aac|ogg|wav)$/i, returns match[1].toLowerCase() ?? 'mp3'.
    `buildDownloadFilename` composes `${artist} - ${title}.${ext}` and applies the EXACT sanitize
    regex `/[/\\?%*:|"&lt;&gt;]/g` → '_' copied verbatim from TrackMenu L204 (do not invent a new
    char class). No store imports — this file is pure and node-testable (RESEARCH anti-pattern:
    never import `names` here; the caller passes already-translated strings). Tabs, single quotes,
    named exports, house comment density with the DL-FILE-01 decision ref.
  </action>
  <verify>
    <automated>pnpm test -- download-filename</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- download-filename` passes with the six behavior cases above (29-VALIDATION DL-FILE-01 rows: translated/raw/sanitize/each-ext/default-mp3, query-strip/case-insensitive/unknown→mp3).
    - Source contains no import of `$lib/stores/names` (grep -c returns 0 after `grep -v '^\s*//'` filtering).
    - Exports exactly `extFromAudioUrl` and `buildDownloadFilename` (named, no default).
  </acceptance_criteria>
  <done>Both helpers exist, are pure, and their tests are green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: download-save.ts — anchor save seam (no window.open / showSaveFilePicker)</name>
  <files>src/lib/services/download-save.ts, src/lib/services/download-save.test.ts</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte lines 206-234 — the CURRENT save flow (showSaveFilePicker block L209-224 + anchor L225-229 + the window.open catch L232). This helper is the replacement for the anchor half; the showSaveFilePicker + window.open halves are DELETED here (never re-implemented).
    - src/lib/services/blob-store.test.ts lines 60-93 — the vi.stubGlobal shim idiom for driving a fake global in node.
  </read_first>
  <behavior>
    - saveBlobToDisk(blob, 'a - b.mp3', fakeDoc) → calls fakeDoc.createElement('a'), sets anchor.download='a - b.mp3' and anchor.href to a created object URL, calls anchor.click() once, then revokes; returns true.
    - When fakeDoc.createElement throws (or doc is undefined) → returns false and NEVER throws.
    - The module source references neither `window.open` nor `showSaveFilePicker` on ANY path (the DL-BUG-01 guarantee): a failure returns false, it does not navigate.
  </behavior>
  <action>
    Write `saveBlobToDisk(blob, filename, doc = globalThis.document)` per the &lt;interfaces&gt;
    contract. Resolve `URL.createObjectURL`/`URL.revokeObjectURL` from globalThis (guard when
    absent → return false). Wrap the anchor creation + click in try/catch → return false; revoke the
    object URL in a finally. NO showSaveFilePicker branch, NO window.open anywhere — those are the
    bug (D-02/D-09); this seam exists precisely so the failure path is a return value, not a
    navigation. Add the DL-BUG-01 decision-ref comment stating why window.open is intentionally
    absent so a future edit does not re-add it.
  </action>
  <verify>
    <automated>pnpm test -- download-save</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- download-save` passes: happy path drives the fake document (createElement/click/revoke asserted) and returns true; failure path returns false without throwing.
    - A test asserts `saveBlobToDisk.toString()` (or the read source) contains neither 'window.open' nor 'showSaveFilePicker' (29-VALIDATION DL-BUG-01 seam).
    - Exports exactly `saveBlobToDisk` (named).
  </acceptance_criteria>
  <done>The anchor-save seam exists, is node-testable, and provably contains no navigation fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| track metadata / translation → filename | untrusted title/artist strings become a filesystem name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | buildDownloadFilename | mitigate | strip path separators + reserved chars via `/[/\\?%*:\|"<>]/g` → '_' (blocks `../` traversal and MediaStore RELATIVE_PATH escape; no `/` or `\` can survive) |
| T-29-01-02 | Elevation | saveBlobToDisk | mitigate | no `window.open`/`showSaveFilePicker`; a failure returns false, never navigates to an attacker-influenced audioUrl (DL-BUG-01) |
| T-29-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (29-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm test -- download-filename download-save` green.
- `pnpm check` clean (no new type errors from the two new modules).
</verification>

<success_criteria>
- Both pure helpers exist with the exact signatures in `<interfaces>`, node-tested, no store/DOM-navigation coupling.
- The sanitize regex matches TrackMenu's original char class verbatim.
- No `window.open` / `showSaveFilePicker` anywhere in `download-save.ts`.
</success_criteria>

<output>
Create `.planning/phases/29-download-ux-folder-control/29-01-SUMMARY.md` when done.
</output>
