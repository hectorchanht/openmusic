---
phase: 24-offline-app-shell-sharing-seo
plan: 05
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03", "24-04"]
files_modified:
  - src/routes/(app)/+layout.svelte
  - src/routes/(app)/search/+page.svelte
  - src/routes/(app)/charts/top/+page.svelte
  - src/routes/(app)/artist/[name]/+page.svelte
  - src/routes/(app)/album/[name]/+page.svelte
  - src/lib/components/TrackMenu.svelte
files_referenced:
  - src/lib/stores/online.svelte.ts
  - src/lib/services/share.ts
  - src/lib/services/blob-store.ts
  - src/lib/services/downloads-queue.ts
autonomous: false
requirements: [OFFL-02, OFFL-03, SHARE-02]
must_haves:
  truths:
    - "A global offline indicator shows whenever the app is offline (driven by the online store)"
    - "Online-only surfaces (search, charts, artist, album discovery) short-circuit to an inline offline empty-state instead of hanging on a fetch — no stuck loaders, no dead screens"
    - "Library/Downloads stay fully usable offline and are surfaced (promoted), with no forced redirect between tabs"
    - "Share buttons build the new readable /{type}/{slug}-{source}{id} entity link (SHARE-02), with ?play= retained as the optional queue carrier"
    - "A downloaded song still plays end-to-end offline with the SW present (OFFL-02 regression-clear)"
    - "Both pnpm build (Cloudflare) and pnpm build:native (static SPA) succeed — the D-03 hard constraint"
    - "D-09: per-surface inline offline states + global indicator + promote Downloads, with no forced redirect between tabs"
    - "D-10: no stuck loaders / dead screens — short-circuit to an inline offline state before firing a fetch"
  artifacts:
    - path: "src/routes/(app)/+layout.svelte"
      provides: "Global offline indicator + online.init() lifecycle wiring (OFFL-03)"
      contains: "online"
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "Share via entityShareUrl (SHARE-02)"
      contains: "entityShareUrl"
  key_links:
    - from: "online-only surfaces"
      to: "src/lib/stores/online.svelte.ts"
      via: "import { online }; if (!online.isOnline) show inline offline state"
      pattern: "online.isOnline"
    - from: "share buttons"
      to: "src/lib/services/share.ts"
      via: "entityShareUrl(type, track)"
      pattern: "entityShareUrl"
---

<objective>
Complete OFFL-03 (graceful offline degradation, simplest implementation per D-09/D-10): a global offline indicator + per-surface inline offline empty-states that short-circuit before firing a fetch (no stuck loaders/dead screens), with Library/Downloads promoted and NO forced redirect. Complete SHARE-02 wiring: point the share buttons at the new readable `entityShareUrl` (with `?play=` retained as the queue carrier, D-06). Verify OFFL-02 (downloaded-song offline playback) still holds with the SW present. Finally, run the [BLOCKING] dual-adapter build-smoke gate `pnpm build && pnpm build:native` (D-03 hard constraint).

Purpose: Offline UX is graceful and share links are readable; the dual-build constraint is proven green before phase sign-off.
Output: offline indicator + inline states, entityShareUrl wiring, OFFL-02 verification, both builds green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-RESEARCH.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-PATTERNS.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-VALIDATION.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-01-SUMMARY.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-02-SUMMARY.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-03-SUMMARY.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-04-SUMMARY.md

<interfaces>
<!-- Wave-0/Wave-2 primitives this plan wires into the UI. -->
From src/lib/stores/online.svelte.ts (Plan 01):
```typescript
export const online: { isOnline: boolean; init(): () => void };
```

From src/lib/services/share.ts (Plan 02):
```typescript
export function entityShareUrl(type: 'song' | 'album' | 'artist', t: { title: string; artist: string; source: string; songid: string }): string;
export function shareUrl(current: Track, queue?: Track[]): string; // existing ?play= builder — D-06 queue carrier, KEEP
```

Existing OFFL-02 path (verify only, do NOT modify): player.svelte.ts reads blobStore.get(uid) → URL.createObjectURL before CDN (lines 377-451, 1379-1477); downloads-queue buildOfflineQueue + player.handleOffline already wired (RESEARCH lines 61-62).

Existing share call sites to repoint:
- src/lib/components/TrackMenu.svelte line 21 imports shareUrl, line 139 `shareUrl(track, player.queue)` in doShare
- src/routes/(app)/album/[name]/+page.svelte line 575 shareAlbum
- src/routes/(app)/artist/[name]/+page.svelte ~line 341 share button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global offline indicator + online store lifecycle (OFFL-03)</name>
  <files>src/routes/(app)/+layout.svelte</files>
  <read_first>
    - src/routes/(app)/+layout.svelte (the app shell that hosts the nowbar/nav — where the global indicator + online.init() belong)
    - src/lib/stores/online.svelte.ts (Plan 01 — online.isOnline + init() teardown contract)
    - src/lib/stores/toast.svelte.ts and src/lib/i18n (existing i18n + toast patterns; add offline-indicator copy to the i18n dicts if a string is shown)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-RESEARCH.md lines 253-272 (Pattern 4 consumption) and D-09/D-10 in 24-CONTEXT.md (global indicator + don't bloat)
  </read_first>
  <action>
    In `src/routes/(app)/+layout.svelte`, call `online.init()` inside an `onMount` (or `$effect` with the returned teardown) so the online/offline listeners attach once for the app shell and tear down on destroy. Add a small, unobtrusive GLOBAL offline indicator (a thin banner or a pill near the top/nowbar) shown via `{#if !online.isOnline}` with a localized "offline" label — keep it simple ("don't bloat", D-10). Do NOT force any navigation/redirect (D-09 — the user is never yanked between tabs). If a visible string is added, add the i18n key to ALL locale dicts (match the existing 15-locale parity convention used by prior phases). The indicator is purely additive to the existing shell layout.
  </action>
  <verify>
    <automated>pnpm check 2>&1 | grep -i "not defined" || echo ok-no-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `online.init` in src/routes/(app)/+layout.svelte
    - grep finds `online.isOnline` gating the indicator markup in src/routes/(app)/+layout.svelte
    - No `goto(` added that fires on offline (no forced redirect — D-09)
    - pnpm check reports no new type errors
  </acceptance_criteria>
  <done>Global offline indicator shows when offline; online store inits/tears down in the app shell; no forced redirect.</done>
</task>

<task type="auto">
  <name>Task 2: Per-surface inline offline empty-states on online-only surfaces (OFFL-03)</name>
  <files>src/routes/(app)/search/+page.svelte, src/routes/(app)/charts/top/+page.svelte, src/routes/(app)/artist/[name]/+page.svelte, src/routes/(app)/album/[name]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/search/+page.svelte (onMount/search fetch entry — short-circuit before firing searchAll when offline)
    - src/routes/(app)/charts/top/+page.svelte (discovery fetch entry)
    - src/routes/(app)/artist/[name]/+page.svelte and src/routes/(app)/album/[name]/+page.svelte (the searchAll / enrich fetch entries)
    - src/lib/stores/online.svelte.ts (online.isOnline)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-RESEARCH.md lines 62, 272 (short-circuit instead of hanging fetch; pair with existing try/catch) and D-10 (no stuck loaders/dead screens)
  </read_first>
  <action>
    On each online-only surface (search, charts/top, artist, album discovery), guard the data-fetch entry point with `online.isOnline`: when offline, SHORT-CIRCUIT — do NOT enter the loading state / fire `searchAll`/enrich (which would hang or show a stuck spinner). Instead render an inline offline empty-state for that surface: a brief localized "you're offline" message that PROMOTES Downloads (a link/CTA to the Library/Downloads tab, which stays usable). Keep each inline state minimal and consistent ("don't bloat", D-10). Do NOT redirect (D-09). When connectivity returns (`online.isOnline` flips true via the store's event listener), the surface may resume normally on the next user action — no automatic refetch required (keep it simple). Add any new i18n strings to all locale dicts (15-locale parity). Reuse the existing skeleton/empty-state markup idioms already on each page where present.
  </action>
  <verify>
    <automated>pnpm check 2>&1 | grep -i "not defined" || echo ok-no-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `online.isOnline` in EACH of: search/+page.svelte, charts/top/+page.svelte, artist/[name]/+page.svelte, album/[name]/+page.svelte
    - Each online-only fetch entry is guarded so it does NOT fire when offline (short-circuit verified by reading the guard placement, recorded in SUMMARY)
    - An inline offline state promoting Downloads renders on each surface; no `goto`/redirect on offline
    - pnpm check reports no new type errors
  </acceptance_criteria>
  <done>Each online-only surface short-circuits to an inline offline state promoting Downloads; no stuck loaders, no dead screens, no redirect.</done>
</task>

<task type="auto">
  <name>Task 3: Wire share buttons to entityShareUrl + verify OFFL-02 (SHARE-02, OFFL-02)</name>
  <files>src/lib/components/TrackMenu.svelte, src/routes/(app)/album/[name]/+page.svelte, src/routes/(app)/artist/[name]/+page.svelte</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte lines 21, 130-145 (doShare: shareUrl import + call to repoint)
    - src/routes/(app)/album/[name]/+page.svelte line 575 + the shareAlbum handler; src/routes/(app)/artist/[name]/+page.svelte ~line 341 share button
    - src/lib/services/share.ts (entityShareUrl signature from Plan 02; shareUrl retained as D-06 queue carrier)
    - src/lib/services/blob-store.ts + src/lib/services/downloads-queue.ts (OFFL-02 path — verify only, do NOT modify)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-CONTEXT.md D-04/D-06 (readable path is the share target; ?play= layered on top as the optional queue carrier)
  </read_first>
  <action>
    Repoint the share buttons to build the new readable entity link. In TrackMenu.svelte `doShare`, build the URL via `entityShareUrl('song', track)` (track has title/artist/source/songid). Per D-06, KEEP the `?play=` queue payload as an OPTIONAL carrier appended to the readable path: build the entity path with `entityShareUrl`, then append `?play=<encodeShare(track, player.queue)>` (and optionally the cosmetic `t=` is no longer needed since the slug is in the path) so the link both identifies the entity (crawler unfurl) AND restores the queue on open. For the album share button use `entityShareUrl('album', ...)` and for artist `entityShareUrl('artist', ...)` (no `?play=` queue needed for album/artist entity links — those are entity pages, not a now-playing restore). Preserve the existing toast/`navigator.share`/clipboard behavior; only the URL construction changes. Do NOT remove `shareUrl` (the homepage `?play=` form stays as the D-06 carrier helper and for backward-compat).
    VERIFY OFFL-02 (no code change): confirm the existing player offline-blob path still plays a downloaded song with the SW present — run the existing player test and confirm green; record in SUMMARY that the SW never intercepts `blob:` URLs (they bypass the fetch handler entirely, RESEARCH line 240) so OFFL-02 is regression-clear.
  </action>
  <verify>
    <automated>pnpm test src/lib/stores/player.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `entityShareUrl` in TrackMenu.svelte, album/[name]/+page.svelte, and artist/[name]/+page.svelte
    - The song share link includes both the readable `/song/...` path AND a `?play=` queue carrier (D-06) — verified by reading doShare, recorded in SUMMARY
    - `shareUrl` is NOT deleted (still exported in share.ts; D-06 carrier preserved)
    - `pnpm test src/lib/stores/player.svelte.test.ts` is green (OFFL-02 regression-clear with SW present)
  </acceptance_criteria>
  <done>Share buttons emit readable entity links with ?play= queue carrier for songs; OFFL-02 verified green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Dual-adapter build-smoke gate + manual offline/crawler smoke</name>
  <action>
    [BLOCKING gate] After Tasks 1-3 and Plans 03/04 are merged, run `pnpm test` then `pnpm build && pnpm build:native`. BOTH builds MUST succeed (the D-03 dual-adapter hard constraint). Then present the manual crawler-curl + iOS-offline smoke steps below for human verification. Do NOT mark the phase complete until both builds are green and the human approves the manual smokes.
  </action>
  <what-built>
    The full Phase 24 surface is in place: native SW (Plan 03) + register guard, SSR entity routes + app.html static meta (Plan 04), offline indicator + inline states + entityShareUrl wiring (this plan). This gate proves the D-03 dual-adapter hard constraint and the manual-only verifications from VALIDATION.md.
  </what-built>
  <how-to-verify>
    1. Run the full unit suite: `pnpm test` — MUST be all green (regression check across Plans 01-05).
    2. Run the [BLOCKING] dual build smoke gate: `pnpm build && pnpm build:native` — BOTH MUST succeed. `pnpm build` = Cloudflare with SSR entity routes present; `pnpm build:native` = adapter-static SPA (no server routes, SW not auto-registered). If `build:native` errors about server routes / prerendering an entity route, the D-03 constraint is violated — STOP and fix before sign-off.
    3. Crawler OG smoke (manual, VALIDATION.md): start `pnpm preview` (wrangler pages dev) and run `curl -A facebookexternalhit http://localhost:4173/song/<slug>-qq123` — assert the returned HTML contains `<meta property="og:title">` describing the song (NOT the generic site default). Repeat for an album and artist entity URL. Then `curl` a shell page (e.g. `/search`) and assert the static `<title>`/description/canonical from app.html are present (SHARE-03).
    4. iOS offline smoke (manual, real device per VALIDATION.md — Pitfall 7): install the PWA, go offline, reload → app shell loads; find a downloaded song in Library → tap → it plays end-to-end offline (OFFL-02); visit an online-only surface (search) → inline offline state shows (no stuck loader), Downloads promoted (OFFL-03). Document any iOS storage-eviction behavior as a known constraint, not a regression.
  </how-to-verify>
  <resume-signal>Type "approved" once both builds are green and the crawler + offline smokes pass, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator.onLine signal → surface gating | Coarse connectivity hint gating fetches; untrusted as a guarantee of reachability |
| track fields → share URL | Title/artist/source/id flow into the readable share link |
| deploy → both build targets | The dual-adapter build must stay green or the Capacitor app breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | DoS / availability | offline-surface short-circuit | mitigate | Surfaces gate on `online.isOnline` and short-circuit BEFORE firing a fetch (no hanging request / stuck loader — D-10). Paired with the existing try/catch fetch-failure handling so a false-positive online still degrades gracefully. |
| T-24-03 | Tampering | entityShareUrl output | mitigate | Built from the track's own enum source + id via the Plan-02 helper; the `?play=` carrier is the existing QUEUE_CAP=30-bounded base64url token (T-gln-01 DoS bound preserved). |
| T-24-09 | Tampering | dual-adapter build | mitigate | [BLOCKING] gate runs `pnpm build && pnpm build:native`; native build must succeed (no server-only files, SW not registered) — the D-03 constraint proven green before sign-off. |
| T-24-01 | Information disclosure | OFFL-02 blob playback under SW | mitigate | Verified: `blob:` URLs never hit the SW fetch handler (RESEARCH line 240) and the SW bypasses cross-origin audio — downloaded playback is unaffected and no audio is cached. |
| T-24-SC | Tampering | npm installs | mitigate | ZERO new packages across the entire phase. No install gate; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm test` full suite green
- `pnpm build && pnpm build:native` BOTH green ([BLOCKING] gate)
- Manual crawler curl: entity routes emit per-entity OG; shell pages emit static default (SHARE-01/03)
- Manual iOS offline smoke: shell loads offline, downloaded song plays (OFFL-02), online-only surfaces show inline offline state promoting Downloads (OFFL-03)
</verification>

<success_criteria>
- Global offline indicator + per-surface inline offline states promoting Downloads, no stuck loaders / dead screens / forced redirect (OFFL-03)
- Share buttons emit readable entity links with ?play= queue carrier for songs (SHARE-02)
- OFFL-02 downloaded-song offline playback verified regression-clear with the SW present
- Both pnpm build and pnpm build:native succeed (D-03 hard constraint, [BLOCKING])
</success_criteria>

<output>
Create `.planning/phases/24-offline-app-shell-sharing-seo/24-05-SUMMARY.md` when done
</output>
