---
phase: 24-offline-app-shell-sharing-seo
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/service-worker.ts
  - svelte.config.js
files_referenced:
  - src/lib/services/sw-cache.ts
autonomous: true
requirements: [OFFL-01]
must_haves:
  truths:
    - "The app shell (build + static files) precaches into a version-keyed cache on install"
    - "On activate, all caches whose name != the current version cache are deleted (stale-shell eviction on deploy)"
    - "The fetch handler delegates to shouldBypass — /api/*, cross-origin audio, and range requests are never cached"
    - "The Capacitor native build (BUILD_TARGET=native) never auto-registers the web service worker"
  artifacts:
    - path: "src/service-worker.ts"
      provides: "Native SW: version-keyed precache + activate eviction + bypass-delegating fetch handler"
      contains: "shouldBypass"
    - path: "svelte.config.js"
      provides: "serviceWorker.register guard for the native build"
      contains: "serviceWorker"
  key_links:
    - from: "src/service-worker.ts"
      to: "src/lib/services/sw-cache.ts"
      via: "import shouldBypass and cacheNameFor"
      pattern: "shouldBypass"
    - from: "svelte.config.js"
      to: "BUILD_TARGET native branch"
      via: "kit.serviceWorker.register"
      pattern: "serviceWorker"
---

<objective>
Add the native service worker (OFFL-01) as a THIN caller of the Plan-01 pure `sw-cache.ts` helpers. The SW precaches the app shell into a `cache-${version}` cache, evicts non-matching caches on activate (stale-shell eviction on deploy), and bypasses `/api/*` + cross-origin audio + range requests via `shouldBypass`. Guard the Capacitor native build so it never auto-registers the web SW (D-03 / Pitfall 1).

Purpose: App shell loads offline; live metadata and audio are never served stale (OFFL-01).
Output: `src/service-worker.ts` (new) + `svelte.config.js` register guard; both builds still succeed (verified in Plan 05's gate).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-RESEARCH.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-PATTERNS.md
@.planning/phases/24-offline-app-shell-sharing-seo/24-01-SUMMARY.md

<interfaces>
<!-- Plan 01 pure core this SW wraps (do NOT reimplement the bypass logic here). -->
From src/lib/services/sw-cache.ts (Plan 01):
```typescript
export function shouldBypass(url: URL, request: { method: string; headers: Headers }, selfOrigin: string): boolean;
export function cacheNameFor(version: string): string; // `cache-${version}`
```

From $service-worker (SvelteKit native, RESEARCH.md Pattern 1 lines 181-202):
```typescript
import { build, files, version } from '$service-worker';
// build = app bundle paths; files = static/ paths; version = per-build hash (changes every deploy)
```

From svelte.config.js (existing dual-adapter switch, line 10):
```javascript
const native = process.env.BUILD_TARGET === 'native'; // NOTE: 'native', NOT 'static'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native service-worker.ts (precache + activate eviction + bypass fetch)</name>
  <files>src/service-worker.ts</files>
  <read_first>
    - src/lib/services/sw-cache.ts (Plan 01 — the shouldBypass/cacheNameFor signatures to call)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-RESEARCH.md lines 179-241 (Pattern 1 canonical install/activate, Pattern 2 fetch bypass, blob-URL note)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-PATTERNS.md lines 192-214 (service-worker.ts thin-wrapper assignment + canonical skeleton)
  </read_first>
  <action>
    Create `src/service-worker.ts` at the SvelteKit-convention path. Start with the triple-slash `@sveltejs/kit` reference directive and import `build, files, version` from `$service-worker` plus `shouldBypass, cacheNameFor` from `$lib/services/sw-cache`. Cast self via `const sw = globalThis.self as unknown as ServiceWorkerGlobalScope;`. Compute `const CACHE = cacheNameFor(version);` and `const ASSETS = [...build, ...files];`. install handler: `event.waitUntil` opens CACHE and runs `cache.addAll(ASSETS)`. activate handler: `event.waitUntil` iterates `await caches.keys()` and deletes every key `!== CACHE` (OFFL-01 stale-shell eviction). fetch handler: build `const url = new URL(event.request.url);` then `if (shouldBypass(url, event.request, sw.location.origin)) return;` passing `sw.location.origin` as the selfOrigin param. Otherwise `event.respondWith` a cache-first-for-ASSETS, else network then cache-on-200, catch then cache-match-or-throw strategy (RESEARCH.md lines 222-238). Do NOT reimplement bypass branches inline — the single `shouldBypass` early-return is the contract. Add a header comment noting this is the thin wrapper over the node-tested sw-cache core (mirrors sleepTimer wrapping sleep-timer).
  </action>
  <verify>
    <automated>pnpm check 2>&1 | grep -i "service-worker" || echo "no service-worker type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "shouldBypass\|cacheNameFor" src/service-worker.ts` shows the SW delegates to the pure helpers (does not reimplement bypass)
    - `grep -c "startsWith('/api/')" src/service-worker.ts` returns 0 (bypass logic lives ONLY in sw-cache.ts, not duplicated)
    - `grep -n "caches.keys\|caches.delete" src/service-worker.ts` shows the activate eviction loop
    - `pnpm check` reports no new type errors in service-worker.ts
  </acceptance_criteria>
  <done>SW precaches ASSETS, evicts non-version caches on activate, bypasses via shouldBypass; bypass logic is not duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Guard native build against SW auto-registration</name>
  <files>svelte.config.js</files>
  <read_first>
    - svelte.config.js (existing dual-adapter switch — `const native = process.env.BUILD_TARGET === 'native'` at line 10)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-RESEARCH.md lines 311-316 (Pitfall 1 and A3: kit.serviceWorker.register boolean)
    - .planning/phases/24-offline-app-shell-sharing-seo/24-PATTERNS.md line 214 (build guard assignment)
  </read_first>
  <action>
    In `svelte.config.js`, add a `serviceWorker: { register: !native }` sub-config to the `kit` object. Reuse the existing `const native = process.env.BUILD_TARGET === 'native'` flag (line 10 — value is `native`, NOT `static`; do NOT introduce any `static` check). When the Capacitor static build runs, `register` is false so SvelteKit does NOT auto-register the web SW (the native shell serves its own files — Pitfall 1). The web (Cloudflare) build keeps `register: true`. Add a one-line comment referencing D-03 / Pitfall 1. Do NOT add any `+page.server.ts` or server-only file (would break the static build).
  </action>
  <verify>
    <automated>grep -n "serviceWorker" svelte.config.js && grep -n "register: !native" svelte.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "register: !native" svelte.config.js` matches (native build gets register=false, web gets true)
    - `grep -c "BUILD_TARGET === 'static'\|=== \"static\"" svelte.config.js` returns 0 (no incorrect static check introduced)
    - The full build-smoke gate in Plan 05 confirms both `pnpm build` and `pnpm build:native` still succeed
  </acceptance_criteria>
  <done>Native build does not auto-register the web SW; web build still does; no static-check regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → SW fetch handler | Every navigation/asset request is intercepted; the bypass gate decides cache vs passthrough |
| deploy → users' cached shells | A new deploy must evict the old shell so users never run a stale app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information disclosure | SW fetch handler | mitigate | Delegates to `shouldBypass` (Plan 01, unit-tested) so `/api/*` + cross-origin audio + range responses are NEVER cached — no stale-auth, cross-user data leak, or stale media. The acceptance grep proves bypass logic is not re-implemented (single source of truth). |
| T-24-02 | Tampering | activate eviction | mitigate | `cache-${version}` rotates per deploy; activate deletes all non-matching caches → no stale-shell poisoning (Pitfall 2). |
| T-24-05 | DoS | precache size on iOS | accept | App-shell precache is HTML/JS/CSS, well under the ~50 MB iOS Cache quota (Pitfall 7); audio is never cached (T-24-01). Documented as a known iOS-eviction constraint, not a regression. |
| T-24-06 | Tampering | native shell SW collision | mitigate | `serviceWorker.register=false` under `BUILD_TARGET=native` so the web SW never registers against a capacitor:// origin (Pitfall 1). |
| T-24-SC | Tampering | npm installs | mitigate | ZERO new packages (native SvelteKit `$service-worker`, no vite-pwa per locked decision). No install gate; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm check` no new type errors in `src/service-worker.ts`
- `grep` acceptance checks confirm bypass delegation + eviction loop + native register guard
- Full `pnpm build && pnpm build:native` gate runs in Plan 05 (both must succeed)
</verification>

<success_criteria>
- Native SW precaches the shell, version-keyed eviction on activate, bypass via the tested pure helper (OFFL-01)
- Native build never auto-registers the web SW (D-03)
- No bypass-logic duplication; no server-only files added; no new deps
</success_criteria>

<output>
Create `.planning/phases/24-offline-app-shell-sharing-seo/24-03-SUMMARY.md` when done
</output>
