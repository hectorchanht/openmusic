---
phase: 35-one-click-export-and-import-of-all-app-data
plan: 04
type: execute
wave: 2
depends_on: ["35-01", "35-02", "35-03"]
files_modified:
  - src/routes/(app)/settings/data/+page.svelte
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-18]
must_haves:
  truths:
    - "Settings → Data shows Export, Import and Re-download missing (N) buttons above the existing clear buttons, and an Undo last import button only when a snapshot exists"
    - "Tapping Export builds the envelope synchronously in the click handler and saves/shares openmusic-backup-YYYY-MM-DD.json"
    - "Tapping Import opens a file picker (accept=\"application/json,.json\"); a rejected file flashes the matching D-11 message and writes nothing; an accepted file asks confirm(), snapshots, replaces, flashes and reloads after ~600 ms"
    - "The N in Re-download missing is computed from blobStore.has at mount and after each sweep; the sweep is sequential, stoppable and reports one summary toast"
    - "Undo validates the sessionStorage snapshot, writes it back and reloads"
  artifacts:
    - path: "src/routes/(app)/settings/data/+page.svelte"
      provides: "The four new controls + hidden file input wired to the pure core and the platform seam"
      contains: "accept=\"application/json,.json\""
      min_lines: 140
  key_links:
    - from: "src/routes/(app)/settings/data/+page.svelte"
      to: "src/lib/backup/backup-logic.ts"
      via: "buildEnvelope / validateEnvelope / applyEnvelope / undoImport"
      pattern: "from '\\$lib/backup/backup-logic'"
    - from: "src/routes/(app)/settings/data/+page.svelte"
      to: "src/lib/services/backup-io.ts"
      via: "exportBackup(json, filename)"
      pattern: "exportBackup\\("
    - from: "src/routes/(app)/settings/data/+page.svelte"
      to: "src/lib/backup/sweep.ts"
      via: "sweepMissing(library.downloads, { has: blobStore.has, download: t => downloadTrack(t, { save: false }), signal })"
      pattern: "save: false"
    - from: "src/routes/(app)/settings/data/+page.svelte"
      to: "location.reload()"
      via: "setTimeout after a successful apply/undo (D-13)"
      pattern: "location\\.reload\\(\\)"
---

<objective>
Wire the Settings → Data page: Export, Import (+ hidden file input), Undo last import, and Re-download missing (N) / Stop. The page stays a thin caller — every decision lives in `backup-logic.ts` (plan 01), every platform branch in `backup-io.ts` (plan 03), every string in the dictionaries (plan 02).

Purpose: this is the user-facing deliverable of the phase. After this plan, a user can export on one device and restore on another, undo a mistaken import, and get the offline copies back with one tap.

Output: modified `src/routes/(app)/settings/data/+page.svelte`; `pnpm test` + `pnpm check` + `pnpm build` green.

Deploy caution: pushing `main` auto-deploys production. This plan completes the feature; no push is planned here — the executor commits only. Device verification follows in plans 05/06.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-CONTEXT.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-PATTERNS.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-01-SUMMARY.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts from plans 01/03 (exact names) and existing code the page calls. -->

From src/lib/backup/backup-logic.ts (plan 01):
```ts
export const UNDO_KEY = 'openmusic:import-undo:v1';
export function backupFilename(now: Date): string;
export function storageKeys(store: Pick<Storage, 'length' | 'key'>): string[];
export function buildEnvelope(read: (k: string) => string | null, allKeys: readonly string[], now?: Date): BackupEnvelope;
export function serializeEnvelope(env: BackupEnvelope): string;
export function validateEnvelope(raw: string): ValidateResult;   // { ok: true, envelope, skipped } | { ok: false, reason: 'not-ours' | 'newer' | 'damaged' }
export function applyEnvelope(env: BackupEnvelope, live: Storage, undo: Storage): 'ok' | 'no-snapshot' | 'write-failed';
export function undoImport(live: Storage, undo: Storage): 'ok' | 'no-snapshot' | 'write-failed';
export function hasUndoSnapshot(undo: Pick<Storage, 'getItem'>): boolean;
```

From src/lib/services/backup-io.ts (plan 03):
```ts
export async function exportBackup(json: string, filename: string): Promise<'ok' | 'dismissed' | 'failed'>;
```

From src/lib/backup/sweep.ts (plan 03):
```ts
export async function findMissing(tracks: readonly Track[], has: (uid: string) => Promise<boolean>): Promise<Track[]>;
export async function sweepMissing(tracks: readonly Track[], deps: { has; download; signal?; delayMs?; onProgress? }): Promise<{ saved; failed; skipped; stopped }>;
```

Existing:
```ts
import { blobStore } from '$lib/services/blob-store';          // blobStore.has(uid)
import { downloadTrack } from '$lib/services/download-track';  // downloadTrack(t, { save: false })
import { library } from '$lib/stores/library.svelte';          // library.downloads: Track[]
import { t } from '$lib/i18n';                                  // t(key, params?)
import { tapBounce } from '$lib/actions/tapBounce';
```

i18n keys available (plan 02): backup.export, backup.exportDesc, backup.exported, backup.exportFailed, backup.import, backup.importDesc, backup.importConfirm, backup.imported, backup.importedSkipped {n}, backup.errNotOurs, backup.errNewer, backup.errDamaged, backup.errNoSnapshot, backup.errWriteFailed, backup.undo, backup.undoDesc, backup.undoConfirm, backup.undone, backup.redownload {n}, backup.redownloadDesc, backup.stop, backup.sweepDone {saved} {failed}

Lucide icons confirmed present in @lucide/svelte: `Download`, `Upload`, `Undo2`, `CloudDownload`, `CircleStop`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export, Import (hidden file input), Undo — wired to the pure core</name>
  <files>src/routes/(app)/settings/data/+page.svelte</files>
  <read_first>
    - src/routes/(app)/settings/data/+page.svelte (whole file, 79 lines — `flash()`, `confirm()` in `clearLibrary`, `.item`/`.hint`/`.flash` markup + styles, per-icon lucide import line)
    - src/lib/backup/backup-logic.ts (exports, as built in plan 01)
    - src/lib/services/backup-io.ts (as built in plan 03)
    - .planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md §Answer 6 (the `accept` crash path + recipe; `confirm()` verified in the WebView; reading the picked file), §Answer 7 (synchronous export in the click handler), §Answer 8 (sessionStorage rationale), §Open Questions 1-2 (A4 probe; 600 ms pre-toast before reload)
    - .planning/phases/35-one-click-export-and-import-of-all-app-data/35-PATTERNS.md §+page.svelte and §No Analog Found (file input, sessionStorage, location.reload are new)
  </read_first>
  <action>
    Extend the existing page (runes; tabs; single quotes). Add imports: `Download, Upload, Undo2` to the lucide line; `backupFilename, buildEnvelope, serializeEnvelope, storageKeys, validateEnvelope, applyEnvelope, undoImport, hasUndoSnapshot` from `$lib/backup/backup-logic`; `exportBackup` from `$lib/services/backup-io`. New state: `let canUndo = $state(false)`; `let fileInput = $state<HTMLInputElement | null>(null)` (or a plain `let fileInput: HTMLInputElement` bound with `bind:this` — either is fine in runes mode). In `onMount`, set `canUndo = hasUndoSnapshot(sessionStorage)` inside try/catch (comment `35-D-09`: snapshot lives in sessionStorage — survives `location.reload()`, own quota, expires with the tab; A4 unverified on the Capacitor WebView — if the Undo button never appears after a reload on the APK (plan 05 checkpoint), the fallback is to pass `localStorage` here and in the two call-sites below plus a `snapshot.length > 2_000_000` pre-check; that is the whole swap).

    `exportNow()`: SYNCHRONOUS up to the seam — `const json = serializeEnvelope(buildEnvelope(k => localStorage.getItem(k), storageKeys(localStorage)))` and `const name = backupFilename(new Date())`, both inside try/catch, then `exportBackup(json, name).then(r => { if (r === 'ok') flash(t('backup.exported')); else if (r === 'failed') flash(t('backup.exportFailed')); /* 'dismissed': say nothing */ })`. Comment `35-D-14`: no `await` before the seam — iOS blocks a programmatic anchor click after an await (RESEARCH Pitfall 5); the read is synchronous so nothing is lost.

    `pickImport()`: `fileInput?.click()`. `async function onPicked(e: Event)`: `const f = (e.currentTarget as HTMLInputElement).files?.[0]; if (!f) return;` then reset `(e.currentTarget as HTMLInputElement).value = ''` so re-picking the same file fires `change` again; `const text = await f.text()`; `const res = validateEnvelope(text)`; if `!res.ok`: `flash(res.reason === 'not-ours' ? t('backup.errNotOurs') : res.reason === 'newer' ? t('backup.errNewer') : t('backup.errDamaged'))` and `return` (comment `35-D-10/D-11`: NOTHING written; the pure module returns a machine reason and the page localizes — services never import `$lib/i18n`). Then `if (!confirm(t('backup.importConfirm'))) return` (comment `35-D-08`: Capacitor 8 renders `confirm()` as a native AlertDialog — `BridgeWebChromeClient.java:168` — so the existing `clearLibrary` idiom works in the APK too). Then `const r = applyEnvelope(res.envelope, localStorage, sessionStorage)`: `'no-snapshot'` → `flash(t('backup.errNoSnapshot'))`; `'write-failed'` → `flash(t('backup.errWriteFailed'))`; `'ok'` → `flash(res.skipped.length ? t('backup.importedSkipped', { n: res.skipped.length }) : t('backup.imported'))` then `setTimeout(() => location.reload(), 600)` (comment `35-D-13`: full reload so every store re-runs `load()` cold — never live re-hydrate, see `restore-effect-self-invalidation-loop`; 600 ms lets the toast show before the APK's splash — RESEARCH Open Question 2, observe at the device checkpoint, do not redesign).

    `undoNow()`: `if (!confirm(t('backup.undoConfirm'))) return; const r = undoImport(localStorage, sessionStorage)`; `'ok'` → `flash(t('backup.undone'))` + `setTimeout(() => location.reload(), 600)`; otherwise `canUndo = false` and (for `'write-failed'`) `flash(t('backup.errWriteFailed'))`.

    Markup — insert directly after the `.muted` counts paragraph and BEFORE the existing `clearPicks` button: an Export `.item` (icon `Download`, label `t('backup.export')`) + `.hint` `t('backup.exportDesc')`; an Import `.item` (icon `Upload`, `t('backup.import')`) + `.hint` `t('backup.importDesc')`; `{#if canUndo}` an Undo `.item` (icon `Undo2`, `t('backup.undo')`) + `.hint` `t('backup.undoDesc')` `{/if}`; and the hidden input EXACTLY: `<input type="file" accept="application/json,.json" bind:this={fileInput} onchange={onPicked} hidden />` (comment above it: a bare `accept=".json"` hits an unguarded `validTypes[0]` in Capacitor's `BridgeWebChromeClient.showFilePicker:379` when `MimeTypeMap` has no `json` entry — leading with a real MIME makes that branch unreachable; escape hatch if a device greys the file out is to DROP `accept` entirely, content validation is the real gate). All buttons get `use:tapBounce`. Reuse `.item` / `.hint` / `.flash` — add no new CSS classes.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -q 'accept="application/json,.json"' "src/routes/(app)/settings/data/+page.svelte" && grep -q "applyEnvelope(res.envelope, localStorage, sessionStorage)" "src/routes/(app)/settings/data/+page.svelte" && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'accept="application/json,.json"' "src/routes/(app)/settings/data/+page.svelte"` prints 1 and `grep -c 'accept=".json"' "src/routes/(app)/settings/data/+page.svelte"` prints 0
    - `grep -c "location.reload()" "src/routes/(app)/settings/data/+page.svelte"` prints 2 (import success + undo success)
    - `grep -c "confirm(t('backup.importConfirm'))" "src/routes/(app)/settings/data/+page.svelte"` prints 1
    - `grep -c "t('backup.errNotOurs')\|t('backup.errNewer')\|t('backup.errDamaged')" "src/routes/(app)/settings/data/+page.svelte"` prints >= 1 per key (each of the three keys appears)
    - `grep -c "isNativePlatform\|@capacitor" "src/routes/(app)/settings/data/+page.svelte"` prints 0 (D-18 branch lives only in backup-io.ts)
    - In `exportNow`, no `await` precedes `exportBackup(` (`awk '/function exportNow/,/^\t}/' "src/routes/(app)/settings/data/+page.svelte" | grep -c "await"` prints 0)
    - `grep -c "library.clearAll()" "src/routes/(app)/settings/data/+page.svelte"` prints 1 (only the pre-existing Clear library button — the import path does not call it)
    - `pnpm check` reports 0 errors
  </acceptance_criteria>
  <done>Export/Import/Undo buttons render; picker uses the safe accept value; rejected files write nothing; accepted files snapshot, replace and reload.</done>
</task>

<task type="auto">
  <name>Task 2: Re-download missing (N) / Stop — derived count + sequential sweep</name>
  <files>src/routes/(app)/settings/data/+page.svelte</files>
  <read_first>
    - src/routes/(app)/settings/data/+page.svelte (as modified in Task 1)
    - src/lib/backup/sweep.ts (plan 03 — `findMissing`, `sweepMissing`, `SweepDeps`)
    - src/lib/services/download-track.ts (lines 54-68 — `{ save: false }` semantics; lines 100-112 raw fetch)
    - src/lib/components/TrackMenu.svelte (lines 200-240 — the existing `blobPresent` probe-on-open precedent for D-07 and its "separate change" scope note)
    - .planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md §Answer 4 (fan-out for `has`, the 7 sweep rules) and Pitfall 3
  </read_first>
  <action>
    Add imports: `CloudDownload, CircleStop` to the lucide line; `findMissing, sweepMissing` from `$lib/backup/sweep`; `blobStore` from `$lib/services/blob-store`; `downloadTrack` from `$lib/services/download-track`. State: `let missing = $state<Track[] | null>(null)` (null = not probed yet; import `type Track` from `$lib/sources/types`), `let sweeping = $state(false)`, `let sweepDone = $state(0)`; a PLAIN (non-`$state`) field `let sweepCtl: AbortController | null = null` (house convention: guards the UI never reads reactively stay plain).

    `async function refreshMissing()`: `missing = await findMissing(library.downloads, blobStore.has)` (comment `35-D-07`: "missing" = bytes absent per `blobStore.has` at render time, never stored; an imported entry whose bytes happen to exist here shows as present — this is what reconciles Replace with real local downloads; scope: this page only, `DownloadControl.svelte` is NOT migrated in this phase per `TrackMenu.svelte:213-214`). Call `refreshMissing()` at the end of `onMount` (after `library.load()`).

    `async function redownloadMissing()`: if `sweeping` → `sweepCtl?.abort(); return` (the same button is the Stop control). Else `sweeping = true; sweepCtl = new AbortController(); sweepDone = 0; try { const r = await sweepMissing(missing ?? library.downloads, { has: blobStore.has, download: (tr) => downloadTrack(tr, { save: false }), signal: sweepCtl.signal, onProgress: (done) => (sweepDone = done) }); flash(t('backup.sweepDone', { saved: r.saved, failed: r.failed })); } finally { sweeping = false; sweepCtl = null; await refreshMissing(); }`. Comments: `35-D-06` never auto-starts (no call from the import path — a large unasked-for network job, possibly on cellular, is exactly the mistake `openmusic-pushes-autodeploy-live` cost once); `{ save: false }` is the 31-D-12 silent-repair mode — persists the offline blob (IDB / Directory.Data + MediaStore) with NO `<a download>` click, so 200 songs produce 0 save dialogs — do NOT use `{ persist: false }` (that skips `blobStore.put`, the opposite of what the sweep restores); sequential + stop + re-probe per iteration = `api-fetch-flood-freeze` mitigation, see `sweep.ts`.

    Markup — after the Undo block from Task 1 and before `clearPicks`: one `.item` button whose icon is `{#if sweeping}<CircleStop size={18} />{:else}<CloudDownload size={18} />{/if}` and whose label is `sweeping ? t('backup.stop') : t('backup.redownload', { n: missing?.length ?? 0 })`, `disabled={!sweeping && (missing?.length ?? 0) === 0}`, `onclick={redownloadMissing}`, `use:tapBounce`; `.hint` `t('backup.redownloadDesc')`. Add ONE style rule `.item:disabled { opacity: 0.5; cursor: default; }` (the only new CSS in the phase). Also refresh `counts` and `missing` after a completed sweep (already covered by `refreshMissing()` in `finally`).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -q "downloadTrack(tr, { save: false })" "src/routes/(app)/settings/data/+page.svelte" && pnpm check && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "save: false" "src/routes/(app)/settings/data/+page.svelte"` prints 1 and `grep -c "persist: false" "src/routes/(app)/settings/data/+page.svelte"` prints 0
    - `grep -c "findMissing(library.downloads, blobStore.has)" "src/routes/(app)/settings/data/+page.svelte"` prints 1
    - `grep -c "new AbortController()" "src/routes/(app)/settings/data/+page.svelte"` prints 1
    - `grep -n "sweepCtl" "src/routes/(app)/settings/data/+page.svelte" | grep -c "\$state"` prints 0 (plain field, not reactive)
    - `awk '/async function onPicked/,/^\t}/' "src/routes/(app)/settings/data/+page.svelte" | grep -c "redownloadMissing\|sweepMissing"` prints 0 (import never auto-starts the sweep — D-06)
    - `pnpm check` 0 errors; `pnpm test` exits 0 (full suite incl. i18n parity, backup-logic, sweep, backup-io, roundtrip); `pnpm build` exits 0 (web adapter — the static `@capacitor/share` import must not break the Cloudflare build)
  </acceptance_criteria>
  <done>Re-download button shows the derived missing count, toggles to Stop while running, runs one-at-a-time, and reports a single summary; full suite, typecheck and web build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picked file → page | User-selected file of arbitrary origin enters via `file.text()` |
| page → localStorage/sessionStorage | Destructive Replace of five key families |
| page → network | User-initiated bulk download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | onPicked | mitigate | `validateEnvelope` gate before `confirm()`; rejected → flash + return, zero writes (D-10) |
| T-35-03 | DoS (self-inflicted) | redownloadMissing | mitigate | Never auto-started (D-06); sequential `sweepMissing` with AbortController stop; button doubles as Stop |
| T-35-05 | DoS (quota) | applyEnvelope call | mitigate | `no-snapshot` / `write-failed` surfaced as distinct toasts; snapshot in sessionStorage |
| T-35-09 | Repudiation / UX | destructive Replace + Undo | mitigate | Explicit `confirm()` naming what is replaced (D-08); Undo affordance persists across the reload (D-09) |
| T-35-SC | Tampering | npm installs | n/a this plan | No installs in this plan |
</threat_model>

<verification>
- `pnpm test`, `pnpm check`, `pnpm build` all green
- Grep gates above: safe `accept`, no Capacitor import in the page, `save: false` not `persist: false`, sweep never called from the import path
</verification>

<success_criteria>
- All four controls present and wired; every D-01..D-14 + D-18 behaviour reachable from Settings → Data
- No new store, no new CSS beyond `.item:disabled`, no second download helper, no live re-hydration
</success_criteria>

<output>
Create `.planning/phases/35-one-click-export-and-import-of-all-app-data/35-04-SUMMARY.md` when done
</output>
