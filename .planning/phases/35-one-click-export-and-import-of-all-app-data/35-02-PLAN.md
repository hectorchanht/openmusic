---
phase: 35-one-click-export-and-import-of-all-app-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/en.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/es.ts
  - src/lib/i18n/fr.ts
  - src/lib/i18n/it.ts
  - src/lib/i18n/pt.ts
  - src/lib/i18n/ru.ts
  - src/lib/i18n/ar.ts
  - src/lib/i18n/hi.ts
  - src/lib/i18n/id.ts
  - src/lib/i18n/th.ts
  - src/lib/i18n/tr.ts
  - src/lib/i18n/vi.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/zh-Hant.ts
autonomous: true
requirements: [D-08, D-11]
must_haves:
  truths:
    - "Every one of the 15 locale dictionaries exposes the 22 new backup.* keys with a non-blank value"
    - "The three D-11 failure messages are distinct strings in every locale"
    - "The D-08 confirm text names what is about to be replaced (liked songs, playlists, downloads list, play history, search history, settings, name translations)"
  artifacts:
    - path: "src/lib/i18n/en.ts"
      provides: "Reference locale — defines TranslationKey for the 22 backup.* keys"
      contains: "\"backup.errNotOurs\""
    - path: "src/lib/i18n/zh-Hant.ts"
      provides: "One of 14 translated dictionaries with identical key set"
      contains: "\"backup.errNotOurs\""
  key_links:
    - from: "src/lib/i18n/i18n.test.ts"
      to: "src/lib/i18n/*.ts"
      via: "key-set parity assertion (all 15 locales)"
      pattern: "IDENTICAL to en"
---

<objective>
Add the 22 UI strings the Settings → Data buttons, confirm dialogs, result toasts and three D-11 failure messages need — in all 15 locale dictionaries, double-quoted, identical key set.

Purpose: the page (plan 04) calls `t('backup.*')`; `TranslationKey` is `keyof typeof en`, so a missing key is a compile error, and `i18n.test.ts:52` fails the whole suite if any locale's key set differs from `en`. Doing this as its own wave-1 plan lets plan 04 reference finished keys.

Output: 15 modified dictionaries; `npx vitest run src/lib/i18n/i18n.test.ts` green.

Correction honoured: there are **15** dictionaries (ar de en es fr hi id it pt ru th tr vi zh-Hans zh-Hant), not 16 as CONTEXT D-11 / CLAUDE.md say. Do not create a 16th.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-CONTEXT.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The exact key set. Plan 04 calls these names; do not rename. English values are the reference text. -->

Keys (namespace `backup.`), English reference values:
- "backup.export": "Export backup"
- "backup.exportDesc": "Saves your liked songs, playlists, downloads list, play history, search history, settings and name translations to one JSON file. Downloaded audio is not included."
- "backup.exported": "Backup exported."
- "backup.exportFailed": "Export failed."
- "backup.import": "Import backup"
- "backup.importDesc": "Replaces everything above with the contents of a backup file, then reloads the app. You can undo the last import afterwards."
- "backup.importConfirm": "Replace your liked songs, playlists, downloads list, play history, search history, settings and name translations on this device with the contents of this file?"
- "backup.imported": "Backup restored — reloading…"
- "backup.importedSkipped": "Backup restored ({n} unsupported entries skipped) — reloading…"
- "backup.errNotOurs": "This file is not an OpenMusic backup."
- "backup.errNewer": "This backup was made by a newer version of the app. Update the app and try again."
- "backup.errDamaged": "This backup file is damaged and cannot be imported."
- "backup.errNoSnapshot": "Could not save an undo snapshot, so nothing was changed. Free up storage and try again."
- "backup.errWriteFailed": "Import failed while writing; your previous data was restored."
- "backup.undo": "Undo last import"
- "backup.undoDesc": "Puts back the data that was on this device before the last import, then reloads."
- "backup.undoConfirm": "Undo the last import and restore the previous data?"
- "backup.undone": "Import undone — reloading…"
- "backup.redownload": "Re-download missing songs ({n})"
- "backup.redownloadDesc": "Downloads, one at a time, every song in your downloads list whose audio is not on this device. Safe to run again; tap Stop to pause."
- "backup.stop": "Stop re-downloading"
- "backup.sweepDone": "Re-downloaded {saved} songs, {failed} failed."

Interpolation: `t(key, { n })` / `t(key, { saved, failed })` — `interpolate()` at `src/lib/i18n/index.ts:67` replaces `{token}` with `string | number`. No new machinery.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 22 backup.* keys to en + de es fr it pt ru</name>
  <files>src/lib/i18n/en.ts, src/lib/i18n/de.ts, src/lib/i18n/es.ts, src/lib/i18n/fr.ts, src/lib/i18n/it.ts, src/lib/i18n/pt.ts, src/lib/i18n/ru.ts</files>
  <read_first>
    - src/lib/i18n/en.ts (lines 100-125 and 445-455 — the existing `settings.*` data-page keys; place the new section directly after `"settings.clearLibraryDesc"` or as a new `// --- backup (Settings → Data) ---` block near the settings.data keys)
    - src/lib/i18n/zh-Hant.ts (lines 1-10 — the non-en file shape: `import type { Dict } from "./index";` with DOUBLE-quoted specifier)
    - src/lib/i18n/i18n.test.ts (lines 46-72 — parity + no-blank checks)
    - .planning/phases/35-one-click-export-and-import-of-all-app-data/35-PATTERNS.md §i18n
    - CLAUDE.md §i18n — DOUBLE QUOTES convention
  </read_first>
  <action>
    In each of the 7 files add a `// --- backup (Settings → Data, phase 35) ---` block containing all 22 keys from `<interfaces>`, in that order, with DOUBLE quotes around every key AND value (this is the one place in the phase where double quotes are mandatory; no formatter enforces it). `en.ts` gets the English reference values verbatim. de/es/fr/it/pt/ru get natural translations that preserve the `{n}`, `{saved}`, `{failed}` tokens literally and keep the product name "OpenMusic" untranslated. The three D-11 messages (`errNotOurs`, `errNewer`, `errDamaged`) must be three distinct sentences in every language. Keep tabs for indentation. Do not touch existing keys.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && for f in en de es fr it pt ru; do test "$(grep -c '"backup\.' src/lib/i18n/$f.ts)" = "22" || { echo "FAIL $f"; exit 1; }; done && grep -c "'backup\." src/lib/i18n/*.ts | grep -v ':0$' | grep -q . && exit 1 || pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"backup\.' src/lib/i18n/en.ts` prints 22 (same for de es fr it pt ru)
    - `grep -c "'backup\." src/lib/i18n/*.ts` prints 0 for every file (no single-quoted keys crept in)
    - `grep -n '"backup.importConfirm"' src/lib/i18n/en.ts` matches a value containing "liked songs", "playlists", "downloads list", "play history", "search history", "settings" and "name translations"
    - `grep -n '"backup.sweepDone"' src/lib/i18n/de.ts` value contains both `{saved}` and `{failed}`
    - `pnpm check` reports 0 errors (the 8 not-yet-edited locales will FAIL the `Dict` type until Task 2 — so run `pnpm check` only as the final gate of Task 2 if it errors here; the i18n vitest parity test is the Task 2 gate)
  </acceptance_criteria>
  <done>7 dictionaries carry the 22 keys with the reference English text in `en.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the same 22 keys to ar hi id th tr vi zh-Hans zh-Hant and prove parity</name>
  <files>src/lib/i18n/ar.ts, src/lib/i18n/hi.ts, src/lib/i18n/id.ts, src/lib/i18n/th.ts, src/lib/i18n/tr.ts, src/lib/i18n/vi.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/zh-Hant.ts</files>
  <read_first>
    - src/lib/i18n/en.ts (the block written in Task 1 — the reference text)
    - src/lib/i18n/zh-Hant.ts and src/lib/i18n/ar.ts (lines 95-125 — where the existing settings/data keys sit in each, to place the block consistently)
    - src/lib/i18n/i18n.test.ts (lines 46-72)
  </read_first>
  <action>
    Add the identical `// --- backup (Settings → Data, phase 35) ---` block with all 22 keys to the remaining 8 dictionaries, translated (zh-Hans simplified vs zh-Hant traditional must differ where the scripts differ; Arabic is RTL text but the file is plain TS — no markup). Same rules: DOUBLE quotes for keys and values, tokens `{n}` `{saved}` `{failed}` preserved literally, "OpenMusic" untranslated, three distinct D-11 sentences, tabs. Then run the parity test and typecheck.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && for f in ar de en es fr hi id it pt ru th tr vi zh-Hans zh-Hant; do test "$(grep -c '"backup\.' src/lib/i18n/$f.ts)" = "22" || { echo "FAIL $f"; exit 1; }; done && npx vitest run src/lib/i18n/i18n.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `for f in ar de en es fr hi id it pt ru th tr vi zh-Hans zh-Hant; do grep -c '"backup\.' src/lib/i18n/$f.ts; done` prints 22 fifteen times
    - `npx vitest run src/lib/i18n/i18n.test.ts` exits 0 (key-set parity across all 15 + no blank values)
    - `grep -c "'backup\." src/lib/i18n/*.ts | grep -v ':0$'` prints nothing
    - `diff <(grep -o '"backup\.[a-zA-Z]*"' src/lib/i18n/en.ts | sort) <(grep -o '"backup\.[a-zA-Z]*"' src/lib/i18n/zh-Hant.ts | sort)` prints nothing (identical key names)
    - `pnpm check` reports 0 errors
  </acceptance_criteria>
  <done>All 15 dictionaries share the identical 22-key backup.* set; parity test and typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static string dictionaries; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Information disclosure | error messages | accept | D-11 messages name the failure class (not ours / newer / damaged) and never echo file contents |
| T-35-SC | Tampering | npm installs | n/a this plan | No installs in this plan |
</threat_model>

<verification>
- `npx vitest run src/lib/i18n/i18n.test.ts` green
- `pnpm check` clean
- 22 double-quoted `backup.*` keys in each of 15 files
</verification>

<success_criteria>
- Plan 04 can call every `t('backup.*')` key listed in `<interfaces>` without a type error
- The parity test (VALIDATION.md row "D-11 (i18n)") passes
</success_criteria>

<output>
Create `.planning/phases/35-one-click-export-and-import-of-all-app-data/35-02-SUMMARY.md` when done
</output>
