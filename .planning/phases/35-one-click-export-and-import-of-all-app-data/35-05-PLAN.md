---
phase: 35-one-click-export-and-import-of-all-app-data
plan: 05
type: execute
wave: 3
depends_on: ["35-04"]
files_modified: []
autonomous: false
requirements: [D-09, D-13, D-15, D-16, D-17]
must_haves:
  truths:
    - "On the Android APK, Import opens the system document picker and a pushed .json backup is selectable (not greyed out); the import completes, the app reloads, and the restored library is visible"
    - "After that reload the Undo last import button is present (sessionStorage survived location.reload() in the Capacitor WebView — research A4) and tapping it restores the prior state"
    - "A damaged fixture is refused with the 'damaged' message and nothing changes"
    - "Export opens the Android share sheet with real targets; the shared file parses as JSON; dismissing the sheet shows no error"
    - "logcat shows no ArrayIndexOutOfBounds / FileProvider / SharePlugin errors during the session"
  artifacts:
    - path: "android/app/build/outputs/apk/debug/app-debug.apk"
      provides: "Debug APK built with @capacitor/share registered"
  key_links:
    - from: "adb push fixture"
      to: "/sdcard/Download/openmusic-backup-2026-09-13.json"
      via: "system document picker"
      pattern: "openmusic-backup-"
---

<objective>
Verify the two Android-only behaviours that `pnpm test` cannot prove — D-15 (file picker in the Capacitor WebView) and D-16/D-17 (share sheet via FileProvider) — plus the research A4 open question (does `sessionStorage` survive `location.reload()` in the WebView, i.e. does Undo appear after import). Claude automates everything up to the tap: build, emulator boot, install, fixture push, logcat capture. The human taps and reports.

Purpose: VALIDATION.md names these as device-only checkpoints; construction-level evidence is not completion.

Output: two signed-off checkpoints; if A4 fails, the documented one-argument fallback is applied in a follow-up quick task, not silently.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-VALIDATION.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Machine facts (verified while planning):
- JDK 21: `/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home` (gradle fails on the default Java 20 — `apk-build-needs-jdk21`)
- `adb`: `/opt/homebrew/bin/adb`; emulator binary: `/Users/laichan/Library/Android/sdk/emulator/emulator` (NOT on PATH); AVD present: `Pixel_3a_API_34`
- `pnpm apk` = `pnpm build:native && npx cap sync android && cd android && ./gradlew assembleDebug`
- Fixture generator: `node --input-type=module -e` importing nothing — write a plain JSON object `{ app: 'openmusic-backup', format: 1, exportedAt, keys: { 'openmusic:library:v1': { liked: [ <2 Track-shaped objects with uid/source/songid/title/artist> ], playlists: [], downloads: [], favArtists: ['Fixture Artist'] }, 'openmusic:history:v1': [], 'openmusic:search-history:v1': [{ query: 'fixture', ts: 1 }], 'openmusic:settings:v1': { theme: 'dark' }, 'openmusic:name-tr:v2:zh-Hant': { 'Fixture': '夾具' } } }` pretty-printed; a second file `openmusic-backup-damaged.json` containing `{"app":"openmusic-backup","format":1,"keys":{"openmusic:library:v1":{"liked":5}}}`; a third `openmusic-backup-newer.json` containing `{"app":"openmusic-backup","format":99,"keys":{}}`.
- CDP attach (optional, for reading console): `adb forward tcp:9222 localabstract:webview_devtools_remote_$(adb shell pidof com.openmusic.app)` then `curl -s localhost:9222/json`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the APK, boot the emulator, install, push fixtures, start logcat</name>
  <files></files>
  <read_first>
    - .planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md §Answer 6 "Verification recipe" (steps 1-7) and §Answer 5 "What npx cap sync covers"
    - package.json (scripts: `apk`, `build:native`)
    - android/capacitor.settings.gradle (confirm `capacitor-share` include is present from plan 03)
  </read_first>
  <action>
    1. `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home pnpm apk` (timeout 600000; expect `android/app/build/outputs/apk/debug/app-debug.apk`). If gradle fails on the JDK, stop and report — do not downgrade anything.
    2. Boot the emulator in the background: `/Users/laichan/Library/Android/sdk/emulator/emulator -avd Pixel_3a_API_34 -no-snapshot-load -no-boot-anim` (run_in_background), then `adb wait-for-device` and poll `adb shell getprop sys.boot_completed` until `1`.
    3. `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`.
    4. Generate the three fixtures from `<interfaces>` into the scratchpad directory with `node` (pretty-print the valid one with `JSON.stringify(obj, null, 2)`), then `adb push` each to `/sdcard/Download/` and confirm with `adb shell ls /sdcard/Download/`.
    5. Start `adb logcat | grep -iE "ArrayIndexOutOfBounds|FileChooser|fileprovider|SharePlugin|chromium.*Uncaught"` in the background writing to `<scratchpad>/logcat-35.txt` (run_in_background), and `adb logcat -c` first to clear.
    6. Launch the app: `adb shell monkey -p com.openmusic.app -c android.intent.category.LAUNCHER 1`.
    Report the emulator serial, the APK path and the three pushed paths for the checkpoints.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test -f android/app/build/outputs/apk/debug/app-debug.apk && adb shell ls /sdcard/Download/ | grep -c "openmusic-backup-" | grep -q "^3$" && adb shell pidof com.openmusic.app</automated>
  </verify>
  <acceptance_criteria>
    - `android/app/build/outputs/apk/debug/app-debug.apk` exists and is newer than `package.json`
    - `unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -c "capacitorjs/plugins/share"` prints >= 1 OR `adb shell dumpsys package com.openmusic.app | grep -c versionName` prints >= 1 (plugin classes are dexed — use the dumpsys check if the jar listing is not inspectable)
    - `adb shell ls /sdcard/Download/ | grep -c openmusic-backup-` prints 3
    - `adb shell pidof com.openmusic.app` prints a PID
    - The logcat background job is running and its output file exists in the scratchpad
  </acceptance_criteria>
  <done>Fresh APK with the share plugin installed on the booted AVD, three fixtures visible in Downloads, logcat capture running, app in the foreground.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-15 picker + import + reload + Undo (A4 probe) + damaged/newer refusals</name>
  <what-built>Settings → Data on the APK now has Import (hidden `<input type="file" accept="application/json,.json">`), Export, Undo last import, Re-download missing (N). Three fixtures are in /sdcard/Download/.</what-built>
  <how-to-verify>
    In the emulator window:
    1. Open Settings → Data. Note the current counts line. Tap **Import backup**. PASS: the Android document picker opens (no crash, app stays alive). In the picker, browse to Downloads. PASS: `openmusic-backup-2026-09-13.json` is selectable (NOT greyed out). Tap it.
    2. A native AlertDialog appears with the replace-confirmation text naming liked songs / playlists / downloads list / play history / search history / settings / name translations. Tap OK.
    3. PASS: a toast "Backup restored — reloading…" shows, the app reloads (splash may flash — note whether it felt jarring; do not redesign), and Settings → Data now shows counts `2 liked · 0 playlists · 0 downloads`; Library → Liked shows the two fixture songs.
    4. **A4 probe:** on that same Settings → Data screen after the reload, PASS: an **Undo last import** button is visible. (FAIL = sessionStorage did not survive `location.reload()` in the WebView — report "A4 FAIL"; the fallback is a one-argument swap to `localStorage` + a size pre-check at the three `sessionStorage` call-sites in `settings/data/+page.svelte`, to be done as a quick task — do not approve this checkpoint.)
    5. Tap **Undo last import** → OK the confirm. PASS: toast "Import undone — reloading…", reload, counts return to what you noted in step 1, and the Undo button is gone.
    6. Tap Import again, pick `openmusic-backup-damaged.json`. PASS: toast "This backup file is damaged and cannot be imported." — NO confirm dialog, NO reload, counts unchanged.
    7. Tap Import again, pick `openmusic-backup-newer.json`. PASS: toast "This backup was made by a newer version of the app…" — no confirm, no reload.
    8. Ask Claude to print `<scratchpad>/logcat-35.txt` — PASS: no `ArrayIndexOutOfBoundsException`, no `Uncaught` lines from the picker path.
  </how-to-verify>
  <resume-signal>Type "approved" (all 8 PASS), or "A4 FAIL" if step 4 failed, or describe which step failed and what you saw</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-16/D-17 share sheet, readable output, dismiss is not an error</name>
  <what-built>Native Export writes `openmusic-backup-YYYY-MM-DD.json` to the app cache (Directory.Cache, UTF-8) and hands a file:// URI to `@capacitor/share`, which the pre-existing `${applicationId}.fileprovider` exposes.</what-built>
  <how-to-verify>
    1. Settings → Data → tap **Export backup**. PASS: the Android share sheet opens and lists real targets (e.g. Files, Drive, Gmail, Bluetooth). If the target list looks unusually narrow or broad, note it (research A3: MIME for `.json` may be null on some API levels — degrades the chooser only).
    2. Choose **Files** (or "Save to Drive") and save the file. Then run, from the host: `adb shell ls /sdcard/Download/` (or wherever Files saved it) and `adb pull` the new file; `node -e "JSON.parse(require('fs').readFileSync(process.argv[1],'utf8')); console.log('parses')" <file>`. PASS: prints `parses`, and the file's top-level has `"app": "openmusic-backup"` and `"format": 1` and a `keys` object containing `openmusic:library:v1`.
    3. Tap **Export backup** again and DISMISS the sheet by tapping outside it / Back. PASS: the app shows NO "Export failed." toast (silence or nothing).
    4. Ask Claude to print `<scratchpad>/logcat-35.txt` again — PASS: no `FileProvider` "Failed to find configured root", no `SharePlugin` "only file urls are supported" errors.
    5. Optional: tap **Re-download missing (N)** with a real downloads list (N > 0 if you have downloads whose bytes are absent). PASS: the label switches to "Stop re-downloading", downloads proceed one at a time (watch the network in CDP or Activity log), and tapping again stops it and shows one summary toast.
  </how-to-verify>
  <resume-signal>Type "approved" (steps 1-4 PASS), or describe which step failed and any logcat lines</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app cache → share target | File leaves the sandbox via FileProvider content:// grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08 | Information disclosure | FileProvider | accept | Provider is `exported="false"`, `<cache-path>` only; verified unchanged in plan 03 |
| T-35-10 | Elevation / crash | Capacitor showFilePicker `validTypes[0]` | mitigate | `accept="application/json,.json"`; logcat grep for ArrayIndexOutOfBounds is part of the checkpoint |
| T-35-SC | Tampering | npm installs | n/a this plan | No installs |
</threat_model>

<verification>
- Both checkpoints approved with all PASS steps
- Logcat file contains no crash/FileProvider/SharePlugin error lines
</verification>

<success_criteria>
- D-15, D-16, D-17 device-verified; A4 answered (Undo visible after reload) or explicitly failed with the fallback queued
- No Android source edits were needed
</success_criteria>

<output>
Create `.planning/phases/35-one-click-export-and-import-of-all-app-data/35-05-SUMMARY.md` when done, recording the A4 outcome and any A3 chooser observation
</output>
