---
phase: 35-one-click-export-and-import-of-all-app-data
plan: 06
type: execute
wave: 3
depends_on: ["35-04"]
files_modified: []
autonomous: false
requirements: [D-14]
must_haves:
  truths:
    - "On a real iPhone (Safari), Export produces a downloadable/openable openmusic-backup-YYYY-MM-DD.json — the anchor click lands inside the tap gesture"
    - "On the same iPhone, Import via the file picker restores the exported file and reloads"
  artifacts: []
  key_links:
    - from: "iPhone Safari"
      to: "pnpm dev --host on LAN port 4321"
      via: "http://<mac-lan-ip>:4321/settings/data"
      pattern: "settings/data"
---

<objective>
Verify D-14 on the primary target: iOS Safari's `<a download>` + `URL.createObjectURL` behaviour is device-specific and research confidence there is LOW–MEDIUM (A1/A2). Serve the built code over the LAN from this Mac — NOT by pushing to `main`, which auto-deploys production.

Purpose: VALIDATION.md lists "Export downloads on a real iPhone" as a manual-only checkpoint.

Output: one signed-off checkpoint; observations on filename / preview behaviour recorded in the SUMMARY.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-VALIDATION.md
@.planning/phases/35-one-click-export-and-import-of-all-app-data/35-04-SUMMARY.md
@CLAUDE.md

<interfaces>
- Dev server: `pnpm dev -- --host` (Vite strictPort 4321; `--host` exposes on the LAN). LAN IP: `ipconfig getifaddr en0` (fall back to `en1`).
- Deploy caution: `openmusic-pushes-autodeploy-live` / `something-auto-pushes-main` — do NOT push; a LAN dev server is the verification channel.
- Sandbox: CN upstreams (netease/qq) are blocked here but kuwo + Deezer work; export/import does not need any upstream.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve the app on the LAN and print the iPhone URL</name>
  <files></files>
  <read_first>
    - package.json (scripts `dev`), vite.config.ts (strictPort 4321)
    - .planning/phases/35-one-click-export-and-import-of-all-app-data/35-RESEARCH.md §Answer 7 (iOS limitations 1-5 — what to observe)
  </read_first>
  <action>
    Confirm nothing is already listening on 4321 (`lsof -i :4321`); if the user's launch.json dev server is running, reuse it (it must have been started with `--host` to be reachable from the phone — if not, ask the user to restart it with `--host` or stop it). Otherwise start `pnpm dev -- --host` in the background (run_in_background) and wait until `curl -s -o /dev/null -w '%{http_code}' http://localhost:4321/settings/data` prints 200. Compute the LAN IP with `ipconfig getifaddr en0` (or `en1`) and print `http://<ip>:4321/settings/data` for the checkpoint. Do NOT deploy, do NOT push.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' http://localhost:4321/settings/data | grep -q 200 && ipconfig getifaddr en0</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:4321/settings/data` prints 200
    - `ipconfig getifaddr en0` (or en1) prints a 192.168.x.x / 10.x.x.x address which is echoed in the checkpoint URL
    - `git status --porcelain` shows no new commits pushed (`git log origin/main..main` is unchanged by this task)
  </acceptance_criteria>
  <done>App reachable from the phone over the LAN; URL printed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-14 on a real iPhone — export downloads, import round-trips</name>
  <what-built>Web export path: `buildEnvelope` synchronously in the tap handler → `saveBlobToDisk` (hidden `<a download>` + object URL, revoked in `finally`) with MIME `application/json` and filename `openmusic-backup-YYYY-MM-DD.json`.</what-built>
  <how-to-verify>
    On an iPhone on the same Wi-Fi, in Safari, open the URL Claude printed (http://<ip>:4321/settings/data). Like one or two songs first if the library is empty so the export is non-trivial.
    1. Tap **Export backup**. PASS: Safari shows the download arrow / a "Download" prompt, and the file appears in Files → Downloads as `openmusic-backup-<today>.json` (or Safari opens a JSON preview tab — that is ACCEPTED per research A2; note which happened, and whether the name was `Unknown`). FAIL: nothing happens at all (that would indicate the anchor click fell outside the gesture — report exactly).
    2. Open the saved file (Files app or the preview). PASS: readable, pretty-printed JSON beginning with `"app": "openmusic-backup"`.
    3. Settings → Data → tap **Clear library** → OK, then tap **Import backup**, pick the exported file from Files. PASS: the confirm dialog appears, then "Backup restored — reloading…", the page reloads, and the liked songs are back.
    4. PASS: **Undo last import** is visible after the reload (sessionStorage survived the reload in iOS Safari). Tap it → OK → the library is empty again after the reload.
    5. Optional: add the page to the Home Screen (PWA) and repeat step 1 from the installed PWA — note any difference.
  </how-to-verify>
  <resume-signal>Type "approved" (steps 1-4 PASS) with a one-line note on step 1's behaviour (download vs preview, filename), or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN dev server | Unauthenticated dev server on the local network for the duration of the check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-11 | Information disclosure | `pnpm dev --host` | accept | Local Wi-Fi only, stopped after the checkpoint; no secrets in the client bundle |
| T-35-SC | Tampering | npm installs | n/a this plan | No installs |
</threat_model>

<verification>
- Checkpoint approved; step-1 behaviour recorded (download vs preview, filename fidelity)
- No push to `main` occurred during this plan
</verification>

<success_criteria>
- D-14 verified on the primary target device without deploying
</success_criteria>

<output>
Create `.planning/phases/35-one-click-export-and-import-of-all-app-data/35-06-SUMMARY.md` when done, recording A1/A2 observations
</output>
