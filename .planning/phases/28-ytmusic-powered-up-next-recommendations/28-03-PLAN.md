---
phase: 28-ytmusic-powered-up-next-recommendations
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - src/lib/services/picks.ts
  - src/lib/services/picks.test.ts
  - src/lib/stores/player.svelte.ts
  - src/lib/stores/player.svelte.test.ts
autonomous: true
requirements: [UPNEXT-FB-01, UPNEXT-YT-03]

must_haves:
  truths:
    - "regenerate() empty branch calls buildTopHitsQueue and logs upnext.source via:'top-hits' (was 'diverse')"
    - "ensureAhead() empty branch calls buildTopHitsQueue and logs upnext.source via:'top-hits'"
    - "buildTopHitsQueue makes ONE chart call at build (no per-item searchAll fan-out) → kuwo-first name-stubs, excluding current queue uids"
  artifacts:
    - path: "src/lib/services/picks.ts"
      provides: "buildTopHitsQueue(count, excludeUids) from getChartTopTracks → name-stubs (Deezer chart no-key fallback); buildDiversePicks RETAINED for the home page"
      contains: "export async function buildTopHitsQueue"
    - path: "src/lib/services/picks.test.ts"
      provides: "buildTopHitsQueue unit tests (one chart call, uid-exclude, never-throw)"
    - path: "src/lib/stores/player.svelte.ts"
      provides: "both empty-branch call sites swapped buildDiversePicks → buildTopHitsQueue; via union +'top-hits'"
      contains: "buildTopHitsQueue"
    - path: "src/lib/stores/player.svelte.test.ts"
      provides: "picks mock + via assertions updated to 'top-hits'"
  key_links:
    - from: "src/lib/services/picks.ts (buildTopHitsQueue)"
      to: "getChartTopTracks"
      via: "one Last.fm chart call → DiscoveryTrack[] → name-stubs"
      pattern: "getChartTopTracks"
    - from: "src/lib/stores/player.svelte.ts (regenerate + ensureAhead empty branch)"
      to: "buildTopHitsQueue"
      via: "replaces buildDiversePicks; logs via:'top-hits'"
      pattern: "buildTopHitsQueue"
---

<objective>
Replace the random `ARTIST_POOL` last-resort up-next fallback with a real top/chart-hits fallback. Add
`buildTopHitsQueue(count, excludeUids)` to `picks.ts` (one `getChartTopTracks` call → kuwo-first name-stubs,
Deezer chart as the no-key fallback) and swap BOTH player empty-branch call sites (`regenerate()` and
`ensureAhead()`) from `buildDiversePicks(8, …)` to `buildTopHitsQueue(8, …)`, logging `via:'top-hits'`
(was `'diverse'`). When "generate by similar songs" is genuinely dry, the queue fills with broadly-popular
real hits instead of a random hard-coded artist pool.

Purpose: Requirement UPNEXT-FB-01 (user-directed). `buildDiversePicks` and its `ARTIST_POOL` are RETAINED —
the home-page fallback grid (`+page.svelte:497`) still uses them; only the player's two empty branches switch.
Output: `buildTopHitsQueue`; both call-site swaps + via union `'top-hits'`; picks tests + updated player tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ytmusic-powered-up-next-recommendations/28-RESEARCH.md
@.planning/phases/28-ytmusic-powered-up-next-recommendations/28-02-SUMMARY.md

CRITICAL constraints:
- `buildTopHitsQueue` = ONE chart call at build (no per-artist searchAll fan-out — the whole point vs the old
  8× `buildDiversePicks`). Map each chart item to a `resolveByName` name-stub (mainstream chart hits DO exist on
  kuwo, so kuwo-first lazy resolution works — unlike YT-only rows in 28-02). Over-fetch (e.g. count*2, min 20)
  so exclude/dedupe still yields `count`. Never-throw → `[]` (caller leaves the queue as-is, same as today).
- Fallback order: `getChartTopTracks` (Last.fm, needs optional LASTFM_KEY) → Deezer chart `/api/deezer/chart`
  (NO key, reachable in-sandbox) → `[]`. Do NOT chain back to buildDiversePicks (the whole point is to remove
  random-pool noise from the player path).
- Do NOT delete `buildDiversePicks` / `ARTIST_POOL` — the home page still imports them.
- The player via union already carries `'ytmusic-related'` from plan 28-02; ADD `'top-hits'` to both.
- Never-throw + full-suite-green (UPNEXT-YT-03).

<interfaces>
<!-- Reusable chart data path (src/lib/services/lastfm.ts:211) — never-throws → []: -->
```ts
export interface DiscoveryTrack { artist: string; title: string; image: string | null; mbid: string | null; }
export async function getChartTopTracks(limit?: number, page?: number): Promise<DiscoveryTrack[]>;
```

<!-- No-key fallback (src/routes/api/deezer/chart/+server.ts) — GET /api/deezer/chart?limit= → : -->
```ts
// { tracks: { artist, title, image, mbid:null }[], artists: [...] }   // reachable in-sandbox, no key
```

<!-- nameStub pattern to mirror (src/lib/services/similar.ts:129) — a resolveByName kuwo-first stub: -->
<!-- { uid:`${primary}:similar-${matchKey(artist,title)}`, source: primary (placeholder), songid:`similar-<key>`, -->
<!--   title, artist, album:'', cover: isHttps(image)?image:null, audioUrl:null, lrc:null, lrcUrl:null, -->
<!--   detailsLoaded:false, quality:null, qualityLabel:null, keyword:`${artist} ${title}`, displayIndex:1, resolveByName:true } -->
<!-- primary source id = getEnabledAdapters({})[0]?.id ?? 'kuwo' (kuwo-first floor); matchKey from $lib/services/match-key -->

<!-- Player call sites to swap (src/lib/stores/player.svelte.ts): -->
<!-- ensureAhead ~:2011-2014   if (!more.length) { more = await buildDiversePicks(8, have); via = 'diverse'; } -->
<!-- regenerate  ~:3077-3081   if (tail.length === 0) { tail = await buildDiversePicks(8, exclude); ... via = 'diverse'; } -->
<!-- import at :17  import { buildDiversePicks } from '$lib/services/picks';  (swap to buildTopHitsQueue) -->

<!-- Player test hooks (src/lib/stores/player.svelte.test.ts): -->
<!-- :33  vi.mock('$lib/services/picks', () => ({ buildDiversePicks: vi.fn(async () => []) })); -->
<!-- :83/:94  import { buildDiversePicks }; const mockPicks = vi.mocked(buildDiversePicks); -->
<!-- assertions referencing buildDiversePicks / via:'diverse': ~:668, :673-688, :919-940, :965-982, :1139 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildTopHitsQueue in picks.ts + picks.test.ts</name>
  <read_first>
    - src/lib/services/picks.ts (buildDiversePicks + ARTIST_POOL — the file to extend; keep them)
    - src/lib/services/similar.ts:91-153 (primarySourceId/onlyPrimarySource + nameStub — reuse the exact name-stub shape + kuwo-first primary + isHttps guard; consider exporting nameStub or replicating it with a shared comment)
    - src/lib/services/lastfm.ts:162-219 (DiscoveryTrack shape + getChartTopTracks — the one chart call)
    - src/routes/api/deezer/chart/+server.ts (the no-key Deezer chart fallback shape)
    - src/lib/services/match-key.ts (matchKey — synthetic uid identity for name-stubs)
    - .planning/phases/28-ytmusic-powered-up-next-recommendations/28-RESEARCH.md §Q5 (exact builder recipe + exclude semantics + the documented synthetic-uid dedup gap)
  </read_first>
  <action>
    In `src/lib/services/picks.ts` add `export async function buildTopHitsQueue(count: number,`
    `excludeUids: Set<string> = new Set()): Promise<Track[]>`. Step 1: `const hits = await`
    `getChartTopTracks(Math.max(count * 2, 20))` (over-fetch). Step 1b: if `hits.length === 0`, fall back to the
    Deezer chart — `apiFetch('/api/deezer/chart?limit=' + Math.max(count*2, 20))`, read `{ tracks }`, use those
    `{artist,title,image}` items instead (never-throw → treat a failure as empty). Step 2: map each item to a
    kuwo-first `resolveByName` name-stub — reuse the `nameStub(artist, title, image)` shape from similar.ts
    (export nameStub from similar.ts and import it, OR replicate it in picks.ts with a comment cross-referencing
    similar.ts:129; do NOT duplicate silently). Step 3: drop stubs whose synthetic uid is in `excludeUids`,
    dedupe by uid, `slice(0, count)`. Wrap in try/catch → `[]` (never-throw). Do NOT touch `buildDiversePicks`
    or `ARTIST_POOL`. Add a house-style comment tagging UPNEXT-FB-01 and the "real chart hits, not random pool"
    rationale + the documented synthetic-uid exclude gap (same as nameStub).

    Create `src/lib/services/picks.test.ts`. Mock `getChartTopTracks` (or the underlying fetch) and assert:
    (a) buildTopHitsQueue makes ONE chart call and returns `count` name-stubs each with `resolveByName===true`
    and a kuwo-first placeholder source; (b) a stub whose uid is in excludeUids is dropped; (c) duplicate chart
    items collapse to one uid; (d) an empty chart result → the Deezer-chart fallback is consulted (mock it) →
    stubs; (e) both dry → `[]`; (f) a thrown chart call → `[]` (never throws).
  </action>
  <verify>
    <automated>pnpm test -- src/lib/services/picks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function buildTopHitsQueue" src/lib/services/picks.ts` returns a match
    - `grep -n "getChartTopTracks" src/lib/services/picks.ts` returns a match (one chart call)
    - `grep -c "buildDiversePicks" src/lib/services/picks.ts` returns >= 1 (buildDiversePicks RETAINED, not deleted)
    - `pnpm test -- src/lib/services/picks.test.ts` exits 0 — one chart call, uid-exclude, dedupe, Deezer fallback, never-throw
    - No new `as any` (grep clean)
  </acceptance_criteria>
  <done>
    buildTopHitsQueue builds `count` kuwo-first name-stubs from a single chart call (Deezer no-key fallback),
    excludes current uids, and never throws — buildDiversePicks/ARTIST_POOL untouched. Proven by picks.test.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap both player empty-branch call sites + via 'top-hits' + update player tests</name>
  <read_first>
    - src/lib/stores/player.svelte.ts:17 (buildDiversePicks import), :2007-2018 (ensureAhead empty branch + via union + logAction), :3070-3085 (regenerate empty branch + via union + logAction)
    - src/lib/services/picks.ts (buildTopHitsQueue from Task 1 — same signature as buildDiversePicks: (count, excludeUids))
    - src/lib/stores/player.svelte.test.ts:32-33 (the picks vi.mock), :82-94 (imports + vi.mocked), and the buildDiversePicks / via:'diverse' assertions at ~:668, :673-688, :919-940, :965-982, :1139
    - .planning/phases/28-ytmusic-powered-up-next-recommendations/28-RESEARCH.md §Q5 (drop-in wiring: exact lines) + Pitfall 4 (stale player mocks)
  </read_first>
  <action>
    In `src/lib/stores/player.svelte.ts`: change the import at :17 to import `buildTopHitsQueue` from
    `$lib/services/picks` (replace `buildDiversePicks`; the player no longer calls buildDiversePicks, so remove
    that import to avoid an unused import). In `ensureAhead()` (~:2011-2014) change the empty branch to
    `more = await buildTopHitsQueue(8, have); via = 'top-hits';`. In `regenerate()` (~:3077-3081) change the
    empty branch to `tail = await buildTopHitsQueue(8, exclude); via = 'top-hits';` (keep the WR-06 queueGen
    re-check after the await). Add `'top-hits'` to BOTH local `via` unions (~:2007, :3070) — they already carry
    `'ytmusic-related'` from plan 28-02, so the unions become
    `'similar' | 'artist' | 'lastresort' | 'empty' | 'diverse' | 'ytmusic-related' | 'top-hits'`. Keep the
    existing `logAction('upnext.source', { via, count })` calls (now they emit `via:'top-hits'`). Update the
    surrounding comments to reflect top-hits (not diverse) as the empty-similar net. Do NOT otherwise change
    control flow.

    In `src/lib/stores/player.svelte.test.ts`: extend the picks `vi.mock` (:33) so it exports BOTH
    `buildDiversePicks` and `buildTopHitsQueue` as `vi.fn(async () => [])` (buildDiversePicks stays because
    other modules import it; the player now uses buildTopHitsQueue). Add `import { buildTopHitsQueue }` (:83)
    and `const mockTopHits = vi.mocked(buildTopHitsQueue)` (:94). Update every empty/dry-fallback assertion that
    referenced `buildDiversePicks` / `via:'diverse'` on the PLAYER path (~:668 "not called", :673-688 ensureAhead
    dry fallback, :919-940 regenerate empty safety net, :965-982 WR-06 safety-net guard, :1139) to use
    `mockTopHits` and `via:'top-hits'`. Do not weaken the WR-06 queueGen-guard assertions — just retarget the fn.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/stores/player.svelte.test.ts && pnpm test && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildTopHitsQueue" src/lib/stores/player.svelte.ts` returns >= 3 (import + both call sites)
    - `grep -c "buildDiversePicks" src/lib/stores/player.svelte.ts` returns 0 (player no longer calls it)
    - `grep -c "'top-hits'" src/lib/stores/player.svelte.ts` returns >= 2 (both via unions/assignments)
    - `grep -n "buildTopHitsQueue" src/lib/stores/player.svelte.test.ts` returns matches (mock + mocked + assertions)
    - `pnpm test` exits 0 (full suite green — regenerate/ensureAhead now assert via:'top-hits'); `pnpm check` exits 0
  </acceptance_criteria>
  <done>
    Both player empty branches call buildTopHitsQueue and log `via:'top-hits'`; buildDiversePicks is gone from
    the player (retained for the home page); the full suite + typecheck are green with the updated mocks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client service → GET /api/lastfm/discovery (chart) + /api/deezer/chart | own-origin edge calls for the top-hits list |
| chart name-stubs → resolveNameStub (on play) | name-stubs resolve kuwo-first (ytmusic excluded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Denial of Service | chart endpoint error / empty / no LASTFM_KEY | mitigate | `getChartTopTracks` never-throws → `[]`; `buildTopHitsQueue` chains to the no-key Deezer chart then never-throws → `[]`; the player caller leaves the queue as-is (never-stop invariant holds). |
| T-28-03-02 | Tampering | name-stub resolves to the wrong track on play | accept | mainstream chart hits exist on kuwo → kuwo-first resolution is reliable; `resolveNameStub` excludes ytmusic; the synthetic-uid exclude/dedup gap is bounded and documented (identical to the existing `nameStub` behavior, 28-RESEARCH §Q5). |
| T-28-03-03 | Information Disclosure | LASTFM_KEY leak | mitigate | the key stays server-side in `/api/lastfm/discovery`; the client only ever sees the clean `DiscoveryTrack[]` shape (existing Phase-26 posture, unchanged). |
| T-28-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Phase installs NO packages (28-RESEARCH Package Legitimacy Audit: N/A). No install task exists. |
</threat_model>

<verification>
- `pnpm test -- src/lib/services/picks.test.ts src/lib/stores/player.svelte.test.ts` — builder + player green.
- `pnpm test` — full suite (~1320 tests) green (player empty branches now assert `via:'top-hits'`).
- `pnpm check` — svelte-check clean (widened via unions; no unused buildDiversePicks import; no `as any`).
- Manual (device / deployed — CN upstreams unreachable in-sandbox): a genuinely-dry similar result grows the
  queue with real chart hits; Activity log shows `upnext.source: top-hits` (was `diverse`).
</verification>

<success_criteria>
- `buildTopHitsQueue` builds a queue from ONE chart call (Deezer no-key fallback), excludes current uids, never
  throws; `buildDiversePicks`/`ARTIST_POOL` retained for the home page.
- Both player empty branches call `buildTopHitsQueue` and log `via:'top-hits'`; no `buildDiversePicks` call
  remains in the player.
- `pnpm test` + `pnpm check` clean.
</success_criteria>

<output>
Create `.planning/phases/28-ytmusic-powered-up-next-recommendations/28-03-SUMMARY.md` when done.
</output>
