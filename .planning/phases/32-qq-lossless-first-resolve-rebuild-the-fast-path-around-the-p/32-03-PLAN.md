---
phase: 32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/qq-lossless-first-resolve.md
autonomous: false
requirements: [D-10b]
must_haves:
  truths:
    - "Real-world tang detail RTT from a mobile client on the target network is MEASURED and recorded before implementation finishes — not discovered at verification time"
    - "If the measured floor is ≥1s, the phase's headline is restated as 'lossless by default with no ADDED latency' rather than an absolute sub-second number"
  artifacts:
    - path: ".planning/notes/qq-lossless-first-resolve.md"
      provides: "an appended 'Real-device RTT (Phase 32 checkpoint)' section with the measured numbers"
      contains: "Real-device RTT"
  key_links: []
---

<objective>
Front-load the RTT reality check (orchestrator directive #7; 32-VALIDATION § Human-Verify
Checkpoints; research Open Question #1). Every latency number the phase was planned on
(2.0–3.8s direct, 3.9–4.7s proxied) was measured US-sandbox → CN hosts. Whether a real user
near the upstream sees 300ms or 3s decides whether the ROADMAP's "tap→audio under a second" is
reachable AT ALL — per 32-D-10b it is reachable only on the D-08 path (mid already in hand), and
only if the real tang RTT is sub-second.

Purpose: cheap to measure, and it can invalidate the phase's headline goal. Measure it in wave 1.
Output: measured numbers appended to the phase's measurement note, plus a goal restatement if needed.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/notes/qq-lossless-first-resolve.md
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-RESEARCH.md (Decisions At Risk #2, Open Questions #1, Assumptions Log A8)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Measure real-device tang RTT on the target network (32-D-10b / A8)</name>
  <action>Pause for the user to time the direct tang detail call and the proxied /api/qq/detail call from a real phone on wifi and cellular; the numbers decide whether the ROADMAP's "tap→audio under a second" is reachable and feed Task 2's recorded verdict.</action>
  <what-built>Nothing yet — this is a pre-implementation measurement the whole phase's headline depends on (32-D-10b / research A8). It needs a real phone on the network you actually listen on; the sandbox cannot produce it.</what-built>
  <how-to-verify>
    On your phone (the network you normally stream on — do it once on wifi and once on cellular if easy):
    1. Open this URL in the phone browser and note roughly how long it takes to render the JSON:
       https://tang.api.s01s.cn/music_open_api.php?type=json&mid=0039MnYb0qxYhV
       (For a precise number, open devtools remote-inspect or use any HTTP-timing app; a stopwatch
       count on the visible load is acceptable — we need the ORDER of magnitude: <1s vs 1-2s vs 2s+.)
    2. Same for the proxied path: https://openmusic.lol/api/qq/detail?type=json&mid=0039MnYb0qxYhV
    3. Reload each once more (warm) and note the second number too.
    Report back: direct cold/warm, proxied cold/warm, and which network(s) you were on.
  </how-to-verify>
  <resume-signal>Reply with the four timings (approximate is fine) and the network type, or "cannot measure now" to defer with the phase headline treated as UNVERIFIED.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the measurement and, if needed, restate the headline</name>
  <files>.planning/notes/qq-lossless-first-resolve.md</files>
  <read_first>.planning/notes/qq-lossless-first-resolve.md (the existing "Measured" table whose caveat this discharges)</read_first>
  <action>
    Append a section `## Real-device RTT (Phase 32 checkpoint, {date})` to the note with the
    reported numbers in the same table style as the existing measurements, and one verdict line:
    either "sub-second direct RTT confirmed — the D-08 path can meet 'tap→audio under a second'"
    or "floor is {N}s — the phase goal is restated as: lossless by default with NO ADDED latency
    versus Phase 31, absolute sub-second is not reachable on any path (32-D-10b)". If restated,
    also note it in this plan's SUMMARY so /gsd:verify-work reads the restated criterion instead
    of failing the phase on the unreachable absolute number. Do not edit ROADMAP.md here.
  </action>
  <verify>
    <automated>grep -c "Real-device RTT" .planning/notes/qq-lossless-first-resolve.md</automated>
  </verify>
  <acceptance_criteria>
    - The note contains the new section with at least the direct-path timing and the network type
    - A one-line verdict states which reading of the phase goal verification must use
  </acceptance_criteria>
  <done>The RTT reality is on record in wave 1; verification has an explicit, measured success criterion</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone → tang upstream | read-only GET of a public endpoint; no credentials involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Info Disclosure | phone IP visible to tang/Tencent during the probe | accept | 32-D-14 — the CDN already sees every listener's IP on every play today |
| T-32-SC | Tampering | package installs | n/a | zero packages installed this phase |
</threat_model>

<verification>
- The measurement section exists in the note and names a verdict.
</verification>

<success_criteria>
- Real-device tang RTT recorded; the phase's headline goal is either confirmed reachable via D-08 or explicitly restated — before, not after, the implementation waves finish.
</success_criteria>

<output>
Create `.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-03-SUMMARY.md` when done
</output>
