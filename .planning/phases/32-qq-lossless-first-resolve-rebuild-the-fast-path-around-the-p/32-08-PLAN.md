---
phase: 32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p
plan: 08
type: execute
wave: 4
depends_on: [32-07]
files_modified: []
autonomous: false
requirements: [D-02, D-03, D-05, D-15, D-18]
must_haves:
  truths:
    - "On a real Android device: wifi under 'auto' plays lossless (LOSSLESS badge, FLAC url in the Activity log); cellular under 'auto' plays the hq tier — and iOS/desktop getting '320' is confirmed as the DOCUMENTED D-03 tradeoff, not filed as a bug"
    - "The rebuilt APK plays via the direct tang call on-device (no TypeError, no proxied /api/qq/detail on the happy path)"
    - "A locked Android device still advances to the next track under the 24 MB ceiling (the bg-lockscreen-stall-noskip case D-15 exists to preserve)"
    - "None of the three freeze classes regressed: nowbar-freeze-reresolve-loop, api-fetch-flood-freeze, restore-effect-self-invalidation-loop (VALIDATION gate #8)"
    - "iOS FLAC decode and the accom=伴奏 inference are discharged by listening (research assumptions A1/A3)"
  artifacts: []
  key_links: []
---

<objective>
Discharge the device-only verification: the four VALIDATION human-verify checkpoints (minus the
tang RTT one, done in wave 1 by plan 32-03) plus the freeze-class regression sweep. Everything
here needs a real network stack, a real lock screen, or human ears — none of it is automatable
under the node-only Vitest project (research § Manual-only verification).

Purpose: closes VALIDATION gate #8 and assumptions A1/A2/A3; produces the on-device evidence
/gsd:verify-work will read.
Output: recorded pass/fail per checkpoint in the SUMMARY.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-VALIDATION.md (§ Human-Verify Checkpoints, § Phase-Specific Gates #8)
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-RESEARCH.md (§ Assumptions Log A1/A2/A3, § Manual-only verification)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Android device sweep — auto tier, APK direct call, lock-screen advance, freeze classes</name>
  <action>Pause for on-device verification of D-02/D-03 tier selection, the D-13 APK direct call, the D-15 lock-screen advance, the D-05 https audit and the gate-#8 freeze sweep, per the numbered steps below.</action>
  <what-built>The full QQ-lossless-first resolve on Android: 'auto' tier detection (Android is the ONE platform whose WebView reports NetworkInformation.type), the direct tang call in the rebuilt APK, the 24 MB prebuffer ceiling, and the three freeze-class protections the phase's changes touch.</what-built>
  <how-to-verify>
    On a real Android device (install the fresh android/app/build/outputs/apk/debug/app-debug.apk from plan 32-07, and/or use Chrome on the deployed site):
    1. AUTO TIER — Settings → Playback quality must show Auto (the new default). On WIFI, play a
       popular CN track (e.g. 周杰伦 晴天): expect the LOSSLESS badge and, in Settings → Activity log,
       a resolved .flac isure6 URL. Turn wifi OFF (cellular): play another track — expect the HQ
       tier (193kbps m4a), not FLAC. (D-02/D-03. Note: iOS and desktop getting '320' under Auto is
       the DOCUMENTED D-03 tradeoff — do not report it as a failure.)
    2. APK DIRECT CALL — in the APK, tap a qq search result and confirm it plays (a pre-guard build
       would fail instantly with a URL TypeError). If it plays and the Activity log shows no
       fallback/error burst, the D-13 guard works end-to-end. (D-13/D-12.)
    3. LOCK-SCREEN ADVANCE — start a queue on cellular ('320' tier, under the ceiling), lock the
       phone, let the song finish: the NEXT song must start while locked (the
       bg-lockscreen-stall-noskip protection D-15 preserves). Then on wifi (lossless, OVER the
       ceiling → stream-not-blob), repeat once: the advance should still happen (it streams; a brief
       gap is acceptable, a stall/skip is not). (D-15.)
    4. FREEZE SWEEP (gate #8) — during ~10 minutes of mixed use (rapid next/prev taps, a few dead
       tracks if you can find them, background/foreground cycles), the app must never freeze; the
       Activity log must show no runaway re-resolve storm and no fetch-flood burst; the console (if
       remote-inspecting) must show none of the "updated at … restore … $effect" self-invalidation
       warnings. HTTPS check while you're there: every playing audio URL in the log starts https://
       (D-05 — a single http:// one means the upgrade missed a rung).
  </how-to-verify>
  <resume-signal>Reply per item: 1 pass/fail (+ which tier played on each network), 2 pass/fail, 3 pass/fail (both variants), 4 pass/fail — with any Activity-log anomalies quoted.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Listening checks — iOS FLAC decode (A1) and accom=伴奏 (A3)</name>
  <action>Pause for the two ear/codec checks the sandbox cannot run: manual-lossless FLAC playback on an iPhone, and a listen to a song_play_url_accom .ogg to settle whether it is the instrumental mix.</action>
  <what-built>The two listening/codec assumptions the research could not verify from a sandbox: iOS Safari FLAC decode (A1) and the accom-rung-is-the-instrumental inference (A3) behind the D-18 demotion.</what-built>
  <how-to-verify>
    1. iOS FLAC (A1) — on an iPhone (Safari or the PWA), manually set Settings → Playback quality to
       Lossless, play a popular CN track, confirm AUDIO IS AUDIBLE and seeking works. If it is
       silent/errors: do NOT change D-03 (iOS already defaults to '320'); record that D-03
       accidentally became load-bearing and file a todo for a FLAC-on-iOS guard on the manual
       'lossless' path.
    2. ACCOM = 伴奏 (A3) — take any qq detail body's song_play_url_accom URL (the Activity log or a
       direct tang detail call in a browser shows it; it is the O801….ogg one), open it on ANY
       platform that plays ogg (Android/desktop), and LISTEN: is it the instrumental/karaoke mix
       (no vocals)? Either answer keeps the D-18 demotion (the .ogg-on-iOS half justifies it alone)
       — this only settles whether the code comment can state 伴奏 as fact or must keep it as
       inference.
  </how-to-verify>
  <resume-signal>Reply: "iOS FLAC plays: yes/no" and "accom is instrumental: yes/no/couldn't check".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record checkpoint outcomes and discharge/file follow-ups</name>
  <files>(SUMMARY only)</files>
  <read_first>32-RESEARCH.md § Assumptions Log (A1-A3 discharge conditions)</read_first>
  <action>
    Write the per-item pass/fail table into this plan's SUMMARY as the phase's device evidence. If
    A3 confirmed 伴奏, update nothing in code now — note that qq.ts's 32-D-18 comment may drop the
    "inference" hedge in a future quick task. If iOS FLAC failed, file
    `.planning/todos/pending/ios-flac-manual-lossless.md` describing the guard needed on the manual
    'lossless' path (NOT a change to D-03's default). If any freeze-class item failed, STOP and
    report — that is a phase blocker routed back through /gsd:plan-phase --gaps, not a quick fix.
  </action>
  <verify>
    <automated>MISSING — device outcomes are human-reported; the SUMMARY table is the artifact (per 32-RESEARCH.md § Manual-only verification, these six behaviors are justified manual-only: no jsdom, no device in sandbox)</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a table with one row per checkpoint item and an explicit pass/fail each
    - Any failure has either a filed todo (with path) or a stated gap-closure recommendation
  </acceptance_criteria>
  <done>Device verification is on record; the phase is ready for /gsd:verify-work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester → deployed app / APK | verification only; no new code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-20 | DoS | freeze-class regression reaching users | mitigate | gate #8 sweep is blocking; a failure routes to gap closure, never ships |
| T-32-SC | Tampering | package installs | n/a | zero packages installed this phase |
</threat_model>

<verification>
- All checkpoint items answered; SUMMARY table complete; failures routed (todo or gap plan).
</verification>

<success_criteria>
- Android auto-tier, APK direct call, lock-screen advance, and freeze sweep verified on-device; A1/A3 discharged by listening; evidence recorded for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-08-SUMMARY.md` when done
</output>
