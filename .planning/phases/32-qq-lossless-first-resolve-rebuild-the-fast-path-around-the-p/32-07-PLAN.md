---
phase: 32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p
plan: 07
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-04, 32-05, 32-06]
files_modified:
  - android/app/build/outputs/apk/debug/app-debug.apk
autonomous: true
requirements: [D-10, D-13, D-16]
must_haves:
  truths:
    - "The full suite and typecheck are green across the assembled phase (all waves merged)"
    - "D-10's live cache behavior is verified against a REAL workerd Cache API (pnpm preview or the deployed URL) — never against pnpm dev, where edgeCache() silently no-ops (VALIDATION gate #3)"
    - "The phase's diff never touched player.svelte.ts lines 2892-3033 (32-D-16 — the post-resolve tail is measured and cleared)"
    - "A fresh APK exists containing the apiUrl guard — a pre-guard APK TypeErrors on the direct call, on the ONE platform (Android) that actually reports NetworkInformation.type (VALIDATION gate #6)"
  artifacts:
    - path: "android/app/build/outputs/apk/debug/app-debug.apk"
      provides: "post-guard native build for plan 32-08's on-device checkpoint"
  key_links:
    - from: "GET /api/resolve (preview/deployed)"
      to: "caches.default"
      via: "second identical GET returns {hit:true} with a songid mid and NO url field"
---

<objective>
Assemble-and-prove: run the full gate across the merged waves, verify D-10 against a real Cache
API runtime (the one thing unit shims cannot prove), audit the D-16 no-touch constraint
mechanically, and rebuild the APK so plan 32-08's device checkpoints test the post-guard binary.

Purpose: closes VALIDATION gates #3 and #6 and the D-16 review gate before any human time is spent.
Output: green gate, live-cache evidence, fresh APK.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-VALIDATION.md
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-RESEARCH.md (§ Environment Availability, § Runtime State Inventory)
@.planning/STATE.md (the JAVA_HOME build-environment gotcha)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate + D-16 diff audit</name>
  <files>(none — verification only)</files>
  <read_first>.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-VALIDATION.md (§ Phase-Specific Gates)</read_first>
  <action>
    Run `pnpm test && pnpm check` — full suite green, svelte-check 0 errors 0 warnings. Then the
    32-D-16 audit: list every phase commit touching the player store with
    `git log --oneline -- src/lib/stores/player.svelte.ts` (since the phase's first commit) and
    inspect each hunk via `git log -p --follow -- src/lib/stores/player.svelte.ts | grep '^@@'` —
    every hunk header must fall inside the prebufferNext region (~2610-2690) and NONE may overlap
    2892-3033 (the post-resolve tail: driveSrc + play + persist/MediaSession — measured and cleared;
    an executor "optimization" there is out of scope and must be reverted, not merged). Record the
    hunk list in the SUMMARY as the D-16 evidence.
  </action>
  <verify>
    <automated>pnpm test && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0; `pnpm check` reports 0 errors 0 warnings
    - The SUMMARY lists every player.svelte.ts hunk range for the phase and states none intersect 2892-3033
  </acceptance_criteria>
  <done>The merged phase is green and the D-16 constraint is evidenced, not assumed</done>
</task>

<task type="auto">
  <name>Task 2: Verify D-10 against a real Cache API (VALIDATION gate #3)</name>
  <files>(none — verification only)</files>
  <read_first>src/lib/proxy/edge-cache.ts (edgeCache() returns null when caches is undefined — the reason pnpm dev proves nothing), src/routes/api/resolve/+server.ts</read_first>
  <action>
    Build and serve under workerd: `pnpm build` then `pnpm preview` (wrangler pages dev against
    .svelte-kit/cloudflare) — NOT `pnpm dev`; under vite dev every cache path silently no-ops and
    "works" is a false pass. Against the preview origin:
    (1) `curl -s '<origin>/api/resolve?a=周杰伦&t=晴天'` — first call expected `{"hit":false}` (fill
    is out-of-band waitUntil); (2) after ~2-3s repeat the SAME GET — expect `{"hit":true, ...}` whose
    entry has `source:"qq"`, a non-empty `songid` (the permanent mid), NO `url` property, and
    `avail.qq === "ok"`; (3) verify the response header `Cache-Control: no-store` on both calls (the
    31-D-09 bug-prevention that must survive); (4) `curl -s -X POST '<origin>/api/resolve' --data
    '{"a":"周杰伦","t":"晴天"}' -H 'content-type: application/json'` then a fresh GET — expect
    `{"hit":false}` again (bust works — the D-10a repair path is live). If the sandbox blocks tang
    (research: tang WAS reachable from this sandbox 2026-08-31, only the qijieya Meting host is
    blocked), fall back to running the same four steps against the DEPLOYED https://openmusic.lol
    after `pnpm run deploy` (NEVER `pnpm deploy` — a pnpm builtin shadows it) — deploying is
    acceptable here because every wave is merged and green. Record all four transcripts in the
    SUMMARY. Also note the folded todo `edge-resolve-cache-returns-miss.md`: the old v=1
    `{"hit":false}` for 周杰伦/稻香 is explained by the entry-shape replacement — confirm 稻香 now
    fills under v=2 as part of step 2 and mark the todo dischargeable.
  </action>
  <verify>
    <automated>curl -s "$PREVIEW_ORIGIN/api/resolve?a=%E5%91%A8%E6%9D%B0%E4%BC%A6&t=%E6%99%B4%E5%A4%A9" | grep -c '"hit"'</automated>
  </verify>
  <acceptance_criteria>
    - A recorded second-GET transcript showing hit:true with songid present and NO url field, from pnpm preview or the deployed URL (the transcript must name which)
    - A recorded POST-then-GET transcript showing the bust returns the key to hit:false
    - Both GETs show Cache-Control: no-store on the ROUTE response
    - No verification claim in the SUMMARY cites pnpm dev
  </acceptance_criteria>
  <done>The permanent-mid cache is proven on a real workerd Cache API, including the retained bust repair</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the APK with the apiUrl guard (VALIDATION gate #6)</name>
  <files>android/app/build/outputs/apk/debug/app-debug.apk</files>
  <read_first>.planning/STATE.md (the JAVA_HOME/openjdk@21 gotcha), package.json (the apk script)</read_first>
  <action>
    Run `pnpm apk` (build:native → cap sync → gradle assembleDebug). On this machine Gradle's
    toolchain detection misses Homebrew's JDK — prefix the invocation with
    `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home` (documented in
    STATE.md; deliberately not committed). Sanity-check the guard is baked in: the built bundle
    under build/ (the SPA webDir) must contain the absolute-URL regex branch — e.g.
    `grep -rc "openmusic.lol" build/ >/dev/null` for the baked VITE_API_BASE and grep the bundled JS
    for the tang host string. Record APK path + size in the SUMMARY; the on-device play test is
    plan 32-08's checkpoint (a pre-guard APK TypeErrors on the direct call — Android is the one
    platform that reports NetworkInformation.type, so the native build is exactly where 'auto'
    lossless lives).
  </action>
  <verify>
    <automated>ls -la android/app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm apk` completes with BUILD SUCCESSFUL and app-debug.apk's mtime is newer than the last phase commit
    - The bundled JS contains the tang host (direct-call code shipped) and https://openmusic.lol (VITE_API_BASE baked)
  </acceptance_criteria>
  <done>A post-guard APK exists for the 32-08 device checkpoint</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification curl → preview/deployed edge | exercises the shared-cache write/bust paths live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-19 | Tampering | POST bust abuse (idempotent delete) | accept | worst case = a cold re-fill; rate limiting is a WAF concern by recorded decision (+server.ts) — verified unchanged in Task 2 |
| T-32-SC | Tampering | package installs | n/a | zero packages installed this phase |
</threat_model>

<verification>
- Task transcripts recorded in the SUMMARY; full suite + typecheck green; APK rebuilt.
</verification>

<success_criteria>
- Gate green; D-10 proven on workerd with bust repair; D-16 evidenced by hunk audit; fresh APK ready for device verification.
</success_criteria>

<output>
Create `.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-07-SUMMARY.md` when done
</output>
