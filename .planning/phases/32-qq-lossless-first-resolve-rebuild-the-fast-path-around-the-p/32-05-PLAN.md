---
phase: 32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p
plan: 05
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - src/lib/sources/__fixtures__/qq.detail.json
  - src/lib/sources/qq.ts
  - src/lib/sources/qq.test.ts
autonomous: true
requirements: [D-01, D-04, D-05, D-09, D-12, D-13, D-14, D-18, D-19]
must_haves:
  truths:
    - "A qq resolve is exactly ONE direct CORS GET to tang.api.s01s.cn given a mid — no msg param, no proxy hop on the hot path, and it stays a simple request (no headers, no credentials, no preflight)"
    - "Every returned qq audio URL is https:// — an http:// upstream URL is upgraded, an https:// one passes through unchanged — proven against an http:// fixture (VALIDATION gate #1)"
    - "The accom (伴奏 / .ogg) rung can never beat a real-song rung — sq/pq/hq/standard/fq all outrank it"
    - "The quality pill reports the tier the ladder actually picked; inferQualityFromUrl no longer relabels a positively-identified tier as 320K"
    - "A tang failure on the direct call falls back ONCE to the retained /api/qq/detail proxy, and a failure of both re-throws so detailsLoaded stays false and the cross-source ladder catches it (D-01's failure path intact)"
  artifacts:
    - path: "src/lib/sources/__fixtures__/qq.detail.json"
      provides: "http:// isure6-shaped tier URLs + song_size_*_str + real measured kbps values"
      contains: "http://isure6.stream.qqmusic.qq.com"
    - path: "src/lib/sources/qq.ts"
      provides: "effectiveQuality call site, https upgrade helper, accom demotion, tag-not-clobbered guard, mid-only direct resolve with proxy fallback"
  key_links:
    - from: "src/lib/sources/qq.ts"
      to: "src/lib/proxy/qq.ts"
      via: "qqProxy.buildUrl('detail', new URLSearchParams({ mid }), undefined) — one host definition"
      pattern: "qqProxy\\.buildUrl"
    - from: "src/lib/sources/qq.ts"
      to: "src/lib/services/api-base.ts"
      via: "apiFetch on BOTH the direct absolute URL and the proxy fallback (governor: dedupe + cap 8 + 25s + breaker)"
      pattern: "apiFetch\\("
    - from: "src/lib/sources/qq.ts"
      to: "src/lib/sources/quality.ts"
      via: "effectiveQuality(quality ?? settings.defaultQuality) before pickBestPlayUrl branches"
      pattern: "effectiveQuality\\("
---

<objective>
Rebuild `sources/qq.ts` into the phase's hot path: quality pref through the 32-01 seam (32-D-02),
corrected tier-claim comments (32-D-04 + Pitfall 2), `http:`→`https:` upgrade at the ladder
boundary (32-D-05), accom demotion (32-D-18), honest tier labels (32-D-19), `msg` stripped from
the detail call (32-D-09), and the detail call routed DIRECT to tang via `apiFetch` +
`qqProxy.buildUrl` with the proxied route retained as a one-shot fallback (32-D-12/32-D-13/32-D-14).

Purpose: with 32-02's rank swap, this file IS the click-to-play path for most plays. One direct
simple-request GET saves the measured ~1s proxy hop; the fixture change makes D-05 actually tested.
Output: rewritten adapter + realistic fixture + the full new test matrix from research Q6.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-CONTEXT.md
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-RESEARCH.md (§ Q4, § Q5, Pitfalls 1/2/5, § Code Examples "The https upgrade" and "The bad-mid failure shape")
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-PATTERNS.md (§ 1-4 — the analogs for every edit in this plan)
@src/lib/sources/qq.ts
@src/lib/proxy/qq.ts
@src/lib/sources/__fixtures__/qq.detail.json

<interfaces>
From src/lib/sources/quality.ts (created in plan 32-01):
  export function effectiveQuality(pref: DefaultQuality): Exclude<DefaultQuality, 'auto'>

From src/lib/services/api-base.ts (guarded in plan 32-02):
  apiUrl now returns absolute https URLs untouched; apiFetch(absoluteUrl, { signal }) is safe on both builds.

From src/lib/proxy/qq.ts (NO edit needed): qqProxy.buildUrl('detail', new URLSearchParams({ mid }), undefined)
  → 'https://tang.api.s01s.cn/music_open_api.php?type=json&mid=…' (msg omitted when absent — already correct).
  Import direction client→proxy for a pure buildUrl is the same direction resolve-edge.ts already uses;
  research A7: if svelte-check/bundling objects, hoist TANG_BASE to a shared const instead of duplicating the host.

From src/lib/services/lrc.ts: inferQualityFromUrl(url) — shared display helper, other sources depend on
its guess. Gate the CALL in qq.ts, do NOT modify the function (Pitfall 2's ponytail note).

Test harness (src/lib/sources/qq.test.ts): stubbed global fetch (mockFetchOnce), fixture-backed,
settings.defaultQuality pinned per-case in beforeEach/afterEach. apiFetch is REAL here (it wraps the
stubbed fetch), so any case that trips failures must call __resetGovernor() from api-base in afterEach.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the fixture real — http:// isure6 URLs, accom rung, sizes, measured kbps (VALIDATION gate #1)</name>
  <files>src/lib/sources/__fixtures__/qq.detail.json</files>
  <read_first>src/lib/sources/__fixtures__/qq.detail.json, src/lib/sources/qq.test.ts (every `detailFixture.` reference), 32-RESEARCH.md § Q6 breakage map row D-05 and § Wave 0 Gaps (fixture bullet)</read_first>
  <action>
    Rewrite the fixture to the REAL upstream shape from the live probe: every `song_play_url_*`
    becomes `http://isure6.stream.qqmusic.qq.com/<name>` (http, NOT https — the current https://dl.stream
    values are why an https-upgrade test would pass without the upgrade existing); filename shapes:
    sq/pq `F000….flac`, hq `M800….m4a`, standard `M500….m4a`, bare song_play_url `C400….m4a`. ADD
    `song_play_url_accom` with an `O801….ogg` filename and `kbps_accom: "689"`. ADD the exact
    byte-count keys `song_size_sq_str: 55397039`, `song_size_pq_str: 31168013`, `song_size_hq_str:
    6519764`, `song_size_standard_str: 3283546`, `song_size_fq_str: 1642440` (from the research live
    probe). Set measured kbps values: `kbps_sq: "1647"`, `kbps_hq: "193"`, `kbps_standard: "97"` so
    the ladder is tested against real numbers, not placeholders. Existing qq.test.ts assertions of
    the form `toBe(detailFixture.song_play_url_*)` remain green at this commit because they compare
    fixture-relative (both sides http) — Task 2 flips them to expect the https-upgraded value.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/sources/qq.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "http://isure6.stream.qqmusic.qq.com" src/lib/sources/__fixtures__/qq.detail.json` ≥ 6
    - `grep -c "https://" src/lib/sources/__fixtures__/qq.detail.json` = 0 among song_play_url_* values
    - `grep -c "song_size_sq_str" src/lib/sources/__fixtures__/qq.detail.json` = 1 and `grep -c "song_play_url_accom" …` = 1
    - qq.test.ts still passes unmodified at this commit
  </acceptance_criteria>
  <done>The fixture reproduces the real mixed-content-blocked upstream, so the D-05 upgrade is provable and the D-18/D-19 rungs have real data</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ladder rebuild — effectiveQuality call site, https upgrade, accom demotion, honest tier labels (32-D-02/D-04/D-05/D-18/D-19)</name>
  <files>src/lib/sources/qq.ts, src/lib/sources/qq.test.ts</files>
  <read_first>src/lib/sources/qq.ts (lines 88-143 pickBestPlayUrl + 260-274 the quality/label block), 32-RESEARCH.md § Code Examples "The https upgrade", Pitfalls 1 and 2, 32-PATTERNS.md § 1 and § 3</read_first>
  <behavior>
    - resolve() under pref 'lossless' returns audioUrl 'https://isure6.stream.qqmusic.qq.com/F000….flac' — https, from the http fixture (D-05 upgrade)
    - An already-https URL passes through unchanged (idempotency — assert on a one-off body with an https value)
    - With sq and pq absent from the body, hq wins over accom; with sq/pq/hq/standard/fq ALL absent, accom is last-resort before the bare song_play_url fallback (D-18)
    - Under pref '320' the picked tier is hq and track.quality/qualityLabel stay 'hq'/'HQ' — NOT relabelled '320k'/'320K' by inferQualityFromUrl (D-19: the m4a extension would relabel it today)
    - Under pref '128' song_play_url_standard is still promoted (existing behavior, existing test stays valid)
    - Under pref 'auto' in node (no navigator.connection → '320' via effectiveQuality) the hq rung is picked — the 'auto' path is exercised end-to-end through the adapter
  </behavior>
  <action>
    In pickBestPlayUrl: (1) resolve the pref FIRST — `const pref = effectiveQuality(quality ??
    settings.defaultQuality);` (import from './quality'), tagged 32-D-02; the 'auto'→'320' case then
    reuses the existing WR-03 '320'→hq branch with ZERO new branches (say so in the comment — the
    'auto' cellular case is literally the '320' promotion already written). (2) Add a private
    `https(url: string | null): string | null` helper — `url.replace(/^http:\/\//i, 'https://')` —
    and route EVERY return through the single return boundary rather than editing all seven rungs
    (one guard where every branch routes through, the blob-store MIN_BLOB_BYTES placement logic);
    32-D-05 comment: tang returns http:// isure6 URLs which are mixed-content-BLOCKED on our https
    origin; the same host serves https (verified live: 200+206, first bytes 0.31s); client-side and
    NOT proxy-side because 32-D-12 sends the hot call direct — a proxy-side upgrade would never fire
    where it matters; idempotent. (3) Move the `song_play_url_accom` rung to LAST among the named
    tiers (below fq, above the bare song_play_url fallback) — this satisfies 32-D-18's "below hq"
    with room to spare; comment: accom = 伴奏 (accompaniment/instrumental — MEDIUM-HIGH inference,
    human-verify in plan 32-08) AND it serves .ogg which iOS Safari does not decode (sufficient on
    its own); rung inherited verbatim from upstream index.html:2373, a copy-forward, not a choice.
    Give it an honest text label (e.g. text `ACCOM …` retained) rather than presenting as an 'HQ'
    quality tier. (4) 32-D-19: gate the inferQualityFromUrl call at qq.ts:268-274 — apply it ONLY
    when `!best.tag` (the ladder's own positively-identified tag must win over the URL sniff; the
    sniff relabels every non-FLAC as 320K including the 97kbps standard and 48kbps fq tiers). Do NOT
    modify inferQualityFromUrl itself (shared helper, other sources depend on it). (5) 32-D-04:
    rewrite pickBestPlayUrl's doc-block — the '128' rung selects song_play_url_standard MEASURED at
    97-98 kbps (not "~128kbps"), and the '320'/hq rung is MEASURED at 193 kbps m4a AAC on 3/3 probed
    tracks (not "~320k") — replace the stale claims in the WR-03 comment too; also fix the stale
    `"rather than the live default ('128')"` comment at qq.test.ts:88. Add/adjust tests per the
    behavior rows; each new it() has ≥1 expect.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/sources/qq.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - qq.test.ts asserts an https:// audioUrl against the http:// fixture (D-05 genuinely tested) plus an idempotency case
    - qq.test.ts has an accom-never-beats-hq case AND an accom-last-resort case
    - qq.test.ts has a tag-not-clobbered case: pref '320' → quality 'hq', qualityLabel 'HQ' (not '320K')
    - `grep -c "~320k" src/lib/sources/qq.ts` = 0 as a live claim; `grep -c "32-D-05\|32-D-18\|32-D-19" src/lib/sources/qq.ts` ≥ 3
    - `grep -n "effectiveQuality" src/lib/sources/qq.ts` shows the import and the single call site
    - `pnpm check` 0 errors 0 warnings
  </acceptance_criteria>
  <done>The ladder is lossless-first with honest labels, https-only output, and the karaoke rung defanged — all pinned against a realistic fixture</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve rebuild — mid-only direct call with retained proxy fallback (32-D-09/D-12/D-13/D-14)</name>
  <files>src/lib/sources/qq.ts, src/lib/sources/qq.test.ts</files>
  <read_first>src/lib/sources/qq.ts (lines 207-284 resolve()), 32-RESEARCH.md § Q4 in full (the two traps: no headers, no credentials) and § Q5 (failure shapes), 32-PATTERNS.md § 2 and § 4 (itunes-cover.ts + joox self-heal analogs)</read_first>
  <behavior>
    - resolve() with a mid calls fetch exactly once on success, with a URL matching /^https:\/\/tang\.api\.s01s\.cn\/music_open_api\.php\?/ containing type=json and mid=… and NO msg param (rewrites the qq.test.ts:146-150 proxy-prefix assertion — the one GOOD break in the map)
    - The fetch init carries NO headers property and NO credentials property (stays a simple request — a preflight measured 1.016s and would give back the ~1s D-12 saves; tang's Allow-Headers is Content-Type only so a custom header would also FAIL the preflight)
    - When the direct call rejects or returns an unusable body, resolve() makes a SECOND call to '/api/qq/detail?type=json&mid=…' (the retained proxy fallback, D-12) — assert both called URLs in order
    - When BOTH fail, resolve() re-throws and detailsLoaded stays false (the public throw contract is unchanged — catalog/allSettled and the never-stop ladder depend on it)
    - The `!d.song_mid` guard survives verbatim: an all-null 200 body (unknown mid) throws; it is NEVER replaced by a res.ok check (Pitfall 5 — vip is populated even on the all-null body and is not a liveness discriminator)
  </behavior>
  <action>
    In resolve(): (1) 32-D-09 — delete the `const msg = …` block and the msg param from the URL; the
    detail endpoint IGNORES msg (verified: mid alone returns the full ladder; a deliberately WRONG
    msg with the right mid returns the correct song). Replace the `(legacy:2312-2315)` ref with a
    32-D-09 ref recording the reversal and the verification — never silently delete the old ref. The
    SEARCH path (qq.ts:153) legitimately keeps msg — do not touch it; `track.qqSearchKey` stays
    WRITTEN at search time (removing the field is a wider Track change, out of scope). (2) 32-D-12 —
    build the direct URL via `qqProxy.buildUrl('detail', new URLSearchParams({ mid }), undefined)`
    (import from '$lib/proxy/qq'; per research A7 hoist TANG_BASE to a shared const only if
    svelte-check objects) and fetch it via `apiFetch(directUrl, { signal })` — 32-D-13: apiFetch not
    raw fetch, so the governor's dedupe/cap-8/25s-timeout/breaker cover the new host; comment that
    tang failures now count toward the ONE shared breaker (a tang outage tripping it is correct
    stop-hammering behavior; do NOT add a second per-host breaker — composing local bounds is the
    api-fetch-flood-freeze root cause; note it so a future debugger doesn't read "covers stopped
    loading" as a cover bug). (3) Structure: a private never-throw helper (the joox self-heal
    contract shape) that tries direct then falls back once to
    `apiFetch('/api/qq/detail?type=json&mid=' + encodeURIComponent(mid), { signal })`, returning the
    parsed QQDetailItem or null; the PUBLIC resolve keeps its throw contract (throws on null) — do
    not flip the public boundary. Keep `encodeURIComponent(mid)` on both paths (V5 ingress). (4)
    32-D-13/Q4 comments at the call: never set credentials:'include' (tang sends ACAO:* with no
    Allow-Credentials — a credentialed request hard-fails CORS; it also sends Set-Cookie, which
    invites exactly that "fix") and never add a request header. (5) 32-D-14 one-liner: direct
    metadata call adds a hostname, not a new exposure category — <audio src> already points at
    isure6. (6) Update the file's header comment block (the "calls the SAME-ORIGIN proxy" divergence
    bullet is now superseded for detail — record 32-D-12, proxy retained as fallback). Tests: rewrite
    qq.test.ts:146-150 to the absolute-host assertion; add not.toMatch(/[?&]msg=/) on the detail URL;
    add the fallback-order case (first fetch rejects → second URL is /api/qq/detail…), the
    both-fail-rethrows case, the no-headers/no-credentials init assertions, and keep the all-null
    body throw case; call __resetGovernor() in afterEach for failure-tripping cases.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/sources/qq.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "msg=" src/lib/sources/qq.ts` shows msg only on the SEARCH path (detail URL builds contain no msg)
    - qq.test.ts asserts calledUrl matches ^https://tang\.api\.s01s\.cn/ AND not.toMatch(/[?&]msg=/)
    - qq.test.ts asserts init has no headers and no credentials on the direct call
    - qq.test.ts fallback case asserts call[0] is the tang host and call[1] starts with /api/qq/detail
    - The `if (!d || typeof d !== 'object' || !d.song_mid) throw` guard is byte-present in qq.ts
    - `pnpm test` full suite green; `pnpm check` 0/0
  </acceptance_criteria>
  <done>QQ resolve is one direct simple-request call given a mid, with a working proxied fallback and the unchanged public throw contract feeding the never-stop ladder</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tang.api.s01s.cn (NEW direct hop) | untrusted third-party JSON parsed client-side; client IP now visible on metadata calls |
| client → isure6 CDN | audio URLs embedded from the untrusted body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | Info Disclosure | client IP to Tencent on the direct metadata call | accept | 32-D-14 — <audio src> already exposes it on every play; adds a hostname, not a category |
| T-32-12 | Tampering | plaintext http:// audio stream (mixed content / MITM) | mitigate | 32-D-05 https upgrade at the single ladder return boundary; tang sends HSTS max-age=31536000 |
| T-32-13 | Spoofing/Session | tang Set-Cookie + ACAO:* inviting credentials:'include' | mitigate | credentials never set (default same-origin → omitted cross-origin); guarded by comment + the no-credentials init test |
| T-32-14 | DoS | tang outage flooding the shared circuit breaker | accept | correct stop-hammering behavior; every caller degrades to a null sentinel; commented per Decisions At Risk #4; NO second breaker |
| T-32-15 | Input Validation | mid entering a URL | mitigate | encodeURIComponent(mid) kept on both direct and fallback paths |
| T-32-SC | Tampering | package installs | n/a | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/sources/qq.test.ts` per task; `pnpm test && pnpm check` 0/0 at plan end
- Grep gates from acceptance criteria (msg-free detail URL, https-only fixture assertions, no-credentials)
</verification>

<success_criteria>
- Detail resolve = one direct mid-only simple-request GET with a proxied one-shot fallback; ladder output is https-only, accom-demoted, honestly labelled; all pinned against an http:// fixture; suite + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-05-SUMMARY.md` when done
</output>
