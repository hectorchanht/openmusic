---
phase: 32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sources/quality.ts
  - src/lib/sources/quality.test.ts
  - src/lib/config/defaults.ts
  - src/lib/stores/settings.svelte.ts
  - src/lib/stores/settings.svelte.test.ts
  - src/lib/sources/kuwo.ts
  - src/lib/sources/kuwo.test.ts
  - src/lib/sources/joox.ts
  - src/lib/sources/joox.test.ts
autonomous: true
requirements: [D-02, D-03, D-04]
must_haves:
  truths:
    - "With defaultQuality 'auto' on a wifi connection (navigator.connection.type 'wifi'), every adapter resolves at the lossless tier"
    - "With 'auto' on cellular, saveData:true, or NO connection signal (iOS Safari, desktop Chrome, SSR), every adapter resolves at '320' — fail-closed"
    - "kuwo and joox never see the literal 'auto' pref — the single seam resolves it before any ladder runs"
    - "The stale '128–160k band' claim is corrected everywhere it appears (measured: 98 kbps)"
  artifacts:
    - path: "src/lib/sources/quality.ts"
      provides: "effectiveQuality() — the ONE 'auto' → concrete-tier resolution seam"
      exports: ["effectiveQuality", "pickByQualityPref"]
    - path: "src/lib/sources/quality.test.ts"
      provides: "wifi/ethernet/cellular/unknown/none/undefined/saveData/pass-through cases for effectiveQuality"
    - path: "src/lib/config/defaults.ts"
      contains: "defaultQuality: 'auto'"
  key_links:
    - from: "src/lib/sources/kuwo.ts"
      to: "src/lib/sources/quality.ts"
      via: "effectiveQuality(quality ?? settings.defaultQuality)"
      pattern: "effectiveQuality\\("
    - from: "src/lib/sources/joox.ts"
      to: "src/lib/sources/quality.ts"
      via: "effectiveQuality wrapped around the pickByQualityPref pref argument"
      pattern: "effectiveQuality\\("
---

<objective>
Give `'auto'` a real meaning (32-D-02/32-D-03): lossless on a positively-identified unmetered
connection, `'320'` everywhere else, resolved in ONE new function that all three pref-reading
adapters call. Flip `PLAYBACK_DEFAULTS.defaultQuality` from `'128'` to `'auto'` and correct the
stale "128–160k band" decision record in all three places it appears (32-D-04).

Purpose: this is the decision that makes playback lossless by default. The single seam is the
difference between D-02 working and D-02 silently shipping a cellular-data regression on kuwo and
joox (32-VALIDATION gate #2).
Output: `effectiveQuality()` in `sources/quality.ts`, three one-line adapter call sites (qq's is
deferred to plan 32-05 which owns `qq.ts`), the default flip, corrected comments, updated tests.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-CONTEXT.md
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-RESEARCH.md (§ Q2, § Pattern 1, § Code Examples "The 'auto' resolution")
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-PATTERNS.md (§ 1, § 6, § 11)
@src/lib/sources/quality.ts
@src/lib/config/defaults.ts

<interfaces>
Existing, extracted from the codebase — use directly, no exploration:

From src/lib/stores/settings.svelte.ts:
  export type DefaultQuality = 'auto' | 'lossless' | '320' | '128';   // union already includes 'auto'
  defaultQuality = $state<DefaultQuality>(PLAYBACK_DEFAULTS.defaultQuality);   // line 158

From src/lib/sources/quality.ts:
  export function pickByQualityPref(tiers: string[], pref: DefaultQuality): string[]

Call sites that read the raw pref today:
  src/lib/sources/kuwo.ts:104   const level = (quality ?? settings.defaultQuality) === '128' ? '128k' : 'zp';
  src/lib/sources/joox.ts:155   const order = pickByQualityPref(JOOX_QUALITY_ORDER, quality ?? settings.defaultQuality);
  (src/lib/sources/qq.ts:102 is the third — owned by plan 32-05, do NOT touch qq.ts here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add effectiveQuality() to sources/quality.ts with full test coverage</name>
  <files>src/lib/sources/quality.ts, src/lib/sources/quality.test.ts</files>
  <read_first>src/lib/sources/quality.ts, src/lib/sources/quality.test.ts, 32-RESEARCH.md § Q2 (the annotated recommended implementation), 32-PATTERNS.md § 11 (guard-style and no-`as any` narrowing precedents)</read_first>
  <behavior>
    - effectiveQuality('auto') with navigator.connection {type:'wifi'} → 'lossless'
    - effectiveQuality('auto') with {type:'ethernet'} → 'lossless'
    - effectiveQuality('auto') with {type:'cellular'} → '320'
    - effectiveQuality('auto') with {type:'unknown'}, {type:'other'}, {type:'none'} → '320' (whitelist fails closed)
    - effectiveQuality('auto') with navigator.connection undefined → '320' (32-D-03: iOS Safari / desktop Chrome)
    - effectiveQuality('auto') with {type:'wifi', saveData:true} → '320' (explicit user opt-in wins)
    - effectiveQuality('lossless') / ('320') / ('128') → returned unchanged regardless of connection (pass-through)
  </behavior>
  <action>
    Add `export function effectiveQuality(pref: DefaultQuality): Exclude<DefaultQuality, 'auto'>`
    to `src/lib/sources/quality.ts`, per 32-D-02/32-D-03. Logic: non-'auto' pref returns unchanged;
    then a WHITELIST that fails closed to '320' — read `navigator.connection` via a local structural
    interface `NetInfo { type?: 'bluetooth'|'cellular'|'ethernet'|'none'|'wifi'|'wimax'|'other'|'unknown'; saveData?: boolean }`
    and the narrowing `(navigator as Navigator & { connection?: NetInfo }).connection` — NEVER `as any`
    (repo has zero in production source; follow the `edge-cache.ts` local-interface precedent).
    Guard style: `typeof navigator === 'undefined'` early-return '320' — the `player.svelte.ts:1143`
    mediaSession-style feature-detect, NOT `import { browser }`, because this file's doc-block contract
    is "NO runes, NO $app, NO store import" and the typeof probe satisfies CLAUDE.md's
    "(or feature-detect)" clause; say so in the comment. Return 'lossless' only when
    `c.type === 'wifi' || c.type === 'ethernet'` and `c.saveData !== true`; everything else '320'.
    Doc-block must record (tagged 32-D-02/32-D-03): the MDN BCD support matrix (type is Chrome
    Android 38+/WebView 50+ only, FALSE on Safari/iOS, ChromeOS-only on desktop Chrome — so iOS AND
    desktop web get '320' under the default, a DELIBERATE accepted tradeoff, NOT a bug to fix), and
    that `effectiveType` is deliberately NOT consulted (it estimates speed, not metering; fast
    cellular reports '4g'). Update `pickByQualityPref`'s "'lossless' / 'auto' → leave the ladder
    as-is" comment to note that adapters now pre-resolve 'auto' through effectiveQuality (32-D-02),
    so the 'auto' branch here is a tolerated legacy input, not the live path — do NOT change
    pickByQualityPref's behavior (the existing "pref 'auto' returns the input order unchanged" tests
    at quality.test.ts:59-72 must stay green untouched).
    Tests: new `describe('effectiveQuality (32-D-02/32-D-03)')` in quality.test.ts using
    `vi.stubGlobal('navigator', { connection: {...} })` per case and `vi.unstubAllGlobals()` in
    afterEach; every `it()` has ≥1 expect (vitest requireAssertions).
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/sources/quality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" src/lib/sources/quality.ts` returns 0
    - `grep -c "effectiveQuality" src/lib/sources/quality.ts` ≥ 2 (declaration + doc mention)
    - quality.test.ts covers all 7 behavior rows above; `pnpm vitest --run src/lib/sources/quality.test.ts` passes
    - The pre-existing pickByQualityPref tests (including both 'auto' no-op cases) pass UNMODIFIED
    - Return type is `Exclude<DefaultQuality, 'auto'>` (compiler proves no adapter can see 'auto')
    - `pnpm check` reports 0 errors 0 warnings
  </acceptance_criteria>
  <done>effectiveQuality exists, is fully tested including the fail-closed and saveData cases, and quality.ts stays a pure node-testable .ts</done>
</task>

<task type="auto">
  <name>Task 2: Flip the default to 'auto' and correct the stale 128–160k decision record (both sites this plan owns)</name>
  <files>src/lib/config/defaults.ts, src/lib/stores/settings.svelte.ts, src/lib/stores/settings.svelte.test.ts</files>
  <read_first>src/lib/config/defaults.ts (lines 81-89), src/lib/stores/settings.svelte.ts (lines 150-160), src/lib/stores/settings.svelte.test.ts (lines 15-25), 32-CONTEXT.md D-02/D-04</read_first>
  <action>
    In `defaults.ts` change `defaultQuality: '128' as DefaultQuality, // D-03 — 128–160k band for fast resolve`
    to `defaultQuality: 'auto' as DefaultQuality` with a `32-D-02` comment: 'auto' = lossless on a
    positively-identified unmetered connection, '320' otherwise (see effectiveQuality); and a
    `32-D-04` correction noting the old '128' rung actually selected `song_play_url_standard`,
    MEASURED at 98 kbps — below the band the old D-03 comment claimed. Do not delete the old D-03
    ref silently — the new comment must name what it supersedes (house rule: decision-ref comments
    are replaced with a ref that says why, never removed).
    In `settings.svelte.ts:154-157` replace the "D-03: default to the 128–160k band…" comment above
    `defaultQuality = $state<...>` with a `32-D-02/32-D-04` comment (same content discipline).
    In `settings.svelte.test.ts:19-21` update `expect(settings.defaultQuality).toBe('128')` to
    `toBe('auto')` and rename the it/describe text — frame it in the test comment as a DECISION
    CHANGE (32-D-02 supersedes D-03), not a bug fix.
    Note: zero i18n work — the settings UI already ships the 'auto' option (`settings.optAuto`,
    settings/playback/+page.svelte:16); none of the 16 dictionaries change. Do not create i18n edits.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/stores/settings.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "defaultQuality: 'auto'" src/lib/config/defaults.ts` returns exactly 1 line
    - `grep -rn "128–160k" src/lib/config/defaults.ts src/lib/stores/settings.svelte.ts` returns 0 matches as a live claim (allowed only inside a "superseded" note)
    - settings.svelte.test.ts asserts `toBe('auto')` and passes
    - `git diff --stat` for this task touches no file under src/lib/i18n/
  </acceptance_criteria>
  <done>The shipped default is 'auto'; the wrong-bitrate decision record is corrected at both non-qq sites; the pinned-default test reflects the new decision</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route kuwo and joox through effectiveQuality (VALIDATION gate #2)</name>
  <files>src/lib/sources/kuwo.ts, src/lib/sources/joox.ts, src/lib/sources/kuwo.test.ts, src/lib/sources/joox.test.ts</files>
  <read_first>src/lib/sources/kuwo.ts (lines 95-110), src/lib/sources/joox.ts (lines 140-166), src/lib/sources/kuwo.test.ts, src/lib/sources/joox.test.ts, 32-RESEARCH.md § Pattern 1</read_first>
  <behavior>
    - joox: with settings.defaultQuality='auto' and NO navigator.connection stub (node = no signal → '320'), pickJooxPlayUrl probes the 320 band FIRST ('OGG 320'/'MP3 320'), NOT the Atmos/FLAC head of JOOX_QUALITY_ORDER — the exact cellular regression gate #2 exists to catch
    - joox: with 'auto' and a stubbed {type:'wifi'} connection, the verbatim lossless-first order is kept
    - kuwo: with 'auto' (no signal → '320') the detail call still uses level='zp'; with '128' it uses level='128k' — kuwo has no distinct 320 rung (pre-existing honesty gap, covered by defaultQualityNote; record in a comment, do not "fix")
  </behavior>
  <action>
    One-line change per adapter, both tagged 32-D-02:
    kuwo.ts:104 → `const level = effectiveQuality(quality ?? settings.defaultQuality) === '128' ? '128k' : 'zp';`
    joox.ts:155 → `const order = pickByQualityPref(JOOX_QUALITY_ORDER, effectiveQuality(quality ?? settings.defaultQuality));`
    Import `effectiveQuality` from `./quality` (joox already imports pickByQualityPref from there — no
    new import edge, no cycle risk). Add the behavior-row tests above to kuwo.test.ts and joox.test.ts
    following each file's existing fixture + stubbed-fetch + pin-settings-in-beforeEach/afterEach
    harness; restore settings.defaultQuality in afterEach exactly as qq.test.ts does.
    Do NOT touch qq.ts (owned by plan 32-05, which makes the identical one-line change there).
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/sources/kuwo.test.ts src/lib/sources/joox.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "effectiveQuality" src/lib/sources/kuwo.ts` = 2 (import + call) and same for joox.ts
    - `grep -n "settings.defaultQuality" src/lib/sources/kuwo.ts src/lib/sources/joox.ts` shows the pref is ALWAYS wrapped in effectiveQuality (no raw read reaches a ladder)
    - New joox test proves 'auto' + no signal probes a 320-band tier first (assert on the first probed URL or the reordered array), with ≥1 expect per it()
    - Full suite green: `pnpm test` passes; `pnpm check` 0 errors 0 warnings
  </acceptance_criteria>
  <done>All three adapters (qq deferred to 32-05) receive a resolved concrete tier; the kuwo/joox cellular regression is pinned by tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → navigator.connection | untrusted/absent platform signal read client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Info Disclosure | effectiveQuality reading connection type | accept | read-only, never transmitted; Chrome already clamps downlink/rtt for anti-fingerprinting and we read neither |
| T-32-02 | DoS (self, data cost) | 'auto' handing FLAC to a metered connection | mitigate | whitelist fails closed to '320'; saveData honoured; pinned by the cellular/unknown/undefined tests |
| T-32-SC | Tampering | package installs | n/a | this phase installs zero packages (research Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/sources/quality.test.ts src/lib/sources/kuwo.test.ts src/lib/sources/joox.test.ts src/lib/stores/settings.svelte.test.ts` — all green
- `pnpm test && pnpm check` — full suite + typecheck 0/0
</verification>

<success_criteria>
- 'auto' resolves through exactly ONE function; kuwo and joox call it; the default is 'auto'; the stale 128–160k claim is gone from defaults.ts and settings.svelte.ts; no i18n file touched.
</success_criteria>

<output>
Create `.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-01-SUMMARY.md` when done
</output>
