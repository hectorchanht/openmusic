---
phase: 32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/dedupe.ts
  - src/lib/services/dedupe.test.ts
  - src/lib/services/api-base.ts
  - src/lib/services/api-base.test.ts
  - src/lib/stores/player.svelte.test.ts
autonomous: true
requirements: [D-08, D-13]
must_haves:
  truths:
    - "In a deduped search list, a qq row beats a netease row on an equal-quality tie, so the surviving row carries song_mid and most FIRST plays need zero lookup — this is the latency lever (D-10b: D-08 owns the latency goal)"
    - "apiUrl returns an absolute https URL untouched even when VITE_API_BASE is set — the native build no longer concatenates 'https://openmusic.lolhttps://tang…'"
  artifacts:
    - path: "src/lib/services/dedupe.ts"
      provides: "SOURCE_RANK with qq at the top"
      contains: "qq: 4"
    - path: "src/lib/services/api-base.ts"
      provides: "absolute-URL guard in apiUrl"
    - path: "src/lib/services/dedupe.test.ts"
      provides: "first-ever SOURCE_RANK tie-break assertions"
  key_links:
    - from: "src/lib/services/api-base.ts"
      to: "the D-12 direct tang call (plan 32-05)"
      via: "apiUrl passing absolute URLs through unchanged"
      pattern: "^https\\?:"
---

<objective>
Two independent one-line levers with outsized effect. (1) 32-D-08: swap qq and netease in
`SOURCE_RANK` so the deduped survivor of a search row carries `song_mid` — per 32-D-10b this, not
the mid cache, is what makes most first plays fast AND lossless. (2) 32-D-13: give `apiUrl` an
"already absolute" guard so the D-12 direct tang call (plan 32-05) works on the native build,
where `VITE_API_BASE` is set and a bare concat produces an unparseable URL that throws at fetch.

Purpose: D-08 is sequenced EARLY by orchestrator directive — it is the actual latency decision.
Output: rank swap + first tie-break tests; apiUrl guard + absolute-URL test.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-CONTEXT.md
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-RESEARCH.md (§ Q4, § Q6 breakage map, Decisions At Risk #2)
@.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-PATTERNS.md (§ 5, § 7)
@src/lib/services/dedupe.ts
@src/lib/services/api-base.ts

<interfaces>
From src/lib/services/dedupe.ts:25 (current):
  const SOURCE_RANK: Record<SourceId, number> = { netease: 4, qq: 3, kuwo: 2, joox: 1, fivesing: 0, jamendo: -1, audius: -1, ytmusic: -1 };
Consumer (dedupe.ts:61-71): rank only breaks an EQUAL qualityRank tie; a `preferred` SourceId outranks it.
At search time every stub has quality:null → qualityRank 0 → the rank IS the sole tie-break (D-08's premise, confirmed in code).

From src/lib/services/api-base.ts:26-29 (current):
  export function apiUrl(path: string): string {
    const BASE = import.meta.env.VITE_API_BASE ?? '';
    return BASE + path;
  }
BASE is read LAZILY inside the function so vi.stubEnv works — keep it lazy; the guard goes inside the function.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap qq to the top of SOURCE_RANK with the first tie-break tests (32-D-08)</name>
  <files>src/lib/services/dedupe.ts, src/lib/services/dedupe.test.ts, src/lib/stores/player.svelte.test.ts</files>
  <read_first>src/lib/services/dedupe.ts (lines 8-75), src/lib/services/dedupe.test.ts, 32-PATTERNS.md § 7, Skill("spike-findings-openmusic") references/source-resolution.md (the "away from netease-first" instruction this REINFORCES)</read_first>
  <behavior>
    - dedupeBest([neteaseStub, qqStub]) where both share the same normalized title+artist and quality:null → the qq row survives (and in the reverse input order too — the win is rank-driven, not order-driven)
    - The surviving row carries song_mid (assert winner.songMid or winner.songid is the qq mid) — the D-10b premise pinned as a test
    - An explicit `preferred: 'netease'` still beats the static rank (existing contract unchanged)
  </behavior>
  <action>
    In dedupe.ts:25 swap qq and netease: `{ netease: 3, qq: 4, ... }` (the swap, not a bump to 5 —
    it keeps the range and every other rank's relative meaning unchanged). Extend the justification
    comment block above the const with a `32-D-08` paragraph in the file's existing prose style:
    netease winning the tie was WHY mid-less stubs were the common case; a qq survivor carries
    `song_mid` in the search body so most first plays are lossless with no lookup (32-D-10b: this is
    the latency lever, the mid cache only saves a call); accepted side effect — qq title/album
    metadata wins on disagreement; cross-ref the roadmap's pending "netease upstream health-gate"
    item which already suspected the rank-4. Add the three behavior tests to dedupe.test.ts using
    its existing `mk()` track factory (research Q6: zero winner-source assertions exist today —
    these are the first). Then run `pnpm vitest --run src/lib/stores/player.svelte.test.ts` and fix
    any case whose fixtures assumed netease wins a dedupeBest tie (the file imports dedupeBest;
    regenerate/setListQueue cases around lines 2937-3220 are the likely candidates) — update
    fixtures/expectations as a DECISION CHANGE with a 32-D-08 comment, never by weakening the
    assertion.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/services/dedupe.test.ts src/lib/stores/player.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "qq: 4" src/lib/services/dedupe.ts` matches line 25's SOURCE_RANK literal; `grep -c "netease: 3" src/lib/services/dedupe.ts` = 1
    - dedupe.test.ts contains a tie-break test asserting the winner's `source === 'qq'` AND its songMid/songid equals the qq stub's mid, in both input orders
    - `pnpm vitest --run src/lib/services/dedupe.test.ts src/lib/stores/player.svelte.test.ts` passes
    - The comment block above SOURCE_RANK gained a 32-D-08 paragraph (grep '32-D-08' in dedupe.ts ≥ 1)
  </acceptance_criteria>
  <done>qq wins equal-quality dedupe ties; the win and the mid-carriage are pinned by tests; player store tests updated deliberately where affected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Absolute-URL guard in apiUrl (32-D-13, the native-build break)</name>
  <files>src/lib/services/api-base.ts, src/lib/services/api-base.test.ts</files>
  <read_first>src/lib/services/api-base.ts (lines 1-40 — the posture doc-block AND apiUrl), src/lib/services/api-base.test.ts (lines 25-45), 32-RESEARCH.md § Q4 "What breaks if an absolute URL is passed"</read_first>
  <behavior>
    - With `vi.stubEnv('VITE_API_BASE', 'https://openmusic.lol')`: `apiUrl('https://tang.api.s01s.cn/music_open_api.php?type=json&mid=X')` returns the input UNCHANGED (the case that today produces 'https://openmusic.lolhttps://tang…' → TypeError at fetch on the APK)
    - Existing branches unchanged: unset base returns path as-is; set base prefixes a relative path (api-base.test.ts:31 and :36 stay green)
  </behavior>
  <action>
    Add one guard at the top of `apiUrl` (keep BASE lazily read INSIDE the function — the doc-block
    records that vi.stubEnv depends on it): `if (/^https?:\/\//i.test(path)) return path;` with a
    32-D-13 comment naming the native failure mode (VITE_API_BASE set → concat → unparseable URL →
    hard TypeError at fetch) and that the direct qq detail call (32-D-12) is the first absolute-URL
    caller; root-cause placement here covers every future absolute caller instead of per-call-site
    branching. Extend the module posture doc-block (api-base.ts:1-19) with a matching third-branch
    bullet in the same voice. Add the absolute-URL test case to the existing
    `describe('apiUrl — VITE_API_BASE branch')` block; the file's afterEach already calls
    `__resetGovernor()` — keep that discipline for any new case that trips failures.
  </action>
  <verify>
    <automated>pnpm vitest --run src/lib/services/api-base.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "https\?:" src/lib/services/api-base.ts` shows the regex guard inside apiUrl, before the BASE read is used
    - New test: base SET + absolute input → returned byte-identical; existing :31/:36 cases pass unmodified
    - `pnpm check` 0 errors 0 warnings
  </acceptance_criteria>
  <done>apiUrl is safe for absolute URLs on both builds; the native break is pinned by a test; posture doc-block updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → dedupe ranking | upstream search rows (untrusted metadata) decide the surviving track |
| client → apiUrl/fetch | URL construction feeding fetch on two build targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Spoofing/Integrity | qq metadata winning dedupe ties | accept | 32-D-08 records the accepted side effect; identity stays uid-based (makeUid), rank never changes identity |
| T-32-04 | DoS | malformed absolute URL reaching fetch on native | mitigate | the regex guard returns absolute URLs untouched; pinned by the stubEnv test |
| T-32-SC | Tampering | package installs | n/a | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/services/dedupe.test.ts src/lib/services/api-base.test.ts src/lib/stores/player.svelte.test.ts`
- `pnpm test && pnpm check` — full suite + typecheck 0/0
</verification>

<success_criteria>
- qq outranks netease on equal-quality ties with tests; apiUrl handles absolute URLs on both builds with tests; no other behavior change.
</success_criteria>

<output>
Create `.planning/phases/32-qq-lossless-first-resolve-rebuild-the-fast-path-around-the-p/32-02-SUMMARY.md` when done
</output>
