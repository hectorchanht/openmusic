---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 09
type: execute
wave: 4
depends_on: ["38-05", "38-07", "38-08"]
files_modified:
  - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md
autonomous: false
requirements: [38-A, 38-B]
must_haves:
  truths:
    - "https://openmusic.lol/.well-known/assetlinks.json returns 200 application/json with no redirect and contains both fingerprints"
    - "`adb shell pm get-app-links com.openmusic.app` reports `openmusic.lol: verified`"
    - "A COLD deep link (app force-stopped) opens the app on the shared song — the getLaunchUrl path"
    - "A WARM deep link (app running) opens the app on the shared song — the appUrlOpen path"
    - "An unclaimed URL (https://openmusic.lol/search) still opens in a browser"
    - "On a real device, tapping a shared link produces sound on the first tap without the ~2.7s cold-resolve wait"
  artifacts:
    - path: ".planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md"
      provides: "all rows green, nyquist_compliant: true"
      contains: "nyquist_compliant: true"
---

<objective>
Verify half (B) end to end on the emulator after the JSON is live, and confirm half (A)'s felt
latency on a real device. Automate everything `adb`/`emulator`/`curl` can do; the human does only
what is device-only (the push/deploy approval, and the perceptual check).

Purpose: RESEARCH §2d — `adb shell am start` alone launches the app even without a verified
domain; the `verified` string from `pm get-app-links` is the real gate. And Pitfall 1 — cold and
warm are DIFFERENT code paths; both must be exercised.
Output: on-device evidence recorded; 38-VALIDATION.md signed off.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN — approve the push to main so Pages deploys assetlinks.json (auto-deploys production)</name>
  <files>none — a git push / Cloudflare Pages deploy, no repo change</files>
  <action>Pause for the user to push `main` (or approve Claude pushing it). A push auto-deploys production AND cuts a release-signed rolling prerelease APK, so this is the T-33-14-style production gate — never push without this approval. After the user reports "deployed", re-run `curl -sI https://openmusic.lol/.well-known/assetlinks.json` yourself and proceed to Task 2 only when it returns 200 with `content-type: application/json` and no `location:` header. If it still 404s, the Pages deployment is not Active yet — wait and retry, do not proceed.</action>
  <verify>
    <automated>curl -sI https://openmusic.lol/.well-known/assetlinks.json | grep -qi "^HTTP/[12].* 200" && curl -sI https://openmusic.lol/.well-known/assetlinks.json | grep -qi "^content-type: application/json" && ! curl -sI https://openmusic.lol/.well-known/assetlinks.json | grep -qi "^location:"</automated>
  </verify>
  <done>assetlinks.json is live on openmusic.lol as 200 application/json with no redirect.</done>
  <read_first>
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §1 "Ordering hazard" and Pitfall 6 (JSON must be live BEFORE any APK carrying the filter is installed)
    - Project memory: openmusic-pushes-autodeploy-live (a push to main auto-deploys prod and, via android-main.yml, cuts a rolling prerelease APK signed with the release key)
  </read_first>
  <what-built>All phase-38 commits are on `main` locally: the web arrival path, the manifest intent-filter, and `static/.well-known/assetlinks.json` with both fingerprints (or debug-only if plan 08 recorded RELEASE_FINGERPRINT_PENDING — in that case the release-signed prerelease will NOT verify; decide whether to push now anyway).</what-built>
  <how-to-verify>
    1. Review `git log origin/main..HEAD --oneline` — every phase-38 commit, nothing unrelated.
    2. Push `main` (or approve Claude pushing it). Cloudflare Pages deploys automatically; android-main.yml builds the `latest` prerelease with the new manifest.
    3. Wait for the Pages deployment to be Active, then run: `curl -sI https://openmusic.lol/.well-known/assetlinks.json` — expect `HTTP/2 200`, `content-type: application/json`, and NO `location:` header.
  </how-to-verify>
  <resume-signal>Type "deployed" once the curl shows 200 application/json (paste the header lines), or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Emulator verification — App Links verified, cold + warm + negative deep links (D-23, D-24, D-25)</name>
  <files>.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md</files>
  <read_first>
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §2d (the literal command sequence), §2e (what to grep in `pm get-app-links`; the Signatures mismatch failure mode), §3b (cold vs warm), Pitfalls 1, 5, 6
    - Project memory: apk-build-needs-jdk21, apk-debug-via-emulator-cdp, sandbox-no-cn-upstream-network (use a `kuwo:` uid for any in-app playback check)
    - static/.well-known/assetlinks.json (the fingerprints the OS must report under `Signatures:`)
  </read_first>
  <action>
    Run, recording every command's output verbatim in the SUMMARY:
    1. Live JSON: `curl -si https://openmusic.lol/.well-known/assetlinks.json` → assert `200`, `content-type: application/json`, no `location`, body byte-identical to `static/.well-known/assetlinks.json` (`diff <(curl -s …) static/.well-known/assetlinks.json`).
    2. Build + install the DEBUG APK: `export JAVA_HOME=$(/usr/libexec/java_home -v 21) && pnpm apk`; boot `~/Library/Android/sdk/emulator/emulator -avd Pixel_3a_API_34 -no-snapshot &` then `adb wait-for-device` and wait for `adb shell getprop sys.boot_completed` = `1`; `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`.
    3. Verification: `adb shell pm set-app-links --package com.openmusic.app 0 all`; `adb shell pm verify-app-links --re-verify com.openmusic.app`; poll `adb shell pm get-app-links com.openmusic.app` every 10s for up to 3 minutes until the `Domain verification state:` block shows `openmusic.lol: verified`. Also assert the `Signatures:` line matches one entry in assetlinks.json (the debug one for this APK). If it shows `1024`/`legacy_failure`/`none` after 3 minutes → STOP and report: diff `Signatures:` against the JSON (Pitfall 5) and re-check step 1; do not "fix" by widening the manifest.
    4. COLD path (getLaunchUrl — plan 07 Task 1): `adb shell am force-stop com.openmusic.app`; `adb shell am start -a android.intent.action.VIEW -c android.intent.category.BROWSABLE -d "https://openmusic.lol/song/Adele/Hello?u=kuwo<a real kuwo id>"`; after 5s assert `adb shell dumpsys window | grep mCurrentFocus` contains `com.openmusic.app/.MainActivity` AND (via CDP into the WebView per apk-debug-via-emulator-cdp, or `adb logcat -d | grep -i "openmusic\|Capacitor"`) the WebView URL path is `/song/Adele/Hello…` — not the home screen. Note: the APK WebView resolves `/api/*` to `https://openmusic.lol` (VITE_API_BASE), so the in-app resolve itself runs against prod, not the sandbox.
    5. WARM path (appUrlOpen — plan 07 Task 2): with the app still in the foreground, press home (`adb shell input keyevent KEYCODE_HOME`), then `am start` a DIFFERENT song URL (`…/song/Coldplay/Yellow?u=kuwo<id>`); assert focus is again `com.openmusic.app/.MainActivity` and the WebView path changed to `/song/Coldplay/Yellow…` (proves the listener routed through `goto`, not a cold relaunch — `adb shell dumpsys activity activities | grep -c "com.openmusic.app"` should show the same task, not a new process start in logcat).
    6. NEGATIVE (D-24): `adb shell am start -a android.intent.action.VIEW -c android.intent.category.BROWSABLE -d "https://openmusic.lol/search"` → `mCurrentFocus` must NOT be `com.openmusic.app` (a browser package such as `com.android.chrome` or the resolver dialog).
    7. Update `38-VALIDATION.md`: flip the three manual rows (`assetlinks.json reachable`, `App Links verified`, `Cold-start deep link opens the app`) to ✅ with the observed evidence; add a WARM row ✅; add the NEGATIVE row ✅.
    If the emulator has no network for the verifier (step 3 never verifies but step 1 is fine), record it and fall back to the physical device in Task 3 — do not mark verified without the literal string.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && curl -sI https://openmusic.lol/.well-known/assetlinks.json | grep -qi "^content-type: application/json" && adb shell pm get-app-links com.openmusic.app | grep -q "openmusic.lol: verified"</automated>
  </verify>
  <acceptance_criteria>
    - `curl -si https://openmusic.lol/.well-known/assetlinks.json` shows status 200, `content-type: application/json`, no `location:` header, body identical to the repo file
    - `adb shell pm get-app-links com.openmusic.app` output contains the literal `openmusic.lol: verified` and a `Signatures:` fingerprint present in assetlinks.json
    - Cold: after `force-stop` + `am start`, `mCurrentFocus` contains `com.openmusic.app/.MainActivity` and the WebView path is the shared `/song/…` route (not `/`)
    - Warm: after HOME + a second `am start`, focus is the app and the WebView path is the SECOND song's route
    - Negative: `am start … /search` does NOT focus `com.openmusic.app`
    - `38-VALIDATION.md` manual rows updated with the evidence; the SUMMARY contains the verbatim outputs
  </acceptance_criteria>
  <done>App Links verified by the OS; cold, warm and negative deep links behave as specified on the emulator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN — real-device felt latency + release-signed APK link open; sign off VALIDATION</name>
  <files>.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md</files>
  <action>Pause for the user to run checklist steps 1-6 in <how-to-verify> on a real Android phone with the release-signed `latest` prerelease. On "approved": set `nyquist_compliant: true` and `status: approved` in 38-VALIDATION.md, flip the perceptual row to ✅, and record device model + Android version in the SUMMARY. On any failure token: record it verbatim in 38-VALIDATION.md and the SUMMARY, leave `nyquist_compliant: false`, and route it to a follow-up — no source or config change is permitted inside this task under any outcome (a blind patch here is how a device-only failure gets papered over).</action>
  <verify>
    <human-check>User reply is "approved" + device model + Android version, or one of the named failure tokens</human-check>
  </verify>
  <done>Real-device behaviour confirmed (or a named failure recorded) and 38-VALIDATION.md reflects the outcome.</done>
  <read_first>
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md "Manual-Only Verifications" (the perceptual row)
    - Project memory: cold-start-budget-measured (the ~2.7s upstream detail + ~2s device-to-CDN baseline the carrier should beat)
  </read_first>
  <what-built>Half (A): share links now carry the song uid; the landing page resolves on mount and arms the player; a warm arrival splices + plays with a toast. Half (B): App Links verified on the emulator with the debug APK (Task 2 evidence in the SUMMARY). What only a real phone can show: the felt first-tap latency, and that the RELEASE-signed `latest` prerelease (Obtainium channel) also verifies — the emulator ran the debug key.</what-built>
  <how-to-verify>
    On an Android phone with the `latest` prerelease installed/updated AFTER the deploy in Task 1 (Android ≤14 verifies at install/update time — if it was installed earlier, update or reinstall):
    1. `adb shell pm get-app-links com.openmusic.app` (phone connected) → expect `openmusic.lol: verified` and a `Signatures:` value equal to the RELEASE fingerprint in assetlinks.json. If plan 08 recorded RELEASE_FINGERPRINT_PENDING this step is expected to FAIL — report it as `RELEASE-PENDING`, not as a bug.
    2. In the app, share any song from the track menu to yourself (e.g. Messages/Telegram). Confirm the URL contains `?u=` (or `&u=`).
    3. Force-stop the app. Tap the link in the messaging app → it must open OpenMusic (not the browser) on the song page, with the song visible in the nowbar and NO audio yet. Tap "Play on openmusic" → sound should start near-instantly (well under the old ~5s tap-to-sound).
    4. With that song playing, tap a link to a DIFFERENT song → it should switch and play at once, show the "Playing shared song · your queue is kept" toast, and your Up Next rows should still be there.
    5. Tap the same link again → nothing changes (no restart).
    6. Tap `https://openmusic.lol/search` from the messaging app → opens in the browser, not the app.
  </how-to-verify>
  <resume-signal>Type "approved" with device model + Android version, or report tokens: `NOT-VERIFIED` (step 1), `BROWSER-OPENED` (step 3 opened a browser), `AUTOPLAYED` (audio before the tap), `SLOW` (first tap still waits seconds), `QUEUE-LOST` (step 4 rows vanished), `RESTARTED` (step 5), `RELEASE-PENDING`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push to main → production | a push deploys prod AND cuts a release-signed APK (openmusic-pushes-autodeploy-live) — the human gate in Task 1 is the control |
| Android verifier ↔ assetlinks.json ↔ APK signature | the OS decides the association |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Spoofing | App Links association | mitigate (verify) | the literal `verified` string from `pm get-app-links` is the gate, never `am start` alone (RESEARCH §2d pitfall); `Signatures:` diffed against the JSON |
| T-38-02 | Spoofing | intent-filter scope | mitigate (verify) | negative test: `/search` must open a browser (D-24) |
| T-38-10 | Repudiation | unverifiable evidence | mitigate | every command output recorded verbatim in the SUMMARY + VALIDATION.md |
| T-38-SC | Tampering | npm installs | accept | none this phase |
</threat_model>

<verification>
- Live JSON 200 application/json; OS reports `verified`; cold/warm/negative deep links behave; human confirms release APK + felt latency
- After Task 3 approval: set `nyquist_compliant: true` and `status: approved` in 38-VALIDATION.md (executor does this on resume) and record the device model/Android version
</verification>

<success_criteria>
- All rows in 38-VALIDATION.md green, `nyquist_compliant: true`
- A shared link opens the installed app (cold and warm), plays on the first tap without the cold-resolve wait, and the recipient's queue survives a warm arrival
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-09-SUMMARY.md` when done
</output>
