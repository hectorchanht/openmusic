---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 05
type: execute
wave: 3
depends_on: ["38-04", "38-06"]
files_modified:
  - src/routes/(app)/song/[artist]/[title]/+page.ts
  - src/routes/(app)/song/[artist]/[title]/+page.svelte
  - src/routes/(app)/song/[slug]/+page.svelte
  - src/routes/(app)/+page.svelte
  - .planning/todos/completed/song-share-stale-cover-comment.md
autonomous: true
requirements: [38-A]
must_haves:
  truths:
    - "Opening /song/{artist}/{title}?u=… with nothing playing shows the shared song in the nowbar, resolved and paused, before any tap; tapping 'Play on openmusic' starts sound from the already-armed element (D-05, D-13, D-19)"
    - "Opening the same link while something is playing splices the song in and plays it instantly, the queue keeps its rows, and a toast says the queue is kept (D-07, D-20)"
    - "No audio starts on mount, ever — including for a first-time visitor with empty localStorage (D-06, D-29)"
    - "A tap while the mount-time resolve is in flight awaits that resolve — no second request (D-16)"
    - "The legacy /song/[slug] route and the legacy /?play= token route through the same arrival path (D-12); a legacy token no longer nukes the recipient's queue"
    - "Both /song pages remain SSR-safe: no top-level store import; both +page.ts loaders stay synchronous and fetch-free"
  artifacts:
    - path: "src/routes/(app)/song/[artist]/[title]/+page.ts"
      provides: "opaque `u` echoed into page data"
      contains: "u: url.searchParams.get('u')"
    - path: "src/routes/(app)/song/[artist]/[title]/+page.svelte"
      provides: "mount-time arriveShared + D-19 play CTA adopting the in-flight promise"
      contains: "await import('$lib/services/share-arrival')"
    - path: "src/routes/(app)/song/[slug]/+page.svelte"
      provides: "same treatment (carrier-free)"
      contains: "await import('$lib/services/share-arrival')"
    - path: "src/routes/(app)/+page.svelte"
      provides: "legacy ?play= decoder re-pointed at arriveTrack"
      contains: "arriveTrack(current)"
  key_links:
    - from: "both /song pages onMount"
      to: "share-arrival.arriveShared"
      via: "lazy import under browser guard inside onMount (never $effect)"
      pattern: "onMount\\("
    - from: "src/routes/(app)/+page.svelte"
      to: "share-arrival.arriveTrack"
      via: "legacy token's `current` only — the decoded queue is ignored (D-11)"
      pattern: "arriveTrack\\(current\\)"
---

<objective>
Re-wire the three arrival surfaces onto the single path: both `/song/*` landing pages fire
`arriveShared` on mount (resolve, never play) and their "Play on openmusic" CTA starts the armed
element; the legacy `/?play=` decoder calls `arriveTrack` instead of `setQueue + play(fresh)`.
Then run the full suite + typecheck + a static audit as the phase's web gate.

Purpose: This is where D-05/D-06/D-07/D-13/D-16/D-17/D-19/D-20 become user-visible. The
open question (deprecate `[slug]`?) is DECIDED here: both routes get identical treatment via the
shared module — a net deletion of the duplicated `resolveAndPlay`, no deprecation needed, old links
keep working (the same reasoning D-11 applies to `?play=`).
Output: 3 route components + 1 loader edited; folded todo closed; full gate green.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-04-SUMMARY.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-06-SUMMARY.md
@CLAUDE.md

<interfaces>
From src/lib/services/share-arrival.ts (plan 04):
```typescript
export type ArrivalOutcome = 'armed' | 'played' | 'noop' | 'notfound';
export async function arriveTrack(track: Track): Promise<ArrivalOutcome>;
export async function arriveShared(input: { artist: string; title: string; u?: string | null }, signal?: AbortSignal): Promise<ArrivalOutcome>;
```
From src/lib/stores/player.svelte.ts: `playing = $state(false)`, `toggle()` (:4263 — `audio.paused ? audio.play() : pauseAudio()`; the D-19 "start the armed element" call), `pendingTrack`.
From src/lib/stores/toast.svelte.ts: `toast.show(msg: string)`. From `$lib/i18n`: `t('toast.sharedPlaying')` (plan 06 key), `t('home.unplayable')`.
Page data (both routes today): `{ og, name, artist }`; plan adds `u` to `[artist]/[title]` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /song/[artist]/[title] — echo `u`, resolve on mount, CTA adopts the in-flight arrival (D-05, D-06, D-13, D-16, D-17, D-18, D-19, D-20)</name>
  <files>src/routes/(app)/song/[artist]/[title]/+page.ts, src/routes/(app)/song/[artist]/[title]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/song/[artist]/[title]/+page.svelte (whole file — the SSR-SAFETY header :8-13 is the contract; :40-52 status/notFoundMsg/retry state; :54-75 resolveAndPlay being replaced; :77-82 the quick-260809-38i onMount whose comment becomes stale — Pitfall 10; :107-112 the CTA)
    - src/routes/(app)/song/[artist]/[title]/+page.ts (whole — synchronous, fetch-free; already reads `ci`)
    - src/lib/services/share-arrival.ts (plan 04 — arriveShared + ArrivalOutcome)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §8a-8b (mount-time resolve pattern: `await import` inside onMount, AbortController teardown, never $effect), Pitfall 3, Pitfall 10
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md "/song pages" section (the onMount-with-disposer shape from DownloadControl.svelte:110-118)
    - src/routes/+layout.svelte:22-40 (the untrack rule — why onMount, not $effect)
  </read_first>
  <action>
    `+page.ts`: return `{ og, name: title, artist, u: url.searchParams.get('u') }`. Add a comment: `38-D-08` — `u` is echoed OPAQUE (no parse, no validation here; `parseEntityParam` runs client-side in share-arrival), the loader stays SYNCHRONOUS and performs NO fetch (T-24-08 posture unchanged — do not add one).
    `+page.svelte` (keep tabs, single quotes; keep every module-top import exactly as is — `browser`, `onMount`, `PageOg`, `apiUrl`, `type PageData`; you MAY add `import type { ArrivalOutcome } from '$lib/services/share-arrival';` because `import type` is erased at compile time and pulls nothing into SSR — say so in a comment):
    1. Header comment: keep the SSR-SAFETY paragraph verbatim; add a `38-D-13/D-06` paragraph: the arrival module is imported LAZILY inside onMount and RESOLVES on mount (one detail call with the `u` carrier, or the name resolve without it) but NEVER plays on mount — `quick-260809-38i`'s no-autoplay decision stands (D-06); what moved is the resolve, not the playback. Rewrite the `quick-260809-38i` lines at :43-44 and :78-82 to that invariant (the ref stays; a comment contradicting the code is worse than none — Pitfall 10).
    2. State: keep `status: 'idle' | 'resolving' | 'playing' | 'notfound'` (an 'armed'/'noop' outcome maps to `'idle'` — the nowbar IS the armed indicator, no new status line, D-17/D-18: NowPlaying stays collapsed, no auto-expand). Keep `notFoundMsg`, keep `retry`. Add `let inflight: Promise<ArrivalOutcome> | null = null;` (plain, not `$state` — nothing renders it) and `let ac: AbortController | null = null;`.
    3. Replace `resolveAndPlay` with two functions:
       - `async function arrive(): Promise<ArrivalOutcome>` — `if (!browser || !data.name) return 'notfound';` `status = 'resolving';` `const { arriveShared } = await import('$lib/services/share-arrival');` `const outcome = await arriveShared({ artist: data.artist, title: data.name, u: data.u }, ac?.signal);` then map: `'played'` → `status = 'playing'` AND show the D-20 toast via lazy imports (`const [{ toast }, { t }] = await Promise.all([import('$lib/stores/toast.svelte'), import('$lib/i18n')]); toast.show(t('toast.sharedPlaying'));` inside try/catch — a failed i18n import must not break the page); `'notfound'` → `status = 'notfound'` and set `notFoundMsg = t('home.unplayable')` via the same lazy `t` in try/catch (keep the English fallback already in state); `'armed' | 'noop'` → `status = 'idle'`. Return the outcome.
       - `async function playNow()` (the D-19 CTA — the real user gesture mobile autoplay policy requires): `const outcome = await (inflight ?? (inflight = arrive()));` (D-16 — a tap during the mount resolve ADOPTS the in-flight promise; never a second request). If `outcome === 'notfound'`: re-run once — `inflight = arrive(); if ((await inflight) === 'notfound') return;` (the CTA is still the retry affordance). Then `const { player } = await import('$lib/stores/player.svelte'); if (!player.playing) player.toggle(); status = 'playing';` — comment: `toggle()` is exactly "start the already-armed element" (the same call the nowbar makes); guard on `player.playing` so a `'noop'` on an already-playing song does not PAUSE it. Never call `playStub` here any more (it did `setQueue + play(fresh:true)` — the queue nuke this phase removes).
    4. `onMount`: `if (!browser) return;` `ac = new AbortController(); inflight = arrive(); retry = () => void playNow(); return () => ac?.abort();` with the comment: onMount, NOT `$effect` — the arrival writes player `$state`, and a tracked effect that reads-then-writes `$state` self-invalidates (restore-effect-self-invalidation-loop, `+layout.svelte:24-33`); the AbortController lets a fast navigation away skip the name fan-out.
    5. CTA: change `disabled={status === 'resolving' || retry === null}` to `disabled={retry === null}` (D-16 — the tap must be possible during the resolve so it can adopt it; the spinner line still shows). Update the CTA comment (:107-109) to the new invariant: the page resolves on mount and this tap starts the armed element / adopts the in-flight resolve / re-runs after a miss.
    Do NOT add `audio.load()`, `preload`, any `fetch`, or any `$effect`. Do NOT import the player, toast or i18n at module top.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && f="src/routes/(app)/song/[artist]/[title]/+page.svelte" && test "$(sed -n '1,25p' "$f" | grep -c "^\s*import .*stores/")" = "0" && test "$(grep -c "await import('\$lib/services/share-arrival')" "$f")" = "1" && test "$(grep -c "playStub" "$f")" = "0" && test "$(grep -c "\$effect" "$f")" = "0" && test "$(grep -c "player.toggle()" "$f")" = "1" && grep -q "u: url.searchParams.get('u')" "src/routes/(app)/song/[artist]/[title]/+page.ts" && test "$(grep -c "fetch(" "src/routes/(app)/song/[artist]/[title]/+page.ts")" = "0" && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - Module-top imports of `+page.svelte` contain no `stores/`, `i18n`, or `share-arrival` VALUE import (`sed -n '1,25p' … | grep -c "^\s*import .*stores/"` prints `0`; a line matching `^\s*import type .*share-arrival` is allowed)
    - `grep -c "await import('\$lib/services/share-arrival')"` prints `1`; `grep -c "playStub"` prints `0`; `grep -c "\$effect"` prints `0`; `grep -c "audio.load\|preload"` prints `0`
    - `grep -c "player.toggle()"` prints `1`; `grep -c "disabled={retry === null}"` prints `1`; `grep -c "status === 'resolving' || retry === null"` prints `0`
    - `grep -c "toast.sharedPlaying"` prints `1`; `grep -c "quick-260809-38i"` is at least `2` (ref preserved in the rewritten comments); `grep -c "38-D-16"` is at least `1`
    - `+page.ts`: contains `u: url.searchParams.get('u')`, `grep -c "fetch("` prints `0`, `grep -c "^export const ssr = true"` prints `1`, and `load` is not `async`
    - `pnpm check` 0 errors
  </acceptance_criteria>
  <done>The current share landing page resolves on mount, never plays on mount, and its CTA starts the armed element or adopts the in-flight resolve; SSR contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: /song/[slug] same treatment + legacy /?play= re-pointed at arriveTrack + close the folded todo (D-11, D-12, D-15)</name>
  <files>src/routes/(app)/song/[slug]/+page.svelte, src/routes/(app)/+page.svelte, .planning/todos/completed/song-share-stale-cover-comment.md</files>
  <read_first>
    - src/routes/(app)/song/[slug]/+page.svelte (whole — character-identical mount logic to Task 1's page apart from the guard comment; :18-25 is the quick-260723-r4p cover comment the folded todo is about — verify it is ALREADY correct and the `<img>` at :83-92 renders)
    - src/routes/(app)/song/[artist]/[title]/+page.svelte (Task 1 output — the shape to mirror exactly, minus `u`)
    - src/routes/(app)/+page.svelte:50-66 (imports: `decodeShare`, `player`, `toast`, `t` are already at module top — this page is NOT an SSR landing surface), :669-686 (the `?play=` block), :690-694 (`token` is read again — keep the binding)
    - .planning/todos/pending/song-share-stale-cover-comment.md (Part 1 mandatory, Part 2 already done)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md "(app)/+page.svelte (MOD — legacy ?play= decoder, D-12)"
  </read_first>
  <action>
    `[slug]/+page.svelte`: apply Task 1's exact rewrite (header paragraph, status mapping, `inflight`/`ac`, `arrive()`, `playNow()`, `onMount`, CTA `disabled={retry === null}`, comment rewrites keeping `quick-260809-38i`) with `u: null` passed to `arriveShared` — comment: `38-D-15` — legacy carrier-free links get the SAME mount-time name resolve; there is no live emitter for this shape but old links are in the wild (the D-11 reasoning). Keep its module-top imports exactly (`browser`, `onMount`, `PageOg`, `type PageData`; `import type { ArrivalOutcome }` allowed). Confirm the `quick-260723-r4p` cover comment at :18-25 already states the true invariant (cover IS carried via `?c=` AND rendered) — if so leave it; if any remnant still says the cover is never carried, correct it.
    `(app)/+page.svelte` `?play=` block (:669-686): add `import { arriveTrack } from '$lib/services/share-arrival';` at module top (this page already imports the player store statically, so no SSR concern). Replace the body inside `if (token)` with: `const { current } = decodeShare(token); if (current) { void arriveTrack(current).then((o) => { if (o === 'played') toast.show(t('toast.sharedPlaying')); }); }` and keep `window.history.replaceState(null, '', location.pathname);`. Keep the `token` binding (it is read at :693). Rewrite the block comment: `38-D-12` — the decoder is KEPT (old links in the wild, D-11) but its payload is now only `current`: the sender's queue is deliberately DROPPED because the RECIPIENT's queue shape is what survives (D-02/D-07); routing through `arriveTrack` means cold → armed (no autoplay, D-06), warm → splice-in-top + play, identical to a `/song/*` arrival. State plainly that `setQueue(...) + play(current, { fresh: true })` was the queue nuke this phase exists to stop. Do not touch anything else in the file.
    Folded todo: `git mv .planning/todos/pending/song-share-stale-cover-comment.md .planning/todos/completed/` and append a `## Resolution (Phase 38, plan 05)` section: Part 1 verified correct at `[slug]/+page.svelte:18-25` (quick-260723-r4p), Part 2 already rendered at :83-92; this phase additionally rewrote the page's mount comments to the resolve-on-mount invariant.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && s="src/routes/(app)/song/[slug]/+page.svelte" && h="src/routes/(app)/+page.svelte" && test "$(sed -n '1,25p' "$s" | grep -c "^\s*import .*stores/")" = "0" && test "$(grep -c "await import('\$lib/services/share-arrival')" "$s")" = "1" && test "$(grep -c "playStub" "$s")" = "0" && test "$(grep -c "\$effect" "$s")" = "0" && test "$(grep -c "arriveTrack(current)" "$h")" = "1" && test "$(grep -c "player.setQueue(queue.length > 1" "$h")" = "0" && test "$(grep -c "play(current, { fresh: true })" "$h")" = "0" && test -f .planning/todos/completed/song-share-stale-cover-comment.md && test ! -f .planning/todos/pending/song-share-stale-cover-comment.md && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `[slug]/+page.svelte`: no module-top `stores/` value import; exactly one `await import('$lib/services/share-arrival')`; `playStub` count `0`; `$effect` count `0`; `player.toggle()` count `1`; `disabled={retry === null}` count `1`; `quick-260809-38i` at least `2`; `quick-260723-r4p` at least `1`; `38-D-15` at least `1`
    - `(app)/+page.svelte`: `arriveTrack(current)` count `1`; `player.setQueue(queue.length > 1` count `0`; `play(current, { fresh: true })` count `0`; `decodeShare(token)` count `1`; `window.history.replaceState(null, '', location.pathname)` count `1`; the later `if (!token && cached.useFallback` line still present; `38-D-12` at least `1`
    - The todo file exists under `.planning/todos/completed/` with a `## Resolution` heading and no longer exists under `pending/`
    - `pnpm check` 0 errors
  </acceptance_criteria>
  <done>The legacy slug route and the legacy token route both use the single arrival path; the folded todo is closed with evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Phase web gate — full suite, typecheck, static audit against the critical constraints</name>
  <files>.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md</files>
  <read_first>
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md (the behaviour → command map whose Status column you flip)
    - The planning_context critical constraints 1-10 (restated in 38-CONTEXT.md D-27..D-30 and RESEARCH pitfalls 1-10)
  </read_first>
  <action>
    Run the full gate: `pnpm test` (all ~70 files) and `pnpm check`. Then run the static audit below and record the results in `38-VALIDATION.md`: flip every web-side row in the Per-Behaviour table to ✅ green (uid carrier, arrivalMode, stub builder, deep-link parse, spliceAndPlay, armTrack, i18n parity) leaving the three manual rows ⬜ for plan 09; tick the Wave 0 checkbox for `share-arrival.test.ts`; set `wave_0_complete: true`; leave `nyquist_compliant: false` until plan 09 completes the manual rows. Audit commands (all must hold; cross-check any "0" with `sed`/Read per grep-false-empty-trust-read-sed):
    - no new raw fetch: `git diff <phase-start-sha>..HEAD -- src | grep -c "^+.*\bfetch("` is `0`
    - no `$effect` calling the player from the new code: `grep -c "\$effect" "src/routes/(app)/song/[artist]/[title]/+page.svelte" "src/routes/(app)/song/[slug]/+page.svelte"` both `0`; `grep -c "\$effect" src/routes/+layout.svelte` is `2`
    - armTrack body has no `driveSrc(`, `.play(`, `audio.load()`, `pendingSeek`
    - `grep -rn "hasPlayedSinceSrc" src/lib/services/share-arrival.ts src/routes` — only comment lines
    - `grep -c "playNext(track, { pin: false })" src/lib/components/NpRelated.svelte` is `0` (one composition, not three)
    - `grep -c "as any" src/lib/services/share-arrival.ts src/lib/services/share.ts src/lib/stores/player.svelte.ts` all `0`
    - i18n: `grep -c "'toast.sharedPlaying'" src/lib/i18n/*.ts` sums to `0`; 15 double-quoted hits
    Record the exact command outputs in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test && pnpm check && grep -q "wave_0_complete: true" .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all files, no skipped suites introduced); `pnpm check` reports 0 errors
    - Every static audit line above holds and its output is recorded in the SUMMARY
    - `38-VALIDATION.md` frontmatter has `wave_0_complete: true`, the web rows are ✅, the three manual rows remain ⬜ pending plan 09
  </acceptance_criteria>
  <done>The web half is green end-to-end and audited against every critical constraint; only the device-side rows remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query `?u=` / path segments → page → `arriveShared` | attacker-controllable; loader echoes `u` opaque, client validates |
| legacy `?play=<token>` → `decodeShare` → `arriveTrack` | existing `isStub` validator gates the decoded Track; the decoded QUEUE is now discarded |
| SSR loader | must stay fetch-free (SSRF posture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `?u=` → `+page.ts` → page | mitigate | loader echoes the string opaquely; `stubFromUidParam` (closed enum) is the only consumer; unknown source → name resolve |
| T-38-04 | SSRF | `+page.ts` loaders | mitigate | both loaders remain synchronous with NO fetch (acceptance criterion `grep -c "fetch("` = 0, `load` not async) |
| T-38-08 | Denial of Service | mount-time resolve fan-out | mitigate | resolve goes through apiFetch governor; AbortController skips the fan-out on navigation; CTA adopts the in-flight promise (no duplicate) |
| T-38-09 | Elevation (autoplay policy bypass attempt) | mount | mitigate | nothing calls play/toggle on mount — only the CTA gesture does (D-06); armTrack test proves `audio.play` uncalled |
| T-38-SC | Tampering | npm installs | accept | none this phase |
</threat_model>

<verification>
- Full `pnpm test` + `pnpm check` green
- Static audit lines all hold; 38-VALIDATION.md web rows flipped
- Manual browser spot-check (optional, sandbox: use a `kuwo:` uid — netease/qq are blocked here): open `http://localhost:4321/song/<artist>/<title>?u=kuwo<id>` with nothing playing → nowbar shows the song, no audio; tap "Play on openmusic" → sound; start another song, reopen the link → instant switch + toast; queue rows intact
</verification>

<success_criteria>
- Both /song routes resolve on mount and never play on mount; CTA starts the armed element or adopts the in-flight resolve
- Legacy `?play=` routes through arriveTrack; the recipient's queue survives
- Full suite + typecheck green; folded todo closed; VALIDATION.md web rows green
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-05-SUMMARY.md` when done
</output>
