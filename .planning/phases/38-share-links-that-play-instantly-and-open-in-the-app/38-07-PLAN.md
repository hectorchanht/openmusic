---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 07
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/routes/+layout.svelte
autonomous: true
requirements: [38-B]
must_haves:
  truths:
    - "On a COLD native launch from an App Link, the app reads App.getLaunchUrl() and navigates to the deep-link path (appUrlOpen never fires cold — RESEARCH §3b)"
    - "On a WARM native re-tap, the appUrlOpen listener navigates to the deep-link path and the listener is removed on teardown"
    - "Both paths pass the URL through deepLinkPath (host gate) before goto(); an off-host URL is dropped"
    - "The web build is byte-for-byte behaviourally unchanged: the whole block is behind Capacitor.isNativePlatform()"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "onMount deep-link bootstrap: getLaunchUrl (cold) + appUrlOpen (warm)"
      contains: "getLaunchUrl"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/services/share-arrival.ts deepLinkPath"
      via: "import — host gate before goto"
      pattern: "deepLinkPath\\("
    - from: "src/routes/+layout.svelte"
      to: "@capacitor/app"
      via: "dynamic import inside onMount under isNativePlatform()"
      pattern: "await import\\(\"@capacitor/app\"\\)"
---

<objective>
Turn an intercepted App Link into a normal in-app navigation so the APK and the web share ONE
arrival path (D-25): read `App.getLaunchUrl()` for the cold start AND register `appUrlOpen` for the
warm re-tap, both gated by `deepLinkPath`, both inside `onMount` in the root layout.

Purpose: RESEARCH §3b proved `appUrlOpen` is emitted only from `handleOnNewIntent` (warm). A plan
that wires only the listener ships a deep link that works with the app open and silently lands on the
home screen when it is not. The two paths are separate tasks with separate on-device checks (plan 09).
Output: one `onMount` block in `src/routes/+layout.svelte`.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From src/lib/services/share-arrival.ts (plan 01):
```typescript
export function deepLinkPath(raw: string | null | undefined): string | null; // pathname+search on https://openmusic.lol, else null
```
From node_modules/@capacitor/app/dist/esm/definitions.d.ts (8.1.0, already a dependency — no install):
```typescript
getLaunchUrl(): Promise<AppLaunchUrl | undefined>;            // { url: string } | undefined (undefined = launched from the launcher)
addListener(eventName: 'appUrlOpen', listenerFunc: (event: URLOpenListenerEvent) => void): Promise<PluginListenerHandle>; // event.url: string; handle.remove(): Promise<void>
```
From @capacitor/core: `Capacitor.isNativePlatform(): boolean` — the guard every native call site in this repo uses (player.svelte.ts:1460, blob-store.ts:309).
Root layout facts: this file uses DOUBLE quotes; `player` is already imported at module top (so importing share-arrival, which imports player, adds no new graph); the existing `$effect` at :22-40 is `untrack`ed — do NOT modify it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COLD path — read App.getLaunchUrl() in a root-layout onMount and goto the deep-link path (D-25)</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>
    - src/routes/+layout.svelte (whole file — the untrack rule at :22-40 and the contrasting comment at :48-50 are the house rule; DOUBLE quotes)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §3b (why cold never fires appUrlOpen — AppPlugin.java / Bridge.java:228), §3c (payload types), §3d (the recommended shape, idempotency hazard, cold-start ordering)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md "Capacitor deep-link listener (NEW)" (the three partial analogs: isNativePlatform guard, add-then-return-disposer, onMount not $effect)
    - src/lib/stores/player.svelte.ts:1459-1467 (the canonical `Capacitor.isNativePlatform()` guard)
  </read_first>
  <action>
    In `src/routes/+layout.svelte` `<script>`: add imports `import { onMount } from "svelte";` (merge with the existing `untrack` import), `import { goto } from "$app/navigation";`, `import { Capacitor } from "@capacitor/core";`, `import { deepLinkPath } from "$lib/services/share-arrival";`. Add ONE `onMount(() => { … })` block below the two existing `$effect`s. First line: `if (!Capacitor.isNativePlatform()) return;` (web build no-op — the web behaviour must be byte-identical when the app is not installed). Inside an async IIFE: `const { App } = await import("@capacitor/app");` then the COLD path: `const launch = await App.getLaunchUrl(); const p = deepLinkPath(launch?.url); if (p) void goto(p);`. Precede the block with the RESEARCH §3d decision-record comment verbatim in spirit: 38-D-25 (one arrival path for web + APK via goto); "TWO SEAMS, NOT ONE" — appUrlOpen fires only from handleOnNewIntent (warm, singleTask re-tap); on a COLD start Bridge captures intent.getData() into a private intentUri and loads index.html, never navigating to it, so getLaunchUrl() is the ONLY way to see the URL (verified against @capacitor/app@8.1.0 AppPlugin.java + @capacitor/android@8.4.0 Bridge.java:228); onMount NOT $effect because goto → the /song route → arrival writes player $state (restore-effect-self-invalidation-loop); getLaunchUrl() returns the same intentUri for the whole activity lifetime, so a re-mount re-fires it — D-03's no-op guard in the arrival path absorbs that. Leave the async IIFE open-ended for Task 2 to add the warm listener and the disposer. Do not modify the `untrack` block or the document.title effect.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test "$(grep -c 'App.getLaunchUrl()' src/routes/+layout.svelte)" = "1" && test "$(grep -c 'Capacitor.isNativePlatform()' src/routes/+layout.svelte)" = "1" && grep -q 'await import("@capacitor/app")' src/routes/+layout.svelte && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "App.getLaunchUrl()" src/routes/+layout.svelte` prints `1`
    - `grep -c "deepLinkPath(" src/routes/+layout.svelte` is at least `1`; every `goto(` in the file is preceded (same statement group) by a `deepLinkPath` null-check — `grep -c "goto(launch" src/routes/+layout.svelte` prints `0` (the raw URL never reaches goto)
    - `grep -c 'await import("@capacitor/app")' src/routes/+layout.svelte` prints `1`; `grep -c 'from "@capacitor/app"' src/routes/+layout.svelte` prints `0` (dynamic only — keeps the web chunk free of it)
    - The existing untrack block is unchanged: `sed -n 22,40p src/routes/+layout.svelte` still contains `untrack(() => {` and `void player.restore();`
    - `grep -c "\$effect" src/routes/+layout.svelte` still prints `2` (no new effect added)
    - `pnpm check` 0 errors
  </acceptance_criteria>
  <done>A cold native launch from an App Link navigates to the shared route; the web build is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: WARM path — register appUrlOpen and release it on teardown (D-25)</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>
    - src/routes/+layout.svelte (Task 1 output — the onMount block you extend)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §3a (AppPlugin.handleOnNewIntent is the only emitter), §3d (the `off` disposer shape)
    - src/lib/components/NowPlaying.svelte:87-93 (add-then-`return () => remove` structure)
  </read_first>
  <action>
    Extend the Task-1 `onMount`: declare `let off: (() => void) | undefined;` before the IIFE; inside the IIFE after the cold path add `const h = await App.addListener("appUrlOpen", (e) => { const p = deepLinkPath(e.url); if (p) void goto(p); }); off = () => void h.remove();`; make the onMount return `() => off?.()`. Comment: WARM — singleTask routes a re-tap while the activity is alive to onNewIntent → this event; same deepLinkPath gate (T-38-02: a hostile app can send ANY VIEW URL; the manifest's host filter is the OS's gate, this is ours); the disposer matters only for a real teardown (HMR in dev) but a leaked duplicate listener would double-fire goto on every warm link — D-03 would absorb it, but do not rely on that. Nothing else changes.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test "$(grep -c '"appUrlOpen"' src/routes/+layout.svelte)" = "1" && grep -q 'h.remove()' src/routes/+layout.svelte && test "$(grep -c 'deepLinkPath(' src/routes/+layout.svelte)" = "2" && pnpm check && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"appUrlOpen"' src/routes/+layout.svelte` prints `1`; `grep -c "h.remove()" src/routes/+layout.svelte` prints `1`
    - `grep -c "deepLinkPath(" src/routes/+layout.svelte` prints `2` (cold + warm, both gated); `grep -c "goto(e.url)" src/routes/+layout.svelte` prints `0`
    - `pnpm check` 0 errors; `pnpm build` (web, adapter-cloudflare) succeeds — proves the dynamic import compiles in the web bundle while the runtime guard no-ops it
    - `grep -c "\$effect" src/routes/+layout.svelte` still prints `2`
  </acceptance_criteria>
  <done>Warm re-taps route through the same goto path; the listener is disposed on teardown; web build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android VIEW intent (any app) → `getLaunchUrl()` / `appUrlOpen` → `goto()` | a hostile app can hand the activity an arbitrary URL string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | Spoofing | deep-link bootstrap | mitigate | every URL passes `deepLinkPath` (https + exact host `openmusic.lol`) before `goto`; the result is a same-origin path, never an absolute URL, so `goto` cannot leave the app origin |
| T-38-07 | Denial of Service | duplicate listeners | mitigate | one `onMount` in the once-mounted root layout; disposer removes the handle; D-03 no-op guard absorbs a re-fire |
| T-38-SC | Tampering | npm installs | accept | `@capacitor/app@8.1.0` is a pre-existing dependency, already registered natively — no install |
</threat_model>

<verification>
- `pnpm check` 0 errors; `pnpm build` green
- Static: `getLaunchUrl()` and `"appUrlOpen"` both present, both gated by `deepLinkPath`, all inside the `isNativePlatform()` guard, no new `$effect`
- On-device cold + warm verification is plan 09 (needs a deployed assetlinks.json + APK)
</verification>

<success_criteria>
- Both native seams (cold `getLaunchUrl`, warm `appUrlOpen`) implemented in the root layout, host-gated, web no-op
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-07-SUMMARY.md` when done
</output>
