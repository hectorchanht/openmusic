---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 06
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/lib/components/TrackMenu.svelte
  - src/lib/i18n/en.ts
  - src/lib/i18n/ar.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/es.ts
  - src/lib/i18n/fr.ts
  - src/lib/i18n/hi.ts
  - src/lib/i18n/id.ts
  - src/lib/i18n/it.ts
  - src/lib/i18n/pt.ts
  - src/lib/i18n/ru.ts
  - src/lib/i18n/th.ts
  - src/lib/i18n/tr.ts
  - src/lib/i18n/vi.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/zh-Hant.ts
autonomous: true
requirements: [38-A]
must_haves:
  truths:
    - "Sharing a song from the track menu produces a URL carrying `u={source}{songid}` for that exact track (D-08)"
    - "The pinned source-text tests (share.test.ts:834, names.test.ts:219) stay green — the call stays single-line with args 1-2 untouched"
    - "`toast.sharedPlaying` exists in all 15 locale dictionaries with double quotes (D-20)"
  artifacts:
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "songShareUrl call with the track as the 4th positional argument"
      contains: "songShareUrl({ title: dTitle, artist: dArtist }, shareCover, recallItunesId(shareCover), track)"
    - path: "src/lib/i18n/en.ts"
      provides: "the D-20 toast key"
      contains: "\"toast.sharedPlaying\""
  key_links:
    - from: "src/lib/components/TrackMenu.svelte"
      to: "src/lib/services/share.ts songShareUrl 4th arg"
      via: "positional `track`"
      pattern: "recallItunesId\\(shareCover\\), track\\)"
---

<objective>
Wire the emit side end-to-end (TrackMenu passes the track identity so every new share link carries
the uid carrier) and add the D-20 warm-arrival toast key to all 15 locale dictionaries.

Purpose: Without the TrackMenu change no link in the wild ever carries `u`, and plan 05's toast
needs a `TranslationKey` that compiles (`en` defines the union) and passes `i18n.test.ts` parity.
Output: one-token change at the single share call site; 15 one-line dictionary additions.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From src/lib/services/share.ts (plan 01):
```typescript
export function songShareUrl(t: { title: string; artist: string }, coverUrl?: string | null, itunesId?: string | null, id?: { uid: string; source: string; songid: string } | null): string;
```
From src/lib/components/TrackMenu.svelte:783 (current — MUST stay single-line, args 1-2 unchanged):
```typescript
const url = songShareUrl({ title: dTitle, artist: dArtist }, shareCover, recallItunesId(shareCover));
```
Pinned by: `share.test.ts:834` `/songShareUrl\(\{ title: dTitle, artist: dArtist \}, /` (positive) and `names.test.ts:219` `/songShareUrl\(\{ title: track\.title/` (negative).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass the track identity from TrackMenu's share call (D-08)</name>
  <files>src/lib/components/TrackMenu.svelte</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte:770-800 (the share handler; `track` is the component's Track prop; `dTitle`/`dArtist` are display names)
    - src/lib/services/share.test.ts:830-836 and src/lib/stores/names.test.ts:214-235 (the source-text regexes — read them so you know exactly what must not change)
    - src/lib/services/share.ts songShareUrl (plan 01 — 4th arg shape)
  </read_first>
  <action>
    Change ONLY the call at `TrackMenu.svelte:783` to append `track` as the 4th positional argument: `songShareUrl({ title: dTitle, artist: dArtist }, shareCover, recallItunesId(shareCover), track)`. Keep it on ONE line, first two arguments byte-identical (the two regex tests pin this shape — Pitfall 7). Add one comment line above it: `// 38-D-08: the 4th arg is the song IDENTITY (uid/source/songid) — the recipient does a direct detail resolve instead of a name search. Display names stay args 1-2 (OG-ZH-01); share.ts skips the carrier for device: uids and non-decodable ids.` Do not touch the quick-260808-vkd block below it.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -c "songShareUrl({ title: dTitle, artist: dArtist }, shareCover, recallItunesId(shareCover), track);" src/lib/components/TrackMenu.svelte && pnpm test -- src/lib/services/share.test.ts src/lib/stores/names.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "songShareUrl({ title: dTitle, artist: dArtist }, shareCover, recallItunesId(shareCover), track);" src/lib/components/TrackMenu.svelte` prints `1`
    - `grep -c "songShareUrl(" src/lib/components/TrackMenu.svelte` prints `1` (still the only call site)
    - `pnpm test -- src/lib/services/share.test.ts src/lib/stores/names.test.ts` exits 0 with no edits to either test file
    - `git diff --numstat src/lib/components/TrackMenu.svelte` shows at most 3 added / 1 deleted lines
    - `pnpm check` 0 errors
  </acceptance_criteria>
  <done>The single live share emitter passes the track identity; both source-text regex suites stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Add `toast.sharedPlaying` to all 15 locale dictionaries (D-20)</name>
  <files>src/lib/i18n/en.ts, src/lib/i18n/ar.ts, src/lib/i18n/de.ts, src/lib/i18n/es.ts, src/lib/i18n/fr.ts, src/lib/i18n/hi.ts, src/lib/i18n/id.ts, src/lib/i18n/it.ts, src/lib/i18n/pt.ts, src/lib/i18n/ru.ts, src/lib/i18n/th.ts, src/lib/i18n/tr.ts, src/lib/i18n/vi.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/zh-Hant.ts</files>
  <read_first>
    - src/lib/i18n/en.ts:1-12 and :393-400 (header convention; the `// --- toasts ---` block and `"toast.addedToQueue"` neighbour)
    - src/lib/i18n/de.ts:340-346, src/lib/i18n/zh-Hant.ts:366-372 (the same block in a Latin and a CJK locale — match each file's existing noun for "queue" as used in its `toast.addedToQueue`)
    - src/lib/i18n/i18n.test.ts:46-62 (parity test — automatic; the explicit-key test is the optional precedent)
    - CLAUDE.md "i18n — DOUBLE QUOTES Convention"
  </read_first>
  <action>
    In every one of the 15 dictionaries insert ONE line directly after `"toast.addedToQueue"` inside the toasts block: `"toast.sharedPlaying": "<value>",` — DOUBLE QUOTES for key and value, tab-indented like its neighbours. Values (Claude's discretion per D-20; the `·` separator matches `toast.skipped`'s house style; reuse each locale's existing word for "queue" from its own `toast.addedToQueue` line if it differs from what is written here):
    en `Playing shared song · your queue is kept` · zh-Hant `正在播放分享的歌曲 · 待播清單保留不變` · zh-Hans `正在播放分享的歌曲 · 待播列表保留不变` · de `Geteilter Song wird gespielt · deine Warteschlange bleibt` · es `Reproduciendo la canción compartida · tu cola se mantiene` · fr `Lecture du titre partagé · ta file d'attente est conservée` · it `Riproduzione del brano condiviso · la tua coda resta intatta` · pt `Tocando a música compartilhada · sua fila foi mantida` · ru `Играет присланный трек · ваша очередь сохранена` · ar `يتم تشغيل الأغنية المشتركة · قائمة الانتظار محفوظة` · hi `साझा गाना चल रहा है · आपकी कतार बनी हुई है` · id `Memutar lagu yang dibagikan · antrean Anda tetap` · th `กำลังเล่นเพลงที่แชร์ · คิวของคุณยังอยู่` · tr `Paylaşılan şarkı çalıyor · sıran korundu` · vi `Đang phát bài hát được chia sẻ · hàng đợi của bạn được giữ nguyên`.
    In `en.ts` only, add a one-line comment above the key: `// 38-D-20: a share arrival changes the music WITHOUT a player tap, so — unlike a Related-row tap — it announces itself and reassures that the queue survived.` Add nothing else; no test edit is needed (parity is automatic).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test "$(grep -l '"toast.sharedPlaying": "' src/lib/i18n/*.ts | grep -v test | wc -l | tr -d ' ')" = "15" && test "$(grep -c "'toast.sharedPlaying'" src/lib/i18n/*.ts | awk -F: '{s+=$2} END {print s}')" = "0" && pnpm test -- src/lib/i18n/i18n.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l '"toast.sharedPlaying": "' src/lib/i18n/*.ts | grep -v test | wc -l` prints `15`
    - No single-quoted variant anywhere: `grep -c "'toast.sharedPlaying'" src/lib/i18n/*.ts` sums to `0`
    - Every value is non-empty: `grep -h '"toast.sharedPlaying": ""' src/lib/i18n/*.ts | wc -l` prints `0`
    - `pnpm test -- src/lib/i18n/i18n.test.ts` exits 0 (key-set parity across all 15)
    - `pnpm check` 0 errors (the key is now a valid `TranslationKey`)
  </acceptance_criteria>
  <done>`t('toast.sharedPlaying')` compiles and resolves in all 15 locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan changes a call-site argument and dictionary strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `u` carrier emit | mitigate (upstream) | `share.ts` (plan 01) gates emission on `parseEntityParam` round-trip + `isDeviceUid`; TrackMenu passes the identity only |
| T-38-SC | Tampering | npm installs | accept | none this phase |
</threat_model>

<verification>
- `pnpm test -- src/lib/services/share.test.ts src/lib/stores/names.test.ts src/lib/i18n/i18n.test.ts` green
- `pnpm check` 0 errors
</verification>

<success_criteria>
- New share links from TrackMenu carry `u=`; pinned source-text regexes untouched and green
- `toast.sharedPlaying` present in all 15 dictionaries, double-quoted, parity test green
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-06-SUMMARY.md` when done
</output>
