---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 08
type: execute
wave: 2
depends_on: ["38-03"]
files_modified:
  - static/.well-known/assetlinks.json
autonomous: false
requirements: [38-B]
user_setup:
  - service: android-release-keystore
    why: "App Links verification for the release-signed APK (Obtainium / rolling prerelease) requires the release certificate's SHA256 fingerprint in assetlinks.json (D-22). The keystore is a GitHub secret and is NOT on this machine."
    dashboard_config:
      - task: "Run keytool locally against the release keystore and paste the SHA256 line"
        location: "Your local copy of the release keystore (the file behind the RELEASE_KEYSTORE GitHub secret)"
must_haves:
  truths:
    - "assetlinks.json carries BOTH the debug and the release SHA256 fingerprints, each in the canonical uppercase colon-hex form"
    - "The release fingerprint came from the user's keytool run — the keystore and its passwords never touched the repo or a shell history line"
  artifacts:
    - path: "static/.well-known/assetlinks.json"
      provides: "two-entry sha256_cert_fingerprints"
      contains: "sha256_cert_fingerprints"
---

<objective>
Obtain the RELEASE keystore SHA256 fingerprint from the user (a genuine human blocker — the keystore
exists only as a GitHub secret) and commit it as the second entry in `assetlinks.json` (D-22).

Purpose: Every APK users actually install (the `latest` rolling prerelease AND `v*` releases) is
signed with the release keystore. Without this entry App Links verify only for local `pnpm apk`
debug builds. Sequenced in wave 2 so the user can run keytool while the web half is still executing.
Output: `assetlinks.json` with two fingerprints.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN — obtain the release keystore SHA256 fingerprint with keytool (D-22)</name>
  <files>none — the fingerprint is pasted by the user; Task 2 writes it</files>
  <action>Pause and ask the user to run the literal keytool command in <how-to-verify> on the machine that holds the release keystore, then paste ONLY the SHA256 line. Claude cannot do this step: the keystore exists solely as the RELEASE_KEYSTORE GitHub secret (D-22). Do not attempt to fetch the secret, decode CI logs, or download a signed APK to derive it without asking — the `-printcert -jarfile` alternative is offered to the user, not run unprompted. No file changes in this task.</action>
  <verify>
    <human-check>The pasted value matches ^([0-9A-F]{2}:){31}[0-9A-F]{2}$ after stripping a `SHA256:` label and whitespace (Task 2 enforces this and re-asks on mismatch)</human-check>
  </verify>
  <done>The user has pasted a canonical release SHA256 fingerprint, or explicitly typed "skip-release".</done>
  <read_first>
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §2c (the literal command, the no-`-storepass`-on-the-command-line rule, the `-printcert -jarfile` alternative) and §2e (how a fingerprint mismatch shows up later)
    - .github/workflows/android-release.yml:79-108 (confirms the keystore is `RELEASE_KEYSTORE` base64 in secrets, the alias is `KEY_ALIAS` — you need the alias you configured there)
  </read_first>
  <what-built>Plan 03 shipped `static/.well-known/assetlinks.json` with the machine-local DEBUG fingerprint only. The release fingerprint cannot be derived by Claude: the release keystore is a GitHub secret, not on this machine.</what-built>
  <how-to-verify>
    Run this on your machine where the release keystore file lives (JDK 21 — the default java here is 20):

    1. `export JAVA_HOME=$(/usr/libexec/java_home -v 21)`
    2. `"$JAVA_HOME/bin/keytool" -list -v -keystore /path/to/release.keystore -alias "<your KEY_ALIAS>" | grep -i 'SHA256:'`
       (it prompts for the keystore password — do NOT pass `-storepass` on the command line; it would land in shell history)
    3. Alternative if only a signed APK from a GitHub Release is handy: `"$JAVA_HOME/bin/keytool" -printcert -jarfile app-release-signed.apk | grep -i 'SHA256:'`
    4. Copy the colon-separated uppercase hex after `SHA256:` (32 pairs, e.g. `AB:CD:…:EF`).

    The fingerprint is PUBLIC data (derived from the cert inside every APK) — pasting it here and committing it is correct. The keystore file and its passwords are the secrets; never paste those.
  </how-to-verify>
  <resume-signal>Paste the SHA256 fingerprint line (32 colon-separated uppercase hex pairs), or type "skip-release" to proceed with debug-only verification for now (App Links will then verify ONLY for local pnpm apk builds — plan 09 will flag this).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append the release fingerprint to assetlinks.json and validate</name>
  <files>static/.well-known/assetlinks.json</files>
  <read_first>
    - static/.well-known/assetlinks.json (plan 03 output — one debug entry)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §2a (target document shape with two fingerprints)
  </read_first>
  <action>
    Normalise the pasted value: strip a leading `SHA256:` label and whitespace, uppercase it, and REJECT (ask again via the checkpoint) unless it matches `^([0-9A-F]{2}:){31}[0-9A-F]{2}$`. Insert it as the SECOND element of `sha256_cert_fingerprints` (debug first, release second — the order carries no meaning to Android but keeps the diff readable). Keep the rest of the document byte-identical. If the user answered "skip-release", make NO change to the file and record `RELEASE_FINGERPRINT_PENDING` in the SUMMARY so plan 09 and the ROADMAP show the gap. Commit message must state: "release fingerprint from user keytool run (D-22); keystore + passwords never in repo".
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && node -e "const a=JSON.parse(require('fs').readFileSync('static/.well-known/assetlinks.json','utf8'));const f=a[0].target.sha256_cert_fingerprints;if(f.length!==2||!f.every(x=>/^([0-9A-F]{2}:){31}[0-9A-F]{2}$/.test(x))||f[0]===f[1])process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `sha256_cert_fingerprints` has exactly 2 DISTINCT entries, both matching `^([0-9A-F]{2}:){31}[0-9A-F]{2}$` (or, on "skip-release", exactly 1 and the SUMMARY contains `RELEASE_FINGERPRINT_PENDING`)
    - `git diff static/.well-known/assetlinks.json` touches only the fingerprints array
    - `git grep -c "storepass" -- ':!*.md' ':!node_modules'` shows no new occurrence introduced by this plan
  </acceptance_criteria>
  <done>assetlinks.json is complete for both signing identities (or the pending state is recorded explicitly).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's terminal → pasted fingerprint → repo | only the PUBLIC fingerprint crosses; keystore/password must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Information disclosure | keystore / passwords | mitigate | command uses the interactive password prompt (no `-storepass` argv); only the SHA256 is pasted; V14: `RELEASE_KEYSTORE` stays a GitHub secret, untouched |
| T-38-05 | Spoofing | fingerprint typo → verification fails silently | mitigate | strict regex validation before write; plan 09 reads the OS-reported `Signatures:` from `pm get-app-links` and diffs against the JSON |
| T-38-SC | Tampering | npm installs | accept | none this phase |
</threat_model>

<verification>
- JSON validity check with two distinct canonical fingerprints (or explicit pending state)
</verification>

<success_criteria>
- Release fingerprint committed alongside the debug one; no secret material in the repo
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-08-SUMMARY.md` when done
</output>
