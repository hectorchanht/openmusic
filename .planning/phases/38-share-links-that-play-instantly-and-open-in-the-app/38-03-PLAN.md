---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - static/.well-known/assetlinks.json
  - android/app/src/main/AndroidManifest.xml
autonomous: true
requirements: [38-B]
must_haves:
  truths:
    - "The web build ships /.well-known/assetlinks.json as a static asset listed in _routes.json exclude (served by Pages, never by the SvelteKit worker)"
    - "The APK's MainActivity declares an autoVerify VIEW intent-filter for https://openmusic.lol restricted to /song, /album, /artist"
    - "The existing MAIN/LAUNCHER intent-filter is untouched"
  artifacts:
    - path: "static/.well-known/assetlinks.json"
      provides: "Digital Asset Links statement for com.openmusic.app with the debug SHA256 (release appended in plan 08)"
      contains: "com.openmusic.app"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "second intent-filter with autoVerify + three pathPrefix data elements"
      contains: "android:autoVerify=\"true\""
  key_links:
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "https://openmusic.lol/.well-known/assetlinks.json"
      via: "OS App Links verification at install/update (autoVerify)"
      pattern: "android:host=\"openmusic.lol\""
---

<objective>
Ship the two static halves of Android App Links: the Digital Asset Links statement at
`static/.well-known/assetlinks.json` (debug fingerprint now; the release fingerprint is a human step
in plan 08) and the `autoVerify` VIEW intent-filter on MainActivity (D-21..D-24, D-26).

Purpose: RESEARCH §1 already VERIFIED that the plain `static/.well-known/` path survives both
adapters and is served `200 application/json` — no workaround is planned. Shipping the JSON in wave 1
respects the ordering hazard (RESEARCH §1 / Pitfall 6): the JSON must be live before any APK
carrying the intent-filter is installed.
Output: one new static file, one manifest edit.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static/.well-known/assetlinks.json with the debug fingerprint (D-22 debug half, D-23, D-26)</name>
  <files>static/.well-known/assetlinks.json</files>
  <read_first>
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §1 (verified pipeline + the two harmless side effects) and §2a (the exact JSON document and the REAL debug fingerprint read from this machine)
    - capacitor.config.ts (appId `com.openmusic.app` — LOCKED, phase 999.1 D-12)
    - static/manifest.webmanifest (the only other hand-authored JSON in static/ — layout precedent)
    - static/robots.txt (confirm `.well-known` is not disallowed)
  </read_first>
  <action>
    Create `static/.well-known/assetlinks.json` containing a JSON array with ONE statement object: `relation: ["delegate_permission/common.handle_all_urls"]`, `target: { namespace: "android_app", package_name: "com.openmusic.app", sha256_cert_fingerprints: [ "<debug fingerprint>" ] }` where `<debug fingerprint>` is the literal value from RESEARCH §2a (`37:30:88:C4:7F:83:64:6F:83:D8:B3:C7:E1:58:FB:37:F5:8D:17:7C:1B:27:00:3F:BF:1A:5E:AD:87:EB:8D:A2`). Re-derive it locally first with `keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep SHA256` and use whatever that prints (it must match RESEARCH; if it does not, use the freshly printed value and note the discrepancy in the SUMMARY). Only the debug entry now — the release fingerprint is appended by plan 08 after the user runs keytool (D-22). Two-space indentation, trailing newline; JSON cannot carry comments, so put the "first fingerprint is the MACHINE-LOCAL debug key (other developers' `pnpm apk` builds will not verify); release fingerprint follows in 38-08" note in the commit message and the SUMMARY. Do NOT touch `static/robots.txt` (already allow-all) or `src/service-worker.ts` (the SW precaching the 171-byte file is harmless — RESEARCH §1) or `android/app/build.gradle` (aapt stripping dot-dirs from the APK is correct and irrelevant — verification fetches from the web host).
    Then prove the pipeline once on this tree: run `pnpm build` and confirm the file lands in `.svelte-kit/cloudflare/.well-known/assetlinks.json` byte-identical and is listed in `.svelte-kit/cloudflare/_routes.json` under `exclude`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && node -e "const a=JSON.parse(require('fs').readFileSync('static/.well-known/assetlinks.json','utf8'));if(!Array.isArray(a)||a[0].target.package_name!=='com.openmusic.app'||a[0].relation[0]!=='delegate_permission/common.handle_all_urls'||!/^([0-9A-F]{2}:){31}[0-9A-F]{2}$/.test(a[0].target.sha256_cert_fingerprints[0]))process.exit(1)" && pnpm build && cmp static/.well-known/assetlinks.json .svelte-kit/cloudflare/.well-known/assetlinks.json && grep -c '"/.well-known/assetlinks.json"' .svelte-kit/cloudflare/_routes.json</automated>
  </verify>
  <acceptance_criteria>
    - `static/.well-known/assetlinks.json` parses as a JSON array whose `[0].target.package_name === 'com.openmusic.app'` and whose `[0].target.sha256_cert_fingerprints` has exactly ONE entry matching `^([0-9A-F]{2}:){31}[0-9A-F]{2}$`
    - `[0].relation` equals `["delegate_permission/common.handle_all_urls"]`; `[0].target.namespace === 'android_app'`
    - After `pnpm build`: `cmp static/.well-known/assetlinks.json .svelte-kit/cloudflare/.well-known/assetlinks.json` exits 0 and `grep -c '"/.well-known/assetlinks.json"' .svelte-kit/cloudflare/_routes.json` prints `1` or more
    - `git status --short static/` shows only the new file (no other static asset touched)
  </acceptance_criteria>
  <done>The Digital Asset Links statement exists with the debug fingerprint and survives the web build as an excluded static asset.</done>
</task>

<task type="auto">
  <name>Task 2: Add the autoVerify VIEW intent-filter to MainActivity (D-23, D-24, D-26)</name>
  <files>android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (whole file — 4-SPACE indentation, the existing MAIN/LAUNCHER filter at :20-23 must not change, `launchMode="singleTask"` + `exported="true"` already present at :17-18)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §2b (the exact block, the BROWSABLE+DEFAULT requirement, the `/song` vs `/songbook` prefix note, https-only rationale)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md "AndroidManifest.xml (MOD)" (in-file precedent for a long XML decision-record comment: the `<uses-permission>` block at :42-63)
    - src/lib/services/share.ts:30-45 (`shareOrigin` — proves only `https://openmusic.lol` is ever emitted)
  </read_first>
  <action>
    Inside the existing `<activity android:name=".MainActivity" …>` element, AFTER the existing MAIN/LAUNCHER `<intent-filter>` and before `</activity>`, add a SECOND `<intent-filter android:autoVerify="true">` containing: `<action android:name="android.intent.action.VIEW" />`, `<category android:name="android.intent.category.DEFAULT" />`, `<category android:name="android.intent.category.BROWSABLE" />`, and three `<data android:scheme="https" android:host="openmusic.lol" android:pathPrefix="…" />` elements for `/song`, `/album`, `/artist` — exactly the block in RESEARCH §2b. Precede it with the XML decision-record comment from §2b (38-D-23/D-24: claims openmusic.lol ONLY, the three share surfaces ONLY, every other URL still opens in a browser; verification is per-HOST, pathPrefix narrows matching; `pathPrefix="/song"` also matches `/songbook` — no such route exists today, a future `/songs` route would silently start opening in the app; https only because shareOrigin() never emits http; D-26: singleTask already set is what routes a re-tap to onNewIntent). Use 4 SPACES per indent level to match the file. Do not modify the MAIN/LAUNCHER filter, the `<provider>`, or any `<uses-permission>`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && xmllint --noout android/app/src/main/AndroidManifest.xml && test "$(grep -c '<intent-filter' android/app/src/main/AndroidManifest.xml)" = "2" && test "$(grep -c 'android:autoVerify="true"' android/app/src/main/AndroidManifest.xml)" = "1" && test "$(grep -c 'android:pathPrefix=' android/app/src/main/AndroidManifest.xml)" = "3" && test "$(grep -c 'android.intent.category.BROWSABLE' android/app/src/main/AndroidManifest.xml)" = "1" && test "$(grep -c 'android.intent.category.LAUNCHER' android/app/src/main/AndroidManifest.xml)" = "1" && test "$(grep -c 'android:scheme="http"' android/app/src/main/AndroidManifest.xml)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `xmllint --noout android/app/src/main/AndroidManifest.xml` exits 0
    - Exactly 2 `<intent-filter` elements; exactly 1 `android:autoVerify="true"`; exactly 3 `android:pathPrefix=` (values `/song`, `/album`, `/artist`); exactly 1 BROWSABLE, 1 DEFAULT, 1 LAUNCHER category
    - Every `<data` element has `android:scheme="https"` and `android:host="openmusic.lol"`; zero occurrences of `android:scheme="http"` (exact, not https)
    - `grep -c 'pages.dev' android/app/src/main/AndroidManifest.xml` prints `0` (D-23)
    - The MAIN/LAUNCHER block (`sed -n 20,23p`) is byte-identical to before; `android:launchMode="singleTask"` and `android:exported="true"` still present
    - No tab characters introduced: `grep -cP '\t' android/app/src/main/AndroidManifest.xml` prints `0`
  </acceptance_criteria>
  <done>MainActivity declares a verified App Links filter for the three share prefixes on openmusic.lol; everything else in the manifest is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android OS verifier → https://openmusic.lol/.well-known/assetlinks.json | the OS decides domain ownership from this document |
| Browser/other app VIEW intent → MainActivity | the intent-filter decides which URLs the app claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Spoofing (domain hijack) | assetlinks.json | mitigate | served from our own https origin with a valid cert, as a Pages static asset (excluded from the worker); the SHA256 pins the signing identity — only APKs signed by our debug/release keys verify |
| T-38-02 | Spoofing | intent-filter | mitigate (defense in depth) | host + https + three pathPrefix only (D-24); the in-app `deepLinkPath` (plan 01) re-checks host — never rely on the OS alone |
| T-38-06 | Information disclosure | fingerprint in repo | accept | the SHA256 fingerprint is PUBLIC data derived from the cert shipped in every APK; the keystore + passwords stay GitHub secrets, untouched by this phase |
| T-38-SC | Tampering | npm installs | accept | none this phase |
</threat_model>

<verification>
- JSON validity + package name + fingerprint format check passes; `pnpm build` output contains the file and `_routes.json` excludes it
- Manifest is well-formed XML with exactly the described filters
</verification>

<success_criteria>
- `static/.well-known/assetlinks.json` committed with the debug fingerprint
- `AndroidManifest.xml` carries the autoVerify filter for openmusic.lol `/song`, `/album`, `/artist`; MAIN/LAUNCHER untouched
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-03-SUMMARY.md` when done
</output>
