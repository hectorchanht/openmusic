---
phase: 38-share-links-that-play-instantly-and-open-in-the-app
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/player.svelte.ts
  - src/lib/stores/player.svelte.test.ts
  - src/lib/components/NpRelated.svelte
autonomous: true
requirements: [38-A]
must_haves:
  truths:
    - "Calling spliceAndPlay(S) while C is playing puts S right after C, plays S non-fresh, keeps every other queue row in order, does NOT pin S, and returns true"
    - "Calling spliceAndPlay(S) when S is already current is a no-op returning false (D-03)"
    - "Calling armTrack(S) on an EMPTY queue with no current seats S resolved with audio.src set and audio.play() is NEVER called (D-05/D-06/D-29)"
    - "Calling armTrack(S) over a paused restored session keeps queue/shuffle/repeatMode/upNextAnchorUid and splices S after the old current (D-02)"
    - "The armed seat is persisted so a reload restores S (D-04)"
  artifacts:
    - path: "src/lib/stores/player.svelte.ts"
      provides: "spliceAndPlay(t): boolean, armTrack(t): Promise<Track|null>, private spliceAfterCurrent(t)"
      contains: "spliceAndPlay"
    - path: "src/lib/components/NpRelated.svelte"
      provides: "relatedTapPlay delegating to player.spliceAndPlay (net deletion)"
      contains: "player.spliceAndPlay(track)"
  key_links:
    - from: "src/lib/stores/player.svelte.ts armTrack"
      to: "audio.src"
      via: "DIRECT assign mirroring restore() (31-D-12), never driveSrc"
      pattern: "audio\\.src = src"
    - from: "src/lib/stores/player.svelte.ts playNext + armTrack"
      to: "spliceAfterCurrent"
      via: "single queue-splice helper (no third inline copy)"
      pattern: "this\\.spliceAfterCurrent\\("
---

<objective>
Add the two thin player entry points the arrival path needs — `spliceAndPlay` (warm, D-07) and
`armTrack` (cold, D-05) — plus the private `spliceAfterCurrent` helper they and `playNext` share, and
collapse `NpRelated.relatedTapPlay` onto `spliceAndPlay` so there is ONE composition, not three.

Purpose: D-29 proves the cold branch cannot ride `playNext` (it autoplays on an empty queue). The
cold seat must be an explicit method that never calls `play()`. CLAUDE.md names per-surface
duplication as the last debt — the warm composition moves INTO the store so the share arrival and the
legacy `?play=` decoder (plans 04/05) call one method.
Output: two public methods + one private helper on `player`, their test suites, NpRelated net deletion.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-CONTEXT.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md
@.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing player internals the new methods reuse (verified line numbers). -->

From src/lib/stores/player.svelte.ts:
```typescript
playing = $state(false);                              // :193 — false after restore(); true only from the audio 'play' event
private lastSrcKind: SrcKind = 'url';                  // :299
private pendingGen = 0;                                // :356 — owned by playStub; do NOT reuse for armTrack
private playGen = 0;                                   // :438 — bumped per play(); snapshot + re-check for supersedence
private cachedBlobUrl: string | null = null;           // :479
private persist()                                      // :499 — writes current + queue + shuffle/repeat (D-04 rides this)
async restore()                                        // :553-655 — the D-05 reference shape (steps 6-14 + 18 are reused)
private backfillLyrics(track: Track)                   // :730
private async enrichFromLocalFile(track, blob, myGen)  // :869
private manualUids = new Set<string>();                // :1115
private syncMetadata()                                 // :1502
playNext(t: Track, opts: { pin?: boolean } = {})       // :2633-2641 — body to extract into spliceAfterCurrent
private indexOf(track: Track | null): number           // :2784
async play(track, opts?: { fresh?; fromFallback?; context?; sameList? })  // :3266
toggle()                                               // :4263 — audio.paused ? play() : pauseAudio()
```

CONTRACTS THIS PLAN CREATES (plan 04 calls these; plan 05's legacy handler calls them via plan 04):
```typescript
/** 38-D-02/D-07 — splice after current + play non-fresh. false = already current (D-03 no-op). */
spliceAndPlay(t: Track): boolean;
/** 38-D-05 — seat RESOLVED + ARMED + PAUSED. Never calls play(). Returns the resolved track when
 *  audio.src was armed, null when the resolve produced no playable src (carrier miss → caller
 *  falls back, D-10) or the call was superseded. */
async armTrack(track: Track): Promise<Track | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract spliceAfterCurrent, add spliceAndPlay, collapse relatedTapPlay onto it (D-03, D-07)</name>
  <files>src/lib/stores/player.svelte.ts, src/lib/components/NpRelated.svelte, src/lib/stores/player.svelte.test.ts</files>
  <read_first>
    - src/lib/stores/player.svelte.ts:2618-2650 (playNext JSDoc + body; addToQueue below it)
    - src/lib/components/NpRelated.svelte:130-166 (relatedTapPlay and its two comment blocks — these RELOCATE onto the store method, they are not deleted)
    - src/lib/stores/player.svelte.test.ts:1-60, :114-200 (mocks, `mk`, `memStore`, `mockEnsure`), :298-330 (`makeFakeAudio` + the suite-wide `play` spy in beforeEach), :768-853 (the existing "Related tap — playNext + non-fresh play preserves Up Next (quick-260910-qjv)" suite — the queue-shape assertions to re-target at spliceAndPlay)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §5 (exact semantics of playNext/pin:false and play/fresh:false; the §5d method sketch)
    - CLAUDE.md "Comments" (relocate decision-ref comments, never delete)
  </read_first>
  <behavior>
    - Test: queue [q1,q2,q3,q4], q2 current (real play), `spliceAndPlay(rel)` → queue uids `[q1,q2,rel,q3,q4]`, `play` invoked with `(rel, { fresh: false })` (or `current.uid === rel.uid` after flush with the real play), `manualUids` does NOT contain `rel.uid`, `upNextAnchorUid` unchanged, returns `true`
    - Test: `spliceAndPlay(current)` → returns `false`, queue unchanged, play spy not called
    - Test: no current + empty queue → `spliceAndPlay(t)` calls play EXACTLY once (playNext's own `!this.current` play + the guard prevents a double) — this documents that spliceAndPlay is NOT the cold path (D-29)
    - Test: `playNext` behaviour is unchanged after the extraction: with current C, `playNext(t)` splices after C AND adds to manualUids; `playNext(t, { pin: false })` splices without pinning (existing assertions in the 768 suite stay green)
  </behavior>
  <action>
    In `src/lib/stores/player.svelte.ts`:
    1. Add `private spliceAfterCurrent(t: Track): void` directly above `playNext`, containing EXACTLY the three queue lines from playNext's body (`const q = this.queue.filter(...)`, `const i = q.findIndex(...)`, `q.splice(i >= 0 ? i + 1 : 0, 0, t)`) followed by `this.queue = q`. JSDoc: `38-D-02` — the one queue-splice primitive; de-dupe by uid FIRST then land after current (index 0 when there is no current); shared by `playNext` (pins + may play), `spliceAndPlay` (plays, never pins) and `armTrack` (seats, never plays) so the shape is provably identical across the three arrivals.
    2. Rewrite `playNext` to: the existing `if (opts.pin !== false) this.manualUids.add(t.uid);` line, then `this.spliceAfterCurrent(t);`, then the existing `if (!this.current) this.play(t); else this.persist();` tail. Byte-identical behaviour; keep its JSDoc intact.
    3. Add `spliceAndPlay(t: Track): boolean` below `playNext`: `if (this.current?.uid === t.uid) return false;` → `this.playNext(t, { pin: false });` → `if (this.current?.uid !== t.uid) void this.play(t, { fresh: false });` → `return true;`. Its JSDoc is the RELOCATED content of NpRelated.svelte:141-165 (the quick-260910-qjv block explaining why fresh:true destroyed the queue, the D-03 no-op reasoning incl. the playNext filter-first mis-splice, the pin:false reasoning, the double-play guard) plus a `38-D-02/D-07` line: extracted here because the share arrival (both /song routes) and the legacy `?play=` decoder are its second and third callers and CLAUDE.md flags a third inline copy as debt. Also state plainly: "NOT the cold path — playNext autoplays on an empty queue (38-D-29); cold arrivals use armTrack."
    In `src/lib/components/NpRelated.svelte`: reduce `relatedTapPlay` to a one-line body `player.spliceAndPlay(track);` and replace the two comment blocks with a two-line pointer comment: `// quick-260910-qjv → player.spliceAndPlay (38-D-07). The full decision record (why fresh:true nuked the queue, why pin:false, the D-03 no-op guard) lives on that method now. No toast/haptic — the row becoming the playing track IS the feedback.` Nothing else in the component changes.
    In `src/lib/stores/player.svelte.test.ts`: add `describe('player.spliceAndPlay — splice after current + non-fresh play (38-D-02/D-03/D-07)')` right after the existing 768 suite, reusing its `beforeEach` restore idiom (real `play`, `mockEnsure` identity, `makeFakeAudio`) and `startQueue()` shape. Implement the `<behavior>` bullets. Observe `manualUids` via `(player as unknown as { manualUids: Set<string> }).manualUids` (tests may cast; production must not). Keep the existing 768 suite intact — it pins `playNext` behaviour and must stay green after the extraction.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- src/lib/stores/player.svelte.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "this.spliceAfterCurrent(" src/lib/stores/player.svelte.ts` is at least `2` (playNext + spliceAndPlay via playNext counts once; armTrack in Task 2 adds the second — after Task 1 alone the count is `1` and after Task 2 it is `2`)
    - `grep -n "^\s*spliceAndPlay(t: Track): boolean" src/lib/stores/player.svelte.ts` matches exactly one line
    - `grep -c "player.spliceAndPlay(track)" src/lib/components/NpRelated.svelte` prints `1`; `grep -c "player.playNext(track, { pin: false })" src/lib/components/NpRelated.svelte` prints `0`
    - `grep -c "quick-260910-qjv" src/lib/stores/player.svelte.ts` is at least `1` AND `grep -c "quick-260910-qjv" src/lib/components/NpRelated.svelte` is at least `1` (decision ref relocated, not deleted)
    - `grep -c "38-D-29" src/lib/stores/player.svelte.ts` is at least `1`
    - `pnpm test -- src/lib/stores/player.svelte.test.ts` exits 0 (including the pre-existing 768 suite untouched)
    - `pnpm check` reports 0 errors
  </acceptance_criteria>
  <done>One splice primitive, one warm composition on the store, NpRelated delegates, all player tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add armTrack — the explicit cold seat that never plays (D-04, D-05, D-06, D-29)</name>
  <files>src/lib/stores/player.svelte.ts, src/lib/stores/player.svelte.test.ts</files>
  <read_first>
    - src/lib/stores/player.svelte.ts:547-655 (restore() — copy steps 6-14 + 18 per the RESEARCH §4 table; DROP the localStorage read, queue/shuffle/repeat install, upNextAnchorUid re-anchor, pendingSeek)
    - src/lib/stores/player.svelte.ts:3183-3243 (playStub — the supersede discipline: "Do NOT touch pendingTrack/loading; the newer call owns them now")
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-PATTERNS.md "armTrack()" section (verbatim cover-seed chain :576-589, try/catch/finally shape, the 31-D-12 direct-assign comment to carry forward)
    - .planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-RESEARCH.md §4a, §4b, §5e, §8e (no audio.load(), no preload, no blob pre-buffer)
    - src/lib/stores/player.svelte.test.ts:2091-2110 (restore() tests — they must stay green untouched; `seedState` helper nearby), :114-130 (`memStore` — assert D-04 persistence through it)
  </read_first>
  <behavior>
    - Test (D-06/D-29 regression — the most valuable assertion in the phase): `player.current = null; player.queue = []`, fake audio attached, `mockEnsure` resolves `{...S, audioUrl:'https://cdn/s.mp3', detailsLoaded:true}` → `await player.armTrack(S)` returns the resolved track; `fake.play` call count is `0`; the `play` spy call count is `0`; `player.current.uid === S.uid`; `fake.src === 'https://cdn/s.mp3'`; `player.queue.map(uid)` equals `[S.uid]`; `player.loading === false`; `player.playing === false`
    - Test (D-02 shape over a restored session): queue `[A, C, B]`, `current = C`, `upNextAnchorUid = A.uid`, `repeatMode = 'one'`, `shuffle = true` → `armTrack(S)` → queue `[A, C, S, B]`, `current.uid === S.uid`, anchor still `A.uid`, repeatMode still `'one'`, shuffle still `true`, `manualUids` does not contain `S.uid`
    - Test (D-04 persisted): after a successful armTrack, `JSON.parse(memStore.get(STATE_KEY)).current.uid === S.uid` (import `STATE_KEY` from `$lib/stores/player-persist` as the restore tests do, or read the one key memStore holds)
    - Test (cover seed): with `getCachedCoverByUid` mocked to return `'https://img/x.jpg'` for S.uid (the suite already mocks cover-cache — see `mockUidCover`), `resolvedCover === 'https://img/x.jpg'` synchronously after the call starts (seat-first: assert before awaiting completion, or assert `player.current.uid === S.uid` before the deferred ensure resolves)
    - Test (carrier miss → null): `mockEnsure` resolves the input unchanged (no audioUrl) → returns `null`, `fake.src` unchanged (`''`), `S.uid` NOT in `player.queue` (the dead stub is removed), `loading === false`
    - Test (supersede): `mockEnsure` returns a deferred promise; while pending, bump `(player as unknown as { playGen: number }).playGen++` (a user play() happened); resolve the deferred → returns `null`, `fake.src` still `''`, and `player.loading` was NOT flipped to false by armTrack (set `player.loading = true` before resolving and assert it is still `true` after)
    - Test (never throws): `mockEnsure` rejects → resolves to `null`, `loading === false`
  </behavior>
  <action>
    In `src/lib/stores/player.svelte.ts`:
    1. Add a PLAIN (non-`$state`) `private armGen = 0;` next to `pendingGen` (:356) with a comment: monotonic counter for `armTrack` supersedence; deliberately NOT `pendingGen`, which `playStub` owns (a shared counter would make an arm cancel an in-flight tap's overlay bookkeeping).
    2. Add `async armTrack(track: Track): Promise<Track | null>` directly below `restore()` (:655). JSDoc = the RESEARCH §4a sketch text (seat RESOLVED + ARMED + PAUSED, same shape restore() builds MINUS everything persistence-specific, never calls play(), D-06 autoplay policy) plus: `38-D-29` — exists because `playNext` autoplays on an empty queue, so the cold arrival cannot ride `spliceAndPlay`; `38-D-04` — persists the seat so a reload restores the shared song. Body, in order:
       a. `if (!browser) return null;` `const gen = ++this.armGen; const playGenAtStart = this.playGen;` `logAction('armTrack', { uid: track.uid, source: track.source });`
       b. `this.spliceAfterCurrent(track);` (D-02 — the shared song lands after the old current, whatever else was queued survives; NOT added to `manualUids`, the D-07 pin:false semantics), then `this.current = track;`
       c. The cover-seed chain VERBATIM from restore() (`getPinnedCover(track.uid) ?? track.cover ?? getCachedCoverByUid(track.uid) ?? getCachedCover(track.artist, track.title) ?? null`) with a condensed version of its comment (cover-hero-mediacard-missing + quick-260915-w4f pin-first), then `this.syncMetadata(); this.loading = true; this.persist();`
       d. `try {` offline-first exactly as restore() (`library.isDownloaded` → `blobStore.get(...).catch(() => null)`); if no blob → `resolved = await ensureTrackDetails(track)`; SUPERSEDE CHECK immediately after EVERY await: `if (gen !== this.armGen || playGenAtStart !== this.playGen) return null;` — and on that path do NOT touch `loading`/`current` (the newer call owns them — playStub's discipline; implement by returning from inside try with a local `superseded = true` flag that the `finally` reads to skip `this.loading = false`). Then `this.current = resolved; const i = this.indexOf(track); if (i >= 0) this.queue[i] = resolved;` and the 31-D-08 `backfillLyrics` line; offline branch → the `localTrack` + `enrichFromLocalFile(localTrack, offlineBlob, this.playGen)` lines as restore().
       e. `if (!this.audio) return resolved;` then revoke a stale `cachedBlobUrl` as restore() does; pick `src` = blob object URL or `resolved.audioUrl`; if NEITHER: remove the dead stub from the queue (`this.queue = this.queue.filter((x) => x.uid !== track.uid)`), `this.persist()`, `return null` — comment `38-D-10`: a carrier miss must not leave a dead row in the recipient's queue; `current` stays seated exactly as restore()'s "track stays in current, user can tap play to retry" precedent, and the arrival module immediately re-arms by name over it.
       f. `this.lastSrcKind = offlineBlob ? 'download-blob' : 'url'; audio.src = src;` — DIRECT assign with the 31-D-12 comment carried forward: this is a one-shot initial arm, not a recovery re-attach; routing it through `driveSrc` would newly subject a share arrival to the re-drive brake (nowbar-freeze-reresolve-loop). NO `audio.load()`, NO `preload`, NO blob pre-buffer, NO `pendingSeek` (a shared song starts at 0) — `38-D-14`. Then `this.persist(); return resolved;`
       g. `} catch { return null; /* resolve failed — seat stays, caller falls back (D-10) */ } finally { if (!superseded) this.loading = false; }` — also filter the stub out of the queue in the catch path the same way as (e).
    3. Do NOT modify `restore()` (zero diff — RESEARCH §4a: ship the new method, factor later if at all; the restore tests at :2091-2110 pin it).
    In `src/lib/stores/player.svelte.test.ts`: add `describe('player.armTrack — cold seat: resolved + armed + paused, never plays (38-D-05/D-06/D-29)')` after the spliceAndPlay suite. Use the same restore idiom (`mockRestore` the play spy OR keep the spy and assert `expect(player.play).not.toHaveBeenCalled()` — the D-06 test is stronger with the spy kept) and `makeFakeAudio()`; implement every `<behavior>` bullet. For the deferred-ensure supersede case copy the deferred-promise idiom the FIX-A playStub suite (:854+) already uses.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- src/lib/stores/player.svelte.test.ts && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^\s*async armTrack(track: Track): Promise<Track | null>" src/lib/stores/player.svelte.ts` matches exactly one line
    - `grep -c "private armGen = 0" src/lib/stores/player.svelte.ts` prints `1`; `grep -c "armGen = \$state" src/lib/stores/player.svelte.ts` prints `0`
    - Inside the armTrack body (`sed -n "$(grep -n 'async armTrack' src/lib/stores/player.svelte.ts | cut -d: -f1),+90p" src/lib/stores/player.svelte.ts`): contains `audio.src = src`, contains `31-D-12`, contains `38-D-14`, contains `38-D-10`, contains `38-D-29`, does NOT contain `driveSrc(`, does NOT contain `.play(`, does NOT contain `audio.load()`, does NOT contain `pendingSeek`, does NOT contain `pendingGen`
    - `git diff --stat src/lib/stores/player.svelte.ts` shows the restore() body unchanged: `sed -n 553,655p src/lib/stores/player.svelte.ts | md5` equals the pre-task value (record it before editing) — or equivalently the three restore() tests at ~:2091-2110 pass without edits
    - `pnpm test -- src/lib/stores/player.svelte.test.ts` exits 0 with the new describe contributing at least 7 tests, including one whose name contains `empty queue` and asserts `play` was not called
    - `pnpm check` reports 0 errors
  </acceptance_criteria>
  <done>armTrack seats a track resolved + armed + paused with the recipient's queue shape intact, never calls play, persists, and its D-06 empty-queue regression test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Track` handed to armTrack/spliceAndPlay | already validated upstream (plan 01 `stubFromUidParam` closed enum, or a decoded legacy token via `isStub`); the store trusts its Track type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `armTrack` → `ensureTrackDetails` → `SOURCES[track.source]` | mitigate (upstream) | only Tracks built by `stubFromUidParam` (closed enum) or `decodeShare`'s `isStub` gate reach here; the store adds no new parse |
| T-38-04 | Denial of Service | `armTrack` resolve | mitigate | resolve goes through `ensureTrackDetails` → adapter `apiFetch` (governor: dedupe + MAX_CONCURRENT 8 + 25s + breaker); no raw fetch, no byte prefetch (D-14) |
| T-38-SC | Tampering | npm installs | accept | no package installed by this phase |
</threat_model>

<verification>
- `pnpm test -- src/lib/stores/player.svelte.test.ts` green including pre-existing restore()/playNext suites
- `pnpm check` 0 errors
- Static: armTrack body has no `driveSrc(`, `.play(`, `audio.load()`, `pendingSeek`; restore() unchanged
</verification>

<success_criteria>
- `spliceAndPlay` and `armTrack` exist on the player with the contracts above; `playNext`, `spliceAndPlay`, `armTrack` share `spliceAfterCurrent`
- NpRelated is a net deletion delegating to the store
- The D-06 "cold + empty queue → no audio" regression test exists and passes
</success_criteria>

<output>
Create `.planning/phases/38-share-links-that-play-instantly-and-open-in-the-app/38-02-SUMMARY.md` when done
</output>
