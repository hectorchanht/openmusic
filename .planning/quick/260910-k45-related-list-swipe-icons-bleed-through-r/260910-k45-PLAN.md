---
phase: quick-260910-k45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/components/NowPlaying.svelte]
autonomous: false
requirements: [QUICK-260910-k45]

must_haves:
  truths:
    - "At rest, a Related-tab row shows title + artist full width with no play-next / add-to-queue icon visible through the text"
    - "A horizontal right-drag on a Related row still reveals the queue icon (left edge) and fires relatedSwipeQueue; a left-drag still reveals play-next (right edge) and fires relatedSwipeNext"
    - "Tap-to-play and long-press menu on a Related row are unaffected"
    - "Up-Next rows, the generic .row, and the related loading skeleton are visually unchanged"
  artifacts:
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "opaque .related-swipe .row masking the reveal layers at rest"
      contains: "quick-260910-k45"
  key_links:
    - from: ".related-swipe .row"
      to: "var(--color-bg)"
      via: "CSS background declaration (same token as .np, so an opaque row is visually identical to the transparent one over the sheet)"
      pattern: "\\.related-swipe \\.row \\{[^}]*background: var\\(--color-bg\\)"
---

<objective>
Stop the Related-tab swipe-reveal icons (ListEnd / ListStart) from bleeding through row text at rest.

Purpose: the swipe-reveal pattern (quick-260625-pzs-02) positions the reveal spans BEHIND the row and relies on the row being OPAQUE to mask them. The `.related-swipe .row` rule set `position: relative; z-index: 1` but never the background, while the generic `.row` is `background: none`. z-index orders layers; it does not occlude. The comment above the rule claims the opaque bg is applied — it never was.
Output: one CSS declaration added, one stale comment corrected, both tagged `quick-260910-k45`.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/components/NowPlaying.svelte

<interfaces>
<!-- Everything the executor needs is in NowPlaying.svelte. No exploration required. -->

Markup (~line 1531):
  <li class="swipe-wrap related-swipe">
    <span class="reveal reveal-queue" aria-hidden="true">…</span>
    <span class="reveal reveal-next"  aria-hidden="true">…</span>
    <button class="row rel-row" use:longpress … onclick={player.play} use:swipeAction={{ onSwipeRight: relatedSwipeQueue, onSwipeLeft: relatedSwipeNext }}>…</button>
  </li>

CSS (~lines 1763-1780):
  .row { … background: none; … }                                   ← generic, DO NOT TOUCH
  /* quick-260625-pzs-02: … The related .row is normally transparent, so it gets an opaque bg + z-index here so
     the reveal stays masked at rest … */                            ← stale: bg was never written
  .related-swipe { position: relative; overflow: hidden; border-radius: 10px; }
  .related-swipe .reveal { position: absolute; … pointer-events: none; }
  .related-swipe .row { position: relative; z-index: 1; }            ← THE FIX GOES HERE
  @media (hover: hover) { .row:hover { background: var(--color-surface); } }
  .row.playing { background: rgba(124,92,255,0.15); }

Reference implementation that works, src/routes/(app)/search/+page.svelte ~line 919:
  .row { … background: var(--color-bg); position: relative; z-index: 1; … }

Token: `var(--color-bg)` — same as `.np { background: var(--color-bg) }` (~line 1569), so the opaque row is
visually identical to today's transparent row over the sheet.

Cascade note (why hover/playing tints keep working): `.related-swipe .row`, `.row:hover` and `.row.playing`
are all specificity (0,2,0); the hover/playing rules come LATER in source, so they still win when active.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give .related-swipe .row an opaque background and fix the stale comment</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>
In `src/lib/components/NowPlaying.svelte`, inside the `<style>` block:

1. Change the rule `.related-swipe .row { position: relative; z-index: 1; }` to also declare `background: var(--color-bg);` (place it before `position`, mirroring search/+page.svelte). Keep `position: relative; z-index: 1;` exactly as-is. Do NOT add a transition, hover override, or any other declaration.

2. Correct the stale `quick-260625-pzs-02` comment immediately above `.related-swipe`. It currently claims the related row "gets an opaque bg + z-index here". Keep that comment's intent but append a `quick-260910-k45` decision-record sentence stating the opaque `background: var(--color-bg)` was missing until now (z-index alone orders layers but does not occlude, so the reveal icons showed through the transparent row at rest), and that `--color-bg` is chosen to match `.np` so the row looks unchanged over the sheet. Do not remove the existing `quick-260625-pzs-02` tag or the search/+page.svelte cross-reference (house style: never delete decision-ref comments).

Scope guard: touch ONLY the `.related-swipe .row` rule and the comment above `.related-swipe`. Do NOT modify the generic `.row`, `.q-row`, `.row.playing`, the `@media (hover: hover)` rule, the `.skel` loading rows, or `use:swipeAction` / `relatedSwipeQueue` / `relatedSwipeNext` in the script.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -v '^\s*/\*\|^\s*\*' src/lib/components/NowPlaying.svelte | grep -c 'related-swipe \.row {.*background: var(--color-bg)' | grep -qx 1 && grep -c 'quick-260910-k45' src/lib/components/NowPlaying.svelte | grep -qE '^[1-9]' && pnpm check</automated>
  </verify>
  <done>
`.related-swipe .row` declares `background: var(--color-bg)` alongside the existing `position: relative; z-index: 1;`; the comment above `.related-swipe` carries a `quick-260910-k45` tag and no longer claims something the CSS doesn't do; `pnpm check` passes with no new errors; the generic `.row` still reads `background: none`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Opaque `var(--color-bg)` background on Related-tab swipe rows so the reveal icons behind them are masked at rest.</what-built>
  <how-to-verify>
1. Start the dev server (`pnpm dev`, port 4321 or 5173 — probe, don't assume) and open the app on a phone-width viewport (DevTools device mode with touch emulation, or a real phone on the LAN).
2. Play any track, expand Now Playing, open the "Related" tab and wait for rows to load.
3. AT REST: every row shows title + artist spanning the full width with NO ListEnd / ListStart icon visible through or beside the text. Row background should be indistinguishable from before (same colour as the sheet).
4. SWIPE RIGHT on a row: the row slides right, the queue icon (ListEnd) is revealed on the LEFT edge, and on release the "added to queue" action fires (toast / Up-Next grows).
5. SWIPE LEFT on a row: the row slides left, the play-next icon (ListStart) is revealed on the RIGHT edge, and on release the play-next action fires.
6. TAP a row: it plays. LONG-PRESS a row: the track menu opens. Hover (desktop) still tints the row; the currently-playing row (if present in Related) still shows its purple tint.
7. Switch to the Up-Next tab: rows look exactly as before (this change must not affect them).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still bleeds through / what regressed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure presentational CSS change in a client component; no input, network, storage, or auth path touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k45-01 | Tampering | NowPlaying.svelte CSS | accept | Single declaration on a scoped selector; cascade order verified so hover/playing tints still win; visual checkpoint guards regression |
| T-k45-SC | Tampering | npm installs | accept | No dependencies added or changed |
</threat_model>

<verification>
- `pnpm check` green.
- Non-comment grep confirms exactly one `.related-swipe .row {…background: var(--color-bg)…}` rule and `quick-260910-k45` tag present.
- Human checkpoint confirms: icons hidden at rest, both swipe directions still reveal + fire, tap/long-press intact, Up-Next unchanged.
</verification>

<success_criteria>
Related-tab rows mask the swipe-reveal icons at rest while swipe-to-queue / swipe-to-play-next, tap, long-press, hover and playing tints all behave exactly as before. No other row style changed. No deploy or push performed.
</success_criteria>

<output>
Create `.planning/quick/260910-k45-related-list-swipe-icons-bleed-through-r/260910-k45-SUMMARY.md` when done.
</output>
