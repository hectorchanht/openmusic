---
phase: quick-260910-k45
plan: 01
subsystem: ui-nowplaying
tags: [css, swipe, related-tab, nowplaying]
requires: [quick-260625-pzs-02]
provides: ["opaque .related-swipe .row masking the swipe-reveal layers at rest"]
affects: [src/lib/components/NowPlaying.svelte]
tech-stack:
  added: []
  patterns: ["opaque row over absolutely-positioned reveal spans (mirrors search/+page.svelte .row)"]
key-files:
  created: []
  modified: [src/lib/components/NowPlaying.svelte]
decisions:
  - "Use var(--color-bg) (the same token as .np) so the now-opaque Related row is visually identical over the sheet"
  - "Declare background before position, mirroring the working reference rule in search/+page.svelte"
metrics:
  duration: ~4 min
  completed: 2026-09-10
---

# Phase quick-260910-k45 Plan 01: Related-list swipe icons bleed-through Summary

Gave `.related-swipe .row` an opaque `background: var(--color-bg)` so the ListEnd / ListStart swipe-reveal spans behind it are occluded at rest, and corrected the stale `quick-260625-pzs-02` comment that claimed the opaque background already existed.

## What Was Built

One CSS declaration and one comment amendment in `src/lib/components/NowPlaying.svelte`:

- `.related-swipe .row { background: var(--color-bg); position: relative; z-index: 1; }` (line ~1780). `z-index` orders layers but does not occlude, so the reveal icons showed through the transparent row.
- The `quick-260625-pzs-02` block comment above `.related-swipe` now carries a `quick-260910-k45` decision-record sentence recording that the described opaque background was never actually written, and why `--color-bg` was chosen. The original tag and the `search/+page.svelte` cross-reference were kept (house style: never delete decision-ref comments).

Scope guard honoured: the generic `.row` still reads `background: none`; `.q-row`, `.row.playing`, the `@media (hover: hover)` rule, the `.skel` rows and the `use:swipeAction` / `relatedSwipeQueue` / `relatedSwipeNext` script wiring were not touched. Cascade order is unchanged — `.row:hover` and `.row.playing` are the same specificity (0,2,0) and come later in source, so hover and playing tints still win.

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Opaque background + comment fix | `d93945c` | `src/lib/components/NowPlaying.svelte` (+6/-2) |

## Verification — Observed Results

Both automated checks were RUN and their real output observed:

- Non-comment grep: `grep -v '^\s*/\*\|^\s*\*' … | grep -c 'related-swipe \.row {.*background: var(--color-bg)'` → exactly `1`. `grep -c 'quick-260910-k45'` → `1`. **PASS.**
- `pnpm check` → `COMPLETED 4406 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`. **PASS.**
- Post-commit deletion check → no deleted files. Commit contains one file only; the unrelated dirty working-tree entries (`.gitignore`, `CLAUDE.md`, `.planning/HANDOFF.json`, `docs/agents/`) were left unstaged as instructed.

**NOT verified by this executor:** the visual/interaction checkpoint (icons hidden at rest, both swipe directions still reveal and fire, tap/long-press intact, Up-Next unchanged). Per the orchestrator's constraints the browser verification was skipped here and is being run by the orchestrator. No claim is made that the visual fix renders correctly — only that the CSS declaration is present and the project typechecks.

## Deviations from Plan

None — the plan's automated task executed exactly as written. The human-verify checkpoint was skipped on explicit orchestrator instruction (not a deviation of the plan's intent; the verification is being performed by the orchestrator instead).

## Constraints Honoured

- No `git push`, no deploy (pushes to main auto-deploy production).
- Docs artifacts (this SUMMARY, STATE.md, PLAN.md) left uncommitted for the orchestrator's docs commit.
- ROADMAP.md untouched.

## Known Stubs

None.

## Self-Check: PASSED

- `src/lib/components/NowPlaying.svelte` — FOUND (modified, contains the new rule).
- Commit `d93945c` — FOUND in `git log`.
