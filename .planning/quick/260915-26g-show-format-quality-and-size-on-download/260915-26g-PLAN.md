---
phase: quick-260915-26g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/download-probe.ts
  - src/lib/services/download-probe.test.ts
  - src/lib/services/download-track.ts
  - src/lib/services/download-track.test.ts
  - src/lib/components/DownloadControl.svelte
  - src/lib/components/TrackMenu.svelte
autonomous: true
requirements: [quick-260915-26g]

must_haves:
  truths:
    - "Opening a song's ⋮ menu shows the REAL container/quality and byte size on the Download row (e.g. `FLAC · 38.2 MB`, `320K · 8.1 MB`), probed at settings.downloadQuality, with a skeleton while the probe is in flight"
    - "If the probe fails (resolve error, HEAD+Range both refused, CDN hides the length) the Download row renders exactly as today — plain `Download` — and the download itself still works"
    - "List rows (library page, album page, RowBadges) issue ZERO extra network calls on render — DownloadControl's `probe` prop defaults to false and no list call site sets it"
    - "Re-opening the same menu costs no network: the probe result is memoised per `uid|downloadQuality`"
    - "Closing the sheet mid-probe aborts the in-flight resolve/HEAD (AbortSignal threaded) and never writes a stale label"
    - "Tapping Download after a successful probe reuses the probed URL (same file the label described) instead of a fresh third resolve, when it is still fresh and meets the tier"
    - "A displayed container is never the `extFromAudioUrl` 'mp3' default — it comes from an `audio/*` Content-Type or a REAL extension match, else it is omitted"
  artifacts:
    - path: "src/lib/services/download-probe.ts"
      provides: "probeDownload (never-throws, memoised, abortable) + formatBytes + formatDownloadMeta + containerFromUrl/containerFromContentType"
      exports: ["probeDownload", "formatBytes", "formatDownloadMeta", "containerFromUrl", "containerFromContentType", "__resetDownloadProbe"]
      min_lines: 80
    - path: "src/lib/services/download-probe.test.ts"
      provides: "Node tests for formatter, container derivation, HEAD→Range→null ladder, memo, abort, never-throw"
      min_lines: 80
    - path: "src/lib/services/download-track.ts"
      provides: "exported currentQualityMeets + reuse of a fresh, tier-satisfying INPUT track (not only player.current)"
      contains: "export function currentQualityMeets"
    - path: "src/lib/components/DownloadControl.svelte"
      provides: "`probe` prop (default false), abortable $effect, `.dc-meta` label, probed-track handoff to downloadTrack"
      contains: "probe = false"
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "menu-open probe (opt-in surface), meta label in the Download row `.count` slot, skeleton while pending, probed-track handoff"
      contains: "probeDownload"
  key_links:
    - from: "src/lib/components/TrackMenu.svelte"
      to: "src/lib/services/download-probe.ts"
      via: "$effect on open && track → probeDownload(track, ac.signal); cleanup aborts"
      pattern: "probeDownload\\("
    - from: "src/lib/components/DownloadControl.svelte"
      to: "src/lib/services/download-probe.ts"
      via: "$effect gated on `probe && uid` → probeDownload; renders formatDownloadMeta"
      pattern: "formatDownloadMeta\\("
    - from: "src/lib/services/download-probe.ts"
      to: "src/lib/services/catalog.ts"
      via: "ensureTrackDetails(copy, signal, settings.downloadQuality)"
      pattern: "ensureTrackDetails\\("
    - from: "src/lib/services/download-probe.ts"
      to: "resolved audioUrl (CDN)"
      via: "RAW fetch HEAD → Range GET bytes=0-0 (never apiFetch)"
      pattern: "method: 'HEAD'"
    - from: "src/lib/services/download-track.ts"
      to: "src/lib/services/track-ready.ts"
      via: "hasFreshAudioUrl(track) && currentQualityMeets(track.quality, want) → reuse input track"
      pattern: "hasFreshAudioUrl\\(track\\)"
---

<objective>
Show the REAL format/quality and byte size on the download affordance, probed at the user's download tier when a focused download surface opens (user decision 1), through the shared download machinery (user decision 2), with the probe OPT-IN per call site and OFF by default so list rows never fan out.

Purpose: `settings.downloadQuality` is a request, not a promise — kuwo answers "lossless" with a 320K mp3, qq's ladder picks whatever tier it has. The user chose ~2-3 s of skeleton for truth over an instant guess.

Output: `download-probe.ts` (+ tests), a `probe` prop on `DownloadControl.svelte`, the label on TrackMenu's Download row, and one small reuse extension in `download-track.ts` so the tap downloads the file the label described.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@src/lib/services/download-track.ts
@src/lib/services/download-track.test.ts
@src/lib/services/download-filename.ts
@src/lib/services/track-ready.ts
@src/lib/services/abort-signal.ts
@src/lib/services/prewarm.ts
@src/lib/components/DownloadControl.svelte
@src/lib/components/TrackMenu.svelte

<planner_findings>
Verified against the codebase before planning — these CORRECT the shape given in the task brief:

1. **NowPlaying has NO download affordance of its own.** `grep -i download NowPlaying.svelte` hits only a passive up-next indicator (line ~1607). NowPlaying's download path is the `TrackMenu` it mounts (import at line 56). So "the NowPlaying download button" surface IS TrackMenu — no NowPlaying edit.
2. **TrackMenu does NOT mount DownloadControl** and cannot without regressing: its header `hd-btn` and list `.mi` row read a blob-backed `blobPresent` Check state (quick-260913-jq4) that DownloadControl lacks, and the row is a full-width text button, not a 40×40 icon. Per decision 2's fallback clause, TrackMenu consumes the SAME `probeDownload` service and the SAME `formatDownloadMeta` helper. No second implementation.
3. **Every current DownloadControl call site is a list row** (library/+page.svelte ×4, album/[name] per-row, RowBadges is passive). So `probe={true}` has no caller today; the prop exists so the contract "default off" is enforced by the component (hard constraint), and TrackMenu is the live opt-in surface.
4. **Menu-open call budget is up to 3, not 2, at the default `downloadQuality='lossless'`:** TrackMenu already fires `prewarmTrack(track)` on open (31-D-03, streaming tier). The probe cannot replace it — a lossless resolve must never become the STREAMING url (52 MB FLAC on cellular, see memory). When downloadQuality is auto/320/128 the probe reuses a fresh resolved track and issues only the HEAD.
5. **`Content-Range` is NOT CORS-safelisted**; the qq CDN exposes `Content-Length,Content-Range` explicitly (player.svelte.ts:2882 note, verified live). Other CDNs may hide both → the ladder ends in `bytes: null` and the label shows format alone. `Content-Length` IS safelisted (download-progress.ts:26). `Range: bytes=0-0` is a safelisted REQUEST header (no preflight).
6. **qq CDN sends `Content-Type: application/x-www-form-urlencoded` for audio** (download-filename.ts `audioMimeForUrl` note) — so the header only counts as a container signal when it starts with `audio/`.
7. **No byte formatter exists** in `src/lib` — write one (1 KB = 1024, one decimal for MB/GB, integer KB).
8. **No new i18n keys are required.** The label is composed of source-emitted tokens (`FLAC`, `320K`, `128k AAC`) plus unit symbols (`KB`/`MB`/`GB`) and the ` · ` separator already used raw in en.ts. The pending-state aria-label reuses the existing `menu.preparing`. `i18n.test.ts` parity therefore stays green with no locale edits — this is the i18n task collapsing to a verified no-op, not a skipped one.
</planner_findings>

<interfaces>
From src/lib/sources/types.ts:
```typescript
interface Track { uid: string; source: SourceId; audioUrl: string | null; quality: string | null; qualityLabel: string | null; detailsLoaded: boolean; resolvedAt?: number; lrc: string | null; lrcUrl?: string | null; /* … */ }
```
From src/lib/services/catalog.ts:
```typescript
export async function ensureTrackDetails(track: Track, signal?: AbortSignal, quality?: DefaultQuality): Promise<Track>;
```
From src/lib/services/track-ready.ts: `export function hasFreshAudioUrl(track: Track): boolean;`
From src/lib/services/abort-signal.ts: `export function combinedSignal(timeoutMs: number, caller?: AbortSignal): AbortSignal;`
From src/lib/services/download-filename.ts: `export const AUDIO_EXTENSIONS = ['mp3','flac','m4a','aac','ogg','wav'] as const;` (extFromAudioUrl is FILENAME-ONLY — its 'mp3' default must never be displayed)
From src/lib/services/download-track.ts (private today, EXPORT in Task 1): `function currentQualityMeets(curQuality: string | null, want: DefaultQuality): boolean` — auto/320/128 → any resolved stream meets; lossless → only when `quality === 'lossless'`.
From src/lib/stores/settings.svelte.ts: `export type DefaultQuality = 'auto' | 'lossless' | '320' | '128'; settings.downloadQuality: DefaultQuality`
Quality vocabulary emitted by adapters: `quality` ∈ {'lossless','320k', qq/joox ladder tags}; `qualityLabel` ∈ {'LOSSLESS','320K','128k AAC', qq/joox `best.label`}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: download-probe service + formatter, and let download-track reuse a tier-satisfying input track</name>
  <files>src/lib/services/download-probe.ts, src/lib/services/download-probe.test.ts, src/lib/services/download-track.ts, src/lib/services/download-track.test.ts</files>
  <behavior>
    - formatBytes(0) → '0 KB'; formatBytes(1023) → '1 KB'; formatBytes(8_493_465) → '8.1 MB'; formatBytes(40_055_930) → '38.2 MB'; formatBytes(1_288_490_189) → '1.2 GB'; formatBytes(null | NaN | negative) → null
    - containerFromContentType('audio/flac') → 'flac'; ('audio/mpeg') → 'mp3'; ('audio/mp4') → 'm4a'; ('application/x-www-form-urlencoded') → null; (null) → null
    - containerFromUrl('https://cdn/x/abc.FLAC?vkey=1') → 'flac'; ('https://cdn/x/abc') → null (NEVER 'mp3' by default); ('https://cdn/x/abc.m4a') → 'm4a'
    - formatDownloadMeta({ container:'flac', qualityLabel:'LOSSLESS', bytes:40_055_930 }) → 'FLAC · 38.2 MB'; ({ container:'mp3', qualityLabel:'320K', bytes:8_493_465 }) → '320K · 8.1 MB'; ({ container:null, qualityLabel:'320K', bytes:null }) → '320K'; ({ container:'m4a', qualityLabel:null, bytes:null }) → 'M4A'; all-null → null
    - probeDownload: resolve ok + HEAD 200 with content-length 40055930 and content-type audio/flac → { container:'flac', qualityLabel: track.qualityLabel, bytes:40055930, track: resolved }; exactly one ensureTrackDetails call carrying settings.downloadQuality as its 3rd arg and a copy with detailsLoaded:false/audioUrl:null/lrc:null
    - probeDownload: HEAD 405 (or ok but no content-length) → second fetch is GET with headers Range 'bytes=0-0'; 206 with content-range 'bytes 0-0/8493465' → bytes 8493465; response body is cancelled
    - probeDownload: HEAD throws AND Range GET has no content-range → bytes null, container from URL ext if any, qualityLabel still reported
    - probeDownload: resolve rejects / returns audioUrl null → { container:null, qualityLabel:null, bytes:null, track:null }; never rejects (await resolves)
    - probeDownload: memo — second call with same uid + same settings.downloadQuality does NOT call ensureTrackDetails or fetch again; a null-bytes-AND-null-container result is NOT memoised (reopen retries); changing settings.downloadQuality is a different key
    - probeDownload: reuse — when player.current.uid === track.uid, hasFreshAudioUrl(current) and currentQualityMeets(current.quality, want) → NO ensureTrackDetails call, HEAD still fires on current.audioUrl; same reuse when the INPUT track itself is fresh and meets
    - probeDownload: an already-aborted caller signal → all-null sentinel, no memo write
    - downloadTrack (extend existing test file): input track with detailsLoaded, fresh resolvedAt, audioUrl and quality 'lossless' under want 'lossless' → ensureTrackDetails NOT called; same input with quality '320k' under want 'lossless' → ensureTrackDetails IS called (existing reuse-current tests keep passing)
  </behavior>
  <action>
    Create `src/lib/services/download-probe.ts` as a PURE-shaped, node-testable, never-throws service (house sentinel contract, mirror the header-comment style of download-track.ts and carry the id `quick-260915-26g`). Import `ensureTrackDetails` from `$lib/services/catalog`, `hasFreshAudioUrl` from `$lib/services/track-ready`, `combinedSignal` from `$lib/services/abort-signal`, `AUDIO_EXTENSIONS` from `$lib/services/download-filename`, `settings` from `$lib/stores/settings.svelte`, `player` from `$lib/stores/player.svelte` (READ-ONLY, D-18 — never assign, never bump gens), and `currentQualityMeets` from `$lib/services/download-track` (Task 1 exports it — see below; download-track does not import probe, so no cycle).

    Export type `DownloadProbe = { container: string | null; qualityLabel: string | null; bytes: number | null; track: Track | null }`.

    Pure helpers (exported, each with tests): `formatBytes(bytes: number | null | undefined): string | null` — 1 KB = 1024; < 1 MB → integer KB; < 1 GB → one-decimal MB; else one-decimal GB; non-finite/negative → null. `containerFromContentType(ct: string | null | undefined): string | null` — strip params, lowercase, accept ONLY `audio/*`, map via a small MIME→ext record inverse of download-filename's `AUDIO_MIME` (`audio/mpeg`→mp3, `audio/flac`→flac, `audio/mp4`→m4a, `audio/aac`→aac, `audio/ogg`→ogg, `audio/wav`|`audio/x-wav`→wav, `audio/x-flac`→flac), unknown audio subtype → null. `containerFromUrl(url: string | null): string | null` — the same `?`-strip + `$`-anchored regex over `AUDIO_EXTENSIONS` that `extFromAudioUrl` uses BUT returning null when nothing matches (comment: extFromAudioUrl's 'mp3' default is filename-only and must never be shown as fact). `formatDownloadMeta(p: Pick<DownloadProbe,'container'|'qualityLabel'|'bytes'>): string | null` — quality token = container is a lossless ext (flac/wav) ? container.toUpperCase() : (qualityLabel ?? container?.toUpperCase() ?? null); join `[token, formatBytes(bytes)].filter(Boolean)` with ' · '; empty → null.

    `probeDownload(track: Track, signal?: AbortSignal): Promise<DownloadProbe>`: key = `${track.uid}|${settings.downloadQuality}`; return the memo hit if present (plain module `Map`, cap ~200 entries with a clear-on-overflow like prewarm.ts `MAX_TRACKED_UIDS`). If `signal?.aborted` → NULL sentinel (`{container:null,qualityLabel:null,bytes:null,track:null}`). Resolve step — pick the first that holds: (a) `player.current` uid-equal, `hasFreshAudioUrl`, `currentQualityMeets(cur.quality, want)` → `{ ...cur }` COPY; (b) same three tests on the INPUT `track` → `{ ...track }`; (c) `await ensureTrackDetails({ ...track, detailsLoaded:false, audioUrl:null, lrc:null }, combinedSignal(PROBE_TIMEOUT_MS, signal), settings.downloadQuality)` — wrap in try/catch → sentinel. No audioUrl → sentinel. Size step — RAW `fetch` (NEVER apiFetch: absolute CDN url, media request — cite download-track.ts:119) with `referrerPolicy: 'no-referrer'` (matches player.svelte.ts:2879) and `combinedSignal(PROBE_TIMEOUT_MS, signal)`: first `fetch(url, { method:'HEAD' })`; if ok and `content-length` parses to a finite > 0 number → bytes; else `fetch(url, { headers:{ Range:'bytes=0-0' } })`, parse `content-range` `/\/(\d+)$/` → bytes, and `void resp.body?.cancel()` so no bytes stream. Take `content-type` from whichever response answered for `containerFromContentType`, falling back to `containerFromUrl(audioUrl)`. Every fetch failure is caught → bytes stays null. `PROBE_TIMEOUT_MS = 8000` as a module constant. After the ladder, if `signal?.aborted` → return sentinel WITHOUT memoising. Memoise only when `bytes != null || container != null` (a total miss must retry on reopen). Export `__resetDownloadProbe()` (TEST-ONLY, mirrors prewarm's `__resetPrewarm`).

    In `src/lib/services/download-track.ts`: change `function currentQualityMeets` to `export function currentQualityMeets` (keep the comment). Extend the reuse decision: after computing `reuseCurrent`, add `const reuseInput = !reuseCurrent && hasFreshAudioUrl(track) && currentQualityMeets(track.quality, settings.downloadQuality)` and when true set `r = { ...track }` (a copy) instead of re-resolving. Comment with `quick-260915-26g`: the probe hands the component a Track resolved at the download tier; without this branch the tap would re-resolve a THIRD time and could download a different file than the label described. The lossless guard in currentQualityMeets keeps a streaming-tier 320k track from being reused under want='lossless', so existing callers are unchanged in the case that matters. Add the two `downloadTrack` behaviors above to `download-track.test.ts` (use its existing hoisted `mocks` + `mkTrack` pattern; set `resolvedAt: Date.now()`).

    Tests in `download-probe.test.ts`: copy the `vi.mock` block shape from download-track.test.ts (mock `$lib/stores/player.svelte`, `$lib/stores/settings.svelte`, `$lib/services/catalog`; do NOT mock download-track — import the real `currentQualityMeets` through it, but its store imports are already mocked; if the import chain pulls a runes store not yet mocked, mock `$lib/stores/library.svelte`, `$lib/stores/cover-version.svelte`, `$lib/stores/names.svelte`, `$lib/stores/actionLog.svelte` the same way download-track.test.ts does). Stub `fetch` with `vi.stubGlobal` returning objects with `ok`, `status`, `headers: new Headers({...})`, `body: { cancel: vi.fn() }`. Call `__resetDownloadProbe()` in `beforeEach`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm vitest --run src/lib/services/download-probe.test.ts src/lib/services/download-track.test.ts</automated>
  </verify>
  <done>Both test files green; `download-probe.ts` exports probeDownload/formatBytes/formatDownloadMeta/containerFromUrl/containerFromContentType/__resetDownloadProbe; `grep -c "apiFetch" src/lib/services/download-probe.ts` = 0; `grep -c "export function currentQualityMeets" src/lib/services/download-track.ts` = 1; no `as any` outside tests.</done>
</task>

<task type="auto">
  <name>Task 2: DownloadControl `probe` prop (default false) with abortable effect, meta label, probed-track handoff</name>
  <files>src/lib/components/DownloadControl.svelte</files>
  <action>
    Add a `probe = false` prop (`/** Opt-in: resolve at settings.downloadQuality + HEAD the url to show real format/size. DEFAULT OFF — list rows (library, album, RowBadges) MUST stay off: a per-row probe is the fan-out this app engineered away (T-26-02-01, apiFetch governor). Only a single focused surface the user just opened may set it. */`). Update the header comment block with the `quick-260915-26g` decision record (user decision 1: probed truth over a settings guess; hard constraint: opt-in default off).

    State: `let probed = $state<DownloadProbe | null>(null)` and `let probing = $state(false)`. Effect: `$effect(() => { const target = resolved ?? track; if (!probe || !target || isDownloaded) { probed = null; probing = false; return; } const ac = new AbortController(); probing = true; untrack(() => probeDownload(target, ac.signal)).then((p) => { if (!ac.signal.aborted) { probed = p; probing = false; } }); return () => ac.abort(); })` — `untrack` from 'svelte' so the store reads inside the service do not re-trigger the effect (memory: restore-effect self-invalidation loop; TrackMenu's `blobStore.has` effect uses the same untrack pattern). Do NOT gate on `browser` beyond what the service already handles (the service only fetches; no DOM/storage). Derived: `const meta = $derived(probed ? formatDownloadMeta(probed) : null)`.

    Rendering (idle branch only — downloaded/busy/unavailable branches unchanged): when `probe` is true, wrap the existing `<button class="dc">` in a `<span class="dc-wrap">` and append `{#if probing}<span class="dc-meta skel" aria-hidden="true"></span>{:else if meta}<span class="dc-meta">{meta}</span>{/if}`; extend the button's `aria-label`/`title` to `meta ? `${t('menu.download')} · ${meta}` : t('menu.download')`. When `probe` is false render EXACTLY today's markup (no wrapper) so the 40×40 list footprint is untouched. CSS: `.dc-wrap { display:inline-flex; align-items:center; gap:4px; }`, `.dc-meta { font-size: 11px; color: var(--color-text-muted); white-space: nowrap; }`, `.dc-meta.skel { width: 64px; height: 11px; border-radius: var(--radius-full); background: var(--color-surface); }`.

    In `run()`: pass `probed?.track ?? target` to `downloadTrack` when `probed.track.uid === target.uid` — the probed Track carries the download-tier URL, so download-track's new `reuseInput` branch skips the third resolve and downloads the file the label described (freshness is re-checked there by `hasFreshAudioUrl`, so a stale probe simply re-resolves).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -5 && grep -c "probe = false" src/lib/components/DownloadControl.svelte && ! grep -rn "probe={true}\|probe$\|probe}" 'src/routes/(app)/library/+page.svelte' 'src/routes/(app)/album/[name]/+page.svelte' src/lib/components/RowBadges.svelte</automated>
  </verify>
  <done>`pnpm check` reports 0 errors; `probe` defaults false; no list call site passes `probe`; idle markup with `probe` false is byte-identical in structure to today (single `<button class="dc">`).</done>
</task>

<task type="auto">
  <name>Task 3: TrackMenu opt-in — probe on open, label in the Download row, probed-track handoff; full gates</name>
  <files>src/lib/components/TrackMenu.svelte</files>
  <action>
    Import `probeDownload, formatDownloadMeta, type DownloadProbe` from `$lib/services/download-probe`. Add `let dlProbe = $state<DownloadProbe | null>(null)` and `let dlProbing = $state(false)`. Add ONE `$effect` next to the existing `blobStore.has` effect (same shape): when `open && track && !isDevice && blobPresent !== true` → `const ac = new AbortController(); dlProbing = true; untrack(() => probeDownload(track, ac.signal)).then((p) => { if (!ac.signal.aborted) { dlProbe = p; dlProbing = false; } }); return () => ac.abort();` else reset both to null/false. Comment block (`quick-260915-26g`): this is the ONE opt-in surface (user decision 1 + hard constraint); it sits beside the 31-D-03 prewarm and does NOT replace it — a download-tier (lossless) resolve must never become the streaming url, so at downloadQuality='lossless' menu-open costs prewarm + probe resolve + HEAD (the user accepted the probe cost); at auto/320/128 the probe reuses the fresh resolved track and issues only the HEAD. Cleanup aborts on close so no stale label lands (same `alive`/abort discipline as blobPresent).

    Derived `const dlMeta = $derived(dlProbe ? formatDownloadMeta(dlProbe) : null)`. Markup, idle Download ROW only (the `{:else}` `<button class="mi" onclick={startDownload}>` at ~line 492): append `{#if dlProbing}<span class="count skel" aria-hidden="true"></span>{:else if dlMeta}<span class="count">{dlMeta}</span>{/if}` — reuse the existing `.count` class the progress percentage already uses so no new layout rule is needed; add only `.count.skel { display:inline-block; width:64px; height:11px; border-radius:var(--radius-full); background:var(--color-surface); }`. Header `hd-btn` idle branch: `title={dlMeta ? `${t('menu.download')} · ${dlMeta}` : t('menu.download')}` and the same for `aria-label` (icon-only slot, no visible text). Downloading/downloaded branches unchanged. No new i18n keys (planner finding 8): tokens are source labels + unit symbols; the skeleton is `aria-hidden` and the button keeps `menu.download`.

    `startDownload()`: call `downloadTrack(dlProbe?.track && dlProbe.track.uid === track.uid ? dlProbe.track : track)` so the tap reuses the probed download-tier URL via download-track's `reuseInput` branch (comment: the label and the file must agree).

    Then run the full gates. Confirm `i18n.test.ts` passes untouched (no locale edits), and confirm no `as any` was introduced in production source.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test 2>&1 | tail -8 && pnpm check 2>&1 | tail -3 && grep -c "probeDownload(" src/lib/components/TrackMenu.svelte && git diff --name-only -- src/lib/i18n | wc -l</automated>
  </verify>
  <done>`pnpm test` and `pnpm check` green; TrackMenu calls probeDownload exactly once (inside the open-gated effect); `git diff --name-only -- src/lib/i18n` is empty; opening a ⋮ menu on a song shows a skeleton then `FLAC · 38.2 MB`-style text in the Download row and in the header button title, closing mid-probe leaves no stale label, and library/album rows issue no probe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → CDN (HEAD / Range GET) | untrusted response headers (`content-length`, `content-range`, `content-type`) from third-party CDNs |
| client → /api/[source] resolve | already-governed via ensureTrackDetails → apiFetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26g-01 | Tampering | download-probe header parsing | mitigate | `Number()` + `isFinite` + `> 0` on content-length / content-range; a garbage header yields `bytes:null`, never NaN in the UI |
| T-26g-02 | Denial of Service | per-row probe fan-out | mitigate | `probe` default false on DownloadControl; only TrackMenu opts in; memo per `uid|quality`; abort on close; 8 s timeout via combinedSignal |
| T-26g-03 | Information Disclosure | probe fetch to CDN | accept | same absolute audioUrl the download already fetches; `referrerPolicy:'no-referrer'` like the existing prebuffer fetch; no new host surface |
| T-26g-04 | Tampering | reuseInput in download-track | mitigate | `hasFreshAudioUrl` (15-min TTL) + `currentQualityMeets` guard; a stale or under-tier input still re-resolves |
| T-26g-SC | Tampering | npm installs | accept | no new dependency in this plan |
</threat_model>

<verification>
- `pnpm test` green (download-probe.test.ts, download-track.test.ts, i18n.test.ts parity untouched).
- `pnpm check` 0 errors.
- Browser (dev server 4321 or 5173; kuwo + Deezer reachable in sandbox): open ⋮ on a kuwo search result → Download row shows skeleton then e.g. `320K · 8.1 MB`; reopen → instant (memo); scroll the library page → network tab shows no HEAD/resolve per row.
</verification>

<success_criteria>
- Real format/quality + byte size rendered on the ⋮ menu Download row after a probe at settings.downloadQuality; plain `Download` on any probe failure.
- Probe is opt-in (`probe` prop default false); list surfaces unchanged and issue zero extra requests.
- Probe never throws, is memoised per `uid|quality`, aborts on close, uses RAW fetch (HEAD → Range → null), never shows the `extFromAudioUrl` default as a container.
- Tapping Download after a probe reuses the probed URL (no third resolve) when fresh and tier-satisfying.
- No new i18n keys needed; all locale files untouched; parity test green.
</success_criteria>

<output>
Create `.planning/quick/260915-26g-show-format-quality-and-size-on-download/260915-26g-SUMMARY.md` when done.
</output>
