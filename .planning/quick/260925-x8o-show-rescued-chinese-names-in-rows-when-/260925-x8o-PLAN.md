---
phase: quick-260925-x8o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/name-rescue.ts
  - src/lib/services/name-rescue.test.ts
  - src/lib/stores/names.svelte.ts
  - src/lib/stores/names.test.ts
  - src/lib/components/SongRow.svelte
  - src/lib/components/NpUpNext.svelte
  - src/lib/components/NpRelated.svelte
  - src/lib/components/Nowbar.svelte
  - src/lib/components/NowPlaying.svelte
  - src/lib/components/TrackMenu.svelte
  - src/lib/components/VersionPicker.svelte
  - src/routes/(app)/+page.svelte
  - src/routes/(app)/search/+page.svelte
  - src/routes/(app)/settings/downloads/+page.svelte
  - src/routes/+layout.svelte
  - src/lib/stores/player.svelte.ts
  - src/lib/services/download-track.ts
  - src/lib/services/download-track.test.ts
  - CLAUDE.md
autonomous: true
requirements: [quick-260925-x8o]

must_haves:
  truths:
    - "With Settings → Translation → Chinese script = zh-Hant, a shelf/row/Up-Next/Now Playing/Nowbar/track-menu/OS media card for the stub 'Coral Sea / Jay Chou' (after the wa7 rescue verified it) shows 珊瑚海 / 周杰倫; with zh-Hans it shows 珊瑚海 / 周杰伦"
    - "With Chinese script = off, every display string is byte-for-byte what it is today (no alias)"
    - "'Coral Sea' shown WITHOUT its artist, or with a different artist (Black Pearl), is never aliased to 珊瑚海 — the title alias is keyed by the pair"
    - "A verified pair survives reloads and keeps displaying after the 30-day lookup TTL; a cached miss never produces an alias"
    - "When the rescue writes a new pair during a tap, visible rows repaint to the Chinese names without a reload"
    - "The display path makes no network call and reads localStorage once per session for the alias map"
    - "Aliased text is never sent through /api/translate (it goes alias → script lock only)"
  artifacts:
    - path: "src/lib/services/name-rescue.ts"
      provides: "readRescueHits() TTL-free hit iterator + onRescueHit() subscribe/notify hook"
      exports: ["readRescueHits", "onRescueHit", "RescueHitListener"]
    - path: "src/lib/stores/names.svelte.ts"
      provides: "alias layer in the display seam: dnTitle(text, artist?) / dnArtist(text) consult an in-memory pair map before the lock"
      contains: "dnTitle(text: string, artist?: string)"
    - path: "src/lib/stores/names.test.ts"
      provides: "alias contract tests (off = byte-for-byte, pair keying, both scripts, TTL-free, miss, live repaint, malformed store, one hydration read, source-grep of song-title call sites)"
    - path: "src/lib/services/name-rescue.test.ts"
      provides: "readRescueHits / onRescueHit tests"
  key_links:
    - from: "src/lib/stores/names.svelte.ts"
      to: "src/lib/services/name-rescue.ts"
      via: "readRescueHits() on first lock-on lookup + onRescueHit() registered in the constructor"
      pattern: "onRescueHit\\("
    - from: "src/lib/services/name-rescue.ts writeRescueCache"
      to: "names.rev"
      via: "listener bumps rev on a non-null (hit) write"
      pattern: "listeners|rescueListeners"
    - from: "src/lib/components/SongRow.svelte (and every song-title surface)"
      to: "names.dnTitle(track.title, track.artist)"
      via: "second argument = the SAME track's raw artist"
      pattern: "dnTitle\\([a-zA-Z.?]+\\.title[^,)]*, [a-zA-Z.?]+\\.artist"
    - from: "src/lib/stores/names.svelte.ts alias path"
      to: "applyLock"
      via: "alias bypasses resolveTranslated; the lock renders the selected script"
      pattern: "applyLock\\("
---

<objective>
Make a wa7-verified English→Chinese pair DISPLAY: after `resolveStub` rescued "Coral Sea / Jay Chou" to 珊瑚海 / 周杰倫, every surface that shows that song's name renders the Chinese names — but only while `settings.zhScript` is `zh-Hant` or `zh-Hans`, in that script. Artist alias by artist text alone; title alias only with the same track's artist as context.

Purpose: the user sees the song they recognise (珊瑚海 / 周杰倫) instead of the label's English name, consistently across rows, shelves, Up Next, Now Playing, Nowbar, the track menu, the document title and the OS media card, with zero per-component logic.

Output: `readRescueHits` + `onRescueHit` in `name-rescue.ts`; alias layer in `names.svelte.ts` (`dnTitle(text, artist?)`); ~30 one-line call-site edits passing the sibling artist; tests; CLAUDE.md row.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/quick/260925-x8o-show-rescued-chinese-names-in-rows-when-/260925-x8o-CONTEXT.md
@.planning/quick/260925-wa7-rescue-english-and-romanized-chinese-son/260925-wa7-SUMMARY.md
@src/lib/services/name-rescue.ts
@src/lib/stores/names.svelte.ts
@src/lib/stores/names.test.ts
@src/lib/services/name-rescue.test.ts

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From src/lib/services/name-rescue.ts (pure, no runes, no $app/*):
```typescript
export interface ZhName { artist: string; title: string }
export const NAME_RESCUE_KEY = 'openmusic:name-rescue:v1';
// record shape: Record<matchKey(artist,title), { a: string; t: string; at: number } | { miss: true; at: number }>
// module-private today: loadRecord(): Record<string, unknown>; validName(v): v is string (1..200 chars)
export function readRescueCache(artist: string, title: string): ZhName | 'miss' | null;
export function writeRescueCache(artist: string, title: string, value: ZhName | null): void; // called by lookupChineseName for hit AND miss
export async function lookupChineseName(artist: string, title: string): Promise<ZhName | null>;
```

From src/lib/services/match-key.ts (the ONE normalizer; name-rescue keys with it):
```typescript
export function norm(s: string): string;                 // lowercase, drops (…)/[…], strips ALL non-letter/number → no '|' can survive
export function matchKey(artist: string, title: string): string; // `${norm(artist)}|${norm(title)}` (artist-first)
```

From src/lib/util/artist-split.ts:
```typescript
export function splitArtists(raw: string): string[]; // splits on , & 、 / feat. ft. x ×; [] for empty
```

From src/lib/stores/settings.svelte.ts:
```typescript
settings.zhScript: 'off' | 'zh-Hant' | 'zh-Hans'  // $state; default is 'zh-Hant' since quick-260925-vtg
```

From src/lib/stores/names.svelte.ts (current public surface):
```typescript
class Names {
  rev = $state(0);
  private resolve(text, target, whitelist): string      // = applyLock(resolveTranslated(...))
  private applyLock(text: string): string               // reads settings.zhScript reactively; 'off' → byte-for-byte; warms dict; lockScriptSync
  zhLock(text: string): string;
  dnArtist(text: string): string;                       // resolve(text, effectiveTarget(settings.artistLang), settings.artistSkip)
  dnTitle(text: string): string;                        // resolve(text, effectiveTarget(settings.titleLang), settings.titleSkip)
  dnLastfm(text: string): string; dnBio(text: string): string; warm(): void; clearCache(): void;
}
export const names = new Names();
```

From src/lib/services/zh-convert.ts:
```typescript
export type ZhScript = 'zh-Hant' | 'zh-Hans';
export function lockScriptSync(text: string, target: ZhScript): string; // identity for non-Chinese / cold dict
export async function warmScript(target: ZhScript): Promise<void>;
```

names.test.ts fixture facts: `$app/environment` mocked `browser: true`; `settingsMock` (hoisted, mutable) with `zhScript: 'off'` restored in beforeEach; `translateMock` for `translateLinesEx`; in-memory `localStorageMock` backed by `memStore` (a `Map`); `vi.resetModules()` + fake timers per test; helper `warmLock(target)` in the 2jo describe awaits `zh.warmScript(target)` on the same module instance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: name-rescue.ts — TTL-free display hits reader + hit-write notify hook</name>
  <files>src/lib/services/name-rescue.ts, src/lib/services/name-rescue.test.ts</files>
  <behavior>
    - readRescueHits() returns a hit whose `at` is 40 days old (display ignores HIT_TTL_MS) as `{ key, zh: { artist, title } }` with key = the record key
    - readRescueHits() excludes `{ miss: true }` entries and skips malformed entries (non-object value, missing/non-string/over-200-char `a` or `t`) without throwing; `'{not json'` / `[1,2]` records → `[]`; no localStorage (node) → `[]`
    - onRescueHit(fn) is called with (artist, title, zh) exactly once when writeRescueCache(artist, title, zh) is called with a non-null value; NOT called for writeRescueCache(..., null)
    - the function returned by onRescueHit unsubscribes; a listener that throws does not throw out of writeRescueCache and does not stop other listeners
    - the listener still fires when localStorage.setItem throws (quota) — the in-memory alias must not depend on persistence succeeding
  </behavior>
  <action>
    In `src/lib/services/name-rescue.ts`, under the "Outcome cache" section, add two exports tagged `quick-260925-x8o` in the house comment style:

    1. `export function readRescueHits(): Array<{ key: string; zh: ZhName }>` — iterate `loadRecord()` (reuse it; do not re-parse) and, for every entry that is an object, has no `miss: true`, and passes the existing `validName` guard for both `a` and `t`, push `{ key, zh: { artist: e.a, title: e.t } }`. Deliberately ignore `at` / `HIT_TTL_MS`: a verified pair is a fact for DISPLAY, the TTL only governs re-lookup, and `MAX_ENTRIES` (write-side oldest-first eviction) still bounds the map. Return `[]` when `typeof localStorage === 'undefined'`. Never throws (loadRecord already swallows parse errors; wrap the loop in try/catch anyway so a hostile record shape cannot escape into the render path — T-x8o-01).

    2. `export type RescueHitListener = (artist: string, title: string, zh: ZhName) => void;` and a module-level `const rescueListeners = new Set<RescueHitListener>();` with `export function onRescueHit(fn: RescueHitListener): () => void` (add, return a deleter). In `writeRescueCache`, AFTER the existing try/catch block (so a quota failure still notifies), when `value` is non-null, call each listener inside its own try/catch. Note in the comment WHY this is a hook and not an import: `names.svelte.ts` (runes store) subscribes; this pure module must never import the store (CLAUDE.md pure/runes split). Keep the existing `typeof localStorage === 'undefined'` early return where it is — under SSR/node there is no display to notify.

    Do NOT change `readRescueCache`, `HIT_TTL_MS`, `MAX_ENTRIES`, or `lookupChineseName` — the lookup's own TTL semantics are unchanged.

    In `src/lib/services/name-rescue.test.ts`, add a `describe('display hits + notify (quick-260925-x8o)')` block next to the existing 'rescue cache' describe, reusing its localStorage stub idiom (`Object.defineProperty(globalThis, 'localStorage', …)` + restore), covering every bullet in `<behavior>`. For the quota case, stub `setItem` to throw and assert the listener still received the pair.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- src/lib/services/name-rescue.test.ts && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>`readRescueHits` and `onRescueHit` exported and covered by the new tests; all pre-existing name-rescue tests unchanged and green; the full gate `pnpm test && pnpm check && pnpm build` is green. Commit ONLY after the gate is green (`feat(quick-260925-x8o): name-rescue display hits + hit-write notify hook`) — main auto-pushes and auto-deploys. This commit is display-neutral (nothing consumes the exports yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: names.svelte.ts — alias layer in the display seam (dnTitle(text, artist?) / dnArtist)</name>
  <files>src/lib/stores/names.svelte.ts, src/lib/stores/names.test.ts</files>
  <behavior>
    Seed `memStore` key `openmusic:name-rescue:v1` with: `'jaychou|coralsea' → { a: '周杰倫', t: '珊瑚海', at: now − 40 d }`, `'lullaboy|someonelikeu' → { miss: true, at: now }`, `'jokerxue|theactor' → { a: '薛之謙, 阿蘭, 劉宇寧, 白舉綱 & 袁成傑', t: '演員', at: now }`. Fixture target langs stay `'ja'` (the default) so the translate API path is observable.
    - zhScript 'off': dnTitle('Coral Sea', 'Jay Chou') === 'Coral Sea' and dnArtist('Jay Chou') === 'Jay Chou' (byte-for-byte pin; translateMock may be called — that is today's behaviour)
    - zhScript 'zh-Hant' (after warmLock): dnTitle('Coral Sea', 'Jay Chou') === '珊瑚海', dnArtist('Jay Chou') === '周杰倫', and translateMock was NOT called for those two (alias bypasses the API)
    - zhScript 'zh-Hans' (after warmLock('zh-Hans')): dnArtist('Jay Chou') === '周杰伦' and dnTitle('Coral Sea', 'Jay Chou') === '珊瑚海' (rendered in the selected script)
    - pair keying under zh-Hant: dnTitle('Coral Sea') === 'Coral Sea' (no artist → no title alias); dnTitle('Coral Sea', 'Black Pearl') === 'Coral Sea'; dnTitle('Coral Sea (Chillout Mix)', 'Black Pearl') === 'Coral Sea (Chillout Mix)'
    - the 40-day-old hit still displays (covered by the positive test — assert with a comment that `at` is past HIT_TTL)
    - a cached miss never aliases: dnTitle('someone like u', 'lullaboy') === 'someone like u'
    - multi-performer zh artist: dnTitle('The Actor', 'Joker Xue') === '演員' but dnArtist('Joker Xue') === 'Joker Xue'
    - live repaint: import `writeRescueCache` from the REAL `$lib/services/name-rescue` after the same `vi.resetModules()`; with lock zh-Hant, `const before = names.rev; writeRescueCache('Eric Chou', 'Zai Ai Ni', { artist: '周興哲', title: '再愛你' })` → `names.rev > before` and dnTitle('Zai Ai Ni', 'Eric Chou') === '再愛你', dnArtist('Eric Chou') === '周興哲'
    - malformed store: `'{not json'`, and `{ 'x|y': 'str', 'a|b': { a: 1, t: 2 } }` → originals returned, nothing throws
    - one hydration read: `vi.spyOn(localStorageMock, 'getItem')`; after 50 mixed dnTitle/dnArtist calls under zh-Hant, calls whose first arg is `'openmusic:name-rescue:v1'` number exactly 1
    - zhLock(text) is unaffected (no alias — it takes no artist and is not a song-name accessor)
  </behavior>
  <action>
    In `src/lib/stores/names.svelte.ts`:

    Imports: `import { readRescueHits, onRescueHit, type ZhName } from '$lib/services/name-rescue';`, `import { matchKey, norm } from '$lib/services/match-key';`, `import { splitArtists } from '$lib/util/artist-split';`. (Import graph verified cycle-free: name-rescue → i18n/detect, match-key, api-base, abort-signal, sources/ytmusic; none import a store.)

    Add PLAIN class fields (not `$state` — the house convention for internal maps/guards the UI never reads directly; reactivity rides `rev` + `settings.zhScript`): `private titleAlias = new Map<string, string>()` keyed by the record key (`matchKey` form `normArtist|normTitle`) → zh title; `private artistAlias = new Map<string, string>()` keyed by `norm(artist)` → zh artist; `private aliasHydrated = false`.

    Add a constructor: `if (browser) onRescueHit((artist, title, zh) => { this.recordAlias(matchKey(artist, title), zh); this.rev++; });` — this is the live-repaint path (D: "visible rows must repaint without a reload").

    `private recordAlias(key: string, zh: ZhName)`: `titleAlias.set(key, zh.title)`; artist part = `key.slice(0, key.indexOf('|'))` (norm strips every non-letter/number, so `|` can only be the separator); set `artistAlias` ONLY when that part is non-empty AND `splitArtists(zh.artist).length === 1`. Comment: the wa7 live smoke paired `Joker Xue / The Actor` with the HK credit line `薛之謙, 阿蘭, 劉宇寧, 白舉綱 & 袁成傑`; an artist-alone alias (locked: no context) cannot attribute a multi-performer credit to one raw artist, so only a single-performer zh artist becomes an artist alias. The pair-keyed title alias still applies. Tag `ponytail: single-performer rule; add per-part alignment if a "A & B" ↔ "甲, 乙" pair is ever needed.`

    `private hydrateAliases()`: `if (!browser || this.aliasHydrated) return; this.aliasHydrated = true;` then `for (const { key, zh } of readRescueHits()) this.recordAlias(key, zh);` inside try/catch. ONE localStorage read per session (T-x8o-03).

    `private aliasOn(): boolean`: `const s = settings.zhScript;` (reactive read — flipping the setting repaints) `return browser && (s === 'zh-Hant' || s === 'zh-Hans');`

    `private aliasArtist(text: string): string | null`: `void this.rev;` `if (!text || !this.aliasOn()) return null; this.hydrateAliases(); if (!this.artistAlias.size) return null; return this.artistAlias.get(norm(text)) ?? null;`
    `private aliasTitle(text: string, artist: string | undefined): string | null`: same shape; require `artist` truthy; look up `this.titleAlias.get(matchKey(artist, text))`. The lookup uses the SAME `matchKey` the rescue keys with (D: "reuse its exported helpers — do not re-inline a second normalizer").

    Change the accessors: `dnArtist(text)` → `const a = this.aliasArtist(text); return a !== null ? this.applyLock(a) : this.resolve(text, effectiveTarget(settings.artistLang), settings.artistSkip);`. `dnTitle(text: string, artist?: string)` → `const a = this.aliasTitle(text, artist); return a !== null ? this.applyLock(a) : this.resolve(text, effectiveTarget(settings.titleLang), settings.titleSkip);`. Backward compatible: a bare `dnTitle(text)` never title-aliases. Update the JSDoc: the second argument is the SAME track's raw artist; album/artist-name callers must NOT pass one.

    Decision (record in the seam comment, tag `quick-260925-x8o`): aliased text BYPASSES `resolveTranslated` and goes to `applyLock` only. The alias IS the Chinese name the user asked to see; routing it through titleLang/artistLang could undo it (titleLang 'en' would send 珊瑚海 to /api/translate and show "Coral Sea" after a round trip), would put network on the render path, and could flicker. `applyLock` still renders the selected script (周杰倫 vs 周杰伦) and still warms the dict. The translation cache `openmusic:name-tr:*` is never written with alias text. `clearCache()` is left alone: the alias source is the RESOLUTION cache (`openmusic:name-rescue:v1`), not the translation cache — note this in a one-line comment.

    Do not touch `dnLastfm` / `dnBio` / `zhLock` / `warm()`.

    In `src/lib/stores/names.test.ts`, add `describe('names — rescued Chinese name aliases (quick-260925-x8o)')` after the 2jo block, reusing its `warmLock` helper idiom (re-declare locally or hoist it — the file re-imports zh-convert per test after resetModules), covering every bullet in `<behavior>`. Seed the rescue record via `memStore.set('openmusic:name-rescue:v1', JSON.stringify({...}))` BEFORE importing `./names.svelte`. Do not touch the existing structural share-composition regexes in this task (they change with the call sites in Task 3).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- src/lib/stores/names.test.ts && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>The seam aliases under zh-Hant/zh-Hans and is byte-for-byte unchanged under off; all bullets in `<behavior>` pass; every pre-existing names/2jo/urx test is green; full gate green. Commit ONLY after the gate is green (`feat(quick-260925-x8o): alias rescued Chinese names in the names display seam`). This intermediate commit renders correctly: `dnArtist` already aliases everywhere (locked: artist alone) and bare `dnTitle(text)` is a no-op alias, so no surface shows a wrong name — some song titles simply keep their English name until Task 3 lands.</done>
</task>

<task type="auto">
  <name>Task 3: Pass the sibling artist at every song-title call site + source-grep guard + CLAUDE.md</name>
  <files>src/lib/components/SongRow.svelte, src/lib/components/NpUpNext.svelte, src/lib/components/NpRelated.svelte, src/lib/components/Nowbar.svelte, src/lib/components/NowPlaying.svelte, src/lib/components/TrackMenu.svelte, src/lib/components/VersionPicker.svelte, src/routes/(app)/+page.svelte, src/routes/(app)/search/+page.svelte, src/routes/(app)/settings/downloads/+page.svelte, src/routes/+layout.svelte, src/lib/stores/player.svelte.ts, src/lib/services/download-track.ts, src/lib/services/download-track.test.ts, src/lib/stores/names.test.ts, CLAUDE.md</files>
  <action>
    Mechanical one-line edits: wherever a SONG title is rendered/emitted next to its own artist, change `names.dnTitle(X.title)` to `names.dnTitle(X.title, X.artist)` with the SAME object the adjacent `names.dnArtist(X.artist)` uses. Exact sites (line numbers as of HEAD db18376; match on the expression, not the number):

    - `src/lib/components/SongRow.svelte` :314 (aria-label) and :331 → `track.title, track.artist`
    - `src/lib/components/NpUpNext.svelte` :247 → `track.title, track.artist`
    - `src/lib/components/NpRelated.svelte` :170 → `track.title, track.artist`
    - `src/lib/components/Nowbar.svelte` :289 → `names.dnTitle(np?.title ?? "", np?.artist ?? "")` (this file uses double quotes — keep them)
    - `src/lib/components/NowPlaying.svelte` :995 → `names.dnTitle(player.current.title, player.current.artist)`
    - `src/lib/components/VersionPicker.svelte` :121 → `v.title, v.artist` (leave :81 — `v.album` is an album name)
    - `src/lib/components/TrackMenu.svelte` :377 (`fileSyncEntry` — keep D-7's "verbatim copy of the settings/downloads sweep entry" true by editing both), :582 and :617 (excludeUid label), :844 (`dTitle` for the share link — see note), :1010 (header) → `track.title, track.artist`; :1339 → `detailTrack.title, detailTrack.artist`. Leave :1341 (`detailTrack.album`). ALSO update the prose comment at :365 (`// library.downloads, so \`dnTitle(track.title)\` reproduces …`) to quote the new form `dnTitle(track.title, track.artist)` — it describes the :377 call site, and left stale it permanently matches both the `LEFT` gate and the TrackMenu `absent` regex (plan-check iteration 2 finding, applied by the orchestrator at max iterations).
    - `src/routes/(app)/+page.svelte` :1211, :1582, :1594, :1614 → `track.title, track.artist`; :1387, :1406, :1421 → `item.title, item.artist` (`DiscoveryTrack` has `artist`). LEAVE :1498/:1514/:1527 (`a.name` — artist/album tiles, not song titles).
    - `src/routes/(app)/search/+page.svelte` :679 → `names.dnTitle(s.title, s.kind === 'song' ? s.artist : undefined)` (`Suggestion.artist` is optional; album suggestions carry the album artist and must not title-alias)
    - `src/routes/(app)/settings/downloads/+page.svelte` :174 → `d.title, d.artist` (display names for the file sweep; the cache lookup on the next line stays RAW per RULE 1 — do not touch it)
    - `src/routes/+layout.svelte` :58 (document.title) → `cur.title, cur.artist`
    - `src/lib/stores/player.svelte.ts` :1020 (`own`), :1699/:4217/:4261 (`cur`), :3669 (`track`), :3863/:4086/:4143 (`resolved`) — each `makeMetadata({ title: names.dnTitle(X.title, X.artist), artist: names.dnArtist(X.artist), … })`. This is the OS media card.
    - `src/lib/services/download-track.ts` :193 → `names.dnTitle(r.title, r.artist)` (DL-FILE-01: the filename/tags already follow the lock via dn*, and 36 Pattern 5 requires the SAME display names in filename and tags; `buildDownloadFilename` → `sanitizeFilename` still sanitizes). `src/lib/services/download-track.test.ts` :209 → `expect(mocks.names.dnTitle).toHaveBeenCalledWith('Lightyears', 'G.E.M.')`.

    LEAVE `src/lib/components/MetadataEditor.svelte` :104 bare (CONTEXT: the editor shows the user's raw editable value — no alias). LEAVE album/artist-name uses (album pages, artist page, `al.name`, `a.name`, `v.album`, `detailTrack.album`).

    Share-link note for the SUMMARY (TrackMenu :844): with artist-alone aliasing locked, leaving the share title bare would produce a HYBRID link `/song/周杰倫/Coral Sea` that the recipient's resolveStub cannot rescue (the artist is no longer Latin, so the wa7 rescue does not fire). Passing the pair gives `/song/周杰倫/珊瑚海`, which the existing t2s rescue-on-miss (quick-260808-urx) resolves. Share links have carried DISPLAY names since urx; this keeps that principle and avoids the hybrid. Record it as the one deliberate deviation from CONTEXT's literal "share links unchanged" wording.

    In `src/lib/stores/names.test.ts`: (a) update the TrackMenu entry of the urx `it.each` structural test so `present` reads `/names\.dnTitle\(track\.title, track\.artist\)/` (the bare form no longer exists in that file); (b) add a source-grep guard `it.each([file, present[], absent[]])` in the x8o describe (same `readFileSync` idiom) covering EVERY code file this task edits. The `absent` regex is the load-bearing half: a `present` match is satisfied by one edited site, but any single dropped edit in a multi-site file leaves a bare form behind and fails `absent`. Since `artist` is optional, svelte-check/build cannot catch an omission — this test is what does. Rows:
    - `src/lib/components/SongRow.svelte` — present `/dnTitle\(track\.title, track\.artist\)/`; absent `/dnTitle\(track\.title\)/`
    - `src/lib/components/NpUpNext.svelte` — same pair
    - `src/lib/components/NpRelated.svelte` — same pair
    - `src/lib/components/Nowbar.svelte` — present `/dnTitle\(np\?\.title \?\? "", np\?\.artist \?\? ""\)/`; absent `/dnTitle\(np\?\.title \?\? ""\)/`
    - `src/lib/components/NowPlaying.svelte` — present `/dnTitle\(player\.current\.title, player\.current\.artist\)/`; absent `/dnTitle\(player\.current\.title\)/`
    - `src/lib/components/VersionPicker.svelte` — present `/dnTitle\(v\.title, v\.artist\)/` AND `/dnTitle\(v\.album\)/` (album stays bare); absent `/dnTitle\(v\.title\)/`
    - `src/lib/components/TrackMenu.svelte` — present `/dnTitle\(track\.title, track\.artist\)/`, `/dnTitle\(detailTrack\.title, detailTrack\.artist\)/`, `/dnTitle\(detailTrack\.album\)/`; absent `/dnTitle\(track\.title\)/`, `/dnTitle\(detailTrack\.title\)/`
    - `src/routes/(app)/+page.svelte` — present `/dnTitle\(track\.title, track\.artist\)/`, `/dnTitle\(item\.title, item\.artist\)/`, `/dnTitle\(a\.name\)/` (artist/album tiles stay bare); absent `/dnTitle\(track\.title\)/`, `/dnTitle\(item\.title\)/`
    - `src/routes/(app)/search/+page.svelte` — present `/dnTitle\(s\.title, s\.kind === 'song' \? s\.artist : undefined\)/`; absent `/dnTitle\(s\.title\)/`
    - `src/routes/(app)/settings/downloads/+page.svelte` — present `/dnTitle\(d\.title, d\.artist\)/`; absent `/dnTitle\(d\.title\)/`
    - `src/routes/+layout.svelte` — present `/dnTitle\(cur\.title, cur\.artist\)/`; absent `/dnTitle\(cur\.title\)/`
    - `src/lib/stores/player.svelte.ts` — present `/dnTitle\(own\.title, own\.artist\)/`, `/dnTitle\(cur\.title, cur\.artist\)/`, `/dnTitle\(track\.title, track\.artist\)/`, `/dnTitle\(resolved\.title, resolved\.artist\)/`; absent `/dnTitle\((own|cur|track|resolved)\.title\)/`
    - `src/lib/services/download-track.ts` — present `/dnTitle\(r\.title, r\.artist\)/`; absent `/dnTitle\(r\.title\)/`
    - `src/lib/components/MetadataEditor.svelte` — present `/names\.dnTitle\(tr\.title\)/`; absent `/dnTitle\(tr\.title, /` (pins the deliberate raw editor)
    Every `present`/`absent` pair MUST fail on HEAD db18376 before the edits and pass after — run the test file once before editing the call sites to see the 14 rows fail, then edit.

    `CLAUDE.md` Shared Primitives table: add one row — `| rescued Chinese name (display alias) | stores/names.svelte.ts (`dnTitle(text, artist)`, `dnArtist`) over services/name-rescue.ts (`readRescueHits`, `onRescueHit`) | never per-component; a song title passes its OWN artist, album/artist names pass none |`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && LEFT=$(grep -rn "dnTitle(" src --include='*.svelte' --include='*.ts' | grep -v '\.test\.' | grep -v 'names.svelte.ts' | grep -E "dnTitle\((track|item|cur|own|resolved|np\?|v|d|r|s|detailTrack|player\.current)(\?)?\.title\)" | grep -v MetadataEditor | wc -l | tr -d ' ') && echo "bare song-title dnTitle sites left: $LEFT" && test "$LEFT" -eq 0 && pnpm test -- src/lib/stores/names.test.ts src/lib/services/download-track.test.ts && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>The completeness gate reports `bare song-title dnTitle sites left: 0` and `test "$LEFT" -eq 0` exits 0 (on HEAD db18376 before the edits it reports 32 and exits 1 — that is the gate proving itself); the 14-row source-grep guard in names.test.ts and the updated download-track assertion pass, so a dropped edit in ANY of the 16 files fails `pnpm test` itself; full gate green. One commit for all call sites (no intermediate half-aliased state). Commit ONLY after the gate is green (`feat(quick-260925-x8o): show rescued Chinese names on every song-title surface`). After this commit every listed surface shows 珊瑚海 / 周杰倫 under the lock and is byte-for-byte unchanged with the lock off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → render | `openmusic:name-rescue:v1` is user-writable; alias text flows to Svelte text nodes, `document.title`, `MediaMetadata.title/artist` (web + native media session), the download filename and embedded tags |
| rescue write → store | `writeRescueCache` (pure, called from the resolve path) notifies the runes store synchronously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x8o-01 | Tampering | `readRescueHits` / alias maps | mitigate | Reuse the existing `validName` guard (string, 1..200 chars) per entry; non-object/array records → `[]`; every sink is a TEXT sink (Svelte escapes text nodes; `document.title` and `MediaMetadata` take strings, never URLs — the native media-session crash class is artwork URLs, not text); the download filename passes `sanitizeFilename`. Residual: a user can make their OWN UI show a wrong name — same class as editing `openmusic:name-tr:*` today (accept). |
| T-x8o-02 | Spoofing (wrong-song aliasing) | `aliasTitle` / `aliasArtist` | mitigate | Title alias keyed by `matchKey(artist, title)` — "Coral Sea" by Black Pearl / with no artist never becomes 珊瑚海 (pinned tests); artist alias only for a single-performer zh artist (`splitArtists(...).length === 1`) so a multi-performer HK credit is never attributed to one raw artist; only wa7-VERIFIED pairs are ever read (misses excluded; the unverified bilingual-title recording was deliberately NOT added). |
| T-x8o-03 | Denial of service (render-path cost) | `dnTitle` / `dnArtist` on every row render | mitigate | Hydrate once per session (`aliasHydrated`; pinned by the getItem-count test); O(1) Map lookups; early return when the lock is off or the maps are empty; no network on the display path (alias bypasses `/api/translate`); the 500-entry cap bounds the maps. |
| T-x8o-04 | Repudiation / Information disclosure | alias path | accept | Nothing leaves the device on the display path; no new logging of names. |
| T-x8o-05 | Tampering (listener re-entrancy) | `onRescueHit` listeners | mitigate | Each listener runs in its own try/catch inside `writeRescueCache`; a throwing listener never breaks the resolve path or other listeners; the store's listener only mutates plain Maps and bumps `rev`. |
| T-x8o-SC | Tampering | package installs | accept | No package-manager installs in this plan (zero new dependencies). |
</threat_model>

<verification>
- `pnpm test && pnpm check && pnpm build` green after EACH task (main auto-pushes → auto-deploys; commit only on green).
- `grep -rn "onRescueHit(" src/lib/stores/names.svelte.ts` → 1 hit (constructor); `grep -rn "readRescueHits\|onRescueHit" src/lib/services/name-rescue.ts` → both exported.
- Task 3 completeness gate: `LEFT` (bare song-title `dnTitle(X.title)` sites outside MetadataEditor) is 0 and `test "$LEFT" -eq 0` exits 0; it exits 1 with LEFT=32 on the un-edited tree. The 14-row source-grep guard in names.test.ts fails `pnpm test` if any file loses its edit.
- Manual smoke (dev server 4321 or 5173, real upstreams reachable in-sandbox): Settings → Translation → Chinese script = zh-Hant; search "Jay Chou Coral Sea" on a shelf/row that carries the English stub, tap it; the row, Nowbar and Now Playing show 珊瑚海 / 周杰倫 without a reload; flip to zh-Hans → 周杰伦; flip to off → Coral Sea / Jay Chou.
</verification>

<success_criteria>
- Under zh-Hant/zh-Hans every listed surface renders the verified Chinese pair in that script; under off, byte-for-byte unchanged.
- Title alias requires the same track's artist; artist alias is by artist text alone (single-performer zh only).
- Pairs come only from `openmusic:name-rescue:v1` hits (no parallel store), display ignores the lookup TTL, misses never alias.
- A new rescue write repaints live via `onRescueHit` → `rev++`; the pure module never imports the store.
- Display path: no network, one localStorage read per session, aliased text never hits `/api/translate`.
- All decision-ref comments tagged `quick-260925-x8o`; CLAUDE.md Shared Primitives row added.
</success_criteria>

<output>
Create `.planning/quick/260925-x8o-show-rescued-chinese-names-in-rows-when-/260925-x8o-SUMMARY.md` when done. Record: the share-link deviation (paired vs hybrid), the single-performer artist rule, the skipped bilingual-title recording (why: single-row title evidence = wa7 deviation 1), and that `names.clearCache()` deliberately does not touch the rescue cache.
</output>
