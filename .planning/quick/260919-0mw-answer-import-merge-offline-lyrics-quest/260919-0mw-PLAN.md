---
phase: quick-260919-0mw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/cover-backfill.ts
  - src/lib/services/cover-backfill.test.ts
  - src/lib/stores/player.svelte.ts
  - src/lib/services/share.ts
  - src/lib/services/share.test.ts
  - src/lib/services/audio-tags.ts
  - src/lib/services/audio-tags.test.ts
  - src/lib/services/download-track.ts
  - src/lib/services/retag.ts
  - src/lib/components/NowPlaying.svelte
autonomous: true
requirements: [Q1, Q2, A1, A2, A3, A4]

must_haves:
  truths:
    - "The SUMMARY states whether app-data import merges or replaces, citing the code that decides it"
    - "The SUMMARY states whether offline playback reads lyrics from the downloaded file or needs the network"
    - "A cover resolve tries YouTube Music before iTunes before Deezer, still stopping at the first https cover"
    - "A share link copied from inside the APK points at https://openmusic.lol, not https://localhost"
    - "A share link copied from the deployed website is byte-identical to today"
    - "A downloaded file whose only known album name is its own song title carries NO album tag"
    - "NowPlaying's transport row has a download control that shows the downloaded state for an already-downloaded song"
  artifacts:
    - path: "src/lib/services/cover-backfill.ts"
      provides: "reordered track tier chain + HQ upgrade"
      contains: "ytmusic"
    - path: "src/lib/services/share.ts"
      provides: "public-origin fallback for localhost/capacitor/file origins"
      contains: "shareOrigin"
    - path: "src/lib/services/audio-tags.ts"
      provides: "pure albumTag() guard dropping a title-echoing album"
      contains: "albumTag"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "download control in the transport row"
      contains: "DownloadControl"
  key_links:
    - from: "src/lib/services/download-track.ts"
      to: "albumTag"
      via: "tag field build"
      pattern: "albumTag\\("
    - from: "src/lib/services/retag.ts"
      to: "albumTag"
      via: "tag field build"
      pattern: "albumTag\\("
    - from: "src/lib/services/share.ts"
      to: "apiUrl"
      via: "public origin reuse (no second hardcoded domain)"
      pattern: "api-base"
---

<objective>
Four independent small fixes plus two read-only code answers, all in one quick task.

Purpose: cover ranking now prefers YouTube Music; APK share links stop emitting `https://localhost`;
downloaded files stop claiming the song title is an album; NowPlaying gains a download control. Plus
two questions answered for the backlog (import merge semantics, offline lyrics self-sufficiency).

Output: 5 atomic, separately-committable changes + a SUMMARY carrying the Q1/Q2 findings.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Project rules that bind every task below:
- Svelte 5 runes only (`$state`/`$derived`/`$effect`/`$props`). Tabs. Single quotes in TS.
- High comment density is house style. Tag every non-obvious edit `quick-260919-0mw`.
- `pnpm check` and `pnpm test` must stay green. Do NOT push to origin.
- No new npm dependencies. No research.

<interfaces>
<!-- Extracted from the codebase during planning. Use directly — no exploration needed. -->

src/lib/services/cover-backfill.ts (the ONE ordered track chain; both public resolvers call it):
```typescript
async function tier(fn: () => Promise<string | null>): Promise<string | null>; // never-throw + https guard
async function resolveTrackChain(artist: string, title: string, signal?: AbortSignal): Promise<string | null>;
export async function resolveCoverForTrack(track: Track, signal?: AbortSignal): Promise<string | null>;
export async function resolveDeezerHQ(track: Track, signal?: AbortSignal): Promise<string | null>;
export async function collectCoverCandidates(track: Track, signal?: AbortSignal): Promise<CoverCandidate[]>;
```

src/lib/sources/registry.ts:
```typescript
export function onlySource(id: SourceId): Partial<Record<SourceId, boolean>>;
export const SOURCES: Record<SourceId, SourceAdapter>; // includes ytmusic
```

src/lib/services/catalog.ts:
```typescript
export async function searchAll(keyword: string, page?: number, prefs?: Partial<Record<SourceId, boolean>>, signal?: AbortSignal): Promise<SearchResult>;
```

src/lib/services/url-safety.ts:
```typescript
export function hasHttpsScheme(url: string | null | undefined): boolean;
```

src/lib/services/share.ts — FOUR functions each open with the same SSR-guarded origin read:
```typescript
const base = typeof location !== 'undefined' ? location.origin : '';
// in: shareUrl (L215), songShareUrl (L374), entityCardUrl (L408), entityShareUrl (L437)
export function isHttpsUrl(url): boolean; // /^https:\/\/\S+$/ — deliberately NOT hasHttpsScheme, leave alone
```

src/lib/services/api-base.ts:
```typescript
export function apiUrl(path: string): string; // BASE = import.meta.env.VITE_API_BASE ?? '' (read LAZILY per call)
```

src/lib/services/audio-tags.ts:
```typescript
export interface AudioTagFields { title?; artist?; album?; albumArtist?; trackNumber?; lyrics? }
export async function tagAudioBlob(blob, fields: AudioTagFields, artDataUrl): Promise<{blob, result, format}>;
// L166: if (fields.album) tag.setAlbum(fields.album);  ← 36-D-10 already omits an EMPTY album
```

src/lib/components/DownloadControl.svelte — the ONE shared tri-state download affordance:
```typescript
let { track = null, persist = true, resolve = null, probe = false } = $props();
// idle → Download icon | downloading → spinner | downloaded → greyed Check | unavailable → CircleAlert
// probe DEFAULTS FALSE and must stay false anywhere that could fan out per row.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Answer Q1 (import merge?) and Q2 (offline lyrics?) — READ ONLY</name>
  <files>(none — findings go in the SUMMARY only)</files>
  <action>
Read-only investigation. Change NO behaviour, write NO source file in this task.

Q1 — does app-data import MERGE or OVERWRITE? Read `src/lib/backup/backup-logic.ts` around the
`applyEnvelope` doc block (~L275) and the function body (~L309), plus `undoImport` (~L342) and
`hasUndoSnapshot` (~L373). Planning already located the answer marker: the doc block carries a
`35-D-08` decision ref that names the policy explicitly. Confirm it against the BODY (which keys it
drops, which it writes, whether an undo snapshot is taken first) — do not report the comment alone.
Also name the caller surface (`src/routes/(app)/settings/data/+page.svelte`) and whether the user is
warned before the destructive step.

Q2 — is offline playback lyrics-self-sufficient? Trace, in `src/lib/stores/player.svelte.ts`:
  - the offline-first branch (~L588-625 in `restore`, mirrored ~L3259-3290 in `play`): when
    `library.isDownloaded(uid)` and `blobStore.get(uid)` returns a blob, `ensureTrackDetails` (the
    network resolve) is SKIPPED and `enrichFromLocalFile(localTrack, offlineBlob, playGen)` runs.
  - `enrichFromLocalFile` (~L784-822) → `localEnrichment(uid, blob)` in
    `src/lib/services/local-tags.ts` (~L80-105) → `readAudioTags` → `tags.lyrics`, gated by
    `parseLRC(tags.lyrics).length > 0` (37-D-07: "has lyrics" must mean "renders lyrics").
  - the network fallback: `if (!found.lrc && !cur.lrc) this.backfillLyrics(q)` — state plainly that
    this ONLY fires when the file carried no usable embedded LRC.
  - the write side that makes it possible: `src/lib/services/download-track.ts` (~L248) passes
    `lyrics: r.lrc || undefined` into `tagAudioBlob` (the `quick-260915-062` seam).
Conclusion must state both halves: lyrics ARE read from the file when present, AND under what exact
condition a downloaded song still reaches for the network.

Record both answers in the SUMMARY under a `## Findings (Q1 / Q2)` heading, each with the file:line
citations above. Keep it to a short paragraph per question. Do NOT open a backlog phase, do NOT
propose fixes for 999.2 — it is explicitly out of scope.
  </action>
  <verify>
    <automated>git diff --quiet -- src/ &amp;&amp; echo "READ-ONLY OK"</automated>
  </verify>
  <done>SUMMARY contains a `## Findings (Q1 / Q2)` section answering both questions with file:line citations; `src/` is untouched by this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (A1): Reorder the cover chain to YouTube Music → iTunes → Deezer → CN</name>
  <files>src/lib/services/cover-backfill.ts, src/lib/services/cover-backfill.test.ts, src/lib/stores/player.svelte.ts</files>
  <behavior>
    - `resolveTrackChain`: a SOLID https YTM cover short-circuits — neither iTunes, Deezer nor CN is issued.
    - YTM miss (null / non-https) → iTunes is tried next; iTunes miss → Deezer; Deezer miss → CN.
    - A THROW in any one tier falls through to the NEXT tier (the existing `tier()` never-throw contract), it does not abort the chain.
    - An aborted signal returns null without issuing any further tier.
    - A non-https ('http://…', '', null) result from ANY tier is a miss, never returned and never cached.
    - The HQ upgrade tries YTM first and issues Deezer ONLY on a YTM miss.
    - On a SOLID hit both chains still write the uid layer (real uid only) AND the name layer, exactly as today.
  </behavior>
  <action>
THE ORDERED CALL SITES (planning grepped them all — there are three, plus the player's import line):

(a) `resolveTrackChain` (cover-backfill.ts ~L146) — the single source of truth for the fall-through
    chain. `resolveCoverForTrack`, `backfillCovers.resolveOne`, `lazyCover` and the player's
    `resolveCoverAsync` / `healCover` ALL route through it, so one edit here covers every consumer.
(b) `resolveDeezerHQ` (~L230) — the hot-path single-tier HQ upgrade called from
    `player.svelte.ts` L3747 (and imported at L44).
(c) `collectCoverCandidates` (~L412-439) — the cover picker's candidate ORDER.

NEW YTM TIER — reuse the existing machinery, add NO new module and NO new fetch:
  `const r = await searchAll(term, 1, onlySource('ytmusic'), signal);`
  `return dedupeBest(r.interleaved, settings.preferredSource)[0]?.cover ?? null;`
This is the SAME shape as today's tier-3 CN block. `onlySource` comes from `$lib/sources/registry`
(already exported, 3 prior call sites) so the fan-out hits ytmusic and nothing else, and because it
goes through `searchAll` → the adapter → `apiFetch`, it inherits the outbound governor (dedupe,
MAX_CONCURRENT, timeout, circuit breaker) for free. Thread `signal` exactly like the CN tier does.

(a) Reorder `resolveTrackChain` to: Tier 1 YTM → Tier 2 iTunes → Tier 3 Deezer → Tier 4 CN. Each
    tier stays wrapped in the existing `tier()` helper and each keeps its `if (signal?.aborted)
    return null` guard. Do NOT touch the `hasHttpsScheme` return guard, the negative-miss cache, the
    CAP=6 pool, or the ARTIST chain in `backfillArtistCovers` (Deezer → iTunes) — the request covers
    the track cover chain only.

(b) Rename `resolveDeezerHQ` → `resolveHqCover` (the old name becomes a lie) and make it a TWO-tier
    ladder: YTM first, Deezer only on a YTM miss. Update the import and the call site in
    `player.svelte.ts` (L44, L3747) and the surrounding doc comments that name the old function
    (L3726, L3731, L3758) plus the mention in `cover-cache.ts` L365. Keep the T-26-02-01 bound
    intact: still bounded to the now-playing track, still NEVER a per-tile fan-out. Note in the
    comment that a YTM miss now costs a second call (worst case 2, common case 1) — that is the
    deliberate price of the new ranking.

(c) Reorder the `Promise.all` tuple + the `all` array in `collectCoverCandidates` to
    own → ytmusic → itunes → deezer → CN so the picker grid agrees with the chain. The tiers stay
    PARALLEL (the picker enumerates, it does not fall through) and `MAX_CANDIDATES` stays 12. Add a
    ytmusic entry sourced the same `onlySource('ytmusic')` way, labelled `'ytmusic'`.

REWRITE the stale prose at the TOP of cover-backfill.ts (the `MULTI-TIER CHAINS` block, L11-30) so
it describes the new order and the new rate-limit reasoning — those comments are load-bearing
decision records and a stale one is worse than none. Keep every existing decision ref
(`T-0bb-01`, `T-26-02-01`, `quick-260607-0bb`, `D-10`, …) and ADD `quick-260919-0mw` noting the
reorder and why (user-chosen ranking).

COST NOTE to put in the comment, not to act on: YTM tier-1 means a cold Home issues an InnerTube POST
per uncached tile instead of an edge-cached Deezer hit. The existing bounds (CAP=6 pool, 5-min
negative-miss cache, skip-cached, apiFetch governor) already contain it; do not add a new throttle.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic &amp;&amp; pnpm vitest --run src/lib/services/cover-backfill.test.ts</automated>
  </verify>
  <done>Tier order is YTM → iTunes → Deezer → CN in `resolveTrackChain`; `resolveHqCover` is YTM→Deezer with no `resolveDeezerHQ` references left (`grep -rn "resolveDeezerHQ" src/` is empty); picker candidates ordered own→ytmusic→itunes→deezer→CN; header comment matches the code; cover-backfill tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (A2): Share links emit the public origin when the runtime origin is localhost/capacitor/file</name>
  <files>src/lib/services/share.ts, src/lib/services/share.test.ts</files>
  <behavior>
    - Runtime origin `https://openmusic.lol` → link origin unchanged (`https://openmusic.lol`).
    - Runtime origin `https://some-preview.pages.dev` → unchanged (any real web origin passes through).
    - Runtime origin `https://localhost` (the APK WebView) → link origin becomes `https://openmusic.lol`.
    - Runtime origin `capacitor://localhost` → `https://openmusic.lol`.
    - Runtime origin `http://localhost:4321` / `http://127.0.0.1:5173` (local dev) → `https://openmusic.lol`.
    - Runtime origin `file://` or `null` → `https://openmusic.lol`.
    - SSR (`typeof location === 'undefined'`) → `''`, exactly as today (relative link, unchanged).
    - The reported broken shape `https://localhost/song/古巨基/愛得太遲?ci=…` becomes `https://openmusic.lol/song/古巨基/愛得太遲?ci=…` — path segments, raw CJK and the `ci` carrier all byte-identical.
  </behavior>
  <action>
ROOT CAUSE: four functions each inline the same origin read —
`const base = typeof location !== 'undefined' ? location.origin : '';` at share.ts L215
(`shareUrl`), L374 (`songShareUrl`), L408 (`entityCardUrl`), L437 (`entityShareUrl`). The APP share
the user reported goes through `songShareUrl` (TrackMenu.svelte L503), but patching only that one
leaves the album/artist card links (album/[name] L515, artist/[name] L236) still emitting
`https://localhost` from the APK. Fix it ONCE, in a shared helper all four call.

Add ONE non-exported-by-default helper — export it so the test can pin it directly:

```
export function shareOrigin(): string
```

Contract:
  - SSR guard first: `typeof location === 'undefined'` → return `''` (preserves today's relative
    link and keeps the module server-importable — several loaders import this file).
  - Read `location.origin`. If it is a real web origin, return it UNCHANGED (the WEBSITE share must
    not change behaviour — that is an explicit requirement).
  - Otherwise (localhost by hostname, a `capacitor:`/`file:` protocol, or the literal string
    `'null'` an opaque origin yields) return the PUBLIC origin.
  - Match on hostname + protocol, not a substring of the whole origin string — `location.origin`
    for the APK is `https://localhost` and for dev is `http://localhost:4321`, and a naive
    `.includes('localhost')` would also rewrite a legitimate host like `localhost.example.com`.
    Parse with `new URL(location.origin)` inside a try/catch (never throw out of a share builder)
    and test `url.hostname === 'localhost' || url.hostname === '127.0.0.1' || url.hostname === '[::1]'`
    plus `url.protocol === 'capacitor:' || url.protocol === 'file:'`.

PUBLIC ORIGIN — do NOT hardcode a second copy of the domain. `src/lib/services/api-base.ts` already
holds it as the native build's `VITE_API_BASE` (= `https://openmusic.lol`). Reuse it:
`import { apiUrl } from '$lib/services/api-base'` and derive the origin from `apiUrl('/')`, or add a
tiny `export function apiOrigin(): string` next to `apiUrl` that returns
`import.meta.env.VITE_API_BASE ?? ''` — read LAZILY per call, matching apiUrl's existing posture so
`vi.stubEnv('VITE_API_BASE', …)` flips it in tests without a rebuild. Prefer the second: it is
clearer than string-trimming a URL, stays one line, and keeps the env read in the module that owns it.
Fall back to the literal `'https://openmusic.lol'` ONLY when `VITE_API_BASE` is unset (the web build
leaves it empty, and a web build that somehow reports a localhost origin is a dev server — the
developer still wants a shareable link).

Replace the inline read in all four functions with `const base = shareOrigin();`. Change NOTHING
else in those functions — path building, `encodePathSegment`, the `ci` cover token, `slugify`, the
`?play=` payload all stay byte-identical.

DO NOT touch `isHttpsUrl` (L530). CLAUDE.md records it as deliberately NOT merged with
`hasHttpsScheme`; it is unrelated to this fix.

Tag the helper and the four replaced reads with `quick-260919-0mw` and one sentence of WHY (the
Capacitor WebView origin is `localhost`, so a runtime-origin link is useless to the recipient).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic &amp;&amp; pnpm vitest --run src/lib/services/share.test.ts</automated>
  </verify>
  <done>`shareOrigin()` exists and is the only origin read in share.ts (`grep -c 'location.origin' src/lib/services/share.ts` returns 1); all four builders use it; tests cover the web-unchanged case AND the localhost/capacitor/file rewrite; no second hardcoded `openmusic.lol` outside the api-base fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (A3): Stop writing the song title into the downloaded file's ALBUM tag</name>
  <files>src/lib/services/audio-tags.ts, src/lib/services/audio-tags.test.ts, src/lib/services/download-track.ts, src/lib/services/retag.ts</files>
  <behavior>
    - `albumTag('Beauty Behind the Madness', 'The Hills')` → `'Beauty Behind the Madness'` (a real album survives).
    - `albumTag('The Hills', 'The Hills')` → `undefined`.
    - `albumTag('The Hills (Explicit)', 'The Hills (Explicit)')` → `undefined`.
    - Case/space/punctuation-insensitive: `albumTag('the hills', 'The Hills')` → `undefined`.
    - Multi-title form: `albumTag('过一招 (feat. 拉天糖)', '过一招 (feat. 拉天糖)', '過一招 (feat. 拉天糖)')` → `undefined` — matching ANY supplied title drops the album.
    - Script-mismatch case: `albumTag('过一招', '過一招')` → `undefined` when the raw title is passed alongside the display title (that is exactly what the call sites do).
    - `albumTag('', 'x')` / `albumTag(undefined, 'x')` → `undefined` (36-D-10 unchanged: an empty album was already omitted).
    - Downstream: `writeAudioTags` never calls `setAlbum` when the resolved album is `undefined` (existing L166 behaviour, re-pinned).
  </behavior>
  <action>
ROOT CAUSE (grepped, not guessed): the Kotlin MediaStore bridge only READS `MediaStore.Audio.Media.ALBUM`
(`android/app/src/main/java/com/openmusic/app/MediaStoreSaverPlugin.kt` L337/L384/L401) — it writes
no tags, so it is NOT the culprit. `writeAudioTags` (audio-tags.ts L166) already honours 36-D-10 and
omits an empty album. The bad value arrives as real data: CN/streaming catalogs set a single's
`album` equal to its own track name, and `Track.album` is copied through verbatim by
`kuwo.resolve` (`album: d.album || track.album`, kuwo.ts L154) and its siblings. We then write it.

The Simplified-album / Traditional-filename mismatch in the `Polar G` evidence confirms the path:
the filename uses the DISPLAY title (`dnTitle`, script-converted by the `names` store) while the
album rides the RAW catalog string. So a comparison must see BOTH titles.

THERE ARE EXACTLY TWO WRITERS (both already call the same `tagAudioBlob` seam):
  - `src/lib/services/download-track.ts` L248 — `album: r.album || undefined` (the 36-D-05 seam all
    four download callers funnel through).
  - `src/lib/services/retag.ts` L85 — `album: entry.album || undefined` (the background retag path).

FIX — one pure exported helper in `audio-tags.ts` (it already owns the tag-field contract), called
from both writers:

```
export function albumTag(album: string | null | undefined, ...titles: (string | null | undefined)[]): string | undefined
```

Normalisation: lowercase, strip whitespace and punctuation, then compare. `src/lib/services/match-key.ts`
already exports `matchKey(artist, title)` which does exactly this strip — reuse it with an empty
artist (`matchKey('', s)`) rather than writing a second normaliser. Do NOT reach for `zh-convert` —
it is async/heavy and the variadic `titles` already covers the script mismatch, because the call
sites hand in both the raw and the display title.

Call sites:
  - download-track.ts L248 → `album: albumTag(r.album, r.title, dnTitle),`
  - retag.ts L85 → `album: albumTag(entry.album, entry.title),` (pass the display title too if the
    entry carries one; check the `RetagEntry` shape at retag.ts L41 and pass whatever title fields exist).

Leave `albumArtist`, `trackNumber`, `lyrics` and the 36-D-05 / 36-D-10 / 36-D-11 / 36-D-12 comments
untouched. Add `quick-260919-0mw` comments on the helper (naming the two evidence files: `The Weeknd
- The Hills (Explicit).flac` and `Polar G - 過一招 (feat. 拉天糖).m4a`) and on both call sites.

Note for the SUMMARY, do not act on it: already-downloaded files keep their bad album tag. The
existing background retag path will correct them the next time it runs over a file; no migration
sweep is in scope here.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic &amp;&amp; pnpm vitest --run src/lib/services/audio-tags.test.ts src/lib/services/retag.test.ts src/lib/services/download-track.test.ts</automated>
  </verify>
  <done>`albumTag` exported from audio-tags.ts with tests for every behavior case; both writers call it (`grep -rn "albumTag(" src/lib/services/` shows the definition plus exactly 2 call sites); no writer passes a raw `album` into `tagAudioBlob` any more.</done>
</task>

<task type="auto">
  <name>Task 5 (A4): Put a download control in the NowPlaying transport row</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>
STALE PREMISE — READ THIS FIRST. The request says "replace the shuffle button with a download
button". There is NO shuffle button in `NowPlaying.svelte`: it was already removed by `ii6`
(comment at L69-73 — "Like replaces Shuffle in the transport row; Shuffle moved into the TrackMenu
kebab menu"). `grep -n -i shuffle src/lib/components/NowPlaying.svelte` returns only those comment
lines — no markup, no icon import. Re-confirm that grep before editing.

So the removal half of the request is ALREADY DONE and the constraints are already satisfied:
shuffle logic stays in `player.svelte.ts`, and shuffle stays reachable from the TrackMenu kebab.
Therefore: ADD the download control, remove NOTHING. Do not invent a substitute removal (do not
displace Repeat, Heart or anything else) — nothing the user asked to remove is present, and guessing
which control to sacrifice is not yours to decide.

The transport row is `<div class="transport" bind:this={transportEl}>` at L1502. Today it holds five
`<button class="t">` controls: Heart (L1503) → SkipBack → Play/Pause → SkipForward → Repeat (L1513).
Add the download control as a SIXTH, placed at the trailing end next to Repeat (mirroring Heart's
leading position, so the two non-transport affordances bracket the three real transport buttons).

REUSE, do not rebuild: `src/lib/components/DownloadControl.svelte` is the ONE shared per-song
download affordance and it already renders the downloaded-state indicator the request asks for
(idle Download icon / neutral spinner while downloading / greyed Check when `library.isDownloaded(uid)`
/ CircleAlert when unavailable), already delegates to the shared `downloadTrack` service, already
toasts its result and already honours the D-18 download-isolation contract. Do NOT re-implement any
of that inline and do NOT import `downloadTrack` into NowPlaying.

```
<DownloadControl track={player.current} />
```

Props:
  - `track={player.current}` — NowPlaying always has a resolved current track here; no `resolve`
    closure is needed (that prop exists for album-page stubs).
  - `persist` — leave at its `true` default (this is a real user-initiated download; it SHOULD land
    in the offline blob store).
  - `probe` — leave FALSE. NowPlaying is a single focused surface so a probe would be permissible in
    principle, but it costs a resolve + a HEAD on every render of the now-playing view and this task
    is not chartered to add a format/size readout. Say so in the comment so the next reader knows it
    was a decision, not an oversight.

STYLING: DownloadControl brings its own button styling, which will not match the transport row's
large ghost-icon `.t` buttons. Wrap it or add a scoped override so its footprint, icon size (20px,
matching its siblings) and tap target line up with the other five. Do not edit DownloadControl.svelte
itself — five other call sites (library rows, album rows) depend on its current look. Keep
`use:tapBounce` behaviour consistent with the row (DownloadControl already applies `tapBounce`
internally — do not double-apply).

NO NEW STRINGS: DownloadControl owns its own `t()` keys, so no dictionary edit is expected. If you
find yourself needing a NowPlaying-specific label, it must be added to ALL 17 files in
`src/lib/i18n/*.ts` with DOUBLE QUOTES for key AND value (`en` is the reference locale;
`i18n.test.ts` guards key-set parity) — but prefer reusing an existing key.

Tag the addition `quick-260919-0mw` with a one-line note that the shuffle button it was meant to
replace was already gone (ii6), so this is an addition rather than a swap.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic &amp;&amp; pnpm check &amp;&amp; pnpm vitest --run src/lib/i18n/i18n.test.ts</automated>
    <human-check>Open the now-playing view: the transport row shows a download icon; tapping it downloads and the icon settles into the greyed Check state; reopening the view on an already-downloaded song shows the Check state immediately.</human-check>
  </verify>
  <done>`<DownloadControl track={player.current} />` renders in the transport row sized to match its siblings; no shuffle markup was added or removed; `pnpm check` clean; i18n parity test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/ytmusic/search` | New tier-1 cover call widens the request volume on an own-origin edge proxy that POSTs to InnerTube |
| share link → recipient's browser | The emitted origin is now partly app-controlled rather than purely runtime-derived |
| catalog metadata → downloaded file tags | Untrusted upstream strings written into an on-device file's ID3/Vorbis frames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0mw-01 | Denial of Service | YTM tier-1 in `resolveTrackChain` | mitigate | Route through `searchAll` → `apiFetch`, inheriting the existing governor (GET dedupe, MAX_CONCURRENT_REQUESTS=8, 25s timeout, circuit breaker) plus the CAP=6 pool, 5-min negative-miss cache and skip-cached gate. Add NO new throttle and NO new fetch path. |
| T-0mw-02 | Tampering | `shareOrigin()` public-origin fallback | mitigate | The fallback origin is a build-time constant (`VITE_API_BASE`) or a hardcoded literal — never derived from user input, a query param or a path segment. Any real web origin passes through unchanged, so the rewrite can only ever narrow to one known-good host. |
| T-0mw-03 | Information Disclosure | `songShareUrl` on a non-public build | accept | A dev-server share link now points at production instead of `http://localhost:4321`. That is the intended fix, and the link carries no secret — only artist/title path segments and the advisory `ci` cover token already public in Phase 30. |
| T-0mw-04 | Tampering | `albumTag()` normalisation | accept | `matchKey` is a pure string normaliser with no regex backtracking risk; the worst outcome of a mis-compare is a dropped or retained album tag in a local file. No trust boundary is crossed. |
| T-0mw-05 | Information Disclosure | `<img src>` on a new `lh3.googleusercontent.com` cover host | mitigate | Client rendering keeps the existing `hasHttpsScheme` gate (`<img src>` only, never CSS `url()`). No change to `proxy/safe-image-url.ts`; the `/api/og` server-side host allowlist is untouched, so a YTM cover simply does not tokenize into the `ci` carrier and the card falls through to today's server chain. |
| T-0mw-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies — explicit constraint. No install task exists in this plan, so the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
```bash
cd /Users/laichan/code/tung/openmusic
pnpm check                      # svelte-check — the only quality gate in this repo
pnpm test                       # full vitest run, must stay green

# A1 — old HQ name fully retired, YTM tier present
grep -rn "resolveDeezerHQ" src/ | grep -v '^\s*//' ; echo "expect: no matches"
grep -c "ytmusic" src/lib/services/cover-backfill.ts

# A2 — exactly one origin read left in share.ts
grep -c "location.origin" src/lib/services/share.ts   # expect 1 (inside shareOrigin)

# A3 — both writers route through the helper, none writes a raw album
grep -rn "albumTag(" src/lib/services/   # expect: 1 definition + 2 call sites

# A4 — no shuffle markup added, DownloadControl wired
grep -n -i "shuffle" src/lib/components/NowPlaying.svelte   # expect: comment lines only
grep -n "DownloadControl" src/lib/components/NowPlaying.svelte
```
</verification>

<success_criteria>
- Q1 and Q2 answered in the SUMMARY with file:line citations; no source file changed by Task 1.
- Cover chain resolves YTM → iTunes → Deezer → CN, first SOLID https wins, both cache layers still written via the existing writers.
- `shareOrigin()` is the single origin authority in share.ts; web behaviour byte-identical, APK/dev links point at `openmusic.lol`.
- No downloaded file receives an album tag equal to its own title; a genuine album name still survives.
- NowPlaying transport row carries a working `DownloadControl` showing the downloaded state; shuffle untouched everywhere.
- `pnpm check` and `pnpm test` green. Five atomic commits. Nothing pushed to origin.
- No work started on backlog phases 999.2 / 999.3 / 999.4.
</success_criteria>

<output>
Create `.planning/quick/260919-0mw-answer-import-merge-offline-lyrics-quest/260919-0mw-SUMMARY.md` when done,
including the `## Findings (Q1 / Q2)` section from Task 1 and the A4 stale-premise note (shuffle was
already absent from NowPlaying).
</output>
