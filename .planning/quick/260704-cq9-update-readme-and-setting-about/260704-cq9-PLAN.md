---
phase: quick-260704-cq9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/routes/(app)/settings/about/+page.svelte
autonomous: true
requirements: [DOCS-REFRESH]

must_haves:
  truths:
    - "README describes all 7 sources (netease, qq, kuwo, joox, fivesing, jamendo, audius) with an accurate CN + Western/global framing"
    - "About page features array lists all 7 sources with the same accurate framing"
    - "Both docs attribute discovery consistently: Last.fm genre/region shelves + Deezer top-hits chart; covers via Deezer → iTunes → CN"
    - "README test-count figures reflect the real number reported by `pnpm test`"
    - "Both docs still state 15 UI languages (unchanged)"
    - "`pnpm check` passes after the .svelte edit"
  artifacts:
    - path: "README.md"
      provides: "Refreshed 7-source framing, consistent discovery attribution, correct test count"
      contains: "fivesing"
    - path: "src/routes/(app)/settings/about/+page.svelte"
      provides: "Refreshed features array covering 7 sources"
      contains: "features"
  key_links:
    - from: "README.md"
      to: "src/lib/sources/registry.ts"
      via: "source list matches the 7 registered adapters"
      pattern: "netease.*qq.*kuwo.*joox|fivesing|jamendo|audius"
    - from: "src/routes/(app)/settings/about/+page.svelte"
      to: "src/lib/sources/registry.ts"
      via: "features[0] source list matches the 7 registered adapters"
      pattern: "features"
---

<objective>
Bring BOTH the root `README.md` and the in-app Settings → About page up to date with the current app. The docs drifted: the app grew from 4 to 7 registered music sources (fivesing, jamendo, audius added), and the discovery/cover attribution is inconsistent between the two docs. This is a COPY/CONTENT-only update — no source-logic, dependency, or refactor changes.

Purpose: Docs (public README + in-app About) should accurately describe the shipped app.
Output: Refreshed `README.md` and `about/+page.svelte`, with the README test-count figures reconciled to the real `pnpm test` number.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Source of truth for the 7 sources
@src/lib/sources/registry.ts

# The two files being edited
@README.md
@src/routes/(app)/settings/about/+page.svelte

<verified_ground_truth>
The planner verified these against the LIVE codebase. Anchor on them; do NOT re-derive from memory or CLAUDE.md (CLAUDE.md is itself partly stale).

1. SOURCES — `src/lib/sources/registry.ts` line 14 registers exactly 7 adapters:
   `netease, qq, kuwo, joox, fivesing, jamendo, audius` (was 4).
   - fivesing = 5sing, a Kugou UGC sub-platform: amateur covers (翻唱), instrumental/karaoke backing tracks (伴奏), original UGC. Chinese.
   - jamendo = Western Creative-Commons indie catalog (net-new, zero overlap with CN sources).
   - audius = decentralized Western/indie/electronic + UGC catalog.
   - FRAMING: the old "aggregated from Chinese music platforms" line is now too narrow — jamendo/audius are Western/global. Broaden to: 4 mainstream CN sources (Netease, QQ, Kuwo, JOOX) PLUS extra sources spanning Chinese UGC (5sing) and Western/global catalogs (Jamendo CC indie, Audius).

   ⚠ ENABLED-BY-DEFAULT CORRECTION (planner override of the orchestrator brief):
   The orchestrator brief said the 3 new sources ship `enabledByDefault: false` (opt-in).
   THIS IS WRONG per the live code. All 7 adapters — including fivesing, jamendo, audius —
   ship `enabledByDefault: true` in the working tree (verified: comments stripped, grep on
   each adapter file). The stale prose comments in `fivesing.ts` / `jamendo.ts` and the
   `registry.test.ts` comment still SAY "false / opt-in", but the actual boolean value is `true`.
   → Docs MUST describe the sources per the LIVE boolean: all 7 are searched by default,
     with a per-source toggle available in Settings → Playback (Advanced → Sources). Do NOT
     write "opt-in only" / "disabled by default" for the 3 new sources. If you want a hedge,
     "all searched by default; each source is individually toggleable in Settings → Playback"
     is accurate and future-proof.
   → Do NOT touch the adapter files or the stale code comments — this is a copy-only task.
     The comment↔value drift is flagged for the user in the SUMMARY, not fixed here.

2. i18n dictionaries (`src/lib/i18n/*.ts`, excluding index/detect/tests) = 15 languages:
   en, zh-Hant, zh-Hans, es, fr, de, pt, it, ru, tr, ar, hi, id, vi, th.
   The existing "15-language" claim in BOTH docs is CORRECT — keep it 15, do NOT change to 16.

3. TEST COUNT — README currently claims "414 tests" / "414 Vitest tests" in TWO places:
   - Getting-started code comment: `pnpm test         # run the Vitest suite (414 tests)`
   - Architecture services line: "Covered by **414 Vitest tests**."
   There are 69 test FILES; the individual test count is UNKNOWN and likely changed. Task 3
   MUST run `pnpm test`, read the real total from the Vitest summary, and replace BOTH 414s
   with the true number. Verify, do NOT guess.

4. DISCOVERY ATTRIBUTION — today the two docs disagree:
   - README says "Last.fm-powered discovery".
   - About page says "Deezer-powered home discovery".
   Reality (verified in the service layer): genre/region shelves come from Last.fm
   (`/api/lastfm/discovery`); the top-hits / top-artists chart comes from the Deezer chart
   (`/api/deezer/chart`); covers come from Deezer → iTunes → CN.
   → Make BOTH docs consistent and accurate: discovery = Last.fm genre/region shelves +
     Deezer top-hits chart; covers = Deezer → iTunes → CN. Remove the single-source
     "Deezer-powered" / "Last.fm-powered" framing that implies one source does everything.
</verified_ground_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README.md source framing + discovery attribution</name>
  <files>README.md</files>
  <action>
Edit README.md prose (Markdown, normal quotes) to reflect the 7 sources and consistent discovery attribution. Do NOT touch the test-count "414" figures in this task — Task 3 owns those.

Specific edits:
1. Intro paragraph (line 7): the current "aggregated from **NetEase, QQ, Kuwo and JOOX**" + "**Last.fm-powered discovery**" is too narrow and single-source. Broaden the source framing to cover the 4 mainstream CN sources PLUS the new sources (5sing UGC, Jamendo CC indie, Audius) — per ground-truth #1, all searched by default with a per-source toggle in Settings. Fix the discovery framing per ground-truth #4 (Last.fm genre/region shelves + Deezer top-hits chart; keep "Deezer cover art" — that is still accurate). Keep "15-language UI" unchanged.
2. Architecture "Source-adapter registry" bullet (line 25): if it names or implies only the CN four, update to reflect 7 registered adapters spanning CN mainstream + CN UGC + Western/global. Keep the "adding a source = new client file + proxy file + one import; aggregation names no source" explanation.
3. Features section:
   - "Search across all sources" (line 34) — reflect the broadened source set (CN + Western/global) if the surrounding copy narrows it to CN.
   - "**Last.fm-powered home discovery**" (line 35) — reword to the accurate split: Last.fm genre/region shelves + Deezer top-hits/top-artists chart. Keep the "tap-to-play re-resolves the best playable match" detail (still accurate; note matches resolve across the enabled sources, not only CN).
   - "**Real cover art**" (line 37) — keep the Deezer → iTunes → CN chain (already correct).
4. Scope & honesty notes (line 147): if the "unofficial third-party proxy APIs" framing implies CN-only, note the mix now includes Western/global catalogs (Jamendo is legit CC; Audius is decentralized) — keep the demo/educational honesty.
5. Project-layout `sources/` comment (line 125-ish) — keep accurate; no per-source list needed there.

Keep it copy-only: no code, no dependency, no structural rewrite. Match existing README tone/heading style. Use the accurate "all searched by default; each source individually toggleable in Settings → Playback" framing for the new sources (NOT "opt-in only").
  </action>
  <verify>
    <automated>grep -Eq 'fivesing|5sing' README.md &amp;&amp; grep -Eq 'Jamendo|jamendo' README.md &amp;&amp; grep -Eq 'Audius|audius' README.md &amp;&amp; grep -q 'Deezer' README.md &amp;&amp; grep -q 'Last.fm' README.md &amp;&amp; grep -q '15-language' README.md &amp;&amp; echo README_OK</automated>
  </verify>
  <done>README intro + Architecture + Features describe all 7 sources with the CN + Western/global framing; discovery is attributed as Last.fm shelves + Deezer top-hits chart; covers Deezer → iTunes → CN; "15-language" retained; no code/dependency changes. Test-count figures untouched (owned by Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the Settings → About page features array</name>
  <files>src/routes/(app)/settings/about/+page.svelte</files>
  <action>
Edit ONLY the `features: string[]` array (lines 15-23) inside the &lt;script&gt; block. These are literal English display strings (NOT translated i18n chrome — preserve the existing explanatory comment on line 14 verbatim). Do NOT add i18n keys, do NOT touch `settings.aboutLine`, the CONTACT/REPO consts, the markup, or the &lt;style&gt; block. Use tabs for indentation (house style) and single quotes for the string literals (matches the existing array).

Specific edits to the features array:
1. features[0] "Search + stream across Netease, QQ, Kuwo & JOOX" → reflect all 7 sources with the CN + Western/global framing, e.g. name the 4 mainstream CN sources and note the extra sources (5sing UGC, Jamendo CC indie, Audius) that are also searched by default. Keep it a short one-line bullet — this is a compact features list, so a concise phrasing like "Search + stream across Netease, QQ, Kuwo & JOOX, plus 5sing, Jamendo & Audius" is appropriate. Frame per ground-truth #1 (searched by default, individually toggleable in Settings → Playback) — NOT "opt-in only".
2. features[1] "Deezer-powered home discovery — ..." → fix the single-source attribution per ground-truth #4: discovery = Last.fm genre/region shelves + Deezer top-hits/top-artists chart. Keep the "pick, reorder & hide" detail.
3. features[2] "Tap-to-play that re-resolves the best match from the CN sources" → broaden "CN sources" to "the enabled sources" (matches now resolve across all 7, not only CN).
4. features[3] "Real album & artist art via Deezer, with iTunes fallback" → keep; optionally align to the full Deezer → iTunes → CN chain for consistency with the README (accurate either way).
5. features[4] keeps "15 UI languages" (correct — do NOT change to 16).
6. features[5], features[6] — leave unless they contain a source/discovery inaccuracy (they do not).

Keep the array a valid TS string[] (trailing commas as in the existing array — the last element has no trailing comma; match that).
  </action>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>features array names all 7 sources with the CN + Western/global framing; discovery attributed as Last.fm shelves + Deezer top-hits chart; "the enabled sources" replaces "CN sources"; "15 UI languages" retained; the literal-strings comment on line 14 preserved; only the features array changed; `pnpm check` (svelte-check) passes 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the README test-count figure to the real number</name>
  <files>README.md</files>
  <action>
Run the full Vitest suite and read the real total-tests number from the summary, then replace the stale "414" figure in BOTH README places with the true number.

Steps:
1. Run `pnpm test` (this is `vitest --run`). Read the Vitest summary line, e.g. `Tests  N passed (N)` — capture N (the total number of individual tests, NOT the 69 test files).
2. Replace BOTH occurrences of 414 in README.md:
   - Getting-started code comment (line 56): `pnpm test         # run the Vitest suite (414 tests)` → `(N tests)`.
   - Architecture services line (line 26): "Covered by **414 Vitest tests**." → "Covered by **N Vitest tests**."
3. Do NOT change any other number (keep "15", keep "69"-derived phrasing if any, keep "~22 genres / ~20 regions").

Use the actual N from the run — verify, do not guess. If the suite has any failing tests, report the failure in the SUMMARY and use the total-tests figure the runner prints (passed + any failing) rather than skipping the update; do NOT attempt to fix test failures (out of scope — copy-only task).
  </action>
  <verify>
    <automated>! grep -q '414' README.md &amp;&amp; grep -Eq 'run the Vitest suite \([0-9]+ tests\)' README.md &amp;&amp; grep -Eq 'Covered by \*\*[0-9]+ Vitest tests\*\*' README.md &amp;&amp; echo TESTCOUNT_OK</automated>
  </verify>
  <done>`pnpm test` was run; both README "414" figures are replaced with the real total-tests number the runner reported; no stale "414" remains; no other numbers changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation/copy-only change. No new trust boundary is introduced — no code paths, inputs, secrets, dependencies, or network surface are touched. README.md is static Markdown; the About-page edit is confined to a literal `features: string[]` display array (no user input, no dynamic evaluation, no i18n key surface).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cq9-01 | Information Disclosure | README source-framing / About page copy | accept | Public docs describe already-public behavior; no secret, endpoint, or token is added. `JOOX_TOKEN`/`LASTFM_*` remain edge-only and are not referenced by value. |
| T-cq9-02 | Tampering | `about/+page.svelte` edit scope creep | mitigate | Edit is scoped to the literal `features` array only; `pnpm check` (svelte-check, strict) gates the .svelte change so a malformed edit fails the build. No new dependency install (no package-manager step in this plan). |
</threat_model>

<verification>
Phase-level checks after all three tasks:
1. `pnpm check` — svelte-check passes 0 errors (guards the About-page edit).
2. `pnpm test` — full Vitest suite run (Task 3 uses its total for the README figure).
3. `grep -n '414' README.md` returns nothing (stale figure gone).
4. README + About both name fivesing/5sing, jamendo/Jamendo, audius/Audius.
5. Both docs attribute discovery as Last.fm shelves + Deezer top-hits chart; neither implies a single source powers everything.
6. "15" language count preserved in both docs; no "16".
7. `git diff --stat` shows ONLY README.md and about/+page.svelte changed (no adapter/i18n/other files).
</verification>

<success_criteria>
- README.md and about/+page.svelte both accurately describe all 7 registered sources with a CN mainstream + Western/global framing, per the live `enabledByDefault: true` reality (all searched by default; individually toggleable in Settings → Playback).
- Discovery attribution is consistent and accurate across both docs (Last.fm genre/region shelves + Deezer top-hits chart; covers Deezer → iTunes → CN).
- README test-count reflects the real `pnpm test` total in both places; no "414" remains.
- 15-language claim unchanged.
- `pnpm check` passes; only the two target files changed; no code/dependency/i18n changes.
- SUMMARY flags the comment↔value drift discovered in fivesing.ts / jamendo.ts / registry.test.ts (they say `enabledByDefault: false` in prose comments but the actual value is `true`) so the user can decide whether to fix the code comments in a follow-up.
</success_criteria>

<output>
Create `.planning/quick/260704-cq9-update-readme-and-setting-about/260704-cq9-SUMMARY.md` when done.
In the SUMMARY, explicitly flag: the orchestrator brief assumed the 3 new sources were `enabledByDefault: false`, but the live code has all 7 at `enabledByDefault: true`; the docs were written to the live value; the stale "false/opt-in" prose comments in `fivesing.ts`, `jamendo.ts`, and `registry.test.ts` were left untouched (copy-only task) and are candidates for a follow-up comment fix.
</output>
