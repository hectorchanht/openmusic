---
quick_id: 260614-2m3
slug: finish-openmusic-pages-dev-to-openmusic-
status: complete
created: 2026-06-14
---

# Quick 260614-2m3 — SUMMARY

## What shipped
- **`src/lib/proxy/http.ts`** — `ALLOWED_ORIGIN_PATTERNS` now allows `openmusic.lol` +
  `*.openmusic.lol` (primary custom domain + CF preview), **keeping** the legacy
  `openmusic.pages.dev` + `*.openmusic.pages.dev` patterns for a safe cutover. localhost/
  127.0.0.1/capacitor patterns and the never-`*` open-relay guard (T-01-02) unchanged. CORS
  stays scoped to the explicit own-origin allowlist — never widened to `*`.
- **Docs** — README.md (Live + deploy-target URLs) and docs/DEPLOY.md (deploy/smoke URLs)
  repointed to openmusic.lol.
- Already-in-tree `.lol` edits committed alongside: api-base.ts, robots.txt, sitemap.xml, and
  the endpoint/proxy test files (the migration contract). (SITE/canonical in +layout.svelte,
  PageOg.svelte, app.html landed earlier with the SEO-title fix.)

## Decision
Keep-both (lol + pages.dev) rather than replace: no test asserts pages.dev is rejected, and the
CF Pages project URL still resolves during cutover. Drop the legacy patterns later once the
Pages URL is fully retired.

## Verification (all green)
- `pnpm test` → 62 files / **840 passed** (the 8 CORS/origin tests that were red now pass).
- `pnpm check` → 0 errors / 0 warnings.
- `pnpm build` (adapter-cloudflare) exit 0; `pnpm build:native` (adapter-static) exit 0.
- `grep openmusic.pages.dev` across src/static/config/docs → none remaining (own domain).

## Out of scope (left untouched)
- `src/lib/components/SleepTimerSheet.svelte` — unrelated local working-tree edit, still uncommitted.
