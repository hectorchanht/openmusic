---
quick_id: 260614-2m3
slug: finish-openmusic-pages-dev-to-openmusic-
status: complete
created: 2026-06-14
---

# Quick 260614-2m3 — finish openmusic.pages.dev → openmusic.lol migration

## Objective
Complete the in-flight domain migration so the suite is green and all own-origin/SEO
references point at the new primary domain `openmusic.lol`.

## Tasks
1. **CORS allowlist (security boundary).** `src/lib/proxy/http.ts` `ALLOWED_ORIGIN_PATTERNS`:
   add `^https://openmusic\.lol$` + `^https://[a-z0-9-]+\.openmusic\.lol$`. KEEP the legacy
   `openmusic.pages.dev` patterns during cutover (CF Pages preview URLs still resolve there;
   no test asserts pages.dev is rejected). Preserve localhost/127.0.0.1/capacitor patterns and
   the never-`*` open-relay guard (T-01-02). The test files (http.test.ts + deezer/lastfm/
   similar/proxy endpoint tests) are the contract — they assert `openmusic.lol` + preview
   subdomain allowed, disallowed → undefined.
2. **Reconcile own-domain literals.** Sweep repo; the only remaining own-domain refs were docs
   (README.md ×2, docs/DEPLOY.md ×3) → updated to openmusic.lol. (SITE/canonical/api-base/
   robots/sitemap were already on .lol in the working tree.)
3. **Gates:** `pnpm test` green, `pnpm check` 0/0, `pnpm build` + `pnpm build:native` exit 0.

## Out of scope
- `src/lib/components/SleepTimerSheet.svelte` (unrelated local edit) — left uncommitted.
