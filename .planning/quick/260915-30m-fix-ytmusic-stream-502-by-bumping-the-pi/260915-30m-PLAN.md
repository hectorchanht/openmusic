---
phase: quick-260915-30m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/api/ytmusic/stream/[videoId]/+server.ts
  - src/routes/api/ytmusic/stream/[videoId]/stream.test.ts
autonomous: true
requirements: [quick-260915-30m]

must_haves:
  truths:
    - "The ANDROID_VR player POST sent by GET /api/ytmusic/stream/:videoId carries clientVersion 1.65.10 in BOTH the JSON body and the user-agent header (the version YouTube currently accepts; 1.60.19 is gated with LOGIN_REQUIRED and produced the 502 that killed every ytmusic track)"
    - "The two pins can no longer drift apart: one constant feeds both the UA string and playerBody()"
    - "getVisitorData / refresh-once-then-502 / isPlayable / selectAudioFormat / Range passthrough / open-relay + governor security posture are byte-for-byte unchanged"
    - "A comment at the pin records that it is a ROTTING VERSION PIN, names the symptom (hard 502 from this route → whole ytmusic source skipped) and the fix (bump the version)"
    - "pnpm check and pnpm test pass with no live network"
  artifacts:
    - path: "src/routes/api/ytmusic/stream/[videoId]/+server.ts"
      provides: "single ANDROID_VR_VERSION constant = '1.65.10' feeding ANDROID_VR_UA + playerBody().clientVersion, plus the rotting-pin comment"
      contains: "ANDROID_VR_VERSION"
    - path: "src/routes/api/ytmusic/stream/[videoId]/stream.test.ts"
      provides: "regression test asserting UA version === body clientVersion and visitorData present on the player POST (fetch-stubbed, no network)"
      contains: "clientVersion"
  key_links:
    - from: "src/routes/api/ytmusic/stream/[videoId]/+server.ts"
      to: "ANDROID_VR_UA"
      via: "template literal embedding ANDROID_VR_VERSION"
      pattern: "oculus/\\$\\{ANDROID_VR_VERSION\\}"
    - from: "playerBody()"
      to: "ANDROID_VR_VERSION"
      via: "clientVersion: ANDROID_VR_VERSION"
      pattern: "clientVersion: ANDROID_VR_VERSION"
---

<objective>
Restore YouTube Music playback by bumping the pinned ANDROID_VR InnerTube clientVersion from 1.60.19 (now gated by YouTube with `LOGIN_REQUIRED` / "Sign in to confirm you're not a bot", zero adaptiveFormats) to 1.65.10 (verified `playabilityStatus.status === 'OK'`, 22 adaptiveFormats, itag-140 direct url serving 206 `audio/mp4`).

Purpose: every ytmusic track currently hard-502s at `/api/ytmusic/stream/:videoId`, trips the client's cross-source fallback and is skipped. Root cause is already diagnosed and isolation-tested (see planning context): `clientVersion` is the ONLY field that matters; visitorData stays mandatory; UA/osName/osVersion are irrelevant.
Output: the version pin bumped and collapsed to one constant, a rotting-pin decision comment, one fetch-stubbed regression test.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/routes/api/ytmusic/stream/[videoId]/+server.ts
@src/routes/api/ytmusic/stream/[videoId]/stream.test.ts

<interfaces>
<!-- Already in context from planning. Use directly — no exploration needed. -->

From src/routes/api/ytmusic/stream/[videoId]/+server.ts (current state, the two rotting pins):
```typescript
// line 4 header comment: "(clientVersion 1.60.19 + a cached anonymous visitorData token)"
const ANDROID_VR_UA =
	'com.google.android.apps.youtube.vr.oculus/1.60.19 (Linux; U; Android 12; Quest 3) gzip';   // pin #1
function playerBody(videoId: string, visitorData: string | null) {
	const client: Record<string, unknown> = {
		clientName: 'ANDROID_VR',
		clientVersion: '1.60.19',                                                                   // pin #2
		androidSdkVersion: 32, deviceModel: 'Quest 3', hl: 'en', gl: 'US'
	};
	if (visitorData) client.visitorData = visitorData;
	return { context: { client }, videoId, contentCheckOk: true, racyCheckOk: true };
}
async function callPlayer(videoId, visitorData) {
	return await innerTubePost(PLAYER_URL, playerBody(videoId, visitorData), {
		headers: { 'user-agent': ANDROID_VR_UA }, signal: AbortSignal.timeout(PLAYER_TIMEOUT_MS) });
}
```
`+server.ts` may only export HTTP verbs or `_`-prefixed names (quick-270715) — do NOT `export` the new constant.

From stream.test.ts: `stubFetch(players)` stubs global `fetch` with `vi.fn(async (u, init) => …)` routed by URL (`SEARCH_URL` → visitorData envelope, `PLAYER_URL` → next scripted player body, googlevideo → 206/200). Returns `{ spy, urls, playerCalls, mediaUrl, mediaRangeHeader }`. `spy.mock.calls` therefore already contains the `[url, init]` of the PLAYER_URL POST — `init.body` is the JSON string, `init.headers` carries `user-agent`. `fixture.ok` is an OK player response. Existing GET tests call `streamGet({ params: { videoId }, request: new Request(...) } as any)` — copy that shape from the `'a non-ranged GET returns 200 …'` test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump ANDROID_VR clientVersion to 1.65.10 via a single constant + rotting-pin comment</name>
  <files>src/routes/api/ytmusic/stream/[videoId]/+server.ts</files>
  <action>
In `+server.ts`, replace the two duplicated `'1.60.19'` literals with ONE module constant so the pins cannot drift apart again (the duplicated-literal shape is what made this bug a two-place edit):

1. Directly above `ANDROID_VR_UA`, add `const ANDROID_VR_VERSION = '1.65.10';` with a comment block in the file's existing high-density decision-record style, tagged `quick-260915-30m`, saying: ROTTING VERSION PIN. YouTube gates stale ANDROID_VR clientVersions with `playabilityStatus.status === 'LOGIN_REQUIRED'` ("Sign in to confirm you're not a bot") and zero adaptiveFormats, so `isPlayable` fails on BOTH the initial call and the visitorData-refresh retry and this route hard-502s — which trips the client's cross-source fallback and silently skips EVERY ytmusic track (diag symptom: `resolve.ok hasUrl:true → src.set → audio.error hasPlayed:false → advance`, never `playing`). The fix is to bump this version to a current ANDROID_VR release; 1.60.19 died 2026-09, 1.65.10 verified OK. Isolation-tested: version alone flips LOGIN_REQUIRED→OK; UA/osName/osVersion are irrelevant; visitorData remains mandatory (no visitorData → LOGIN_REQUIRED even on 1.65.10).
2. Make `ANDROID_VR_UA` a template literal embedding the constant: `` `com.google.android.apps.youtube.vr.oculus/${ANDROID_VR_VERSION} (Linux; U; Android 12; Quest 3) gzip` `` — the rest of the UA is unchanged.
3. In `playerBody()`, set `clientVersion: ANDROID_VR_VERSION`.
4. In the header comment (line 4), replace the literal `clientVersion 1.60.19` with `clientVersion ANDROID_VR_VERSION` so the header cannot rot independently.

Do NOT `export` the constant (SvelteKit `+server.ts` rejects non-verb exports at request time — quick-270715). Do NOT touch `getVisitorData`, `callPlayer`'s shape, the refresh-once-then-502 retry, `isPlayable`, `selectAudioFormat`, Range passthrough, `fetchWithRetry` usage, or any T-27-03-* security comment. Do not remove any existing decision-ref comment. Tabs, single quotes.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && ! grep -rn "1\.60\.19" src/ && grep -c "ANDROID_VR_VERSION" "src/routes/api/ytmusic/stream/[videoId]/+server.ts" | awk '$1>=4' && pnpm check</automated>
  </verify>
  <done>No `1.60.19` remains anywhere under `src/`; `ANDROID_VR_VERSION = '1.65.10'` is referenced by the UA template, by `playerBody()`, and by the header comment; the rotting-pin comment is present; `pnpm check` passes; the request path is otherwise unchanged (git diff shows only the constant, the two pin sites, and comments).</done>
</task>

<task type="auto">
  <name>Task 2: Regression test — UA version and body clientVersion agree, visitorData sent</name>
  <files>src/routes/api/ytmusic/stream/[videoId]/stream.test.ts</files>
  <action>
Add ONE `it` inside the existing `describe('GET /api/ytmusic/stream/:videoId — …')` block, using the existing `stubFetch([OK])` harness (no network, no new fixtures). Call `streamGet` with a plain non-ranged `Request` exactly like the existing `'a non-ranged GET returns 200 …'` test. Then find the PLAYER_URL call in `spy.mock.calls` (`calls.find(([u]) => String(u) === PLAYER_URL)`), `JSON.parse(init.body as string)`, and assert:
- `body.context.client.clientName === 'ANDROID_VR'`
- `body.context.client.clientVersion` is a non-empty string AND equals the version parsed out of the sent `user-agent` header (`new Headers(init.headers).get('user-agent')` matched with `/oculus\/(\S+) /`). This is the wire-level guard against the two pins drifting apart — it deliberately pins NO literal version so it does not rot at the next bump.
- `body.context.client.visitorData` is a non-empty string (visitorData is mandatory — isolation testing showed 1.65.10 without it is still LOGIN_REQUIRED).

Name it along the lines of `'the player POST sends one consistent ANDROID_VR clientVersion in body + UA, with visitorData (rotting pin guard, quick-260915-30m)'`. Add a one-line comment above it pointing at the `ANDROID_VR_VERSION` comment in `+server.ts` for the why. Do not add a test with a live network call; do not modify the fixture JSON.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- "src/routes/api/ytmusic/stream" && pnpm test</automated>
  </verify>
  <done>New test passes alongside all existing stream.test.ts tests; full `pnpm test` is green; the test reads the player POST from the fetch stub and contains no hardcoded version literal and no real network call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge Worker → InnerTube `player` | outbound POST with a fixed body; only `videoId` is client-influenced |
| edge Worker → googlevideo | fetches ONLY the adaptiveFormats url from the player response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q30m-01 | Tampering | `playerBody()` clientVersion | accept | constant string, not client-influenced; existing T-27-03-01 open-relay control (never fetch a client URL) untouched |
| T-q30m-02 | DoS | refresh-once-then-502 | accept | retry ceiling unchanged; a future version rot degrades to the same bounded 502, never a hang |
| T-q30m-03 | Info disclosure | visitorData | accept | still edge-side only (T-27-03-02); the test only inspects a stubbed fetch, never a real token |
| T-q30m-SC | Tampering | npm installs | accept | no package installs in this plan |
</threat_model>

<verification>
- `! grep -rn "1\.60\.19" src/` → no stale pin anywhere.
- `pnpm check` green.
- `pnpm test` green (vitest, node-only, fetch stubbed).
- `git diff --stat` touches exactly the two files listed in `files_modified`.
</verification>

<success_criteria>
- `/api/ytmusic/stream/:videoId` POSTs ANDROID_VR clientVersion 1.65.10 in body and UA from one constant.
- Rotting-pin comment present; no existing decision-ref comment removed.
- Regression test guards body/UA version agreement + visitorData presence without a literal version or network.
- `pnpm check` and `pnpm test` pass.
</success_criteria>

<output>
Create `.planning/quick/260915-30m-fix-ytmusic-stream-502-by-bumping-the-pi/260915-30m-SUMMARY.md` when done.
</output>
