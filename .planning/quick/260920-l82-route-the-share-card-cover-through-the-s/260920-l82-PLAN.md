---
phase: quick-260920-l82
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/cover-backfill.ts
  - src/lib/services/cover-backfill.test.ts
  - src/lib/components/TrackMenu.svelte
autonomous: true
requirements: [QUICK-260920-L82]
must_haves:
  truths:
    - "TrackMenu no longer imports itunesSongCover; the share-card prewarm calls resolveShareCover from cover-backfill (the module every other cover consumer already routes through) — zero direct cover-tier calls remain outside cover-backfill.ts"
    - "resolveShareCover runs iTunes → (on miss) Deezer via the shared tier() wrapper, never YTM and never the CN searchAll, and returns the first https URL or null"
    - "A resolveShareCover hit writes NEITHER the uid layer NOR the name layer of the cover cache — the art the app DISPLAYS (rows, hero, media card) is byte-identical before and after a menu open"
    - "A song iTunes does not carry but Deezer does now yields a d:<32hex> carrier on its share link instead of falling to the branded OpenMusic card (closes the residual gap kn4 shipped with)"
    - "A share for a song whose displayed cover already tokenizes (Deezer/Last.fm/kuwo/iTunes-with-id) issues no network request and produces a byte-identical link"
    - "resolveTrackChain / resolveCoverForTrack / resolveHqCover / backfillCovers are unchanged; their existing tests pass untouched"
    - "doShare still calls navigator.share() with no new await before it (iOS user-gesture rule)"
  artifacts:
    - path: "src/lib/services/cover-backfill.ts"
      provides: "resolveShareCover(track, signal) + session memo + __resetShareCoverMemo"
      exports: ["resolveShareCover", "__resetShareCoverMemo"]
    - path: "src/lib/services/cover-backfill.test.ts"
      provides: "describe('resolveShareCover …') — iTunes hit / Deezer fallback / both miss / no cache write / memo / abort / no YTM-CN"
      contains: "resolveShareCover"
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "prewarm effect calling resolveShareCover; no <script module> memo; no itunesSongCover import"
  key_links:
    - from: "src/lib/components/TrackMenu.svelte"
      to: "src/lib/services/cover-backfill.ts"
      via: "import { collectCoverCandidates, resolveShareCover, … } from '$lib/services/cover-backfill'"
      pattern: "resolveShareCover\\("
    - from: "src/lib/services/cover-backfill.ts resolveShareCover"
      to: "itunesSongCover / deezerSongCover"
      via: "tier(() => itunesSongCover(…)) then tier(() => deezerSongCover(…))"
      pattern: "tier\\(\\(\\) => itunesSongCover"
---

<objective>
Route the share-card cover prewarm through the shared cover chain module instead of TrackMenu calling
`itunesSongCover` directly. Add `resolveShareCover` to `cover-backfill.ts` as the THIRD sibling of
`resolveCoverForTrack` / `resolveHqCover` — same `tier()` never-throw wrapper, same https guard, a
different tier subset (iTunes → Deezer) for a different purpose (a cover from a host `coverToken` can
tokenize), and ONE deliberate divergence: it never writes the cover cache.

Purpose: the user asked that "the cover resolve chain should be reused and shared across all the covers
usage." The orchestrator's audit found exactly one bypass — the kn4 prewarm effect in TrackMenu. Fixing
it inside cover-backfill also closes a residual gap kn4 shipped with: a song iTunes lacks now falls to
Deezer and yields a `d:<32hex>` token instead of the branded fallback card.

Output: `resolveShareCover` + memo in cover-backfill.ts, its unit tests, TrackMenu rewired.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/lib/services/cover-backfill.ts
@src/lib/services/cover-backfill.test.ts
@src/lib/components/TrackMenu.svelte
@src/lib/services/share.ts

<interfaces>
<!-- Verified against the tree on 2026-09-20. Use directly — no exploration needed. -->

From src/lib/services/cover-backfill.ts (all present; do NOT modify these):
```typescript
// L79-80 imports already present
import { deezerSongCover, deezerArtistCover, deezerSearchTopN } from '$lib/services/deezer';
import { itunesSongCover, itunesArtistCover } from '$lib/services/itunes-cover';
// L117-135 negative-miss cache (the neighbour the new memo sits beside)
const missAt = new Map<string, number>();
export function __resetCoverMissCache(): void;
// L144 shared never-throw wrapper — returns url only if hasHttpsScheme(url), else null; catches throws
async function tier(fn: () => Promise<string | null>): Promise<string | null>;
// L278 the sibling to mirror (shape only — it WRITES cache; the new one must NOT)
export async function resolveHqCover(track: Track, signal?: AbortSignal): Promise<string | null>;
```

From src/lib/services/itunes-cover.ts:
```typescript
export async function itunesSongCover(artist: string, title: string, signal?: AbortSignal): Promise<string | null>;
// side effect on a hit: rememberItunesId → setCachedItunesId(artworkKey, id) — the DISJOINT `itunes:`
// key family of the cover cache, NOT the uid/name layers. This is what lets recallItunesId → `i:<id>`.
export function recallItunesId(coverUrl: string | null | undefined): string | null;
```

From src/lib/services/deezer.ts:
```typescript
// returns result.cover = album.cover_xl ?? cover_big ?? cover_medium → always /images/cover/<32hex>/…
// (artist pictures live on the separate artistPicture field → deezerArtistCover only). Never throws.
export async function deezerSongCover(artist: string, title: string, signal?: AbortSignal): Promise<string | null>;
```

From src/lib/services/abort-signal.ts:
```typescript
export function combinedSignal(timeoutMs: number, caller?: AbortSignal): AbortSignal;
```

From src/lib/services/share.ts L331 — closed grammar; `d:` requires /^\/images\/cover\/([0-9a-f]{32})\/[^/]+$/:
```typescript
export function coverToken(url: string | null | undefined, itunesId?: string | null): string | null;
```

TrackMenu.svelte current state (line numbers verified):
- L1-14  `<script module>` block holding `const shareItunesMemo = new Map<string, string>()` + kn4 comment
- L80    `import { collectCoverCandidates, type CoverCandidate } from '$lib/services/cover-backfill';`
- L86    `import { combinedSignal } from '$lib/services/abort-signal';`
- L87    `import { recallItunesId, itunesSongCover } from '$lib/services/itunes-cover';`  ← the bypass
- L187-252 kn4 prewarm effect; L236 `untrack(() => itunesSongCover(target.artist, target.title, combinedSignal(8_000, ac.signal)))`
- L247-250 🔴 no-cache-write comment
- L808-818 doShare reads `shareCoverFallback ?? activeCover` BEFORE onclose(); L875 songShareUrl(…, shareCover, recallItunesId(shareCover), track); L895 nav.share

Test harness (cover-backfill.test.ts L1-112): MemStorage localStorage stub, `mk(source, songid, extra)`
Track factory, `mockSearch({ytm, cn})`, `ytmCalls()` / `cnCalls()` counters, `beforeEach` defaults
searchAll to a miss + calls `__resetCoverMissCache()`; `vi.spyOn(itunes, 'itunesSongCover')`,
`vi.spyOn(deezer, 'deezerSongCover')`; cache assertions via `getCachedCover` / `getCachedCoverByUid`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resolveShareCover (iTunes → Deezer, no cache write, uid memo) to cover-backfill.ts with tests</name>
  <files>src/lib/services/cover-backfill.ts, src/lib/services/cover-backfill.test.ts</files>
  <behavior>
    New `describe('resolveShareCover — share-card carrier chain (quick-260920-l82)')` in cover-backfill.test.ts, mirroring the resolveHqCover block's style. `beforeEach` of the new block (or the file-level one) calls `__resetShareCoverMemo()` so a memo hit cannot leak across tests. Use distinct uids per test.
    - Test 1 iTunes hit: itunesSongCover → 'https://is1-ssl.mzstatic.com/image/thumb/X/600x600bb.jpg'; deezerSongCover spied. Expect that URL returned; deezer NOT called; `ytmCalls()` and `cnCalls()` both length 0 (searchAll never issued — YTM and CN are excluded by construction).
    - Test 2 iTunes miss + Deezer hit: itunes → null, deezer → 'https://cdn-images.dzcdn.net/images/cover/fe1082c5ef54876802146897e76b592e/1000x1000-000000-80-0-0.jpg'. Expect the Deezer URL; deezer called with (artist, title, signal) in that order; assert `coverToken(url) === 'd:fe1082c5ef54876802146897e76b592e'` (import coverToken from './share') — this pins the bonus: the Deezer tier output IS tokenizable, so the residual kn4 gap is closed.
    - Test 3 both miss: both → null. Expect null, no throw, `getCachedCover(artist, title)` null.
    - Test 4 NO COVER-CACHE WRITE ON A HIT (the property most likely to regress): iTunes hit for `mk('kuwo', 'share1', { artist: 'Drake', title: 'Hotline Bling' })`. Expect `getCachedCoverByUid('kuwo:share1')` null AND `getCachedCover('Drake', 'Hotline Bling')` null after the call. Add a one-line comment: this is the ONE divergence from resolveCoverForTrack/resolveHqCover — activeCover reads readCoverByUidOrName, so a write here would flip displayed art.
    - Test 5 memo: iTunes hit, call twice with the same track. Expect itunes spy called exactly ONCE and both calls return the same URL (second is a memo hit, zero tier calls).
    - Test 6 memo is hits-only: both tiers → null on call 1, then iTunes → URL on call 2 for the same track. Expect call 2 returns the URL and itunes spy called twice (a miss was NOT memoised).
    - Test 7 per-tier never-throw: itunes rejects, deezer → Deezer URL. Expect the Deezer URL. Then both reject → resolves null.
    - Test 8 non-https: itunes → 'http://insecure.example/x.jpg', deezer → Deezer https URL. Expect the Deezer URL (http iTunes result treated as a miss and fell through).
    - Test 9 already-aborted signal: expect null, itunes and deezer NOT called.
    - Test 10 empty uid stub (`mk('netease','ignored',{ uid: '' , …})`): iTunes hit twice → itunes spy called TWICE (empty uid is never memoised — mirrors the siblings' empty-uid posture so unrelated stubs cannot share one slot).
  </behavior>
  <action>
    In `src/lib/services/cover-backfill.ts`, directly after `resolveHqCover` (ends L297) and before `backfillCovers`, add:

    1. A module-scope `const shareCoverMemo = new Map<string, string>()` placed BESIDE the existing `missAt` negative-miss cache (after `__resetCoverMissCache`, ~L135), plus `export function __resetShareCoverMemo(): void { shareCoverMemo.clear(); }` (TEST-ONLY, same convention as `__resetCoverMissCache`). RELOCATE the kn4 `<script module>` comment from TrackMenu.svelte L2-12 here, amended with a `quick-260920-l82` line: the memo moved out of the component because a service module is the natural cross-instance home (5-6 list pages each mount their own TrackMenu; an instance field would re-issue the GET per page crossing) and because every future share surface should get the memo for free. Keep its HITS-ONLY sentence verbatim (a miss or abort is never memoised — CLAUDE.md: never cache a failure).

    2. `export async function resolveShareCover(track: Track, signal?: AbortSignal): Promise<string | null>` — signature matches its two exported siblings (both take the whole `Track`; only the unexported chain internals take (artist, title)); the Track is also what keys the memo. Body, in order:
       - `if (signal?.aborted) return null;`
       - memo check: `if (track.uid) { const hit = shareCoverMemo.get(track.uid); if (hit) return hit; }`
       - `let cover = await tier(() => itunesSongCover(track.artist ?? '', track.title ?? '', signal));` then `if (signal?.aborted) return null;`
       - `if (!cover) { cover = await tier(() => deezerSongCover(track.artist ?? '', track.title ?? '', signal)); if (signal?.aborted) return null; }`
       - `if (hasHttpsScheme(cover)) { if (track.uid) shareCoverMemo.set(track.uid, cover); return cover; } return null;`
       No `setCachedCoverByUid`, no `setCachedCover`, no `markHit`/`markMiss` (that negative cache keys the DISPLAY chain by name and must not be poisoned by a share-only probe).

    3. A doc comment in the house style (load-bearing decision record, tag `quick-260920-l82`), covering:
       - WHAT: the share-card carrier chain, the third sibling of resolveCoverForTrack (miss-recovery, YTM → iTunes → Deezer → CN, writes cache) and resolveHqCover (upgrade, YTM → Deezer, writes cache). Same `tier()` never-throw + `hasHttpsScheme` guard (T-0bb-01) — NOT a new fetch ladder.
       - WHY a different subset: `coverToken` (share.ts) is a CLOSED grammar — `d:` Deezer, `l:` Last.fm, `k:` kuwo, `i:` iTunes-by-retained-id. YTM (googleusercontent / ytimg) is EXCLUDED because it is not in the grammar — that is the exact reason kn4 exists; calling resolveTrackChain here would hand back the untokenizable YTM cover and regress the card to the branded fallback. CN is excluded because netease/qq/joox hosts are on no /api/og tier allow-list (T-3uo-02) and a 7-source searchAll is far too expensive for a menu-open prewarm.
       - WHY iTunes first: `itunesSongCover → fetchTopArtwork → rememberItunesId` retains the numeric id in the disjoint `itunes:` cache family, so `recallItunesId` yields it at share time and `coverToken` emits `i:<id>`. Deezer second (quick-260920-l82 — NEW over kn4, which issued iTunes only): a song iTunes does not carry now yields `d:<32hex>` instead of the branded card. `deezerSongCover` returns `album.cover_*` only (`/images/cover/<32hex>/…`; artist pictures are a separate field), so its output always tokenizes; Deezer's art-less placeholder has an empty md5, fails DZ_COVER_PATH, and simply yields no carrier — advisory, falls through to today's server chain.
       - 🔴 THE ONE DIVERGENCE (carry the kn4 L247-250 reasoning here so it lives with the code that must honour it): this function MUST NOT write the uid or name layer of the cover cache (no setCachedCoverByUid / setCachedCover / writeCoverBoth). `activeCover` in TrackMenu reads `readCoverByUidOrName`, so caching the iTunes/Deezer URL would flip the art the app DISPLAYS — every list row and the hero — from the YTM cover to this one. The ask was about the share card only. The result lives in the caller's `$state` and the session memo, nowhere else. The `itunes:` id family write inside itunesSongCover is fine: it is a disjoint key family no display surface reads.
       - Cost note: at most 2 requests per menu open, 0 on a memo hit, 0 when the displayed cover already tokenizes (the caller's guard). iTunes is a direct CORS-open GET; Deezer goes through the own-origin proxy and inherits the apiFetch governor.

    Then write the tests described in `<behavior>` in `cover-backfill.test.ts`: add `resolveShareCover` and `__resetShareCoverMemo` to the L2-9 import, add `import { coverToken } from './share';`, and add `__resetShareCoverMemo()` to the file-level `beforeEach` next to `__resetCoverMissCache()`. Do NOT edit any existing test.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm vitest --run src/lib/services/cover-backfill.test.ts src/lib/services/share.test.ts</automated>
  </verify>
  <done>resolveShareCover + __resetShareCoverMemo exported; all 10 new tests green; every pre-existing test in cover-backfill.test.ts green and unedited; `git diff src/lib/services/cover-backfill.ts` shows zero changes inside resolveTrackChain / resolveCoverForTrack / resolveHqCover / backfillCovers bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire TrackMenu's share-cover prewarm to resolveShareCover; drop the direct iTunes import and the component-local memo</name>
  <files>src/lib/components/TrackMenu.svelte</files>
  <action>
    1. Delete the `<script module lang="ts">` block (L1-14) entirely — its comment was relocated into cover-backfill.ts in Task 1 and the Map now lives there. The file must start with `<script lang="ts">`.

    2. Imports: change L87 to `import { recallItunesId } from '$lib/services/itunes-cover';` (recallItunesId is still needed by the effect's "already tokenizes" guard at L217 and by doShare at L875). Extend the L80 import to `import { collectCoverCandidates, resolveShareCover, type CoverCandidate } from '$lib/services/cover-backfill';` and add a one-line comment tagged quick-260920-l82: the share-card carrier chain now lives beside the display chain; this component no longer calls a cover tier directly (CLAUDE.md "Shared Primitives — import these, never re-inline them"). Keep the `combinedSignal` import (L86) — still used.

    3. The prewarm effect (L187-252). Keep its shape: the tracked-deps read of {open, track, activeCover}; the `coverToken(cover, recallItunesId(cover)) !== null` early return (no request when the displayed cover already tokenizes — link byte-identical); the `new AbortController()`; `combinedSignal(8_000, ac.signal)`; the `untrack(...)`; the `.then` stale-uid drop `if (ac.signal.aborted || track?.uid !== target.uid) return;`; the `return () => ac.abort()` cleanup. Change:
       - Remove the `shareItunesMemo.get(target.uid)` branch (L221-225) and the `shareItunesMemo.set(...)` line (L242) — memoisation is inside resolveShareCover now. Note in a comment that a memo hit now lands one microtask later instead of synchronously; doShare reads `shareCoverFallback` on a later user tap so the ordering is unaffected.
       - Replace L236's `itunesSongCover(target.artist, target.title, combinedSignal(8_000, ac.signal))` with `resolveShareCover(target, combinedSignal(8_000, ac.signal))` (pass the Track — RAW catalog artist/title travel inside it, satisfying the existing "RAW target.artist / target.title, NOT names.dn*" rule; keep that comment).
       - In `.then`: `if (url) shareCoverFallback = url;`
    4. Amend (never delete) the kn4 comment block L187-204: keep ROOT CAUSE and REJECTED paragraphs verbatim; rewrite the "SO:" paragraph to say the prewarm now calls `resolveShareCover` (iTunes → Deezer, cover-backfill.ts) and that the Deezer tier is the l82 addition closing kn4's residual gap (a song iTunes lacks now yields `d:<32hex>` rather than the branded card). Keep the "PREWARM and not an await in doShare because navigator.share() must run synchronously inside the user gesture on iOS Safari" sentence verbatim. Amend the `untrack` rationale (L233-235) to name resolveShareCover (its iTunes tier still reads/writes the `itunes:` id family of the cover cache, and Deezer's ttl-cache reads localStorage — those reads would re-invalidate the effect). Replace the 🔴 block (L247-250) with a short pointer: the no-cache-write prohibition is now enforced by resolveShareCover itself and pinned by its "NO cover-cache write on a hit" test in cover-backfill.test.ts; this component still must never call writeCoverBoth here.
    5. Update the doShare comment L815-817 ("The fallback is the iTunes prewarm result") to "the resolveShareCover prewarm result (iTunes, else Deezer)". Do NOT change any code in doShare: `const shareCover = shareCoverFallback ?? activeCover;` before `onclose()`, the songShareUrl call, and `nav.share` stay byte-identical — no await is introduced before `nav.share()`.

    Sanity greps after editing: `grep -n "itunesSongCover\|shareItunesMemo\|script module" src/lib/components/TrackMenu.svelte` must print nothing.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && test -z "$(grep -n 'itunesSongCover\|shareItunesMemo' src/lib/components/TrackMenu.svelte)" && grep -c 'resolveShareCover(' src/lib/components/TrackMenu.svelte | grep -qv '^0$' && test -z "$(grep -rn 'from .\$lib/services/itunes-cover.' src/lib/components src/routes src/lib/stores src/lib/actions | grep -v recallItunesId)" && pnpm check && pnpm vitest --run src/lib/services/cover-backfill.test.ts src/lib/services/share.test.ts src/lib/services/itunes-cover.test.ts src/lib/components/track-menu-gate.test.ts</automated>
  </verify>
  <done>TrackMenu imports resolveShareCover from $lib/services/cover-backfill and only recallItunesId from itunes-cover; no `<script module>` block; `pnpm check` clean; the four vitest files green; doShare's code (L808-897 region) unchanged except comment prose; kn4 decision-record comments preserved/amended with quick-260920-l82 tags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iTunes / Deezer response → cover URL → share link token | Third-party API output becomes an `<img src>` candidate and a `coverToken` input |
| Share link `?ci=` → /api/og fetcher | Untouched by this plan; carrier is a closed-grammar id, never a URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l82-01 | Tampering | resolveShareCover output | mitigate | Same `tier()` + `hasHttpsScheme` guard as siblings (T-0bb-01); coverToken still parses with `new URL` and a closed host/path grammar — no new host reaches /api/og (T-3uo-02) |
| T-l82-02 | Tampering (state) | cover cache uid/name layers | mitigate | resolveShareCover writes neither layer; pinned by the "NO cover-cache write on a hit" unit test so displayed art cannot be flipped by a share probe |
| T-l82-03 | DoS | menu-open prewarm | mitigate | ≤2 requests per open, 0 on memo hit / tokenizable cover / aborted signal; 8 s combinedSignal ceiling; Deezer inherits apiFetch governor; no CN searchAll |
| T-l82-04 | Information disclosure | session memo | accept | Stores only public CDN cover URLs keyed by track uid, in-memory, session-lived |
| T-l82-SC | Tampering | npm installs | accept | No new dependency introduced by this plan |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/services/cover-backfill.test.ts src/lib/services/share.test.ts src/lib/services/itunes-cover.test.ts src/lib/components/track-menu-gate.test.ts` green
- `pnpm check` clean
- `grep -rn "itunesSongCover" src --include=*.svelte` → no matches (cover-backfill.ts is the only non-test caller of a cover tier)
- `git diff` on cover-backfill.ts touches only: imports unchanged, new memo block, new resolveShareCover + doc comment, new __resetShareCoverMemo — the three existing chains byte-identical
</verification>

<success_criteria>
- Exactly one cover-tier call site outside cover-backfill.ts existed (TrackMenu L87/L236); it is gone, replaced by `resolveShareCover` from the shared module
- Share links for YTM-covered songs carry `i:<id>` when iTunes has the song, `d:<32hex>` when only Deezer does (new over kn4), and fall through unchanged otherwise
- Displayed cover art is unaffected (no cache write; test-pinned)
- Existing resolveTrackChain / resolveCoverForTrack / resolveHqCover behaviour and tests untouched
- iOS user-gesture rule for navigator.share preserved
</success_criteria>

<output>
Create `.planning/quick/260920-l82-route-the-share-card-cover-through-the-s/260920-l82-SUMMARY.md` when done. State explicitly in the SUMMARY that the Deezer tier closes the residual gap flagged open when kn4 shipped (songs iTunes lacks now get a `d:` carrier instead of the branded card), and that the memo moved from TrackMenu's `<script module>` into cover-backfill.ts.
</output>
