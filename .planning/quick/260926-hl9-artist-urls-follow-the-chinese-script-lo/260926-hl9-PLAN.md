---
phase: quick-260926-hl9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/names.svelte.ts
  - src/lib/stores/names.test.ts
  - src/lib/stores/library.svelte.ts
  - src/lib/stores/library.svelte.test.ts
  - src/lib/components/TrackMenu.svelte
  - src/lib/components/NowPlaying.svelte
  - src/routes/(app)/+page.svelte
  - src/routes/(app)/charts/top/+page.svelte
  - src/routes/(app)/album/[name]/+page.svelte
  - src/routes/(app)/search/+page.svelte
  - src/routes/(app)/library/+page.svelte
  - src/routes/(app)/artist/[name]/+page.svelte
autonomous: true
requirements: [quick-260926-hl9]

must_haves:
  truths:
    - "With Settings → Translation → Chinese script = Traditional, tapping any artist (row menu, now-playing, home tiles/shelves, charts, search, library favourites, related artists, album back button) lands on /artist/周杰倫 whether the upstream row said 周杰伦 or 周杰倫"
    - "With the lock = Simplified the same taps land on /artist/周杰伦"
    - "With the lock OFF every artist URL is byte-identical to today (encodeURIComponent of the raw name)"
    - "The URL is never TRANSLATED: an artist name never becomes its English/Japanese translation in the route, even with an artist translation target set"
    - "Non-Chinese names are untouched: /artist/Coldplay, /artist/AC%2FDC"
    - "A favourite saved as 周杰伦 shows a filled heart on /artist/周杰倫 (and vice versa) whenever the t2s dict is warm (any lock on); tapping the heart there un-favourites it"
    - "A cold deep link to /artist/周杰倫 repaints the heart once the lock dict lands (no stale empty heart)"
  artifacts:
    - path: "src/lib/stores/names.svelte.ts"
      provides: "Names.artistHref(name) — the ONE in-app artist route builder, script lock only"
      contains: "artistHref(name: string): string"
    - path: "src/lib/stores/library.svelte.ts"
      provides: "script-blind favKey (Chinese folds to Simplified via t2sConvertLineSync)"
      contains: "t2sConvertLineSync"
    - path: "src/lib/stores/names.test.ts"
      provides: "artistHref describe (zh-Hant both inputs, zh-Hans, off, non-Chinese, never translates)"
      contains: "quick-260926-hl9"
    - path: "src/lib/stores/library.svelte.test.ts"
      provides: "script-blind favArtists describe"
      contains: "quick-260926-hl9"
  key_links:
    - from: "every in-app artist navigation (14 sites)"
      to: "names.artistHref"
      via: "direct call, no local '/artist/' + encodeURIComponent builders left"
      pattern: "names\\.artistHref\\("
    - from: "src/lib/stores/names.svelte.ts artistHref"
      to: "Names.zhLock (applyLock)"
      via: "'/artist/' + encodeURIComponent(this.zhLock(name))"
      pattern: "encodeURIComponent\\(this\\.zhLock"
    - from: "src/routes/(app)/artist/[name]/+page.svelte favArtist $derived"
      to: "names.zhLock → names.rev + settings.zhScript"
      via: "$derived(library.isFavArtist(names.zhLock(name)))"
      pattern: "isFavArtist\\(names\\.zhLock\\(name\\)\\)"
---

<objective>
Make in-app artist URLs follow the Chinese script lock. User's words: "the artist name in url should
also be translated if in chinese, if the user choose to see either simplified or traditional chinese,
they will see it everywhere". Locked interpretation: SCRIPT LOCK ONLY (names.zhLock), never
translation (names.dnArtist) — translation would break artist resolution.

Purpose: one spelling of an artist everywhere, including the address bar / history, and a favourites
heart that does not split 周杰伦 and 周杰倫 into two artists.
Output: `names.artistHref()` helper + tests, script-blind `library.favKey` + tests, 14 call sites
swept to the helper, reactive heart on the artist page.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

Already verified by the orchestrator against real upstreams — do NOT re-investigate:
- The artist page resolves everything off `page.params.name` (searchAll, MusicBrainz, Last.fm
  enrich, Deezer, the fav heart). `searchAll` is script-blind in practice (周杰伦 / 周杰倫, 陈奕迅 /
  陳奕迅, 邓紫棋 / 鄧紫棋 return identical per-source counts and top artists) and Last.fm
  `getAlbumTracklist` is script-blind too. Routing with the LOCKED name is safe for resolution.
- Share links already carry the display-language name (quick-260808-urx, `share.ts` +
  artist page `shareArtist`) — OUT OF SCOPE, do not touch `share.ts`.
- The fav heart is NOT script-blind today: `library.favKey` is `trim().toLowerCase()`.

Out of scope (leave raw, mention in SUMMARY): album hrefs from `discography.ts` `albumHref(entry, name)`
need no change (on the artist + albums pages `name` is the route param, already locked); chart-album
hrefs in `home-charts.ts` (pure service) stay raw.

<interfaces>
From src/lib/stores/names.svelte.ts (class Names, singleton `export const names = new Names();` at EOF):
- `private applyLock(text: string): string` — reads `this.rev` (reactive) + `settings.zhScript`
  (reactive); 'off' / unknown value → input byte-for-byte; otherwise `warmLock(target)` then
  `lockScriptSync(text, target)` (identity while the dict is cold, non-Chinese passthrough).
- `zhLock(text: string): string` (line ~360) — public, sync, network-free wrapper of applyLock.
- `dnArtist(text)` — translation + lock. MUST NOT be used for routes.

From src/lib/services/zh-convert.ts:
- `export function isChineseLine(text: string): boolean`
- `export function t2sConvertLineSync(text: string): string | null` — null = t2s dict cold; caller
  must gate on isChineseLine first.
- `export async function warmScript(target: 'zh-Hant' | 'zh-Hans'): Promise<void>` — zh-Hans builds
  t2s; zh-Hant builds s2t AND t2s (quick-260926-bxg). Never rejects.

From src/lib/stores/library.svelte.ts (lines 208-227):
- `private favKey(name: string): string` → currently `(name ?? '').trim().toLowerCase()`
- `isFavArtist(name)` / `toggleFavArtist(name)` both compare via favKey. Only caller of isFavArtist
  outside the store: `src/routes/(app)/artist/[name]/+page.svelte:152`.
- Current imports: browser, blobStore, setCachedCover, hasHttpsScheme, matchKey, type Track.
  zh-convert imports only `$lib/i18n/detect` → no import cycle. library MUST NOT import names.

Test harness facts:
- names.test.ts: `vi.mock('$app/environment', () => ({ browser: true }))`; hoisted mutable
  `settingsMock` (artistLang/titleLang/... 'ja', `zhScript: 'off'`) restored in `beforeEach`, which
  also does `vi.resetModules()` + `vi.useFakeTimers()`; `translateMock`; `flush()` helper. Lock
  tests import fresh: `const { names } = await import('./names.svelte')` then warm via
  `const zh = await import('$lib/services/zh-convert'); await zh.warmScript(target)` (see the
  `describe('names — Chinese script lock (quick-260919-2jo)'` block, lines 253-333, local
  `warmLock` helper).
- library.svelte.test.ts: STATIC `import { library } from './library.svelte'` (no resetModules),
  browser ON, in-memory `memStore` localStorage, blobStore mocked. Per-describe reset pattern:
  `beforeEach(() => { library.favArtists = []; memStore.clear(); })` (see the vb9 block, line 256).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: names.artistHref helper + script-blind favKey, with tests</name>
  <files>src/lib/stores/names.svelte.ts, src/lib/stores/names.test.ts, src/lib/stores/library.svelte.ts, src/lib/stores/library.svelte.test.ts</files>
  <behavior>
    names.test.ts, new `describe('names.artistHref — artist routes follow the script lock (quick-260926-hl9)')`
    (own local warm helper mirroring the 2jo block; fresh `await import('./names.svelte')` per test):
    - lock zh-Hant, warmed: artistHref('周杰伦') === '/artist/' + encodeURIComponent('周杰倫'); artistHref('周杰倫') gives the SAME string (idempotent, quick-260926-bxg)
    - lock zh-Hans, warmed: artistHref('周杰倫') === '/artist/' + encodeURIComponent('周杰伦')
    - lock 'off' (warm zh-Hant anyway): artistHref('周杰伦') === '/artist/' + encodeURIComponent('周杰伦') and artistHref('周杰倫') === '/artist/' + encodeURIComponent('周杰倫') — byte-identical to the old builders
    - lock zh-Hant: artistHref('Coldplay') === '/artist/Coldplay'; artistHref('AC/DC') === '/artist/AC%2FDC'
    - never translation: with settingsMock.artistLang left at 'ja' and lock zh-Hant, call artistHref('邓紫棋'), `await flush()`, expect translateMock not called and the result === '/artist/' + encodeURIComponent('鄧紫棋')
    library.svelte.test.ts, new `describe('quick-260926-hl9 script-blind favArtists')` (beforeEach resets favArtists + memStore):
    - after `await warmScript('zh-Hans')` (static `import { warmScript } from '$lib/services/zh-convert'`): toggleFavArtist('周杰伦') → isFavArtist('周杰倫') true and isFavArtist('周杰伦') true; then toggleFavArtist('周杰倫') → favArtists toEqual([])
    - Latin unchanged: toggleFavArtist('Daft Punk') → isFavArtist('  daft punk  ') true, favArtists toEqual(['Daft Punk']) (case preserved)
  </behavior>
  <action>
    RED first: add the two describes above, run them, confirm the names ones fail (artistHref missing) and the Chinese fav one fails (周杰倫 ≠ 周杰伦 key). Commit test-only is optional; a single commit per task is fine.

    names.svelte.ts (per locked decision 1+2): add a public method `artistHref(name: string): string` on class Names directly after `zhLock`, returning `'/artist/' + encodeURIComponent(this.zhLock(name))`. Doc-comment it with quick-260926-hl9: the ONE in-app artist route builder; script lock ONLY (zhLock — sync, network-free, Chinese-only, 'off' = byte-identical), NEVER dnArtist, because a translated name ("Jay Chou" for 周杰伦) would change what the artist page resolves, while searchAll / Last.fm are script-blind so a re-scripted name resolves identically; encodes exactly once (SvelteKit decodes the param, OG-COMPAT-01); share.ts builds its own display-language share URLs (quick-260808-urx) and deliberately does not use this. Keep the method reactive-transparent (it inherits zhLock's rev/zhScript reads) — add nothing else.

    library.svelte.ts (per locked decision 3): add `import { isChineseLine, t2sConvertLineSync } from '$lib/services/zh-convert';` and change favKey body to: `const k = (name ?? '').trim().toLowerCase(); return isChineseLine(k) ? (t2sConvertLineSync(k) ?? k) : k;`. Extend the favKey doc comment: quick-260926-hl9 — Chinese names fold to Simplified so 周杰伦 / 周杰倫 are one favourite now that artist URLs follow the script lock; t2s is warm whenever a lock is on (warmScript('zh-Hans') builds t2s, warmScript('zh-Hant') builds both since quick-260926-bxg); cold t2s returns the raw key = the exact old behaviour; library stays leaf-ish and must not import names. Add a `ponytail:` comment naming the ceiling: with the lock OFF and t2s never warmed, a favourite saved under the other script is not matched; upgrade path = warm t2s (warmScript('zh-Hans')) in load() when favArtists contains a Chinese name. Do NOT touch toggleFavArtist (it stores the trimmed input, compares via favKey).

    Tabs, single quotes, keep all existing decision-ref comments.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic/.claude/worktrees/festive-kapitsa-dac746 && pnpm vitest --run src/lib/stores/names.test.ts src/lib/stores/library.svelte.test.ts && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>New describes pass; full suite, svelte-check and build green; one commit on the current branch (claude/festive-kapitsa-dac746), e.g. `feat(quick-260926-hl9): names.artistHref + script-blind favourite artists`. Not pushed.</done>
</task>

<task type="auto">
  <name>Task 2: sweep all 14 artist-route builders to names.artistHref + reactive heart + comments</name>
  <files>src/lib/components/TrackMenu.svelte, src/lib/components/NowPlaying.svelte, src/routes/(app)/+page.svelte, src/routes/(app)/charts/top/+page.svelte, src/routes/(app)/album/[name]/+page.svelte, src/routes/(app)/search/+page.svelte, src/routes/(app)/library/+page.svelte, src/routes/(app)/artist/[name]/+page.svelte</files>
  <action>
    Per locked decision 2. Every file below ALREADY imports `names` from '$lib/stores/names.svelte' — add no imports. Replace ONLY the href expression; keep each site's surrounding logic (guards, overlays.navigateAway wrappers, ternaries) exactly. The orchestrator's list said "15" but enumerates 14 sites; the 15th grep hit is a COMMENT in PageHeader.svelte:16 — leave PageHeader untouched.
    - TrackMenu.svelte:513 `gotoArtist`: `const dest = names.artistHref(track.artist);` — keep the `!track || !hasArtist` guard. Rewrite the comment at lines 509-511: keep the quick-260919-dlring hasArtist sentence; replace "Navigating with the RAW track.artist is load-bearing — names.dnArtist is a display translation and the route must not see it" with: the route sees the RAW track.artist passed through the script lock ONLY (names.artistHref → zhLock), never names.dnArtist — a translation would change what the artist page resolves; the lock is safe because searchAll / Last.fm are script-blind (quick-260926-hl9). Update the matching markup comment at ~line 994-996 ("Note gotoArtist() navigates with the RAW track.artist; names.dnArtist is display-only and must never reach the route") the same way: raw name through the script lock only (names.artistHref, quick-260926-hl9); dnArtist must never reach the route.
    - NowPlaying.svelte:416 `openArtistName`: `overlays.navigateAway(() => goto(names.artistHref(name)));` — keep `if (!name) return;`.
    - (app)/+page.svelte lines 1270, 1347, 1364, 1671, 1685: `goto('/artist/' + encodeURIComponent(X))` → `goto(names.artistHref(X))` (X = `name` at 1270, `a.name` elsewhere).
    - charts/top/+page.svelte:289: `goto(names.artistHref(a.name))`.
    - album/[name]/+page.svelte:624: `onback={() => goto(albumArtist ? names.artistHref(albumArtist) : '/')}`.
    - search/+page.svelte:171 `goto(names.artistHref(s.title));` — update the comment above it to say the helper encodes once (keep the OG-COMPAT-01 ref); :733 `goto(names.artistHref(tile.name))`.
    - library/+page.svelte:493 `else goto(names.artistHref(name));` (edit-mode toggle branch untouched).
    - artist/[name]/+page.svelte:534 `href={names.artistHref(name) + '/albums'}`; :618 `goto(names.artistHref(a.name))`.
    - artist/[name]/+page.svelte:152 heart (locked decision 3): `const favArtist = $derived(library.isFavArtist(names.zhLock(name)));` with a quick-260926-hl9 comment: zhLock reads names.rev (bumped by warmLock once the dict lands) and settings.zhScript, so a cold deep link — whose first favKey ran before t2s was warm — re-derives and repaints the heart; on a locked route `name` is already locked so the value is unchanged. `toggleFavourite` / `library.toggleFavArtist(name)` unchanged.
    Do NOT touch share.ts, discography.ts, home-charts.ts.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic/.claude/worktrees/festive-kapitsa-dac746 && test "$(grep -rnE "['\`]/artist/['\$]" src --include='*.svelte' --include='*.ts' | grep -v '\.test\.ts:' | grep -vE '^[^:]+:[0-9]+:[[:space:]]*(//|\*|<!--)' | cut -d: -f1 | sort -u)" = "src/lib/stores/names.svelte.ts" && test "$(grep -rn 'names\.artistHref(' src --include='*.svelte' | wc -l | tr -d ' ')" = "14" && grep -q 'isFavArtist(names.zhLock(name))' 'src/routes/(app)/artist/[name]/+page.svelte' && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>The only non-comment, non-test `'/artist/'` / backtick-`/artist/${` builder left in src is the helper in names.svelte.ts (share.ts uses `${base}/artist/` and is not matched — out of scope); 14 `names.artistHref(` call sites; heart derived through zhLock; both TrackMenu comments updated; test/check/build green; one commit on the current branch, e.g. `feat(quick-260926-hl9): artist URLs follow the Chinese script lock`. Not pushed, main untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream catalog → in-app route | artist names come from third-party APIs and are placed in a URL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hl9-01 | Tampering | names.artistHref path segment | mitigate | encodeURIComponent applied exactly once inside the helper (same encoding as the 14 builders it replaces); test pins 'AC/DC' → '/artist/AC%2FDC' so a slash can never escape the segment |
| T-hl9-02 | Information disclosure | artist route | accept | zhLock is sync + network-free; the helper never calls /api/translate (test asserts translateMock not called), so no name leaves the device via this path |
| T-hl9-03 | Denial of service | favKey on every isFavArtist compare | accept | t2sConvertLineSync is a sync in-memory dict lookup over a short list; no network, no retry |
</threat_model>

<verification>
- `pnpm test && pnpm check && pnpm build` green after each task.
- Optional browser spot check (dev server 4321 or 5173 — probe): Settings → Translation → Chinese script = Traditional, open a 周杰伦 row's menu → Go to artist → address bar shows /artist/周杰倫; favourite it, flip the lock to Simplified, tap the artist again → /artist/周杰伦 with the heart still filled.
</verification>

<success_criteria>
- Every in-app artist navigation goes through `names.artistHref`; URLs follow the script lock, 'off' is byte-identical to today, nothing is ever translated into the route.
- 周杰伦 and 周杰倫 are one favourite whenever t2s is warm; the ceiling (lock off + t2s cold) is marked with a `ponytail:` comment.
- Two commits on claude/festive-kapitsa-dac746, nothing pushed.
</success_criteria>

<output>
Create `.planning/quick/260926-hl9-artist-urls-follow-the-chinese-script-lo/260926-hl9-SUMMARY.md` when done.
</output>
