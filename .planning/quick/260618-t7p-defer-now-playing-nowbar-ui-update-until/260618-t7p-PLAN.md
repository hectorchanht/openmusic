---
phase: quick-260618-t7p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/player.svelte.ts
  - src/lib/stores/player.svelte.test.ts
  - src/lib/components/Nowbar.svelte
  - src/lib/components/NowPlaying.svelte
autonomous: true
requirements: [T7P-01, T7P-02, T7P-03]

must_haves:
  truths:
    - "Clicking a new song while one is playing keeps the OLD song's title/artist/cover/lyrics displayed in BOTH the Nowbar and NowPlaying until the new song fires a real `playing` event."
    - "During the load window (click → new song's `playing` event) the now-bar/now-playing running-line (marquee) animates to signal loading-in-progress."
    - "If the new song never reaches `playing` (fails), the old song info stays displayed; existing stall-watchdog / cross-source fallback / loop-guard / auto-advance behaviour is unchanged."
    - "With 'Hide parenthesised lines' ON, the active lyric line centres correctly (no over-scroll / off-centre) in closed, half, and full sheet modes."
    - "When a lyric moment is active, the rendered translation text of every active line is highlighted with the same active style as the original line."
  artifacts:
    - path: "src/lib/stores/player.svelte.ts"
      provides: "A `displayed` track field that advances on `playing`; existing `current` semantics intact."
    - path: "src/lib/components/Nowbar.svelte"
      provides: "Now-bar bound to the displayed track for title/artist/cover."
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "Now-playing meta/cover/lyrics bound to the displayed track; filtered-list-aware active-line scroll anchor; active translation highlight."
  key_links:
    - from: "el.addEventListener('playing')"
      to: "this.displayed = this.current"
      via: "swap-commit point in the playing listener"
      pattern: "displayed = this.current"
    - from: "Nowbar.svelte / NowPlaying.svelte"
      to: "player.displayed"
      via: "reactive read for title/artist/cover/lyrics"
      pattern: "player\\.displayed"
---

<objective>
Three independent player/lyrics behaviour fixes in the SvelteKit app (the live app under `src/`, NOT the stale `index.html`).

1. Defer the now-playing/nowbar UI swap (title/artist/cover/lyrics) until the newly-clicked song ACTUALLY starts playing; run the marquee as a loading indicator during the load window.
2. Fix lyric over-scroll / off-centre when "Hide parenthesised lines" is ON (the active-index/rendered-list index-space mismatch).
3. Highlight ALL translated lines belonging to the current active lyric moment.

Purpose: A new song should not visually "take over" the now-playing surfaces while the previous song is still audible; lyrics should track + highlight correctly under the paren-hide setting.
Output: Edits to `player.svelte.ts` (+ tests), `Nowbar.svelte`, `NowPlaying.svelte`. Three atomic commits, one per task.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

NOTE: CLAUDE.md is STALE — it describes the old single-file `index.html` desktop player. The
LIVE app is SvelteKit under `src/`. Do all work under `src/`, never `index.html`.

Key files (read in full before editing — they are heavily-engineered with documented invariants):
@src/lib/stores/player.svelte.ts
@src/lib/components/Nowbar.svelte
@src/lib/components/NowPlaying.svelte
@src/lib/services/lrc.ts
@src/lib/stores/settings.svelte.ts
@src/lib/actions/marquee.ts
@src/lib/stores/player.svelte.test.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

player.svelte.ts (class Player → exported singleton `player`):
- `current = $state<Track | null>(null)` — line 118. The INTERNAL "track being played": queue
  index source, MediaSession source, prefetch/auto-advance source, playGen source. Set SYNCHRONOUSLY
  on `play()` entry (line 1728). DO NOT change this field's timing — invariants D-06/CR-01/D-13/D-14,
  stall watchdog, loop-guard, prefetch, MediaSession all depend on it being set synchronously.
- `pendingTrack = $state<PendingTrack | null>(null)` — line 192. Optimistic stub for the
  resolve-on-tap path (discovery tiles → resolveStub). {artist,title,cover} only, NO uid. Cleared
  when `current` is set.
- `resolvedCover = $state<string | null>(null)` — line 207. "The ONE cover field every now-playing
  surface reads" (COVER-01/D-09). Set SYNCHRONOUSLY on `play()` entry (line 1736) + async-landed by
  resolveCoverAsync / resolveCoverForTrack.
- `playing = $state(false)`, `loading = $state(false)` — lines 119-120.
- `play(track, opts?: { fresh?, fromFallback? })` — line 1721. Sets current/resolvedCover/loading
  synchronously, then awaits resolve + audio.src + audio.play().
- The `playing` event listener — lines 981-1004 in `attach()`. Comment: "`playing` is the event that
  means audio is ACTUALLY producing output (CR-01)." This is the swap-commit point for Task 1.
- The `error` event listener — lines 1118-1168 (cross-source fallback). The `pause` listener —
  lines 1005-1018. The stall watchdog `armStall()` — line 777.

Nowbar.svelte:
- line 29: `const np = $derived(player.current ?? player.pendingTrack);` — title/artist/cover source.
- line 30: `const resolving = $derived(!player.current && !!player.pendingTrack);`
- np?.title / np?.artist / np?.cover and player.resolvedCover drive the rendered cover/title/artist.

NowPlaying.svelte:
- line 80-82: `lines = $derived(player.current?.lrc ? splitParenLines(reorderPairs(parseLRC(...))) : [])`
- line 89-104: `activeIndexAndTime` ($derived.by) → `activeLine` (= idx), `activeTime` (= maxTime).
  Computed over the FULL `lines` array.
- line 189-224: the active-line scroll anchor `$effect`. Uses
  `lyricsEl.querySelectorAll('p')[idx]` — positional index into the RENDERED <p> list.
- line 1198-1220: the lyric render `{#each lines as l, i (i)}` with the
  `{#if !(l.fromParen && settings.lyricsHideParenLines)}` filter — the rendered list is FILTERED but
  `idx`/`activeTime` are computed over the UNFILTERED `lines`.
- line 1211: `<p class:active={l.time === activeTime && activeTime >= 0} ...>` — active class.
- line 1212-1217: translation render. `replace` mode: `{translated[i]}`. `below` mode:
  `{l.text}` + `<span class="tr">{translated[i]}</span>`. The `.tr` span never gets `.active`.
- line 293: `showTr = $derived(settings.lyricsLang !== 'off' && translated.length === lines.length)`
- line 379: `effectiveCover = $derived(swappedCover ?? player.resolvedCover ?? null)` — cover source.
- line 1078-1081: title/artist `{#key player.current?.uid}` + `use:marquee` + `.marquee-inner`.

lrc.ts: `LyricLine { time, text, fromParen? }`, `parseLRC`, `reorderPairs`, `splitParenLines`,
`lineSeekFraction`. (Pure; do NOT edit for these tasks — the index-space fix is in the component.)

settings.svelte.ts: `lyricsHideParenLines` (line 134), `lyricsHideParenTranslation` (line 130),
`translateMode` ('replace' | 'below').

marquee.ts: `use:marquee` action — adds `.marquee-on` + `--marquee-dx` when text overflows its clip
box (threshold MIN_OVERFLOW_PX=8). Re-measures on a ResizeObserver WIDTH change. It is gated ONLY by
text overflow + prefers-reduced-motion — NOT by playing/loading state today.

player.svelte.test.ts: `makeFakeAudio()` (line 183) records addEventListener handlers and exposes
`.fire(type)` to synchronously invoke a named listener (e.g. `el.fire('playing')`). `player.attach(el)`
binds it. beforeEach resets current/pendingTrack/loading/error + private session state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defer now-playing/nowbar UI swap until the new song actually plays</name>
  <files>src/lib/stores/player.svelte.ts, src/lib/stores/player.svelte.test.ts, src/lib/components/Nowbar.svelte, src/lib/components/NowPlaying.svelte</files>
  <behavior>
    Add a NEW reactive `displayed = $state&lt;Track | null&gt;(null)` field to the Player class — the
    "track currently DISPLAYED on the now-playing surfaces" — SEPARATE from `current` (which keeps its
    synchronous timing + all queue/MediaSession/playGen/prefetch/watchdog semantics unchanged).

    Test expectations (extend the existing `playing`-listener suite in player.svelte.test.ts, using
    makeFakeAudio + el.fire):
    - Test A (commit point): with a `displayed` track = song A and `current` set to song B (a new
      click mid-playback), firing the `playing` event sets `displayed` to equal `current` (song B).
    - Test B (keep-old on no playing): set `displayed` = A, then drive a `play(B)` so `current` = B
      but DO NOT fire `playing`; assert `displayed` is still A (the old song stays displayed while B
      loads / if B fails).
    - Test C (first play / cold start): with `displayed` null and `current` set, firing `playing`
      sets `displayed` to `current` (the very first song shows once it actually plays — verify the
      now-playing surface is not blank forever; if you choose to seed `displayed` synchronously ONLY
      on a cold start where `displayed` is null, encode THAT rule as the test instead and document it).
    - Test D (restore): `restore()` (the persisted-track resume path) should leave the restored track
      DISPLAYED so a reload shows the last track in the now-bar without requiring a `playing` event
      (restore is a paused resume, not a fresh load-over-old-audio). Assert `displayed` is set by
      restore(). (Decide + document: restore sets `displayed = current` directly.)
  </behavior>
  <action>
    Read the entire play path end-to-end first: `play()` (1721), the `playing`/`play`/`pause`/`error`/
    `ended` listeners in `attach()` (970-1168), `restore()` (361), `reresolveCurrent()` (465), and
    `runFallback()`/`handleTotalFailure()` (2230+). Then implement a DISPLAYED-vs-PENDING split:

    STORE (player.svelte.ts):
    - Add `displayed = $state&lt;Track | null&gt;(null)` near `current` (line 118). Document it: "The track
      shown on the now-playing surfaces (Nowbar + NowPlaying title/artist/cover/lyrics). Advances to
      `current` ONLY on a real `playing` event (the takeover commit) so a new click does not visually
      swap the now-bar while the old audio is still producing output and the new one is still loading.
      `current` keeps its synchronous timing for queue/MediaSession/playGen/prefetch/watchdog."
    - Swap-commit point: in the `playing` listener (line 981-1004, the "ACTUALLY producing output
      (CR-01)" block) set `this.displayed = this.current;` — this is the ONLY place a normal load-over-old
      audio advances the displayed track. Place it alongside the existing D-06 success reset.
    - Cold-start seed (so the FIRST ever play is not blank until `playing`): in `play()` entry, set
      `this.displayed ??= track` ONLY when `displayed` is currently null (no song was ever displayed).
      Do NOT overwrite an existing displayed track on entry — that is the whole point of the defer.
      (Document this exact rule inline.)
    - restore(): set `this.displayed = this.current` after the restored track is assigned to `current`
      (a reload resume must show the restored track immediately — it is not a load-over-old-audio swap).
    - reresolveCurrent() (same-track URL refresh): displayed already equals current for the same song,
      so no change is needed; verify it does not regress (the song identity is unchanged).
    - persist()/serialize: NO new persisted field — `displayed` is session-derived (restore re-seeds it).
    - DO NOT touch: `current` timing, `resolvedCover` timing, playGen, the stall watchdog, loop-guard,
      prefetch, MediaSession writes, queue/auto-advance. Cover for the DISPLAYED track: see UI note
      below — the now-playing cover should track the DISPLAYED song, not jump to the loading song.

    COVER for the displayed song: `resolvedCover` is repointed synchronously to the NEW song in `play()`
    (line 1736), so binding the now-bar/now-playing cover to `resolvedCover` would still swap the cover
    early. To keep the cover with the OLD song until takeover, derive the displayed cover from the
    DISPLAYED track in the components: prefer `player.displayed?.cover`, and only fall back to
    `player.resolvedCover` when `displayed` is null OR `displayed.uid === current?.uid` (i.e. the
    resolvedCover async-land applies to the song actually displayed). Specify this precisely in the
    component edits below and keep it consistent across both surfaces.

    NOWBAR (Nowbar.svelte):
    - Change `np` (line 29) to read the DISPLAYED track: `const np = $derived(player.displayed ?? player.current ?? player.pendingTrack);`
      so title/artist render the displayed (old) song during a load-over-old swap, but a cold start /
      resolve-on-tap stub still shows something.
    - Cover (line 122-124): use the displayed-aware cover described above instead of bare
      `player.resolvedCover ?? np?.cover`.
    - Marquee-as-loading-indicator: the now-bar title/artist do NOT currently use `use:marquee` (only
      NowPlaying's meta does). Per the task, make the running line signal loading. The existing
      `.np-prog indet`/`sliver` already animates while `player.loading` (line 74-82) — that IS the
      now-bar's running-line loader. Confirm it animates across the whole load window (click →
      `playing`); if `loading` is cleared too early for the deferred case, keep `loading` true until the
      `playing` commit OR add the marquee to the now-bar title and force `.marquee-on` while loading.
      Choose the smallest change that makes a visible running line during the load window and document
      which mechanism you used. Keep `resolving` (resolve-on-tap stub) behaviour intact.

    NOWPLAYING (NowPlaying.svelte):
    - Bind the now-playing meta (title/artist), cover, and lyrics to the DISPLAYED track instead of
      `player.current`:
      * `lines` (line 80-82): `player.displayed?.lrc ?? player.current?.lrc` — keep lyrics on the
        displayed song. (Decide the exact fallback; document it. The lyrics MUST stay with the
        displayed/old song until takeover.)
      * title/artist `{#key}` + text (line 1078-1081): read `player.displayed ?? player.current`.
      * `effectiveCover` (line 379) / cover cells: use the displayed-aware cover.
      * The active/transport heart, like, related, enrich effects keyed on `player.current` — leave
        on `current` UNLESS a stale-old-song display would look wrong; the conservative choice is to
        keep transport controls on `current` (they act on the playing song) but display on `displayed`.
        Document each binding you change vs keep.
    - The marquee on the now-playing meta already runs on overflow; no forced-loading change needed
      there unless you want parity with the now-bar (optional, document if done).

    Constraints recap: this is the highest-risk task. The ONLY new store field is `displayed`. The
    ONLY swap-commit point is the `playing` listener. Do NOT regress any documented invariant
    (D-06/CR-01/D-13/D-14/playGen/stall-watchdog/loop-guard/prefetch/MediaSession/auto-advance).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && npx vitest run src/lib/stores/player.svelte.test.ts</automated>
    <human-check>Run `npm run dev` (strictPort 4321). Start song A. While A plays, tap song B in a
    list: the now-bar AND now-playing title/artist/cover/lyrics must STAY on A and the running-line
    loader must animate until B audibly starts, at which point everything swaps to B. Tap a known-bad
    song: A's info stays, music stops, no crash, auto-advance/skip still works.</human-check>
  </verify>
  <done>
    `displayed` field added; `playing` listener commits `displayed = current`; cold-start seed +
    restore set `displayed`; Nowbar + NowPlaying read the displayed track for title/artist/cover/lyrics;
    a running line animates during the load window; all existing player.svelte.test.ts tests pass and
    the new defer tests (A-D) pass; `npx svelte-check` shows no new type errors in the edited files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix lyric over-scroll / off-centre when "Hide parenthesised lines" is ON</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>
    ROOT CAUSE (confirmed): `activeLine` (= `activeIndexAndTime.idx`, line 103) is an index into the
    FULL `lines` array (the loop at line 89-101 walks all of `lines`). But the rendered `<p>` list is
    FILTERED at line 1199 (`{#if !(l.fromParen && settings.lyricsHideParenLines)}`), so when
    `lyricsHideParenLines` is ON the rendered <p> count is SMALLER than `lines.length`. The scroll
    anchor `$effect` (line 195) does `lyricsEl.querySelectorAll('p')[idx]` with the FULL-array `idx`,
    so it selects the wrong (or out-of-range / undefined) `<p>` → over-scroll / off-centre.

    FIX (give each rendered <p> a stable data-index and select by THAT, so the index spaces match):
    - In the render loop (line 1198-1220), add `data-i={i}` to the `<p>` element (line 1211). `i` is the
      FULL-`lines` index of that line — the same index space `activeLine`/`activeIndexAndTime.idx` use.
    - In the anchor `$effect` (line 195), replace `lyricsEl.querySelectorAll('p')[idx]` with a
      data-index lookup: `lyricsEl.querySelector(\`p[data-i="\${idx}"\`)` (the active line's full-array
      index). When `lyricsHideParenLines` is ON and the active line happens to be a hidden paren line
      (no rendered <p> for that idx), fall back to the nearest rendered line at-or-before idx so the
      view still anchors sanely (e.g. query `p[data-i]` whose numeric data-i is the largest ≤ idx, or
      simply the previous visible line). Document the fallback you choose.
    - Do NOT change `activeIndexAndTime` / `activeLine` / `activeTime` themselves (they are also used by
      the active-class match at line 1211 via `activeTime`, which is timestamp-based and already
      filter-agnostic — leave that path alone).
    - Keep the visible-band centring math (line 212-223: visTop/visBottom/visHeight, the closed
      TOP_PAD top-pin, the half/full centre, the `spacerH` end-spacer) BYTE-UNCHANGED — only the
      element-selection line changes from positional `[idx]` to data-index lookup.

    Verify the active line centres with paren-hide ON and OFF, in closed / half / full sheet modes.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && npx svelte-check --threshold error 2>&1 | grep -F 'NowPlaying.svelte' || echo "no NowPlaying errors"</automated>
    <human-check>`npm run dev` (4321). Open a song with CN lyrics that carry parenthesised
    second-language clauses. Settings → enable "Hide parenthesised lines". Open Now Playing → Lyrics.
    As the song plays, the active (bold) line must stay vertically CENTRED (half/full) or top-pinned
    (closed) with NO over-scroll past the active line. Toggle the setting OFF — centring still correct.
    Repeat in closed, half, and full sheet states.</human-check>
  </verify>
  <done>
    Each rendered lyric `<p>` carries `data-i={i}`; the anchor `$effect` selects the active line by
    `data-i` (full-array index) with a documented fallback when the exact idx is a hidden paren line;
    the visible-band centring math is unchanged; active line centres correctly with paren-hide ON and
    OFF in all three sheet modes; no new svelte-check errors in NowPlaying.svelte.
  </done>
</task>

<task type="auto">
  <name>Task 3: Highlight all translated lines of the current active line</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>
    Today the active highlight (`.active` class) is applied to the `<p>` at line 1211 via
    `class:active={l.time === activeTime && activeTime >= 0}` — this correctly marks ALL sibling lines
    sharing `activeTime` (original + split paren clauses, which are separate `<p>`s) as active. The GAP
    is the per-line TRANSLATION text rendered INSIDE an active line:
    - In `below` mode (line 1216): `<span class="tr">{translated[i]}</span>` — this `.tr` span is a
      CHILD of the active `<p>`, but `.lyrics p.active` (line 1468) only restyles the `<p>`'s own
      color/weight; `.lyrics .tr` (line 1472) pins its own muted color/weight, so the translation does
      NOT visually highlight when its parent line is active.
    - In `replace` mode (line 1213): the translation REPLACES the line text inside the `<p>`, so it
      already inherits `.active` — verify, and only adjust if needed.

    FIX (make the active line's translation receive the active style too):
    - Give the `.tr` span an active modifier when its parent line is active. Cleanest: add
      `class:active={l.time === activeTime && activeTime >= 0}` to the `<span class="tr">` at line 1216
      (mirror the exact same condition the parent `<p>` uses), then add a CSS rule
      `.lyrics .tr.active { color: var(--color-text); font-weight: 700; }` (or `opacity:1` to match the
      active-line emphasis) so an active translation reads as highlighted, consistent with the original
      active line at line 1468. Use existing design tokens (no new hardcoded colors).
    - Because split paren clauses are their OWN `<p>` lines that already match `activeTime`, their text
      is already highlighted via `p.active`; this change specifically fixes the `below`-mode `.tr`
      child translation. Confirm `replace` mode already highlights (the translated text is the `<p>`
      body) and leave it unless it visibly fails.
    - Apply to ALL active lines: the condition is the same `activeTime` match used for the parent, so
      every line of the current moment (and its translation span) highlights together.

    Read the render template region (line 1194-1221) + the lyrics CSS (line 1466-1472) before editing.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && npx svelte-check --threshold error 2>&1 | grep -F 'NowPlaying.svelte' || echo "no NowPlaying errors"</automated>
    <human-check>`npm run dev` (4321). Open a song with translatable lyrics, set Lyrics translation
    language to a target that differs from the lyrics, and use translateMode = 'below'. Open Now
    Playing → Lyrics. As the song plays, the active line's TRANSLATION text under it must highlight
    (brighter / bold) in lockstep with the active original line, and un-highlight when the moment
    passes. Switch translateMode to 'replace' and confirm the active translated line is highlighted
    too. If a moment has multiple active lines (original + clause), all their translations highlight.</human-check>
  </verify>
  <done>
    The `below`-mode `.tr` translation span gets an `active` class when its parent line matches
    `activeTime`, with a matching `.lyrics .tr.active` CSS rule using existing tokens; `replace` mode
    confirmed highlighted; all translation lines of the current active moment highlight consistently
    with the original; no new svelte-check errors in NowPlaying.svelte.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/stores/player.svelte.test.ts` passes (all existing + the 4 new Task-1 tests).
- `npx svelte-check --threshold error` introduces no NEW errors in the three edited files.
- Manual (dev server, strictPort 4321): the three human-check scenarios above behave as described.
- No regression to documented player invariants (D-06/CR-01/D-13/D-14, stall watchdog, loop-guard,
  prefetch, MediaSession, auto-advance) — the only new store field is `displayed`, the only swap-commit
  is the `playing` listener.
</verification>

<success_criteria>
- Task 1: new song's now-playing/nowbar info (title/artist/cover/lyrics) defers to the real `playing`
  event; a running line animates during the load window; failure keeps the old info; all player tests pass.
- Task 2: active lyric line centres correctly with "Hide parenthesised lines" ON and OFF in closed/half/full.
- Task 3: all translated lines of the active moment are highlighted consistently with the original line.
- Three atomic commits (one per task), each scoped to that task's files.
</success_criteria>

<output>
Create `.planning/quick/260618-t7p-defer-now-playing-nowbar-ui-update-until/260618-t7p-SUMMARY.md` when done.
</output>
