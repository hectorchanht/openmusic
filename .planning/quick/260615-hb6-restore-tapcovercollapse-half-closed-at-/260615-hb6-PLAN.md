---
quick_id: 260615-hb6
slug: restore-tapcovercollapse-half-closed-at-half-open
date: 2026-06-15
status: complete
files: [src/lib/components/NowPlaying.svelte]
---

# Quick 260615-hb6: Restore tapCoverCollapse half→closed at half-open

## Objective

Reverse the half-open no-op shipped in 260615-gww. A tap on the cover at the
**half-open** sheet state should again collapse the sheet (half → closed), while
keeping the gww **closed → play/pause** behavior. `full` stays a no-op.

Net cover-tap behavior:
- `closed` → `player.toggle()` (play/pause) — unchanged from gww.
- `half`   → `sheetState = 'closed'` — RESTORED (was no-op after gww).
- `full`   → no-op — unchanged.

## Task 1 — restore the half branch in tapCoverCollapse

- **file:** src/lib/components/NowPlaying.svelte
- **action:** In `tapCoverCollapse()`, add `else if (sheetState === 'half') sheetState = 'closed';`
  after the `closed → player.toggle()` line; keep `full` as a no-op. Update the comment block.
  `tapCoverKey` inherits via delegation (no change). No pointer/capture/movement-guard changes —
  sub-slop-tap-reaches-onclick invariant preserved; swipe/drag/grip machine + coverSwipe.ts untouched.
- **verify:** `npm run check` 0 errors; `npx vitest --run src/lib/actions/coverSwipe` green.
- **done:** half-open cover tap collapses to closed; closed tap still toggles play/pause.

## Note
Trivial one-branch change — executed inline by the orchestrator (no subagent), with the standard
quick-task guarantees preserved (atomic commit, PLAN + SUMMARY, STATE.md row).
