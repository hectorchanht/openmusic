---
phase: quick-260630-fu2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/en.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/zh-Hant.ts
autonomous: true
requirements: [QUICK-260630-fu2]
must_haves:
  truths:
    - "All string literals in en.ts, zh-Hans.ts, zh-Hant.ts use double-quote delimiters"
    - "Every exported dictionary value is byte-identical to its pre-edit value (only delimiters changed)"
    - "The i18n parity test and full vitest suite stay green"
    - "svelte-check reports 0 errors (no broken/over-escaped literals)"
  artifacts:
    - path: "src/lib/i18n/en.ts"
      provides: "English dict, double-quoted, values unchanged"
      contains: 'export default en'
    - path: "src/lib/i18n/zh-Hans.ts"
      provides: "Simplified Chinese dict, double-quoted, values unchanged"
      contains: 'export default zhHans'
    - path: "src/lib/i18n/zh-Hant.ts"
      provides: "Traditional Chinese dict, double-quoted, values unchanged"
      contains: 'export default zhHant'
  key_links:
    - from: "src/lib/i18n/zh-Hans.ts"
      to: "./index"
      via: 'import type { Dict }'
      pattern: 'import type \{ Dict \} from "\./index"'
---

<objective>
Normalize the three remaining single-quoted i18n locale dictionaries (`en.ts`, `zh-Hans.ts`, `zh-Hant.ts`) to use double-quote string delimiters, matching the 12 already-double-quoted locales (e.g. `de.ts`). This is a pure delimiter change: every exported KEY and VALUE must be BYTE-IDENTICAL before and after — only the surrounding quote character changes.

Purpose: Satisfy the directive "all i18n files should use double quotes" with zero behavioral risk. Translations are user-visible; a single corrupted value is a visible regression, so the plan enforces a value-equality gate that proves the dicts are unchanged.

Output: Three files converted to double quotes, proven value-preserving by an automated `toEqual` gate plus the existing parity test, the full suite, and `svelte-check`. One atomic, code-only commit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260630-fu2-normalize-i18n-locale-files-to-double-qu

<facts>
Verified during planning (do not re-derive):
- File sizes: en.ts = 398 lines, zh-Hans.ts = 386 lines, zh-Hant.ts = 386 lines.
- Package manager is **pnpm** (pnpm-lock.yaml). Scripts: `check` = `svelte-kit sync && svelte-check --tsconfig ./tsconfig.json`; `test` = `vitest --run`.
- NO prettier / eslint config exists (`.prettierrc*`, `prettier.config.*`, `.eslintrc*`, `eslint.config.*` all absent). `pnpm check` is svelte-check / types only — it will NOT re-flip quotes. Double quotes will stick.
- Each of the 3 files has exactly ONE backtick, and it is inside a leading `//` comment (en.ts line 2: "selecting `en`"; both zh files line 2: "the `Dict` type"). These are NOT template literals — leave comments and backticks untouched.
- en.ts already contains 14 `"` characters: they are VALUES that hold an apostrophe and are therefore already double-quoted while their KEY is single-quoted on the same line. Examples to preserve verbatim:
    'offline.indicator': "You're offline",
    'offline.title': "You're offline",
    'offline.body': 'This needs a connection. Your downloaded songs still play.',
    'home.unplayable': "Couldn't find a playable version",
  After conversion these become e.g. `"offline.indicator": "You're offline",` — the key flips to double quotes; the already-double-quoted value with its bare apostrophe stays exactly as-is.
- en.ts has exactly 2 single-quoted VALUES that contain an escaped apostrophe `\'` (the ONLY `\'` occurrences in any of the 3 files):
    line 370 'settings.appLanguageDesc': '...the app\'s buttons...'
    line 372 'settings.accentColorDesc': '...the track that\'s playing...'
  In double quotes these MUST drop the backslash: `\'` → `'`. The resulting value text ("...the app's buttons...") is byte-identical to the original logical value.
- zh-Hans.ts and zh-Hant.ts have ZERO `\'` and their existing 6 `"` each are values-with-apostrophe-style entries; same rule applies (flip the key delimiter, leave already-double values alone).
- No value in any of the 3 files currently contains a literal `"` character inside a single-quoted string (the 14/6/6 `"` counts are all already-double-quoted values). IF the executor's method encounters a single-quoted value containing a bare `"`, it MUST escape it to `\"`. Verify this assumption holds after conversion via the gate below.
- Bottom-of-file exports (must remain unchanged): en.ts → `export default en;` (with `} as const;` above it); zh-Hans.ts → `const zhHans: Dict = { ... }; export default zhHans;`; zh-Hant.ts → `const zhHant: Dict = { ... }; export default zhHant;`.
- The `import type { Dict } from './index';` specifier in the two zh files is itself single-quoted and IS in scope — convert `'./index'` → `"./index"`.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert en.ts, zh-Hans.ts, zh-Hant.ts to double quotes and prove values unchanged</name>
  <files>src/lib/i18n/en.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/zh-Hant.ts</files>

  <action>
Convert every single-quoted string literal in the three files to double-quoted, changing ONLY the delimiter and necessary escaping. This includes object keys, string values, AND the `import type { Dict } from './index'` specifier in the two zh files (`'./index'` → `"./index"`). Do NOT add, remove, reword, reorder, or retype any key or value. Do NOT touch any other file (index.ts, detect.ts, *.test.ts, etc.). Method is your choice (careful hand-edit, or a comment-aware sed/node script) — but it MUST pass the value-equality gate in <verify>; do not trust the method blindly.

Escape rules (apply exactly):
- An escaped apostrophe inside a single-quoted value (`\'`) becomes a plain apostrophe in the double-quoted form: `\'` → `'`. This affects exactly two en.ts values (settings.appLanguageDesc, settings.accentColorDesc) and no others.
- A bare `"` inside a (former) single-quoted value must be escaped to `\"`. Per planning, no such case currently exists; if your method surfaces one, escape it — the gate will confirm.
- Values that are ALREADY double-quoted (en.ts has 14, each zh file has 6 — they contain bare apostrophes like "You're offline") must be left BYTE-IDENTICAL: only the single-quoted KEY on the same line flips to double quotes.

Leave comments and the single backtick per file untouched: do not let the transform treat `//` comment text as code. Comment-internal apostrophes/backticks must not break parsing (cosmetic comment-quote flips are acceptable but value text must never change).

VALUE-EQUALITY GATE (mandatory — run, see GREEN, then clean up; none of these temp artifacts may be committed):
1. For each of the three files, capture the PRE-EDIT exported dictionary as a sibling temp module so its relative `./index` import still resolves:
   - `git show HEAD:src/lib/i18n/en.ts > src/lib/i18n/__before_en.ts`
   - `git show HEAD:src/lib/i18n/zh-Hans.ts > src/lib/i18n/__before_zhHans.ts`
   - `git show HEAD:src/lib/i18n/zh-Hant.ts > src/lib/i18n/__before_zhHant.ts`
   (These are siblings of the originals, so their `import type { Dict } from './index'` and `export default` resolve identically.)
2. Write a throwaway vitest at `src/lib/i18n/__quote_gate.test.ts` that imports the three NEW (edited, post-conversion) default exports and the three `__before_*` default exports, and asserts `expect(newDict).toEqual(oldDict)` for all three. Because `toEqual` is a deep value comparison, ANY changed character in ANY key or value (and any added/dropped/mis-escaped key) fails the gate.
3. Run `npx vitest --run src/lib/i18n/__quote_gate.test.ts` — it MUST pass GREEN. If RED, fix the conversion (not the test) until the dicts are deeply equal.
4. DELETE all four temp artifacts: `src/lib/i18n/__before_en.ts`, `src/lib/i18n/__before_zhHans.ts`, `src/lib/i18n/__before_zhHant.ts`, `src/lib/i18n/__quote_gate.test.ts`. Confirm they are gone (`git status --short src/lib/i18n/` shows only the three intended files modified, plus the pre-existing untracked .planning entry — and NO `__before_*` / `__quote_gate*`).

Then run the standing checks (full <verify> below). Finally commit CODE ONLY with explicit paths — never `git add -A`, never stage `.planning/HANDOFF.json`:
  git add src/lib/i18n/en.ts src/lib/i18n/zh-Hans.ts src/lib/i18n/zh-Hant.ts
  git commit -m "refactor(quick-260630-fu2-01): normalize en/zh-Hans/zh-Hant i18n dicts to double quotes"
(Append the GSD co-author trailer to the commit message.)
  </action>

  <verify>
    <automated>git show HEAD:src/lib/i18n/en.ts > src/lib/i18n/__before_en.ts && git show HEAD:src/lib/i18n/zh-Hans.ts > src/lib/i18n/__before_zhHans.ts && git show HEAD:src/lib/i18n/zh-Hant.ts > src/lib/i18n/__before_zhHant.ts && npx vitest --run src/lib/i18n/__quote_gate.test.ts</automated>
    <automated>npx vitest --run src/lib/i18n/i18n.test.ts</automated>
    <automated>npx vitest --run</automated>
    <automated>pnpm check 2>&1 | grep -E '0 errors|svelte-check found 0' || pnpm check</automated>
    <automated>test -z "$(ls src/lib/i18n/__before_*.ts src/lib/i18n/__quote_gate.test.ts 2>/dev/null)" && echo "temp artifacts removed"</automated>
    <automated>git diff --staged --name-only | sort | tr '\n' ' '; echo; git diff HEAD -- src/lib/i18n/en.ts src/lib/i18n/zh-Hans.ts src/lib/i18n/zh-Hant.ts | grep -E '^[+-]' | grep -vE "^[+-]{3} " | grep -Ev "['\"]" | grep -v '^[+-]\s*//' || echo "diff lines all touch quotes only"</automated>
  </verify>

  <done>
- en.ts, zh-Hans.ts, zh-Hant.ts contain no single-quoted string literals or import specifiers; all string delimiters are double quotes (the only single-quote characters remaining are bare apostrophes INSIDE double-quoted values, e.g. You're, and inside `//` comments).
- The `__quote_gate.test.ts` `toEqual` gate passed GREEN against the pre-edit dicts, proving all three dictionaries are value-identical to HEAD.
- `npx vitest --run src/lib/i18n/i18n.test.ts` and `npx vitest --run` (full suite) are GREEN.
- `pnpm check` reports 0 errors.
- All four temp artifacts (`__before_en.ts`, `__before_zhHans.ts`, `__before_zhHant.ts`, `__quote_gate.test.ts`) are deleted and NOT committed.
- A single atomic commit stages ONLY the three i18n files; `.planning/HANDOFF.json` and the `.planning/quick/...` directory are NOT staged.
- `git diff` for the three files shows only quote-delimiter changes (plus at most cosmetic comment-quote flips and the two `\'`→`'` unescapes) — no changed value text.
  </done>
</task>

</tasks>

<verification>
- Value-equality gate (`toEqual` against `git show HEAD:` snapshots) proves the three dictionaries are byte-equivalent in value to their pre-edit form.
- Existing i18n parity test (`i18n.test.ts`) confirms all 15 locales still expose an identical key set and no blank values.
- Full `vitest --run` suite green confirms nothing else regressed.
- `pnpm check` (svelte-check) green confirms no broken/over-escaped TS string literals.
- `git diff` inspection confirms only delimiter (and the two known `\'`→`'`) changes.
</verification>

<success_criteria>
- All three locale files use double-quote delimiters exclusively for strings and import specifiers.
- Zero value changes (proven by the `toEqual` gate; visually confirmed by `git diff`).
- Parity test + full suite + `pnpm check` all green.
- One atomic, code-only commit (three i18n files), no temp artifacts, no `.planning` files staged.
</success_criteria>

<output>
Create `.planning/quick/260630-fu2-normalize-i18n-locale-files-to-double-qu/260630-fu2-SUMMARY.md` when done.
</output>
