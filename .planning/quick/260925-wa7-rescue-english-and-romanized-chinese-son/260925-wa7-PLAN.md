---
phase: quick-260925-wa7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/proxy/ytmusic-innertube.ts
  - src/lib/proxy/ytmusic.ts
  - src/lib/proxy/ytmusic.test.ts
  - src/routes/api/ytmusic/search/+server.ts
  - src/lib/services/match-key.ts
  - src/lib/sources/ytmusic.ts
  - src/lib/services/name-rescue.ts
  - src/lib/services/name-rescue.test.ts
  - src/lib/services/discovery.ts
  - src/lib/services/discovery.test.ts
autonomous: true
requirements: [quick-260925-wa7]

must_haves:
  truths:
    - "resolveStub('Eric Chou', 'Graduation (\"Mom, Don't Do That!\" TV Series Theme Song)') whose first searchAll returns only 'Graduation / NCT DREAM' (a WEAK best) runs the English→Chinese lookup, re-searches once with the Simplified Chinese names ('周兴哲 最后一堂课') and returns that strong CN row"
    - "resolveStub('Jay Chou', 'Coral Sea') whose first searchAll returns '珊瑚海(钢琴曲) / 酷客音乐' is rescued the same way; the lookup's YTM stage rejects (en title 珊瑚海 carries no English) and the iTunes HK↔US cross-store stage accepts (US 'Coral Sea (feat. Lara Veronin)' / Jay Chou ↔ HK '珊瑚海 (feat. 梁心頤)' / 周杰倫)"
    - "A normal English hit ('someone like u / lullaboy', 'Good In Me / Bones & The Boy') costs EXACTLY ONE searchAll call and ZERO lookups (pinned by call-count tests); a Japanese/Korean/Chinese-script query never enters the rescue (kana/hangul/Han gate); the existing t2s rescue path is byte-identical"
    - "The rescue never makes a resolve worse: lookup miss → today's result (best weak match or null); re-search row that does not strongly match the Chinese names → today's result"
    - "Lookup outcomes (hit 30 d / miss 1 d) are cached in localStorage under openmusic:name-rescue:v1 keyed by matchKey(artist,title), so a re-tap costs zero lookups; the store is shape-guarded, size-capped, try/catch, node-safe"
    - "GET /api/ytmusic/search?q=…&hl=zh-TW posts InnerTube context hl=zh-TW gl=TW; any other hl (absent, 'fr', 'zh-TW;x') posts en/US — the allowlist is pure and unit-tested; no raw hl ever reaches upstream"
    - "itunes.apple.com is called ONLY from the browser (never from the edge — spike 012 rate-limits the shared Workers egress IP), through the apiFetch governor (apiUrl passes absolute https URLs through, 32-D-13), with a bounded timeout"
    - "pnpm test, pnpm check and pnpm build are green after EVERY task commit (main auto-pushes → auto-deploys)"
  artifacts:
    - path: "src/lib/proxy/ytmusic-innertube.ts"
      provides: "innerTubeLocale(hl) allowlist → {hl,gl} (en/US default, zh-TW/TW)"
      contains: "innerTubeLocale"
    - path: "src/lib/proxy/ytmusic.ts"
      provides: "searchInnerTube(query, params, signal?, locale?) building the client context with the locale"
      contains: "locale"
    - path: "src/routes/api/ytmusic/search/+server.ts"
      provides: "reads ?hl=, allowlists via innerTubeLocale, passes it to both filter POSTs"
      contains: "innerTubeLocale"
    - path: "src/lib/services/name-rescue.ts"
      provides: "PURE predicates (isStrongMatch, cleanTitle, pairYtmRows, pairItunes), the bounded localStorage cache, and the never-throw lookupChineseName orchestrator"
      exports: ["isStrongMatch", "cleanTitle", "pairYtmRows", "pairItunes", "lookupChineseName", "NAME_RESCUE_KEY"]
      min_lines: 150
    - path: "src/lib/services/name-rescue.test.ts"
      provides: "fixture tests from the 2026-09-26 live probes (YTM en↔zh-TW pairs, iTunes HK/US pairs, weak/strong examples, cache TTL/cap, orchestrator stage order + never-throw)"
      min_lines: 150
    - path: "src/lib/services/discovery.ts"
      provides: "resolveStub Latin-query weak-match gate → lookupChineseName → one Simplified re-attempt → strong-or-original"
      contains: "lookupChineseName"
    - path: "src/lib/services/discovery.test.ts"
      provides: "exact searchAll + lookup call-count assertions for every rescue branch (quick-260925-wa7 describe block)"
      contains: "quick-260925-wa7"
    - path: "src/lib/services/match-key.ts"
      provides: "norm() exported (no behaviour change) so name-rescue reuses the ONE normalizer"
      contains: "export function norm"
    - path: "src/lib/sources/ytmusic.ts"
      provides: "parseSearchEnvelope exported (no behaviour change) so the rescue reuses the ONE InnerTube row parser"
      contains: "export function parseSearchEnvelope"
  key_links:
    - from: "src/lib/services/discovery.ts"
      to: "src/lib/services/name-rescue.ts"
      via: "resolveStub calls lookupChineseName only when the query is CJK-free AND the first attempt is null/weak"
      pattern: "lookupChineseName\\("
    - from: "src/lib/services/name-rescue.ts"
      to: "/api/ytmusic/search?q=…&hl=en|zh-TW"
      via: "two parallel apiFetch GETs joined by videoId via parseSearchEnvelope"
      pattern: "hl=zh-TW"
    - from: "src/lib/services/name-rescue.ts"
      to: "https://itunes.apple.com/search + /lookup (country=hk|us)"
      via: "apiFetch (absolute URL passthrough 32-D-13) + combinedSignal timeout; client-side only"
      pattern: "itunes\\.apple\\.com/(search|lookup)"
    - from: "src/routes/api/ytmusic/search/+server.ts"
      to: "src/lib/proxy/ytmusic.ts searchInnerTube"
      via: "locale from innerTubeLocale(url.searchParams.get('hl')) passed as 4th arg to BOTH filter POSTs"
      pattern: "searchInnerTube\\(q, (SONGS|VIDEOS)_FILTER, undefined, locale\\)"
    - from: "src/lib/services/name-rescue.ts"
      to: "src/lib/services/match-key.ts"
      via: "norm() is the ONLY normalizer used for title/artist containment (matchKey for the cache key)"
      pattern: "import \\{ matchKey, norm \\} from '\\$lib/services/match-key'"
---

<objective>
Rescue English / romanized names of Chinese songs inside the SHARED name resolver `resolveStub`
(`src/lib/services/discovery.ts`): when a CJK-free {artist, title} query resolves to nothing or to a
WEAK best candidate, look up the Chinese names (YouTube Music edge search hl=en + hl=zh-TW joined by
videoId, then client-side iTunes HK↔US cross-store by trackId — first VERIFIED hit wins), re-search once
with the Simplified Chinese names, and return that row only if it strongly matches. Same insertion point
and reasoning as the existing t2s rescue-on-miss (quick-260808-urx): one fix here covers all ~15 call
sites (playStub taps, radio, share pages, long-press menus, album batch resolve, DownloadControl,
cross-source fallback).

Purpose: "Coral Sea — Jay Chou" currently plays 珊瑚海(钢琴曲)/酷客音乐 (or a DJ remix); "Graduation —
Eric Chou" plays NCT DREAM. The CN catalogs index the Chinese name; the shelves/radio/charts carry the
label's English name. A good first resolve must keep costing ZERO extra calls (user constraint).

Output: `hl` allowlist on the ytmusic edge search; a pure `name-rescue.ts` service (+ fixture tests);
`resolveStub` wired with call-count-pinned tests. No new dependency, no new env var, no new route.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/quick/260925-wa7-rescue-english-and-romanized-chinese-son/260925-wa7-CONTEXT.md
@./CLAUDE.md
@src/lib/services/discovery.ts
@src/lib/services/discovery.test.ts
@src/lib/services/match-key.ts
@src/lib/services/zh-convert.ts
@src/lib/proxy/ytmusic-innertube.ts
@src/lib/proxy/ytmusic.ts
@src/lib/proxy/ytmusic.test.ts
@src/routes/api/ytmusic/search/+server.ts
@src/lib/sources/ytmusic.ts
@src/lib/services/itunes-cover.ts
@src/lib/services/api-base.ts
@src/lib/services/abort-signal.ts
@src/lib/services/cover-cache.test.ts

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From src/lib/proxy/ytmusic-innertube.ts (pure, client-importable, MUST stay import-free):
```typescript
export interface InnerTubeContext { client: { clientName: string; clientVersion: string; hl: string; gl: string; visitorData?: string; androidSdkVersion?: number; deviceModel?: string } }
export const WEB_REMIX_CONTEXT: InnerTubeContext = { client: { clientName: 'WEB_REMIX', clientVersion: '1.20240101.01.00', hl: 'en', gl: 'US' } };
```

From src/lib/proxy/ytmusic.ts (EDGE only — fetchWithRetry, never apiFetch; `export * from './ytmusic-innertube'`):
```typescript
export const SONGS_FILTER = 'EgWKAQIIAWoKEAkQBRAKEAMQBA%3D%3D';
export const VIDEOS_FILTER = 'EgWKAQIQAWoKEAkQChAFEAMQBBAV';
export async function innerTubePost(url: string, body: unknown, opts: InnerTubePostOptions = {}): Promise<unknown>;
export async function searchInnerTube(query: string, params: string, signal?: AbortSignal): Promise<unknown>;
// body today: { context: WEB_REMIX_CONTEXT, query, params }
```

From src/routes/api/ytmusic/search/+server.ts (only GET/OPTIONS exports allowed in a +server.ts):
```typescript
// const q = (url.searchParams.get('q') ?? '').trim();
// const cache = edgeCache(); const cacheReq = ownOriginCacheKey(url);   // key = FULL own-origin URL incl. query string → ?hl= variants never collide
// const [songs, videos] = await Promise.allSettled([searchInnerTube(q, SONGS_FILTER), searchInnerTube(q, VIDEOS_FILTER)]);
// returns { ytmusicMerged: [songsJson, videosJson] } (both) or EMPTY_SEARCH_ENVELOPE
```

From src/lib/sources/ytmusic.ts (client adapter; parser is currently module-private):
```typescript
function parseSearchEnvelope(json: unknown, keyword: string): Track[]; // walks every musicShelfRenderer, rowToTrack: title = flexColumns[0] first run, artist via pageType MUSIC_PAGE_TYPE_ARTIST, songid = videoId; dedupes by videoId; THROWS 'ytmusic: contract-drift' when no shelf
// search(): const path = '/api/ytmusic/search?q=' + encodeURIComponent(keyword); apiFetch(path, { signal })
```

From src/lib/services/match-key.ts (norm is currently module-private):
```typescript
function norm(s: string): string; // lowercase → drop (…)/[…]/【…】 → drop " - remaster|live|acoustic|explicit|feat.|ft. …" suffix → strip ALL non-letter/digit (keeps CJK+latin+digits)
export function matchKey(artist: string, title: string): string; // `${norm(artist)}|${norm(title)}`
```

From src/lib/i18n/detect.ts:
```typescript
export type LangTag = 'en' | 'zh-Hant' | 'zh-Hans' | 'ja' | 'ko';
export function detectLang(text: string): LangTag; // kana→'ja', hangul→'ko', Han→'zh-*', else 'en'  ⇒  detectLang(x) === 'en' ⇔ x carries NO kana/hangul/Han
```

From src/lib/services/zh-convert.ts:
```typescript
export function isChineseLine(text: string): boolean;                 // zh-Hant | zh-Hans only
export async function t2sConvertLines(lines: string[]): Promise<string[]>; // Traditional→Simplified, positional, never throws (identity fallback), lazy memoized dict
export function t2sConvertLineSync(text: string): string | null;      // null until the t2s dict is warm (it IS warm right after an awaited t2sConvertLines)
```

From src/lib/services/api-base.ts:
```typescript
export function apiUrl(path: string): string;  // 32-D-13: an absolute http(s):// url is returned UNCHANGED on both builds
export function apiFetch(path: string, init?: RequestInit): Promise<Response>; // GET dedupe + MAX_CONCURRENT_REQUESTS=8 + 25 s timeout + circuit breaker; rejects with AbortError on caller abort
```

From src/lib/services/abort-signal.ts:
```typescript
export function combinedSignal(timeoutMs: number, caller?: AbortSignal): AbortSignal;
```

From src/lib/services/discovery.ts (today):
```typescript
async function attempt(artist: string, title: string): Promise<Track | null>; // searchAll(`${artist} ${title}`, 1) → dedupeBest → scoreMatch stable-max; throws only what searchAll throws
export async function resolveStub(artist: string, title: string): Promise<Track | null>; // single try/catch; hit → return; !isChineseLine both → null; t2s → identity gate → attempt(a2,t2)
```

Test conventions (from discovery.test.ts / ytmusic.test.ts / cover-cache.test.ts):
```typescript
vi.spyOn(catalog, 'searchAll').mockResolvedValueOnce(result([...]));   // ESM namespace spy; exact toHaveBeenCalledTimes pins
vi.stubGlobal('fetch', fetchSpy);  afterEach(() => { vi.unstubAllGlobals(); vi.restoreAllMocks(); });
Object.defineProperty(globalThis, 'localStorage', { value: <Map-backed getItem/setItem/removeItem stub>, configurable: true }); // in-memory stub, no jsdom
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allowlisted `hl` locale on the ytmusic edge search (en | zh-TW)</name>
  <files>src/lib/proxy/ytmusic-innertube.ts, src/lib/proxy/ytmusic.ts, src/lib/proxy/ytmusic.test.ts, src/routes/api/ytmusic/search/+server.ts</files>
  <behavior>
    - innerTubeLocale('zh-TW') → { hl: 'zh-TW', gl: 'TW' }
    - innerTubeLocale('en') / innerTubeLocale(null) / innerTubeLocale(undefined) / innerTubeLocale('') → { hl: 'en', gl: 'US' }
    - innerTubeLocale('fr'), innerTubeLocale('zh-TW;evil'), innerTubeLocale('ZH-TW'), innerTubeLocale(' zh-TW') → { hl: 'en', gl: 'US' } (exact-match allowlist, no trimming/case-folding — anything not literally in the table is the default)
    - searchInnerTube('q', SONGS_FILTER, undefined, { hl: 'zh-TW', gl: 'TW' }) POSTs a body whose context.client is { clientName: 'WEB_REMIX', clientVersion: WEB_REMIX_CONTEXT.client.clientVersion, hl: 'zh-TW', gl: 'TW' } (stub fetch, JSON.parse the request body)
    - searchInnerTube('q', SONGS_FILTER) (no locale) POSTs context === WEB_REMIX_CONTEXT byte-for-byte (existing callers unchanged)
  </behavior>
  <action>
    Per CONTEXT "Stage 1": add an allowlisted `hl` param to the existing edge YTM search; `gl` FOLLOWS `hl`
    (never a second client-controlled param). Tag every new comment `quick-260925-wa7`.

    1. `src/lib/proxy/ytmusic-innertube.ts` (pure, keep it import-free): add
       `export const INNERTUBE_LOCALES = { en: 'US', 'zh-TW': 'TW' } as const;` and
       `export function innerTubeLocale(hl: string | null | undefined): { hl: string; gl: string }` that
       returns `{ hl, gl: INNERTUBE_LOCALES[hl] }` when `hl` is an OWN key of the table
       (`Object.prototype.hasOwnProperty.call`, so 'constructor'/'__proto__' cannot match) and
       `{ hl: 'en', gl: 'US' }` otherwise. Comment: this is the SSRF/param-tampering allowlist
       (T-wa7-01) — the raw query value is never forwarded; only a table-derived pair is.

    2. `src/lib/proxy/ytmusic.ts`: extend `searchInnerTube(query, params, signal?, locale?)` with an
       optional 4th arg `locale?: { hl: string; gl: string }`. Build the context as
       `locale ? { client: { ...WEB_REMIX_CONTEXT.client, hl: locale.hl, gl: locale.gl } } : WEB_REMIX_CONTEXT`
       so the no-locale path posts the identical object reference as today. Do not touch
       `getVisitorData` (it must keep grabbing an en/US token).

    3. `src/routes/api/ytmusic/search/+server.ts`: after reading `q`, read
       `const locale = innerTubeLocale(url.searchParams.get('hl'));` (import `innerTubeLocale` from
       `$lib/proxy/ytmusic` — it re-exports the pure module) and pass it to BOTH filter POSTs:
       `searchInnerTube(q, SONGS_FILTER, undefined, locale)` and
       `searchInnerTube(q, VIDEOS_FILTER, undefined, locale)`. Nothing else in the route changes: the
       edge cache key is already the full own-origin URL (`ownOriginCacheKey(url)` → `new Request(url)`),
       so `?q=X&hl=zh-TW` and `?q=X` are distinct entries — state that in a one-line comment so nobody
       "fixes" the key later. A `+server.ts` may only export HTTP verbs — the helper lives in the proxy
       module, NOT here (SvelteKit `Invalid export` 500s at request time; unit tests miss it).

    4. `src/lib/proxy/ytmusic.test.ts`: add the behaviors above as a
       `describe('innerTubeLocale + searchInnerTube locale (quick-260925-wa7)')` block using the file's
       existing `vi.stubGlobal('fetch', …)` + `jsonRes` pattern; assert the POST body via
       `JSON.parse(fetchSpy.mock.calls[0][1].body)`.

    Optional live check (sandbox reaches YTM; dev server is 4321 or 5173 — probe, don't assume):
    `curl -s 'http://localhost:4321/api/ytmusic/search?q=Eric%20Chou%20Zai%20Ai%20Ni&hl=zh-TW' | grep -c 周興哲`
    should be ≥ 1, and the same URL with `hl=en` should contain `Zai Ai Ni`.

    Commit as `feat(quick-260925-wa7): allowlisted hl locale on ytmusic edge search`. This commit is
    self-contained and backwards-compatible (no caller passes `hl` yet), so test/check/build stay green.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm vitest --run src/lib/proxy/ytmusic.test.ts && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>innerTubeLocale + the 4-arg searchInnerTube exist and are tested; the route forwards the allowlisted locale to both filter POSTs; existing ytmusic tests untouched. The commit is made ONLY after the full gate `pnpm test && pnpm check && pnpm build` is green (main auto-pushes → auto-deploys).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure name-rescue module — weak/strong predicate, YTM en↔zh-TW pairing, iTunes HK↔US pairing, bounded localStorage cache</name>
  <files>src/lib/services/match-key.ts, src/lib/sources/ytmusic.ts, src/lib/services/name-rescue.ts, src/lib/services/name-rescue.test.ts</files>
  <behavior>
    isStrongMatch(query, cand) — all four examples from CONTEXT must classify exactly:
    - {Eric Chou, Graduation ("Mom, Don't Do That!" TV Series Theme Song)} vs {NCT DREAM, Graduation} → false (title equal, artist mismatch)
    - {Jay Chou, Coral Sea} vs {酷客音乐, 珊瑚海(钢琴曲)} → false; vs {Black Pearl, Coral Sea (Chillout Mix)} → false (artist mismatch)
    - {Eric Chou, Zai Ai Ni} vs {汪苏泷, 左转灯} → false; vs {Eric周兴哲, 怎么了} → false
    - {lullaboy, someone like u} vs {lullaboy, someone like u} → true; {Bones & The Boy, Good In Me} vs {Bones & The Boy, Good In Me} → true
    - {周兴哲, 再爱你} vs {Eric周兴哲, 再爱你} → true (artist containment, ≥3 chars); {周杰伦, 珊瑚海} vs {周杰伦, 珊瑚海 (Live)} → true (norm drops the parenthetical)
    - {Joker Xue, The Actor} vs {Joker Xue, Actor} → true (containment both ways, shorter side ≥ 3); {X, Ok} vs {X, Okay} → false (shorter side < 3 → equality only)
    cleanTitle:
    - '珊瑚海 (feat. 梁心頤)' → '珊瑚海'; '最後一堂課 (《媽,別鬧了!》影集片尾曲)' → '最後一堂課'; '演員(Live)' → '演員'; 'Some Song - EP' → 'Some Song'; '再愛你 - Zai Ai Ni' → '再愛你 - Zai Ai Ni' (a " - <text>" that is not EP/feat is KEPT — the YTM bilingual dash form is data, not decoration); '  Mojito  ' → 'Mojito'
    pairYtmRows(enRows, zhRows, query) (rows are { songid, title, artist }; join on songid):
    - Zai Ai Ni: en [{v1, '再愛你 - Zai Ai Ni', 'Eric Chou'}], zh [{v1, '再愛你', '周興哲'}] → { artist: '周興哲', title: '再愛你' }
    - Graduation: en [{v2, '最後一堂課 (《媽，別鬧了！》影集片尾曲) - Graduation ("Mom, Don\'t Do That!" TV Series Theme Song)', 'Eric Chou'}], zh [{v2, '最後一堂課 (《媽，別鬧了！》影集片尾曲)', '周興哲'}] → { artist: '周興哲', title: '最後一堂課' } (cleanTitle applied)
    - Coral Sea: en [{v3, '珊瑚海', '周杰倫'}], zh [{v3, '珊瑚海', '周杰倫'}] → null (en title carries no English evidence)
    - Mojito: en [{v4, 'Mojito', '周杰倫'}], zh [{v4, 'Mojito', '周杰倫'}] with query {Jay Chou, Mojito} → { artist: '周杰倫', title: 'Mojito' } (title contains query; zh pair has CJK in artist and differs from the query pair)
    - Uploader video with identical Latin names in both locales (no CJK anywhere) → null
    - Row whose en artist ALSO matches the query artist is preferred over an earlier title-only row (two-pass scan); a videoId present in only one locale is ignored
    pairItunes(query, hkSearch, usSearch, hkLookup, usLookup) (rows are { trackId, trackName, artistName }):
    - Coral Sea: hkSearch [{1721454126, '珊瑚海 (feat. 梁心頤)', '周杰倫'}], usLookup [{1721454126, 'Coral Sea (feat. Lara Veronin)', 'Jay Chou'}] → { artist: '周杰倫', title: '珊瑚海' }
    - Graduation: usSearch [{1634754406, 'Graduation ("Mom, Don\'t Do That!" TV Series Theme Song)', 'Eric Chou'}], hkLookup [{1634754406, '最後一堂課 (《媽,別鬧了!》影集片尾曲)', '周興哲'}] → { artist: '周興哲', title: '最後一堂課' }
    - The Actor: usSearch [{9, 'Actor (Live)', 'Joker Xue'}], hkLookup [{9, '演員(Live)', '薛之謙'}] → { artist: '薛之謙', title: '演員' }
    - Zai Ai Ni: usSearch [{5, 'Unbreakable Love', 'Eric Chou'}], hkLookup [{5, '永不失聯的愛', '周興哲'}] → null (US title ≠ query title)
    - Mojito: usSearch [{7, '屋頂', 'Landy Wen & Jay Chou'}], hkLookup [{7, '屋頂', '溫嵐 & 周杰倫'}] → null (title mismatch)
    - trackId that is not a positive integer, or a side missing trackName/artistName → skipped, never throws
    cache (readRescueCache / writeRescueCache, key = matchKey(artist,title), storage key 'openmusic:name-rescue:v1'):
    - write hit → read returns { artist, title }; write miss → read returns 'miss'; unknown key → null
    - hit older than 30 d reads null; miss older than 1 d reads null (read-side TTL, no delete-on-read, Date.now spied)
    - after 501 writes the record holds ≤ 500 entries and the OLDEST write was evicted
    - malformed stored JSON / non-object / entry with non-string or > 200-char names → null, no throw; no localStorage global (node) → read null / write no-op, no throw
  </behavior>
  <action>
    Create `src/lib/services/name-rescue.ts` — PURE `.ts` (no runes, no `$app/*`; node-testable) — with the
    predicates + cache. The async fetch orchestrator is added to this same file in Task 3; this task
    ships only pure code (file is unused by the app until Task 3, so the commit is green). Tag every
    decision comment `quick-260925-wa7`. Two tiny enabling exports first:

    1. `src/lib/services/match-key.ts`: add the `export` keyword to the existing `norm` function (no
       rename, no behaviour change) — name-rescue reuses the ONE normalizer instead of re-inlining
       (CLAUDE.md Shared Primitives rule). Add a one-line comment that it is exported for name-rescue.

    2. `src/lib/sources/ytmusic.ts`: add the `export` keyword to `parseSearchEnvelope` (no behaviour
       change) — the rescue reuses the ONE InnerTube row parser (title = flexColumns[0], artist via
       pageType) instead of a second walker. One-line comment.

    3. `name-rescue.ts` exports (all pure, all synchronous, none throw on malformed input):
       - `export interface ZhName { artist: string; title: string }`
       - `hasCjk(s)` — `detectLang(s) !== 'en'` from `$lib/i18n/detect` (kana/hangul/Han all count;
         reuses the shared classifier rather than a new regex).
       - `componentMatches(a, b)` — `na = norm(a), nb = norm(b)`; true iff both non-empty AND
         (`na === nb` OR (the SHORTER of the two has length ≥ 3 AND one `includes` the other)).
         The ≥ 3 floor is the same order as score-match's `length >= 2` substring guard, raised by one
         so 2-char Latin tokens never containment-match; equality still covers them.
       - `isStrongMatch(query: ZhName, cand: ZhName)` — `componentMatches(query.title, cand.title) &&
         componentMatches(query.artist, cand.artist)`. This is the "weak" predicate's complement:
         `resolveStub` calls the rescue when `!isStrongMatch`. It intentionally does NOT reuse
         scoreMatch (which never thresholds, D-03) — this is a separate, thresholded, artist-AND-title
         check used only to decide whether to spend rescue calls. Add a `ponytail:` comment naming the
         ceiling: containment is one-directional string logic, no fuzzy distance; upgrade to a token
         Jaccard if a real false-weak shows up.
       - `cleanTitle(s)` — strip `(…)` / `（…）` / `[…]` / `【…】` groups, a trailing ` - EP` / ` - Single`,
         and a `feat.`/`ft.` suffix (` - feat. …` or `feat. …` to end), collapse whitespace, trim. Do NOT
         strip a generic ` - <text>` (the YTM bilingual `再愛你 - Zai Ai Ni` is kept for the en-side
         containment test; the zh-TW side has no dash).
       - `pairYtmRows(enRows: YtmRow[], zhRows: YtmRow[], query: ZhName): ZhName | null` where
         `YtmRow = { songid: string; title: string; artist: string }` (structurally satisfied by `Track`).
         Index `zhRows` by songid. Accept rule for an en row R with zh partner Z: (a) English evidence —
         `norm(R.title).includes(norm(query.title))` with `norm(query.title)` non-empty; (b) Z carries
         CJK in title OR artist (`hasCjk`); (c) `matchKey(Z.artist, cleanTitle(Z.title)) !==
         matchKey(query.artist, query.title)` (it gives us something NEW to search). Two passes over
         `enRows` in order: pass 1 additionally requires `componentMatches(R.artist, query.artist)`;
         pass 2 is (a)(b)(c) only (the CONTEXT's Mojito example has a CJK en-artist, so an artist
         requirement cannot be mandatory). Return `{ artist: Z.artist, title: cleanTitle(Z.title) }`.
       - `pairItunes(query: ZhName, hkSearch: ItunesRow[], usSearch: ItunesRow[], hkLookup: ItunesRow[],
         usLookup: ItunesRow[]): ZhName | null` where `ItunesRow = { trackId?: unknown; trackName?: unknown;
         artistName?: unknown }` (untrusted). Build `Map<id, { hk?: {t,a}; us?: {t,a} }>` — HK-store rows
         (hkSearch + hkLookup) fill `.hk`, US-store rows (usSearch + usLookup) fill `.us`; keep only rows
         whose trackId is a positive integer (`Number.isInteger(n) && n > 0`, accept numeric strings)
         and whose names are non-empty strings. Iterate ids in order hkSearch → usSearch; accept the
         first id with BOTH sides where `componentMatches(us.t, query.title) && componentMatches(us.a,
         query.artist)` AND (hk.t or hk.a) `hasCjk` AND `matchKey(hk.a, cleanTitle(hk.t)) !==
         matchKey(query.artist, query.title)`. Return `{ artist: hk.a, title: cleanTitle(hk.t) }`.
       - Cache: `export const NAME_RESCUE_KEY = 'openmusic:name-rescue:v1'`; `HIT_TTL_MS = 30 d`,
         `MISS_TTL_MS = 1 d`, `MAX_ENTRIES = 500`, `MAX_NAME_LEN = 200`. Record shape
         `Record<string, { a: string; t: string; at: number } | { miss: true; at: number }>` keyed by
         `matchKey(artist, title)`. `readRescueCache(artist, title): ZhName | 'miss' | null` — guard
         `typeof localStorage === 'undefined'`, wrap getItem + JSON.parse in try/catch, shape-guard
         every field (strings ≤ MAX_NAME_LEN, finite `at`), read-side TTL (strict `>` → null, no
         delete-on-read — mirrors cover-cache). `writeRescueCache(artist, title, value: ZhName | null)`
         — insert, then if `Object.keys(rec).length > MAX_ENTRIES` evict oldest `at` first (write-time
         LRU, mirrors cover-cache), setItem in try/catch, never throws. A poisoned entry can only
         change the text of a search query sent to our own `/api/*` (no HTML/URL sink), which is why
         shape + length guards suffice (T-wa7-03).

    4. `src/lib/services/name-rescue.test.ts`: every bullet in `<behavior>` as a test, fixtures copied
       VERBATIM from CONTEXT's probe outputs (the strings above). localStorage via the Map-backed
       `Object.defineProperty(globalThis, 'localStorage', …)` stub from cover-cache.test.ts
       (`configurable: true`, restore in afterEach); TTL via `vi.spyOn(Date, 'now')`. One test runs the
       cache with NO localStorage defined and asserts null / no throw.

    Commit as `feat(quick-260925-wa7): pure name-rescue predicates, pairing and cache`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm vitest --run src/lib/services/name-rescue.test.ts src/lib/services/match-key.test.ts src/lib/sources/ytmusic.test.ts && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>name-rescue.ts exports the pure predicates, pairing functions and cache; every CONTEXT fixture (Zai Ai Ni / Graduation / Coral Sea / Mojito / The Actor accept-or-reject, the four weak and two strong examples) passes; cache TTL/cap/malformed/no-storage cases pass; `norm` and `parseSearchEnvelope` are exported with zero behaviour change. The commit is made ONLY after the full gate `pnpm test && pnpm check && pnpm build` is green (main auto-pushes → auto-deploys).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lookupChineseName orchestrator + resolveStub wiring with call-count-pinned tests</name>
  <files>src/lib/services/name-rescue.ts, src/lib/services/discovery.ts, src/lib/services/discovery.test.ts, src/lib/services/name-rescue.test.ts</files>
  <behavior>
    lookupChineseName (name-rescue.test.ts; `vi.spyOn(apiBase, 'apiFetch')` routed by URL):
    - cache hit → returns it, apiFetch called 0 times; cache 'miss' → null, 0 calls
    - stage 1 verified (Zai Ai Ni envelopes for hl=en / hl=zh-TW, minimal musicShelfRenderer rows with playlistItemData.videoId + flexColumns) → returns { 周興哲, 再愛你 }; NO itunes.apple.com call; result written to cache
    - stage 1 unverified (Coral Sea: en title 珊瑚海) → stage 2 runs: 2 search GETs (country=hk, country=us, entity=song, limit=3) then lookup GETs for the ids each found in the OTHER store → returns { 周杰倫, 珊瑚海 }; a store whose search returned no ids gets NO lookup call
    - both stages miss → null AND the miss is cached (second call → 0 apiFetch calls)
    - apiFetch rejects / returns 500 / returns non-JSON on any hop → that hop counts as empty, function still resolves (never throws), overall null is cached as a miss
    - every apiFetch call carries an AbortSignal (combinedSignal timeout) and the ytmusic URLs are exactly '/api/ytmusic/search?q=<enc>&hl=en' and '…&hl=zh-TW'
    resolveStub (discovery.test.ts, new describe 'quick-260925-wa7', `vi.spyOn(nameRescue, 'lookupChineseName')`):
    - strong English hit ('someone like u' / 'lullaboy') → returns it; searchAll 1; lookup 0
    - English zero-result miss + lookup null → null; searchAll 1; lookup 1 (called with the ORIGINAL artist/title)
    - weak English hit (query Eric Chou / Graduation ("Mom, Don't Do That!" TV Series Theme Song), row 'Graduation' / 'NCT DREAM') + lookup null → returns the NCT DREAM row (never worse); searchAll 1; lookup 1
    - weak hit + lookup { 周興哲, 最後一堂課 } + second searchAll returning '最后一堂课' / 'Eric周兴哲' → returns that row; searchAll 2; second call's query is exactly '周兴哲 最后一堂课' (Simplified — t2s applied BEFORE the re-search)
    - weak hit + lookup hit + second searchAll returning an unrelated row ('Aaa' / 'Alpha') → returns the ORIGINAL weak row; searchAll 2
    - weak hit + lookup hit + second searchAll returning [] → returns the original weak row
    - weak hit + lookup hit + second searchAll THROWS → resolves (never throws) to null-or-original (resolveStub's single try/catch; assert `resolves` and no rejection)
    - Japanese miss ('YOASOBI' / '夜に駆ける') → null; searchAll 1; lookup 0 (kana gate)
    - Traditional Chinese miss ('周傑倫' / '止戰之殤') → existing t2s retry exactly as before; lookup 0 (the existing quick-260808-urx tests stay untouched and green)
    - mixed Latin-artist / Chinese-title miss ('Jay Chou' / '珊瑚海') → lookup 0 (CJK present → not a Latin query; t2s path only)
  </behavior>
  <action>
    1. `src/lib/services/name-rescue.ts` — add the never-throw orchestrator
       `export async function lookupChineseName(artist: string, title: string): Promise<ZhName | null>`:
       - `const cached = readRescueCache(artist, title); if (cached === 'miss') return null; if (cached) return cached;`
       - Stage 1 (per CONTEXT): `const q = encodeURIComponent(\`${artist} ${title}\`)`; fire
         `apiFetch('/api/ytmusic/search?q=' + q + '&hl=en', { signal })` and the same with `&hl=zh-TW` in
         parallel (`Promise.all` over two never-throw helpers: `fetchJson(path)` → `res.ok ? res.json()
         : null`, catch → null). Parse each with `parseSearchEnvelope(json, keyword)` from
         `$lib/sources/ytmusic` inside try/catch (it throws on a shelf-less body → treat as `[]`).
         `pairYtmRows(en, zh, { artist, title })` → if non-null, `writeRescueCache` + return.
       - Stage 2 (sequential — CONTEXT discretion: only when stage 1 gives no verified hit): build
         `https://itunes.apple.com/search?term=<artist title>&entity=song&limit=3&country=hk` and
         `…country=us` via `URLSearchParams` (mirror `buildItunesSearchUrl` in itunes-cover.ts but with
         `limit=3` + `country`; do not import it — its limit is pinned to 1). Fetch both in parallel
         through `apiFetch` — `apiUrl` returns an absolute https URL unchanged (32-D-13), so the
         governor (dedupe, concurrency cap, timeout, circuit breaker) applies; the itunes-cover.ts
         comment claiming apiFetch would corrupt the URL predates 32-D-13 — do NOT edit itunes-cover.ts
         here. Collect the positive-integer trackIds from each store's `results`; if HK found ids →
         `apiFetch('https://itunes.apple.com/lookup?id=<hkIds,>&country=us')`; if US found ids →
         `…lookup?id=<usIds,>&country=hk`; run the (0–2) lookups in parallel. `pairItunes(query,
         hkSearch.results, usSearch.results, hkLookup.results, usLookup.results)` → write + return.
         itunes.apple.com is CLIENT-SIDE ONLY (spike 012 — the edge egress IP is rate-limited); this
         module runs in the browser (called from resolveStub), never from `$lib/proxy/*`.
       - Every fetch gets `signal: combinedSignal(LOOKUP_TIMEOUT_MS)` with `LOOKUP_TIMEOUT_MS = 6000`
         (same deadline as itunes-cover.ts) via `$lib/services/abort-signal` (shared primitive — do not
         re-inline). No caller signal is threaded: resolveStub has none today, and adding one would
         touch ~15 call sites for no supersedence gain (playStub re-checks pendingGen after the await).
       - Miss → `writeRescueCache(artist, title, null)`; return null. Whole body in try/catch → null.
         Never throws. Cost ceiling comment: ≤ 2 YTM GETs + ≤ 4 iTunes GETs per COLD rescue, only on a
         weak/miss Latin query, then cached 30 d / 1 d (T-wa7-04).

    2. `src/lib/services/discovery.ts` — wire the gate. Imports: `detectLang` from `$lib/i18n/detect`,
       `t2sConvertLineSync` (add to the existing zh-convert import), `isStrongMatch, lookupChineseName`
       from `$lib/services/name-rescue`. Replace the first three lines of the try block with:
       - `const hit = await attempt(artist, title);`
       - `const latin = detectLang(artist) === 'en' && detectLang(title) === 'en';` — GATE A (CONTEXT
         trigger (a)): NO kana/hangul/Han in either field. `detectLang` is the shared classifier;
         `'en'` is its "no CJK at all" verdict, so a Japanese/Korean query never enters (isChineseLine
         alone would let JA/KO through).
       - `if (hit && !(latin && !isStrongMatch({ artist, title }, hit))) return hit;` — GATE B: a strong
         hit, or any hit on a non-Latin query, returns exactly as today (zero extra calls).
       - `if (latin) return (await rescueLatin(artist, title)) ?? hit;` then the existing Chinese
         `isChineseLine` / t2s block continues UNCHANGED below (a Latin miss returned null there before;
         it now returns the rescue result or null — same for the non-rescued case).
       - New module-private `async function rescueLatin(artist, title): Promise<Track | null>`:
         `const zh = await lookupChineseName(artist, title); if (!zh) return null;`
         `const [a2, t2] = await t2sConvertLines([zh.artist, zh.title]);` — CONVERT FIRST (differs
         from the on-miss t2s asymmetry documented above: this path is brand-new, and the 2026-09-26
         probe showed Traditional "周杰倫 珊瑚海" → [] on QQ while "周杰伦 珊瑚海" hit; converting first
         costs one search instead of two). ACCEPTED DEVIATION — recorded in 260925-wa7-CONTEXT.md under
         "Accepted deviation (orchestrator, 2026-09-26, plan-check iteration 1)"; do not re-litigate at
         verification. `const r = await attempt(a2, t2); if (!r) return null;`
         Fold the candidate to Simplified for the comparison (a kuwo/netease row may carry Traditional):
         `const fold = (s: string) => t2sConvertLineSync(s) ?? s;` (warm — we just awaited the dict);
         `return isStrongMatch({ artist: a2, title: t2 }, { artist: fold(r.artist), title: fold(r.title) }) ? r : null;`
         Null → the caller falls back to `hit` (today's best weak match or null) — the rescue never
         makes a resolve worse (CONTEXT "After a verified lookup").
       - Extend the resolveStub doc comment with a `quick-260925-wa7` paragraph: what fires it (Latin +
         weak/miss), the never-worse rule, the supersedence contract still holding by construction
         (still ONE awaited call from playStub; do NOT add a generation guard here), and that
         `lookupChineseName` is never-throw + cached so a shelf re-tap costs zero lookups.

    3. `src/lib/services/discovery.test.ts` — new `describe('resolveStub — English/romanized name
       rescue (quick-260925-wa7)')` with every bullet in `<behavior>`; `import * as nameRescue from
       './name-rescue'` and `vi.spyOn(nameRescue, 'lookupChineseName')`; searchAll via the file's
       existing `vi.spyOn(catalog, 'searchAll').mockResolvedValueOnce(result([...]))` + `mk()`; pin
       EVERY branch with `toHaveBeenCalledTimes` on BOTH spies (the t2s block's pattern). The real
       `t2sConvertLines` runs (deterministic offline dict, as the existing block notes), so assert the
       second query string literally: `expect(spy.mock.calls[1][0]).toBe('周兴哲 最后一堂课')`.

    4. `name-rescue.test.ts` — add the `lookupChineseName` block from `<behavior>` (`import * as apiBase
       from './api-base'; vi.spyOn(apiBase, 'apiFetch').mockImplementation(async (path) => …)` routed
       by `path.includes('hl=zh-TW')` / `'country=hk'` / `'/lookup?'`; YTM envelopes are minimal
       `{ contents: { sectionListRenderer: { contents: [{ musicShelfRenderer: { contents: [{
       musicResponsiveListItemRenderer: { playlistItemData: { videoId }, flexColumns: [ {…runs:[{text:
       title}]}, {…runs:[{text: artist, navigationEndpoint:{browseEndpoint:{browseEndpointContextSupportedConfigs:
       {browseEndpointContextMusicConfig:{pageType:'MUSIC_PAGE_TYPE_ARTIST'}}}}}]} ] } }] } }] } } }`
       — build one `envelope(rows)` helper). Use the localStorage stub so cache writes are observable.

    Run the full suite, check and build before committing (main auto-deploys):
    `pnpm test && pnpm check && pnpm build`. Commit as
    `feat(quick-260925-wa7): rescue English/romanized Chinese-song names in resolveStub`.

    Optional live verify (sandbox reaches QQ/Netease/YTM/iTunes; dev server 4321 or 5173): in the browser
    console `(await import('/src/lib/services/discovery.ts')).resolveStub('Eric Chou', 'Graduation ("Mom, Don\'t Do That!" TV Series Theme Song)')`
    should resolve to a row titled 最后一堂课 / 周兴哲, and `localStorage['openmusic:name-rescue:v1']`
    should now hold that pair; a second call issues no /api/ytmusic or itunes request (Network tab).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test && pnpm check && pnpm build</automated>
  </verify>
  <done>lookupChineseName runs stage 1 → stage 2 → cache with every hop never-throw and every fetch deadline-bounded; resolveStub fires the rescue only for CJK-free weak/miss queries, re-searches once with Simplified names, and returns the rescued row only when it strongly matches (else today's result); all call counts pinned; full `pnpm test`, `pnpm check`, `pnpm build` green on the commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/ytmusic/search` | untrusted `q` + NEW `hl` query params cross to the edge |
| edge → InnerTube (music.youtube.com) | untrusted upstream JSON; fixed URL; public key never echoed |
| browser → itunes.apple.com | untrusted upstream JSON; CORS `*`; shared with itunes-cover.ts precedent |
| browser localStorage → resolveStub | `openmusic:name-rescue:v1` is user-writable; a poisoned entry becomes a search query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wa7-01 | Tampering / SSRF | `+server.ts` `hl` param → `searchInnerTube` | mitigate | `innerTubeLocale` exact-key allowlist (`en`→US, `zh-TW`→TW, own-property check); `gl` is table-derived, never a second param; raw value never forwarded; POST stays fixed-URL, `q`/`hl` only in the body (T-27-02-01 unchanged) |
| T-wa7-02 | Tampering | InnerTube + iTunes response JSON | mitigate | optional-chaining typed walkers (`parseSearchEnvelope` contract-drift throw caught → `[]`; `pairItunes` positive-integer trackId + non-empty-string name guards); malformed → null, never into a render path |
| T-wa7-03 | Tampering | localStorage `openmusic:name-rescue:v1` | mitigate | read-side shape guard (strings ≤ 200 chars, finite `at`, TTL), try/catch on parse; value is consumed ONLY as text inside `searchAll(...)` → own `/api/*` query (no HTML/URL/`src` sink); write-side cap 500 |
| T-wa7-04 | DoS (self-flood) | `lookupChineseName` fan-out | mitigate | fires only on Latin + weak/miss (call-count tests); ≤ 2 YTM + ≤ 4 iTunes GETs per cold rescue; hits 30 d / misses 1 d cached; every GET through `apiFetch` (dedupe + `MAX_CONCURRENT_REQUESTS` + circuit breaker) with `combinedSignal(6000)` |
| T-wa7-05 | DoS (edge cache bloat) | `?hl=<junk>` variants keyed by full URL | accept | unknown `hl` collapses to en/US upstream; distinct cache keys already possible today via arbitrary `q`; Cloudflare cache is bounded/evicting; TTL 600 s |
| T-wa7-06 | Information disclosure | query text to YTM/iTunes | accept | artist/title are already-public metadata (same as itunes-cover.ts); no secret, key, token or PII crosses; no `/api/ytmusic` body echoes the WEB_REMIX key (invariant unchanged) |
| T-wa7-07 | DoS (edge rate-limit) | itunes.apple.com from Workers egress | mitigate | iTunes calls live in `$lib/services/name-rescue.ts` (browser) only — never in `$lib/proxy/*` (spike 012: 403/429 on the shared egress IP) |
| T-wa7-08 | Spoofing (wrong song accepted) | `pairYtmRows` / `pairItunes` acceptance | mitigate | verified-evidence rules (en/US title must contain/equal the query title; iTunes also requires US artist match; zh pair must carry CJK and differ from the query); the re-search row must then pass `isStrongMatch` against the Chinese names or the original result is returned — a wrong pairing costs calls, never a worse resolve |
| T-wa7-SC | Tampering | npm installs | n/a | no package installed — zero new dependencies (platform `fetch`/`URL`/`localStorage` only) |
</threat_model>

<verification>
- `pnpm test` green (name-rescue.test.ts, discovery.test.ts incl. the untouched quick-260808-urx block, proxy/ytmusic.test.ts, sources/ytmusic.test.ts, match-key.test.ts).
- `pnpm check` green (strict TS; `+server.ts` exports only GET/OPTIONS).
- `pnpm build` green on every commit (main auto-pushes → Cloudflare Pages auto-deploys).
- `grep -n "itunes.apple.com" src/lib/proxy -r` returns nothing new (iTunes stays client-side).
- `grep -c "quick-260925-wa7" src/lib/services/discovery.ts src/lib/services/name-rescue.ts src/lib/proxy/ytmusic-innertube.ts` ≥ 1 each (decision-ref comments present).
</verification>

<success_criteria>
- The four CONTEXT failure cases (Coral Sea, Zai Ai Ni, Graduation, plus the Mojito accept and the two MUST-REJECT iTunes pairs) behave exactly as the fixtures specify in unit tests.
- A strong English hit costs one `searchAll` and zero lookups; JA/KO/ZH queries never enter the rescue; the t2s path is unchanged.
- The rescue never returns a worse result than today (original hit or null on any lookup/re-search failure).
- All fetches never-throw, deadline-bounded, governed; outcomes cached under `openmusic:name-rescue:v1`.
- `hl` reaches InnerTube only as an allowlisted `{hl, gl}` pair.
</success_criteria>

<output>
Create `.planning/quick/260925-wa7-rescue-english-and-romanized-chinese-son/260925-wa7-SUMMARY.md` when done.
</output>
