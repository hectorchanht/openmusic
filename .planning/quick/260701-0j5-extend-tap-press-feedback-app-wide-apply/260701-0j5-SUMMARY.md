---
phase: quick-260701-0j5
plan: 01
subsystem: ui-feedback
tags: [ux, tap-feedback, svelte-action, wiring-sweep]
requires:
  - src/lib/actions/tapBounce.ts (existing one-shot press action)
  - src/app.css @keyframes tap-bounce + .tap-bouncing (existing, untouched)
provides:
  - app-wide use:tapBounce on tap-to-act controls (nav, players, sheets, settings, album/artist/charts)
affects:
  - 21 .svelte files (3 components, 1 layout, 17 routes)
tech-stack:
  added: []
  patterns:
    - "Composable use:tapBounce directive added beside existing onclick/use:longpress/use:swipeAction/use:coverSwipe — no preventDefault, listens passively on pointerdown"
key-files:
  created: []
  modified:
    - src/routes/(app)/+layout.svelte
    - src/lib/components/Nowbar.svelte
    - src/lib/components/NowPlaying.svelte
    - src/lib/components/TrackMenu.svelte
    - src/lib/components/SleepTimerSheet.svelte
    - src/routes/(app)/search/+page.svelte
    - src/routes/(app)/library/+page.svelte
    - src/routes/(app)/settings/+page.svelte
    - src/routes/(app)/settings/general/+page.svelte
    - src/routes/(app)/settings/appearance/+page.svelte
    - src/routes/(app)/settings/playback/+page.svelte
    - src/routes/(app)/settings/translation/+page.svelte
    - src/routes/(app)/settings/activity/+page.svelte
    - src/routes/(app)/settings/data/+page.svelte
    - src/routes/(app)/settings/about/+page.svelte
    - src/routes/(app)/settings/lastfm/+page.svelte
    - src/routes/(app)/album/[name]/+page.svelte
    - src/routes/(app)/artist/[name]/+page.svelte
    - src/routes/(app)/charts/top/+page.svelte
    - src/routes/(app)/charts/tags/[tag]/+page.svelte
    - src/routes/(app)/charts/countries/[country]/+page.svelte
decisions:
  - "About-subpage .item rows (static info + external mailto/repo links) and the disabled Last.fm coming-soon .item are NOT action buttons → tapBounce added only to their .back buttons"
  - "Translation .link (jump-to-general) and search input excluded — not in the inclusion_map's button classes"
  - "charts/top has no back button; tapBounce added to both track result rows and artist result rows (the two genuine .row tap-to-act buttons) for row-pattern consistency"
metrics:
  duration: ~10 min
  completed: 2026-07-01
---

# Phase quick-260701-0j5 Plan 01: Extend Tap Press-Feedback App-Wide Summary

Wired the existing one-shot shrink-spring `use:tapBounce` action onto every genuinely tap-to-act control across nav, both players, sheets/modals, search, library, all settings pages, and album/artist/charts — a pure directive+import sweep with zero new action, keyframe, CSS, or logic changes. Scope-guarded away from inputs, range sliders, toggle switches, the seek bar, draggable cover/carousel/grip surfaces, sheet/modal drag containers, full-viewport scrims, and the home reorderable chips.

## What Was Built

Three atomic auto-task commits, each adding only `use:tapBounce` directives and the per-file `import { tapBounce } from '$lib/actions/tapBounce';`:

| Task | Commit | Files | tapBounce sites added |
|------|--------|-------|------------------------|
| 1 — Nav + players | `074d8e6` | +layout, Nowbar, NowPlaying | layout 2 (nav tabs, notice retry), Nowbar 2 (play/pause, sleep badge), NowPlaying 11 (2 header icons, 5 transport, st-readout, 3 subnav) |
| 2 — Sheets + search + library | `9e20201` | TrackMenu, SleepTimerSheet, search, library | TrackMenu 16 (2 hd-btn, 11 main .mi, picker rows, .mi.accent, detail .x), SleepTimer 3, search +4 new (submit, clear, 2 suggest rows, offline-cta — counts 6 incl 1 pre-existing 0aa row), library +8 new (5 tabs, 2 edit-btn, 2 del, fav-tile — counts 14 incl 4 pre-existing 0aa rows) |
| 3 — Settings + album/artist/charts | `c8ed8dd` | settings index + 8 subpages, album, artist, 3 charts pages | settings index 2; general 6; appearance 2; playback 8; translation 6; activity 3; data 7; about 1; lastfm 1; album 10; artist 8; charts top 2, tags 1, countries 1 |

Final `npx svelte-check --threshold error` → **0 errors, 0 warnings**.

## Exclusions Honored (verified by scan)

- No `use:tapBounce` on `.scrim` / `.menu` / `.modal` drag containers (TrackMenu, SleepTimerSheet, album picker).
- No `use:tapBounce` on `type="range"` sliders or `.row-toggle` switches (general, appearance, playback, translation).
- No `use:tapBounce` on NowPlaying `.np-top`, `.cover`, `.cover-strip`, `.grip`, `.track` (seek), `.artist-link`, `.cover-cell`, or Nowbar `.np-open` (coverSwipe carousel).
- `src/routes/(app)/settings/home/+page.svelte` left entirely untouched (chipReorder drag-transform conflict).
- `src/app.css` untouched — keyframe + reduced-motion guard already global.

## Deviations from Plan

### Auto-fixed Issues
None — plan executed as written. No bugs, missing functionality, or blocking issues encountered.

### Judgment calls within plan scope (not deviations)
- **about subpage:** Only the `.back` button got tapBounce. Its other `.item` rows are a static info line (`cursor: default`) and two external mailto/GitHub `<a>` links, not the "action rows" the inclusion_map enumerates for activity/data. (1 site)
- **lastfm subpage:** Only `.back`. The single `.item` is a `disabled` coming-soon placeholder (no press to feed back). (1 site)
- **charts/top:** No back button exists (top-level tab). Added tapBounce to both the track result `.row` and the artist result `.row` — both are genuine tap-to-act rows, consistent with the search/library/album/artist row pattern the plan asked to extend "for consistency". (2 sites)
- **translation `.link`** (jump to general settings) was left alone — it is a text-link, not one of the inclusion_map's `.chip`/`.seg`/`.swatch`/`.item`/back/reset classes.

## Flagged for Human-Verify (unchanged, surfaced per plan)
1. Pre-existing `:active { transform: scale(0.92) }` (not hover-gated) on Nowbar `.np-btn` + `.st-badge` and album `.act` — left untouched per constraint; tapBounce composes (0.22s one-shot overrides briefly then springs back). Verify no fight/jank on these three.
2. `.swatch` accent circles have a box-shadow selection ring — verify the center-origin scale bounce does not clip it.

## Checkpoint Status

The final task `checkpoint:human-verify` (gate="blocking") is **PENDING**. Per the execution constraints, no preview server was started and the app was not run/clicked. The reviewer should follow the 11-step verification checklist in the PLAN (`<how-to-verify>`) and reply "approved" or describe any control that bounces wrong, latches under a held finger, fails reduced-motion, or whose existing click/longpress/drag/swipe broke.

## Self-Check: PASSED

- All 21 modified files verified present on disk (sampled 6, all FOUND).
- All three commits present in `git log`: `074d8e6`, `9e20201`, `c8ed8dd`.
- `svelte-check --threshold error` → 0 errors after each task and at completion.
- Node scan confirms import + expected tapBounce count in every file, and zero tapBounce on any excluded surface; `git diff` shows only directive/import additions (no CSS, no app.css, no logic), home untouched.
