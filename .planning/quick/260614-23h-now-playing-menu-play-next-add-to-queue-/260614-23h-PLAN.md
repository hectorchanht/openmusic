---
phase: quick-260614-23h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/TrackMenu.svelte
autonomous: true
requirements: [QUICK-260614-23h]
must_haves:
  truths:
    - "Opening the track menu on the currently-playing track does NOT show Play next or Add to queue"
    - "Opening the track menu on any OTHER track still shows Play next and Add to queue"
    - "On desktop Chrome/Edge, Download opens a native Save-As dialog at the chosen quality/filename"
    - "Cancelling the desktop save dialog aborts silently with no error toast and no fallback download"
    - "On iOS Safari / Android Chrome, Download still saves to the default folder via the anchor element (unchanged)"
  artifacts:
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "uid-gated queue buttons + desktop showSaveFilePicker download branch"
      contains: "showSaveFilePicker"
  key_links:
    - from: "src/lib/components/TrackMenu.svelte template"
      to: "player.current"
      via: "uid equality guard on Play next / Add to queue"
      pattern: "track.uid !== player.current"
    - from: "doDownload"
      to: "window.showSaveFilePicker"
      via: "feature-detect + createWritable/write/close"
      pattern: "showSaveFilePicker"
---

<objective>
Two scoped tweaks to the track menu, both in `src/lib/components/TrackMenu.svelte`:

1. Hide the "Play next" and "Add to queue" rows when the menu's `track` IS the currently-playing track (`player.current`). TrackMenu is shared across search rows / queue rows / now-playing, so guard on `uid` equality (correct everywhere, not page-based).
2. Add a desktop-only save-path picker (File System Access API `showSaveFilePicker`) to the existing `doDownload` flow, with graceful fallback to the current anchor-element download on mobile/unsupported browsers. Quality + filename logic already work and stay untouched.

Purpose: stop offering no-op queue actions on the current song; give desktop users a real Save-As dialog.
Output: edited `TrackMenu.svelte`, passing typecheck/build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/lib/components/TrackMenu.svelte

<interfaces>
<!-- Confirmed from src/lib/stores/player.svelte.ts -->
player.current: Track | null      // $state — the playing track (or null)
player.playNext(track: Track)     // used by playNext() at TrackMenu line 59
player.addToQueue(track: Track)   // used by addQueue() at TrackMenu line 60

<!-- Confirmed from src/lib/sources/types -->
Track has a `uid` string field (e.g. "netease-12345678")

<!-- doDownload (TrackMenu lines 98-133): the re-resolved track is `r`. -->
<!-- ext is derived from r.audioUrl at line 122; filename at line 125 uses r.artist/r.title. -->
<!-- Use `r` (not `resolved`) for the picker's suggestedName + ext. -->
</interfaces>

<env>
- WIP NOTE: TrackMenu.svelte has uncommitted working-tree changes (a max-height 80vh->90vh tweak). Build ON the current working-tree state. Do NOT revert that WIP.
- SvelteKit + Svelte 5 runes, TypeScript. No new dependencies.
- Verify scripts: `npm run check` (svelte-check) and `npm run build` (vite build).
</env>
</context>

<tasks>

<task type="auto">
  <name>Task A: Hide Play next / Add to queue for the current track</name>
  <files>src/lib/components/TrackMenu.svelte</files>
  <action>
    In the template, wrap BOTH the Play next button (line 249) and the Add to queue button (line 250)
    in a single `{#if track && track.uid !== player.current?.uid}` ... `{/if}` block so they render only
    when this menu's track is NOT the currently-playing one. The two buttons stay adjacent and inside the
    same conditional. Do NOT touch the Remix button (line 253), the comment block (lines 246-248), or any
    other rows. The `player` store is already imported (line 6) and `player.current` is reactive ($state),
    so no new imports are needed.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    Both queue buttons are inside `{#if track && track.uid !== player.current?.uid}`; svelte-check passes
    with no new errors. Menu on a non-current track still shows both rows; menu on the current track hides them.
  </done>
</task>

<task type="auto">
  <name>Task B: Desktop showSaveFilePicker branch in doDownload</name>
  <files>src/lib/components/TrackMenu.svelte</files>
  <action>
    Edit `doDownload(resolved: Track)` (lines 98-133). Keep everything through `await blobStore.put(r.uid, blob)`
    (line 121) and the ext derivation (line 122) unchanged. Compute the sanitized filename once
    (`${r.artist} - ${r.title}.${ext}` with the existing `.replace(/[/\\?%*:|"<>]/g, '_')`) so both branches reuse it.
    Add a minimal ext->mime lookup: mp3->audio/mpeg, flac->audio/flac, m4a->audio/mp4, aac->audio/aac,
    ogg->audio/ogg, wav->audio/wav (default audio/mpeg).
    Then branch:
      - If `typeof window !== 'undefined' && 'showSaveFilePicker' in window`: await
        `(window as any).showSaveFilePicker({ suggestedName: <sanitized filename>, types: [{ description: 'Audio',
        accept: { [mime]: ['.' + ext] } }] })`, then `await handle.createWritable()`, `await writable.write(blob)`,
        `await writable.close()`, and `toast.show(t('toast.downloaded'))`. Wrap this picker block in its own
        try/catch: if `err?.name === 'AbortError'` (user cancelled), return silently — no error toast, no anchor
        fallback (cancel means cancel). For any OTHER picker error, fall through to the anchor download below.
      - Else: use the EXISTING anchor-element download (create `a`, set href to objectURL, `a.download = filename`,
        `a.click()`, `URL.revokeObjectURL(a.href)`, `toast.show(t('toast.downloaded'))`) unchanged in behavior.
    Keep the OUTER try/catch (the fetch-failure -> `window.open(r.audioUrl, '_blank')` + `t('toast.openedAudio')`
    fallback at lines 129-131) intact wrapping the whole fetch+save sequence. Use the narrow `(window as any)` cast
    for showSaveFilePicker since the Window type may not declare it.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
  <done>
    `doDownload` contains a `showSaveFilePicker` branch gated by feature detection; AbortError returns silently
    (no toast, no fallback); non-supporting browsers keep the anchor download; the outer fetch-failure window.open
    fallback is intact. svelte-check and vite build both pass.
  </done>
</task>

</tasks>

<verification>
- `npm run check` passes (no new TypeScript / svelte-check errors).
- `npm run build` passes.
- Manual verify (note for executor summary, cannot be automated here):
  1. Open the track menu on a track that is NOT currently playing -> Play next + Add to queue visible.
  2. Open the track menu on the currently-playing track -> both rows hidden; Remix and the rest still present.
  3. Desktop Chrome/Edge: tap Download -> native Save-As dialog appears with `Artist - Title.<ext>` suggested;
     cancelling it produces no error toast and no download; confirming writes the file.
  4. Mobile (iOS Safari / Android Chrome): tap Download -> file saves to default folder (anchor behavior unchanged).
</verification>

<success_criteria>
- Queue actions are uid-gated and hidden on the current track everywhere TrackMenu is used.
- Desktop download offers a Save-As picker; mobile/unsupported keeps existing behavior.
- No new dependencies; WIP max-height tweak preserved; typecheck + build green.
</success_criteria>

<output>
Create `.planning/quick/260614-23h-now-playing-menu-play-next-add-to-queue-/260614-23h-SUMMARY.md` when done.
</output>
