---
phase: quick-260915-vb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/library.svelte.ts
  - src/lib/stores/library.svelte.test.ts
  - src/routes/(app)/library/+page.svelte
  - src/lib/i18n/en.ts
  - src/lib/i18n/zh-Hant.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/es.ts
  - src/lib/i18n/fr.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/pt.ts
  - src/lib/i18n/it.ts
  - src/lib/i18n/ru.ts
  - src/lib/i18n/tr.ts
  - src/lib/i18n/ar.ts
  - src/lib/i18n/hi.ts
  - src/lib/i18n/id.ts
  - src/lib/i18n/vi.ts
  - src/lib/i18n/th.ts
autonomous: true
requirements: [quick-260915-vb9]

must_haves:
  truths:
    - "The Library page heading is the active tab's label alone (Liked / Playlists / Downloads / Favourite artists / History) — the word 'Library' no longer appears in the page header (the bottom-nav tab already says it)"
    - "Under the icon pill row there is ONE action row with, in order: Play, Shuffle, Edit, ⋯ — every button carries use:tapBounce, and the row fits a 360px-wide phone in a single line"
    - "Play installs the current tab's track list as the queue with the tab's QueueContext (liked→'liked', downloads→'downloads', playlist detail→'playlist', history→'history') and fresh-plays its first track; Shuffle does the same over a Fisher-Yates COPY (services/discovery shuffle) so the queue order itself is shuffled — player.shuffle mode is NOT toggled"
    - "Play and Shuffle are HIDDEN (not disabled, not dead) when the tab has no playable list: fav-artists tab, playlists tab without a ?playlist detail pin, and any empty list"
    - "Edit toggles the EXISTING editMode (per-row remove) under the existing editableTabHasContent gate; the header no longer has its own Edit / Clear-history buttons"
    - "⋯ opens a bottom sheet (scrim + fly + dragClose + focusTrap + overlays.open/dismiss single-dismiss path) with only the rows that apply to the current tab: Add to queue, Play next (when the tab has tracks), Delete playlist (playlist detail view only), Clear all (any non-empty tab incl. fav-artists and history)"
    - "Clear all and Delete playlist each call window.confirm() first and do NOTHING when it returns false; Clear all removes only the current tab's list (liked / downloads / fav-artists / history / the pinned playlist's tracks) — never the whole library"
    - "Clearing Downloads also deletes the cached blobs (blobStore.del per uid, whose device: refusal still protects imported files) and empties the unavailable marks — in ONE persisted save, not N"
    - "Every new user-facing string has a key in ALL 15 src/lib/i18n/*.ts dictionaries, double-quoted; i18n.test.ts parity passes"
    - "pnpm check and pnpm test are green"
  artifacts:
    - path: "src/lib/stores/library.svelte.ts"
      provides: "clearLiked(), clearDownloads(), clearFavArtists(), clearPlaylistTracks(id) — one save() each"
      contains: "clearPlaylistTracks"
    - path: "src/lib/stores/library.svelte.test.ts"
      provides: "a describe block covering the four per-list clears (one save, blobs deleted for downloads, other lists untouched)"
      contains: "clearDownloads"
    - path: "src/routes/(app)/library/+page.svelte"
      provides: "tabLabel heading, .actions row, tabList derived, playAll/shuffleAll, inline list-menu sheet with confirm-gated destructive rows"
      contains: "library-list-menu"
    - path: "src/lib/i18n/en.ts"
      provides: "library.clearList, library.clearListConfirm, library.deletePlaylistConfirm"
      contains: "library.clearListConfirm"
  key_links:
    - from: "src/routes/(app)/library/+page.svelte shuffleAll()"
      to: "src/lib/services/discovery.ts shuffle()"
      via: "named import (file already imports mapWithConcurrency from there)"
      pattern: "shuffle\\(tabList\\)"
    - from: "src/routes/(app)/library/+page.svelte list menu $effect"
      to: "overlays.open('library-list-menu', …) / overlays.dismiss('library-list-menu')"
      via: "untrack()-wrapped effect keyed on the sheet's open flag ONLY (album/+page.svelte:570-575 idiom)"
      pattern: "overlays\\.(open|dismiss)\\('library-list-menu'"
    - from: "src/routes/(app)/library/+page.svelte clearCurrentList()"
      to: "library.clearLiked / clearDownloads / clearFavArtists / clearPlaylistTracks / history.clear"
      via: "confirm(t('library.clearListConfirm', { name })) gate, then a per-tab dispatch"
      pattern: "confirm\\(t\\('library\\.clearListConfirm'"
---

<objective>
Clean up the Library page header (the duplicated "Library" word goes; the active tab label IS the heading) and give every tab a per-list action row — Play / Shuffle / Edit / ⋯ — where ⋯ opens a per-tab sheet of list actions (Add to queue, Play next, Delete playlist, Clear all), with destructive actions confirm-gated.

Purpose: the page header currently says "Library  Liked" under a bottom-nav tab that already says Library, and the only list-level actions are a lone Edit button and a history-only Clear. Users want to play/shuffle a whole list and wipe a list in one place, per tab.

Output: four tiny per-list clear methods on the library store (+ tests), three new i18n keys in all 15 dictionaries, and the rewritten header/action row/list sheet in `src/routes/(app)/library/+page.svelte`. No new component, no new store state, no new dependency.

Shortest-diff decisions (quick-260915-vb9), already made — do not relitigate:
- The list sheet is INLINE in the page, copying the album page's playlist-picker idiom (`src/routes/(app)/album/[name]/+page.svelte:570-575` effect + `:691-699` markup + `:770-775` CSS). TrackMenu is track-scoped (needs a `Track`) and cannot host list actions; a new `ListMenu.svelte` would be a one-consumer component.
- Shuffle = `shuffle(tabList)` from `$lib/services/discovery` then the existing `playList(copy, copy[0])`. It does NOT call `player.toggleShuffle()`: that only reorders the tail AFTER current and is a no-op when the mode is already on, so the result would depend on prior state.
- Labels reuse existing keys wherever one exists: Play → `library.playAll`, Shuffle → `nowplaying.shuffle`, Edit → `library.edit` / `common.done`, ⋯ aria-label → `menu.options`, sheet rows → `menu.addToQueue` / `menu.playNext` / `library.deletePlaylist`, toasts → `toast.addedToQueue` / `toast.playingNext`. Only THREE keys are new (Task 1).
- `library.heading` and `history.clear` become unused. Leave them in place — no test flags unused keys and deleting from 15 files is churn for nothing.
- Dropped candidates (see `260915-vb9-deferred-items.md` in this directory): Multi-select, Sort, Download all, Rename playlist, Share list. Each needs new state, a new store method that does not exist, or a platform fork — none is a one-or-two-line call onto an existing method.

**DO NOT PUSH.** Pushing `main` auto-deploys production (memory: push to main auto-deploys prod). Commit locally only.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@/Users/laichan/code/tung/openmusic/CLAUDE.md
@/Users/laichan/code/tung/openmusic/src/routes/(app)/library/+page.svelte
@/Users/laichan/code/tung/openmusic/src/lib/stores/library.svelte.ts
@/Users/laichan/code/tung/openmusic/src/lib/stores/library.svelte.test.ts
@/Users/laichan/code/tung/openmusic/src/routes/(app)/album/[name]/+page.svelte

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From src/lib/stores/library.svelte.ts (runes singleton `library`):
```ts
liked = $state<Track[]>([]); playlists = $state<Playlist[]>([]); downloads = $state<Track[]>([]);
favArtists = $state<string[]>([]); unavailable = $state<Set<string>>(new Set());
private save(): void;                         // ONE localStorage write of the whole payload
toggleLike(t: Track); removeDownload(uid: string); removeFromPlaylist(id: string, uid: string);
deletePlaylist(id: string); toggleFavArtist(name: string); setDownloads(next: Track[]); clearAll();
// blobStore is imported at top: `import { blobStore } from '$lib/services/blob-store';` — blobStore.del(uid) never throws, refuses device: uids.
```

From src/lib/stores/history.svelte.ts (runes singleton `history`):
```ts
entries: Track-shaped[];  clear(): void;   // wipes + saves
```

From src/lib/stores/player.svelte.ts:
```ts
setListQueue(tracks: Track[], context: QueueContext = null, cover: string | null = null): void;
play(t: Track, opts: { fresh?: boolean }): Promise<void>;
playNext(t: Track, opts: { pin?: boolean } = {}): void;   // splices right after current
addToQueue(t: Track): void;                               // append, de-duped; plays if nothing playing
```

From src/lib/config/defaults.ts:
```ts
type QueueContext = 'liked' | 'downloads' | 'playlist' | 'history' | … | null;  // 'playlist' is SINGULAR
```

From src/lib/services/discovery.ts:
```ts
export function shuffle<T>(arr: T[]): T[];   // returns a NEW Fisher-Yates permutation, never mutates input
```

From src/lib/i18n/index.ts:
```ts
export function t(key: TranslationKey, params?: Record<string, string | number>): string;  // {name} interpolation
```

From src/lib/stores/overlays.svelte.ts (used via the album picker idiom):
```ts
overlays.open(id: string, onBack: () => void): void;  overlays.dismiss(id: string): void;
```

Lucide icons available (`@lucide/svelte`): `Play`, `Shuffle`, `Pencil`, `Check`, `Ellipsis` (the ⋯ glyph — NOT `MoreHorizontal`), `ListEnd`, `ListStart`, `Trash2`, `X`.

Existing page symbols to REUSE (src/routes/(app)/library/+page.svelte): `tab`, `tabLabel` (:97), `detailPlaylist` (:84), `editMode` (:153), `editableTabHasContent` (:169), `playList(list, t)` (:184), `playEntry(track)` (:194), `.edit-btn` / `.edit-btn.danger` CSS (:355-358).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-list clear methods on the library store + the three new i18n keys in all 15 locales</name>
  <files>src/lib/stores/library.svelte.ts, src/lib/stores/library.svelte.test.ts, src/lib/i18n/en.ts, src/lib/i18n/zh-Hant.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/es.ts, src/lib/i18n/fr.ts, src/lib/i18n/de.ts, src/lib/i18n/pt.ts, src/lib/i18n/it.ts, src/lib/i18n/ru.ts, src/lib/i18n/tr.ts, src/lib/i18n/ar.ts, src/lib/i18n/hi.ts, src/lib/i18n/id.ts, src/lib/i18n/vi.ts, src/lib/i18n/th.ts</files>
  <behavior>
    - clearLiked(): liked becomes []; downloads / playlists / favArtists untouched; the persisted payload's `liked` is [].
    - clearDownloads(): downloads becomes [], unavailable becomes an empty Set, blobStore.del called once per former uid (assert via the existing hoisted `blobDel` mock), other lists untouched, localStorageMock.setItem called exactly ONCE for the whole clear (spy on it) — not once per track.
    - clearFavArtists(): favArtists becomes []; liked untouched.
    - clearPlaylistTracks(id): that playlist keeps its id + name with tracks []; other playlists' tracks untouched; unknown id is a no-op that still does not throw.
  </behavior>
  <action>
    Write the failing tests FIRST as a new `describe('quick-260915-vb9 per-list clears', …)` block appended to `src/lib/stores/library.svelte.test.ts`, using the file's existing `mk()` factory, the hoisted `blobDel` mock and the in-memory `localStorageMock` (a `beforeEach` that resets `library.liked/downloads/playlists/favArtists/unavailable` directly and calls `blobDel.mockClear()`, matching the existing describe blocks). Run `pnpm test -- src/lib/stores/library.svelte.test.ts` and confirm the new tests FAIL (methods missing). Commit: `test(quick-260915-vb9): add failing specs for per-list library clears`.

    Then add four methods to the `Library` class in `src/lib/stores/library.svelte.ts`, placed directly ABOVE the existing `clearAll()` so the destructive methods sit together, each with a `// quick-260915-vb9:` comment:
    - `clearLiked()` — `this.liked = []; this.save();`
    - `clearDownloads()` — snapshot the uids first, then `this.downloads = []; this.unavailable = new Set(); this.save();` then `for (const uid of uids) void blobStore.del(uid);`. Comment WHY this is not a `removeDownload` loop: removeDownload saves the entire library payload once per call, so a 300-song list would serialise the whole store 300 times on a phone; this is one save. Also note the blob delete is still per-uid so `blobStore.del`'s device: refusal (Plan 34-01) keeps protecting imported files exactly as the single-remove path does.
    - `clearFavArtists()` — `this.favArtists = []; this.save();`
    - `clearPlaylistTracks(id: string)` — `this.playlists = this.playlists.map((p) => (p.id === id ? { ...p, tracks: [] } : p)); this.save();` (same immutable-map shape as `removeFromPlaylist`).
    Do NOT touch `clearAll()` and do NOT add any new `$state`. Run the test file again — all green. Commit: `feat(quick-260915-vb9): per-list clear methods on the library store`.

    i18n — add EXACTLY these three keys to `src/lib/i18n/en.ts` in the `library.*` block (after `"library.remove"`), then the same three keys, translated, to the identical position in ALL other 14 dictionaries (zh-Hant, zh-Hans, es, fr, de, pt, it, ru, tr, ar, hi, id, vi, th). DOUBLE quotes for every key and value (the i18n test's single-quote scan will fail otherwise). en values:
    - `"library.clearList": "Clear all"`
    - `"library.clearListConfirm": "Remove everything from {name}? This cannot be undone."` — `{name}` is interpolated by `t()`; keep the literal `{name}` token in every translation.
    - `"library.deletePlaylistConfirm": "Delete playlist {name}?"`
    No other new keys: Play / Shuffle / Edit / ⋯ / Add to queue / Play next / Delete playlist all reuse existing keys (listed in the objective). Run `pnpm test -- src/lib/i18n/i18n.test.ts` — parity + no-blank + double-quote checks must pass. Commit: `feat(quick-260915-vb9): i18n keys for the library list menu`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm test -- src/lib/stores/library.svelte.test.ts src/lib/i18n/i18n.test.ts</automated>
  </verify>
  <done>Four new methods exist on `Library`, each with a single `save()`; the new describe block passes including the "setItem called once" and "blobDel per uid" assertions; all 15 dictionaries expose the 3 new keys with identical key sets; i18n.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Library page — tab-label heading, Play / Shuffle / Edit / ⋯ action row, inline per-tab list sheet with confirm-gated destructive rows</name>
  <files>src/routes/(app)/library/+page.svelte</files>
  <action>
    All edits in `src/routes/(app)/library/+page.svelte`. Tabs for indent, single quotes, `// quick-260915-vb9:` comment at each decision point named below.

    **1. Heading.** Replace the `<header class="head">` block (:202-211) with `<header class="head"><h1>{tabLabel}</h1></header>` — the Edit and Clear-history buttons MOVE into the action row (step 3), so the header holds nothing else. Update the `tabLabel` comment (:95-96) to say it is now the heading itself. Delete the `.tab-sub` CSS rule (:354) — an unused selector is a svelte-check warning.

    **2. Derived list + context.** Add, after `editableTabHasContent`:
    - `const tabList = $derived<Track[]>(…)` — liked → `library.liked`; downloads → `library.downloads`; history → `history.entries as Track[]`; playlists → `detailPlaylist?.tracks ?? []`; fav-artists → `[]`. Comment: on the Playlists tab with no `?playlist` detail pin the list is deliberately EMPTY (Play/Shuffle hide) — each folder already has its own Play button in `.pl-head`, and concatenating every playlist into one queue is not something a user asked for.
    - Extend the `ctx` ternary inside `playList()` (:187) with `tab === 'history' ? 'history'` so one function serves every tab, and reduce `playEntry()` (:194-197) to a one-line delegate `playList(history.entries as Track[], track)` so the two paths cannot drift.
    - `function playAll()` — `if (tabList.length) playList(tabList, tabList[0]);`
    - `function shuffleAll()` — `const s = shuffle(tabList); if (s.length) playList(s, s[0]);` with `shuffle` added to the existing `import { mapWithConcurrency } from '$lib/services/discovery';`. Comment WHY not `player.toggleShuffle()` (reorders only the tail after current; no-op when the mode is already on → result would depend on prior state; a shuffled COPY is deterministic and leaves the mode alone).

    **3. Action row.** Insert a `<div class="actions">` directly UNDER the closing `</nav>` of `.tabs` (:222) and before the `{#if tab === 'liked'}` block. Four buttons, all `use:tapBounce`, all reusing the existing `.edit-btn` class (rename nothing — reusing it is the shortest diff; add the `.actions` container rule only):
    - `{#if tabList.length}` Play: `<Play size={16} /> {t('library.playAll')}` → `playAll`; Shuffle: `<Shuffle size={16} /> {t('nowplaying.shuffle')}` → `shuffleAll`. `{/if}`
    - `{#if editableTabHasContent}` the existing Edit toggle button MOVED here verbatim (aria-pressed, Check/Pencil, `common.done`/`library.edit`). `{/if}`
    - `{#if listMenuHasItems}` ⋯: `<button class="edit-btn" aria-label={t('menu.options')} title={t('menu.options')} onclick={() => (listMenuOpen = true)} use:tapBounce><Ellipsis size={16} /></button>` — icon-only. `{/if}`
    where `const listMenuHasItems = $derived(tabList.length > 0 || (tab === 'fav-artists' && library.favArtists.length > 0) || !!detailPlaylist)`. Add `Shuffle` and `Ellipsis` to the lucide import; drop icons that become unused (`Trash2` stays — rows + sheet use it).
    CSS: `.actions { display: flex; gap: 8px; margin-bottom: 14px; }` and `.actions .edit-btn { flex: 0 1 auto; white-space: nowrap; }`. Mobile fit (quick-260915-vb9 comment on the rule): four 13px pills — Play ~70px, Shuffle ~90px, Edit ~70px, ⋯ ~40px, gaps 24px — is ~300px, inside a 360px viewport minus page padding; ⋯ is already icon-only so nothing further collapses. If the executor measures overflow at 360px in `pnpm dev`, add `@media (max-width: 359px) { .actions .edit-btn > :not(svg) { display: none; } }` — icon-only fallback, no other layout change.

    **4. Inline list sheet.** Add `let listMenuOpen = $state(false);` next to `menuOpen`. Copy the album page's picker idiom exactly (imports: `untrack` from `svelte`, `fly` from `svelte/transition`, `dragClose` from `$lib/actions/dragClose`, `focusTrap` from `$lib/actions/focusTrap`, `overlays` from `$lib/stores/overlays.svelte`):
    - `$effect(() => { if (listMenuOpen) { untrack(() => overlays.open('library-list-menu', () => (listMenuOpen = false))); return () => untrack(() => overlays.dismiss('library-list-menu')); } });` — dep is `listMenuOpen` ONLY (the TrackMenu :404-420 rationale: cleanup is the sole `dismiss` caller so scrim / X / drag / Back converge on one history pop). The overlay id `library-list-menu` must be distinct from TrackMenu's `trackmenu-*` ids because TrackMenu is co-mounted on this page.
    - Markup, placed right before the existing `<TrackMenu …/>` line: `{#if listMenuOpen}` scrim button (`aria-label={t('menu.close')}`, closes) + `<div class="sheet" transition:fly={{ y: 240, duration: 200 }} use:dragClose={{ onclose: () => (listMenuOpen = false) }} use:focusTrap>` containing `<div class="sheet-head">{tabLabel}</div>` and these rows, each `class="mi"` + `use:tapBounce`, each closing the sheet after acting, and each rendered ONLY when it applies (no dead rows):
      - `{#if tabList.length}` **Add to queue** `<ListEnd size={18} /> {t('menu.addToQueue')}` → `for (const x of tabList) player.addToQueue(x); toast.show(t('toast.addedToQueue')); hapticTick();` and **Play next** `<ListStart size={18} /> {t('menu.playNext')}` → `for (const x of [...tabList].reverse()) player.playNext(x); toast.show(t('toast.playingNext')); hapticTick();` — comment: reversed so the list lands after current in its ORIGINAL order (each playNext splices at current+1). Comment on Add to queue: `addToQueue` persists per call; fine at library sizes (`// ponytail: N persists, batch if a 1000-song list ever measures slow`). `{/if}`
      - `{#if detailPlaylist}` **Delete playlist** `<Trash2 size={18} /> {t('library.deletePlaylist')}` class `mi danger` → `if (!confirm(t('library.deletePlaylistConfirm', { name: detailPlaylist.name }))) return; library.deletePlaylist(detailPlaylist.id); detailPlaylistId = null;` `{/if}`
      - `{#if listMenuHasItems}` **Clear all** `<Trash2 size={18} /> {t('library.clearList')}` class `mi danger` → `clearCurrentList()`. `{/if}`
      - a trailing `<button class="mi close">` `<X size={18} /> {t('menu.close')}` (album picker :699 idiom).
    - `function clearCurrentList()` — `if (!confirm(t('library.clearListConfirm', { name: detailPlaylist?.name ?? tabLabel }))) return;` then dispatch on `tab`: liked → `library.clearLiked()`; downloads → `library.clearDownloads()`; fav-artists → `library.clearFavArtists()`; history → `history.clear()`; playlists → `if (detailPlaylist) library.clearPlaylistTracks(detailPlaylist.id)`. Then `editMode = false; listMenuOpen = false;`. Comment: `confirm()` is the house pattern for destructive taps (settings/data/+page.svelte:85-87 — Capacitor 8 renders it as a native AlertDialog); never wipe on a single tap.
    - CSS: copy from the album page :770-775 — `.scrim`, the sheet container (rename `.picker` → `.sheet`; same rule), `.sheet-head` (= `.picker-head` rule), `.mi`, `.mi:hover`, `.mi.close`; add `.mi.danger { color: #ff7a90; }` (the existing `.edit-btn.danger` tint).

    **5. Cleanup + gates.** Remove `.edit-btn.danger` / `.edit-btn.danger:hover` rules only if nothing uses them after the header rewrite (svelte-check reports unused selectors). Run `pnpm check` (zero errors, zero new warnings) and `pnpm test` (all green). Smoke in `pnpm dev` (port 4321 or 5173 — probe, do not assume): each tab shows the right heading and only applicable buttons/rows; Cancel on the confirm leaves the list intact; Clear all on Downloads empties the tab; Back gesture after opening ⋯ closes the sheet and does NOT leave the page. Commit: `feat(quick-260915-vb9): library heading cleanup + per-tab action row and list menu`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check && pnpm test && grep -c "library-list-menu" "src/routes/(app)/library/+page.svelte" && ! grep -q "t('library.heading')" "src/routes/(app)/library/+page.svelte" && grep -c "confirm(t('library.clearListConfirm'" "src/routes/(app)/library/+page.svelte" && grep -c "confirm(t('library.deletePlaylistConfirm'" "src/routes/(app)/library/+page.svelte"</automated>
  </verify>
  <done>Header renders only `{tabLabel}`; the `.actions` row holds Play / Shuffle (hidden when `tabList` is empty), Edit (existing gate), ⋯ (hidden when the sheet would be empty), all with `use:tapBounce`; the inline sheet opens/closes through the single `library-list-menu` overlay path and renders only applicable rows; both destructive rows are `confirm()`-gated; `pnpm check` and `pnpm test` pass; every decision point carries a `quick-260915-vb9` comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → destructive store write | A single tap must never wipe a persisted list |
| stored playlist name → confirm() text | `detailPlaylist.name` is user-typed and flows into a native/browser dialog string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-vb9-01 | Tampering | clearCurrentList / Delete playlist rows | mitigate | `window.confirm()` gate before every destructive call; `false` → return with no write (Task 2 step 4) |
| T-vb9-02 | Denial of service | clearDownloads | mitigate | one `save()` for the whole clear (not N); blob deletes are fire-and-forget `void blobStore.del` that never throw (Task 1) |
| T-vb9-03 | Tampering | clearDownloads on device: uids | accept | `blobStore.del` already refuses device: uids (Plan 34-01); the list entry is removed on explicit user intent exactly as `removeDownload` does |
| T-vb9-04 | Information disclosure | playlist name in confirm() | accept | plain-text dialog, same-device data, no markup interpretation (`confirm()` renders text only) |
| T-vb9-05 | Tampering | Add to queue / Play next loops | accept | O(n) / O(n²) over the user's own local list; bounded by library size, no network |
| T-vb9-SC | Tampering | npm installs | accept | no package installs in this plan |
</threat_model>

<verification>
- `pnpm check` — zero errors; no new unused-selector warnings in library/+page.svelte.
- `pnpm test` — all green, including the new per-list-clear describe block and i18n parity across 15 locales.
- Manual (dev server): Liked / Playlists / Downloads / Favourite artists / History each show their label as the sole heading; Play + Shuffle appear only on tabs with tracks; ⋯ shows only applicable rows; confirm-Cancel is a no-op; Back closes the sheet without leaving the page.
</verification>

<success_criteria>
- Header text is the active tab label only; no "Library" word in the page header.
- Action row (Play / Shuffle / Edit / ⋯) fits one line at 360px, every button `use:tapBounce`.
- Shuffle produces a shuffled queue order without toggling `player.shuffle`.
- ⋯ sheet: Add to queue, Play next, Delete playlist (detail only), Clear all — nothing else, nothing dead.
- Clear all / Delete playlist confirm first; Clear all is per-list, one persisted save, downloads' blobs deleted.
- 3 new i18n keys present in all 15 dictionaries, double-quoted.
- Deferred items recorded in `260915-vb9-deferred-items.md` (multi-select, sort, download all, rename, share list).
- Committed locally; NOT pushed.
</success_criteria>

<output>
Create `.planning/quick/260915-vb9-library-page-header-cleanup-and-per-tab-/260915-vb9-SUMMARY.md` when done.
</output>

<amendment id="quick-260915-vb9-a1" source="user, mid-run">
**Play / Shuffle must populate Up Next — this is the point of the buttons, not a side effect.**

After tapping Play (or Shuffle) on a tab, the NowPlaying "Up Next" list must show the rest of
that tab's songs in order (Play) or in the shuffled order (Shuffle), with the first track
playing. This is the DEFAULT behaviour for both buttons — no setting, no long-press variant.

Executor MUST verify this explicitly, not assume it:
- `playList()` routes through `player.setListQueue(list, ctx)` then `player.play(first, { fresh: true })`.
- Check `upNextAnchorUid` handling in `player.svelte.ts` — a stale anchor from a previous queue
  can slice Up Next to the wrong window (known pitfall, quick-260712-hm9). If `setListQueue`
  does not already reset/clear the anchor for the new queue, that reset is IN SCOPE for this task.
- Verification is behavioural: tap Play on Liked, confirm the Up Next pane lists liked tracks
  2..N in order; tap Shuffle, confirm Up Next lists the same set in a different order. A green
  `pnpm test` alone does NOT satisfy this must-have.
</amendment>
