---
quick_id: 260712-0md
slug: charts-top-row-swipe-left-play-next-was-
date: 2026-07-11
status: ready
---

# Quick Task 260712-0md: charts/top song-row swipe-left = play next (was like)

## Problem

`charts/top/+page.svelte` (Top Hits view) is the LAST track list still binding
row swipe-left to a **like** action (`swipeLike` → `library.toggleLike`, `Heart`
`reveal-left`, `likedRows` state). Every sibling — search, library, artist,
album, charts/countries, charts/tags, NowPlaying up-next — uses swipe-left =
**play next**. Structurally identical to the pre-fix countries/tags (260711-t51).

## Fix (single file, mirrors 260711-t51)

1. Import line 4: `{ ListEnd, Heart, ChevronLeft }` → `{ ListEnd, ListStart, ChevronLeft }`
   (drop `Heart`, add `ListStart`; keep `ChevronLeft` — the back button).
2. Remove `import { library }` (line 19) — used only by the row like-path
   (charts/top has NO album-level like, unlike album/[name]).
3. Remove `likedRows` state + its WR-04 comment.
4. Replace `swipeLike(it)` with `swipeNext(it)`: resolve the stub, then
   `player.playNext(tr)` + `haptics.tick()` + `toast.show(t('toast.playingNext'))`.
   Keep the WR-03 in-flight guard, key `n:${rowKey(it)}`.
5. Markup: remove `{@const liked}`; left reveal `<Heart>` + `class:on={liked}` →
   `<ListStart size={20} />` (no `.on`).
6. Binding: `onSwipeLeft: () => swipeLike(it)` → `onSwipeLeft: () => swipeNext(it)`.
7. CSS: remove the now-dead `.reveal-left.on { ... }` rule.

**Out of scope (another session owns it):** header, `.head` layout, the
ChevronLeft back button, and the `.reveal-*` background styling. Only the
like→next swap.

## Tasks

- [ ] T1: Convert charts/top Top-Hits row swipe-left → play next

## Verify

- `pnpm check` passes (0 errors) — confirms `library`/`Heart`/`likedRows`/`liked`
  fully removed and the playNext path compiles.
- Row swipe-left reveal shows `ListStart`, fires `player.playNext` + "Playing next".
- NOTE: grep is malfunctioning on this file in-shell (returns empty for matching
  patterns — binary/NUL suppression). Verify with Read/sed, NOT grep.

## Done

charts/top row swipe-left = play next. swipe-left = play next now consistent across
EVERY track list in the app. Artist-tab rows (no swipe) untouched.
