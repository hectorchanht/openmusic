---
quick_id: 260704-3ac
slug: delete-orphan-spike-route-dead-phase-1-a
status: complete
subsystem: routes / dead-code
source: optimization backlog #6 (.planning/codebase/CONCERNS.md)
---

# Quick Task 260704-3ac: Delete orphan `/spike` route

## Objective

Remove the dead `/spike` route — a Phase-1 audio-engine / egress-spike test harness
(`src/routes/spike/+page.svelte`, 277 lines) that is not linked from any nav or router.
Optimization backlog item #6 from the 2026-07-04 codebase remap.

## Verified before deletion

- `grep -rn "/spike" src` → the ONLY reference is a comment in `src/routes/+layout.svelte:17`
  ("playback persists across page changes (incl. into /spike)"). No `<a href>`, no nav entry,
  no test, no import. Reachable only by manually typing `/spike`.

## Tasks

1. **Delete the route + fix the stale comment.**
   - `git rm -r src/routes/spike/`.
   - Trim the `(incl. into /spike)` clause from the root-layout audio-persistence comment
     (`src/routes/+layout.svelte`) — the point (audio persists across route-group nav) stays.
   - Verify: `pnpm check` (svelte-check) → 0/0.

## Out of scope

- The 74 stale `index.html` references in `CLAUDE.md` / `AGENTS.md` (backlog #4) — handled
  separately via `/gsd:docs-update` (those are GSD-managed generated files regenerated from
  `.planning/codebase/`, not hand-edited).
