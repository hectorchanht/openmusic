---
phase: quick-260919-oc6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/library-tabs.ts
  - src/lib/services/library-tabs.test.ts
  - src/routes/(app)/library/+page.svelte
  - src/routes/(app)/+layout.svelte
  - src/lib/components/NowPlaying.svelte
  - src/lib/components/Nowbar.svelte
autonomous: true
requirements: [QUICK-260919-OC6]

must_haves:
  truths:
    - "At >=1024px the rail lists Home, Search, Liked, Playlists, Downloads, Favourite artists, History, Settings — the five library entries link to /library?tab=<id>."
    - "At <1024px the bottom bar still renders exactly three tabs (Home, Search, Library) and their active-state logic is byte-for-byte the same expression it was."
    - "Clicking a library rail entry while already on /library switches the page's tab without a remount, and exactly ONE library rail entry is lit (never zero, never two) — including after the page's own pill row is used."
    - "?playlist beats ?tab beats the stored tab (openmusic:library:tab); a garbage ?tab= never reaches state (T-23-10)."
    - "Opening NowPlaying at <1024px still removes the nav from render (display:none = no box, no paint, no tab order, no a11y node) for the whole fly-in; at >=1024px the rail stays mounted and the .np overlay starts at var(--rail-w) so it never covers the rail."
  artifacts:
    - path: "src/lib/services/library-tabs.ts"
      provides: "LibraryTab / LIBRARY_TAB_SET / DEFAULT_LIBRARY_TAB + pure navActive(url, href)"
      exports: ["LibraryTab", "LIBRARY_TAB_SET", "DEFAULT_LIBRARY_TAB", "navActive"]
    - path: "src/lib/services/library-tabs.test.ts"
      provides: "node tests pinning navActive: tab-less hrefs behave exactly like the old inline expression; tabbed hrefs compare ?tab= via pickTab with the liked default"
    - path: "src/routes/(app)/library/+page.svelte"
      provides: "$effect that follows page.url (pickTab precedence preserved) + goto-based URL write so page.url stays truthful"
      contains: "navActive"
    - path: "src/routes/(app)/+layout.svelte"
      provides: "five desktopOnly library rail entries, mobileOnly generic Library entry, class:np-open nav gate"
      contains: "np-open"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: ".np left offset past the rail at >=1024px"
      contains: "left: var(--rail-w)"
  key_links:
    - from: "src/routes/(app)/+layout.svelte"
      to: "src/lib/services/library-tabs.ts"
      via: "navActive(page.url, tab.href) replaces the inline pathname match"
      pattern: "navActive\\(page\\.url"
    - from: "src/routes/(app)/library/+page.svelte"
      to: "src/lib/services/url-tab.ts"
      via: "pickTab read in the $effect, tabHref for the goto write"
      pattern: "pickTab\\(url|tabHref\\("
    - from: "src/routes/(app)/library/+page.svelte"
      to: "$app/navigation"
      via: "goto(href, { replaceState: true, noScroll: true, keepFocus: true }) — the write that updates page.url"
      pattern: "replaceState: true"
    - from: "src/lib/components/NowPlaying.svelte"
      to: "src/app.css"
      via: "var(--rail-w) inside @media (min-width: 1024px)"
      pattern: "left: var\\(--rail-w\\)"
---

<objective>
Desktop rail, two changes: (1) the five library tabs become rail entries riding on the EXISTING
`?tab=` mechanism (`pickTab`/`tabHref`, `openmusic:library:tab`), with exactly one lit at a time;
(2) the NowPlaying overlay no longer takes the rail away at >=1024px, while the mobile fix from
quick-260919-npfix (nav is ABSENT during the translucent fly-in) is kept as absence, not paint order.

Purpose: the rail is the desktop's only navigation; a single "Library" entry hides four
destinations behind a second click, and losing the rail whenever a song opens breaks the
keep-the-rail model the codebase already cites.
Output: one new pure module + test, edits to the library page, the app layout, NowPlaying CSS.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@src/routes/(app)/+layout.svelte
@src/routes/(app)/library/+page.svelte
@src/lib/services/url-tab.ts
@src/lib/services/url-tab.test.ts
@src/lib/styles/breakpoints.test.ts

<interfaces>
<!-- Existing contracts. Use directly. -->

From src/lib/services/url-tab.ts (pure, keep unchanged):
```ts
export function pickTab<T extends string>(url: URL, param: string, valid: ReadonlySet<string>, fallback: T): T;
export function tabHref(url: URL, param: string, value: string, defaultValue: string): string; // copies url; deletes param when value===default (D-5)
export function syncTabUrl(url: URL, param: string, value: string, defaultValue: string): void; // RAW history.replaceState — does NOT update $app/state page.url
```

From src/routes/(app)/+layout.svelte:287 (the ONLY nav definition — rail and bottom bar are the same <nav>):
```ts
const tabs: { href: string; labelKey: TranslationKey; icon: Component; desktopOnly?: boolean }[] = [
	{ href: '/', labelKey: 'nav.home', icon: House },
	{ href: '/search', labelKey: 'nav.search', icon: Search },
	{ href: '/library', labelKey: 'nav.library', icon: Library },
	{ href: '/settings', labelKey: 'home.settings', icon: Settings, desktopOnly: true }
];
// line 380, the match to replace:
{@const active = page.url.pathname === tab.href || page.url.pathname.startsWith(tab.href + '/')}
// lines 373/399: {#if !player.expanded} <nav class="tabbar">…</nav> {/if}   ← the npfix gate
// line 459 (mobile cascade): .tab.desktop-only { display: none; }   line 591 (desktop block): .tab.desktop-only { display: flex; }
```

From src/routes/(app)/library/+page.svelte:34-91:
```ts
type Tab = 'liked' | 'playlists' | 'downloads' | 'fav-artists' | 'history';
const VALID_TABS: ReadonlySet<Tab> = new Set([...]);
const TAB_KEY = 'openmusic:library:tab';
function loadInitialTab(): Tab;          // ?playlist > ?tab > stored > 'liked'; runs ONCE at init
function loadInitialPlaylist(): string | null;
let detailPlaylistId = $state<string | null>(loadInitialPlaylist());
let tab = $state<Tab>(loadInitialTab());
function setTab(v: Tab) { tab = v; detailPlaylistId = null; localStorage.setItem(TAB_KEY, v); syncTabUrl(page.url, 'tab', v, 'liked'); }
// i18n keys already present in en.ts (all 16 locales): library.liked, library.playlists, library.downloads, library.favArtists, history.heading — NO new keys needed.
```

From src/lib/components/NowPlaying.svelte:
```
line 62-68: comment — the 1280px three-up breakpoint reasoning assumes ".np is fixed inset:0 over the rail … full viewport width"
line 847-855: <section class="np" transition:fly={{ y: 600, duration: 320 }} use:focusTrap> (transform-only inline styles; inset is NOT set inline on .np)
line 1175: .np { position: fixed; inset: 0; z-index: 50; … }
only @media in file: prefers-reduced-motion (no width breakpoint in CSS; the 1280 one is JS)
```

From src/lib/components/Nowbar.svelte:655-662: `@media (min-width: 1024px) { .nowbar:not(.embed) { left: var(--rail-w); … } }` and a comment saying the sheet "still covers the rail. That is a deliberate omission".

Token: `--rail-w: 88px` in src/app.css. Breakpoint guard: src/lib/styles/breakpoints.test.ts only allows 640 and 1024.
Z ladder: nowbar 20, tabbar/rail 21, .np 50, toast 90.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure library-tabs module — shared constants + navActive(url, href)</name>
  <files>src/lib/services/library-tabs.ts, src/lib/services/library-tabs.test.ts</files>
  <behavior>
    Tab-less hrefs are the OLD inline expression, verbatim (pins the three mobile tabs + Settings unchanged):
    - navActive(u('https://x/'), '/') → true; navActive(u('https://x/search'), '/') → false ('/' can never match '//')
    - navActive(u('https://x/search'), '/search') → true; navActive(u('https://x/library'), '/search') → false
    - navActive(u('https://x/library?tab=downloads'), '/library') → true (generic Library lights on ANY library tab — mobile behaviour)
    - navActive(u('https://x/settings/general'), '/settings') → true (subpath match kept); navActive(u('https://x/settingsx'), '/settings') → false
    Tabbed hrefs compare ?tab= through pickTab with the liked default:
    - navActive(u('https://x/library?tab=downloads'), '/library?tab=downloads') → true; same url with '/library?tab=liked' → false
    - navActive(u('https://x/library'), '/library?tab=liked') → true (D-5: the default is omitted from the canonical URL)
    - navActive(u('https://x/library?tab=nope'), '/library?tab=liked') → true and '/library?tab=nope' → false (T-23-10 garbage → default; never lights a bogus entry)
    - navActive(u('https://x/library?playlist=abc&tab=playlists'), '/library?tab=playlists') → true (other params ignored)
    - navActive(u('https://x/search?tab=liked'), '/library?tab=liked') → false (pathname gate runs first)
    - exactly-one invariant: for each url in ['/library', '/library?tab=liked', '/library?tab=history', '/library?tab=zzz'], mapping navActive over the five tabbed hrefs yields exactly one true
    - never throws on a malformed url-like object (mirror url-tab.test.ts's `{ searchParams: null } as unknown as URL` case) → false
  </behavior>
  <action>
    Create `src/lib/services/library-tabs.ts` (pure `.ts`, no runes, no `$app` imports except none — import `pickTab` from `./url-tab`). Header comment tagged `quick-260919-oc6` explaining: the library tab allowlist and its default used to live only inside `library/+page.svelte`; the desktop rail now needs the same allowlist + default to decide which of five `/library?tab=` entries is lit, so they move here ONCE (same rung-2 reasoning as url-tab.ts: the thing being avoided is a runes store, not a shared helper).

    Export:
    - `export type LibraryTab = 'liked' | 'playlists' | 'downloads' | 'fav-artists' | 'history';`
    - `export const LIBRARY_TAB_SET: ReadonlySet<LibraryTab> = new Set<LibraryTab>([...five in that order]);`
    - `export const DEFAULT_LIBRARY_TAB: LibraryTab = 'liked';` (the D-5 default that `tabHref` strips from the URL)
    - `export function navActive(url: URL, href: string): boolean` — wrapped in try/catch returning false. Resolve `const target = new URL(href, url.origin)`. Pathname gate: `url.pathname === target.pathname || url.pathname.startsWith(target.pathname + '/')` — this MUST be the exact expression from `+layout.svelte:380` (quote it in a comment so the "provably unchanged for the mobile tabs" claim is inspectable). If false → false. `const want = target.searchParams.get('tab')`; if `want === null` → return true (href carries no tab constraint — the generic entries). Else return `pickTab(url, 'tab', LIBRARY_TAB_SET, DEFAULT_LIBRARY_TAB) === want`. Comment why pickTab-with-default and not a raw `searchParams.get`: the canonical URL for Liked is plain `/library` (D-5), and a tampered `?tab=` must fall back to the default rather than light nothing / light a bogus entry (T-23-10, same allowlist the page uses).

    Write `library-tabs.test.ts` first (RED), in the style of `url-tab.test.ts` (`const u = (href) => new URL(href)`, `describe` per contract), covering every bullet in `<behavior>`. Then implement (GREEN). Do not add anything else to the module (no href builders, no icons, no labels — YAGNI).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm vitest --run src/lib/services/library-tabs.test.ts</automated>
  </verify>
  <done>Module exports the four names; all behavior cases pass; module imports nothing from `$app/*` or runes stores.</done>
</task>

<task type="auto">
  <name>Task 2: Library page follows page.url reactively and writes the URL through the router</name>
  <files>src/routes/(app)/library/+page.svelte</files>
  <action>
    Root defect to fix (task_detail's "single most likely defect"): `loadInitialTab()` runs once at init, and a SvelteKit client-side nav `/library?tab=liked` → `/library?tab=downloads` is the SAME route, so the component is NOT remounted and `tab` never changes. Confirmed by reading: nothing in the page reads `page.url` after init.

    Second, related defect the rail exposes: `syncTabUrl` writes the address bar with RAW `history.replaceState`, which SvelteKit never observes, so `$app/state` `page.url` goes STALE after every pill tap. The rail's active state (Task 3) reads `page.url`; with a stale `page.url` the rail would light the wrong library entry as soon as the in-page pill row is used. So the library's URL write must go through the router. This is NOT a new mechanism: same `tab` param, same `tabHref` default-stripping rule (D-5), same `openmusic:library:tab` persistence, same `pickTab` allowlist. The only change is WHICH replaceState performs the write. `goto(href, { replaceState: true, noScroll: true, keepFocus: true })` is a normal router navigation, not the `$app/navigation` shallow-routing `replaceState` that url-tab.ts / overlays.svelte.ts warn about (those desync the router's history index; goto owns the index). No history entry is added, so Back still leaves the page and the overlay-depth == history-depth invariant is untouched. Precedent for goto-with-replaceState on mount already exists in `(app)/+layout.svelte:181`. The artist albums page keeps using `syncTabUrl` (no rail entry reads its URL); leave url-tab.ts unchanged.

    Edits, all tagged `quick-260919-oc6` in comments:
    1. Imports: replace `import { pickTab, syncTabUrl } from '$lib/services/url-tab'` with `import { pickTab, tabHref } from '$lib/services/url-tab'`; add `import { LIBRARY_TAB_SET, DEFAULT_LIBRARY_TAB, type LibraryTab } from '$lib/services/library-tabs'`. Replace the local `type Tab = …` with `type Tab = LibraryTab;` and `const VALID_TABS: ReadonlySet<Tab> = …` with `const VALID_TABS = LIBRARY_TAB_SET;` (keep the names so the ~20 downstream `tab === …` / `VALID_TABS.has` sites are untouched). Keep `loadInitialTab()` / `loadInitialPlaylist()` and the `$state` seeds EXACTLY as they are — frame 1 must still be synchronous and correct.
    2. Add a plain (non-`$state`) field `let appliedSearch: string | null = null;` — the "URL I already reconciled" marker (plain field per the store convention: the UI never reads it).
    3. Add `function writeTabUrl(v: Tab)`: `const href = tabHref(page.url, 'tab', v, DEFAULT_LIBRARY_TAB); if (href === page.url.href) return; appliedSearch = new URL(href).search; void goto(href, { replaceState: true, noScroll: true, keepFocus: true });`. Comment: `appliedSearch` is set BEFORE the goto so the effect below recognises its own echo and does not re-apply.
    4. In `setTab`, replace the `syncTabUrl(page.url, 'tab', v, 'liked')` line with `writeTabUrl(v)`; update the 2jo comment above it (keep it, append the oc6 reason: page.url must stay truthful because the rail reads it).
    5. Add ONE `$effect` (after `setTab`):
       - `const url = page.url;` is the only tracked read; everything else inside `untrack(() => { … })`.
       - `if (url.search === appliedSearch) return; appliedSearch = url.search;`
       - `const pid = loadInitialPlaylist(); const qp = pickTab(url, 'tab', VALID_TABS, '' as Tab | '');`
       - Precedence exactly as `loadInitialTab()` documents: `if (pid) { detailPlaylistId = pid; tab = 'playlists'; } else if (qp) { detailPlaylistId = null; tab = qp; }` — else (plain `/library`, no usable param) leave `tab` alone: the stored tab keeps winning, T-23-10 garbage falls through to it.
       - `if (pid || qp) try { localStorage.setItem(TAB_KEY, tab); } catch { /* quota */ }` — a URL-driven switch (rail click) persists like a pill tap; nothing else can tell them apart.
       - Finally `writeTabUrl(tab);` — publishes the EFFECTIVE tab back into `page.url` so the rail agrees with the page. This is what makes plain `/library` with a stored `history` tab light History, makes `/library?playlist=X` light Playlists (URL becomes `?playlist=X&tab=playlists`, deep link preserved by tabHref), strips `?tab=liked` back to the canonical `/library` (D-5), and sanitises a garbage `?tab=`. Idempotent: `writeTabUrl` early-returns when the href already equals `page.url.href`, and its echo is skipped by `appliedSearch`.
       - First run at mount: `appliedSearch` is null so the effect reconciles once; for pid/qp URLs it recomputes the same value `loadInitialTab()` already seeded (idempotent), so frame 1 is unchanged.
       Comment block on the effect: why `untrack` (a `library.playlists` edit or a `tab` write must not re-run URL parsing), why the marker is compared on `.search` only (pathname is constant while mounted), and the loop-safety argument (goto → page.url → effect → search === appliedSearch → return).
    6. Do NOT touch the pill row markup, `tabLabel`, `editMode`, or anything else.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -5 && grep -c 'syncTabUrl' 'src/routes/(app)/library/+page.svelte' | grep -qx 0 && grep -n 'replaceState: true' 'src/routes/(app)/library/+page.svelte'</automated>
  </verify>
  <done>svelte-check passes; `syncTabUrl` no longer referenced by the library page; `writeTabUrl` uses goto with replaceState; one `$effect` tracks only `page.url`; `loadInitialTab`/`loadInitialPlaylist` bodies unchanged; localStorage key and T-23-10 fallback intact.</done>
</task>

<task type="auto">
  <name>Task 3: Rail entries + navActive + mobile-only display gate; .np insets past the rail</name>
  <files>src/routes/(app)/+layout.svelte, src/lib/components/NowPlaying.svelte, src/lib/components/Nowbar.svelte</files>
  <action>
    All comments tagged `quick-260919-oc6`. Keep every existing decision-ref comment; extend, never delete.

    A. `+layout.svelte` — the `tabs` array (line 287):
    - Add `mobileOnly?: boolean` to the element type. Mark the generic `{ href: '/library', … }` entry `mobileOnly: true` (discretion call: "spread the five into the rail" means the single entry is REPLACED by five at desktop; keeping both would light two entries on every library URL and duplicate a destination. Reverting is deleting one flag). Mobile still renders exactly Home/Search/Library — the flag only gains a display rule inside the desktop media block.
    - Insert, between the generic Library entry and Settings, five `desktopOnly: true` entries: `/library?tab=liked` → `library.liked` / `Heart`; `/library?tab=playlists` → `library.playlists` / `ListMusic`; `/library?tab=downloads` → `library.downloads` / `Download`; `/library?tab=fav-artists` → `library.favArtists` / `Users`; `/library?tab=history` → `history.heading` / `Clock`. Import those five icons from `@lucide/svelte` (same glyphs the page's pill row uses). All labels are EXISTING TranslationKeys — no i18n edits, and the `{#each … (tab.href)}` key stays valid because all hrefs are unique. Comment: Liked's href carries an explicit `?tab=liked` even though D-5 strips it from the canonical URL, because a plain `/library` means "the stored tab" (mobile Library tab, home links) and could not express "Liked" when the stored tab is something else; the page's `writeTabUrl` reconciles the URL back to `/library` immediately.
    - Replace the `{@const active = …}` expression at line 380 with `{@const active = navActive(page.url, tab.href)}`; import `navActive` from `$lib/services/library-tabs`. Update the comment above it: for tab-less hrefs navActive IS the previous expression (pinned by library-tabs.test.ts), for `?tab=` hrefs it also compares the tab via `pickTab` with the liked default — hence exactly one library entry lit.
    - Add `class:mobile-only={tab.mobileOnly}` next to `class:desktop-only`.
    - CSS, INSIDE the existing `@media (min-width: 1024px)` block only: `.tab.mobile-only { display: none; }` (mirror of `.tab.desktop-only { display: flex; }` there). Nothing added to the mobile cascade for this.

    B. `+layout.svelte` — the NowPlaying gate (lines 352-399):
    - Change `{#if !player.expanded} <nav class="tabbar"> … </nav> {/if}` to an always-mounted `<nav class="tabbar" class:np-open={player.expanded}>` (drop the `{#if}`/`{/if}` pair around the nav only; the `<Nowbar />` gate six lines up is unchanged).
    - Mobile cascade, right after `.tabbar { … }`: `.tabbar.np-open { display: none; }`.
    - Desktop block: `.tabbar.np-open { display: flex; }`.
    - Extend the `quick-260919-npfix (Fix 1)` comment — keep every sentence, append an oc6 paragraph: the reproduced failure was a TRANSPARENT occluder, so paint-order fixes (z-index, opacity) cannot work and absence is still the mechanism here; `display:none` IS absence — no box is generated, nothing paints, the nav is out of the tab order and the accessibility tree, exactly what the `{#if}` achieved and exactly what focusTrap wants — the difference is only that a class can be opted back to `display:flex` inside the one `@media (min-width: 1024px)` block, which keeps CSS the single source of truth for the breakpoint (the `.tab.desktop-only` / `<ShelfChevrons />` convention) and needs NO `matchMedia` in JS. On desktop the rail therefore stays mounted while `.np` is open, and `.np` is inset past it (NowPlaying.svelte), so the two never overlap — the z ladder (nowbar 20 / rail 21 / .np 50 / toast 90) is unchanged. Also note the known ceiling (ponytail): a rail click while NowPlaying is open navigates UNDERNEATH the open sheet and does not collapse it; the sheet's focusTrap keeps keyboard focus inside `.np`, so the rail is pointer-reachable only while open. Not in scope here; collapse-on-rail-click is a one-line `player.collapse()` if wanted later, but its interaction with the overlays history sentinel needs its own look.

    C. `NowPlaying.svelte` styles: add, near the `.np` rule (line ~1175), `@media (min-width: 1024px) { .np { left: var(--rail-w); } }` with a comment: at desktop the rail stays mounted (layout `class:np-open`), so the sheet starts where the rail ends — same token and same rule shape as `.nowbar:not(.embed)` in Nowbar.svelte. `inset: 0` on `.np` is stylesheet-only (inline styles on the section are transform/transition), so this longhand override wins in the cascade; the sheet's `style:inset` is on the inner `.sheet`, unaffected. `1024` is an allowed rung (breakpoints.test.ts). Then amend the `quick-260919-np3` comment at line 62-68: `.np` is no longer full-viewport at >=1024 — it is `100vw - var(--rail-w)`; at 1280 the three-up split is now ~326/457/326 instead of 355/497/355, still well above the 278 "cramped" measurement, so the 1280 rung stands. Do not change `WIDE_MQ` or any markup.

    D. `Nowbar.svelte` comment at ~655-658 ("still covers the rail. That is a deliberate omission"): append one oc6 sentence — the sheet now insets past the rail and the rail stays mounted at desktop; the deliberate omission that remains is the desktop-specific transport layout / NowPlaying split. Comment-only edit.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -5 && pnpm test 2>&1 | tail -8 && grep -c 'desktopOnly: true' 'src/routes/(app)/+layout.svelte' | grep -qx 6 && grep -n 'left: var(--rail-w)' src/lib/components/NowPlaying.svelte && ! grep -n '{#if !player.expanded}' 'src/routes/(app)/+layout.svelte' | grep -q tabbar</automated>
    <human-check>Desktop (>=1024px): rail shows Home, Search, Liked, Playlists, Downloads, Favourite artists, History, Settings — no generic Library. Click Downloads then History: page tab switches in place, URL shows ?tab=…, exactly one library entry lit. Tap the in-page pill row (e.g. Liked): rail follows, URL drops ?tab (canonical /library). Reload on /library with a stored tab: that tab is lit in the rail. Open NowPlaying: rail stays visible, sheet starts at the rail's right edge. Mobile (375px): bottom bar = exactly Home/Search/Library; open NowPlaying: bar disappears for the whole fly-in (no leak). Playlist deep link /library?playlist=<id> still pins the detail view and lights Playlists.</human-check>
  </verify>
  <done>`pnpm check` and `pnpm test` (incl. breakpoints.test.ts and the new library-tabs tests) pass; rail has six desktopOnly entries (five library + Settings) and one mobileOnly; nav is always mounted with `class:np-open`; `.tabbar.np-open` is display:none below 1024 and display:flex at/above; `.np` has `left: var(--rail-w)` at >=1024; npfix/np3/Nowbar comments extended, none deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → component state | `?tab=` / `?playlist=` are attacker-controllable query strings that now drive state on EVERY navigation, not just mount |
| rail href → router | hrefs are static literals in the layout; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-oc6-01 | Tampering | library/+page.svelte $effect | mitigate | every `?tab=` read goes through `pickTab` against `LIBRARY_TAB_SET`; `?playlist` validated against existing ids (unchanged T-23-10 path); garbage → stored/default, never thrown |
| T-oc6-02 | Denial of Service | writeTabUrl ↔ $effect loop | mitigate | `appliedSearch` marker set before goto + `href === page.url.href` early return; loop-safety argued in code comment; goto is replaceState so no history growth |
| T-oc6-03 | Information Disclosure | navActive(url, href) | accept | reads pathname/search of the app's own URL only; no external data |
| T-oc6-SC | Tampering | npm installs | accept | no new dependencies in this task |
</threat_model>

<verification>
- `pnpm vitest --run src/lib/services/library-tabs.test.ts` — navActive contract incl. the "exactly one lit" invariant and the verbatim old-expression cases.
- `pnpm test` — full suite; breakpoints.test.ts must still see only 640/1024; i18n parity untouched (no key changes).
- `pnpm check` — svelte-check clean.
- Grep gates: `syncTabUrl` absent from library page; no `{#if !player.expanded}` wrapping the nav; `.tabbar.np-open` present twice (mobile none / desktop flex); `left: var(--rail-w)` in NowPlaying.
- Manual desktop + mobile pass per Task 3 human-check.
</verification>

<success_criteria>
- Desktop rail lists the five library tabs (plus Home, Search, Settings), each linking `/library?tab=<id>`, exactly one lit on any library URL, and staying correct after in-page pill taps and reloads.
- Client-side nav between two `/library?tab=` URLs switches the tab without remount; `?playlist` > `?tab` > stored precedence and `openmusic:library:tab` persistence preserved.
- Mobile bottom bar unchanged (three tabs; active expression provably identical via tests); NowPlaying still hides it as absence during the fly-in.
- Desktop rail stays mounted while NowPlaying is open and `.np` starts at `var(--rail-w)`.
- No new i18n keys, no new dependencies, no JS breakpoint added, z ladder unchanged.
</success_criteria>

<output>
Create `.planning/quick/260919-oc6-desktop-rail-library-tabs-keep-rail-visi/260919-oc6-SUMMARY.md` when done.
</output>
