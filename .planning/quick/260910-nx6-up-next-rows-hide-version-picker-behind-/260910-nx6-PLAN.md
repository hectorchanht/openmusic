---
phase: quick-260910-nx6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/NowPlaying.svelte
autonomous: true
requirements: [QUICK-260910-NX6]
must_haves:
  truths:
    - "At rest an Up Next row shows NO version-picker (Layers) button; the GripVertical grip is still visible at the row's right edge"
    - "Swipe RIGHT past threshold on any Up Next row (including the current one) reveals a Layers icon under the row and, on release, opens the VersionPicker for that track"
    - "Swipe LEFT past threshold on a non-current Up Next row reveals a Trash2 icon and, on release, removes that track from the queue"
    - "Swipe LEFT on the CURRENTLY-PLAYING row does nothing (row springs back, queue unchanged, no remove reveal shown)"
    - "Mid-swipe nothing renders through the row's title/artist text — the row is opaque (computed backgroundColor is NOT rgba(0,0,0,0), opacity is 1)"
    - "Tap-to-play, long-press track menu, skipped-row retry tap, and grip drag-to-reorder behave exactly as before"
  artifacts:
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "Up Next rows wrapped in .swipe-wrap.q-swipe with two reveal spans and use:swipeAction; .ver trigger removed; opaque .q-swipe .q-row CSS"
      contains: "q-swipe"
  key_links:
    - from: "src/lib/components/NowPlaying.svelte (.q-row)"
      to: "src/lib/actions/swipeAction.ts"
      via: "use:swipeAction={{ onSwipeRight, onSwipeLeft }}"
      pattern: "use:swipeAction=\\{\\{ onSwipeRight: \\(\\) => queueSwipeVersions"
    - from: "queueSwipeVersions()"
      to: "openVersionPicker(track)"
      via: "direct call"
      pattern: "openVersionPicker\\(track\\)"
    - from: "queueSwipeRemove()"
      to: "player.removeFromQueue(track.uid)"
      via: "direct call"
      pattern: "player\\.removeFromQueue\\(track\\.uid\\)"
---

<objective>
Hide the Up Next version-picker trigger behind a directional swipe reveal (right = version picker, left = remove), keep the grip visible, and give the row an opaque background so the reveal layers never bleed through the title — the same defect class quick-260910-k45 fixed on the Related list.

Purpose: declutter every Up Next row (one fewer 44px button) while keeping both actions one gesture away, and eliminate the confirmed grip-through-title bleed.
Output: `src/lib/components/NowPlaying.svelte` — Up Next markup + CSS only.
</objective>

<execution_context>
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/workflows/execute-plan.md
@/Users/laichan/.claude/plugins/cache/gsd-plugin/gsd/4.5.3/templates/summary.md
</execution_context>

<context>
@.planning/quick/260910-nx6-up-next-rows-hide-version-picker-behind-/260910-nx6-CONTEXT.md
@src/lib/components/NowPlaying.svelte
@src/lib/actions/swipeAction.ts

<interfaces>
<!-- Everything the executor needs. Do not explore beyond these. -->

From src/lib/actions/swipeAction.ts (already imported in NowPlaying.svelte line 41):
```typescript
export interface SwipeActionOpts {
	onSwipeRight?: () => void;   // dx > 0 commit — an undefined callback is a silent no-op
	onSwipeLeft?: () => void;    // dx < 0 commit
	threshold?: number;          // default 96
	enabled?: boolean;           // default true — GLOBAL, not per-direction
}
export const swipeAction: Action<HTMLElement, SwipeActionOpts>;
// Row ALWAYS springs back to translateX(0) after release; no opacity fade; tap-preserving;
// vertical-dominant drags go passive (grip reorder + scroll untouched); WR-01 swallows the
// trailing click after a committed drag so onclick (tap-to-play) does not double-fire.
```

Existing NowPlaying.svelte symbols to reuse (verified present):
- line 17: `import { ChevronDown, MoreVertical, Heart, SkipBack, SkipForward, Play, Pause, Repeat, Repeat1, GripVertical, Moon, ListEnd, ListStart, Layers } from '@lucide/svelte';` — `Trash2` is NOT yet imported; add it here.
- line 40: `import { swipeRemove } from '$lib/actions/swipeRemove';` — NowPlaying is the ONLY caller in `src/` (grep confirmed); remove this import. Do NOT delete `swipeRemove.ts` or its test.
- line 47: `import { tick as hapticTick } from '$lib/util/haptics';`
- line 108: `const verOpenLabel = $derived(t('versions.open'));` — becomes unused once `.ver` is removed; delete it (svelte-check warns on unused).
- line 110: `async function openVersionPicker(track: Track)` — keep; the swipe-right commit calls it.
- lines 658-667: `relatedSwipeQueue(track)` / `relatedSwipeNext(track)` — the helper shape to mirror (store call + hapticTick; toast optional).
- `player.removeFromQueue(uid)`, `player.current?.uid`, `player.isUnplayable(uid)`, `player.retryUnplayable(track)` — all already used on this list.

Reference markup (Related list, lines ~1531-1537) — copy its structure:
```svelte
<li class="swipe-wrap related-swipe">
	<span class="reveal reveal-queue" aria-hidden="true"><ListEnd size={20} /></span>
	<span class="reveal reveal-next" aria-hidden="true"><ListStart size={20} /></span>
	<button class="row rel-row" ... use:swipeAction={{ onSwipeRight: ..., onSwipeLeft: ... }}>…</button>
</li>
```
Reference CSS (lines ~1770-1777): `.related-swipe { position: relative; overflow: hidden; border-radius: 10px; }`, `.related-swipe .reveal { position:absolute; top:0; bottom:0; width:96px; display:flex; align-items:center; justify-content:center; pointer-events:none; }`, `.related-swipe .reveal-queue { left:0 }`, `.related-swipe .reveal-next { right:0 }`, `.related-swipe .row { background: var(--color-bg); position: relative; z-index: 1; }`.

Accessibility reach (confirmed, no new work needed): `TrackMenu.svelte` line 380 already exposes `t('menu.versions')` ("Play from source" → `openVersions`) and the Up Next row already opens that menu via `use:longpress`, which also fires on `contextmenu` (longpress.ts line 86) — so the version picker stays reachable without a swipe. Removal has no menu entry, but it was swipe-only before this task too (swipeRemove) — not a regression; do not add one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the .ver trigger + swipeRemove with a swipe-wrap + directional swipeAction on the Up Next row</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>
All edits tagged `quick-260910-nx6` in comments; keep every existing decision-ref comment (Gap 4 (26-10), quick-260615-i9u, Gap 3, quick-260723, T-26-10-01/02) — rewrite only the sentences that become false (e.g. "use:swipeRemove" → "use:swipeAction", "Shown on EVERY row" → "hidden at rest, revealed by swipe-right").

1. Imports (script block): add `Trash2` to the `@lucide/svelte` import on line 17. Delete line 40 (`import { swipeRemove } ...`). Delete `const verOpenLabel = $derived(t('versions.open'));` (line 108) — its only consumer goes away. `swipeAction` and `hapticTick` are already imported.

2. Helpers (place directly under `relatedSwipeNext`, ~line 667), mirroring the related helpers:
   - `function queueSwipeVersions(track: Track)` → `openVersionPicker(track); hapticTick();`
   - `function queueSwipeRemove(track: Track)` → `player.removeFromQueue(track.uid); hapticTick();`
   No toast on remove (the row disappearing is the feedback; no i18n key exists and adding one touches 16 locale files — out of scope). Comment: swipeAction is a pure DOM gesture, the host fires haptics on commit (PATTERNS.md §3.3), same as the related helpers.

3. Markup inside `{#each upNextList as track, i (track.uid)}` (lines ~1449-1490). The `<li>` keeps its `class:lifted` / `class:over` / `style:transform` and stays a flex row of exactly TWO children: the new wrapper, then the untouched `.grip-handle` button.
   - DELETE the `<button class="ver" ...>` sibling and its Gap 4 comment block; replace the comment with a quick-260910-nx6 note: the picker trigger is now the swipe-right reveal (hidden at rest per the user decision); keyboard/AT reach is the long-press/contextmenu TrackMenu "Play from source" row.
   - WRAP the `.q-row` button in `<span class="swipe-wrap q-swipe" class:is-current={track.uid === player.current?.uid}>` containing, in order: `<span class="reveal reveal-versions" aria-hidden="true"><Layers size={20} /></span>` (left edge — exposed by a RIGHT drag), `<span class="reveal reveal-remove" aria-hidden="true"><Trash2 size={20} /></span>` (right edge — exposed by a LEFT drag), then the `.q-row` button. Use a `<span>`, not a `<div>`, so the li stays a valid flex row of inline-level children and, critically, the button is still NOT nested in another button (the Gap 4 no-button-in-button constraint holds — the wrapper is a span).
   - On the `.q-row` button replace `use:swipeRemove={{ onremove: ..., enabled: track.uid !== player.current?.uid }}` with `use:swipeAction={{ onSwipeRight: () => queueSwipeVersions(track), onSwipeLeft: track.uid === player.current?.uid ? undefined : () => queueSwipeRemove(track) }}`. This is how the locked "swipe-left is a no-op on the current row" is preserved: swipeAction's `enabled` is global (it would also kill swipe-right, which the user wants available on the current row), so the LEFT callback is simply undefined for the current row — swipeAction treats an undefined callback as a no-op and springs back. Comment this rationale inline. Keep `class:playing`, `class:skipped`, `use:longpress`/`onlongpress`, the `onclick` skipped-retry/play branch, `title`, `.q-art`, `.q-text`, `<RowBadges>` byte-for-byte.
   - The `.grip-handle` button and its pointer handlers are untouched and remain OUTSIDE the wrapper (grip stays visible at rest and never slides with the row).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -5 && grep -c 'swipeRemove' src/lib/components/NowPlaying.svelte | grep -qx 0 && grep -c 'class="ver"' src/lib/components/NowPlaying.svelte | grep -qx 0 && grep -q 'swipe-wrap q-swipe' src/lib/components/NowPlaying.svelte && echo MARKUP-OK</automated>
  </verify>
  <done>`pnpm check` reports 0 errors and no new warnings (no unused `verOpenLabel`/`swipeRemove`/`Layers`), NowPlaying.svelte has zero `swipeRemove` references and no `.ver` button, the Up Next `.q-row` sits inside `.swipe-wrap.q-swipe` with two aria-hidden reveal spans and `use:swipeAction`, and the grip button is unchanged outside the wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Up Next swipe CSS — reveal layers, opaque row, and the three translucency leaks</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>
In the `<style>` block, in the Up Next region (~lines 1779-1812). Tag with `quick-260910-nx6`. Do NOT edit the generic `.row`, `.related-swipe*`, `.row.skel`, or `.row.rel-row` rules.

1. DELETE the `.ver` rule and its `@media (hover: hover) { .ver:hover ... }` rule plus the Gap 4 comment above them (the element no longer exists). Rewrite the stale sentence in the `.related-swipe` comment "The Up-Next list (use:swipeRemove) is untouched" → "The Up-Next list gets its own `.q-swipe` variant (quick-260910-nx6)".

2. ADD, placed BEFORE the `@media (hover: hover) { .row:hover ... }` rule (same position the `.related-swipe` block occupies, so the equal-specificity `.row:hover` still wins on hover-capable devices):
   - `.q-swipe { position: relative; overflow: hidden; border-radius: 10px; flex: 1; min-width: 0; }` — `flex:1; min-width:0` is what search/+page.svelte's `.swipe-wrap` uses to fill the li beside a sibling button; the old `.q-row { flex: 1; min-width: 0 }` now applies inside the wrapper where `.row { width: 100% }` sizes it.
   - `.q-swipe .reveal { position: absolute; top: 0; bottom: 0; width: 96px; display: flex; align-items: center; justify-content: center; color: var(--color-text-muted); pointer-events: none; }`
   - `.q-swipe .reveal-versions { left: 0; }` and `.q-swipe .reveal-remove { right: 0; }`
   - `.q-swipe.is-current .reveal-remove { display: none; }` — the current row cannot be removed (swipe-left is a no-op there), so never show a trash icon that does nothing.
   - `.q-swipe .q-row { background: var(--color-bg); position: relative; z-index: 1; }` — the k45 root-cause fix: z-index orders, only an opaque background occludes. `--color-bg` matches `.np` so the row looks unchanged.

3. FIX the three rules that would re-introduce bleed by making the row translucent again (each verified in the current CSS):
   - `.row.playing { background: rgba(124,92,255,0.15); }` is translucent and, being later in the cascade at equal specificity, would beat the opaque rule on the playing row. Add (after `.row.playing`): `.q-swipe .q-row.playing { background: linear-gradient(rgba(124,92,255,0.15), rgba(124,92,255,0.15)) var(--color-bg); }` — same tint composited over the opaque bg. Leave `.row.playing` itself alone (the Related list has no playing row but the rule is generic).
   - `.q-row.skipped { opacity: 0.45; }` makes the whole row see-through at rest. Change it to `.q-row.skipped > * { opacity: 0.45; }` so the art, text and badges dim while the button's own background stays opaque. Update the i9u comment sentence accordingly.
   - `.list li.lifted .q-row { background: var(--color-surface); box-shadow: 0 6px 18px rgba(0,0,0,0.4); }` — the wrapper's `overflow: hidden` would now clip that drop shadow during a grip drag. Move ONLY the `box-shadow` to a new `.list li.lifted .q-swipe { box-shadow: 0 6px 18px rgba(0,0,0,0.4); }` rule and keep the background on `.q-row`. `.list li.over .q-row { box-shadow: inset ... }` is inset and unaffected — leave it.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -3 && pnpm test 2>&1 | tail -6 && grep -c '\.ver\b' src/lib/components/NowPlaying.svelte | grep -qx 0 && grep -q '\.q-swipe \.q-row { background: var(--color-bg)' src/lib/components/NowPlaying.svelte && grep -q '\.q-row\.skipped > \*' src/lib/components/NowPlaying.svelte && echo CSS-OK</automated>
  </verify>
  <done>`pnpm check` clean, `pnpm test` fully green (swipeAction.test.ts 15 + swipeRemove.test.ts 11 among them, untouched), no `.ver` selector remains, and the `.q-swipe` block, opaque `.q-swipe .q-row`, playing-tint composite, child-scoped skipped opacity, and lifted shadow on `.q-swipe` are all present.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioural verification in the running app</name>
  <files>(none — verification only)</files>
  <action>
Start the dev server if not running (`pnpm dev`, port 4321 per launch.json or 5173 bare — probe, do not assume). Open the app, start playback of any track so the queue has 3+ rows, expand Now Playing, select the Up Next tab. Drive the checks with the browser tool available to you (synthetic PointerEvents against a `.q-row`, exactly as the pre-task probe did: pointerdown at x, pointermove to x+120 over 3-4 steps with pointerType 'touch', pointerup), reading computed styles via `getComputedStyle`:

1. AT REST — `document.querySelectorAll('.q-row').length >= 3`; `document.querySelector('.ver') === null`; every `.grip-handle` is visible (`getBoundingClientRect().width > 0`); for a `.q-row`: `getComputedStyle(el).backgroundColor` is NOT `rgba(0, 0, 0, 0)` and `opacity === '1'`; the `.reveal` spans have `getBoundingClientRect().width === 96` but are fully covered (take a screenshot — no Layers/Trash icon visible at rest).
2. MID-SWIPE — after pointerdown + moves to +80px WITHOUT pointerup, the `.q-row` has `transform: matrix(1, 0, 0, 1, 80, 0)`, `opacity: 1`, opaque backgroundColor; screenshot shows the Layers icon in the exposed left strip and NOTHING rendering through the row's title text (the pre-task defect). Then pointerup → row springs back.
3. SWIPE RIGHT COMMIT — full drag of +130px and release on a NON-current row → the VersionPicker sheet opens (`overlayId="versionpicker-page"` element present / `pickerOpen`); close it. Repeat on the CURRENT (`.q-row.playing`) row → picker also opens (swipe-right stays available on the current row per the locked decision).
4. SWIPE LEFT COMMIT — count rows, full drag of -130px + release on a non-current row → row count decreases by 1 and that uid is gone. Also assert `getComputedStyle(document.querySelector('.q-swipe.is-current .reveal-remove')).display === 'none'`.
5. SWIPE LEFT ON CURRENT ROW — same drag on `.q-row.playing` → row count UNCHANGED, `player.current` unchanged, row back at `transform: none`/`matrix(1,0,0,1,0,0)`.
6. REGRESSIONS — a plain click on a non-current `.q-row` plays it (current changes); a long-press (pointerdown, wait ~600ms, pointerup) or `contextmenu` on a row opens TrackMenu and it lists the "Play from source" item; a vertical pointer drag on a `.grip-handle` reorders (row order changes) with no horizontal slide of the row; if any `.q-row.skipped` exists, its children are dimmed while its own background stays opaque, and clicking it calls the retry branch (no throw).
Record the observed values (not "looks fine") in the SUMMARY.
  </action>
  <verify>
    <human-check>Screenshots from steps 1 and 2 show: no Layers button at rest, grip visible, and no grip/icon bleeding through the title at translateX(80px). Steps 3-6 observed values recorded.</human-check>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -2 && pnpm test 2>&1 | grep -E 'Tests|Test Files'</automated>
  </verify>
  <done>All six behavioural checks pass with recorded values; `pnpm check` and `pnpm test` green. No deploy, push, or APK build is performed (pushes to main auto-deploy production).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input → row action | Untrusted gesture input decides remove / open-picker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-nx6-01 | Tampering | swipe-left on the current row | mitigate | `onSwipeLeft` is `undefined` when `track.uid === player.current?.uid`; the playing track can never be removed by gesture (preserves the swipeRemove `enabled:` gate) |
| T-nx6-02 | Denial of Service | committed swipe + trailing click | mitigate | swipeAction WR-01 click suppression (inherited, unchanged) prevents remove/open + immediate tap-to-play double-fire |
| T-nx6-03 | Information Disclosure | reveal spans behind row | accept | Purely decorative icons, `aria-hidden`; opaque row prevents visual bleed |
| T-nx6-SC | Tampering | npm installs | accept | No new dependencies in this task |
</threat_model>

<verification>
- `pnpm check` 0 errors; `pnpm test` green.
- Behavioural checks in Task 3 recorded with observed values.
- `git diff --stat` touches only `src/lib/components/NowPlaying.svelte` (plus the SUMMARY). `swipeAction.ts`, `swipeRemove.ts`, their tests, and the Related list markup/CSS are unchanged (`git diff -- src/lib/actions` is empty; `grep -n 'related-swipe' src/lib/components/NowPlaying.svelte` rule bodies unchanged apart from the one stale-comment sentence).
</verification>

<success_criteria>
- Up Next rows show no version-picker button at rest; grip stays visible (D: resting state).
- Swipe right → VersionPicker for that row, on any row including current; swipe left → remove, no-op on current (D: swipe semantics + current row).
- Row is opaque at rest and mid-swipe; playing/skipped/lifted variants do not re-introduce bleed (D: bleed fix).
- Tap-to-play, long-press menu, skipped retry, grip reorder unaffected.
- Every new/changed line carries a `quick-260910-nx6` comment; no existing decision-ref comment deleted.
</success_criteria>

<output>
Create `.planning/quick/260910-nx6-up-next-rows-hide-version-picker-behind-/260910-nx6-SUMMARY.md` when done.
</output>
