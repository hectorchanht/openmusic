---
quick_id: 260704-3df
slug: resync-stale-gsd-managed-blocks-in-claud
subsystem: docs / GSD-managed context files
source: optimization backlog #4 (.planning/codebase/CONCERNS.md)
---

# Quick Task 260704-3df: Resync stale GSD-managed blocks in CLAUDE.md + AGENTS.md

## Problem

`CLAUDE.md` and `AGENTS.md` each carry **74 references to a deleted `index.html`** and describe
the project as a vanilla single-file HTML/JS player served on GitHub Pages with "no build step"
and "no test framework". The LIVE app is a **SvelteKit 5 (runes) + Vite + TypeScript + pnpm +
vitest** mobile PWA under `src/`, deployed on **Cloudflare** (Pages + Workers). An agent following
these files edits a phantom file and reintroduces fixed bugs (e.g. the false "JOOX_TOKEN hardcoded
at index.html:2165" — it is now correctly edge-only at `src/lib/proxy/joox.ts:32`).

Both files are GSD-managed: content lives inside `<!-- GSD:<block>-start source:<X> --> … <!-- GSD:<block>-end -->`
delimited blocks assembled from `.planning/` sources. There is no standalone CLI to regenerate them,
so this task mechanically resyncs the stale blocks from the freshly-remapped `.planning/codebase/`
docs (rewritten 2026-07-04) while HONORING the delimiter contract.

## Block map (identical in both CLAUDE.md and AGENTS.md)

| Block | Source | Action |
|-------|--------|--------|
| `GSD:project` | `.planning/PROJECT.md` | LIGHT edit: keep the origin story but fix the dead `([index.html](index.html))` link — describe index.html as the *former desktop original that no longer lives in this repo*. Keep "Built with SvelteKit … Cloudflare". |
| `GSD:stack` | `.planning/codebase/STACK.md` | **REPLACE** the whole block body with a condensed, accurate SvelteKit stack (Svelte 5 runes, Vite, TS ~5.9, pnpm 8.15.5, vitest 4.x / 67 test files, dual adapter cloudflare/static + Capacitor Android, `wrangler.jsonc`, `svelte.config.js`, `vite.config.ts`). NO index.html / GitHub Pages / "no build step". |
| `GSD:conventions` | `.planning/codebase/CONVENTIONS.md` | **REPLACE** with the real conventions (TS strict, runes `$state`/`$derived`/`$effect`, `.svelte.ts` runes store vs `.ts` pure split, never-throw service pattern, generation-guard idiom, i18n double-quotes rule, `$lib` aliasing, SSR `browser` guards). |
| `GSD:architecture` | `.planning/codebase/ARCHITECTURE.md` | **REPLACE** the huge stale component table (index.html line numbers) with the real layered architecture: routes → components → stores → services → sources → api proxies → upstreams; single `<audio>` at `+layout.svelte`; Track colon-uid shape; playContext/queue; three-family cover cache + healCover self-heal; playGen/queueGen guards; playback lifecycle. Keep it condensed (roughly the current block's length), not a verbatim copy of the 266-line source. |
| `GSD:skills` | `skills/` | **PRESERVE verbatim** — still accurate ("No project skills found …"; no `.claude/skills` dir exists). |
| `GSD:workflow` | GSD defaults | **PRESERVE verbatim** — the GSD Workflow Enforcement block. |
| `GSD:profile` | generate-claude-profile | **PRESERVE verbatim** — managed by `generate-claude-profile`, do not edit. |

## Tasks

1. **Rewrite CLAUDE.md** — replace the `stack`, `conventions`, `architecture` block bodies (condensed
   from the fresh `.planning/codebase/*.md`); light-edit the `project` block's dead index.html link.
   Keep every `<!-- GSD:*-start/-end -->` delimiter EXACTLY (same markers, same order). Preserve the
   `skills`, `workflow`, `profile` blocks byte-for-byte. Result: 0 references implying the LIVE app is
   index.html/vanilla (a historical "was originally index.html" mention in the project block is fine).

2. **Rewrite AGENTS.md** — apply the SAME block rewrites (identical block structure + line ranges).
   The two files' generated blocks should match (AGENTS.md may differ only in any non-GSD tail).

## Verification

- `grep -c "index.html" CLAUDE.md AGENTS.md` → only the intentional historical origin mention(s)
  remain (target: ≤2 each, none claiming the live app IS index.html).
- All 7 `GSD:*-start` + 7 `GSD:*-end` markers still present in each file (`grep -c "GSD:.*-start"` == 7).
- `GSD:workflow` + `GSD:profile` + `GSD:skills` block bodies unchanged (diff shows no edits inside them).
- Content cross-checks against the live repo: mentions `src/lib/stores/player.svelte.ts`, `vitest`,
  `pnpm`, Cloudflare adapter — and does NOT say "no build step" / "GitHub Pages" / "no test framework".

## Out of scope

- No code changes. No new docs. Do not run `/gsd:docs-update` (it targets README + docs/*.md, not these files).
- PROJECT.md itself is a planning artifact; leave its body as-is beyond what the project block needs
  (its "reskin of index.html" origin is historically accurate).
