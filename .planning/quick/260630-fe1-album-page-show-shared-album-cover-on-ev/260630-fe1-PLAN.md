---
phase: quick-260630-fe1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/(app)/album/[name]/+page.svelte
autonomous: true
requirements: [QUICK-260630-fe1]

must_haves:
  truths:
    - "Album track rows show the single resolved album cover (heroImg) instead of resolving a cover per row"
    - "No per-track cover network resolve fires on the album page (use:lazyCover removed from rows)"
    - "When the album has no resolvable cover (heroImg null), rows show the gradient fallback"
    - "pnpm check passes with 0 errors (no unused imports/vars left behind by the removals)"
    - "Tap-to-play, swipe, long-press, album-level actions, and enrichment effects are unchanged"
  artifacts:
    - path: "src/routes/(app)/album/[name]/+page.svelte"
      provides: "Album track rows painting the shared album hero cover"
      contains: "heroImg ?"
  key_links:
    - from: "src/routes/(app)/album/[name]/+page.svelte (.art row)"
      to: "heroImg ($derived merged.cover)"
      via: "style:background-image"
      pattern: "heroImg \\? `url\\(\\$\\{heroImg\\}\\)`"
---

<objective>
On the album page, every track row currently runs its own per-track cover resolve via
`use:lazyCover` (Deezer→iTunes→CN search PER row) even though all tracks share one album cover
that the page already resolves once into `heroImg`. Make every row reuse the shared `heroImg`
instead, eliminating the per-track network storm.

Purpose: One album = one cover resolve. Rows paint the album art the moment it lands (heroImg is
a reactive `$derived`), and fall back to the existing gradient when the album has no cover.
Output: A single atomic, code-only edit to the album page (presentation-only).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260630-fe1-album-page-show-shared-album-cover-on-ev/260630-fe1-PLAN.md

# The whole album page is the only file touched. Key facts the executor needs:
# - `heroImg = $derived(merged.cover ?? null)` (line ~122) is the single shared album cover.
#   It is ALREADY painted on the hero (.cover, line ~538). Reactive: rows repaint when it lands.
# - `fallbackCover(seed: string)` (line ~71) returns the gradient string — KEEP it.
# - `stubUid(stub)` (line ~88) builds `album:<artist> <title>` keys — KEEP (used by swipeQueue/
#   swipeLike at lines ~261/~277 for the in-flight guard set).
# - Row markup is at line ~616-621; the `.art` span is line ~618.
#
# Symbols to REMOVE (all page-local, nothing else imports them — they live inside this component):
#   - `import { lazyCover } from '$lib/actions/lazyCover';`  (line ~21)
#   - `let resolvedCovers = $state<Record<string, string>>({});` + `function onCoverResolved(...)` (lines ~84-87)
#   - `function stubAsTrack(stub): Track { ... }`  (lines ~91-109)
#   - The COVER-02 D-14 comment block (lines ~76-83) that explains the now-removed lazyCover wiring.
# - `Track` type import (line ~37) is ALSO used only by stubAsTrack's return type within this file;
#   after removing stubAsTrack, verify no other reference to `Track` remains in the file before
#   deciding whether to drop that import. swipeQueue/openMenu/resolveAll use `Track` via
#   `resolveStub` return inference and `resolvedCache`/`menuTrack` `$state<Track ...>` — so `Track`
#   IS still referenced. KEEP the `Track` import. (Confirm via the grep in <verify>.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint album rows to the shared heroImg cover and remove the per-row resolver</name>
  <files>src/routes/(app)/album/[name]/+page.svelte</files>
  <action>
Presentation-only change. Make every track row paint the shared album cover (`heroImg`) instead
of running its own `use:lazyCover` resolve, then delete the now-unused per-row resolver symbols so
`pnpm check` stays clean.

Make exactly these edits in src/routes/(app)/album/[name]/+page.svelte:

1. ROW MARKUP (the `.art` span, ~line 618). Remove the `use:lazyCover={{ track: stubAsTrack(track),
   onResolved: onCoverResolved }}` action and change the `style:background-image` to read the
   shared album cover, else the existing gradient fallback. The resulting span is:
   `<span class="art" style:background-image={heroImg ? `url(${heroImg})` : fallbackCover(track.artist + track.title)}></span>`
   Do NOT change anything else on the row `<button>` (longpress, onclick={() => playStub(track)},
   swipeAction onSwipeRight/onSwipeLeft, the rank span, meta span, or the Play icon). Do NOT touch
   the swipe reveal layers or the `(i)` keyed each block.

2. Remove the `lazyCover` import line (~line 21): `import { lazyCover } from '$lib/actions/lazyCover';`.

3. Remove `resolvedCovers` state and `onCoverResolved` (~lines 84-87):
   the `let resolvedCovers = $state<Record<string, string>>({});` line and the whole
   `function onCoverResolved(uid, url) { ... }` function.

4. Remove `stubAsTrack` (~lines 91-109): the entire `function stubAsTrack(stub: AlbumStub): Track { ... }`.

5. KEEP `stubUid` (~lines 88-90) — still used by swipeQueue/swipeLike in-flight keys.
   KEEP `fallbackCover` (~lines 71-74) — now the row fallback when heroImg is null.
   KEEP the `Track` type import (~line 37) — still referenced by resolveAll/resolvedCache/menuTrack.

6. Remove (or trim to remain accurate) the COVER-02 D-14 comment block (~lines 76-83) that
   documented the lazyCover→resolvedCovers wiring, since that wiring no longer exists. The
   surrounding `stubUid` and `fallbackCover` doc comments stay accurate; keep them.

Do NOT change tap-to-play, swipe, long-press, album-level actions (play/download/like/share/
addToPlaylist), the enrichment / Deezer / tracklist `$effect`s, or any resolve-on-tap behavior.
Do NOT add album-cover-to-cache seeding and do NOT touch cover-cache or cover-version — out of
scope. ONLY the row cover source changes plus the dead-symbol cleanup.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && pnpm check 2>&1 | tail -20</automated>
    <automated>cd /Users/laichan/code/tung/openmusic && npx vitest --run 2>&1 | tail -25</automated>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -nE 'lazyCover|stubAsTrack|resolvedCovers|onCoverResolved' 'src/routes/(app)/album/[name]/+page.svelte'; test -z "$(grep -nE 'lazyCover|stubAsTrack|resolvedCovers|onCoverResolved' 'src/routes/(app)/album/[name]/+page.svelte')" && echo REMOVED_OK</automated>
    <automated>cd /Users/laichan/code/tung/openmusic && grep -nE 'heroImg \? `url' 'src/routes/(app)/album/[name]/+page.svelte' && grep -nE 'stubUid|fallbackCover' 'src/routes/(app)/album/[name]/+page.svelte'</automated>
  </verify>
  <done>
The `.art` row span uses `heroImg ? `url(${heroImg})` : fallbackCover(...)` with no `use:lazyCover`.
The `lazyCover` import, `resolvedCovers`, `onCoverResolved`, and `stubAsTrack` are all gone (grep
returns nothing → REMOVED_OK). `stubUid`, `fallbackCover`, and the `Track` import remain.
`pnpm check` reports 0 errors and `npx vitest --run` is fully green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved album cover → DOM | `heroImg` (from merged Deezer/Last.fm) flows into CSS `url()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fe1-01 | Tampering (CSS injection) | `.art` `style:background-image={`url(${heroImg})`}` | accept | `heroImg` is `merged.cover` — already used identically on the hero `.cover` (line ~538) since this phase; same SOLID-https-gated enrichment source, no new trust boundary introduced by this change |
| T-fe1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (edit-only, no new deps) |
</threat_model>

<verification>
- `pnpm check` → 0 errors (catches any unused import/var left by the removals).
- `npx vitest --run` → full suite green (no album-specific tests exist; this confirms no other
  module broke — the removed symbols were page-local).
- grep confirms the four removed symbols are gone and the new `heroImg ? url(...)` row binding plus
  retained `stubUid`/`fallbackCover` are present.
</verification>

<success_criteria>
- Album track rows paint the single shared `heroImg` album cover; gradient fallback when null.
- Zero per-track cover network resolves on the album page.
- `pnpm check` 0 errors and `npx vitest --run` green.
- No behavioral change to tap-to-play, swipe, long-press, album-level actions, or enrichment.
- One atomic, code-only commit touching only src/routes/(app)/album/[name]/+page.svelte.
  Do NOT stage .planning/HANDOFF.json.
</success_criteria>

<output>
Create `.planning/quick/260630-fe1-album-page-show-shared-album-cover-on-ev/260630-fe1-SUMMARY.md` when done
</output>
