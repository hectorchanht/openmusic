---
phase: quick-260616-0n6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config/defaults.ts
autonomous: true
requirements: [QUICK-260616-0n6]
must_haves:
  truths:
    - "Album-originated queues default their up-next mode to 'same-list' instead of the global 'generated'"
    - "effectiveUpnextMode('album') resolves to 'same-list' on a fresh load with no user override"
    - "npm run check passes (the perContext literal still typechecks against Partial<Record<...>>)"
  artifacts:
    - path: "src/lib/config/defaults.ts"
      provides: "UPNEXT_DEFAULTS.perContext with an album → 'same-list' override"
      contains: "album: 'same-list'"
  key_links:
    - from: "src/lib/config/defaults.ts (UPNEXT_DEFAULTS.perContext)"
      to: "src/lib/stores/settings.svelte.ts (upnextPerContext field init + resetPlayback)"
      via: "DEFAULTS.upnext.perContext spread"
      pattern: "DEFAULTS\\.upnext\\.perContext"
---

<objective>
Set the `album` queue context to default to up-next mode `'same-list'` so that album-originated
queues continue down the same album list instead of falling through to the global `'generated'`
genre-generation default.

Purpose: Albums are curated, ordered collections; users expect "play the rest of the album" rather
than genre-similar generation. This is the per-context override the existing Phase-17 machinery
was built to support.

Output: One added entry in `UPNEXT_DEFAULTS.perContext` in `src/lib/config/defaults.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Verified contracts — executor uses these directly, no exploration needed. -->

From src/lib/config/defaults.ts (lines 92-112):
```typescript
export type UpnextMode = 'same-list' | 'generated';
export type QueueContext =
  | 'liked' | 'search' | 'downloads' | 'playlist' | 'album'
  | 'artist' | 'home-discovery' | 'history' | 'remix' | null;

export const UPNEXT_DEFAULTS = {
  mode: 'generated' as UpnextMode,
  perContext: {} as Partial<Record<Exclude<QueueContext, null>, UpnextMode>>
} as const;
```

Live consumers (do NOT need editing — they spread the default automatically):
- src/lib/stores/settings.svelte.ts:119 — `upnextMode = $state<UpnextMode>(UPNEXT_DEFAULTS.mode)`
- src/lib/stores/settings.svelte.ts:451 — `this.upnextPerContext = { ...DEFAULTS.upnext.perContext }` (resetPlayback)
- src/lib/stores/settings.svelte.ts:460 — `effectiveUpnextMode(ctx)` reads `upnextPerContext` then falls back to `upnextMode`

Test facts (verified — no test edit required):
- src/lib/stores/settings.svelte.test.ts:37 — `beforeEach` resets `settings.upnextPerContext = {}`,
  so tests do NOT read `UPNEXT_DEFAULTS.perContext`. Changing the default does not break the suite.
- The `album` test (lines 53-60) sets its own per-context state and is unaffected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add album → 'same-list' default to UPNEXT_DEFAULTS.perContext</name>
  <files>src/lib/config/defaults.ts</files>
  <action>
    Edit the `perContext` initializer in `UPNEXT_DEFAULTS` (currently the empty object literal
    `{}` on line 111). Change it from an empty object to one containing a single entry:
    `album: 'same-list'`. Keep the existing `as Partial<Record<Exclude<QueueContext, null>, UpnextMode>>`
    type assertion exactly as-is — the literal `{ album: 'same-list' }` typechecks against it
    because `'album'` is a valid `QueueContext` key and `'same-list'` is a valid `UpnextMode`.

    Do NOT touch `mode: 'generated'` (line 109) — the global default stays 'generated' per the
    roadmap lock; only the per-context override changes.

    Do NOT touch the pre-existing `console.log('UPNEXT_DEFAULTS', UPNEXT_DEFAULTS)` (line 113) —
    it is out of scope.

    Update the inline doc comment on the `perContext` field (currently "Per-context overrides;
    empty = every context resolves to the global `mode`.") to reflect that `album` now overrides
    to 'same-list' while other contexts still resolve to the global `mode`.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/openmusic && npm run check && npx vitest run src/lib/stores/settings.svelte.test.ts</automated>
  </verify>
  <done>
    `UPNEXT_DEFAULTS.perContext` contains `album: 'same-list'`; `npm run check` (svelte-check /
    tsc) reports no new errors; the existing settings test suite passes unchanged.
  </done>
</task>

</tasks>

<verification>
- `grep -n "album: 'same-list'" src/lib/config/defaults.ts` returns a match inside the `perContext` literal.
- `npm run check` passes (the per-context literal typechecks).
- `npx vitest run src/lib/stores/settings.svelte.test.ts` passes — confirms no consumer regression.
</verification>

<success_criteria>
- A fresh load (no persisted user override) resolves `effectiveUpnextMode('album')` to `'same-list'`.
- All other contexts still resolve to the global `'generated'` default.
- No type errors and no test failures introduced.
</success_criteria>

<output>
Create `.planning/quick/260616-0n6-set-album-same-list-for-upnext-defaults/260616-0n6-SUMMARY.md` when done.
</output>
