---
gsd_state_version: 1.0
milestone: v1.2
milestone_name: Resilient Playback & UX Polish` below). The Last.fm write-side
status: Awaiting next milestone
stopped_at: Phase 24 context gathered
last_updated: "2026-06-15T18:02:50.009Z"
last_activity: 2026-06-15 — Milestone v1.2 completed and archived
progress:
  total_phases: 10
  completed_phases: 10
  total_plans: 43
  completed_plans: 43
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-10)

**Core value:** A user on their phone can search a song, tap it, and have it play instantly with a smooth, native-app-like experience — and keep playing when the screen locks.
**Current focus:** Phase 24 — offline-app-shell-sharing-seo

## Current Position

Phase: Milestone v1.2 complete
Plan: —
Status: Awaiting next milestone
Last activity: 2026-07-04 — Completed quick task 260704-2os: edge-cached the core CN metadata SEARCH proxy (optimization backlog #1). The hot catch-all `/api/[source]/[...path]` was the only metadata route with no edge cache; added `caches.default` (SEARCH_TTL=300, Deezer `edgeCache()` pattern) for the `search` path ONLY — `url`/`detail`/`lrc` stay uncached streaming passthrough so no expiring audio/lyric URL is ever frozen. Own-origin key, CORS-free body + per-hit CORS re-apply, 200-only. 2 code commits (1bf4094, 4e95c94), proxy 16/16 (+5), check 0/0. (Also earlier 2026-07-04: full codebase remap `.planning/codebase/` (7 docs, commit 50ad3b8) — old map was Jun-5 stale; confirmed no index.html at root, CLAUDE.md/AGENTS.md describe a deleted vanilla player; produced a 10-item optimization backlog in CONCERNS.md.) (Prior: Completed quick task 260704-20e: fixed now-playing song cover stuck black — new `player.healCover(uid)` probes the displayed cover, evicts + re-resolves a DEAD url (the counterpart to `resolveCoverAsync`, which only fired on a null cover), wired via a one-shot `$effect` in NowPlaying (swappedCover/Last.fm still wins, gradient on true miss, playGen-guarded, not `playing`-event gated). Ports the list-row self-heal (260630-ey2) to the carousel CURRENT cell. 2 code commits (5e289c0, 43be983), player 161/161 (+8), check 0/0, lazyCover 13/13. (Prior: Completed quick task 260703-i7e: removed foreground auto-resume ("on visible" autoplay) — deleted resumeOnForeground + resumeIfStalled() from player.svelte.ts; visibilitychange now only logs + flushes persistence on hide. 2 code commits (355303b, 29cc5f1), 987/67 green, check 0/0. (Prior: Completed quick tasks 260701-0aa + 260701-0j5: native-app tap/press feedback. 0aa built reusable `use:tapBounce` (one-shot shrink-spring, reduced-motion safe) + active-row grey highlight on songs; 0j5 extended it app-wide (21 .svelte files: nav, both players, sheets, search, library, all settings, album/artist/charts), scope-guarded off inputs/sliders/toggles/drag-surfaces/scrims/home-chips. No new action/keyframe/CSS in 0j5 — pure wiring. Both have a pending human-verify checkpoint (on-device touch). svelte-check 0/0, player 161/161. (Prior: 260630-sgw: action log (localStorage ring buffer, `openmusic:action-log:v1`, cap 2000) + verbose 16-point player instrumentation (never-throw `logAction`, no timeupdate spam) + Settings → Activity log viewer (copy/clear, live); 7 i18n keys × 15 locales; full suite 987/67 green, svelte-check 0/0. Built as the diagnostics foundation for the NEXT bug (background auto-advance stall: next track at 0:00, no duration, plays on foreground). (Same-day prior: 260630-q03 never-stop dead-tail; debug autoadvance-pauses-after-1s external-pause self-heal; 260630-fu2 i18n quotes, 260630-fce settings helper text, 260630-fe1 album cover, 260630-ey2 cover self-heal, 260630-efr clear-cache hint, 260630-e6e JOOX identity — all unpushed on main.))))

## Performance Metrics

**Velocity:**

- Total plans completed: 34
- Average duration: 7 min
- Total execution time: 0.1 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 8 | 1 | 7 min | 7 min |
| 14 | 2 | - | - |
| 16 | 3 | - | - |
| 17 | 4 | - | - |
| 18 | 3 | - | - |
| 19 | 3 | - | - |
| 20 | 4 | - | - |
| 23 | 8 | - | - |
| 999.1 | 6 | - | - |

**Recent Trend:**

- Last 5 plans: 08-01 (7 min)
- Trend: —

*Updated after each plan completion*
| Phase 8 P03 | 5 | 1 tasks | 1 files |
| Phase 09 P01 | 9min | 3 tasks | 8 files |
| Phase 09 P02 | 4min | 3 tasks | 6 files |
| Phase 09 P03 | 7min | 3 tasks | 5 files |
| Phase 14 P01 | 8min | 4 tasks | 18 files |
| Phase 14 P02 | 4 | 2 tasks | 6 files |
| Phase 18 P01 | 6 min | 2 tasks | 4 files |
| Phase 18 P02 | 10 min | 2 tasks | 2 files |
| Phase 18 P03 | 12 min | 2 tasks | 6 files |
| Phase 19 P01 | 7 min | 3 tasks | 22 files |
| Phase 19 P02 | 10 min | 3 tasks | 1 files |
| Phase 19 P03 | 5 min | 2 tasks | 7 files |

## Accumulated Context

### Roadmap Evolution

- Phase 14 added: Search & Data Responsiveness — first-load search skeleton, search query+results preserved across navigation, default audio quality 128–160kbps, TTL query cache for search/discovery (off-milestone UX/playback polish; not part of v1.1 Last.fm)
- Phase 15 added: Now-Playing Shared-Element Expand/Collapse — swipe-up nowbar→full expand mirroring swipe-down collapse; cover/title/artist morph between positions driven by drag progress, with blur during drag (off-milestone UX polish; UI-spec recommended)

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [Roadmap v1.2]: Phase numbering continues at 16 (last used = 15, now-playing shared element). v1.2 = Phases 16–24 (9 phases, standard granularity). The Phases 11–13 Last.fm write-side (labelled "v1.2" in an earlier ROADMAP draft) is re-deferred to v1.3 (decision 2026-06-10) and re-titled "Last.fm Write-side — Deferred to v1.3" in ROADMAP.md.
- [Roadmap v1.2]: Phase order follows research A–H dependency chain — 16 (resilience core, the `queueContext`/2-state-repeat/skip-loop-guard dependency root) → 17 (up-next sourcing + batched settings/config + Deezer enrichment) → 18 (sleep timer) → 19 (TrackMenu rework incl. Remix) → 20 (now-playing gestures/scroll) → 21 (search+cover polish) → 22 (lyrics, after 20 to avoid NowPlaying merge churn) → 23 (cross-cutting UX audit + homepage/artist density) → 24 (offline SW + SEO, net-new infra isolated last).
- [Roadmap v1.2]: Most PLAY/QUEUE work is policy + wiring + UI on the existing `player.svelte.ts` engine (failover/prefetch/buildSimilarQueue/offline-blob already ship) — zero net-new runtime deps. The only genuinely new infra is the offline app-shell service worker and per-entity SSR OG/slugs, both in Phase 24 and both flagged HIGH research.
- [Roadmap v1.2]: Audit-adds placed in sensible phases — QUEUE-05 (swipe-remove/clear) in 17 (queue context), NP-05 (nowbar swipe) in 20 (now-playing gestures), LYR-01 (tap-to-seek) in 22 (lyrics), UX-04 (row swipe-actions)/UX-05 (haptics)/UX-06 (a11y) in 23 (UX audit). QUEUE-04 (Remix) lives in 19 because it is a track-menu action.
- [Phase 8-01]: Read-proxy route shape = DEDICATED `/api/lastfm/info` route (mirrors `/api/similar`), NOT the `/api/[source]/[...path]` catch-all — avoids widening `SourceId` to `'lastfm'` (which breaks `SOURCES: Record<SourceId, SourceAdapter>` until a Phase-10 client source exists) and gets the absent-key-is-200-clean-empty posture for free.
- [Phase 8-01]: Enriched fields = OPTIONAL fields on `Track` (tags?/bio?/bioUrl?/lastfmArt?), NOT a side cache. No `serializeTrack` whitelist on the library path; fields persist JSON-safe with no migration. Deliberately NOT added to `HistoryEntry`/`toEntry` (re-enrich on replay). `matchKey` is artist-first (`norm(artist)|norm(title)`, Pitfall 9); `dedupe.ts` keeps its legacy title|artist order and is left untouched.
- [Roadmap v1.1]: Read-only-first phase order — enrichment (8), discovery (9), source (10) ship value with zero auth surface before the highest-risk signed-call infrastructure + auth lands once (11), then scrobble (12) and loved-sync (13) layer on top. Auth cannot move earlier: scrobble/loved-sync hard-depend on the `sk` cookie.
- [Roadmap v1.1]: Last.fm-searchable source resolves via the EXISTING CN-source re-search resolver (searchAll + dedupeBest + best-match scoring). GD Studio `ytmusic` is OUT of v1.1 (deferred to v2 / LFSRC-FB-01); would need its own feasibility spike if pulled in.
- [Roadmap v1.1]: `LASTFM_SECRET` + session key stay edge-only; sk in an httpOnly+Secure+SameSite cookie, never localStorage, never in a response body (JOOX_TOKEN parity / threats T-lfm-01/02/03). The match-key normalization primitive lands in Phase 8 and is reused by Phase 13 reconciliation.
- [Roadmap]: Bottom-up phase order — extract data layer + prove proxy boundary headless before any UI is built (avoids building on an audio engine whose iOS behavior is unproven).
- [Roadmap]: Proxy metadata only through SvelteKit `+server.ts`; audio bytes stream browser → CDN directly (preserves geo/IP context, stays within Worker free-tier limits).
- [Roadmap]: Single module-scoped `<audio>` element owned by an `AudioEngine` singleton; Svelte 5 runes in `.svelte.ts` for all shared state; source-adapter registry so adding a source touches only new files.
- [Phase ?]: 09-01: Endpoint fork B — dedicated /api/lastfm/discovery for LIST methods; /api/lastfm/info extended only for album tracklist
- [Phase ?]: 09-01: Cache API cache key = own-origin discovery Request (secret never cached); per-method public TTLs charts 1h/tags 6h/topalbums 24h
- [Phase ?]: 09-02: Home is the Last.fm discovery surface — FOUR shelves (top hits / top artists / per-tag / per-country); tag+country fan-out capped via mapWithConcurrency (≤4 in-flight, Pitfall 11); v2 localStorage shelf cache + background revalidate
- [Phase ?]: 09-02: Discovery tracks are {artist,title} stubs — tap-to-play resolves via resolveStub (searchAll+dedupeBest) then player.play (D-03); top-artist tiles goto to the artist page; buildDiversePicks stays as the no-key/empty fallback (D-06)
- [Phase ?]: 09-03: album artist carried via ?artist= URL query param (artist-page link), read with page.url.searchParams.get — replaces the tracks[0]?.artist derivation; the album tracklist is the real album.getInfo ordered stubs resolved on tap via resolveStub
- [Phase 14-01]: D-04 TTL cache wraps the searchAll seam (key=normQuery|sources|page, 5-min TTL, __clearSearchCache() for tests); caches final merged superset metadata only, never audio URLs
- [Phase 14-01]: D-03 client-ladder reorder via pure pickByQualityPref; settings.defaultQuality default auto->128 read by QQ(STD)/JOOX(probe order)/Kuwo(level=128k best-effort) adapters importing settings directly; JOOX_BR=4 untouched (proxy.test.ts green)
- [Phase 14-01]: D-05 searchHistory store (key openmusic:search-history:v1, cap 12) DISTINCT in symbol+key from play-history; pure recordQuery/parseSearchHistory + thin browser-guarded runes wrapper
- [Phase 14-01]: D-06 progressive searchAll via optional trailing onPartial (non-breaking); accumulate->re-interleave->emit with sig.aborted guard in .finally; cache HIT fires onPartial once with pending:0
- [Phase ?]: [Phase 14-02]: D-02 searchSession is IN-MEMORY runes singleton (q/results/page/hasMore/scrollY/searched); SSR-leak (T-14-05) mitigated by browser-side-write discipline + HAS_WINDOW backstop, NOT persistence; onMount restores instantly (no refetch) + scroll after tick, new query overwrites via save()
- [Phase ?]: [Phase 14-02]: D-01 one {#snippet skeletonRows} shared by first-load (loading && results.length===0) + load-more; D-06 onPartial re-runs dedupeBest per partial with two-layer abort guard (myAc.signal.aborted || kw!==q.trim()), results cleared at run() start so skeleton yields to first partial; loadMore stays blocking
- [Phase ?]: [Phase 14-02]: D-05 suggestions on inputFocused && q.trim()==='' && !searched; 150ms blur-delay + onmousedown-preventDefault so tap registers; recorded on submit (zero-result queries listed); +search.recent/search.clear i18n in 3 dicts
- [Phase ?]: [Phase 18-01]: Sleep-timer deadline is ABSOLUTE wall-clock (Date.now()+ms, D-14), survives bg-tab throttling; 1s UI tick recomputes remaining, never accumulates. Store in-memory only (D-13). decideEndedAction pure-tested locks D-03 (sleep beats repeat-one beats advance). canFadeVolume typed structural {volume} so iOS read-only path is node-testable.
- [Phase ?]: [Phase 18-02]: Sleep-timer expiry wired into player at four seams; blocker proven in code (expiry never calls next()/runFallback, never bumps playGen, never touches failure counters); wake-timer backstop armed via public player.onSleepTimerSet().
- [Phase 18-03]: Sleep-timer UI is a globally-mounted SleepTimerSheet (3rd pickerOpen instance, overlay 'trackmenu-timer', open-only $effect) driven by a new sleepTimer.sheetOpen flag; TrackMenu item + nowbar moon/countdown badge + now-playing full readout all converge on the same sheet (D-08). Four i18n keys (timer.minutes a single {n} interpolation key) in all 15 dicts, no expiry-toast key (D-09). The sheet arms player.onSleepTimerSet() after set() (18-02 contract). Browser-verified (set/reopen/restart/cancel/i18n/zero-console); expiry-fade + D-03/D-04/D-05 + lock-screen deferred to device testing.
- [Phase 19]: 19-01: D-06 force-generate = a 'remix' QueueContext enum value + a one-line effectiveUpnextMode early-return (if ctx===remix return generated); least-churn vs threading a mode param (A1). Player store unchanged — Remix reuses setQueue/play({fresh})/regenerate verbatim.
- [Phase 19]: 19-01: MENU-01 gating extracted to PURE helper track-menu-gate.ts (isGatedReady = detailsLoaded && uid && audioUrl; shouldStartResolve = !inFlight.has(key)) so it is node-testable without a DOM; the new Set() reassign-for-reactivity stays in the component (19-02). i18n parity test widened from 3 to all 15 locales.
- [Phase 19]: 19-02: TrackMenu rework landed as ONE atomic commit (the 3 contracts are mutually-dependent edits to one component file that won't compile in partial states); overlay $effect kept byte-unchanged (dep open-only, untrack, sole-dismiss cleanup, {#if open && track}); new Close (X) calls close() only. doDownload/doDetail refactored to gated run(resolved) callbacks; Download re-resolves at downloadQuality inside the callback (WR-07 preserved).
- [Phase ?]: 19-03: MENU-03/D-12 stuck-highlight fix lands ENTIRELY at the 6 long-press trigger sites + one global app.css tap-highlight reset; longpress.ts and all onclick handlers untouched (suppressNextClick trailing-click guard + pointercancel/pointerup :active release preserved). Each sticky :hover/:active wrapped in @media (hover: hover) byte-unchanged; every onlongpress calls (e.currentTarget as HTMLElement)?.blur(). pnpm check 0/0, 626 tests green; device-pass deferred.

### Pending Todos

[From .planning/todos/pending/ — ideas captured during sessions]

None yet.

### Blockers/Concerns

[Issues that affect future work]

- [Phase 24]: NET-NEW INFRA, HIGH research — both halves are Cloudflare-specific and not trivially testable locally. Offline: SW lifecycle on Cloudflare Pages, iOS Safari PWA+SW+background-audio interplay, cache-versioning on deploy — verify under `wrangler pages dev` with the native `src/service-worker.ts` (NOT vite-pwa). SEO: `+page.server.ts` SSR OG at the edge, `og:image` strategy (unstable CDN cover vs composed card vs static fallback), CJK slug encode/decode, real scraper testing. Run `/gsd:plan-phase --research-phase 24` (consider splitting offline vs SEO spikes). Research flag set.
- [Phase 20]: Cover-swipe vs sheet-collapse gesture collision (research Pitfall 7) is the highest-risk interaction in v1.2 — never `setPointerCapture` on `pointerdown`, commit axis in `pointermove` after slop, sub-slop movement must still reach `onclick` (tap contract). Reuse the existing slop/velocity idiom exactly.
- [Phase 19]: Overlay `$effect` history invariant — dep must stay `open`-only with `untrack()` around overlay calls, single dismiss path, `{#if open && track}` for visibility. Any new sub-sheet follows the `pickerOpen` precedent. Act-on-unresolved-stub must be gated (`detailsLoaded && uid`).
- [Phase 16]: Infinite auto-skip loop guard (research P-FAILOVER) — `consecutiveSkips` cap ~5, stop + one sticky toast, reset on a successful `audio playing` event or user gesture, gate the whole chain on `navigator.onLine === false`. iOS rejected `play()` after async src-swap must count as a failure into the loop-guard counter, never a silent `.catch(() => {})`.
- [Phase 18]: Sleep-timer stop must NOT collide with Phase 16's skip-loop guard (suppress-`next()` path); use an absolute-timestamp deadline to survive background-tab `setTimeout` throttling.
- [Phase 1]: Worker egress geo-behavior against JOOX/QQ audio CDNs is unconfirmed — spike required before the audio data-flow architecture is locked. Research flag set.
- [Phase 6]: iOS standalone-PWA background audio is contested (STACK.md vs PITFALLS.md); only a real-device spike (iOS 15.4 / 16 / 17 / 18 / 18.4+) covering play-while-locked AND pause→wait→resume-from-lock can resolve it. Research flag set.
- [Phase 11]: (Deferred → v1.3) Highest-risk write-side surface — owns T-lfm-01/02/03 (secret/sk leakage, CSRF) + `api_sig` UTF-8/CJK correctness. Mandatory `周杰伦`/`稻香` signing fixture test must run in the workerd/`wrangler dev` runtime (throws NotSupportedError under jsdom/Node — that's runtime, not a code bug).
- [Phase 13]: (Deferred → v1.3) May need `/gsd:plan-phase --research-phase 13` IF CJK normalization proves complex (Traditional/Simplified folding, CJK punctuation variants, "ghost" loved stubs with no playable source).
- [Phase 10]: GD Studio `ytmusic` is deferred from v1.1; if ever pulled in it warrants its own feasibility spike (`s`-checksum drift, 50 req/5 min cap, instance failover, Western-catalog match rate).

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260605-wq1 | Sketch-001 variant C home shell + search, wired to data layer; deployed to openmusic.pages.dev | 2026-06-05 | 0684b67 | [260605-wq1-implement-sketch-001-variant-c-home-shel](./quick/260605-wq1-implement-sketch-001-variant-c-home-shel/) |
| 260606-2k7 | Full-screen now-playing (expand/drag-collapse) + artist page + seekable progress (NaN fix); deployed | 2026-06-06 | a1422fd | [260606-2k7-full-screen-now-playing-artist-page-prog](./quick/260606-2k7-full-screen-now-playing-artist-page-prog/) |
| 260606-3a0 | Persist playback across nav + nowbar progress sliver + diverse cached top picks (localStorage) + Randomize; deployed | 2026-06-06 | 3a0832b | [260606-3a0-persist-playback-nowbar-progress-diverse](./quick/260606-3a0-persist-playback-nowbar-progress-diverse/) |
| 260606-3f6 | Hide source + cross-source dedupe/best-quality + draggable subnav sheet + smart lyrics scroll + auto-advance/auto-grow queue; deployed | 2026-06-06 | aa0260a | [260606-3f6-dedupe-cross-source-hide-source-draggabl](./quick/260606-3f6-dedupe-cross-source-hide-source-draggabl/) |
| 260606-4g1 | Rebrand openmusic + lucide icons + settings & library pages + now-playing options menu (download/like/playlist/album/artist/share/detail) + sensitive grip; deployed | 2026-06-06 | e959635 | [260606-4g1-rebrand-openmusic-settings-library-nowpl](./quick/260606-4g1-rebrand-openmusic-settings-library-nowpl/) |
| 260606-4pn | Settings: live lyrics translation (/api/translate, replace/below) + default quality/source + accent/reduce-motion/auto-expand; deployed | 2026-06-06 | ec862cc | [260606-4pn-settings-lyrics-translation-default-qual](./quick/260606-4pn-settings-lyrics-translation-default-qual/) |
| 260606-54l | Translate displayed song/artist names (settings nameLang) + long-press track context menu (Play next / Add to queue + 7 actions, reusable TrackMenu); deployed | 2026-06-06 | ac8b6fd | [260606-54l-translate-song-artist-names-long-press-t](./quick/260606-54l-translate-song-artist-names-long-press-t/) |
| 260606-5du | Live-drag now-playing panel grip (translateY follows finger, smooth snap like full-page close); deployed | 2026-06-06 | d5201c7 | [260606-5du-smooth-live-drag-now-playing-panel-grip](./quick/260606-5du-smooth-live-drag-now-playing-panel-grip/) |
| 260606-5ji | openmusic logo/favicon (SVG mark) + SEO (meta, OG/Twitter share card, manifest, robots/sitemap); deployed | 2026-06-06 | 9621a70 | [260606-5ji-favicon-logo-seo-meta-og-twitter-manifes](./quick/260606-5ji-favicon-logo-seo-meta-og-twitter-manifes/) |
| 260606-5of | Remove unused/fork-cruft: orphan favicon dup, docs/logo.png (README fix), legacy/ (~220KB), g4f issue bot + workflow, fork issue templates + FUNDING; svelte-check clean | 2026-06-05 | e688cfe | [260606-5of-remove-things-that-are-not-used-or-neede](./quick/260606-5of-remove-things-that-are-not-used-or-neede/) |
| 260606-5vm | Rewrite README for the openmusic SvelteKit project (doc-only; reconciled removed legacy/ refs) | 2026-06-06 | bde56ea | [260606-5vm-update-readme-for-svelte-project](./quick/260606-5vm-update-readme-for-svelte-project/) |
| 260606-5ug | Smarter Up-Next: Last.fm artist.getSimilar gen (server-side key + same-artist fallback) regen-on-fresh-play preserving manual adds, pointer-drag reorder (GripVertical, pins manual), current-track length; check clean, 67/67 tests | 2026-06-05 | be967a3 | [260606-5ug-up-next-list-auto-generated-from-similar](./quick/260606-5ug-up-next-list-auto-generated-from-similar/) |
| 260606-6p7 | App-language selector (English/繁體中文/简体中文) + whole-app i18n: dependency-free runes t() in src/lib/i18n/, ~74 chrome strings across 12 files (toasts/menus/aria), persisted appLang + first-visit auto-detect, nameLang/lyricsLang kept independent; check clean, 78/78 tests | 2026-06-05 | 66f871c | [260606-6p7-add-app-language-selector-to-settings-i1](./quick/260606-6p7-add-app-language-selector-to-settings-i1/) |
| 260606-ggv | Grouped drill-in settings: /settings → 7-group list (general/translation/playback/history/lastfm/data/about) each its own route; NEW local recently-played History (pure logic+test, runes store, openmusic:history:v1, recorded in player.play, cap 50/dedupe/tap-to-play); Last.fm route = disabled "coming soon" placeholder (real auth deferred to Phase 11); i18n keys in 3 locales; check clean, 89/89 tests | 2026-06-06 | fa142b2 | [260606-ggv-restructure-settings-into-grouped-drill-](./quick/260606-ggv-restructure-settings-into-grouped-drill-/) |
| 260606-ggj | Mobile UX + gesture overhaul: reusable `dragClose` action (finger-follow translateY dismiss, tap-preserving) + `overlays.svelte.ts` History-API back-to-close stack (single dismiss path, depth==stack invariant); now-playing sheet now 3-state (closed/half/full) with direction-biased nearest-snap, subnav row doubles as drag handle (tap still switches tab), YT-Music full-bleed cover-squeeze reflow on half/full; drag-to-close + back-gesture wired into all 3 TrackMenu sheets + now-playing; no new deps/i18n keys; check clean, 89/89 tests | 2026-06-06 | caf9f6a | [260606-ggj-make-it-more-mobile-friendly-and-enhance](./quick/260606-ggj-make-it-more-mobile-friendly-and-enhance/) |
| 260606-h4s | Half-open now-playing fixes (follow-up to ggj): (1) flush half-open — `halfOffset` measured from live `.transport` bottom edge (was arbitrary 50% height) + persistent resting `translateY(halfOffset)` so half stops collapsing to full coverage, panel fills all space below controls with no gap; (2) subnav item tap now has priority over the open-gesture — `data-tab` + `gripStartTab` route the tap to `selectTab` (switch tab + half-open), drags still snap without switching; (3) `.np.reflow .bar` floats `position:absolute;top:0` transparent over the full-bleed cover. Single file (NowPlaying.svelte), no new deps/i18n; check 0/0, 113/113 tests | 2026-06-06 | 7fb2845 | [260606-h4s-fix-half-open-now-playing-remove-gap-bet](./quick/260606-h4s-fix-half-open-now-playing-remove-gap-bet/) |
| 260606-kyf | Wire W3C Media Session API so OS/browser media UI (Chrome hub, macOS Now Playing, lock screen, BT car) shows track title/artist/album/cover + transport + scrubber like YouTube Music (was page-title + Chrome icon). Pure node-testable `media-session.ts` helpers (`buildArtwork`/`safePositionState`/`playbackStateFor`, 16 tests) + wired into player store behind a single `get ms()` SSR+feature guard: 7 action handlers (play/pause/prev/next/seek±10s/seekto) registered once in attach(), `MediaMetadata` from resolved track in play(), `playbackState`/`setPositionState` synced off existing listeners, `clearMedia()` on error; reuses toggle/prev/next/seekFraction, no backend touch, no new deps, native DOM types, `/favicon.svg` fallback art; check 0/0, 129/129 tests | 2026-06-06 | 8f9f3d8 | [260606-kyf-wire-media-session-api-os-browser-media-](./quick/260606-kyf-wire-media-session-api-os-browser-media-/) |
| 260606-ncw | Audit: outside (scrim) click always closes any modal. Verdict = ALREADY SATISFIED, no code change. All 3 TrackMenu sheets (menu/picker/detail) have sibling `.scrim` buttons (full-viewport, z80 below modal z81) whose onclick only flips local `$state`; the `$effect` cleanup is the sole `overlays.dismiss(id)` site → scrim/X/drag/back-gesture all converge on one dismiss path (no history-depth desync). Nested menu→picker verified: picker scrim paints on top, closes correct layer LIFO. No other custom modals exist (album=toast, NowPlaying=full-screen, prompt/confirm=native). check 0/0, 165/165 tests | 2026-06-06 | (no-op) | [260606-ncw-outside-scrim-click-always-closes-any-mo](./quick/260606-ncw-outside-scrim-click-always-closes-any-mo/) |
| 260606-nqf | Now-playing gesture fixes (follow-up ggj/h4s): (1) VELOCITY snap — new pure `src/lib/gestures/velocity.ts` (`createVelocityTracker`, e.timeStamp, 6 tests) wired into `gripUp` (fast flick steps one state in swipe dir; slow drag keeps nearest-by-position) + `dragClose.up()` (flick-dismiss past velocity threshold even if not dragged far, dy>8 tap guard); (2) HALF-OPEN GAP root-caused — `measureOffsets` ran at reflow START reading pre-shrink transport bottom (0.32s cover transition) → overshoot; now re-measures after reflow settles (`transitionend` + double-rAF + 340ms fallback) → flush, zero gap; (3) LYRICS half-open — replaced `scrollIntoView` (walked ancestors → expanded sheet) with panel-scoped `scrollTo` rect-delta math (stays half) + finger-presence auto-scroll (pause on touch/wheel, resume ~600ms after release, was 2.5s idle timer). 3 files, no new deps/i18n; check 0/0, 171/171 tests | 2026-06-06 | ef4fda5 | [260606-nqf-now-playing-gesture-fixes-velocity-inert](./quick/260606-nqf-now-playing-gesture-fixes-velocity-inert/) |
| 260606-nza | Home discovery UX: (A) optimistic tap-to-play — `player.playStub` instantly locks the tapped stub into the now-bar with an indeterminate loading sliver before the ~10s CN-source resolve, same-song double-tap deduped, a newer tap supersedes the stale resolve (generation guard), success→play / miss→toast; (B) Cover Art Archive tile covers — surface Last.fm `mbid` through the discovery reshape, client `caaReleaseGroupCover` builds `coverartarchive.org/release-group/{mbid}/front-250` as an `<img>` over the gradient (onerror→gradient, never a broken image, no safeImageUrl change since mbid is `<img src>` not CSS url()); MusicBrainz no-mbid search fallback deferred. check 0/0, 186/186 tests | 2026-06-06 | 4b7a03d | [260606-nza-home-discovery-optimistic-tap-to-play-wi](./quick/260606-nza-home-discovery-optimistic-tap-to-play-wi/) |
| 260606-oil | Per-part translation redesign (--discuss): each surface (artist name / song title / lyrics / Last.fm tags) gets its OWN target lang + a skip-whitelist of source langs to leave original; no single master lang. New pure `src/lib/i18n/detect.ts` (`detectLang` precise zh-vs-ja via kana/hangul/Latin + simplified/traditional Han signal; `shouldTranslate` skips when source∈whitelist or source==target; 15 tests). Settings split `nameLang`→independent `artistLang`+`titleLang`, kept `lyricsLang`/`appLang`, added `lastfmLang` + per-part whitelist sets; non-destructive localStorage migration (mirrors old nameLang, empty whitelists). appLang stays en/zh-Hant/zh-Hans (ja/ko content-only, no ja chrome dict). Redesigned `/settings/translation` 5-section UI + i18n keys in 3 dicts. Applied: `names.dn`→`dnArtist`/`dnTitle`/`dnLastfm` swept across ~26 call sites (incl home), lyrics per-line whitelist, tag gating. NOTE: merged across a concurrent uncommitted NowPlaying.svelte edit (parallel lyrics-anchor work) — stash/merge/pop auto-merged clean, both preserved. check 0/0, 201/201 tests | 2026-06-06 | b5cacae | [260606-oil-per-part-translation-settings-redesign-i](./quick/260606-oil-per-part-translation-settings-redesign-i/) |
| 260606-rvy | Home-tile polish (follow-up nza): (A) real covers for no-mbid tiles — new `cover-cache.ts` (localStorage `openmusic:cover-cache:v1`, matchKey key, pure+tested) + lazy concurrency-capped (≤3, max 24) `cover-backfill.ts` resolving the real CN-source cover via searchAll→dedupeBest[0].cover; `tileCover` order image→CAA-by-mbid→cached-CN→gradient, lazy `<img>` over gradient (onerror→gradient, never broken/blocking), MusicBrainz no-mbid search still deferred; (B) `dragScroll` action — pointer/mouse drag-to-scroll shelves + capture-phase tap-vs-drag click suppression (touch keeps native scroll); (C) `marquee` action — overflowing tile labels bounce L↔R (ResizeObserver, prefers-reduced-motion), ellipsis otherwise; wired into home + artist albumrow. check 0/0, 217/217 tests | 2026-06-06 | a8b2644 | [260606-rvy-home-tiles-backfill-cache-real-covers-cn](./quick/260606-rvy-home-tiles-backfill-cache-real-covers-cn/) |
| 260606-pkl | Auto-deploy on push to main (--discuss). Method = Cloudflare Pages NATIVE Git integration (dashboard), NOT GitHub Actions — so deliverable is repo config CF reads + setup docs (no workflow file, no GH secrets, no tokens in repo). Scope Pages-only (API proxy is bundled `+server.ts` via adapter-cloudflare `_worker.js`, no separate Worker). Added `.nvmrc` (22) + `package.json` engines.node `>=22` so CF build uses Node 22; `wrangler.jsonc` already correct (name openmusic, output `.svelte-kit/cloudflare`). New `docs/DEPLOY.md` + README Deployment section: connect repo hectorchanht/musicsquare-mobile → openmusic (account f1868a071996e836eae6da2b65f37929), branch main, build `pnpm build`, output `.svelte-kit/cloudflare`, Node 22 / NODE_VERSION fallback, prod env-var reminder (JOOX_TOKEN required; LASTFM_KEY/LASTFM_SECRET optional; LASTFM_ENDPOINT is a constant, NOT a var). No app code. pnpm build → .svelte-kit/cloudflare OK, check 0/0, 201/201 tests | 2026-06-06 | e3ddc69 | [260606-pkl-auto-deploy-to-cloudflare-pages-on-push-](./quick/260606-pkl-auto-deploy-to-cloudflare-pages-on-push-/) |
| 260606-t5r | Prefetch next track for gapless-ish play: private `prefetchNext()` on the Player store pre-resolves `queue[indexOf(current)+1]` (exactly what `next()` picks) via idempotent `ensureTrackDetails(target, signal)`, writing the resolved track back into the queue so a later `play()` short-circuits = instant advance. Fired best-effort `void this.prefetchNext()` from end of `play()` (after ensureAhead/regenerate), non-blocking. Guards: no-op at end/no-current, skip already-`detailsLoaded`, in-flight dedupe (`prefetchingUid`), stale-discard (AbortController + seedUid/slot recheck on current-change). No 2nd `<audio>` (iOS single-element); `next()`/`prev()` semantics unchanged; catalog.ts untouched. 1 file + tests; check 0/0, 222/222 tests (+5) | 2026-06-06 | 01b2ed2 | [260606-t5r-prefetch-next-track-for-gapless-ish-play](./quick/260606-t5r-prefetch-next-track-for-gapless-ish-play/) |
| 260606-v7k | Real covers for home 精選推薦 tiles (fixes: CN-source backfill missed Western charts; artist tiles never backfilled). New no-auth `itunes-cover.ts` (iTunes Search API — `buildItunesSearchUrl`/`upgradeArtwork` 100→600/`itunesSongCover`/`itunesArtistCover`, `AbortSignal.timeout(6000)`, never-throws, NO new dep). `cover-backfill` `resolveOne` now CN-first→iTunes-fallback on miss; new `backfillArtistCovers` (CAP=3, capped, cached, never-throws). `cover-cache` artist-only key (`'artist:'+matchKey`). `+page.svelte` artist-tile cover branch + artist backfill on `coverVer++`. `<img src>` only, onerror→gradient, no edge `safeImageUrl` change, no secret. check 0/0, 326 tests, build OK. Task 4 (runtime visual) DEFERRED-TO-HUMAN (needs LASTFM_KEY locally or openmusic.pages.dev). | 2026-06-06 | e2c1ce3 | [260606-v7k-resolve-real-covers-for-home-discovery-t](./quick/260606-v7k-resolve-real-covers-for-home-discovery-t/) |
| 260606-vx2 | Fix 隨機推薦 / Randomize button — it re-fetched deterministic Last.fm charts (same tiles every press → "broken"). Now genuinely varies: pure `shuffle` (non-mutating Fisher-Yates) + `pickRandomPage` in `discovery.ts`; optional `page` arg threaded into `getChartTopTracks`/`getTagTopTracks`/`getGeoTopTracks` (lastfm.ts) only when page>1 (cold/bg keep cache-friendly request, edge already supported `page`); `refresh()` gains `randomize` param → Randomize fetches a random page + shuffles shelf order + within-shelf tile order + topHits/topArtists, cache-bypassing, saved so reload matches. WR-02/WR-04/never-throws/D-06 fallback preserved; no new dep/endpoint; DISCOVERY_TAGS/COUNTRIES left non-configurable (later task). check 0/0, 332 tests (+6), build OK. Task 3 (runtime visual) DEFERRED-TO-HUMAN (needs LASTFM_KEY). | 2026-06-06 | a8e31ae | [260606-vx2-fix-randomize-button-to-actually-vary-th](./quick/260606-vx2-fix-randomize-button-to-actually-vary-th/) |
| 260606-w87 | Home layout customization settings. New `/settings/home` sub-page: drag-reorder + show/hide the home sections (top-hits/top-artists/tag+country shelves), pick which discovery tags/countries show, items-per-shelf slider, default landing tab (home/search/library), tile density (comfortable/compact), chrome toggles (search pill + Randomize). New pure `home-layout.ts` (resolveSectionOrder/resolveSubset/clampShelfSize + owns the discovery pools — broke a settings↔discovery import cycle), `dragReorder` touch action (computeDropIndex), 9 settings fields with NON-destructive migration (defaults reproduce today exactly), corrupt-config validation on load. Home + (app) shell consume it; landing-tab redirect guards ?play=/SSR. i18n ×3 (parity test). Theme (light/dark + accent CSS-var wiring) intentionally DEFERRED to a separate task. check 0/0, 365 tests (+33), build OK. Task 4 (runtime: touch-drag/visual/Last.fm-home) DEFERRED-TO-HUMAN. | 2026-06-06 | 5493f1a | [260606-w87-home-layout-customization-settings-reord](./quick/260606-w87-home-layout-customization-settings-reord/) |
| 260606-wv8 | Deezer edge proxy + Deezer as PRIMARY cover source (covers-only; charts/info = later tasks). New `/api/deezer/search` edge proxy (Deezer blocks browser CORS — confirmed via curl) mirroring the Last.fm proxy posture: own-origin corsHeaders, OPTIONS 204, encoded `q` passthrough, `.dzcdn.net` https image-host allow-list, caches.default TTL, AbortSignal.timeout, fetchWithRetry, NO key/secret (Deezer public). New `deezer.ts` client (`deezerSongCover`/`deezerArtistCover`). Cover chain now: track = Last.fm image→CAA(mbid)→backfill(**Deezer→CN**)→gradient; artist = Last.fm→CAA→backfill(**Deezer**)→gradient. **iTunes fully removed** (deleted itunes-cover.ts + test + all refs). STRIDE register T-wv8-01..07. Preserves backfill contracts (lazy/post-paint, CAP=3+max, skip-cached, never-throws→gradient, onerror→hide, coverVer). check 0/0, 383 tests (+18), build OK (worker entry emitted). Task 3 (runtime visual) DEFERRED-TO-HUMAN. | 2026-06-07 | 917ee88 | [260606-wv8-deezer-edge-proxy-plus-deezer-as-primary](./quick/260606-wv8-deezer-edge-proxy-plus-deezer-as-primary/) |
| 260607-0bb | Strengthen cover backfill so MOST tiles fill (was: only ~36 of ~270+18 tiles attempted → rest stayed gradient forever). Root cause = home `scheduleBackfill` hard-capped `max:24`/`12`. Fix: restored iTunes resolver (verbatim from 6c44889; re-added to chain), multi-tier first-solid-wins chains — track **Deezer→iTunes→CN**, artist **Deezer→iTunes** (each tier never-throws, `isSolidCover` https-only guard); CAP 3→6; `max` → `rows.length`/`artistNames.length` (every gathered gradient tile attempted); skip-cached + per-call AbortSignal.timeout bound self-DoS (iTunes/CN only fire on a Deezer miss → first visit mostly cached Deezer, warm reload ~0 requests). Last.fm stays the synchronous `item.image` pre-check in tileCover (album.getInfo last-resort dropped as low-value). check 0/0, 408 tests (+~25), build OK. Task 3 (runtime: MOST tiles + artist avatars show real covers — 4th attempt at this symptom) DEFERRED-TO-HUMAN. | 2026-06-07 | 8a0a695 | [260607-0bb-strengthen-cover-backfill-chain-deezer-i](./quick/260607-0bb-strengthen-cover-backfill-chain-deezer-i/) |
| 260607-n0u | Nowbar component extraction + reused as NP fullshrink top bar (mtv follow-up — user explicitly asked to reuse the nowbar). New `src/lib/components/Nowbar.svelte` (170 LOC) with two variants: **'docked'** (default, position:fixed bottom, the original layout mini-player; behavior unchanged) and **'embed'** (position:static; sits in parent flow). NowPlaying.svelte renders `<Nowbar variant="embed" onOpen={() => sheetState='closed'} />` at the top of .np when sheetState==='full'; tapping the bar collapses the subnav sheet (returns to full Now Playing view, queue hidden, cover + controls visible again). `.np.fullshrink` CSS now HIDES the previous fullshrink chrome (.bar/.cover/.meta/.prog/.transport/.np-error) — embed Nowbar carries the same information in one compact row, replacing the absolute-positioned scaffolding from mtv. Sheet inset pushed by `var(--nowbar-h) + 16px` so queue/lyrics panel never overlaps. Layout file dropped ~95 lines of inline nowbar markup + CSS in favor of `<Nowbar />`. Verified live: docked Nowbar still renders at viewport bottom when NP collapsed (regression on the extraction); embed-variant verification bounded to the gated conditional + static CSS (drag-up gesture not feasible via synthetic pointer events). check 0/0, 415 tests. | 2026-06-07 | b1edb6d | [260607-n0u-nowbar-component-extraction-np-fullshrin](./quick/260607-n0u-nowbar-component-extraction-np-fullshrin/) |
| 260607-mtv | NP fullshrink mini-bar visible above open sheet (ju0 P4 follow-up — bug fix). The ju0 P4 fullshrink layout positioned cover/title/artist/mini-play absolutely at the top of `.np` (z:2-3), but `.sheet.full { inset: 0; z:5 }` was painting over the entire viewport including the mini-bar area — user never saw the mini-bar, only the queue/lyrics panel. **Fix**: in `.np.fullshrink`, override sheet inset to `64px 0 0 0` so the sheet starts BELOW the mini-bar (matching the existing reserved `padding-top: 64px`). Mini-bar paints above; sheet renders panel underneath; spatially separated, no z-fight. Single-line CSS addition. Verified live (preview-eval, forcing the classes to bypass drag-up gesture): sheet computed `top: 64px` (was 0px); cover 48×48 at `left:64 top:6`; meta at `left:120 top:8`; bar at `top:4` — matches the YouTube Music sticky-bar shape in the user's reference screenshot. check 0/0, 415 tests. | 2026-06-07 | b9ff702 | [260607-mtv-np-fullshrink-mini-bar-above-open-sheet](./quick/260607-mtv-np-fullshrink-mini-bar-above-open-sheet/) |
| 260607-m4e | Playlist Play-all head + History Clear top-right + **seek-restart bug fix**. **P1**: playlist head Trash2 now editMode-only; non-edit shows Play (runs playlist from track 0); empty playlists hide the button. Consistent with row affordance (Play default, Trash2 in edit). **P2**: Clear History button moved from list bottom to header-right slot (same row as "Library · History") via new `.edit-btn.danger` variant; bottom `.clear-history` + CSS deleted. **P3** — fixed reported bug "click on progress bar on songs triggered from Downloads/History restarts from 0". Root cause: seeking past buffered range on non-range-capable CDN raises audio.error → cross-source fallback (gte SRC-FB-01) treated it as playback failure → called play() → `currentTime = 0`. Fix: new `lastSeekAt` field + static `SEEK_ERROR_WINDOW_MS=1500`; seekFraction + both OS seekbackward/seekforward handlers stamp `lastSeekAt = Date.now()`; the audio.error listener returns early when `Date.now() - lastSeekAt < window`. Genuine playback failures (no recent seek) still flow through runFallback. Universal fix — Downloads/History showed it more often because their tracks land on CDNs without HTTP Range support more often. i18n: `library.playAll` × 15. Verified live: playlist head shows lucide-play in non-edit, lucide-trash-2 in edit; History header reads "Clear history" with danger tint; bottom .clear-history gone. check 0/0, 415 tests. | 2026-06-07 | c31cbea | [260607-m4e-library-playlist-play-all-history-clear-](./quick/260607-m4e-library-playlist-play-all-history-clear-/) |
| 260607-lw9 | Play default icon on Playlists/Downloads rows + longer cache TTLs. **T1**: Playlists + Downloads rows now render `lucide-play` in non-edit mode (matches Liked/History); edit-mode keeps Trash2 + row-click removes. Inline per-row `.del` bin on Downloads removed — removal lives behind the Edit pill like every other bulk-editable tab. Consistent affordance, no more visual ambiguity. **T2** — user said long TTLs fine for a music app's stable data: TTL_COVER 24h→**7d** (covers effectively immutable per release), TTL_SEARCH/RELATED 1h→**6h** (rankings drift slowly, charts update daily but stale chart invisible to user), TTL_LASTFM 1h→**6h** (bio/tags/listeners stable), TTL_SIMILAR 1h→**6h**, catalog SEARCH_TTL_MS 5min→**60min** (search-page repeat is the hot path; fresh load still on the search button). Drops cold-fetch load to near zero within a typical session. Cleaned up the now-dead .rowline CSS while at it. Verified live: Downloads + Playlists row SVGs = `lucide-play`; inline `.del` gone; edit-mode flips to `lucide-trash-2`. check 0/0, 415 tests. | 2026-06-07 | 0b5519d | [260607-lw9-library-play-default-longer-ttls](./quick/260607-lw9-library-play-default-longer-ttls/) |
| 260607-lry | Library tab persistence + full cache audit. **T1**: library `tab` persists to `openmusic:library:tab` on every switch via a new `setTab(v)` helper; restored synchronously from $state init so the right tab renders frame 1 (no flash). Corrupt values fall back to 'liked'. **T2 — cache audit** (user asked "is everything cached?"): confirmed every fetched layer is cached in at least one place. Home tile covers in localStorage cover-cache (`openmusic:cover-cache:v1`); browser HTTP cache handles repeat image GETs (persistent Deezer/iTunes/CN/CAA hosts). Deezer + Last.fm query helpers TTL-cached at 1h/24h (k3y). **`searchAll` aggregate ALREADY TTL-cached 5min** at the catalog seam (`cached()` wrap in catalog.ts, key=`${query}|${enabledSources}|${page}` — covers the search page, discovery stub-resolve, diverse-picks, similar-queue). Track details kept un-TTL-cached on purpose (audio URLs expire ~1–2h — must re-resolve). Downloaded songs → local-play via blobStore + player.play() blob-URL gate (kyf). Search-page restoration on tab return via searchSession in-memory (last query, scroll position). Verified live: switch to Downloads → localStorage="downloads" → hard nav `/` then back → restored on Downloads, header reads "Library · Downloads". No code change for T2 — already done. check 0/0, 415 tests. | 2026-06-07 | 576478b | [260607-lry-library-tab-persist-cache-audit](./quick/260607-lry-library-tab-persist-cache-audit/) |
| 260607-lni | Search artist-tiles loosened (kyf follow-up). The kyf gate (≥2 tracks AND name contains-or-contained-by query, capped to 3 tiles) surfaced 0–2 tiles on most real queries. Replaced with: one tile per UNIQUE artist in `results`, sorted by track count desc (tie-break first-seen-order so the row mirrors the song-list ranking), NO cap. Row is horizontally scrollable via `use:dragScroll` + `flex-wrap:nowrap; overflow-x:auto` (scrollbar hidden). Avatar concurrency bumped 3→6 for the longer list; enrichArtist + deezerArtistCover are already TTL-cached client-side so repeat queries are free. Verified live (search "love"): 35 tiles for 37 songs (≈1:1 unique-artist ratio); scrollWidth 3768px vs clientWidth 351px (overflow confirmed); 24/35 tiles show real LF/Deezer avatars within ~12s, rest keep gradient fallback (graceful posture preserved). check 0/0, 415 tests. | 2026-06-07 | b7773bf | [260607-lni-search-artist-tiles-all-unique-artists-n](./quick/260607-lni-search-artist-tiles-all-unique-artists-n/) |
| 260607-ljl | Library tabs overflow fix (kyf follow-up). 5 pills (Liked/Playlists/Downloads/Favourite artists/History) clipped at narrow widths. Pills shrunk to icon-only (16px lucide, no text); active tab's label moved beside the heading as "Library · {tabLabel}" via a $derived switch on the Tab union. aria-label + title preserved on each pill (screen readers + hover tooltips). Header is now `align-items: baseline; flex-wrap: wrap` so heading row can wrap on extremely narrow viewports without crowding the Edit pill. Single file. Verified live: 5 pills render in 374px container with scrollWidth===clientWidth (zero horizontal overflow); header reads "Library · Favourite artists" when the 4th tab is active. check 0/0, 415 tests. | 2026-06-07 | c3b068f | [260607-ljl-library-tabs-icon-only-active-label-move](./quick/260607-ljl-library-tabs-icon-only-active-label-move/) |
| 260607-kyf | Four sub-asks, one batch. **P1 — Offline blob cache** (completes deferred ju0 P3): new `src/lib/services/blob-store.ts` wraps IndexedDB (db `openmusic-blobs`, store `tracks`, key=uid) behind a never-throws + SSR-safe `{put,get,del}` API; lazy/cached open. TrackMenu's doDownload writes the same fetched Blob into the store before the anchor.click. `library.removeDownload` also calls `blobStore.del` so cache + registry stay in sync. `player.play()` consults the cache after ensureTrackDetails settles — when `library.isDownloaded(uid)` AND `blobStore.get(uid)` returns a Blob, `audio.src = URL.createObjectURL(blob)` (prior Object URL revoked on the swap); miss falls through to the CDN URL transparently. **P2 — Artist back-arrow**: replaced the literal "‹ Back" anchor with a `ChevronLeft` icon button (history.back + aria-label "Back" via existing common.back key), matching every other page's back affordance. **P3 — Search artist tiles**: after each result settle, derive ≤3 artist tiles from groups in `results` where ≥2 tracks share an artist whose name contains-or-is-contained-by the query; avatars via the same LF-primary→Deezer-fallback chain used on the artist page (same source as the hero — jip avatar fix); race-guarded on active query; round 96×96 tiles above the song list, tap → /artist/<name>. **P4a — Library fav-artists tab**: new `'fav-artists'` Tab with Users lucide icon; responsive auto-fill grid of round avatars from `library.favArtists`; tap navigates, editMode tap removes via toggleFavArtist; per-tile avatars lazy-load (LF→Deezer, cap 4, cached per-mount via favCovers Record); editableTabHasContent extended; empty-state hint. **P4b — Home shelf reorder**: `HOME_SECTIONS` canonical moved `'fav-artists'` from after `'downloads'` (kmn position) to directly after `'top-artists'` — natural artist-shaped neighbor + more visible default; resolveSectionOrder still preserves saved user order, only auto-append position for legacy saves changes; test fixtures updated. **i18n**: 3 keys × 15 locales (search.artists, library.favArtists, library.noFavArtists). Verified live (vite dev): back-arrow renders lucide chevron-left with aria "Back"; search "Daft Punk" → 2 artist tiles (Daft Punk + Daft-Punk collab) above 23 song results; library tab strip = Liked/Playlists/Downloads/**Favourite artists**/History with Daft Punk avatar in the new tab; IndexedDB `openmusic-blobs.tracks` writable + readable from page. Blob-routing path in player.play() is mechanical (isDownloaded gate → blobStore.get → URL.createObjectURL) + never-throws miss-falls-through-to-CDN — exercised on real downloaded tracks. check 0/0, 415 tests. | 2026-06-07 | cefd7dc | [260607-kyf-offline-blob-cache-artist-back-arrow-sea](./quick/260607-kyf-offline-blob-cache-artist-back-arrow-sea/) |
| 260607-kmn | Artist-page action buttons (3 pills in the hero: **Favourite/Play/Share**) + completes the deferred ju0 P5 fav-artists shelf. **Library**: new `favArtists: string[]` bucket + `isFavArtist`/`toggleFavArtist` (case-insensitive match, case-preserving), persisted into existing `openmusic:library:v1` with non-destructive migration. **Home**: `HOME_SECTIONS` extended with `'fav-artists'` (resolveSectionOrder auto-appends for existing users — new test for the upgrade path); new round-avatar shelf snippet using the existing `backfillArtistCovers` chain (Deezer → iTunes); arrangement persisted in `LibraryShelfCache` with a legacy-cache fallback that seeds from the live bucket on first reload. Settings/home exposes the new section in the reorder list. **Artist page**: 3 pill buttons (matching album-page visual language) — Heart toggles favArtists with toast + aria flip; Play picks a random song from `songs`, Fisher-Yates shuffles rest, queues `[picked, ...rest]`, `player.play(.., {fresh:true})` (disabled while loading); Share uses `navigator.share({title,text,url})` with clipboard fallback + existing `toast.shareCopied`. **i18n**: 7 new keys × 15 locales (settings.homeSectionFavArtists, artist.favorite/unfavorite/playArtist/share, toast.artistFavorited/Unfavorited). Verified live (vite dev): 3 buttons render on /artist/Daft Punk → tap Favourite → class flips to `.on`, label → "Favourited", localStorage carries `favArtists:["Daft Punk"]`; home `/` shows new "Favourite artists" subhead with round-avatar tile for Daft Punk; /settings/home reorder list includes "Favourite artists". check 0/0, 415 tests (+1). | 2026-06-07 | 93dbf71 | [260607-kmn-artist-page-action-buttons-favourite-wir](./quick/260607-kmn-artist-page-action-buttons-favourite-wir/) |
| 260607-k3y | Centralized defaults config + per-tab reset + cache audit (3 sub-asks). **P1 — Defaults + reset**: new `src/lib/config/defaults.ts` (single editable source of truth — GENERAL/APPEARANCE/TRANSLATION/PLAYBACK/HOME `as const` groups + aggregate `DEFAULTS`); `settings.svelte.ts` gains `resetGeneral`/`resetTranslation`/`resetPlayback`/`resetHome` (mirrors existing `resetAppearance`), each reads from DEFAULTS group → applies → saves. Reset pill added to header of every customizable settings tab (general/appearance/translation/playback/home) — confirm() then call. 3 i18n keys × 15 locales (`settings.resetGroup`/`resetConfirm`/`resetDone`). **P2 — Client TTL cache (Deezer)**: wrapped `deezerSongCover` (24h) / `deezerArtistCover` (24h) / `deezerSearchTopN` (1h, the dedupe-deezer hot path) / `deezerRelatedArtists` (1h) / `deezerChart` (1h) via existing `services/ttl-cache.ts` `cached()`. **P3 — Cache audit + broader caching**: confirmed CF auto-caches Pages static assets but NOT Worker routes (`/api/*` must explicitly call `caches.default`). Existing routes already edge-cache: deezer/search/related/chart, lastfm/discovery. **Added edge cache** to `/api/jamendo/search` + `/api/fivesing/search` at TTL 1h, mirroring wv8 posture (own-origin Request cache key, CORS-free cached body, origin re-applied on hit, no cache write on upstream error). **Added client cache** to `getSimilarArtists` (1h) + `fetchInfo` (1h, sorted-param key — covers all LF helpers: enrichTrack/Artist/Album + chart/tag/geo/album-tracklist). Fivesing `/url` intentionally left uncached (audio URLs expire ~1-2h). Test isolation: added `__clearSearchCache()` in `beforeEach`/`afterEach` of lastfm.test.ts + similar.test.ts (module-scoped store was leaking across runs). check 0/0, 414 tests. | 2026-06-07 | b67f026 | [260607-k3y-reset-tabs-defaults-deezer-ttl-cache](./quick/260607-k3y-reset-tabs-defaults-deezer-ttl-cache/) |
| 260607-ju0 | Partial (3/5 items; user pivoted to new task). Shipped: **Auto (app language) translation** — LyricsLang widened to include 'auto', new `effectiveTarget()` resolver in settings, threaded through names.dnArtist/dnTitle/dnLastfm/dnBio + NP lyrics-translate effect, Auto chip added to all per-part pickers. **More-like-this LF avatars** — artist-page related shelf was hitting deezerArtistCover (wrong matches on partial names), switched to enrichArtist().lastfmArt (same source as hero); Deezer fallback when LF empty. **NP full-shrink top bar** — new .np.fullshrink CSS class (superset of .reflow), cover+title+artist+mini-play collapse to YT-Music-style horizontal bar at top when sheetState==='full'; transport+progress hidden (sheet covers them). Deferred: offline-cache (P3, IndexedDB blob store + play() routing — standalone task), fav-artists (P5, partial scaffold reverted to avoid dangling UI). check 0/0, 414 tests, build OK. | 2026-06-07 | c7f9ac7 | [260607-ju0-offline-download-cache-np-full-open-mini](./quick/260607-ju0-offline-download-cache-np-full-open-mini/) |
| 260607-jip | Wired jau's Deezer helpers into real consumers. **Artist page** gains "More like this" round-avatar shelf using `getSimilarArtists()` (Last.fm primary → Deezer fallback) + `deezerArtistCover` for covers at concurrency 4; race-guarded $effect mirrors albums/enrich shape; `.al-cover.round` + `.al-name.center` style variants added; new `artist.moreLikeThis` i18n key × 15 locales. **Cross-source dedupe quality boost**: new `services/dedupe-deezer.ts` exports `dedupeBestWithDeezer(tracks, preferred, signal)` — async wrapper that runs sync `dedupeBest` for baseline order, re-groups input by matchKey, fires `deezerSearchTopN(title+artist, 2)` per multi-candidate group at concurrency 3 (limit MUST be >1 — proxy only populates results[] on >1, caught+fixed during live verify), scores candidates via pure `scoreAgainstDeezer` (title 0/2/3 + artist 0/2/4 + album 0/1/3), swaps non-baseline winner only when score strictly higher (ties keep sync floor). Wired into /search after first paint; aborts on supersede. Verified live: artist page renders 6 real Deezer-avatar tiles (Justice/Modjo/Stardust/Thomas Bangalter/Kavinsky/Cassius); dedupe boost test flipped a netease(wrong album)→joox(canonical album) pick (sync score 7 vs 10, sync picked netease via SOURCE_RANK, boost flipped to joox via album signal). check 0/0, 414 tests, build OK. | 2026-06-07 | bad6185 | [260607-jip-more-like-this-shelf-on-artist-page-deez](./quick/260607-jip-more-like-this-shelf-on-artist-page-deez/) |
| 260607-jau | Deezer metadata extension (SoundCloud skipped via AskUserQuestion — HLS-only on newer uploads + closed free-tier client_id reg). Extended `/api/deezer/search` with `?limit` ([1,25]); limit=1 stays byte-compat (cover/backfill callers unchanged), limit>1 surfaces `results: DeezerHit[]` (id/title/artist/album/cover/preview), previews validated via new `safePreviewUrl` (https + *.dzcdn.net). New `/api/deezer/related` proxy: search/artist→id then artist/{id}/related, returns `{artists:string[]}` mirroring /api/similar shape. services/deezer.ts adds `deezerSearchTopN` + `deezerRelatedArtists` (never throw, FETCH_TIMEOUT_MS bound). services/similar.ts: Last.fm primary → Deezer fallback → same-artist fallback (a no-LASTFM_KEY deployment now gets real recommendations instead of only same-artist). Verified live: backcompat limit=1 has no results key; limit=5 returns 5 hits with cover+preview; /related Daft-Punk→[Justice, Cassius, Etienne de Crécy, Mr. Oizo, Yuksek]; getSimilarArtists works (LF primary). DeezerHit type exposed; no UI consumes it yet — building blocks ready for a future "More like this" shelf or cross-source dedupe signal. check 0/0, 414 tests, build OK. | 2026-06-07 | 141d21d | [260607-jau-soundcloud-source-if-shippable-deezer-me](./quick/260607-jau-soundcloud-source-if-shippable-deezer-me/) |
| 260607-j49 | Extended library bulk-edit from Liked (ii6) to Downloads + per-playlist views. `rowAction(track, list, playlistId?)` now dispatches by tab: liked→toggleLike, downloads→removeDownload(uid), playlists→removeFromPlaylist(plId, uid). Edit pill appears via new `editableTabHasContent` derived on any non-empty bulk-editable tab. Each row gets `.edit-row` + Trash2 affordance in edit mode (mirrors Liked). History intentionally read-only (own Clear-all suffices). Downloads' inline per-row `.del` hidden during edit-mode (unambiguous affordance) but still works outside edit. Playlist-head delete left alone (different scope). Verified live (3 downloads, 1 playlist×2 tracks): Edit toggles, Downloads 3→2 + inline del disappears, Playlists 2→1. check 0/0, 414 tests. | 2026-06-07 | e1e4152 | [260607-j49-extend-library-bulk-edit-to-downloads-pe](./quick/260607-j49-extend-library-bulk-edit-to-downloads-pe/) |
| 260607-ixw | Added Jamendo (Western CC-indie) as 6th song source. User shipped `client_id=1df0a42f` (public-by-design like LASTFM_KEY); `client_secret` intentionally NOT carried (only needed for OAuth flows we don't implement). Ships `enabledByDefault:false` — opt-in via the Playback Advanced—Sources accordion (ii6). New: types.ts widens SourceId; sources/jamendo.ts adapter (search returns direct mp3 in `audio`, resolve() just stamps quality from URL ext); routes/api/jamendo/search/+server.ts proxy → api.jamendo.com/v3.0/tracks with audioformat=mp32, limit/offset clamped; proxy-types Env adds OPTIONAL JAMENDO_CLIENT_ID (absent → `{results:[]}` graceful state mirroring LASTFM_KEY); wrangler.jsonc `vars.JAMENDO_CLIENT_ID:'1df0a42f'`; registry+test (EXPECTED_KEYS=6); SOURCE_RANK.jamendo=-1 (below fivesing) so never wins ties; --src-jamendo color. Verified live: proxy returned 3 hits + headers.code=0 for "guitar"; adapter via `searchAll('guitar',1,{jamendo:true})` returned 20 tracks with uid='jamendo:1270090', cover+audioUrl populated, audio at prod-1.storage.jamendo.com (real progressive mp3). check 0/0, 414 tests, build OK. Summary also enumerates the remaining-candidate sources (Deezer-metadata + SoundCloud realistically shippable; Migu/Kugou geo-blocked; Bilibili DASH; JioSaavn DES-encrypted; YouTube Music still deferred). | 2026-06-07 | 533d989 | [260607-ixw-add-jamendo-western-cc-indie-as-6th-song](./quick/260607-ixw-add-jamendo-western-cc-indie-as-6th-song/) |
| 260607-ii6 | UI polish across album / library / playback / NowPlaying (7 items, single commit c623a5f). **Album**: `albumBusy` boolean → `busyAction: AlbumAction | null` so only the clicked button disables (other buttons stay live); long-press track rows now open TrackMenu against resolveStub'd track (skeleton while resolving); album-like heart fill = `$derived albumLiked` over resolvedCache + library.liked (filled when all liked → unlike all → outline; outline → like missing → fill); download falls back to `window.open(audioUrl)` per track when fetch→blob hits CORS (root cause of "no files downloaded"). **TrackMenu**: like toast fixed (`menu.like` action verb → `toast.liked`/`toast.unliked` past tense against post-toggle state); new "Shuffle queue" menu item gated on `queue.length>1` driving `player.toggleShuffle()`. **Library bulk-edit (Liked tab v1)**: editMode flag + Edit/Done pill top-right; in edit-mode row click runs `library.toggleLike` (removes) instead of playList, Trash2 icon replaces play affordance, `.edit-row` red tint; tab switch auto-exits via `$effect`. **Playback Advanced — Sources accordion** (locked via AskUserQuestion): new `settings.enabledSources: Partial<Record<SourceId,boolean>>`; `getEnabledAdapters` precedence = explicit prefs > settings.enabledSources > adapter.enabledByDefault (single change reaches every searchAll caller); `<details>` chips at bottom of /settings/playback let user opt INTO 5sing. **NowPlaying transport swap**: leftmost button now Like (Heart fill follows `currentLiked` derived from `library.isLiked(player.current.uid)`, flashes local 1.5s toast); Shuffle removed from transport row → moved into TrackMenu's new shuffle-queue item. **i18n**: 7 new keys × 15 locales (common.done, library.edit, toast.liked, toast.unliked, settings.sourcesAdvanced + Note, menu.shuffleQueue). Verified live: NP transport order = Like/Prev/Play-Pause/Next/Repeat with toggle+toast working; Library 3 liked → Edit→Done flips, tap row removes 3→2; Playback accordion shows 5 chips, toggling 5sing persists `enabledSources:{fivesing:true}`. check 0/0, 414 tests, build OK. | 2026-06-07 | c623a5f | [260607-ii6-album-polish-per-button-disable-download](./quick/260607-ii6-album-polish-per-button-disable-download/) |
| 260607-hvu | Album-actions fixes + verify cross-source fallback + add 5sing (Kugou UGC). **PART A (9975ce2)**: album-page action bar made truly functional — Download now saves REAL files to disk via fetch→blob→anchor.click (was only adding to library Downloads tab) for every track at downloadQuality, staggered 250ms apart; Like-all idempotent + toggle-when-full (any unliked → like missing; all liked → unlike all, locked via AskUserQuestion); "preparing..." toast now flashes immediately on like + add-to-playlist click for visible feedback during the ~10s resolveAll fan-out. **PART B**: walked + live-traced cross-source fallback from gte SRC-FB-01 (audio.error → runFallback → tryFallback → play(swap, fromFallback:true), with playGen supersedence + AbortController watchdog). Synthesized an audio error on a real track, observed ~2s fallback, fresh netease URL took over in audio.src. No code change. **PART C (9311228)**: added **5sing** (Kugou's UGC sub-platform — covers/karaoke/originals) as the 5th song source. Researcher (musicsquare+musicdl+edge probes) confirmed 5sing the only candidate hitting all four constraints (edge-reachable, direct progressive mp3, no signed-state auth, fits one quick task) AND filling a real catalog gap. Ships `enabledByDefault:false` (UGC noisier than mainstream). Wired: SourceId widened, new SourceAdapter (strips <em class="keyword">, defensive songtype recovery from songid prefix), two edge proxies via http (upstream TLS cert mismatched — Workers allow http, clients see own-origin https), registry entry, registry-test EXPECTED_KEYS updated, PROXIES type changed to Partial since fivesing uses dedicated routes not catch-all (catch-all now 404s absent ids), SOURCE_RANK.fivesing=0 so UGC never wins ties, --src-fivesing color. Identity-critical: uid folds songtype as `fivesing:<type>-<id>` because songId isn't unique across fc/bz/yc (mirrors JOOX Pitfall 4). Verified live: search "稻香" → 10 results (mix of fc covers + bz karaoke), resolve returned code:1000 + real mp3 on wsaudiobssdlbig.kugou.com (HQ+LQ tiers), adapter end-to-end via `searchAll('稻香',1,{fivesing:true})` returns properly-shaped Tracks with uid='fivesing:fc-15504842', songid='fc-15504842', title='稻香' (em stripped), album='翻唱', fivesingSongType='fc'. check 0/0, 414 tests, build OK. Jamendo deferred (needs free API-key registration). | 2026-06-07 | 9311228 | [260607-hvu-album-actions-wiring-verify-cross-source](./quick/260607-hvu-album-actions-wiring-verify-cross-source/) |
| 260607-hhd | Library shelves on home + revert gte regressions. **PART A (ba7f437)**: HOME_SECTIONS extended 4→8 (`['liked','downloads','top-hits','top-artists','tags','countries','playlists','history']`); brand-new users see Liked+Downloads pinned ABOVE Top hits, returning users keep their saved order (resolveSectionOrder appends new ids cleanly). New library shelves on home built from `library.liked/downloads/playlists` + `playHistory.entries`; `buildLibraryShelves(randomize)` does Fisher-Yates or head-of-list slice, capped to homeShelfSize. Persisted as uid arrays in `openmusic:home-library:v1` (separate from chart cache); `loadLibraryCache()` resolves uids→live Tracks on cold mount, drops stale uids. Plugged into existing `refresh()` so Randomize re-rolls library shelves alongside charts. 4 new `{#snippet}` blocks reuse the `.album/.al-cover` tile layout via a shared `librarySongRow(track)` snippet; tap → `player.play(track, {fresh:true})`. Empty-source gating renders nothing (no header). Settings/home `sectionLabel` record extended → reorder+toggle UI surfaces them automatically. Settings/data "Clear cached top picks" wipes the new library cache too. 4 new i18n keys × 15 locales. Import-shadow bug spotted+fixed: `history` store imported as `playHistory` so it doesn't shadow `window.history.replaceState` in the existing `/?play=` deep-link path. **PART B revert (5449864)**: dropped quick-260607-gte's swipe-up gesture + FLIP morph (regressions: tap-to-open broke, morph didn't visibly grow inner elements — user offered fallback). Deleted `morph.svelte.ts`; removed lift state/handlers + nowbar pointer markup + `.nowbar.lifting` CSS from layout; removed onMount/tick/morph store imports + titleEl2/artistEl2 refs + morphActive/morphRunning + applyMorphFrom + onMount runner from NowPlaying; restored single `transition:fly`; dropped `.np.morph` chrome CSS. KEPT gte's keyboard shortcuts \$effect. Verified live: new-user default subhead order = Liked → Downloads → Top hits/artists/genres/countries → My Playlist → Recently played; Randomize reshuffled liked A,B,C→C,A,B and downloads A,B→B,A; reload preserved randomized order; nowbar click opens NowPlaying. check 0/0, 414 tests, build OK. | 2026-06-07 | ba7f437 | [260607-hhd-library-sourced-home-shelves-liked-playl](./quick/260607-hhd-library-sourced-home-shelves-liked-playl/) |
| 260607-gte | NowPlaying + playback resilience (4 parts; decisions via AskUserQuestion: hand-rolled FLIP for the morph + industry-standard tri-state repeat). **P1 — SRC-FB-01**: cross-source fallback on play failure — new `services/fallback.ts` (`tryFallback` + pure `fallbackOrder`) walks enabled sources (preferred-source first, failed source excluded) via `searchAll → dedupeBest → ensureTrackDetails`; hooked into `play()` no-audioUrl path + `<audio>` error event; `playGen` supersedence guard + watchdog AbortController; fallback-continuation play() skips history.record + playGen bump. **P2**: real `player.shuffle` (Fisher-Yates queue tail, current pinned, no auto-unshuffle) + `player.repeatMode: 'off'|'one'|'all'`, audio `ended` honors 'one' (rewind), `next()` end-of-queue honors 'all' (wrap to queue[0]). **P3**: NowPlaying transport binds to store; repeat icon `Repeat1` for 'one' else `Repeat`, `.on` whenever !== off; aria-label per mode; 2 new i18n keys × 15 locales. **P4**: window keydown $effect on NowPlaying mount — Space=toggle (preventDefault) / ArrowLeft=prev / ArrowRight=next; suppressed on INPUT/TEXTAREA/SELECT/contentEditable + isComposing (IME). **P5**: shared-element FLIP morph nowbar→NP — new `stores/morph.svelte.ts` (from-rect store), nowbar click + swipe-up commit snapshot rects of .np-art/.np-title/.np-artist BEFORE expand; NowPlaying.onMount applies inverse translate+scale + inline font-size on .cover/.title/.artist, two rAFs drop transforms → CSS transition (320ms) morphs into rest; .morph class on .np fades+scales chrome (bar/prog/transport/sheet) in parallel; `in:fly` duration 0 when morph owns entry, `out:fly` stays for collapse; reduce-motion skips FLIP. Swipe-up gesture on nowbar: pointerdown/move/up state machine, LIFT_THRESHOLD=60px, mid-drag transform translateY+scale 1..1.06, snap-back below threshold, expand commit above; pointer-capture try/catch'd. Verified live: cover scale 0.168→0.354→0.514→0.743 (frame-by-frame), title fs 13→24px, swipe 30px→snap, swipe 80px→commit; keyboard advanced track; shuffle/repeat icon cycles correct. check 0/0, 414 tests, build OK. Reverse-collapse-morph deferred. | 2026-06-07 | 95c61e6 | [260607-gte-now-playing-playback-resilience-cross-so](./quick/260607-gte-now-playing-playback-resilience-cross-so/) |
| 260607-gmy | Unify marquee implementations onto the global transform-based `.marquee-inner` system in app.css. Home/search already used it; artist + NowPlaying carried a parallel `text-indent`-based `marquee-bounce` keyframe + `:global(.marquee-on)` rule per file (text-indent reflows each frame → jank; transform composites). NowPlaying: wrapped `.title`/`.artist` text in `<span class="marquee-inner">`, deleted local `@media(.title/.artist:global(.marquee-on))` + `@keyframes marquee-bounce`. Artist: markup already used `.marquee-inner` since rvy; just deleted the dead local rule + keyframe + stale FIX-C comment. Verified: only `marquee-scroll` keyframe remains in src; every `use:marquee` site has an inner `.marquee-inner`; NowPlaying overflow case (scrollW 921 > clientW 465) animates `marquee-scroll 8s alternate`. check 0/0, 414 tests, build OK. | 2026-06-07 | db4921c | [260607-gmy-unify-marquee-convert-artist-nowplaying-](./quick/260607-gmy-unify-marquee-convert-artist-nowplaying-/) |
| 260607-fnp | Appearance customization + Data/Translation polish (4 parts; decisions via AskUserQuestion: %-sliders, Title/Artist/Lyrics, cover-size+grid-cols). **P1:** new `/settings/appearance` page — per-part font-size % sliders (title/artist/lyrics) + cover-size scale + home grid-columns, applied app-wide via CSS custom props set in `settings.applyTheme()` (`--fs-*`/`--cover-scale`/`--home-grid-cols`), `app.css :root` defaults 1×/3 so SSR+returning users unchanged; threaded the vars into every title/artist/lyrics font-size (home/search/queue/NowPlaying/TrackMenu/artist/album/library) + home tile/cover/grid; `resetAppearance()`; new Appearance row in settings index. **P2:** Data tab clears name-tr cache (`names.clearCache`) / cover cache (new `clearCoverCache`) / search history + reset appearance. **P3:** translation page reordered Lyrics-mode→Lyrics→Artist→Title→Bio→AppLang with dividers; "Translate mode"→"Lyrics translate mode" + clearer copy. **P4 (corrects f4y):** Bio info is a real picker again — new `bioLang` (`'auto'`\|lang, default Auto=app/device lang) + `names.dnBio` honors it; no "Last.fm" text. 20 i18n keys × 15 locales. Verified live: Title 160%→home label 12→19.2px, reset works, bio default Auto. check 0/0, 414 tests, build OK. | 2026-06-07 | 6c4c4d9 | [260607-fnp-appearance-settings-per-part-font-sizes-](./quick/260607-fnp-appearance-settings-per-part-font-sizes-/) |
| 260607-f4y | Translation policy + Now Playing polish (two-part; decisions via AskUserQuestion: names=flip-defaults-off-keep-pickers, lyrics=untouched, bio=auto-by-app-lang). **Part A:** names no longer auto-translate by default — dropped the legacy `nameLang`→`artistLang`/`titleLang` migration (defaults stay `'off'`, per-part pickers kept for opt-in); new `names.dnBio()` auto-translates the Last.fm bio to the app/device language (wired into the artist-page bio, was untranslated under D-07); renamed "Last.fm info translation" → "Bio info" (manual tag picker removed, now a read-only auto-translate note) with `settings.translateLastfm`/`Note` values updated across all 15 locales (keys unchanged → parity intact). **Part B (NowPlaying):** title+artist now single-line marquee-bounce when overflowing (reuse `use:marquee` + keyframe, `{#key uid}` remount for per-track re-measure, no more 3-line wrap); genre/tag chips hidden (`TagChips` removed; enrichment kept only for hi-res cover, dropped vestigial `enrich` state); inline error banner bound to `player.error`. Verified live (real Last.fm/CN data): settings shows Bio-info note + no Last.fm picker; played track → single-line untranslated title/artist, 0 genre chips, no error banner, lyrics translation preserved. check 0/0, 414 tests, build OK. | 2026-06-07 | 0be3358 | [260607-f4y-default-no-name-title-translation-rename](./quick/260607-f4y-default-no-name-title-translation-rename/) |
| 260611-fr9 | Restyle docked nowbar to blend FLUSH into the bottom nav (YouTube-Music style): full-width (`left/right:0`), `bottom: var(--tabbar-h)` (6px gap removed → gap=0), rounded TOP corners only (`var(--radius-lg) ... 0 0`), top+sides-only border (no double divider), `.nowbar::before` → `border-radius:inherit`; `.nowbar.embed` byte-identical. Filled-vs-outline active-tab nav icons via the existing Lucide `fill` prop idiom (`fill={active?'currentColor':'none'}` + lighter active strokeWidth; NO new deps). NowPlaying `.np` top confirmed flush (paddingTop 0, header at y=0), bottom `env(safe-area-inset-bottom)` preserved. Persisted last-played + up-next were ALREADY wired (`player.restore()` in root layout) → surfacing-confirm only; `player.svelte.ts` untouched. Verified live (mobile viewport, vite dev): nowbar↔tabbar gap=0 + rounded top; Home/Search/Library active-icon fill swap correct; NP full view top flush (npRectTop 0); cold-load surfaces persisted "Offline Test"; Up Next shows "No queue yet" for an empty persisted queue (correct). pnpm check 0/0. App is dark-only (ignores prefers-color-scheme); blend is structural/theme-independent. | 2026-06-11 | e4a6ae7 / 1003463 | [260611-fr9-restyle-nowbar-to-blend-with-bottom-nav-](./quick/260611-fr9-restyle-nowbar-to-blend-with-bottom-nav-/) |
| 260611-gln | Sharing/metadata/persistence bundle (6 atomic commits). (1) Shared-link continuity+prefetch: `?play` handler decodes via `decodeShare` → `setQueue` → `play(current,{fresh:true})` so a shared song auto-advances/regenerates+prefetches instead of stopping. (2) Share the up-next queue: new v2 token `{v,c,q}` carries a capped (30) queue; legacy v1 single-track token still decodes. (3) Human-readable slug: `shareUrl` emits `?t=<slug>&play=<payload>` (CJK-safe pure `slugify`). (4) Crawler-correct OG: three universal `+page.ts` loads (home `?play`, artist, album) render per-page `og:`/`twitter:` in SSR HTML via shared `PageOg.svelte`; root-layout OG gated behind `{#if !page.data?.og}`; pure `buildOg` helper+tests. (5) Clear-queue moved into now-playing options (kebab→TrackMenu, gated `queue>1`, `Trash2`) — standalone subnav Clear button removed; `menu.clearQueue` in all 15 dicts. (6) Android persistence: `flushPersist()` on `visibilitychange`/`pagehide`/`freeze` + `pageshow(persisted)` re-sync writes EXACT `currentTime` immediately (was 2s-throttled→stale→restart-at-0); `restore()` pendingSeek seek unchanged. Verified live (vite SSR + mobile preview): artist/album/`?play` OG present in curl'd SSR HTML with single page-specific tags (`og:title "稻香 — Jay Chou"`); shared link restored queue [稻香,七里香] + URL cleaned to `/`; kebab shows "Clear queue", subnav button gone, clears to 1; `visibilitychange`-hidden flushed exact 37.24s to localStorage immediately; regenerated track played a real qq stream (continuity confirmed). pnpm check 0/0, 614 tests (+28). NOTE: real Android background-resume is device-dependent — NOT verifiable in desktop preview. NO new deps. | 2026-06-11 | fd40765/970288f/4c8093d/2ff7397/fb73238/323209c | [260611-gln-sharing-metadata-persistence-bundle-1-sh](./quick/260611-gln-sharing-metadata-persistence-bundle-1-sh/) |
| 260611-ql0 | Search-page autocomplete (typeahead) for song/artist names. Source decision (user-delegated): Deezer via already-shipped `deezerSearchTopN` (keyless `/api/deezer/search` edge proxy, client 6h + edge 24h cache) — Last.fm rejected (key not guaranteed client-side), CN resolvers rejected (slow/flaky, defeats "not heavy to api fetching"). New pure `src/lib/search/autocomplete-logic.ts` (`deriveSuggestions` dedupe song+artist suggestions cap 8 + `debounce`, 14 tests, TDD red→green) mirroring `search-history-logic` split; wired into search `+page.svelte`: 300ms debounce, MIN_QUERY_LEN=2, AbortController cancel-in-flight + stale-guard, tap-to-fill-and-search real `<button>`s, silent graceful degrade, coexists with recent-search block (mutually exclusive gates). `search.suggestions` i18n key ×15 locales (parity gate; `en` landed with Task 2 since `TranslationKey` derives from `en.ts`). No new API route/credential/dep. check 0/0, 672/672 tests (+14) | 2026-06-11 | 97a7af7 | [260611-ql0-add-autocomplete-for-song-artist-names-i](./quick/260611-ql0-add-autocomplete-for-song-artist-names-i/) |
| 260611-rhx | Auto-focus empty search input at mount on search page: `queryInputEl` $state ref + `bind:this` + `if (!q.trim()) queryInputEl?.focus()` at end of existing `onMount`, AFTER the `searchSession.hasPrior` restore block — restored/back-nav query suppresses focus, mount-only placement (not $effect) never re-steals focus on mid-session clear; programmatic `.focus()` (no bare `autofocus` attr → no a11y_autofocus warning). 1 file, no new deps/i18n. check 0/0 | 2026-06-11 | 78b585d | [260611-rhx-focus-the-search-query-input-if-it-is-em](./quick/260611-rhx-focus-the-search-query-input-if-it-is-em/) |
| 260612-gn9 | README "Android APK (build & install)" section: prerequisites (node 22/pnpm, JDK 21 brew + `invalid source release: 21` gotcha, ANDROID_HOME, gitignored local.properties), build chain `pnpm build:native` → `cap sync` → `gradlew assembleDebug`, install via `adb install -r --user 0` (Samsung Secure Folder quirk) + RSA prompt + `am start` launch, and the stale-deploy CORS warning (openmusic.lol must carry current CORS hook; manual wrangler deploy). All facts verified from real phase-999.1 builds. | 2026-06-12 | 0b33339 | [260612-gn9-update-readme-with-apk-build-and-phone-i](./quick/260612-gn9-update-readme-with-apk-build-and-phone-i/) |
| 260613-4v0 | Fix NowPlaying grip ghost-click: tap on grip synchronously moved the sheet, so the browser's trailing compatibility click (fired after pointerup at the tap point) landed on the `.play` toggle (half/closed → unwanted play-pause) or `.track` seek slider (closed → ~15-25s seek-back). Fix = one-shot WINDOW-level capture-phase click suppressor (coverSwipe WR-05 idiom; window+capture is load-bearing because the ghost click targets the element that slid under the finger, never the grip node), armed in the two `gripUp` tap branches that move the sheet (generic toggle + subnav-tab), NOT in the plain-subnav-button branch (WR-02 Clear-queue), disarmed on new `gripDown` + 350ms self-expiry. Removed two dead guards that couldn't work (both reset before the trailing click fires): the grip `onclick` `gripActive` check and the user's parallel `gripHandledPointer` transport-button wrappers (also fixed its `false \| (() => void)` check ERROR). Merged cleanly across concurrent main commit 57c4805. check 0/0. Touch-device tap-test checkpoint pending human verify. | 2026-06-13 | 267728b | [260613-4v0-fix-nowplaying-grip-ghost-click-trailing](./quick/260613-4v0-fix-nowplaying-grip-ghost-click-trailing/) |
| 260613-fhw | Make non-stop playback reliable against transient source failures. (1) Inter-source search stagger: exported `sleep(ms)` in proxy/http.ts (native-Promise, no hand-rolled AbortController; `backoff` delegates to it); `searchAllUncached` waits `SEARCH_STAGGER_MS (200ms) * idx` before launching adapter idx so proxies aren't hit simultaneously — abort mid-stagger skips later launches, interleave/searchAll/return shape unchanged. (2) Hardened `prefetchNext` into a bounded (`PREFETCH_MAX_CANDIDATES = 4`) forward-resolve loop: skips a candidate that rejects or resolves without audioUrl and lands the first playable next track; preserves all guards (end-of-queue no-op, already-complete short-circuit, in-flight dedupe, single `prefetchController` abort, `seedUid` stale-guard, fresh-by-uid write-back), never throws/bumps playGen/calls next()/runFallback — composes with never-stop + CR-03 chain. Full suite 810 passed, svelte-check 0/0. Device verify (Task 3, human-verify checkpoint) PENDING: first-search feel + auto-advance reliability + audio canplay streaming on iOS Safari + Android Chrome (not node-testable). | 2026-06-13 | 642f4f5 | [260613-fhw-gapless-prefetch](./quick/260613-fhw-gapless-prefetch/) |
| 260613-fwq | Related tab in NowPlaying now shows shaped skeleton rows (title + artist shimmer bars matching the real `.row`, reusing the global `.sk` primitive) while fetching, instead of plain "Loading related…" text. Added a race-guarded `relatedLoading` `$state` flag (set before `searchAll`, cleared in `.then`/`.catch` with a `relatedFor === t.uid` guard) and split the markup three ways: list / skeleton / distinct empty state (`nowplaying.noRelated`, added to all 15 locales) — which also fixes a latent bug where the loading text never cleared on a zero-result fetch. svelte-check 0/0. Live-preview verified the skeleton→loaded lifecycle (8 skel rows → 33 real rows). | 2026-06-13 | 8d8297d | [260613-fwq-related-list-in-now-playing-should-be-sk](./quick/260613-fwq-related-list-in-now-playing-should-be-sk/) |
| 260614-1rt | Relax prefetch to a slow single-song lookahead so endless playback stops tripping audio-source bot-blocking (>5 songs failing in a short window). Was: `prefetchNext()` fired the INSTANT `play()` set a track and walked up to `PREFETCH_MAX_CANDIDATES=4` queue entries resolving each until one landed → multiple proxy resolves at t=0 = burst. Now: (1) single-song lookahead — resolves ONLY the immediate-next track (`queue[i+1]`) via idempotent `ensureTrackDetails`; a reject / no-`audioUrl` is abandoned this invocation, never advances to a later candidate (`PREFETCH_MAX_CANDIDATES` removed); (2) `primeNext()` no longer prefetches on `play()` entry (keeps `ensureAhead` only); (3) prefetch is now armed ONCE per src by a `timeupdate` gate after `PREFETCH_PLAYBACK_DELAY_MS=5000` of REAL playback (`prefetchArmedForSrc` reset at both initial-load arming points) — so a skip within the first few seconds fires NO prefetch (next song resolves on-demand at skip time, no error spam); (4) relaxed failure handling preserved (commented loop-guard + 15s stall watchdog untouched). All write-back-into-queue + asset-prewarm caching contracts kept for the single immediate-next track. Tests updated to the single-song delayed-prefetch contract (removed 3 multi-candidate cases + unused mirror; added single-song-abandon, single-candidate stale-guard, real-`timeupdate` delayed-trigger). Full suite 835 passed, `pnpm check` 0/0. | 2026-06-14 | 76b3e6f | [260614-1rt-relax-prefetch-single-song-slow-lookahea](./quick/260614-1rt-relax-prefetch-single-song-slow-lookahea/) |
| 260614-1w3 | Refine Phase-24 song sharing (supersedes D-04/D-06 for the song surface only; album/artist unchanged). New `songShareUrl(track)` emits a short readable `/song/{slug}?n={title}&a={artist}` — drops the long base64 `?play=` queue token and the `{source}{id}` suffix; `slug = slugify(title,artist) \|\| 's'` placeholder for all-CJK titles. Song `+page.ts` reads `n`/`a` from the query and ALWAYS builds the OG card (title=name, description includes artist, image=`/og.svg` fallback, no server-side cover fetch → no SSRF); dropped `decodeShare`/`?play=`. Song `+page.svelte` stays SSR-safe (lazy player/i18n imports in `onMount`) and resolves a playable track by name+artist via existing `player.playStub`→`resolveStub`/`searchAll`, with a `notfound` retry branch (no stuck loader). `TrackMenu.doShare` rewired to `songShareUrl`. `entityShareUrl`/`parseEntityParam`/`encodeShare`/`decodeShare` kept for album/artist + home queue-restore. Root `+layout` stays `ssr=false`; no `+page.server.ts`. 840 tests pass, `pnpm check` 0/0, `pnpm build` + `pnpm build:native` both exit 0. | 2026-06-14 | 681d996 | [260614-1w3-refine-phase-24-song-sharing-short-reada](./quick/260614-1w3-refine-phase-24-song-sharing-short-reada/) |
| 260614-23h | TrackMenu: (1) hide Play next / Add to queue when the menu's track IS the currently-playing one — both buttons wrapped in `{#if track && track.uid !== player.current?.uid}` (uid-gate, works everywhere the shared menu mounts; queuing the already-playing song was redundant). (2) Download save-path picker — verified `doDownload` already resolves at `settings.downloadQuality` and uses a sanitized `artist - title.ext` name (no change needed). Added a desktop branch: feature-detect `'showSaveFilePicker' in window` → `createWritable`/`write(blob)`/`close` so Chrome/Edge users pick the save location; `AbortError` (user cancel) returns silently (no toast, no anchor fallback); other picker errors and all mobile/iOS Safari/Android Chrome fall through to the existing anchor-element download to the default Downloads folder. ext→mime lookup added. Offline `blobStore.put` cache + `window.open` fetch-failure fallback preserved. `npm run check` 0/0, `npm run build` ok. | 2026-06-14 | d744f63 | [260614-23h-now-playing-menu-play-next-add-to-queue-](./quick/260614-23h-now-playing-menu-play-next-add-to-queue-/) |
| 260614-2m3 | Finish the openmusic.pages.dev → openmusic.lol domain migration. `src/lib/proxy/http.ts` `ALLOWED_ORIGIN_PATTERNS` now allows `https://openmusic.lol` + `https://*.openmusic.lol` (primary custom domain + CF preview), KEEPING the legacy `openmusic.pages.dev` + `*.openmusic.pages.dev` patterns for a safe cutover (no test asserts pages.dev is rejected; the CF Pages URL still resolves). localhost/127.0.0.1/capacitor patterns + the never-`*` open-relay guard (T-01-02) unchanged — CORS stays scoped to the explicit own-origin allowlist. Repointed remaining own-domain doc URLs (README.md ×2, docs/DEPLOY.md ×3) to openmusic.lol; the already-in-tree `.lol` edits (api-base.ts, robots.txt, sitemap.xml, deezer/lastfm/similar/proxy endpoint tests) committed alongside (SITE/canonical in +layout/PageOg/app.html landed earlier with the SEO-title fix). Decision: keep-both, drop pages.dev later once retired. Full suite 840 passed (the 8 red CORS/origin tests now green), `pnpm check` 0/0, `pnpm build` + `pnpm build:native` exit 0. SleepTimerSheet.svelte (unrelated local edit) left uncommitted. | 2026-06-14 | (pending) | [260614-2m3-finish-openmusic-pages-dev-to-openmusic-](./quick/260614-2m3-finish-openmusic-pages-dev-to-openmusic-/) |
| 260615-fva | Polish cover-swipe track-change transition. (1) Carousel gutter: `--cover-gap` var on `.cover-strip`, neighbor cells offset to `calc(±100% ± gap)` so covers don't touch during the 1:1 finger-drag; `coverSwipe.ts` unchanged (its raw `translateX(dx)` reveals the gutter; commit-settle 0.32s lands the swapped neighbor centered at `left:0`). (2) NowPlaying meta: `{#key uid}` block now `transition:fade` so title+artist crossfade on track change (marquee re-measure preserved). (3) Nowbar: keyed inner `.np-art`+`.np-meta` on an `npKey` (current uid ∥ artist\|title — `PendingTrack` has no uid, so the planned `np?.uid` was corrected) → cover+title+artist crossfade as one unit; `.np-open` gesture host left un-keyed so the drag surface never remounts. Reduced-motion: Svelte JS fades gated via `settings.reduceMotion`→duration 0 (the global CSS `transition:none` rule doesn't stop JS transitions); positional gap needs no guard. `npm run check` 0/0; coverSwipe action test 23/23 green; full suite 845. Human visual check pending. | 2026-06-15 | 28fc1d6 | [260615-fva-polish-cover-swipe-track-change-transiti](./quick/260615-fva-polish-cover-swipe-track-change-transiti/) |
| 260615-gcy | Now-playing cover full 4-direction gestures (--discuss). Wired the cover's vertical drag into the EXISTING grip snap machine (no parallel impl): flipped the `.np-top` slop check from downward-only (`dy > SLOP`) to axis-locked (`abs(dy) > SLOP && abs(dy) > abs(dx)`) and removed the `dragY = Math.max(0, dy)` clamp. UP = expand mirroring grip 1:1 (live follow + flick + nearest-snap via `startGripFromCover` reusing `gripActive/sheetDragging/sheetDragY/gripMoved/gripVel/offsetFor/measureOffsets/gripUp`). DOWN = LAYERED (`npTopDeleg` owner: open sheet steps full→half→closed first; only a closed sheet collapses the page past ~120px). LEFT/RIGHT unchanged (coverSwipe prev/next, untouched + 23/23 green). Bonus `tapCoverCollapse` (half→closed). Pitfall-7 invariants preserved (no capture/preventDefault pre-slop, sub-slop tap reaches onclick, horizontal-dominant falls through to coverSwipe, no mid-drag axis flip); reduced-motion via existing CSS gating. Gesture-coverage AUDIT in SUMMARY across 8 surfaces → **no gesture left undone**; hardware/edge-back intentionally not intercepted. `npm run check` 0/0; full suite 845. Human device-verify pending. | 2026-06-15 | b13dda2 | [260615-gcy-now-playing-cover-full-4-direction-gestu](./quick/260615-gcy-now-playing-cover-full-4-direction-gestu/) |
| 260615-gww | Now-playing cover tweaks (follow-on to fva/gcy). (1) Removed the inter-cover gap from 260615-fva: dropped `--cover-gap` on `.cover-strip`, reverted neighbor cells to flush `-100%`/`+100%` (no calc/var) — covers touch again during swipe; the fva crossfade kept. (2) Rewrote `tapCoverCollapse` to branch on `sheetState`: closed → `player.toggle()` (play/pause), half → no-op (removed the gcy half→closed collapse), full → no-op. `tapCoverKey` still delegates (keyboard parity). Sub-slop-tap-reaches-onclick invariant + swipe/drag/grip machine + coverSwipe.ts untouched. `npm run check` 0/0; coverSwipe 23/23; full suite 845. Human device-verify pending. | 2026-06-15 | 92de46b | [260615-gww-now-playing-cover-tweaks-remove-inter-co](./quick/260615-gww-now-playing-cover-tweaks-remove-inter-co/) |
| 260615-hb6 | Reverse the gww half-open no-op: `tapCoverCollapse` now restores the gcy half→closed collapse-on-tap (`else if (sheetState === 'half') sheetState = 'closed'`), kept atop gww's closed→`player.toggle()`. Net: closed → play/pause; half → collapse to closed; full → no-op. `tapCoverKey` inherits; invariants + swipe/grip machine + coverSwipe.ts untouched. One-branch change, done inline (no subagent). `npm run check` 0/0; coverSwipe 23/23. | 2026-06-15 | 2ea0597 | [260615-hb6-restore-tapcovercollapse-half-closed-at-](./quick/260615-hb6-restore-tapcovercollapse-half-closed-at-/) |
| 260615-hep | Global resolved-cover reuse + resolve-on-view (--discuss). Root cause: now-playing displayed a cover (track.cover path) without writing it to the shared cache, and homepage tiles read name-key only at render with no lazyCover and no reactive dependency → same song empty on homepage. Fix (3 commits): (1) new reactive wrapper `src/lib/stores/cover-version.svelte.ts` — `writeCoverBoth` (writes BOTH uid + name layers, single canonical writer), `readCoverByUidOrName` (uid-first→name), `$state` cover-version bumped on every write; pure `cover-cache.ts` untouched (tests green). (2) now-playing writes every displayed https cover into both layers + bumps the signal (3 sites in player.svelte.ts). (3) homepage uid-first reactive reads via the global signal + `use:lazyCover` on real-Track song tiles (discovery tiles keep existing capped backfillCovers — no uid, no parallel resolver). Decisions: both keys w/ name as cross-source bridge; global cache-version signal for live cross-render repaint; resolve-on-view added to coverless song tiles. `npm run check` 0/0; 4 protected suites 155 green; full suite 845. Human visual verify pending. | 2026-06-15 | 435dc9f | [260615-hep-global-resolved-cover-reuse-resolve-on-v](./quick/260615-hep-global-resolved-cover-reuse-resolve-on-v/) |
| 260615-mnr | Up-Next list auto-scrolls current song into view constantly. Diagnosis: CSS scroll-anchoring (`.panel` scroller defaulted to `overflow-anchor: auto`); the keyed queue `{#each (track.uid)}` re-anchors to the tinted `.row.playing` on every queue mutation (advance/remove/retry/reorder), yanking it back into view. Fix (1 commit): `overflow-anchor: none` on `.panel` + defensive `blur()` on queue-row play/retry tap (mirrors longpress idiom). Lyrics auto-scroll/swipeRemove/longpress/grip/skipped-retry untouched. `npm run check` 0/0. Human device-verify pending. | 2026-06-15 | ff2f351 | [260615-mnr-up-next-list-auto-scrolls-current-song-i](./quick/260615-mnr-up-next-list-auto-scrolls-current-song-i/) |
| 260615-i9u | Up-Next: ✗ skipped-track markers + history-preserving click-enqueue (--discuss). Two deferred features on the existing queue engine (3 commits). FEATURE A: `unplayableUids` converted to a reactive `SvelteSet`; public `isUnplayable()`/`retryUnplayable()`; NowPlaying renders a dimmed leading-✗ "skipped" Up-Next row that taps to retry (non-fresh `play`), without disturbing gripDrag/swipeRemove/longpress; `nowplaying.skippedRetry` i18n key across all 15 dicts. FEATURE B: fresh user play no longer wipes the queue — captures prior state via a one-shot `pendingHistory` seam set inside setQueue/setListQueue (zero edits to the ~10 call sites that do setQueue→play{fresh}) and consumed by play()'s fresh branch (`weaveFreshHistory`). New shape `[...history(capped 50), priorCurrent, clicked(new current), ...tail]` with tail per `settings.effectiveUpnextMode` (generated→buildSimilarQueue; same-list→list remainder); `prev()` revisits prior tracks; regenerate/setListQueue history-aware; shuffle pins history+current; `queueGen` bumped on the explicit re-install (WR-06). FEATURE C: replaced the old wipe-on-fresh-play test contract + added coverage. `npm run check` 0/0; full suite 854 (player 98→107, +9; i18n parity green). Human device-verify pending. | 2026-06-15 | 192218c | [260615-i9u-up-next-skipped-track-markers-history-pr](./quick/260615-i9u-up-next-skipped-track-markers-history-pr/) |
| 260616-0n6 | Default `album` queue context to `'same-list'` up-next: `UPNEXT_DEFAULTS.perContext = { album: 'same-list' }` in defaults.ts. `effectiveUpnextMode('album')` now resolves to continue-the-album; other contexts still fall back to global `'generated'`. Updated the `resetPlayback` test expectation (now `{ album: 'same-list' }`). `npm run check` 0/0; settings suite 20/20. | 2026-06-15 | 81835fc | [260616-0n6-set-album-same-list-for-upnext-defaults](./quick/260616-0n6-set-album-same-list-for-upnext-defaults/) |
| 260618-fiz | Four queue/playback fixes (4 commits). **Fix 2** — swipe right-to-left now enqueues play-next (`player.playNext`) instead of like, across all 6 `onSwipeLeft` bindings (search + 4 library rows + artist); Heart reveal → `ListStart` icon (`.reveal-like`→`.reveal-next`), `toast.playingNext`. **Fix 4** — explicit queue entries survive a fresh play: new one-shot `pendingManual` carrier (mirrors `pendingHistory`) captured before queue wipe; `weaveManualAfterSeed()` re-inserts `manualUids` entries after the seed in BOTH up-next modes, dropping stale auto/context tail; `manualUids` stays the single provenance Set (no Track-level field). **Fix 3** — `ensureAhead()` queue-dry continuation now seeds from the last-played `current` via `buildSimilarQueue(current, have)`; `buildDiversePicks` demoted to empty-result fallback; supersedence + re-entrancy guards preserved. **Fix 1** — lyrics translation re-renders immediately on whitelist/lang toggle: `$effect` clears `translated`/`trKey` on no-translate early return and resolves the all-whitelisted case synchronously via `stitch([])`. `svelte-check` 0/0; player 121 (+5 new) + library 3 = 124 green. (6 pre-existing failures in `services/catalog.test.ts` + `home-layout.test.ts` confirmed unrelated/untouched.) Human device-verify pending. | 2026-06-18 | 063b5ba | [260618-fiz-four-queue-playback-fixes-immediate-tran](./quick/260618-fiz-four-queue-playback-fixes-immediate-tran/) |
| 260618-lsw | Auto-advance keeps the played song in the Up-Next list (3 commits). Follow-up to 260618-ink: ink derived the Up-Next slice start (`upNextStart`) from the LIVE current index, so on track-end auto-advance the just-played song fell before the slice and vanished from the list. **Fix** — new session-scoped `player.upNextAnchorUid = $state<string\|null>` set ONLY on a fresh play (`play()` fresh branch, after `weaveFreshHistory`) + new-list install (`setQueue`/`setListQueue`/`clearQueue`); deliberately NOT touched by `next()`/`prev()`/failover/manual inserts. `NowPlaying.svelte` derives `upNextStart` from the anchor uid's LIVE queue index (`findIndex` each render, clamped to `ci` then 0) instead of the live current index — so auto-advance just moves the `.playing` highlight down while played songs stay listed, while an explicit click still resets the anchor → new song is first row (ink behavior preserved). Anchor-by-uid keeps drag-reorder offset, prev()/history, cover carousel, and the 260618-ink one-shot scroll-to-`.q-row.playing` all correct; not persisted. 4 new anchor tests (auto-advance/prev keep anchor, fresh resets, survives reorder/removal). Full suite 904 / 65 files green; `svelte-check` 0/0. Human device-verify pending. | 2026-06-18 | fdd5efe | [260618-lsw-auto-advance-keeps-played-song-in-up-nex](./quick/260618-lsw-auto-advance-keeps-played-song-in-up-nex/) |
| 260618-ink | Up-Next list starts at the new song on click-to-play + scroll-to-current on open (3 commits). **Tweak 1 (view-level slice)** — clicking a song now shows it as the FIRST Up-Next row: `NowPlaying.svelte` derives `upNextStart`/`upNextList = player.queue.slice(ci)` and the `{#each}` iterates the slice, so woven history no longer precedes current in the LIST. Store queue shape (history-before-current) is UNCHANGED — `prev()`/history (260615-i9u) and the cover carousel still read the unsliced `player.queue`. Manual (`manualUids`) entries still sit right after current; prior auto/context tail already dropped by 260618-fiz Fix 4. Drag-reorder commits offset list-relative indices by `upNextStart` to stay queue-absolute. **Tweak 2 (one-shot scroll)** — a `$effect` keyed only on `tab`/`sheetState` scrolls the current `.q-row.playing` to the `.panel` top once on Up-Next open (rect-delta `scrollTo` in one rAF), latched by `upNextScrollDone`, re-armed on close; no queue-mutation dependency, so the 260615-mnr `overflow-anchor:none` no-continuous-scroll behavior is preserved. 2 regression tests lock the store-shape-unchanged + view-slice contract. Full suite 900 tests / 65 files green; `svelte-check` 0/0. Human device-verify pending (rAF/layout scroll timing). | 2026-06-18 | 25173a5 | [260618-ink-click-to-play-puts-new-song-at-top-of-up](./quick/260618-ink-click-to-play-puts-new-song-at-top-of-up/) |
| 260618-t7p | ⚠ **Task 1 REVERTED (206e405) — broke mobile playback.** The defer committed `displayed = current` only on the audio `playing` event; on mobile (iOS Safari autoplay/background quirks) `playing` fires unreliably and `loading` clears before real output, so `displayed` never advanced → now-bar froze on the previous song, np-top-loader stuck running, click-to-play dead, now-bar wouldn't expand. Desktop unaffected. Tasks 2 & 3 (lyric fixes) kept. Original scope below ⬇. Defer now-playing/nowbar UI swap until the new song actually plays + two lyrics fixes (3 commits). **Task 1 (TDD)** — new session-scoped `Player.displayed = $state<Track\|null>` that advances to `current` ONLY in the `playing` listener (single swap-commit point), cold-start-seeded via `displayed ??= track` in `play()` (null-only, never overwrites), and set directly by `restore()`; `Nowbar.svelte` + `NowPlaying.svelte` now read the displayed track for title/artist/cover/lyrics (transport/like/related/enrich stay on `current`); the now-bar running-line loader animates while `loading \|\| current.uid !== displayed.uid` so it spans the whole click→`playing` window; a failed load keeps the old song info (invariants D-06/CR-01/D-13/D-14, stall-watchdog, loop-guard, prefetch, MediaSession, auto-advance all untouched; only new store field is `displayed`). **Task 2** — lyric over-scroll with "Hide parenthesised lines" ON fixed: each rendered `<p>` carries `data-i={i}` and the scroll-anchor `$effect` selects by `data-i` (full-array index, with nearest-visible-at-or-before fallback for hidden-paren active lines) instead of positional `querySelectorAll('p')[idx]`; visible-band centring math byte-unchanged. **Task 3** — `below`-mode `.tr` translation span gets `class:active` (same `activeTime` condition as parent `<p>`) + a `.lyrics .tr.active` rule using existing tokens; `replace` mode already inherits `p.active`. player suite 132/132 (5 new t7p tests); full suite 909/909 / 65 files; `svelte-check` 0 errors. Human device-verify pending. | 2026-06-18 | f956653 | [260618-t7p-defer-now-playing-nowbar-ui-update-until](./quick/260618-t7p-defer-now-playing-nowbar-ui-update-until/) |
| 260618-goe | Home 3×3 grid layout mode + configurable crow art size (4 commits). **Layout rename** — `HomeDensity` `comfortable`/`compact` renamed to coder-clear `pile`/`list`; pure `migrateDensity` helper maps legacy values on load (non-destructive for returning users) across `homeDensity` + every `homeSectionDensity` entry. **New `grid` mode** — `HomeGridPager.svelte` renders a YT-Music speed-dial 3×3 cover grid (9/page, max 3 pages = 27 tiles, dot indicator, scroll-snap), device-width-sized (no slider), title+artist overlaid on cover bottom reusing the `.tile/.scrim/.label` look; selectable per-section AND global via the Settings → Home toggle with the `Grid3x3` lucide icon. **Crow size fix** — root cause: `.crow .art` was hardcoded `40px` so `coverScale` never reached the tiles; now `calc(40px * var(--cover-scale))`. **Live demos** — cover-size + grid-columns previews on the Appearance page update live; `densityList/Pile/Grid` + `settings.preview` i18n keys across all 15 locales. Full suite 898 tests / 65 files green; `svelte-check` 0/0 (4289 files). Human device/browser smoke pending. | 2026-06-18 | 9231196 | [260618-goe-add-home-layout-3x3-cover-grid-3-pages-a](./quick/260618-goe-add-home-layout-3x3-cover-grid-3-pages-a/) |
| 260619-47g | **Research-only — DO-NOT-INTEGRATE (no code change).** Evaluated github.com/DarinRowe/googletrans for translate reliability (follow-up to the c34325a soft-fail fix). Rejected on two independent grounds: (1) **hard gate fails** — it depends on `axios`, which cannot run on our Cloudflare Workers fetch-only API proxy (verified from the published tarball); (2) **no improvement** — it wraps the SAME unofficial Google endpoint we already call, lacking our sentinel 1:1 alignment / echo-mode detection / chunking / per-line fallback / `flags[]` poison-resistant cache + the c34325a client retry. Node TS lib (not the Python port), MIT, ~44★. Borrowable-but-deferred idea: its TKK `tk`-token `client=t` endpoint flavor — only worth an A/B port to Workers if telemetry shows our `client=gtx` path degrading. | 2026-06-19 | n/a (research) | [260619-47g-evaluate-whether-github-com-darinrowe-go](./quick/260619-47g-evaluate-whether-github-com-darinrowe-go/) |
| 260625-pzs | Batch of 5 player improvements (5 atomic commits, SvelteKit `src/`). **(1) Per-artist links** — new pure `splitArtists()` util (`src/lib/util/artist-split.ts`) splitting on `, & 、 / feat. ft. x ×  &amp;`, trims/dedupes, keeps embedded-x names (Maxwell) intact + 13 tests; NowPlaying artist row now renders one tappable link per artist → `/artist/<name>` (single-artist case unchanged). **(2) Swipe-to-queue on Related** — reuses shared `swipeAction` + reveal layers (swipe-right=add-to-queue, swipe-left=play-next) like search/library; tap-to-play + long-press menu preserved. **(3) Cache complete lyric translations** — module-scoped `trCache` Map keyed by `trKey`, hydrated synchronously before fetch so tab blur/refocus or component remount no longer re-POSTs `/api/translate`; caches ONLY `translateLinesEx` output with `complete===true` && length-match (soft-fail echoes never frozen — respects c34325a learning). **(4) Download isolation** — `doDownload` reuses the already-resolved currently-playing URL instead of force-re-resolving (no duplicate CDN pull saturating the active stream / wiping lyrics); never mutates `player.current`/`lrc`/`playGen`; isolation contract documented in-code. **(5) No premature skip** — audio `error` listener now gates on the existing internal `hasPlayedSinceSrc` flag (mirrors the stall watchdog): a track that has produced audio re-resolves in place via `reresolveCurrent()` instead of cross-source failover/restart (fixes "plays ~3s then skips"); never-started tracks still fail over (never-stop preserved); no `playing`-event UI dependency (respects reverted player-displayed-defer regression). Full suite 921 tests / 66 files green; `svelte-check` 0/0. Human device/browser verify pending. | 2026-06-25 | 07bee6b | [260625-pzs-batch-of-5-player-improvements-sveltekit](./quick/260625-pzs-batch-of-5-player-improvements-sveltekit/) |
| 260627-huo | Fix: a genuinely-playable Next-up song was being marked non-playable (dimmed ✗ row) after 2 quick transient resolve/probe blips hit `STRIKE_CAP`. **(1) Delayed bounded re-resolve** — replaced "strike → promote to dead" with "strike → schedule a backed-off delayed fresh re-resolve → only mark dead after `RETRY_RESOLVE_MAX`(2) attempts exhausted"; new `scheduleRetryResolve`/`cancelRetryResolve`/`cancelAllRetryResolves` + a shared `handleDefinitiveFailure` decision called from both `prefetchNext` definitive-failure branches (no-url + hard probe error); `STRIKE_CAP` UNCHANGED; every timer cancelled on `clearQueue`/`recoverFromStop`/`retryUnplayable`/real-`playing`; convergence driven by per-uid `retryResolveAttempts` budget (callback does NOT `clearStrike`, by design). **(2) Eager immediate-next prefetch** — `prefetchNext()` now fires at both `play()` src-set points (network + offline-blob), independent of the ~5s `timeupdate` gate AND not gated on the `playing` event, so short tracks / fast skips have the next song pre-resolved + probe-verified for gapless non-stop advance; the timeupdate gate becomes a no-op-when-already-armed backstop (single walk per src). Pure logic in the existing `Player` class, no new deps. Full suite 928 tests / 66 files green (+7 new); `svelte-check` 0/0. Human device verify pending. | 2026-06-27 | df9f55a | [260627-huo-playable-song-in-the-next-up-list-got-ma](./quick/260627-huo-playable-song-in-the-next-up-list-got-ma/) |
| 260630-e6e | Fix: JOOX identity guard wrongly REFUSED a correct song (有人). Root cause: guard compared songmid↔songmid and 歌曲ID↔歌曲ID only, but upstream swaps which value lands in `songmid` vs `歌曲ID` between the search and detail endpoints (expected 歌曲ID `Z25681844789DA` equalled the returned songmid) → same-field compare missed it and threw. **New guard** pools an EXPECTED token set `[songMid, jooxSongMid, jooxSongId, songid]` against a RETURNED set `[songmid, 歌曲ID]`; ANY cross-field token equality → CONFIRMED → enrich + play (fixes 有人 + every field-swap, since a swap always overlaps). Unconfirmed → THROW only in the narrow STRONG-DISJOINT case (both sides fully populated mid+歌曲ID AND zero cross-field overlap = a genuinely different song, Pitfall-4 wrong-song protection intact); otherwise SOFT-ALLOW via `console.warn` + play through to keep music flowing. Enrichment/`detailsLoaded=true` path unchanged. joox suite 11 passed (+2 new: cross-field/有人 resolves, soft-allow warns-and-plays; old "mismatch THROWS" test → strong-disjoint, still throws; happy-path/`n=`/quality-order unchanged); full suite 949 tests / 66 files green, no regressions; `pnpm check` 0/0. Human device verify pending. | 2026-06-30 | a959bb1 | [260630-e6e-relax-joox-identity-match-guard-to-keep-](./quick/260630-e6e-relax-joox-identity-match-guard-to-keep-/) |
| 260630-efr | Fix unresolved song covers — discoverability fix (`--discuss`). **Premise corrected during discussion**: the "Clear cover cache" button ALREADY existed and was wired (`settings/data/+page.svelte` → `clearCoverCache()`), and the cover-cache NEVER caches failures (`writeKey` no-ops on empty URL) — so no negative-cache bug. True root cause of "still not resolved": `lazyCover` serves a cached URL WITHOUT probing it (`lazyCover.ts:69-74`), so a cover whose CDN URL later dies is painted from localStorage forever; only the nuke-all button recovers it. User chose **manual-clear-only** (auto-repair/probe/onerror-eviction/TTL all OFFERED + DECLINED — noted as future enhancement) + **keep button + add helper text**. Shipped: new i18n key `settings.clearCoverCacheHint` across all 15 locales (EN: "Removes saved cover art so missing or outdated covers are fetched again next time." — accurate to manual behavior, no false "automatic" claim) + a muted `.hint` caption rendered under the existing button. No behavior change to cover-cache.ts/lazyCover.ts/clearCovers(). i18n key-parity test 12 passed; `pnpm check` 0/0 (4291 files). Human visual check pending. | 2026-06-30 | 2036d9b | [260630-efr-fix-unresolved-song-covers-replace-perma](./quick/260630-efr-fix-unresolved-song-covers-replace-perma/) |
| 260630-ey2 | Stale song covers now SELF-HEAL (root-cause follow-up to 260630-efr; the auto-repair the user deferred that pass, then requested). Root cause: `lazyCover.resolveCoverForRow` served a CACHE-HIT cover URL WITHOUT probing it (the `probeImage()` check only ran for `track.cover`), so a cover whose CDN URL later died was painted from localStorage forever — only the nuke-all button recovered it. **Shipped:** (1) pure per-entry removers `removeCachedCoverByUid`/`removeCachedCover`/`removeCachedArtistCover` in `cover-cache.ts` (shared `removeKey()`, deletes exactly one entry, no-op on miss, never throws — file stays PURE, no rune/bump import so its node test runs); (2) `removeCoverBoth(uid,artist,title)` in `cover-version.svelte.ts` mirroring `writeCoverBoth` (evict both layers + `bumpCoverVersion`; uid layer guarded by truthy uid); (3) `lazyCover` now probes the cache-HIT url via the existing `probeImage()` — loads OK → fast path unchanged (zero network on healthy caches), dead → `removeCoverBoth` + fall through to the `resolveCoverForTrack` chain to re-resolve + re-cache. Steps 2/3, the in-flight Set, and empty-uid read guards unchanged; `clearCoverCache`/Settings button/`clearCoverCacheHint` left as-is. Targeted 46 tests pass (cover-cache + lazyCover, +new self-heal cases), full suite 960 / 66 files green (+11), `pnpm check` 0/0. Human visual check pending. | 2026-06-30 | 0f62ade | [260630-ey2-self-heal-stale-song-covers-per-entry-co](./quick/260630-ey2-self-heal-stale-song-covers-per-entry-co/) |
| 260630-fe1 | Album page no longer resolves each track's cover one-by-one — all rows reuse the single already-resolved album hero cover. Root: every track row bound `use:lazyCover` → a Deezer→iTunes→CN resolve PER track on scroll-into-view, despite the page already resolving the album art once (`heroImg = merged.cover`, Deezer+Last.fm). Fix (presentation-only, 1 file `album/[name]/+page.svelte`, +3/-33): row `.art` background-image now `heroImg ? url(heroImg) : fallbackCover(...)`, dropped `use:lazyCover`; removed unused `lazyCover` import + `resolvedCovers`/`onCoverResolved`/`stubAsTrack` + stale COVER-02 comment; kept `stubUid` (swipe keys) + `fallbackCover`. No change to tap/swipe/long-press/album-actions/enrichment/cover-cache. Zero per-track network on the album page now. `pnpm check` 0/0, full suite 960/66 green. Human visual check pending. | 2026-06-30 | dd2e29e | [260630-fe1-album-page-show-shared-album-cover-on-ev](./quick/260630-fe1-album-page-show-shared-album-cover-on-ev/) |
| 260630-fce | Added DETAILED helper text under 22 previously-unexplained settings controls across 5 pages (one atomic commit per page, parity green at each step). New `settings.*Desc` keys × 15 locales: General 4 (appLanguage/theme/accentColor/reduceMotion, `.muted`), Appearance 7 (5 fontSize + coverScale + gridColumns, `.note`), Home 5 (itemsPerShelf/defaultLandingTab/tileDensity[layout≠count]/showSearchPill/showRandomize, `.muted`), Playback 1 (autoExpand, `.muted`), Data 5 (clearPicks/clearNameCache/clearSearchHistory/resetAppearance/clearLibrary[destructive·cannot-be-undone], `.hint`). Each line a full sentence (what + when/why); reused each page's existing helper class, no restyle; Translation/Last.fm/About/index untouched (already explained). i18n key-parity test 12 passed after every commit; final `pnpm check` 0/0 (4291 files). Human visual check pending. | 2026-06-30 | fa92115 | [260630-fce-add-detailed-helper-text-under-unexplain](./quick/260630-fce-add-detailed-helper-text-under-unexplain/) |
| 260630-fu2 | Normalized the 3 single-quoted i18n locale dicts (en.ts/zh-Hans.ts/zh-Hant.ts) to DOUBLE quotes so all 15 locales are uniform (user convention — see memory). Keys, string values, AND the `from './index'` import specifier flipped; 2 `\'`→`'` unescapes; the 14+6 already-double apostrophe-bearing values (e.g. "You're offline") left byte-identical; `//` comments + the 1 in-comment backtick untouched. Method proven SAFE by a value-preservation gate: a throwaway test deep-equal'd each new dict against its `git show HEAD:` snapshot (3/3 green) → only delimiters changed, then temp artifacts deleted (not committed). Commit is symmetric 995+/995−. No prettier/eslint config exists so double quotes stick; `pnpm check` is svelte-check/types only. i18n parity 12 passed, full suite 960/66 green, pnpm check 0/0. | 2026-06-30 | fa233ac | [260630-fu2-normalize-i18n-locale-files-to-double-qu](./quick/260630-fu2-normalize-i18n-locale-files-to-double-qu/) |
| 260630-q03 | Never-stop on a dead up-next tail. When the next up-next songs were all probe-confirmed dead (`unplayableUids`), advancing on current-song-end stalled: `next()`→`nextPlayableIndex()` skipped every dead uid and an all-dead tail with a dry `ensureAhead()` left `next()` a silent no-op = playback STOPPED; dead tracks were also never re-attempted (a transient probe failure sidelined a playable song all session). **Fix** (`player.svelte.ts`): new session-scoped `retriedDeadUids` Set + `nextAdvanceIndex` (first entry after current that is playable OR dead-but-not-yet-retried) + `advanceTo` (a still-dead target gets its ONE retry: record in `retriedDeadUids`→never twice, drop from `unplayableUids`, reset strike+delayed-retry budget, replay non-fresh; else play). `next()` rewritten over these for both in-queue and post-`ensureAhead` branches → dead next retried once IN ORDER; retried-still-dead skipped; whole tail exhausted → GROW more up-next and advance into it; only a truly dry source lets the reactive never-stop chain stop. `retriedDeadUids` cleared in lockstep with `unplayableUids` (clearQueue/recoverFromStop/per-uid on real `playing`/manual retryUnplayable). Contracts preserved: stall watchdog, runFallback, external-pause self-heal, loop-guard/FAILURE_CAP; a genuinely-dead track still skipped after its one retry. Player 154/154, full suite 978/978 (66 files), svelte-check 0/0. Human device verify pending. | 2026-06-30 | fed628f | [260630-q03-never-stop-on-dead-up-next-retry-dead-tr](./quick/260630-q03-never-stop-on-dead-up-next-retry-dead-tr/) |
| 260630-sgw | Action log (localStorage ring buffer) + verbose player instrumentation + Settings viewer — diagnostics foundation so player behavior is observable instead of guessed. **Pure logic** `src/lib/diagnostics/action-log-logic.ts` (`openmusic:action-log:v1`, cap 2000, `parseActionLog`/`appendEntry`/`serializeActionLog`, 9 tests). **Runes store** `src/lib/stores/actionLog.svelte.ts` — SSR-guarded singleton, reactive `entries`, throttled ~1s persist with quota-halve-retry, immediate flush on `clear()`; exports never-throw bare `logAction(ev,d?)`; no player import (no cycle). **Instrumentation** in `player.svelte.ts` — 16 additive one-line log calls (play, resolve.ok/fail, playing, pause[deliberate], ended, audio.error, ext-resume.schedule/play, advance, retry-dead, grow.request/added, mark-dead, fallback, visibility, bfcache.restore); none on the `timeupdate` firehose; zero logic change. **Viewer** `settings/activity/+page.svelte` — newest-first reactive monospaced log (HH:MM:SS.mmm + ev + compact-JSON), Copy + Clear, empty state. **Settings row** (ScrollText, before About) + 7 i18n keys × 15 locales (double quotes; en authoritative, zh-Hans/zh-Hant translated). Commits fd5379c/b0652ae/0302d3b/c1902bf/4ec4dd2. action-log 9, i18n parity 12, player 154, full suite 987/67 green, svelte-check 0/0. | 2026-06-30 | 4ec4dd2 | [260630-sgw-action-log-in-localstorage-with-viewer-i](./quick/260630-sgw-action-log-in-localstorage-with-viewer-i/) |
| 260701-0aa | On-click/press feedback for songs. New reusable `src/lib/actions/tapBounce.ts` — one-shot shrink-then-spring on `pointerdown`, cleared on `animationend` (guarded on `animationName==='tap-bounce'`), reduced-motion short-circuit, composes with existing `longpress`/`swipeAction`/`onclick` (no preventDefault). Global `@keyframes tap-bounce` (scale 1→0.94→1) + `.tap-bouncing` + reduced-motion override in `src/app.css`. One-shot keyframe (not `:active`) so it never latches under a held finger — preserves MENU-03/D-12 long-press-latch avoidance. Wired `use:tapBounce` into search row, all 4 library track rows, CompactRow track variant, and 5 home song `.tile`/`.album` tiles (artist tiles + nav excluded). Persistent active-row highlight via `class:is-active={player.current?.uid===…}` → `.is-active{background:var(--color-surface)}` (un-gated so touch shows it) in search/library/CompactRow; library `.edit-row` keeps bg precedence. Commits 8ac417e/135bac0/cde892d. svelte-check 0/0, player 161/161 green. Human-verify checkpoint pending (on-device touch). | 2026-07-01 | cde892d | [260701-0aa-add-on-click-press-feedback-in-list-page](./quick/260701-0aa-add-on-click-press-feedback-in-list-page/) |
| 260701-0j5 | Extend tap/press feedback app-wide (follow-up to 0aa, per user "buttons and many more"). Pure WIRING SWEEP — `use:tapBounce` directive + per-file import added across **21 .svelte files**, reusing the existing action + `@keyframes tap-bounce` (no new action/keyframe/CSS, no `app.css` edit, no logic change). **Included** (tap-to-act only): bottom-nav tabs + notice retry; Nowbar play/pause + sleep badge; NowPlaying header icons + 5 transport + st-readout + 3 subnav tabs; all TrackMenu `.mi`/head/`.x` rows; SleepTimerSheet rows; search submit/clear/suggest/offline-cta; library filter tabs/edit/del/fav-tile; settings index + 8 subpages (back/reset/chip/seg/swatch/item); album & artist back/`.act`/tiles/rows; 3 charts pages. **Scope-guarded OUT**: text inputs, `type=range` sliders (font-size/seek/volume), `.row-toggle` switches, NowPlaying seek `.track`, drag/transform surfaces (`.np-open`/`.np-top`/`.cover`/`.cover-strip`/`.grip`/`.cover-cell`), sheet/modal `.menu`/`.modal` dragClose containers, `.scrim` dismiss layers, and entire `settings/home` (chipReorder drag-transform conflict). Flagged (unchanged): pre-existing non-hover-gated `:active{scale(.92)}` on Nowbar `.np-btn`/`.st-badge` + album `.act` (touch-latch; tapBounce composes; hover-gating them is a possible follow-up). Commits 074d8e6/9e20201/c8ed8dd. svelte-check 0/0. Human-verify checkpoint pending (on-device touch, 11-step checklist in PLAN). | 2026-07-01 | c8ed8dd | [260701-0j5-extend-tap-press-feedback-app-wide-apply](./quick/260701-0j5-extend-tap-press-feedback-app-wide-apply/) |
| 260703-i7e | Remove foreground auto-resume ("on visible" autoplay). Deleted `resumeOnForeground` field + `resumeIfStalled()` method from `src/lib/stores/player.svelte.ts` — sole trigger was visibilitychange visible-branch calling `el.play()`, which caused unintended playback (playback now only starts from explicit user action or normal track-end auto-advance). visibilitychange listener kept: still logs visibility + `flushPersist()` on hide; does nothing on visible. Untouched by design: pageshow/pagehide/freeze bfcache handlers, external-pause RESPECT + deliberatePause, resumeTimer/disarmResume, re-resolve/skip logic. Removed 2 now-dead test describe blocks (12 tests). check 0/0, 987 tests / 67 files green. | 2026-07-03 | 29cc5f1 | [260703-i7e-remove-on-visible-autoplay-song-mechanis](./quick/260703-i7e-remove-on-visible-autoplay-song-mechanis/) |
| 260704-20e | Fix now-playing song cover often stuck black. Root cause: `player.resolvedCover` is seeded first from `track.cover` (an expiring/`http:` source-CDN thumbnail) and `resolveCoverAsync` only re-resolves when `resolvedCover` is **null** — so a non-null-but-DEAD URL was never repaired and painted as a black CSS `background-image` on the carousel CURRENT cell. Fix ports the list-row self-heal (`lazyCover` quick-260630-ey2) to the current cell: new public `player.healCover(uid)` — probe the displayed https cover with `new Image()`; on error `removeCoverBoth` (evict both cache layers) + re-resolve via shared `resolveCoverForTrack` tier chain; all under the `playGen` supersedence guard + one-shot `${uid}|${url}` guard (cleared at `play()` entry); commits only a SOLID https result, a miss keeps the gradient; never throws; NOT gated on the audio `playing` event (iOS-bg-freeze memory). Wired via a `$effect` in NowPlaying keyed on current uid + `effectiveCover`, early-returns when `swappedCover` present (Last.fm hi-res keeps winning) or `effectiveCover` null. Reuses existing helpers — no new dep/route/cache/resolver. Commits 5e289c0/43be983. player 161/161 (+8), svelte-check 0/0, lazyCover 13/13 regression green. | 2026-07-04 | 43be983 | [260704-20e-fix-now-playing-song-cover-often-stuck-b](./quick/260704-20e-fix-now-playing-song-cover-often-stuck-b/) |
| 260704-2os | Edge-cache the core CN metadata SEARCH proxy (optimization backlog #1 from the 2026-07-04 codebase remap). The hot catch-all `src/routes/api/[source]/[...path]/+server.ts` (netease/qq/kuwo/joox) was the only metadata route with NO edge cache while Deezer/lastfm/translate all use `caches.default`. Added `SEARCH_TTL=300`, inlined the Deezer `edgeCache()` helper (with `typeof caches` dev guard), and branched caching off the normalized `[...path]` segment so **only `search` edge-caches** — own-origin cache key (never the JOOX-token-bearing upstream URL), CORS-free stored body with CORS re-applied per hit, cached only on `res.status===200`. CORRECTNESS INVARIANT: `url`/`detail`/`lrc` keep the byte-identical streaming passthrough and are NEVER cached (they carry expiring audio/lyric URLs — caching would freeze dead URLs, the stale-URL bug class). +5 vitest tests in `src/routes/api/proxy.test.ts` (caches-on-200, never-caches-url/detail/lrc, non-200-not-cached, per-origin CORS re-apply, no-Cache-API dev fallback). Commits 1bf4094/4e95c94. proxy 16/16, svelte-check 0/0. | 2026-07-04 | 4e95c94 | [260704-2os-edge-cache-the-core-cn-metadata-search-p](./quick/260704-2os-edge-cache-the-core-cn-metadata-search-p/) |

> Note: off planned phase order (Phase-4-shaped UI pulled forward as a demo). Basic playback only; full audio engine = Phase 6, formal Mobile UI Shell = Phase 4. NOTE (2026-06-10): many of the quick-tasks above already exercise the v1.2 surfaces (failover/prefetch in gte/t5r/hvu, offline blob in kyf, Deezer enrichment plumbing in jau/jip, cover backfill in rvy/0bb/wv8, gesture machines in ggj/h4s/nqf) — v1.2 phases formalize, harden, and complete these rather than build from scratch.

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Resilience | SRC-FB-01 source fallback on play failure (cross-source matching) | Promoted → v1.2 PLAY-07 (Phase 16); base shipped in quick-gte | 2026-06-10 |
| Delight | LYR-01 tap-lyric-to-seek | Promoted → v1.2 (Phase 22) | 2026-06-10 |
| Delight | TIMER-01 sleep timer | Promoted → v1.2 (Phase 18) | 2026-06-10 |
| Delight | HOME-01 recently-played / search history | Deferred to v2 | 2026-06-05 |
| Delight | COACH-01 custom PWA install coachmark | Deferred to v2 | 2026-06-05 |
| Last.fm write-side | LFAUTH/SCROB/LOVE (Phases 11–13) | Re-deferred to v1.3 | 2026-06-10 |

## Session Continuity

Last session: 2026-06-13T03:29:53.532Z
Stopped at: Phase 24 context gathered
Resume: plan Phase 16 (`/gsd:plan-phase 16`). Phase 16 is the resilience-core dependency root; everything else builds on its `queueContext` / 2-state repeat / skip-loop guard.

## Deferred Items

Items acknowledged and deferred at v1.2 milestone close on 2026-06-15 (81 total):

| Category | Item | Status | Note |
|----------|------|--------|------|
| verification | 18-VERIFICATION.md | human_needed | iOS/device-only audio-fade + ended-handler checks; cannot run in CI/desktop |
| uat_gap | 18-HUMAN-UAT.md | partial (5 open) | iOS instant-pause vs Android/desktop fade; gesture-abort mid-fade; repeat-one boundary; manual-pause-past-deadline |
| uat_gap | 20-HUMAN-UAT.md | partial (13 open) | now-playing cover-swipe / sheet-scroll / nowbar-swipe gestures — real-device touch only |
| uat_gap | 999.1-04-HUMAN-UAT.md | partial (6 open) | Capacitor background audio + media-session — Android device only (v2.0 backlog) |
| uat_gap | 999.1-05-HUMAN-UAT.md | partial (4 open) | signed-APK release pipeline + sideload — needs keystore + device (v2.0 backlog) |
| uat_gap | 999.1-06-HUMAN-UAT.md | partial (5 open) | native install/storage smokes — Android device only (v2.0 backlog) |
| quick_task | 75 tasks (260605→260616) | missing | quick-task status markers unresolved by scanner; most shipped via `quick-*` commits, unconfirmed at close — triage with `/gsd:check-todos` / `/gsd:cleanup` |

The 🔴 verification/UAT gaps are device-only human tests (iOS audio, Android APK) that cannot be reproduced in this environment. The 75 quick-tasks are predominantly false-positives (completed work whose completion marker the scanner could not match).

## Operator Next Steps

- Start the next milestone with /gsd-new-milestone
